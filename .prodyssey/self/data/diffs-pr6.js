window.DIFFS_BY_PR = window.DIFFS_BY_PR || {};
window.DIFFS_BY_PR[6] = {
  ".claude-plugin/marketplace.json": "diff --git a/.claude-plugin/marketplace.json b/.claude-plugin/marketplace.json\nindex 7bdc70e..3484a0a 100644\n--- a/.claude-plugin/marketplace.json\n+++ b/.claude-plugin/marketplace.json\n@@ -2,6 +2,6 @@\n   \"name\": \"prodyssey\",\n   \"owner\": { \"name\": \"bjornslib\", \"url\": \"https://github.com/bjornslib\" },\n   \"plugins\": [\n-    { \"name\": \"prodyssey\", \"source\": \".\", \"description\": \"Codebase Odyssey generator plugin\", \"version\": \"0.1.0\" }\n+    { \"name\": \"prodyssey\", \"source\": \".\", \"description\": \"Codebase Odyssey generator plugin\", \"version\": \"0.3.0\" }\n   ]\n }",
  ".claude-plugin/plugin.json": "diff --git a/.claude-plugin/plugin.json b/.claude-plugin/plugin.json\nindex a46b685..50b7a8a 100644\n--- a/.claude-plugin/plugin.json\n+++ b/.claude-plugin/plugin.json\n@@ -1,9 +1,9 @@\n {\n   \"name\": \"prodyssey\",\n-  \"version\": \"0.2.0\",\n-  \"description\": \"Codebase Odyssey generator: turn merged PRs of any locally checked-out repo into four-level narrated stories with scene art, voice narration, and extracted architecture decision records. Output is a portable .prodyssey/ bundle with a built-in viewer.\",\n+  \"version\": \"0.3.0\",\n+  \"description\": \"Codebase Odyssey generator: turn merged PRs of any locally checked-out repo into four-level narrated stories with scene art, voice narration, and extracted architecture decision records. Also interviews a change's author before the PR opens and assesses the change against the bundle. Output is a portable .prodyssey/ bundle with a built-in viewer.\",\n   \"author\": { \"name\": \"bjornslib\" },\n   \"repository\": \"https://github.com/bjornslib/prodyssey\",\n   \"license\": \"MIT\",\n-  \"keywords\": [\"claude-code\", \"storytelling\", \"architecture\", \"adr\", \"pr-review\", \"codebase-odyssey\"]\n+  \"keywords\": [\"claude-code\", \"storytelling\", \"architecture\", \"adr\", \"pr-review\", \"code-review\", \"codebase-odyssey\"]\n }",
  ".gitignore": "diff --git a/.gitignore b/.gitignore\nindex 2424499..178ee9c 100644\n--- a/.gitignore\n+++ b/.gitignore\n@@ -11,6 +11,11 @@ __pycache__/\n # migrate_bundle.py's pre-migration story.json snapshot — disposable once a\n # migration proves sound, and machine-local like the entries above.\n .prodyssey/*/.migration-backup/\n+# submit mode's pre-PR diff cache. Exactly reproducible from\n+# `git diff <merge-base>..<head>`, and self-referential: committing it into the\n+# branch it diffs rewrites it on every commit, and each version would then\n+# contain the last one. The authored intent/assessment beside it ARE committed.\n+.prodyssey/*/exports/branch-*/diff.json\n \n .mcp.json\n .serena/",
  ".prodyssey/cobuilder-harness-a103a550/bundle.json": "diff --git a/.prodyssey/cobuilder-harness-a103a550/bundle.json b/.prodyssey/cobuilder-harness-a103a550/bundle.json\nindex f73cffb..a82dafc 100644\n--- a/.prodyssey/cobuilder-harness-a103a550/bundle.json\n+++ b/.prodyssey/cobuilder-harness-a103a550/bundle.json\n@@ -1,6 +1,6 @@\n {\n   \"bundle_format\": 1,\n-  \"schema_version\": \"1.1\",\n-  \"generator_version\": \"0.2.0\",\n-  \"migrated_at\": \"2026-08-02\"\n+  \"schema_version\": \"1.2\",\n+  \"generator_version\": \"0.3.0\",\n+  \"migrated_at\": \"2026-08-04\"\n }",
  ".prodyssey/cobuilder-harness-a103a550/data/story.js": "diff --git a/.prodyssey/cobuilder-harness-a103a550/data/story.js b/.prodyssey/cobuilder-harness-a103a550/data/story.js\nindex 85fd8ef..f019149 100644\n--- a/.prodyssey/cobuilder-harness-a103a550/data/story.js\n+++ b/.prodyssey/cobuilder-harness-a103a550/data/story.js\n@@ -2,7 +2,7 @@ window.STORY = {\n   \"meta\": {\n     \"repo\": \"cobuilder-harness\",\n     \"generated\": \"2026-07-21\",\n-    \"schema_version\": \"1.1\",\n+    \"schema_version\": \"1.2\",\n     \"title\": \"cobuilder-harness — Codebase Odyssey\",\n     \"description\": \"CoBuilder: a pipeline execution engine that turns product requirements into working software through autonomous multi-agent workflows. The runner has zero LLM intelligence — it reads a DOT graph, dispatches project-owned agents, and transitions node states mechanically.\",\n     \"levels\": [",
  ".prodyssey/cobuilder-harness-a103a550/data/story.json": "diff --git a/.prodyssey/cobuilder-harness-a103a550/data/story.json b/.prodyssey/cobuilder-harness-a103a550/data/story.json\nindex ad57b1a..0724964 100644\n--- a/.prodyssey/cobuilder-harness-a103a550/data/story.json\n+++ b/.prodyssey/cobuilder-harness-a103a550/data/story.json\n@@ -2,7 +2,7 @@\n   \"meta\": {\n     \"repo\": \"cobuilder-harness\",\n     \"generated\": \"2026-07-21\",\n-    \"schema_version\": \"1.1\",\n+    \"schema_version\": \"1.2\",\n     \"title\": \"cobuilder-harness — Codebase Odyssey\",\n     \"description\": \"CoBuilder: a pipeline execution engine that turns product requirements into working software through autonomous multi-agent workflows. The runner has zero LLM intelligence — it reads a DOT graph, dispatches project-owned agents, and transitions node states mechanically.\",\n     \"levels\": [",
  ".prodyssey/cobuilder-harness-a103a550/viewer/index.html": "diff --git a/.prodyssey/cobuilder-harness-a103a550/viewer/index.html b/.prodyssey/cobuilder-harness-a103a550/viewer/index.html\nindex b10e8aa..14a7fca 100644\n--- a/.prodyssey/cobuilder-harness-a103a550/viewer/index.html\n+++ b/.prodyssey/cobuilder-harness-a103a550/viewer/index.html\n@@ -292,6 +292,9 @@\n \n   .alt-section-label{font:10.5px 'JetBrains Mono',monospace;letter-spacing:.1em;color:var(--ink-dim);text-transform:uppercase;margin-bottom:8px;}\n   .alt-row{display:flex;gap:12px;flex-wrap:wrap;}\n+  /* A `display:` rule beats the plain `hidden` attribute, so the ADR sheet's\n+     \"+N more\" disclosure needs this to stay hidden until the button is clicked. */\n+  .alt-row[hidden]{display:none;}\n   .alt-card{width:240px;background:#fff;border:1px dashed oklch(0.6 0.13 25);border-radius:8px;padding:10px 12px;}\n   .alt-card .alt-opt{font-size:12px;font-weight:600;color:oklch(0.3 0.02 250);}\n   .alt-card .alt-rej{font-size:11px;color:oklch(0.45 0.14 25);margin-top:5px;line-height:1.4;}\n@@ -304,6 +307,16 @@\n   .adr-badge:hover{background:oklch(0.94 0.04 60);border-color:oklch(0.48 0.13 60);}\n   .no-adrs{font:11.5px system-ui,sans-serif;color:var(--ink-dim);}\n \n+  /* Submit-mode assessment badge: same row as the ADR badges, deliberately a\n+     different hue so a verdict never reads as an ADR id. */\n+  .assessment-badge{\n+    font:10.5px 'JetBrains Mono',monospace;padding:4px 10px;border-radius:3px;cursor:pointer;\n+    border:1px solid oklch(0.55 0.12 265);color:oklch(0.44 0.12 265);background:oklch(0.97 0.02 265);\n+    display:inline-flex;align-items:center;gap:6px;\n+  }\n+  .assessment-badge:hover{background:oklch(0.94 0.04 265);border-color:oklch(0.48 0.13 265);}\n+  .assessment-badge .ab-dot{width:6px;height:6px;border-radius:50%;background:currentColor;}\n+\n   .beats-stack{display:flex;flex-direction:column;gap:10px;}\n   .beat-card.fixed-card{height:180px;}\n   .beat-card .fc-kicker{color:oklch(0.5 0.02 250);}\n@@ -453,18 +466,42 @@\n   /* ================= ADR SHEET (same shadcn-Sheet family as the comments drawer,\n      just wider — reuses .drawer-header/.drawer-close/.drawer-body). Shares\n      #drawer-scrim with the comments drawer; JS enforces only one open at a time. ================= */\n-  #adr-sheet{\n+  #adr-sheet,\n+  #assessment-sheet{\n     position:fixed;top:0;right:0;bottom:0;width:clamp(480px, 40vw, 720px);max-width:92vw;background:#fff;\n     border-left:1px solid var(--line);border-radius:14px 0 0 14px;box-shadow:-12px 0 34px rgba(20,25,35,0.2);\n     transform:translateX(100%);transition:transform .25s ease;z-index:41;\n     display:flex;flex-direction:column;min-height:0;\n   }\n-  #adr-sheet.open{transform:translateX(0);}\n-  #adr-sheet .drawer-header{align-items:flex-start;}\n+  #adr-sheet.open,\n+  #assessment-sheet.open{transform:translateX(0);}\n+  #adr-sheet .drawer-header,\n+  #assessment-sheet .drawer-header{align-items:flex-start;}\n   .adr-sheet-meta{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:6px;}\n   .adr-state-badge{font:10px 'JetBrains Mono',monospace;letter-spacing:.04em;padding:3px 9px;border-radius:10px;border:1px solid;text-transform:uppercase;white-space:nowrap;}\n   .adr-sheet-sourceline{font:11px 'JetBrains Mono',monospace;color:var(--ink-dim);}\n-  #adr-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+  #adr-sheet .drawer-body,\n+  #assessment-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+\n+  /* Assessment-only blocks. Everything else in the sheet reuses .adr-block,\n+     .ab-kicker, .force-chip, .alt-section-label, and .adr-body-md. */\n+  .asm-finding{border-radius:8px;border:1px solid var(--line);background:#fff;padding:10px 12px;border-left:3px solid var(--line);}\n+  .asm-finding.blocker{border-left-color:oklch(0.55 0.19 25);}\n+  .asm-finding.concern{border-left-color:oklch(0.66 0.15 70);}\n+  .asm-finding.note{border-left-color:oklch(0.7 0.05 250);}\n+  .asm-finding .asm-sev{font:700 9.5px 'JetBrains Mono',monospace;letter-spacing:.1em;text-transform:uppercase;color:var(--ink-dim);}\n+  .asm-finding.blocker .asm-sev{color:oklch(0.5 0.18 25);}\n+  .asm-finding.concern .asm-sev{color:oklch(0.5 0.14 70);}\n+  .asm-finding .asm-claim{font-size:12.5px;line-height:1.5;color:oklch(0.28 0.02 250);margin-top:5px;}\n+  .asm-finding .asm-ev{font:10.5px 'JetBrains Mono',monospace;color:var(--ink-dim);margin-top:6px;word-break:break-all;}\n+  .asm-finding .asm-fix{font-size:12px;line-height:1.5;color:oklch(0.4 0.02 250);margin-top:6px;}\n+  .asm-stack{display:flex;flex-direction:column;gap:8px;}\n+  .asm-stack[hidden]{display:none;}\n+  .asm-check{display:flex;gap:8px;align-items:baseline;font-size:12px;line-height:1.5;color:oklch(0.3 0.02 250);}\n+  .asm-check .asm-res{font:700 9.5px 'JetBrains Mono',monospace;letter-spacing:.06em;text-transform:uppercase;white-space:nowrap;}\n+  .asm-check.pass .asm-res{color:oklch(0.45 0.13 145);}\n+  .asm-check.violation .asm-res{color:oklch(0.5 0.18 25);}\n+  .asm-delta{font:11.5px 'JetBrains Mono',monospace;color:oklch(0.3 0.02 250);line-height:1.7;}\n \n   .adr-block{border-radius:10px;border:1px solid var(--line);background:#fff;padding:14px 16px;}\n   .adr-block.problem{border-left:3px solid oklch(0.6 0.16 25);}\n@@ -636,6 +673,22 @@\n     <div class=\"drawer-body\" id=\"adr-sheet-body\"></div>\n   </div>\n \n+  <!-- Submit-mode assessment sheet (same Sheet family as the ADR sheet above,\n+       reachable from the Assessment badge on level 3). Reads the timeline\n+       entry's own `assessment`/`intent` blocks — there is no window.ASSESSMENTS,\n+       because both live inline in story.json. Shares #drawer-scrim; only one\n+       sheet is ever open. -->\n+  <div id=\"assessment-sheet\" role=\"dialog\" aria-label=\"Architecture assessment\">\n+    <div class=\"drawer-header\">\n+      <div>\n+        <h2 id=\"assessment-sheet-title\">Assessment</h2>\n+        <div class=\"adr-sheet-meta\" id=\"assessment-sheet-meta\"></div>\n+      </div>\n+      <button class=\"drawer-close\" id=\"assessment-sheet-close-btn\" title=\"Close (Esc)\">&times;</button>\n+    </div>\n+    <div class=\"drawer-body\" id=\"assessment-sheet-body\"></div>\n+  </div>\n+\n   <!-- Narration audio dialog (shadcn Dialog look, centered). Shares #drawer-scrim above;\n        mutually exclusive with the comments drawer / ADR sheet — see openAudioDialog. -->\n   <div id=\"audio-dialog-layer\">\n@@ -749,7 +802,9 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     // ADR sheet: mutually exclusive with the comments drawer (see openCommentsDrawer/openAdrSheet).\n     adrOpen: false,\n     activeAdrId: null,\n-    // Audio dialog: mutually exclusive with both sheets above (see openAudioDialog).\n+    // Submit-mode assessment sheet: mutually exclusive with both panels above.\n+    assessmentOpen: false,\n+    // Audio dialog: mutually exclusive with every sheet above (see openAudioDialog).\n     audioDialogOpen: false,\n     // true while a level/PR content transition animation is in flight — guards re-entrant\n     // nav input (arrow mashing, double rail clicks) from stacking transitions.\n@@ -1620,6 +1675,15 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n         ? adrs.map(a => `<button class=\"adr-badge\" data-adr-id=\"${escapeHtml(a)}\">${escapeHtml(a)}</button>`).join('')\n         : `<span class=\"no-adrs\">No ADRs recorded for this PR.</span>`;\n \n+      // Assessment badge, only when submit mode actually left one behind. A PR\n+      // generated before submit mode existed simply shows no badge.\n+      const asm = p.assessment;\n+      const assessmentHtml = (asm && asm.verdict)\n+        ? `<button class=\"assessment-badge\" id=\"assessment-badge\" title=\"Architecture assessment (submit mode)\">\n+             <span class=\"ab-dot\"></span>Assessment: ${escapeHtml(asm.verdict)}\n+           </button>`\n+        : '';\n+\n       const boxesHtml = DISTRICTS.map(d => {\n         const isTouched = !!touched[d.id];\n         const color = KIND_COLOR[d.kind] || KIND_COLOR.tooling;\n@@ -1640,13 +1704,15 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n       setMainHtml(main, `\n         <div class=\"level-stack narrow\">\n           ${heroFrame(p.pr, 3, `PR #${p.pr} architecture diagram`)}\n-          <div class=\"adr-row\">${adrHtml}</div>\n+          <div class=\"adr-row\">${adrHtml}${assessmentHtml}</div>\n           <div class=\"district-boxes\">${boxesHtml}</div>\n           ${beatsHtml}\n         </div>`);\n       main.querySelectorAll('.adr-badge[data-adr-id]').forEach(btn => {\n         btn.addEventListener('click', () => openAdrSheet(btn.dataset.adrId));\n       });\n+      const asmBtn = main.querySelector('#assessment-badge');\n+      if (asmBtn) asmBtn.addEventListener('click', openAssessmentSheet);\n \n     } else {\n       const fc = content.file_changes;\n@@ -1887,6 +1953,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   function openAudioDialog(){\n     if (state.commentsOpen) state.commentsOpen = false;\n     if (state.adrOpen) state.adrOpen = false;\n+    if (state.assessmentOpen) state.assessmentOpen = false;\n     state.audioDialogOpen = true;\n     renderAudioDialogContent();\n     const dialog = document.getElementById('audio-dialog');\n@@ -2117,6 +2184,10 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n       state.level = 1;\n       state.focusDistrict = null;\n       state.draftMergeMethod = null;\n+      // The assessment sheet renders the CURRENT pr(), so leaving it open across\n+      // a PR switch would show the new PR's data under the old PR's scroll\n+      // position — and level resets to 1, where its badge is not even visible.\n+      if (state.assessmentOpen) closeAssessmentSheet();\n       closeMergeMenu();\n       stopAudio();\n       renderAll();\n@@ -2148,19 +2219,22 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   }\n \n   /* ============================================================\n-     COMMENTS DRAWER + ADR SHEET (shadcn Sheet look, recreated in vanilla JS/CSS)\n-     Both panels share one #drawer-scrim; opening either one closes the other first, so\n-     at most one sheet is ever open — this is the \"only one sheet at a time\" rule.\n+     COMMENTS DRAWER + ADR SHEET + ASSESSMENT SHEET (shadcn Sheet look, recreated\n+     in vanilla JS/CSS). All three panels share one #drawer-scrim; opening any one\n+     closes the others first, so at most one sheet is ever open — this is the\n+     \"only one sheet at a time\" rule.\n      ============================================================ */\n   function renderSheetVisibility(){\n     document.getElementById('comments-drawer').classList.toggle('open', state.commentsOpen);\n     document.getElementById('adr-sheet').classList.toggle('open', state.adrOpen);\n+    document.getElementById('assessment-sheet').classList.toggle('open', state.assessmentOpen);\n     const scrim = document.getElementById('drawer-scrim');\n-    scrim.classList.toggle('open', state.commentsOpen || state.adrOpen || state.audioDialogOpen);\n+    scrim.classList.toggle('open', state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen);\n     scrim.classList.toggle('audio-dim', state.audioDialogOpen);\n   }\n   function openCommentsDrawer(){\n     if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.commentsOpen = true;\n     renderSheetVisibility();\n@@ -2172,6 +2246,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   }\n   function openAdrSheet(id){\n     if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.adrOpen = true;\n     state.activeAdrId = id;\n@@ -2182,9 +2257,22 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     state.adrOpen = false;\n     renderSheetVisibility();\n   }\n+  function openAssessmentSheet(){\n+    if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n+    state.assessmentOpen = true;\n+    renderAssessmentSheet();\n+    renderSheetVisibility();\n+  }\n+  function closeAssessmentSheet(){\n+    state.assessmentOpen = false;\n+    renderSheetVisibility();\n+  }\n   function closeAnyOpenSheet(){\n     if (state.commentsOpen) closeCommentsDrawer();\n     else if (state.adrOpen) closeAdrSheet();\n+    else if (state.assessmentOpen) closeAssessmentSheet();\n     else if (state.audioDialogOpen) stopAudio();\n   }\n \n@@ -2352,6 +2440,201 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     }\n   }\n \n+  /* ---------- Submit-mode assessment sheet ----------\n+     Reads pr().assessment / pr().intent straight off window.STORY. No new\n+     global and no new data file: both blocks live inline on the timeline\n+     entry, which is what gives them migrate_bundle.py's authored-field guard.\n+     Verdict/tier vocabulary: skills/odyssey/references/review-mode.md §8, §4. */\n+  const VERDICT_COLOR = {\n+    sound:    ['oklch(0.94 0.06 145)', 'oklch(0.4 0.13 145)', 'oklch(0.6 0.12 145)'],\n+    concerns: ['oklch(0.95 0.06 70)',  'oklch(0.44 0.13 70)', 'oklch(0.65 0.13 70)'],\n+    rework:   ['oklch(0.95 0.05 25)',  'oklch(0.45 0.17 25)', 'oklch(0.62 0.16 25)'],\n+  };\n+  const PATTERN_LABEL = {\n+    conforms: 'Conforms to an existing pattern',\n+    'new-valuable': 'New pattern, and it earns its place',\n+    duplicate: 'Duplicates something the repo already has',\n+    reinvention: 'Reinvents a solved problem',\n+  };\n+  const DRIFT_LABEL = {\n+    out_of_scope: 'Out of scope, but touched',\n+    unaddressed_risk: 'Risk with no guard',\n+    adopted_alternative: 'Rejected option adopted',\n+    delta_shift: 'District delta shifted',\n+  };\n+\n+  function verdictBadgeHtml(v){\n+    const [bg, fg, border] = VERDICT_COLOR[v] || ['oklch(0.96 0.02 250)', 'var(--ink-dim)', 'var(--line)'];\n+    return `<span class=\"adr-state-badge\" style=\"background:${bg};color:${fg};border-color:${border};\">${escapeHtml((v || 'unknown').toUpperCase())}</span>`;\n+  }\n+\n+  function asmQuestionHtml(kicker, block, extraLabel, extraKey){\n+    if (!block || !block.answer) return '';\n+    const ev = (block.evidence || []).filter(Boolean);\n+    return `\n+      <div class=\"adr-block\">\n+        <div class=\"ab-kicker\" style=\"color:var(--accent-text);\">${escapeHtml(kicker)}</div>\n+        <p>${escapeHtml(block.answer)}</p>\n+        ${extraKey && block[extraKey] ? `<p style=\"margin-top:8px;\"><strong>${escapeHtml(extraLabel)}:</strong> ${escapeHtml(block[extraKey])}</p>` : ''}\n+        ${ev.length ? `<div class=\"chip-row\" style=\"margin-top:10px;\">${ev.map(e => `<span class=\"force-chip\">${escapeHtml(e)}</span>`).join('')}</div>` : ''}\n+      </div>`;\n+  }\n+\n+  function renderAssessmentSheet(){\n+    const p = pr();\n+    const a = p && p.assessment;\n+    const intent = (p && p.intent) || null;\n+    const titleEl = document.getElementById('assessment-sheet-title');\n+    const metaEl = document.getElementById('assessment-sheet-meta');\n+    const body = document.getElementById('assessment-sheet-body');\n+\n+    if (!a){\n+      titleEl.textContent = 'Assessment';\n+      metaEl.innerHTML = '';\n+      body.innerHTML = `\n+        <div class=\"empty-card\">\n+          <div class=\"empty-detail\">No assessment recorded for this PR. Run <code>/prodyssey:submit</code> against it.</div>\n+        </div>`;\n+      return;\n+    }\n+\n+    titleEl.textContent = `PR #${p.pr} — assessment`;\n+    const metaBits = [\n+      a.risk_tier ? `risk: ${a.risk_tier}` : '',\n+      a.stage === 'post' ? 'post-merge' : 'pre-merge',\n+      a.generated ? a.generated : '',\n+    ].filter(Boolean).join(' · ');\n+    metaEl.innerHTML = verdictBadgeHtml(a.verdict)\n+      + (metaBits ? `<span class=\"adr-sheet-sourceline\">${escapeHtml(metaBits)}</span>` : '');\n+\n+    const summaryHtml = a.summary ? `\n+      <div class=\"adr-block decision\">\n+        <div class=\"ab-kicker\">Summary</div>\n+        <p>${escapeHtml(a.summary)}</p>\n+      </div>` : '';\n+\n+    // The author's own words come first — the assessment is a response to them.\n+    let intentHtml = '';\n+    if (intent && intent.problem){\n+      const flags = [\n+        intent.source === 'inferred' ? 'intent inferred, not stated by the author' : '',\n+        intent.authorship && intent.authorship !== 'human' ? `authorship: ${intent.authorship}` : '',\n+        (intent.unknowns || []).length ? `${intent.unknowns.length} author-flagged unknown(s)` : '',\n+      ].filter(Boolean);\n+      intentHtml = `\n+        <div class=\"adr-block problem\">\n+          <div class=\"ab-kicker\">The author's stated problem</div>\n+          <p>${escapeHtml(intent.problem)}</p>\n+          ${flags.length ? `<div class=\"chip-row\" style=\"margin-top:10px;\">${flags.map(f => `<span class=\"force-chip\">${escapeHtml(f)}</span>`).join('')}</div>` : ''}\n+        </div>`;\n+    }\n+\n+    let patternHtml = '';\n+    if (a.pattern && a.pattern.answer){\n+      const label = PATTERN_LABEL[a.pattern.verdict] || a.pattern.verdict || '';\n+      const dupes = (a.pattern.duplicates || []).filter(Boolean);\n+      patternHtml = asmQuestionHtml('Q3 — pattern, duplicate, or reinvention', a.pattern);\n+      patternHtml = patternHtml.replace(\n+        '<p>',\n+        `${label ? `<p style=\"margin-bottom:8px;\"><strong>${escapeHtml(label)}</strong></p>` : ''}<p>`\n+      );\n+      if (dupes.length){\n+        patternHtml = patternHtml.replace(\n+          '</div>\\n      </div>',\n+          `</div>\\n        <div class=\"asm-delta\" style=\"margin-top:8px;\">duplicates: ${dupes.map(d => escapeHtml(d)).join(', ')}</div>\\n      </div>`\n+        );\n+      }\n+    }\n+\n+    const findings = a.findings || [];\n+    const order = { blocker: 0, concern: 1, note: 2 };\n+    const sorted = findings.slice().sort((x, y) => (order[x.severity] ?? 3) - (order[y.severity] ?? 3));\n+    const findingCard = (f) => `\n+      <div class=\"asm-finding ${escapeHtml(f.severity || 'note')}\">\n+        <div class=\"asm-sev\">${escapeHtml(f.severity || 'note')}${f.district ? ` · ${escapeHtml(f.district)}` : ''}</div>\n+        <div class=\"asm-claim\">${escapeHtml(f.claim || '')}</div>\n+        ${f.evidence ? `<div class=\"asm-ev\">${escapeHtml(f.evidence)}</div>` : ''}\n+        ${f.suggestion ? `<div class=\"asm-fix\">${escapeHtml(f.suggestion)}</div>` : ''}\n+      </div>`;\n+    let findingsHtml = '';\n+    if (sorted.length){\n+      // Same +N more disclosure the ADR sheet uses for rejected alternatives.\n+      const visible = sorted.slice(0, 3);\n+      const hidden = sorted.slice(3);\n+      findingsHtml = `\n+        <div>\n+          <div class=\"alt-section-label\">Findings</div>\n+          <div class=\"asm-stack\">${visible.map(findingCard).join('')}</div>\n+          ${hidden.length ? `\n+            <button class=\"link-btn\" id=\"asm-findings-more-btn\" style=\"margin-top:8px;\">+${hidden.length} more</button>\n+            <div class=\"asm-stack\" id=\"asm-findings-hidden\" hidden style=\"margin-top:8px;\">${hidden.map(findingCard).join('')}</div>\n+          ` : ''}\n+        </div>`;\n+    }\n+\n+    const checks = a.boundary_checks || [];\n+    const checksHtml = checks.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">Boundary checks</div>\n+        <div class=\"asm-stack\">${checks.map(c => `\n+          <div class=\"asm-check ${escapeHtml(c.result || '')}\">\n+            <span class=\"asm-res\">${escapeHtml(c.result || '?')}</span>\n+            <span>${escapeHtml(c.rule || '')}${c.source ? ` <span class=\"adr-sheet-sourceline\">(${escapeHtml(c.source)})</span>` : ''}</span>\n+          </div>`).join('')}</div>\n+      </div>` : '';\n+\n+    const d = a.delta || {};\n+    const deltaLines = []\n+      .concat((d.districts_added || []).map(x => `+ district ${x}`))\n+      .concat((d.districts_changed || []).map(x => `~ ${x.id}: ${x.files_before} → ${x.files_after} files`))\n+      .concat((d.edges_added || []).map(x => `+ edge ${x}`))\n+      .concat((d.edges_removed || []).map(x => `- edge ${x}`));\n+    const deltaHtml = deltaLines.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">District delta</div>\n+        <div class=\"asm-delta\">${deltaLines.map(l => escapeHtml(l)).join('<br>')}</div>\n+      </div>` : '';\n+\n+    const drift = a.drift || [];\n+    const driftHtml = drift.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">Drift from the stated intent</div>\n+        <div class=\"asm-stack\">${drift.map(x => `\n+          <div class=\"asm-finding concern\">\n+            <div class=\"asm-sev\">${escapeHtml(DRIFT_LABEL[x.kind] || x.kind || '')}</div>\n+            <div class=\"asm-claim\">${escapeHtml(x.claim || '')}</div>\n+            ${x.evidence ? `<div class=\"asm-ev\">${escapeHtml(x.evidence)}</div>` : ''}\n+          </div>`).join('')}</div>\n+      </div>` : '';\n+\n+    const regretHtml = a.regret_risk ? `\n+      <div class=\"adr-block\">\n+        <div class=\"ab-kicker\" style=\"color:oklch(0.5 0.15 25);\">Will we regret this?</div>\n+        <p>${escapeHtml(a.regret_risk)}</p>\n+      </div>` : '';\n+\n+    body.innerHTML = `\n+      ${summaryHtml}\n+      ${intentHtml}\n+      ${asmQuestionHtml('Q1 — is this sensible?', a.sensible)}\n+      ${asmQuestionHtml('Q2 — maintainability and readability', a.maintainability, 'Constraint introduced', 'constraint_introduced')}\n+      ${patternHtml}\n+      ${regretHtml}\n+      ${findingsHtml}\n+      ${checksHtml}\n+      ${deltaHtml}\n+      ${driftHtml}\n+    `;\n+\n+    const moreBtn = document.getElementById('asm-findings-more-btn');\n+    if (moreBtn){\n+      moreBtn.addEventListener('click', () => {\n+        document.getElementById('asm-findings-hidden').hidden = false;\n+        moreBtn.hidden = true;\n+      });\n+    }\n+  }\n+\n   document.getElementById('tb-prev').addEventListener('click', () => selectPR(state.prIndex - 1));\n   document.getElementById('tb-next').addEventListener('click', () => selectPR(state.prIndex + 1));\n \n@@ -2371,6 +2654,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   document.getElementById('btn-comments-toggle').addEventListener('click', openCommentsDrawer);\n   document.getElementById('drawer-close-btn').addEventListener('click', closeCommentsDrawer);\n   document.getElementById('adr-sheet-close-btn').addEventListener('click', closeAdrSheet);\n+  document.getElementById('assessment-sheet-close-btn').addEventListener('click', closeAssessmentSheet);\n   document.getElementById('drawer-scrim').addEventListener('click', closeAnyOpenSheet);\n   document.getElementById('btn-add-comment').addEventListener('click', addCommentOnly);\n   commentInput.addEventListener('keydown', (e) => { if (e.key === 'Enter'){ e.preventDefault(); addCommentOnly(); } });\n@@ -2393,7 +2677,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   window.addEventListener('keydown', (e) => {\n     // Escape must close whichever sheet is open, even while the comment input is\n     // focused — checked before the input/textarea early-return below so it always works.\n-    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n+    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n \n     const tag = document.activeElement && document.activeElement.tagName;\n     if (tag === 'TEXTAREA' || tag === 'INPUT') return;",
  ".prodyssey/digital-curator-80f83abb/bundle.json": "diff --git a/.prodyssey/digital-curator-80f83abb/bundle.json b/.prodyssey/digital-curator-80f83abb/bundle.json\nindex f73cffb..a82dafc 100644\n--- a/.prodyssey/digital-curator-80f83abb/bundle.json\n+++ b/.prodyssey/digital-curator-80f83abb/bundle.json\n@@ -1,6 +1,6 @@\n {\n   \"bundle_format\": 1,\n-  \"schema_version\": \"1.1\",\n-  \"generator_version\": \"0.2.0\",\n-  \"migrated_at\": \"2026-08-02\"\n+  \"schema_version\": \"1.2\",\n+  \"generator_version\": \"0.3.0\",\n+  \"migrated_at\": \"2026-08-04\"\n }",
  ".prodyssey/digital-curator-80f83abb/data/story.js": "diff --git a/.prodyssey/digital-curator-80f83abb/data/story.js b/.prodyssey/digital-curator-80f83abb/data/story.js\nindex e489ea7..e0faa2e 100644\n--- a/.prodyssey/digital-curator-80f83abb/data/story.js\n+++ b/.prodyssey/digital-curator-80f83abb/data/story.js\n@@ -2,7 +2,7 @@ window.STORY = {\n   \"meta\": {\n     \"repo\": \"digital-curator\",\n     \"generated\": \"2026-07-21\",\n-    \"schema_version\": \"1.1\",\n+    \"schema_version\": \"1.2\",\n     \"title\": \"digital-curator — Codebase Odyssey\",\n     \"description\": \"\",\n     \"levels\": [",
  ".prodyssey/digital-curator-80f83abb/data/story.json": "diff --git a/.prodyssey/digital-curator-80f83abb/data/story.json b/.prodyssey/digital-curator-80f83abb/data/story.json\nindex a1fb90e..ce0d901 100644\n--- a/.prodyssey/digital-curator-80f83abb/data/story.json\n+++ b/.prodyssey/digital-curator-80f83abb/data/story.json\n@@ -2,7 +2,7 @@\n   \"meta\": {\n     \"repo\": \"digital-curator\",\n     \"generated\": \"2026-07-21\",\n-    \"schema_version\": \"1.1\",\n+    \"schema_version\": \"1.2\",\n     \"title\": \"digital-curator — Codebase Odyssey\",\n     \"description\": \"\",\n     \"levels\": [",
  ".prodyssey/digital-curator-80f83abb/viewer/index.html": "diff --git a/.prodyssey/digital-curator-80f83abb/viewer/index.html b/.prodyssey/digital-curator-80f83abb/viewer/index.html\nindex b10e8aa..14a7fca 100644\n--- a/.prodyssey/digital-curator-80f83abb/viewer/index.html\n+++ b/.prodyssey/digital-curator-80f83abb/viewer/index.html\n@@ -292,6 +292,9 @@\n \n   .alt-section-label{font:10.5px 'JetBrains Mono',monospace;letter-spacing:.1em;color:var(--ink-dim);text-transform:uppercase;margin-bottom:8px;}\n   .alt-row{display:flex;gap:12px;flex-wrap:wrap;}\n+  /* A `display:` rule beats the plain `hidden` attribute, so the ADR sheet's\n+     \"+N more\" disclosure needs this to stay hidden until the button is clicked. */\n+  .alt-row[hidden]{display:none;}\n   .alt-card{width:240px;background:#fff;border:1px dashed oklch(0.6 0.13 25);border-radius:8px;padding:10px 12px;}\n   .alt-card .alt-opt{font-size:12px;font-weight:600;color:oklch(0.3 0.02 250);}\n   .alt-card .alt-rej{font-size:11px;color:oklch(0.45 0.14 25);margin-top:5px;line-height:1.4;}\n@@ -304,6 +307,16 @@\n   .adr-badge:hover{background:oklch(0.94 0.04 60);border-color:oklch(0.48 0.13 60);}\n   .no-adrs{font:11.5px system-ui,sans-serif;color:var(--ink-dim);}\n \n+  /* Submit-mode assessment badge: same row as the ADR badges, deliberately a\n+     different hue so a verdict never reads as an ADR id. */\n+  .assessment-badge{\n+    font:10.5px 'JetBrains Mono',monospace;padding:4px 10px;border-radius:3px;cursor:pointer;\n+    border:1px solid oklch(0.55 0.12 265);color:oklch(0.44 0.12 265);background:oklch(0.97 0.02 265);\n+    display:inline-flex;align-items:center;gap:6px;\n+  }\n+  .assessment-badge:hover{background:oklch(0.94 0.04 265);border-color:oklch(0.48 0.13 265);}\n+  .assessment-badge .ab-dot{width:6px;height:6px;border-radius:50%;background:currentColor;}\n+\n   .beats-stack{display:flex;flex-direction:column;gap:10px;}\n   .beat-card.fixed-card{height:180px;}\n   .beat-card .fc-kicker{color:oklch(0.5 0.02 250);}\n@@ -453,18 +466,42 @@\n   /* ================= ADR SHEET (same shadcn-Sheet family as the comments drawer,\n      just wider — reuses .drawer-header/.drawer-close/.drawer-body). Shares\n      #drawer-scrim with the comments drawer; JS enforces only one open at a time. ================= */\n-  #adr-sheet{\n+  #adr-sheet,\n+  #assessment-sheet{\n     position:fixed;top:0;right:0;bottom:0;width:clamp(480px, 40vw, 720px);max-width:92vw;background:#fff;\n     border-left:1px solid var(--line);border-radius:14px 0 0 14px;box-shadow:-12px 0 34px rgba(20,25,35,0.2);\n     transform:translateX(100%);transition:transform .25s ease;z-index:41;\n     display:flex;flex-direction:column;min-height:0;\n   }\n-  #adr-sheet.open{transform:translateX(0);}\n-  #adr-sheet .drawer-header{align-items:flex-start;}\n+  #adr-sheet.open,\n+  #assessment-sheet.open{transform:translateX(0);}\n+  #adr-sheet .drawer-header,\n+  #assessment-sheet .drawer-header{align-items:flex-start;}\n   .adr-sheet-meta{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:6px;}\n   .adr-state-badge{font:10px 'JetBrains Mono',monospace;letter-spacing:.04em;padding:3px 9px;border-radius:10px;border:1px solid;text-transform:uppercase;white-space:nowrap;}\n   .adr-sheet-sourceline{font:11px 'JetBrains Mono',monospace;color:var(--ink-dim);}\n-  #adr-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+  #adr-sheet .drawer-body,\n+  #assessment-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+\n+  /* Assessment-only blocks. Everything else in the sheet reuses .adr-block,\n+     .ab-kicker, .force-chip, .alt-section-label, and .adr-body-md. */\n+  .asm-finding{border-radius:8px;border:1px solid var(--line);background:#fff;padding:10px 12px;border-left:3px solid var(--line);}\n+  .asm-finding.blocker{border-left-color:oklch(0.55 0.19 25);}\n+  .asm-finding.concern{border-left-color:oklch(0.66 0.15 70);}\n+  .asm-finding.note{border-left-color:oklch(0.7 0.05 250);}\n+  .asm-finding .asm-sev{font:700 9.5px 'JetBrains Mono',monospace;letter-spacing:.1em;text-transform:uppercase;color:var(--ink-dim);}\n+  .asm-finding.blocker .asm-sev{color:oklch(0.5 0.18 25);}\n+  .asm-finding.concern .asm-sev{color:oklch(0.5 0.14 70);}\n+  .asm-finding .asm-claim{font-size:12.5px;line-height:1.5;color:oklch(0.28 0.02 250);margin-top:5px;}\n+  .asm-finding .asm-ev{font:10.5px 'JetBrains Mono',monospace;color:var(--ink-dim);margin-top:6px;word-break:break-all;}\n+  .asm-finding .asm-fix{font-size:12px;line-height:1.5;color:oklch(0.4 0.02 250);margin-top:6px;}\n+  .asm-stack{display:flex;flex-direction:column;gap:8px;}\n+  .asm-stack[hidden]{display:none;}\n+  .asm-check{display:flex;gap:8px;align-items:baseline;font-size:12px;line-height:1.5;color:oklch(0.3 0.02 250);}\n+  .asm-check .asm-res{font:700 9.5px 'JetBrains Mono',monospace;letter-spacing:.06em;text-transform:uppercase;white-space:nowrap;}\n+  .asm-check.pass .asm-res{color:oklch(0.45 0.13 145);}\n+  .asm-check.violation .asm-res{color:oklch(0.5 0.18 25);}\n+  .asm-delta{font:11.5px 'JetBrains Mono',monospace;color:oklch(0.3 0.02 250);line-height:1.7;}\n \n   .adr-block{border-radius:10px;border:1px solid var(--line);background:#fff;padding:14px 16px;}\n   .adr-block.problem{border-left:3px solid oklch(0.6 0.16 25);}\n@@ -636,6 +673,22 @@\n     <div class=\"drawer-body\" id=\"adr-sheet-body\"></div>\n   </div>\n \n+  <!-- Submit-mode assessment sheet (same Sheet family as the ADR sheet above,\n+       reachable from the Assessment badge on level 3). Reads the timeline\n+       entry's own `assessment`/`intent` blocks — there is no window.ASSESSMENTS,\n+       because both live inline in story.json. Shares #drawer-scrim; only one\n+       sheet is ever open. -->\n+  <div id=\"assessment-sheet\" role=\"dialog\" aria-label=\"Architecture assessment\">\n+    <div class=\"drawer-header\">\n+      <div>\n+        <h2 id=\"assessment-sheet-title\">Assessment</h2>\n+        <div class=\"adr-sheet-meta\" id=\"assessment-sheet-meta\"></div>\n+      </div>\n+      <button class=\"drawer-close\" id=\"assessment-sheet-close-btn\" title=\"Close (Esc)\">&times;</button>\n+    </div>\n+    <div class=\"drawer-body\" id=\"assessment-sheet-body\"></div>\n+  </div>\n+\n   <!-- Narration audio dialog (shadcn Dialog look, centered). Shares #drawer-scrim above;\n        mutually exclusive with the comments drawer / ADR sheet — see openAudioDialog. -->\n   <div id=\"audio-dialog-layer\">\n@@ -749,7 +802,9 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     // ADR sheet: mutually exclusive with the comments drawer (see openCommentsDrawer/openAdrSheet).\n     adrOpen: false,\n     activeAdrId: null,\n-    // Audio dialog: mutually exclusive with both sheets above (see openAudioDialog).\n+    // Submit-mode assessment sheet: mutually exclusive with both panels above.\n+    assessmentOpen: false,\n+    // Audio dialog: mutually exclusive with every sheet above (see openAudioDialog).\n     audioDialogOpen: false,\n     // true while a level/PR content transition animation is in flight — guards re-entrant\n     // nav input (arrow mashing, double rail clicks) from stacking transitions.\n@@ -1620,6 +1675,15 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n         ? adrs.map(a => `<button class=\"adr-badge\" data-adr-id=\"${escapeHtml(a)}\">${escapeHtml(a)}</button>`).join('')\n         : `<span class=\"no-adrs\">No ADRs recorded for this PR.</span>`;\n \n+      // Assessment badge, only when submit mode actually left one behind. A PR\n+      // generated before submit mode existed simply shows no badge.\n+      const asm = p.assessment;\n+      const assessmentHtml = (asm && asm.verdict)\n+        ? `<button class=\"assessment-badge\" id=\"assessment-badge\" title=\"Architecture assessment (submit mode)\">\n+             <span class=\"ab-dot\"></span>Assessment: ${escapeHtml(asm.verdict)}\n+           </button>`\n+        : '';\n+\n       const boxesHtml = DISTRICTS.map(d => {\n         const isTouched = !!touched[d.id];\n         const color = KIND_COLOR[d.kind] || KIND_COLOR.tooling;\n@@ -1640,13 +1704,15 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n       setMainHtml(main, `\n         <div class=\"level-stack narrow\">\n           ${heroFrame(p.pr, 3, `PR #${p.pr} architecture diagram`)}\n-          <div class=\"adr-row\">${adrHtml}</div>\n+          <div class=\"adr-row\">${adrHtml}${assessmentHtml}</div>\n           <div class=\"district-boxes\">${boxesHtml}</div>\n           ${beatsHtml}\n         </div>`);\n       main.querySelectorAll('.adr-badge[data-adr-id]').forEach(btn => {\n         btn.addEventListener('click', () => openAdrSheet(btn.dataset.adrId));\n       });\n+      const asmBtn = main.querySelector('#assessment-badge');\n+      if (asmBtn) asmBtn.addEventListener('click', openAssessmentSheet);\n \n     } else {\n       const fc = content.file_changes;\n@@ -1887,6 +1953,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   function openAudioDialog(){\n     if (state.commentsOpen) state.commentsOpen = false;\n     if (state.adrOpen) state.adrOpen = false;\n+    if (state.assessmentOpen) state.assessmentOpen = false;\n     state.audioDialogOpen = true;\n     renderAudioDialogContent();\n     const dialog = document.getElementById('audio-dialog');\n@@ -2117,6 +2184,10 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n       state.level = 1;\n       state.focusDistrict = null;\n       state.draftMergeMethod = null;\n+      // The assessment sheet renders the CURRENT pr(), so leaving it open across\n+      // a PR switch would show the new PR's data under the old PR's scroll\n+      // position — and level resets to 1, where its badge is not even visible.\n+      if (state.assessmentOpen) closeAssessmentSheet();\n       closeMergeMenu();\n       stopAudio();\n       renderAll();\n@@ -2148,19 +2219,22 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   }\n \n   /* ============================================================\n-     COMMENTS DRAWER + ADR SHEET (shadcn Sheet look, recreated in vanilla JS/CSS)\n-     Both panels share one #drawer-scrim; opening either one closes the other first, so\n-     at most one sheet is ever open — this is the \"only one sheet at a time\" rule.\n+     COMMENTS DRAWER + ADR SHEET + ASSESSMENT SHEET (shadcn Sheet look, recreated\n+     in vanilla JS/CSS). All three panels share one #drawer-scrim; opening any one\n+     closes the others first, so at most one sheet is ever open — this is the\n+     \"only one sheet at a time\" rule.\n      ============================================================ */\n   function renderSheetVisibility(){\n     document.getElementById('comments-drawer').classList.toggle('open', state.commentsOpen);\n     document.getElementById('adr-sheet').classList.toggle('open', state.adrOpen);\n+    document.getElementById('assessment-sheet').classList.toggle('open', state.assessmentOpen);\n     const scrim = document.getElementById('drawer-scrim');\n-    scrim.classList.toggle('open', state.commentsOpen || state.adrOpen || state.audioDialogOpen);\n+    scrim.classList.toggle('open', state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen);\n     scrim.classList.toggle('audio-dim', state.audioDialogOpen);\n   }\n   function openCommentsDrawer(){\n     if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.commentsOpen = true;\n     renderSheetVisibility();\n@@ -2172,6 +2246,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   }\n   function openAdrSheet(id){\n     if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.adrOpen = true;\n     state.activeAdrId = id;\n@@ -2182,9 +2257,22 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     state.adrOpen = false;\n     renderSheetVisibility();\n   }\n+  function openAssessmentSheet(){\n+    if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n+    state.assessmentOpen = true;\n+    renderAssessmentSheet();\n+    renderSheetVisibility();\n+  }\n+  function closeAssessmentSheet(){\n+    state.assessmentOpen = false;\n+    renderSheetVisibility();\n+  }\n   function closeAnyOpenSheet(){\n     if (state.commentsOpen) closeCommentsDrawer();\n     else if (state.adrOpen) closeAdrSheet();\n+    else if (state.assessmentOpen) closeAssessmentSheet();\n     else if (state.audioDialogOpen) stopAudio();\n   }\n \n@@ -2352,6 +2440,201 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     }\n   }\n \n+  /* ---------- Submit-mode assessment sheet ----------\n+     Reads pr().assessment / pr().intent straight off window.STORY. No new\n+     global and no new data file: both blocks live inline on the timeline\n+     entry, which is what gives them migrate_bundle.py's authored-field guard.\n+     Verdict/tier vocabulary: skills/odyssey/references/review-mode.md §8, §4. */\n+  const VERDICT_COLOR = {\n+    sound:    ['oklch(0.94 0.06 145)', 'oklch(0.4 0.13 145)', 'oklch(0.6 0.12 145)'],\n+    concerns: ['oklch(0.95 0.06 70)',  'oklch(0.44 0.13 70)', 'oklch(0.65 0.13 70)'],\n+    rework:   ['oklch(0.95 0.05 25)',  'oklch(0.45 0.17 25)', 'oklch(0.62 0.16 25)'],\n+  };\n+  const PATTERN_LABEL = {\n+    conforms: 'Conforms to an existing pattern',\n+    'new-valuable': 'New pattern, and it earns its place',\n+    duplicate: 'Duplicates something the repo already has',\n+    reinvention: 'Reinvents a solved problem',\n+  };\n+  const DRIFT_LABEL = {\n+    out_of_scope: 'Out of scope, but touched',\n+    unaddressed_risk: 'Risk with no guard',\n+    adopted_alternative: 'Rejected option adopted',\n+    delta_shift: 'District delta shifted',\n+  };\n+\n+  function verdictBadgeHtml(v){\n+    const [bg, fg, border] = VERDICT_COLOR[v] || ['oklch(0.96 0.02 250)', 'var(--ink-dim)', 'var(--line)'];\n+    return `<span class=\"adr-state-badge\" style=\"background:${bg};color:${fg};border-color:${border};\">${escapeHtml((v || 'unknown').toUpperCase())}</span>`;\n+  }\n+\n+  function asmQuestionHtml(kicker, block, extraLabel, extraKey){\n+    if (!block || !block.answer) return '';\n+    const ev = (block.evidence || []).filter(Boolean);\n+    return `\n+      <div class=\"adr-block\">\n+        <div class=\"ab-kicker\" style=\"color:var(--accent-text);\">${escapeHtml(kicker)}</div>\n+        <p>${escapeHtml(block.answer)}</p>\n+        ${extraKey && block[extraKey] ? `<p style=\"margin-top:8px;\"><strong>${escapeHtml(extraLabel)}:</strong> ${escapeHtml(block[extraKey])}</p>` : ''}\n+        ${ev.length ? `<div class=\"chip-row\" style=\"margin-top:10px;\">${ev.map(e => `<span class=\"force-chip\">${escapeHtml(e)}</span>`).join('')}</div>` : ''}\n+      </div>`;\n+  }\n+\n+  function renderAssessmentSheet(){\n+    const p = pr();\n+    const a = p && p.assessment;\n+    const intent = (p && p.intent) || null;\n+    const titleEl = document.getElementById('assessment-sheet-title');\n+    const metaEl = document.getElementById('assessment-sheet-meta');\n+    const body = document.getElementById('assessment-sheet-body');\n+\n+    if (!a){\n+      titleEl.textContent = 'Assessment';\n+      metaEl.innerHTML = '';\n+      body.innerHTML = `\n+        <div class=\"empty-card\">\n+          <div class=\"empty-detail\">No assessment recorded for this PR. Run <code>/prodyssey:submit</code> against it.</div>\n+        </div>`;\n+      return;\n+    }\n+\n+    titleEl.textContent = `PR #${p.pr} — assessment`;\n+    const metaBits = [\n+      a.risk_tier ? `risk: ${a.risk_tier}` : '',\n+      a.stage === 'post' ? 'post-merge' : 'pre-merge',\n+      a.generated ? a.generated : '',\n+    ].filter(Boolean).join(' · ');\n+    metaEl.innerHTML = verdictBadgeHtml(a.verdict)\n+      + (metaBits ? `<span class=\"adr-sheet-sourceline\">${escapeHtml(metaBits)}</span>` : '');\n+\n+    const summaryHtml = a.summary ? `\n+      <div class=\"adr-block decision\">\n+        <div class=\"ab-kicker\">Summary</div>\n+        <p>${escapeHtml(a.summary)}</p>\n+      </div>` : '';\n+\n+    // The author's own words come first — the assessment is a response to them.\n+    let intentHtml = '';\n+    if (intent && intent.problem){\n+      const flags = [\n+        intent.source === 'inferred' ? 'intent inferred, not stated by the author' : '',\n+        intent.authorship && intent.authorship !== 'human' ? `authorship: ${intent.authorship}` : '',\n+        (intent.unknowns || []).length ? `${intent.unknowns.length} author-flagged unknown(s)` : '',\n+      ].filter(Boolean);\n+      intentHtml = `\n+        <div class=\"adr-block problem\">\n+          <div class=\"ab-kicker\">The author's stated problem</div>\n+          <p>${escapeHtml(intent.problem)}</p>\n+          ${flags.length ? `<div class=\"chip-row\" style=\"margin-top:10px;\">${flags.map(f => `<span class=\"force-chip\">${escapeHtml(f)}</span>`).join('')}</div>` : ''}\n+        </div>`;\n+    }\n+\n+    let patternHtml = '';\n+    if (a.pattern && a.pattern.answer){\n+      const label = PATTERN_LABEL[a.pattern.verdict] || a.pattern.verdict || '';\n+      const dupes = (a.pattern.duplicates || []).filter(Boolean);\n+      patternHtml = asmQuestionHtml('Q3 — pattern, duplicate, or reinvention', a.pattern);\n+      patternHtml = patternHtml.replace(\n+        '<p>',\n+        `${label ? `<p style=\"margin-bottom:8px;\"><strong>${escapeHtml(label)}</strong></p>` : ''}<p>`\n+      );\n+      if (dupes.length){\n+        patternHtml = patternHtml.replace(\n+          '</div>\\n      </div>',\n+          `</div>\\n        <div class=\"asm-delta\" style=\"margin-top:8px;\">duplicates: ${dupes.map(d => escapeHtml(d)).join(', ')}</div>\\n      </div>`\n+        );\n+      }\n+    }\n+\n+    const findings = a.findings || [];\n+    const order = { blocker: 0, concern: 1, note: 2 };\n+    const sorted = findings.slice().sort((x, y) => (order[x.severity] ?? 3) - (order[y.severity] ?? 3));\n+    const findingCard = (f) => `\n+      <div class=\"asm-finding ${escapeHtml(f.severity || 'note')}\">\n+        <div class=\"asm-sev\">${escapeHtml(f.severity || 'note')}${f.district ? ` · ${escapeHtml(f.district)}` : ''}</div>\n+        <div class=\"asm-claim\">${escapeHtml(f.claim || '')}</div>\n+        ${f.evidence ? `<div class=\"asm-ev\">${escapeHtml(f.evidence)}</div>` : ''}\n+        ${f.suggestion ? `<div class=\"asm-fix\">${escapeHtml(f.suggestion)}</div>` : ''}\n+      </div>`;\n+    let findingsHtml = '';\n+    if (sorted.length){\n+      // Same +N more disclosure the ADR sheet uses for rejected alternatives.\n+      const visible = sorted.slice(0, 3);\n+      const hidden = sorted.slice(3);\n+      findingsHtml = `\n+        <div>\n+          <div class=\"alt-section-label\">Findings</div>\n+          <div class=\"asm-stack\">${visible.map(findingCard).join('')}</div>\n+          ${hidden.length ? `\n+            <button class=\"link-btn\" id=\"asm-findings-more-btn\" style=\"margin-top:8px;\">+${hidden.length} more</button>\n+            <div class=\"asm-stack\" id=\"asm-findings-hidden\" hidden style=\"margin-top:8px;\">${hidden.map(findingCard).join('')}</div>\n+          ` : ''}\n+        </div>`;\n+    }\n+\n+    const checks = a.boundary_checks || [];\n+    const checksHtml = checks.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">Boundary checks</div>\n+        <div class=\"asm-stack\">${checks.map(c => `\n+          <div class=\"asm-check ${escapeHtml(c.result || '')}\">\n+            <span class=\"asm-res\">${escapeHtml(c.result || '?')}</span>\n+            <span>${escapeHtml(c.rule || '')}${c.source ? ` <span class=\"adr-sheet-sourceline\">(${escapeHtml(c.source)})</span>` : ''}</span>\n+          </div>`).join('')}</div>\n+      </div>` : '';\n+\n+    const d = a.delta || {};\n+    const deltaLines = []\n+      .concat((d.districts_added || []).map(x => `+ district ${x}`))\n+      .concat((d.districts_changed || []).map(x => `~ ${x.id}: ${x.files_before} → ${x.files_after} files`))\n+      .concat((d.edges_added || []).map(x => `+ edge ${x}`))\n+      .concat((d.edges_removed || []).map(x => `- edge ${x}`));\n+    const deltaHtml = deltaLines.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">District delta</div>\n+        <div class=\"asm-delta\">${deltaLines.map(l => escapeHtml(l)).join('<br>')}</div>\n+      </div>` : '';\n+\n+    const drift = a.drift || [];\n+    const driftHtml = drift.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">Drift from the stated intent</div>\n+        <div class=\"asm-stack\">${drift.map(x => `\n+          <div class=\"asm-finding concern\">\n+            <div class=\"asm-sev\">${escapeHtml(DRIFT_LABEL[x.kind] || x.kind || '')}</div>\n+            <div class=\"asm-claim\">${escapeHtml(x.claim || '')}</div>\n+            ${x.evidence ? `<div class=\"asm-ev\">${escapeHtml(x.evidence)}</div>` : ''}\n+          </div>`).join('')}</div>\n+      </div>` : '';\n+\n+    const regretHtml = a.regret_risk ? `\n+      <div class=\"adr-block\">\n+        <div class=\"ab-kicker\" style=\"color:oklch(0.5 0.15 25);\">Will we regret this?</div>\n+        <p>${escapeHtml(a.regret_risk)}</p>\n+      </div>` : '';\n+\n+    body.innerHTML = `\n+      ${summaryHtml}\n+      ${intentHtml}\n+      ${asmQuestionHtml('Q1 — is this sensible?', a.sensible)}\n+      ${asmQuestionHtml('Q2 — maintainability and readability', a.maintainability, 'Constraint introduced', 'constraint_introduced')}\n+      ${patternHtml}\n+      ${regretHtml}\n+      ${findingsHtml}\n+      ${checksHtml}\n+      ${deltaHtml}\n+      ${driftHtml}\n+    `;\n+\n+    const moreBtn = document.getElementById('asm-findings-more-btn');\n+    if (moreBtn){\n+      moreBtn.addEventListener('click', () => {\n+        document.getElementById('asm-findings-hidden').hidden = false;\n+        moreBtn.hidden = true;\n+      });\n+    }\n+  }\n+\n   document.getElementById('tb-prev').addEventListener('click', () => selectPR(state.prIndex - 1));\n   document.getElementById('tb-next').addEventListener('click', () => selectPR(state.prIndex + 1));\n \n@@ -2371,6 +2654,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   document.getElementById('btn-comments-toggle').addEventListener('click', openCommentsDrawer);\n   document.getElementById('drawer-close-btn').addEventListener('click', closeCommentsDrawer);\n   document.getElementById('adr-sheet-close-btn').addEventListener('click', closeAdrSheet);\n+  document.getElementById('assessment-sheet-close-btn').addEventListener('click', closeAssessmentSheet);\n   document.getElementById('drawer-scrim').addEventListener('click', closeAnyOpenSheet);\n   document.getElementById('btn-add-comment').addEventListener('click', addCommentOnly);\n   commentInput.addEventListener('keydown', (e) => { if (e.key === 'Enter'){ e.preventDefault(); addCommentOnly(); } });\n@@ -2393,7 +2677,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   window.addEventListener('keydown', (e) => {\n     // Escape must close whichever sheet is open, even while the comment input is\n     // focused — checked before the input/textarea early-return below so it always works.\n-    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n+    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n \n     const tag = document.activeElement && document.activeElement.tagName;\n     if (tag === 'TEXTAREA' || tag === 'INPUT') return;",
  ".prodyssey/self/bundle.json": "diff --git a/.prodyssey/self/bundle.json b/.prodyssey/self/bundle.json\nindex f73cffb..a82dafc 100644\n--- a/.prodyssey/self/bundle.json\n+++ b/.prodyssey/self/bundle.json\n@@ -1,6 +1,6 @@\n {\n   \"bundle_format\": 1,\n-  \"schema_version\": \"1.1\",\n-  \"generator_version\": \"0.2.0\",\n-  \"migrated_at\": \"2026-08-02\"\n+  \"schema_version\": \"1.2\",\n+  \"generator_version\": \"0.3.0\",\n+  \"migrated_at\": \"2026-08-04\"\n }",
  ".prodyssey/self/data/story.js": "diff --git a/.prodyssey/self/data/story.js b/.prodyssey/self/data/story.js\nindex 5bd5397..b337b7a 100644\n--- a/.prodyssey/self/data/story.js\n+++ b/.prodyssey/self/data/story.js\n@@ -2,7 +2,7 @@ window.STORY = {\n   \"meta\": {\n     \"repo\": \"prodyssey\",\n     \"generated\": \"2026-07-22\",\n-    \"schema_version\": \"1.1\",\n+    \"schema_version\": \"1.2\",\n     \"title\": \"prodyssey — Codebase Odyssey\",\n     \"description\": \"\",\n     \"levels\": [",
  ".prodyssey/self/data/story.json": "diff --git a/.prodyssey/self/data/story.json b/.prodyssey/self/data/story.json\nindex 0ed98a9..60b326a 100644\n--- a/.prodyssey/self/data/story.json\n+++ b/.prodyssey/self/data/story.json\n@@ -2,7 +2,7 @@\n   \"meta\": {\n     \"repo\": \"prodyssey\",\n     \"generated\": \"2026-07-22\",\n-    \"schema_version\": \"1.1\",\n+    \"schema_version\": \"1.2\",\n     \"title\": \"prodyssey — Codebase Odyssey\",\n     \"description\": \"\",\n     \"levels\": [",
  ".prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/assessment.json": "diff --git a/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/assessment.json b/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/assessment.json\nnew file mode 100644\nindex 0000000..324095d\n--- /dev/null\n+++ b/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/assessment.json\n@@ -0,0 +1,93 @@\n+{\n+  \"stage\": \"pre\",\n+  \"generated\": \"2026-08-04\",\n+  \"verdict\": \"concerns\",\n+  \"risk_tier\": \"architectural\",\n+  \"summary\": \"The change adds a fifth mode that captures author intent before a PR opens, assesses the change against the bundle, and then opens the PR. It reuses the open-PR discovery, the migration guard, and the ADR record shape rather than duplicating them, and it activates a consumer contract the stack cards already declared. Two costs are real and accepted: story.json grows, and the plugin takes its first outward-facing action.\",\n+  \"sensible\": {\n+    \"answer\": \"Yes, on both halves. The stated problem is real and visible in the codebase, not hypothetical: decision-records-lite.md §3.4 already documents the escape hatch that exists because intent is lost at submit time. The change also belongs at this layer. Intent capture has to live where the diff and the baseline both are, and that is the odyssey skill.\",\n+    \"evidence\": [\n+      \"skills/odyssey/references/decision-records-lite.md:74\",\n+      \"skills/odyssey/references/stacks/README.md\"\n+    ]\n+  },\n+  \"maintainability\": {\n+    \"answer\": \"Helps, on balance. The intent block removes a whole category of inference from generate mode, and the alternatives shape matches adrs.json exactly, so an interviewed alternative moves into an ADR with no translation layer. The cost is that story.json now carries two more authored blocks, and one of them can be large.\",\n+    \"constraint_introduced\": \"Submit mode owns `intent` and `assessment` on a timeline entry. Generate mode reads them and never overwrites them, the same discipline extract_story.py already applies to authored narrative. Both fields are in AUTHORED_TIMELINE_FIELDS, so any future migration that touches either one must declare it or the guard aborts the run.\",\n+    \"evidence\": [\n+      \"scripts/migrate_bundle.py:69\",\n+      \"scripts/extract_story.py\"\n+    ]\n+  },\n+  \"pattern\": {\n+    \"verdict\": \"new-valuable\",\n+    \"answer\": \"The mode is new to this plugin, and it does not duplicate anything in the bundle. It is close to a pattern the repo already designed and never wired: stacks/README.md's Card Fields table names `## Boundary Rules` as consumed by \\\"review findings\\\" and `## Review Checks` as consumed by \\\"review/maintenance\\\", for a review mode that README.md's extraction manifest records as deliberately excluded. This change consumes those sections rather than inventing a parallel rubric, which is why it reads as activation rather than reinvention. What it adds that the excluded mode did not have is author-side intent capture.\",\n+    \"duplicates\": [],\n+    \"evidence\": [\n+      \"skills/odyssey/references/stacks/README.md\",\n+      \"README.md:334\"\n+    ]\n+  },\n+  \"findings\": [\n+    {\n+      \"severity\": \"concern\",\n+      \"claim\": \"story.json carries assessment findings inline, and grows with every reviewed PR\",\n+      \"evidence\": \".prodyssey/self/data/story.json\",\n+      \"district\": \"scripts\",\n+      \"suggestion\": \"Accept for now. The fix, if it becomes a problem, is a separate file with a migration ladder — not a second adrs.json.\"\n+    },\n+    {\n+      \"severity\": \"concern\",\n+      \"claim\": \"SKILL.md's preamble calls this a read-only, generate-only instrument, and submit mode pushes a branch and opens a PR\",\n+      \"evidence\": \"skills/odyssey/SKILL.md:20\",\n+      \"district\": \"skills\",\n+      \"suggestion\": \"Reword the preamble and the Notes invariant so the claim stays true, and keep the confirmation step mandatory.\"\n+    },\n+    {\n+      \"severity\": \"note\",\n+      \"claim\": \"adrs.json still has no migration ladder and no authored-field guard\",\n+      \"evidence\": \"CLAUDE.md:284\",\n+      \"district\": \"scripts\",\n+      \"suggestion\": \"Unchanged by this PR. Storing intent and assessment on the timeline entry avoids adding a second file with the same gap.\"\n+    },\n+    {\n+      \"severity\": \"note\",\n+      \"claim\": \"detect_default_branch leaked git's `fatal:` to stderr on every call in a repo with no origin/HEAD symref\",\n+      \"evidence\": \"scripts/extract_diffs.py:96\",\n+      \"district\": \"scripts\",\n+      \"suggestion\": \"Fixed in this change, in both copies of the deliberately duplicated helper.\"\n+    }\n+  ],\n+  \"boundary_checks\": [\n+    {\n+      \"rule\": \"Inner layers never import outer layers (generic.md dependency rule)\",\n+      \"source\": \"stacks/generic.md\",\n+      \"result\": \"pass\",\n+      \"evidence\": \"render_review.py imports only stdlib; no script imports the skill or the viewer\"\n+    },\n+    {\n+      \"rule\": \"Config crosses into code in one place\",\n+      \"source\": \"stacks/generic.md\",\n+      \"result\": \"pass\",\n+      \"evidence\": \"SCHEMA_VERSION and CURRENT_BUNDLE_FORMAT stay in scripts/_bundle_meta.py; no new literal added elsewhere\"\n+    },\n+    {\n+      \"rule\": \"Scripts move data, Claude judges (repo-local rule, CLAUDE.md)\",\n+      \"source\": \"CLAUDE.md\",\n+      \"result\": \"pass\",\n+      \"evidence\": \"render_review.py renders; it makes no verdict and calls no service\"\n+    }\n+  ],\n+  \"delta\": {\n+    \"districts_added\": [],\n+    \"districts_changed\": [\n+      {\"id\": \"scripts\", \"files_before\": 12, \"files_after\": 13},\n+      {\"id\": \"skills\", \"files_before\": 50, \"files_after\": 53},\n+      {\"id\": \"commands\", \"files_before\": 4, \"files_after\": 5}\n+    ],\n+    \"edges_added\": [],\n+    \"edges_removed\": []\n+  },\n+  \"regret_risk\": \"The regret case is a mode that ships and nobody runs twice, because the interview asks generic questions and the assessment restates the diff. That failure is invisible in code review and only shows up in use, which is why interview-guide.md §2 puts the evidence-first rule ahead of everything else and caps the question count. The second regret case is smaller and reversible: story.json grows past comfort, and the fix is a separate file with a proper migration ladder. The outward-facing PR creation is the change hardest to walk back, because it sets a precedent for what this plugin is allowed to do. That one is worth holding the line on — opening the PR the author asked for is the whole scope, and comments, body edits, labels, and merges stay out.\",\n+  \"drift\": []\n+}",
  ".prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/assessment.md": "diff --git a/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/assessment.md b/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/assessment.md\nnew file mode 100644\nindex 0000000..1372bab\n--- /dev/null\n+++ b/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/assessment.md\n@@ -0,0 +1,69 @@\n+# Assessment — Submit mode — author interview and architecture assessment\n+\n+**Verdict:** Concerns  \n+**Risk tier:** Architectural  \n+**Stage:** pre-merge\n+\n+## Summary\n+\n+The change adds a fifth mode that captures author intent before a PR opens, assesses the change against the bundle, and then opens the PR. It reuses the open-PR discovery, the migration guard, and the ADR record shape rather than duplicating them, and it activates a consumer contract the stack cards already declared. Two costs are real and accepted: story.json grows, and the plugin takes its first outward-facing action.\n+\n+## Question 1 — is this sensible?\n+\n+Yes, on both halves. The stated problem is real and visible in the codebase, not hypothetical: decision-records-lite.md §3.4 already documents the escape hatch that exists because intent is lost at submit time. The change also belongs at this layer. Intent capture has to live where the diff and the baseline both are, and that is the odyssey skill.\n+\n+Evidence: `skills/odyssey/references/decision-records-lite.md:74` `skills/odyssey/references/stacks/README.md`\n+\n+## Question 2 — maintainability and readability\n+\n+Helps, on balance. The intent block removes a whole category of inference from generate mode, and the alternatives shape matches adrs.json exactly, so an interviewed alternative moves into an ADR with no translation layer. The cost is that story.json now carries two more authored blocks, and one of them can be large.\n+\n+**Constraint introduced:** Submit mode owns `intent` and `assessment` on a timeline entry. Generate mode reads them and never overwrites them, the same discipline extract_story.py already applies to authored narrative. Both fields are in AUTHORED_TIMELINE_FIELDS, so any future migration that touches either one must declare it or the guard aborts the run.\n+\n+Evidence: `scripts/migrate_bundle.py:69` `scripts/extract_story.py`\n+\n+## Question 3 — new pattern, duplicate, or reinvention?\n+\n+**New pattern, and it earns its place**\n+\n+The mode is new to this plugin, and it does not duplicate anything in the bundle. It is close to a pattern the repo already designed and never wired: stacks/README.md's Card Fields table names `## Boundary Rules` as consumed by \"review findings\" and `## Review Checks` as consumed by \"review/maintenance\", for a review mode that README.md's extraction manifest records as deliberately excluded. This change consumes those sections rather than inventing a parallel rubric, which is why it reads as activation rather than reinvention. What it adds that the excluded mode did not have is author-side intent capture.\n+\n+Evidence: `skills/odyssey/references/stacks/README.md` `README.md:334`\n+\n+## Will we regret this?\n+\n+The regret case is a mode that ships and nobody runs twice, because the interview asks generic questions and the assessment restates the diff. That failure is invisible in code review and only shows up in use, which is why interview-guide.md §2 puts the evidence-first rule ahead of everything else and caps the question count. The second regret case is smaller and reversible: story.json grows past comfort, and the fix is a separate file with a proper migration ladder. The outward-facing PR creation is the change hardest to walk back, because it sets a precedent for what this plugin is allowed to do. That one is worth holding the line on — opening the PR the author asked for is the whole scope, and comments, body edits, labels, and merges stay out.\n+\n+## Findings\n+\n+| Severity | Finding | Evidence |\n+|---|---|---|\n+| concern | story.json carries assessment findings inline, and grows with every reviewed PR | `.prodyssey/self/data/story.json` |\n+| concern | SKILL.md's preamble calls this a read-only, generate-only instrument, and submit mode pushes a branch and opens a PR | `skills/odyssey/SKILL.md:20` |\n+| note | adrs.json still has no migration ladder and no authored-field guard | `CLAUDE.md:284` |\n+| note | detect_default_branch leaked git's `fatal:` to stderr on every call in a repo with no origin/HEAD symref | `scripts/extract_diffs.py:96` |\n+\n+**Suggestions**\n+\n+- **story.json carries assessment findings inline, and grows with every reviewed PR** — Accept for now. The fix, if it becomes a problem, is a separate file with a migration ladder — not a second adrs.json.\n+- **SKILL.md's preamble calls this a read-only, generate-only instrument, and submit mode pushes a branch and opens a PR** — Reword the preamble and the Notes invariant so the claim stays true, and keep the confirmation step mandatory.\n+- **adrs.json still has no migration ladder and no authored-field guard** — Unchanged by this PR. Storing intent and assessment on the timeline entry avoids adding a second file with the same gap.\n+- **detect_default_branch leaked git's `fatal:` to stderr on every call in a repo with no origin/HEAD symref** — Fixed in this change, in both copies of the deliberately duplicated helper.\n+\n+## Boundary checks\n+\n+| Result | Rule | Source | Evidence |\n+|---|---|---|---|\n+| pass | Inner layers never import outer layers (generic.md dependency rule) | `stacks/generic.md` | `render_review.py imports only stdlib; no script imports the skill or the viewer` |\n+| pass | Config crosses into code in one place | `stacks/generic.md` | `SCHEMA_VERSION and CURRENT_BUNDLE_FORMAT stay in scripts/_bundle_meta.py; no new literal added elsewhere` |\n+| pass | Scripts move data, Claude judges (repo-local rule, CLAUDE.md) | `CLAUDE.md` | `render_review.py renders; it makes no verdict and calls no service` |\n+\n+## District delta\n+\n+- `scripts`: 12 -> 13 files\n+- `skills`: 50 -> 53 files\n+- `commands`: 4 -> 5 files\n+\n+---\n+\n+_Generated by prodyssey submit mode on 2026-08-04._",
  ".prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/description.md": "diff --git a/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/description.md b/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/description.md\nnew file mode 100644\nindex 0000000..e9c07a3\n--- /dev/null\n+++ b/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/description.md\n@@ -0,0 +1,50 @@\n+# Submit mode — author interview and architecture assessment\n+\n+## Problem\n+\n+Prodyssey reconstructs author intent from merged diffs, long after the author forgot it. That is expensive, and it is lossy: decision-records-lite.md §3.4 carries an explicit escape hatch for the case where no rejected alternative survives in the record, because the intent was lost when the PR was submitted. Meanwhile PR review itself has moved. Writing code is cheap now, verifying it is not, and a diff can look intentional while carrying almost no human intent.\n+\n+The bundle already holds the three things a reviewer needs and a generic diff-reading tool does not have: the district map in inventory.yaml, every structural decision in adrs.json, and the narrated history of how the repo got here. Nothing consumed them at review time. The stack cards even declare the consumer contract already, in stacks/README.md, and it has been dormant since the extraction.\n+\n+## Why this approach\n+\n+A fifth mode, `submit`, inside the odyssey skill rather than a separate skill, because it needs the bundle, the baseline, and the decision history. The pre stage reads the evidence, interviews the author for only what the evidence cannot settle, assesses the change against the bundle, and then opens the pull request with the generated description as its body. The post stage compares what shipped against what the author claimed. The captured `intent` then feeds generate mode, so narrative and ADR authoring stop guessing.\n+\n+## Alternatives considered\n+\n+- **Keep review as a separate skill in the plugin** — rejected because the whole value is reviewing against the accumulated odyssey, which means sharing Hub resolution, the bundle dir, migration, and verification with the other four modes.\n+- **Target only an existing open PR number** — rejected because the author wants the interview before submitting, and the existing open-PR path already requires the PR to exist on GitHub first.\n+- **Let a working-branch run write a synthetic PR key into story.json** — rejected because story.json keys on an integer `pr`, and a synthetic key would leak into verify_bundle.py, record_publish.py, the viewer, and the publish manifest.\n+- **End a working-branch run at a dead-end export directory, and require a second run once the PR exists** — rejected because the author pointed out this leaves the PR-number hole open; ending the interview by opening the PR earns a real number instead of inventing one.\n+- **Post the assessment to GitHub as a PR comment, behind a flag** — rejected because the author asked for local output written into the repo the user points at; opening the PR is the only outward action the mode takes.\n+- **Add a fifth level to the viewer rail** — rejected because it touches renderMainContent, deriveLevelContent, audio derivation, and the comment-tagging chip, and it changes what \"four-level odyssey\" means across every doc.\n+- **Store the assessment in a new top-level reviews.json** — rejected because adrs.json already carries the no-migration-ladder debt, and a second such file doubles it; the timeline entry gets the guard for free.\n+\n+## Out of scope\n+\n+- Lint-grade findings — missing null checks, naming, unhandled error paths, test-coverage gaps. Commodity work that buries the judgment it is mixed with.\n+- Any score, grade, or pass rate.\n+- Any merge gate. A `rework` verdict does not block PR creation.\n+- The ~170-YAML principle corpus from the parent skill. The stack cards' `## Corpus Load` sections point at paths this plugin does not ship.\n+- A migration ladder for adrs.json. Still absent, still known.\n+\n+## Risks\n+\n+- The interview is the product. Generic questions make this a worse version of a generic AI reviewer.\n+- story.json grows. An assessment with findings is much larger than a tagline.\n+- Opening a PR is the first outward-facing action this plugin takes, and it contradicts the read-only claim in SKILL.md's preamble unless that preamble is reworded.\n+- The duplicate and reinvention check is only as good as the bundle. A repo with two generated PRs cannot answer it.\n+\n+## How this was tested\n+\n+migrate_bundle.py --dry-run on all three committed bundles: one-line diff each, zero authored-field guard violations. verify_bundle.py required-failure set compared against master for .prodyssey/self: identical, so the new keys add no regression. cobuilder-harness required-failure count 86 on master and 86 on this branch. extract_diffs.py --branch cross-checked against `git diff --shortstat`: 99 files, +26559/-335, exact match. Error paths exercised: --prs with --branch, neither flag, a branch with no commits beyond its base.\n+\n+## Where to focus\n+\n+- references/interview-guide.md §2 and §3. If the evidence-first rule and the question budget do not hold up, nothing downstream matters.\n+- The confirmation step before push and PR creation. It is the only thing standing between this mode and an unwanted public action.\n+- verify_bundle.py's optional-by-default treatment of intent and assessment. A bundle generated before this mode existed must not start failing.\n+\n+---\n+\n+_Authorship: agent-generated._",
  ".prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/intent.json": "diff --git a/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/intent.json b/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/intent.json\nnew file mode 100644\nindex 0000000..93405b6\n--- /dev/null\n+++ b/.prodyssey/self/exports/branch-claude-pr-odyssey-improvements-7suouc/intent.json\n@@ -0,0 +1,59 @@\n+{\n+  \"captured\": \"2026-08-04\",\n+  \"source\": \"author\",\n+  \"authorship\": \"agent-generated\",\n+  \"title\": \"Submit mode — author interview and architecture assessment\",\n+  \"problem\": \"Prodyssey reconstructs author intent from merged diffs, long after the author forgot it. That is expensive, and it is lossy: decision-records-lite.md §3.4 carries an explicit escape hatch for the case where no rejected alternative survives in the record, because the intent was lost when the PR was submitted. Meanwhile PR review itself has moved. Writing code is cheap now, verifying it is not, and a diff can look intentional while carrying almost no human intent.\",\n+  \"why_now\": \"The bundle already holds the three things a reviewer needs and a generic diff-reading tool does not have: the district map in inventory.yaml, every structural decision in adrs.json, and the narrated history of how the repo got here. Nothing consumed them at review time. The stack cards even declare the consumer contract already, in stacks/README.md, and it has been dormant since the extraction.\",\n+  \"approach\": \"A fifth mode, `submit`, inside the odyssey skill rather than a separate skill, because it needs the bundle, the baseline, and the decision history. The pre stage reads the evidence, interviews the author for only what the evidence cannot settle, assesses the change against the bundle, and then opens the pull request with the generated description as its body. The post stage compares what shipped against what the author claimed. The captured `intent` then feeds generate mode, so narrative and ADR authoring stop guessing.\",\n+  \"alternatives\": [\n+    {\n+      \"option\": \"Keep review as a separate skill in the plugin\",\n+      \"rejected_because\": \"the whole value is reviewing against the accumulated odyssey, which means sharing Hub resolution, the bundle dir, migration, and verification with the other four modes\"\n+    },\n+    {\n+      \"option\": \"Target only an existing open PR number\",\n+      \"rejected_because\": \"the author wants the interview before submitting, and the existing open-PR path already requires the PR to exist on GitHub first\"\n+    },\n+    {\n+      \"option\": \"Let a working-branch run write a synthetic PR key into story.json\",\n+      \"rejected_because\": \"story.json keys on an integer `pr`, and a synthetic key would leak into verify_bundle.py, record_publish.py, the viewer, and the publish manifest\"\n+    },\n+    {\n+      \"option\": \"End a working-branch run at a dead-end export directory, and require a second run once the PR exists\",\n+      \"rejected_because\": \"the author pointed out this leaves the PR-number hole open; ending the interview by opening the PR earns a real number instead of inventing one\"\n+    },\n+    {\n+      \"option\": \"Post the assessment to GitHub as a PR comment, behind a flag\",\n+      \"rejected_because\": \"the author asked for local output written into the repo the user points at; opening the PR is the only outward action the mode takes\"\n+    },\n+    {\n+      \"option\": \"Add a fifth level to the viewer rail\",\n+      \"rejected_because\": \"it touches renderMainContent, deriveLevelContent, audio derivation, and the comment-tagging chip, and it changes what \\\"four-level odyssey\\\" means across every doc\"\n+    },\n+    {\n+      \"option\": \"Store the assessment in a new top-level reviews.json\",\n+      \"rejected_because\": \"adrs.json already carries the no-migration-ladder debt, and a second such file doubles it; the timeline entry gets the guard for free\"\n+    }\n+  ],\n+  \"out_of_scope\": [\n+    \"Lint-grade findings — missing null checks, naming, unhandled error paths, test-coverage gaps. Commodity work that buries the judgment it is mixed with.\",\n+    \"Any score, grade, or pass rate.\",\n+    \"Any merge gate. A `rework` verdict does not block PR creation.\",\n+    \"The ~170-YAML principle corpus from the parent skill. The stack cards' `## Corpus Load` sections point at paths this plugin does not ship.\",\n+    \"A migration ladder for adrs.json. Still absent, still known.\"\n+  ],\n+  \"risks\": [\n+    \"The interview is the product. Generic questions make this a worse version of a generic AI reviewer.\",\n+    \"story.json grows. An assessment with findings is much larger than a tagline.\",\n+    \"Opening a PR is the first outward-facing action this plugin takes, and it contradicts the read-only claim in SKILL.md's preamble unless that preamble is reworded.\",\n+    \"The duplicate and reinvention check is only as good as the bundle. A repo with two generated PRs cannot answer it.\"\n+  ],\n+  \"testing\": \"migrate_bundle.py --dry-run on all three committed bundles: one-line diff each, zero authored-field guard violations. verify_bundle.py required-failure set compared against master for .prodyssey/self: identical, so the new keys add no regression. cobuilder-harness required-failure count 86 on master and 86 on this branch. extract_diffs.py --branch cross-checked against `git diff --shortstat`: 99 files, +26559/-335, exact match. Error paths exercised: --prs with --branch, neither flag, a branch with no commits beyond its base.\",\n+  \"reviewer_focus\": [\n+    \"references/interview-guide.md §2 and §3. If the evidence-first rule and the question budget do not hold up, nothing downstream matters.\",\n+    \"The confirmation step before push and PR creation. It is the only thing standing between this mode and an unwanted public action.\",\n+    \"verify_bundle.py's optional-by-default treatment of intent and assessment. A bundle generated before this mode existed must not start failing.\"\n+  ],\n+  \"unknowns\": []\n+}",
  ".prodyssey/self/viewer/index.html": "diff --git a/.prodyssey/self/viewer/index.html b/.prodyssey/self/viewer/index.html\nindex b10e8aa..14a7fca 100644\n--- a/.prodyssey/self/viewer/index.html\n+++ b/.prodyssey/self/viewer/index.html\n@@ -292,6 +292,9 @@\n \n   .alt-section-label{font:10.5px 'JetBrains Mono',monospace;letter-spacing:.1em;color:var(--ink-dim);text-transform:uppercase;margin-bottom:8px;}\n   .alt-row{display:flex;gap:12px;flex-wrap:wrap;}\n+  /* A `display:` rule beats the plain `hidden` attribute, so the ADR sheet's\n+     \"+N more\" disclosure needs this to stay hidden until the button is clicked. */\n+  .alt-row[hidden]{display:none;}\n   .alt-card{width:240px;background:#fff;border:1px dashed oklch(0.6 0.13 25);border-radius:8px;padding:10px 12px;}\n   .alt-card .alt-opt{font-size:12px;font-weight:600;color:oklch(0.3 0.02 250);}\n   .alt-card .alt-rej{font-size:11px;color:oklch(0.45 0.14 25);margin-top:5px;line-height:1.4;}\n@@ -304,6 +307,16 @@\n   .adr-badge:hover{background:oklch(0.94 0.04 60);border-color:oklch(0.48 0.13 60);}\n   .no-adrs{font:11.5px system-ui,sans-serif;color:var(--ink-dim);}\n \n+  /* Submit-mode assessment badge: same row as the ADR badges, deliberately a\n+     different hue so a verdict never reads as an ADR id. */\n+  .assessment-badge{\n+    font:10.5px 'JetBrains Mono',monospace;padding:4px 10px;border-radius:3px;cursor:pointer;\n+    border:1px solid oklch(0.55 0.12 265);color:oklch(0.44 0.12 265);background:oklch(0.97 0.02 265);\n+    display:inline-flex;align-items:center;gap:6px;\n+  }\n+  .assessment-badge:hover{background:oklch(0.94 0.04 265);border-color:oklch(0.48 0.13 265);}\n+  .assessment-badge .ab-dot{width:6px;height:6px;border-radius:50%;background:currentColor;}\n+\n   .beats-stack{display:flex;flex-direction:column;gap:10px;}\n   .beat-card.fixed-card{height:180px;}\n   .beat-card .fc-kicker{color:oklch(0.5 0.02 250);}\n@@ -453,18 +466,42 @@\n   /* ================= ADR SHEET (same shadcn-Sheet family as the comments drawer,\n      just wider — reuses .drawer-header/.drawer-close/.drawer-body). Shares\n      #drawer-scrim with the comments drawer; JS enforces only one open at a time. ================= */\n-  #adr-sheet{\n+  #adr-sheet,\n+  #assessment-sheet{\n     position:fixed;top:0;right:0;bottom:0;width:clamp(480px, 40vw, 720px);max-width:92vw;background:#fff;\n     border-left:1px solid var(--line);border-radius:14px 0 0 14px;box-shadow:-12px 0 34px rgba(20,25,35,0.2);\n     transform:translateX(100%);transition:transform .25s ease;z-index:41;\n     display:flex;flex-direction:column;min-height:0;\n   }\n-  #adr-sheet.open{transform:translateX(0);}\n-  #adr-sheet .drawer-header{align-items:flex-start;}\n+  #adr-sheet.open,\n+  #assessment-sheet.open{transform:translateX(0);}\n+  #adr-sheet .drawer-header,\n+  #assessment-sheet .drawer-header{align-items:flex-start;}\n   .adr-sheet-meta{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:6px;}\n   .adr-state-badge{font:10px 'JetBrains Mono',monospace;letter-spacing:.04em;padding:3px 9px;border-radius:10px;border:1px solid;text-transform:uppercase;white-space:nowrap;}\n   .adr-sheet-sourceline{font:11px 'JetBrains Mono',monospace;color:var(--ink-dim);}\n-  #adr-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+  #adr-sheet .drawer-body,\n+  #assessment-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+\n+  /* Assessment-only blocks. Everything else in the sheet reuses .adr-block,\n+     .ab-kicker, .force-chip, .alt-section-label, and .adr-body-md. */\n+  .asm-finding{border-radius:8px;border:1px solid var(--line);background:#fff;padding:10px 12px;border-left:3px solid var(--line);}\n+  .asm-finding.blocker{border-left-color:oklch(0.55 0.19 25);}\n+  .asm-finding.concern{border-left-color:oklch(0.66 0.15 70);}\n+  .asm-finding.note{border-left-color:oklch(0.7 0.05 250);}\n+  .asm-finding .asm-sev{font:700 9.5px 'JetBrains Mono',monospace;letter-spacing:.1em;text-transform:uppercase;color:var(--ink-dim);}\n+  .asm-finding.blocker .asm-sev{color:oklch(0.5 0.18 25);}\n+  .asm-finding.concern .asm-sev{color:oklch(0.5 0.14 70);}\n+  .asm-finding .asm-claim{font-size:12.5px;line-height:1.5;color:oklch(0.28 0.02 250);margin-top:5px;}\n+  .asm-finding .asm-ev{font:10.5px 'JetBrains Mono',monospace;color:var(--ink-dim);margin-top:6px;word-break:break-all;}\n+  .asm-finding .asm-fix{font-size:12px;line-height:1.5;color:oklch(0.4 0.02 250);margin-top:6px;}\n+  .asm-stack{display:flex;flex-direction:column;gap:8px;}\n+  .asm-stack[hidden]{display:none;}\n+  .asm-check{display:flex;gap:8px;align-items:baseline;font-size:12px;line-height:1.5;color:oklch(0.3 0.02 250);}\n+  .asm-check .asm-res{font:700 9.5px 'JetBrains Mono',monospace;letter-spacing:.06em;text-transform:uppercase;white-space:nowrap;}\n+  .asm-check.pass .asm-res{color:oklch(0.45 0.13 145);}\n+  .asm-check.violation .asm-res{color:oklch(0.5 0.18 25);}\n+  .asm-delta{font:11.5px 'JetBrains Mono',monospace;color:oklch(0.3 0.02 250);line-height:1.7;}\n \n   .adr-block{border-radius:10px;border:1px solid var(--line);background:#fff;padding:14px 16px;}\n   .adr-block.problem{border-left:3px solid oklch(0.6 0.16 25);}\n@@ -636,6 +673,22 @@\n     <div class=\"drawer-body\" id=\"adr-sheet-body\"></div>\n   </div>\n \n+  <!-- Submit-mode assessment sheet (same Sheet family as the ADR sheet above,\n+       reachable from the Assessment badge on level 3). Reads the timeline\n+       entry's own `assessment`/`intent` blocks — there is no window.ASSESSMENTS,\n+       because both live inline in story.json. Shares #drawer-scrim; only one\n+       sheet is ever open. -->\n+  <div id=\"assessment-sheet\" role=\"dialog\" aria-label=\"Architecture assessment\">\n+    <div class=\"drawer-header\">\n+      <div>\n+        <h2 id=\"assessment-sheet-title\">Assessment</h2>\n+        <div class=\"adr-sheet-meta\" id=\"assessment-sheet-meta\"></div>\n+      </div>\n+      <button class=\"drawer-close\" id=\"assessment-sheet-close-btn\" title=\"Close (Esc)\">&times;</button>\n+    </div>\n+    <div class=\"drawer-body\" id=\"assessment-sheet-body\"></div>\n+  </div>\n+\n   <!-- Narration audio dialog (shadcn Dialog look, centered). Shares #drawer-scrim above;\n        mutually exclusive with the comments drawer / ADR sheet — see openAudioDialog. -->\n   <div id=\"audio-dialog-layer\">\n@@ -749,7 +802,9 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     // ADR sheet: mutually exclusive with the comments drawer (see openCommentsDrawer/openAdrSheet).\n     adrOpen: false,\n     activeAdrId: null,\n-    // Audio dialog: mutually exclusive with both sheets above (see openAudioDialog).\n+    // Submit-mode assessment sheet: mutually exclusive with both panels above.\n+    assessmentOpen: false,\n+    // Audio dialog: mutually exclusive with every sheet above (see openAudioDialog).\n     audioDialogOpen: false,\n     // true while a level/PR content transition animation is in flight — guards re-entrant\n     // nav input (arrow mashing, double rail clicks) from stacking transitions.\n@@ -1620,6 +1675,15 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n         ? adrs.map(a => `<button class=\"adr-badge\" data-adr-id=\"${escapeHtml(a)}\">${escapeHtml(a)}</button>`).join('')\n         : `<span class=\"no-adrs\">No ADRs recorded for this PR.</span>`;\n \n+      // Assessment badge, only when submit mode actually left one behind. A PR\n+      // generated before submit mode existed simply shows no badge.\n+      const asm = p.assessment;\n+      const assessmentHtml = (asm && asm.verdict)\n+        ? `<button class=\"assessment-badge\" id=\"assessment-badge\" title=\"Architecture assessment (submit mode)\">\n+             <span class=\"ab-dot\"></span>Assessment: ${escapeHtml(asm.verdict)}\n+           </button>`\n+        : '';\n+\n       const boxesHtml = DISTRICTS.map(d => {\n         const isTouched = !!touched[d.id];\n         const color = KIND_COLOR[d.kind] || KIND_COLOR.tooling;\n@@ -1640,13 +1704,15 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n       setMainHtml(main, `\n         <div class=\"level-stack narrow\">\n           ${heroFrame(p.pr, 3, `PR #${p.pr} architecture diagram`)}\n-          <div class=\"adr-row\">${adrHtml}</div>\n+          <div class=\"adr-row\">${adrHtml}${assessmentHtml}</div>\n           <div class=\"district-boxes\">${boxesHtml}</div>\n           ${beatsHtml}\n         </div>`);\n       main.querySelectorAll('.adr-badge[data-adr-id]').forEach(btn => {\n         btn.addEventListener('click', () => openAdrSheet(btn.dataset.adrId));\n       });\n+      const asmBtn = main.querySelector('#assessment-badge');\n+      if (asmBtn) asmBtn.addEventListener('click', openAssessmentSheet);\n \n     } else {\n       const fc = content.file_changes;\n@@ -1887,6 +1953,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   function openAudioDialog(){\n     if (state.commentsOpen) state.commentsOpen = false;\n     if (state.adrOpen) state.adrOpen = false;\n+    if (state.assessmentOpen) state.assessmentOpen = false;\n     state.audioDialogOpen = true;\n     renderAudioDialogContent();\n     const dialog = document.getElementById('audio-dialog');\n@@ -2117,6 +2184,10 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n       state.level = 1;\n       state.focusDistrict = null;\n       state.draftMergeMethod = null;\n+      // The assessment sheet renders the CURRENT pr(), so leaving it open across\n+      // a PR switch would show the new PR's data under the old PR's scroll\n+      // position — and level resets to 1, where its badge is not even visible.\n+      if (state.assessmentOpen) closeAssessmentSheet();\n       closeMergeMenu();\n       stopAudio();\n       renderAll();\n@@ -2148,19 +2219,22 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   }\n \n   /* ============================================================\n-     COMMENTS DRAWER + ADR SHEET (shadcn Sheet look, recreated in vanilla JS/CSS)\n-     Both panels share one #drawer-scrim; opening either one closes the other first, so\n-     at most one sheet is ever open — this is the \"only one sheet at a time\" rule.\n+     COMMENTS DRAWER + ADR SHEET + ASSESSMENT SHEET (shadcn Sheet look, recreated\n+     in vanilla JS/CSS). All three panels share one #drawer-scrim; opening any one\n+     closes the others first, so at most one sheet is ever open — this is the\n+     \"only one sheet at a time\" rule.\n      ============================================================ */\n   function renderSheetVisibility(){\n     document.getElementById('comments-drawer').classList.toggle('open', state.commentsOpen);\n     document.getElementById('adr-sheet').classList.toggle('open', state.adrOpen);\n+    document.getElementById('assessment-sheet').classList.toggle('open', state.assessmentOpen);\n     const scrim = document.getElementById('drawer-scrim');\n-    scrim.classList.toggle('open', state.commentsOpen || state.adrOpen || state.audioDialogOpen);\n+    scrim.classList.toggle('open', state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen);\n     scrim.classList.toggle('audio-dim', state.audioDialogOpen);\n   }\n   function openCommentsDrawer(){\n     if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.commentsOpen = true;\n     renderSheetVisibility();\n@@ -2172,6 +2246,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   }\n   function openAdrSheet(id){\n     if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.adrOpen = true;\n     state.activeAdrId = id;\n@@ -2182,9 +2257,22 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     state.adrOpen = false;\n     renderSheetVisibility();\n   }\n+  function openAssessmentSheet(){\n+    if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n+    state.assessmentOpen = true;\n+    renderAssessmentSheet();\n+    renderSheetVisibility();\n+  }\n+  function closeAssessmentSheet(){\n+    state.assessmentOpen = false;\n+    renderSheetVisibility();\n+  }\n   function closeAnyOpenSheet(){\n     if (state.commentsOpen) closeCommentsDrawer();\n     else if (state.adrOpen) closeAdrSheet();\n+    else if (state.assessmentOpen) closeAssessmentSheet();\n     else if (state.audioDialogOpen) stopAudio();\n   }\n \n@@ -2352,6 +2440,201 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     }\n   }\n \n+  /* ---------- Submit-mode assessment sheet ----------\n+     Reads pr().assessment / pr().intent straight off window.STORY. No new\n+     global and no new data file: both blocks live inline on the timeline\n+     entry, which is what gives them migrate_bundle.py's authored-field guard.\n+     Verdict/tier vocabulary: skills/odyssey/references/review-mode.md §8, §4. */\n+  const VERDICT_COLOR = {\n+    sound:    ['oklch(0.94 0.06 145)', 'oklch(0.4 0.13 145)', 'oklch(0.6 0.12 145)'],\n+    concerns: ['oklch(0.95 0.06 70)',  'oklch(0.44 0.13 70)', 'oklch(0.65 0.13 70)'],\n+    rework:   ['oklch(0.95 0.05 25)',  'oklch(0.45 0.17 25)', 'oklch(0.62 0.16 25)'],\n+  };\n+  const PATTERN_LABEL = {\n+    conforms: 'Conforms to an existing pattern',\n+    'new-valuable': 'New pattern, and it earns its place',\n+    duplicate: 'Duplicates something the repo already has',\n+    reinvention: 'Reinvents a solved problem',\n+  };\n+  const DRIFT_LABEL = {\n+    out_of_scope: 'Out of scope, but touched',\n+    unaddressed_risk: 'Risk with no guard',\n+    adopted_alternative: 'Rejected option adopted',\n+    delta_shift: 'District delta shifted',\n+  };\n+\n+  function verdictBadgeHtml(v){\n+    const [bg, fg, border] = VERDICT_COLOR[v] || ['oklch(0.96 0.02 250)', 'var(--ink-dim)', 'var(--line)'];\n+    return `<span class=\"adr-state-badge\" style=\"background:${bg};color:${fg};border-color:${border};\">${escapeHtml((v || 'unknown').toUpperCase())}</span>`;\n+  }\n+\n+  function asmQuestionHtml(kicker, block, extraLabel, extraKey){\n+    if (!block || !block.answer) return '';\n+    const ev = (block.evidence || []).filter(Boolean);\n+    return `\n+      <div class=\"adr-block\">\n+        <div class=\"ab-kicker\" style=\"color:var(--accent-text);\">${escapeHtml(kicker)}</div>\n+        <p>${escapeHtml(block.answer)}</p>\n+        ${extraKey && block[extraKey] ? `<p style=\"margin-top:8px;\"><strong>${escapeHtml(extraLabel)}:</strong> ${escapeHtml(block[extraKey])}</p>` : ''}\n+        ${ev.length ? `<div class=\"chip-row\" style=\"margin-top:10px;\">${ev.map(e => `<span class=\"force-chip\">${escapeHtml(e)}</span>`).join('')}</div>` : ''}\n+      </div>`;\n+  }\n+\n+  function renderAssessmentSheet(){\n+    const p = pr();\n+    const a = p && p.assessment;\n+    const intent = (p && p.intent) || null;\n+    const titleEl = document.getElementById('assessment-sheet-title');\n+    const metaEl = document.getElementById('assessment-sheet-meta');\n+    const body = document.getElementById('assessment-sheet-body');\n+\n+    if (!a){\n+      titleEl.textContent = 'Assessment';\n+      metaEl.innerHTML = '';\n+      body.innerHTML = `\n+        <div class=\"empty-card\">\n+          <div class=\"empty-detail\">No assessment recorded for this PR. Run <code>/prodyssey:submit</code> against it.</div>\n+        </div>`;\n+      return;\n+    }\n+\n+    titleEl.textContent = `PR #${p.pr} — assessment`;\n+    const metaBits = [\n+      a.risk_tier ? `risk: ${a.risk_tier}` : '',\n+      a.stage === 'post' ? 'post-merge' : 'pre-merge',\n+      a.generated ? a.generated : '',\n+    ].filter(Boolean).join(' · ');\n+    metaEl.innerHTML = verdictBadgeHtml(a.verdict)\n+      + (metaBits ? `<span class=\"adr-sheet-sourceline\">${escapeHtml(metaBits)}</span>` : '');\n+\n+    const summaryHtml = a.summary ? `\n+      <div class=\"adr-block decision\">\n+        <div class=\"ab-kicker\">Summary</div>\n+        <p>${escapeHtml(a.summary)}</p>\n+      </div>` : '';\n+\n+    // The author's own words come first — the assessment is a response to them.\n+    let intentHtml = '';\n+    if (intent && intent.problem){\n+      const flags = [\n+        intent.source === 'inferred' ? 'intent inferred, not stated by the author' : '',\n+        intent.authorship && intent.authorship !== 'human' ? `authorship: ${intent.authorship}` : '',\n+        (intent.unknowns || []).length ? `${intent.unknowns.length} author-flagged unknown(s)` : '',\n+      ].filter(Boolean);\n+      intentHtml = `\n+        <div class=\"adr-block problem\">\n+          <div class=\"ab-kicker\">The author's stated problem</div>\n+          <p>${escapeHtml(intent.problem)}</p>\n+          ${flags.length ? `<div class=\"chip-row\" style=\"margin-top:10px;\">${flags.map(f => `<span class=\"force-chip\">${escapeHtml(f)}</span>`).join('')}</div>` : ''}\n+        </div>`;\n+    }\n+\n+    let patternHtml = '';\n+    if (a.pattern && a.pattern.answer){\n+      const label = PATTERN_LABEL[a.pattern.verdict] || a.pattern.verdict || '';\n+      const dupes = (a.pattern.duplicates || []).filter(Boolean);\n+      patternHtml = asmQuestionHtml('Q3 — pattern, duplicate, or reinvention', a.pattern);\n+      patternHtml = patternHtml.replace(\n+        '<p>',\n+        `${label ? `<p style=\"margin-bottom:8px;\"><strong>${escapeHtml(label)}</strong></p>` : ''}<p>`\n+      );\n+      if (dupes.length){\n+        patternHtml = patternHtml.replace(\n+          '</div>\\n      </div>',\n+          `</div>\\n        <div class=\"asm-delta\" style=\"margin-top:8px;\">duplicates: ${dupes.map(d => escapeHtml(d)).join(', ')}</div>\\n      </div>`\n+        );\n+      }\n+    }\n+\n+    const findings = a.findings || [];\n+    const order = { blocker: 0, concern: 1, note: 2 };\n+    const sorted = findings.slice().sort((x, y) => (order[x.severity] ?? 3) - (order[y.severity] ?? 3));\n+    const findingCard = (f) => `\n+      <div class=\"asm-finding ${escapeHtml(f.severity || 'note')}\">\n+        <div class=\"asm-sev\">${escapeHtml(f.severity || 'note')}${f.district ? ` · ${escapeHtml(f.district)}` : ''}</div>\n+        <div class=\"asm-claim\">${escapeHtml(f.claim || '')}</div>\n+        ${f.evidence ? `<div class=\"asm-ev\">${escapeHtml(f.evidence)}</div>` : ''}\n+        ${f.suggestion ? `<div class=\"asm-fix\">${escapeHtml(f.suggestion)}</div>` : ''}\n+      </div>`;\n+    let findingsHtml = '';\n+    if (sorted.length){\n+      // Same +N more disclosure the ADR sheet uses for rejected alternatives.\n+      const visible = sorted.slice(0, 3);\n+      const hidden = sorted.slice(3);\n+      findingsHtml = `\n+        <div>\n+          <div class=\"alt-section-label\">Findings</div>\n+          <div class=\"asm-stack\">${visible.map(findingCard).join('')}</div>\n+          ${hidden.length ? `\n+            <button class=\"link-btn\" id=\"asm-findings-more-btn\" style=\"margin-top:8px;\">+${hidden.length} more</button>\n+            <div class=\"asm-stack\" id=\"asm-findings-hidden\" hidden style=\"margin-top:8px;\">${hidden.map(findingCard).join('')}</div>\n+          ` : ''}\n+        </div>`;\n+    }\n+\n+    const checks = a.boundary_checks || [];\n+    const checksHtml = checks.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">Boundary checks</div>\n+        <div class=\"asm-stack\">${checks.map(c => `\n+          <div class=\"asm-check ${escapeHtml(c.result || '')}\">\n+            <span class=\"asm-res\">${escapeHtml(c.result || '?')}</span>\n+            <span>${escapeHtml(c.rule || '')}${c.source ? ` <span class=\"adr-sheet-sourceline\">(${escapeHtml(c.source)})</span>` : ''}</span>\n+          </div>`).join('')}</div>\n+      </div>` : '';\n+\n+    const d = a.delta || {};\n+    const deltaLines = []\n+      .concat((d.districts_added || []).map(x => `+ district ${x}`))\n+      .concat((d.districts_changed || []).map(x => `~ ${x.id}: ${x.files_before} → ${x.files_after} files`))\n+      .concat((d.edges_added || []).map(x => `+ edge ${x}`))\n+      .concat((d.edges_removed || []).map(x => `- edge ${x}`));\n+    const deltaHtml = deltaLines.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">District delta</div>\n+        <div class=\"asm-delta\">${deltaLines.map(l => escapeHtml(l)).join('<br>')}</div>\n+      </div>` : '';\n+\n+    const drift = a.drift || [];\n+    const driftHtml = drift.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">Drift from the stated intent</div>\n+        <div class=\"asm-stack\">${drift.map(x => `\n+          <div class=\"asm-finding concern\">\n+            <div class=\"asm-sev\">${escapeHtml(DRIFT_LABEL[x.kind] || x.kind || '')}</div>\n+            <div class=\"asm-claim\">${escapeHtml(x.claim || '')}</div>\n+            ${x.evidence ? `<div class=\"asm-ev\">${escapeHtml(x.evidence)}</div>` : ''}\n+          </div>`).join('')}</div>\n+      </div>` : '';\n+\n+    const regretHtml = a.regret_risk ? `\n+      <div class=\"adr-block\">\n+        <div class=\"ab-kicker\" style=\"color:oklch(0.5 0.15 25);\">Will we regret this?</div>\n+        <p>${escapeHtml(a.regret_risk)}</p>\n+      </div>` : '';\n+\n+    body.innerHTML = `\n+      ${summaryHtml}\n+      ${intentHtml}\n+      ${asmQuestionHtml('Q1 — is this sensible?', a.sensible)}\n+      ${asmQuestionHtml('Q2 — maintainability and readability', a.maintainability, 'Constraint introduced', 'constraint_introduced')}\n+      ${patternHtml}\n+      ${regretHtml}\n+      ${findingsHtml}\n+      ${checksHtml}\n+      ${deltaHtml}\n+      ${driftHtml}\n+    `;\n+\n+    const moreBtn = document.getElementById('asm-findings-more-btn');\n+    if (moreBtn){\n+      moreBtn.addEventListener('click', () => {\n+        document.getElementById('asm-findings-hidden').hidden = false;\n+        moreBtn.hidden = true;\n+      });\n+    }\n+  }\n+\n   document.getElementById('tb-prev').addEventListener('click', () => selectPR(state.prIndex - 1));\n   document.getElementById('tb-next').addEventListener('click', () => selectPR(state.prIndex + 1));\n \n@@ -2371,6 +2654,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   document.getElementById('btn-comments-toggle').addEventListener('click', openCommentsDrawer);\n   document.getElementById('drawer-close-btn').addEventListener('click', closeCommentsDrawer);\n   document.getElementById('adr-sheet-close-btn').addEventListener('click', closeAdrSheet);\n+  document.getElementById('assessment-sheet-close-btn').addEventListener('click', closeAssessmentSheet);\n   document.getElementById('drawer-scrim').addEventListener('click', closeAnyOpenSheet);\n   document.getElementById('btn-add-comment').addEventListener('click', addCommentOnly);\n   commentInput.addEventListener('keydown', (e) => { if (e.key === 'Enter'){ e.preventDefault(); addCommentOnly(); } });\n@@ -2393,7 +2677,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   window.addEventListener('keydown', (e) => {\n     // Escape must close whichever sheet is open, even while the comment input is\n     // focused — checked before the input/textarea early-return below so it always works.\n-    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n+    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n \n     const tag = document.activeElement && document.activeElement.tagName;\n     if (tag === 'TEXTAREA' || tag === 'INPUT') return;",
  "CLAUDE.md": "diff --git a/CLAUDE.md b/CLAUDE.md\nindex bc47acb..90b34df 100644\n--- a/CLAUDE.md\n+++ b/CLAUDE.md\n@@ -11,6 +11,13 @@ scene art, voice narration, and retro-extracted ADRs, in a portable HTML\n viewer. The target is the session's own repo, or any other checkout reached\n through `--repo`.\n \n+Submit mode is the one part that does not narrate history. It runs before the\n+history exists — it interviews the author of a change, assesses that change\n+against the bundle, and opens the pull request. It exists because the rest of\n+the plugin spends real effort reconstructing intent that nobody wrote down.\n+Capture the intent at submit time, and generate mode stops guessing. See\n+Submit mode below, and `skills/odyssey/references/{interview-guide,review-mode}.md`.\n+\n Where the bundle lands depends on the target. Analyzing your own repo writes\n to `<target>/.prodyssey/self/` — that is the case with no `--repo`, or with\n `--repo` that resolves to the session's own checkout. Analyzing a foreign\n@@ -39,21 +46,24 @@ files.\n ```\n .claude-plugin/       plugin.json (manifest) + marketplace.json\n commands/              thin dispatchers: baseline.md, generate.md, view.md,\n-                       publish.md, view.md → Skill(\"odyssey\", args=...)\n+                       publish.md, submit.md → Skill(\"odyssey\", args=...)\n skills/\n   odyssey/\n     SKILL.md          orchestration: prereq gate → baseline → per-PR sweep →\n-                       view → publish → verify\n+                       submit → view → publish → verify\n     references/       loaded on demand (story-mode, decision-records-lite,\n-                       baseline-derivation, diagram-mode, adr-template, stacks/*)\n+                       baseline-derivation, diagram-mode, review-mode,\n+                       interview-guide, adr-template, pr-description-template,\n+                       stacks/*)\n   mermaid/            authoring rules for the Mermaid diagrams below; not\n                        invoked by the orchestrating Claude directly — the\n                        per-PR diagram-authoring subagent invokes it as\n                        Skill(\"prodyssey:mermaid\")\n-scripts/               9 PEP-723 uv scripts, called by the skill, never edited by it:\n+scripts/              11 PEP-723 uv scripts, called by the skill, never edited by it:\n                        extract_story.py, generate_prompts.py, generate_audio.py,\n                        extract_diffs.py, build_diagrams.py, verify_bundle.py,\n-                       export_artifact.py, export_index.py, record_publish.py\n+                       export_artifact.py, export_index.py, record_publish.py,\n+                       migrate_bundle.py, render_review.py\n viewer/index.html      the bundle viewer (~2000 lines, single file, see below)\n ```\n \n@@ -87,6 +97,45 @@ them.\n Scripts are PEP 723 (`uv run script.py` resolves `google-genai`, `pillow`,\n `python-dotenv` inline — no venv, no `requirements.txt`).\n \n+## Submit mode — what is load-bearing about it\n+\n+`/prodyssey:submit` interviews a change's author, assesses the change, and\n+opens the pull request. Four things about it are easy to break by accident.\n+\n+**The interview is the product.** `references/interview-guide.md` §2 says\n+never ask a question the evidence already answers, and §3 caps the count at\n+six. A future session that turns this into a fixed questionnaire has removed\n+the only thing that separates it from a generic diff-reading reviewer. The\n+mode reads the diff, the districts, the ADRs, and the stack card *before* it\n+asks anything.\n+\n+**The PR number comes from opening the PR.** `story.json` keys on an integer\n+`pr`, and `verify_bundle.py`, `record_publish.py`, `manifest.js`, and the\n+viewer all depend on that. A working branch therefore cannot enter the\n+timeline. Rather than mint a synthetic key, the pre stage ends with\n+`gh pr create` and files the result under the real number. Before that, the\n+content stages in `<bundle-dir>/exports/branch-<slug>/`. Do not \"fix\" this by\n+inventing a branch key.\n+\n+**Pushing and opening a PR are the only actions outside `.prodyssey/`,** and\n+they only run after an explicit confirmation. Nothing else on GitHub is in\n+scope: no comments, no edits to an existing PR body, no labels, no reviewers,\n+no merges. `--non-interactive` cannot open a PR at all, because there is\n+nobody there to confirm.\n+\n+**`intent` and `assessment` live on the timeline entry, not in a file of\n+their own.** That is what puts them under `migrate_bundle.py`'s\n+authored-field guard — both names are in `AUTHORED_TIMELINE_FIELDS` — and it\n+is why the viewer reads them off `window.STORY` with no new global and no new\n+`<script src>` tag. A separate `reviews.json` would repeat the `adrs.json`\n+debt described under Bundle versioning below.\n+\n+Assessment is Claude judgment work, like narrative and ADRs.\n+`scripts/render_review.py` lays the result out as markdown and judges none of\n+it. `verify_bundle.py` gained `intent` and `assessment` keys that are\n+**optional by default** — a bundle generated before this mode existed must\n+keep passing — and `--require-review` promotes them.\n+\n ## The viewer is not self-contained — this matters for anything artifact-related\n \n `viewer/index.html` is a normal multi-file web page in disguise: one HTML\n@@ -184,6 +233,9 @@ and migration below.\n   inventory.yaml\n   viewer/index.html\n   exports/{publish-manifest.json, pr-{N}.html…, index.html}   # written by /prodyssey:publish\n+  exports/pr-{N}-{description,assessment}.md                  # written by /prodyssey:submit\n+  exports/branch-{slug}/{diff,intent,assessment}.json         # /prodyssey:submit, pre-PR staging\n+  exports/branch-{slug}/{description,assessment}.md\n   .migration-backup/  # pre-migration story.json snapshots, written by migrate_bundle.py\n ```\n \n@@ -198,13 +250,13 @@ flag it was generated with (see Generate mode in `skills/odyssey/SKILL.md`).\n `exports/` appears only after `/prodyssey:publish` runs at least once. It is\n as committable as the rest of the bundle — see Publish mode notes below.\n \n-`story.json`'s `meta.schema_version` is currently `\"1.1\"`, and it is the\n+`story.json`'s `meta.schema_version` is currently `\"1.2\"`, and it is the\n source of truth for a bundle's data shape. `bundle.json` only mirrors it.\n `scripts/_bundle_meta.py` holds the constant, and `verify_bundle.py` gates\n-on it through `SCHEMA_VERSION_KNOWN`. That set also accepts `\"1.0\"`, so\n+on it through `SCHEMA_VERSION_KNOWN`. That set also accepts `\"1.0\"` and `\"1.1\"`, so\n migration can still read an older bundle.\n \n-This repo commits everything under `.prodyssey/`. Only four bookkeeping\n+This repo commits everything under `.prodyssey/`. Only five bookkeeping\n entries are gitignored:\n \n - `.prodyssey/.view-server.pid` and `.prodyssey/.view-server.log`, which are\n@@ -213,6 +265,11 @@ entries are gitignored:\n   breaks every other clone and churns the diff on each view switch.\n - Each bundle's `.migration-backup/`, which holds pre-migration `story.json`\n   snapshots. They are disposable once a migration proves sound.\n+- `exports/branch-*/diff.json`, submit mode's pre-PR diff cache. It is exactly\n+  reproducible from `git diff <merge-base>..<head>`, and it is\n+  self-referential — committing it into the branch it diffs rewrites it on\n+  every commit, and each version would then contain the last one. The authored\n+  `intent.json` and `assessment.json` beside it are committed.\n \n The self-bundle and the foreign-repo cache used to live under two separate\n top-level directories. One `.prodyssey/` root now holds both, and the\n@@ -262,8 +319,8 @@ ways, and each way needs its own fix, not one migration framework:\n \n `migrate_bundle.py` runs all three phases against `--bundle-dir`, in this\n order: the unconditional viewer refresh, then the layout ladder, then the\n-data ladder. `skills/odyssey/SKILL.md` calls it at the start of all four\n-modes — Baseline, Generate, View, and Publish. A stale bundle therefore\n+data ladder. `skills/odyssey/SKILL.md` calls it at the start of all five\n+modes — Baseline, Generate, Submit, View, and Publish. A stale bundle therefore\n self-heals before any other step reads it.\n \n A data migration must never regenerate content. `story.json` holds\n@@ -308,7 +365,7 @@ hand.\n - `extract_story.py` never overwrites an authored narrative field for a PR\n   already in `story.json`. A new PR gets a minimal stub. A second run is\n   safe.\n-- `--repo <path>` works on the skill and on all four commands. It targets\n+- `--repo <path>` works on the skill and on all five commands. It targets\n   any local checkout, not only the session's own working directory. The Hub\n   resolution storage rule decides where the bundle lands:\n   `<target>/.prodyssey/self/` for self-analysis, or\n@@ -351,6 +408,10 @@ resolution / central storage (`--store`, `.prodyssey/`, `view` command) →\n unification of the two former bundle-storage roots, with the self-bundle\n moved to `.prodyssey/self/` → authored Mermaid diagrams as an `--art`\n alternative to Gemini scene art, adding the `mermaid` skill and\n-`build_diagrams.py` (see `git log` for the WS-A/B/C/D workstream commits).\n+`build_diagrams.py` (see `git log` for the WS-A/B/C/D workstream commits) →\n+`submit` mode, which reverses part of the review-mode exclusion recorded in\n+`README.md`'s extraction manifest and adds schema 1.2, the\n+`interview-guide`/`review-mode` references, `render_review.py`, and the\n+viewer's assessment sheet.\n No test suite, no CI config, no package manager — this is prose + Python\n scripts + one HTML file.",
  "README.md": "diff --git a/README.md b/README.md\nindex 0664fa0..392cb55 100644\n--- a/README.md\n+++ b/README.md\n@@ -120,6 +120,80 @@ environment. If Claude lacks read access to the path, grant it once with\n `/add-dir ~/code/other-project`. The bundle itself does not land inside that\n repo. See [Multiple repos](#multiple-repos) below for where it goes.\n \n+---\n+\n+## Reviewing before you merge\n+\n+The four commands above narrate history. This one runs before the history\n+exists.\n+\n+```\n+/prodyssey:submit\n+```\n+\n+Run it on a branch that has no pull request. Prodyssey reads the diff, the\n+district map, and every architecture decision the repo already recorded. It\n+then asks you only what that evidence cannot answer, usually four to six\n+questions. It writes your answers down, assesses the change, and opens the\n+pull request with a description built from what you said.\n+\n+The assessment answers three questions that a diff cannot:\n+\n+1. **Is this sensible?** Does the change solve the problem you state, and does\n+   that problem belong here?\n+2. **Does it help or hurt maintainability?** It names the invariant the change\n+   establishes, in the same words an ADR uses.\n+3. **New pattern, duplicate, or reinvention?** This is the question the bundle\n+   exists to answer. A `duplicate` or `reinvention` verdict must cite the ADR\n+   or the district it duplicates. Without a citation, there is no verdict.\n+\n+Then it answers the one a senior reviewer actually asks: **will we regret\n+this?**\n+\n+The verdict is `sound`, `concerns`, or `rework`. **It never blocks a merge.**\n+A `rework` verdict gives you three options. Open the PR, open it as a draft,\n+or fix the change first. You choose.\n+\n+### It asks who wrote the code\n+\n+Prodyssey records whether the change is `human`, `agent-assisted`, or\n+`agent-generated`. It also records the parts you cannot explain. \"The agent\n+wrote that, and I am not sure why\" is a useful answer, not a failed interview.\n+Those notes raise the risk tier and go in the PR description where a reviewer\n+reads them. Code that nobody can explain costs a team more than the same code\n+with an author who can.\n+\n+### What it writes, and what it opens\n+\n+Everything lands in the bundle. The PR description and the assessment go to\n+`<bundle-dir>/exports/` as markdown. The two structured blocks go onto the\n+PR's timeline entry in `story.json`. The viewer shows the assessment on level\n+3, behind a badge next to the ADR chips.\n+\n+Opening the pull request is the only thing Prodyssey does outside\n+`.prodyssey/`. It shows you the description and asks first. It does nothing\n+else on GitHub. Use `--no-create` to stop before that and keep the files.\n+\n+```\n+/prodyssey:submit --no-create          # write the files, open nothing\n+/prodyssey:submit --draft              # open it as a draft\n+/prodyssey:submit --base develop       # a base branch other than the default\n+/prodyssey:submit --prs 73             # review an open PR instead of creating one\n+/prodyssey:submit --prs 73 --stage post\n+```\n+\n+`--stage post` runs after the merge. It compares what shipped against what\n+you said before it shipped. Scope you declared out of bounds and touched\n+anyway. Risks that never got a guard. Options you rejected that the code\n+adopted. It never rewrites what you said earlier.\n+\n+### It makes the story better\n+\n+The intent captured here stays in the bundle. When `/prodyssey:generate` runs\n+on that PR later, it reads your stated problem and your rejected alternatives\n+instead of inferring them from the diff. The ADR it extracts is marked\n+`provenance: authored`, not `inferred`.\n+\n ## Viewing the result\n \n - **Bundled viewer**: `/prodyssey:view` starts one long-lived `python3 -m\n@@ -226,12 +300,26 @@ repos](#multiple-repos) below.\n   inventory.yaml\n   viewer/index.html\n   exports/{publish-manifest.json, pr-{N}.html…, index.html}   # written by /prodyssey:publish\n+  exports/pr-{N}-{description,assessment}.md                  # written by /prodyssey:submit\n+  exports/branch-{slug}/{diff,intent,assessment}.json         # /prodyssey:submit, before a PR exists\n+  exports/branch-{slug}/{description,assessment}.md\n ```\n \n The `diagrams/` and `assets/` entries depend on the `--art` mode that\n generated the PR. `--art diagram` writes only `.mmd` files and no PNGs.\n `--art image` writes only PNGs. `--art both`, the default, writes both.\n \n+`/prodyssey:submit` also writes two blocks onto the PR's own timeline entry\n+in `story.json`: `intent`, which holds what the author said, and\n+`assessment`, which holds the judgment written against it. They live there,\n+and not in a file of their own, so the migration guard protects them the way\n+it protects the narrative.\n+\n+A `branch-{slug}/` directory is the staging area for a branch that has no\n+pull request yet. Once the PR opens, the same content moves into\n+`story.json` under the real PR number, and the staging directory is a\n+leftover you can delete.\n+\n Commit the bundle, and a share link is only the raw GitHub URL. You can\n also import the bundle into the viewer directly, by upload or by local\n path. The `schema_version` field gates compatibility (AC G5). A bundle\n@@ -254,15 +342,19 @@ prodyssey/\n │   ├── baseline.md           # thin: invokes the skill with args=\"baseline\"\n │   ├── generate.md           # thin: invokes the skill with args=\"generate --prs ...\"\n │   ├── view.md                # thin: invokes the skill with args=\"view ...\"\n-│   └── publish.md             # thin: invokes the skill with args=\"publish --prs ...\"\n+│   ├── publish.md             # thin: invokes the skill with args=\"publish --prs ...\"\n+│   └── submit.md              # thin: invokes the skill with args=\"submit ...\"\n ├── skills/\n │   ├── odyssey/\n-│   │   ├── SKILL.md          # orchestration: prereq gate → baseline → per-PR sweep → view → publish → verify\n+│   │   ├── SKILL.md          # orchestration: prereq gate → baseline → per-PR sweep → submit → view → publish → verify\n │   │   └── references/       # extracted from architecture-review-design-maintenance (see below)\n │   │       ├── story-mode.md\n │   │       ├── decision-records-lite.md\n │   │       ├── baseline-derivation.md      # describe-lite: district + inventory procedure\n │   │       ├── diagram-mode.md             # authoring rules for the per-PR Mermaid diagrams\n+│   │       ├── review-mode.md              # submit mode: the three questions, verdicts, risk tiers\n+│   │       ├── interview-guide.md          # submit mode: what to ask the author, and what not to\n+│   │       ├── pr-description-template.md  # the PR body skeleton render_review.py fills\n │   │       ├── adr-template.md\n │   │       └── stacks/{README,nextjs,react-typescript,python-fastapi,generic}.md\n │   └── mermaid/               # authoring rules for Mermaid diagrams, invoked by the\n@@ -278,7 +370,8 @@ prodyssey/\n │   ├── verify_bundle.py       # schema_version + completeness check (drives resumability)\n │   ├── export_artifact.py     # flattens one PR into a self-contained artifact-safe HTML\n │   ├── export_index.py        # renders the cross-PR index artifact from publish-manifest.json\n-│   └── record_publish.py      # records a published Artifact URL back into publish-manifest.json\n+│   ├── record_publish.py      # records a published Artifact URL back into publish-manifest.json\n+│   └── render_review.py       # lays out the captured intent + assessment as two markdown files\n ├── viewer/\n │   └── index.html            # portable bundle viewer\n └── README.md                 # this file\n@@ -289,7 +382,7 @@ Key manifest fields (`plugin.json`):\n ```json\n {\n   \"name\": \"prodyssey\",\n-  \"version\": \"0.2.0\"\n+  \"version\": \"0.3.0\"\n }\n ```\n \n@@ -321,6 +414,7 @@ Contact the author for more information.\n | `references/architecture-documentation.md` | `references/baseline-derivation.md` | Keeps describe-mode's verification discipline as the inventory procedure: enumerate modules, grep import edges in both directions, and never assert a boundary that is not verified. Drops the 8-section canvas, `boundary.yaml` authoring, and INVENTORY.md bookkeeping, replaced by one flat `inventory.yaml` file |\n | `references/stacks/*` (4 cards + README) | `references/stacks/*` | Kept verbatim. Detection precedence and ADR-topic checklists drive stack detection and extraction prompts |\n | `references/templates/adr-template.md` | `references/adr-template.md` | Trimmed frontmatter (no state machine fields) |\n+| `references/stacks/*`'s `## Boundary Rules` and `## Review Checks` | `references/review-mode.md` | The card sections were extracted with the cards, and nothing read them until now. Submit mode runs the literal grep commands in `Boundary Rules` and records each result. It never reads `## Corpus Load`, because those paths do not ship with this plugin |\n | `docs/prototypes/codebase-evolution/data/extract_story.py` | `scripts/extract_story.py` | Generalized. Takes a repo path as a parameter, selects a PR by number through merge-commit lookup, writes to `<bundle-dir>/`, and never overwrites an authored or generated narrative field |\n | `docs/prototypes/codebase-evolution/nanobanana/generate_prompts.py` | `scripts/generate_prompts.py` | Reads district and world data from the bundle instead of hand-authored `story.json` fields |\n | `utils/generate_audio.py` | `scripts/generate_audio.py` | Keeps the same flow: voice scripts feed Gemini TTS. The output path changes to `<bundle-dir>/data/audio/` |\n@@ -329,7 +423,7 @@ Contact the author for more information.\n \n | Not extracted | Why |\n |---|---|\n-| **review / maintenance modes** + `saas-checklist.md`, `harness-security.md`, report templates, `compute_scores.py` | An audit instrument for maintainers, out of scope for Prodyssey (consensus 7/7) |\n+| **maintenance mode** + `saas-checklist.md`, `harness-security.md`, report templates, `compute_scores.py` | An audit instrument for maintainers, out of scope for Prodyssey (consensus 7/7). **Partly reversed.** `/prodyssey:submit` now reviews a change, and it is a different instrument: it interviews the author, and it answers three judgment questions against this bundle's own districts and ADRs. What stayed out is what made the original an audit — the score, the corpus, the checklists, and the gate. Submit mode consumes the stack cards' `## Boundary Rules` and `## Review Checks` sections, which `stacks/README.md` already declared as review inputs |\n | **corpus/** (~170 principle YAMLs) + **books/** (14 vendored volumes) | Grounding for audit depth. Story generation needs the style rules and the record shapes, not the review corpus, and dropping it keeps the plugin download small |\n | **decisions-mode governance** (state machine, viewpoints, ADR numbering) | Governs a living doc set in a repo you own. Prodyssey generates immutable bundle records instead |\n | **describe-mode full canvas** | The flat inventory serves as the `maps_to` anchor. The full canvas adds documentation-program overhead that a foreign repo cannot sustain |",
  "commands/submit.md": "diff --git a/commands/submit.md b/commands/submit.md\nnew file mode 100644\nindex 0000000..c893fb4\n--- /dev/null\n+++ b/commands/submit.md\n@@ -0,0 +1,75 @@\n+---\n+title: \"Odyssey: Submit PR with Review\"\n+status: active\n+type: command\n+last_verified: 2026-08-04\n+---\n+\n+# Odyssey: Submit PR with Review\n+\n+Interviews the author of a change, assesses the change against the bundle, and\n+opens the pull request with the generated description as its body. Answers three\n+questions a diff cannot: is this sensible, does it help or hurt maintainability,\n+and does it introduce a valuable pattern or duplicate one the repo already has.\n+The captured intent stays in `.prodyssey/`, so `/prodyssey:generate` later reads\n+what the author said instead of inferring it.\n+\n+Invoke the `odyssey` skill in submit mode, forwarding any arguments the user\n+supplied after `/prodyssey:submit` (`--repo <path>`, `--store local|central`,\n+`--prs`, `--stage pre|post`, `--branch <ref>`, `--base <branch>`, `--draft`,\n+`--no-create`, `--non-interactive`):\n+\n+```\n+Skill(\"odyssey\", args=\"submit $ARGUMENTS\")\n+```\n+\n+Run it with no arguments on a branch that has no PR yet, and it interviews you\n+and opens the PR. Run it with `--prs <N>` against an open PR, and it reviews\n+that PR without creating anything.\n+\n+## What this opens\n+\n+This is the one command in the plugin that acts outside `.prodyssey/`. On a\n+branch with no pull request, it pushes the branch and runs `gh pr create` —\n+after showing you the description, the base branch, and the verdict, and asking\n+first. Nothing is pushed or opened without that confirmation.\n+\n+It does nothing else on GitHub. No comments, no edits to an existing PR body, no\n+labels, no reviewers, no merges. `--no-create` stops before the push and leaves\n+the description and the assessment in `<bundle-dir>/exports/branch-<slug>/`.\n+\n+## Stages\n+\n+`--stage pre` (default) runs before the merge: interview, assess, open the PR.\n+\n+`--stage post` runs after it, and compares what actually shipped against what\n+the author said before it shipped — scope that was declared out and touched\n+anyway, risks that never got a guard, rejected options the merged code adopted.\n+It never rewrites the intent captured earlier.\n+\n+## Requirements\n+\n+- A git repo, `uv` on PATH, and `gh` authenticated if you want the PR opened.\n+  Without `gh`, the command still writes both markdown files and prints the\n+  `gh pr create` line for you to run.\n+- No `GEMINI_API_KEY`. Submit mode generates no art and no audio.\n+- A baseline helps and is not required. The duplicate-or-reinvention check reads\n+  `inventory.yaml` and `data/adrs.json`, so a bundle with little history reports\n+  \"insufficient history\" rather than guessing.\n+\n+## Verdicts\n+\n+`sound`, `concerns`, or `rework`. A `rework` verdict never blocks the PR — it is\n+reported, and you decide whether to open it, open it as a draft, or fix first.\n+\n+## Examples\n+\n+```\n+/prodyssey:submit\n+/prodyssey:submit --draft\n+/prodyssey:submit --no-create\n+/prodyssey:submit --base develop\n+/prodyssey:submit --prs 73\n+/prodyssey:submit --prs 73 --stage post\n+/prodyssey:submit --repo ~/code/other-project --prs 12\n+```",
  "scripts/_bundle_meta.py": "diff --git a/scripts/_bundle_meta.py b/scripts/_bundle_meta.py\nindex 40f3f6a..3599c53 100644\n--- a/scripts/_bundle_meta.py\n+++ b/scripts/_bundle_meta.py\n@@ -17,6 +17,6 @@ CURRENT_BUNDLE_FORMAT - current directory-layout version\n \"\"\"\n from __future__ import annotations\n \n-SCHEMA_VERSION = \"1.1\"\n-SCHEMA_VERSION_KNOWN = {\"1.0\", \"1.1\"}\n+SCHEMA_VERSION = \"1.2\"\n+SCHEMA_VERSION_KNOWN = {\"1.0\", \"1.1\", \"1.2\"}\n CURRENT_BUNDLE_FORMAT = 1",
  "scripts/extract_diffs.py": "diff --git a/scripts/extract_diffs.py b/scripts/extract_diffs.py\nindex 5754ecb..15350de 100755\n--- a/scripts/extract_diffs.py\n+++ b/scripts/extract_diffs.py\n@@ -22,9 +22,21 @@ The diff is split per file (on `diff --git a/... b/...` boundaries), each\n file's diff capped at 4000 lines with a truncation marker, and written as a\n namespaced JS file: `window.DIFFS_BY_PR[N] = {\"<path>\": \"<diff text>\", ...}`.\n \n+`--branch` is the pre-submit path: it diffs a working branch that has no PR\n+number yet (`git diff <merge-base(base, ref)>..<ref>`), which is what submit\n+mode's interview reads before the PR exists. It writes JSON to\n+`<bundle-dir>/exports/branch-<slug>/diff.json` and touches NOTHING under\n+`data/` — story.json keys on an integer `pr`, so a branch cannot enter the\n+timeline, and a synthetic key would leak into verify_bundle.py, the viewer,\n+and the publish manifest. The PR number arrives when submit mode opens the\n+PR; the content is filed under it then. A branch diff always overwrites,\n+because the branch tip is the point.\n+\n Usage:\n     uv run extract_diffs.py --repo <path> --prs 73,75\n     uv run extract_diffs.py --repo <path> --prs 73 --force\n+    uv run extract_diffs.py --repo <path> --branch\n+    uv run extract_diffs.py --repo <path> --branch feature/x --base develop\n \"\"\"\n from __future__ import annotations\n \n@@ -77,6 +89,14 @@ def run_git(repo: Path, args: list[str]) -> str:\n     return subprocess.check_output([\"git\", \"-C\", str(repo)] + args, text=True)\n \n \n+def run_git_quiet(repo: Path, args: list[str]) -> str:\n+    \"\"\"run_git for a probe whose failure is expected and handled. Keeps git's\n+    own stderr off the user's terminal.\"\"\"\n+    return subprocess.check_output(\n+        [\"git\", \"-C\", str(repo)] + args, text=True, stderr=subprocess.DEVNULL\n+    )\n+\n+\n def get_remote_origin(repo: Path) -> str | None:\n     try:\n         return run_git(repo, [\"remote\", \"get-url\", \"origin\"]).strip()\n@@ -88,7 +108,10 @@ def detect_default_branch(repo: Path) -> str:\n     \"\"\"Detect the repo's default branch: origin/HEAD symref first, then try\n     `main` and `master` directly. Exits 1 with remediation if none resolve.\"\"\"\n     try:\n-        out = run_git(repo, [\"symbolic-ref\", \"--short\", \"refs/remotes/origin/HEAD\"]).strip()\n+        # This is a probe, and a repo with no origin/HEAD symref is the normal\n+        # case, not an error — swallow git's \"fatal:\" so it never reaches the\n+        # user's terminal on the way to the `main`/`master` fallback below.\n+        out = run_git_quiet(repo, [\"symbolic-ref\", \"--short\", \"refs/remotes/origin/HEAD\"]).strip()\n         if out.startswith(\"origin/\"):\n             out = out[len(\"origin/\"):]\n         if out:\n@@ -248,6 +271,88 @@ def resolve_prs(repo: Path, pr_nums: list[int], dot_range: str | None) -> dict[i\n     return resolved\n \n \n+# ---- working-branch resolution (no PR number yet) ----\n+\n+def slugify(name: str) -> str:\n+    \"\"\"Same rule as SKILL.md's Hub resolution slug: lowercase, every\n+    non-alphanumeric run collapsed to one hyphen, no leading/trailing\n+    hyphen.\"\"\"\n+    out = re.sub(r\"[^a-z0-9]+\", \"-\", name.lower())\n+    return out.strip(\"-\") or \"branch\"\n+\n+\n+def resolve_branch(repo: Path, ref: str, base: str | None) -> dict:\n+    \"\"\"Resolve a working branch against its base. Returns the same\n+    {hash, kind, diff_base} shape the open-PR path produces, plus the\n+    branch name, its slug, and the base it was diffed against.\"\"\"\n+    base_ref = base or detect_default_branch(repo)\n+\n+    try:\n+        head_sha = run_git(repo, [\"rev-parse\", ref]).strip()\n+    except subprocess.CalledProcessError:\n+        print(\n+            f\"error: '{ref}' does not resolve to a commit in this repo.\\n\"\n+            \"remediation: pass --branch <existing-branch>, or omit it to use HEAD.\",\n+            file=sys.stderr,\n+        )\n+        sys.exit(1)\n+\n+    # A meaningful slug needs the branch NAME, not \"HEAD\". Detached HEAD has no\n+    # name, so fall back to the short sha rather than slugging \"head\".\n+    name = ref\n+    if ref == \"HEAD\":\n+        try:\n+            name = run_git(repo, [\"rev-parse\", \"--abbrev-ref\", \"HEAD\"]).strip()\n+        except subprocess.CalledProcessError:\n+            name = \"\"\n+        if not name or name == \"HEAD\":\n+            name = head_sha[:8]\n+\n+    merge_base = compute_merge_base(repo, base_ref, head_sha)\n+    if not merge_base:\n+        print(\n+            f\"error: no merge-base between '{base_ref}' and '{ref}'.\\n\"\n+            f\"Tried: `git merge-base {base_ref} {ref}`, then `git merge-base origin/{base_ref} {ref}`.\\n\"\n+            \"remediation: fetch the base branch, or pass --base <branch> explicitly.\",\n+            file=sys.stderr,\n+        )\n+        sys.exit(1)\n+\n+    if merge_base == head_sha:\n+        print(\n+            f\"error: '{ref}' has no commits beyond '{base_ref}' — there is nothing to review.\\n\"\n+            \"remediation: commit your work on this branch first.\",\n+            file=sys.stderr,\n+        )\n+        sys.exit(1)\n+\n+    return {\n+        \"hash\": head_sha,\n+        \"kind\": \"open\",\n+        \"diff_base\": merge_base,\n+        \"branch\": name,\n+        \"slug\": slugify(name),\n+        \"base\": base_ref,\n+    }\n+\n+\n+def diff_stats(repo: Path, base_sha: str, head_sha: str) -> dict[str, int]:\n+    \"\"\"files/adds/dels from `git diff --numstat`. A binary file reports \"-\"\n+    for both counts, and contributes to `files` only.\"\"\"\n+    out = run_git(repo, [\"diff\", \"--numstat\", f\"{base_sha}..{head_sha}\"])\n+    files = adds = dels = 0\n+    for line in out.splitlines():\n+        parts = line.split(\"\\t\")\n+        if len(parts) < 3:\n+            continue\n+        files += 1\n+        if parts[0].isdigit():\n+            adds += int(parts[0])\n+        if parts[1].isdigit():\n+            dels += int(parts[1])\n+    return {\"files\": files, \"adds\": adds, \"dels\": dels}\n+\n+\n # ---- diff extraction ----\n \n def get_diff_text(repo: Path, entry: dict) -> str:\n@@ -307,6 +412,26 @@ def write_diffs_file(data_dir: Path, pr_num: int, files: dict[str, str]) -> Path\n     return out_path\n \n \n+def write_branch_diff(bundle_dir: Path, entry: dict, files: dict[str, str], stats: dict[str, int]) -> Path:\n+    \"\"\"Write the pre-submit staging file. JSON, not JS: nothing loads a branch\n+    diff into the viewer, and submit mode reads it directly.\"\"\"\n+    out_dir = bundle_dir / \"exports\" / f\"branch-{entry['slug']}\"\n+    out_dir.mkdir(parents=True, exist_ok=True)\n+    payload = {\n+        \"kind\": \"branch\",\n+        \"branch\": entry[\"branch\"],\n+        \"slug\": entry[\"slug\"],\n+        \"base\": entry[\"base\"],\n+        \"merge_base\": entry[\"diff_base\"],\n+        \"head\": entry[\"hash\"],\n+        \"stats\": stats,\n+        \"files\": files,\n+    }\n+    out_path = out_dir / \"diff.json\"\n+    out_path.write_text(json.dumps(payload, indent=2, ensure_ascii=False) + \"\\n\")\n+    return out_path\n+\n+\n def rewrite_manifest(bundle_dir: Path, manifest_path: Path) -> None:\n     data_dir = bundle_dir / \"data\"\n     assets_dir = bundle_dir / \"assets\"\n@@ -372,7 +497,21 @@ def main() -> None:\n     parser = argparse.ArgumentParser(description=__doc__, formatter_class=argparse.RawDescriptionHelpFormatter)\n     parser.add_argument(\"--repo\", default=None, help=\"path to the target git repo (default: cwd)\")\n     parser.add_argument(\"--bundle-dir\", default=None, help=\"bundle output dir (default: <repo>/.prodyssey/self)\")\n-    parser.add_argument(\"--prs\", required=True, help=\"comma-separated PR numbers, e.g. 73,75\")\n+    parser.add_argument(\"--prs\", default=None, help=\"comma-separated PR numbers, e.g. 73,75\")\n+    parser.add_argument(\n+        \"--branch\",\n+        nargs=\"?\",\n+        const=\"HEAD\",\n+        default=None,\n+        metavar=\"REF\",\n+        help=\"pre-submit path: diff a working branch with no PR number yet (default ref: HEAD). \"\n+        \"Writes exports/branch-<slug>/diff.json and nothing under data/. Mutually exclusive with --prs\",\n+    )\n+    parser.add_argument(\n+        \"--base\",\n+        default=None,\n+        help=\"base branch --branch diffs against (default: repo's detected default branch)\",\n+    )\n     parser.add_argument(\n         \"--dot-range\",\n         default=None,\n@@ -381,11 +520,30 @@ def main() -> None:\n     parser.add_argument(\"--force\", action=\"store_true\", help=\"overwrite diffs-pr{N}.js files that already exist\")\n     args = parser.parse_args()\n \n+    if bool(args.prs) == bool(args.branch):\n+        print(\n+            \"error: pass exactly one of --prs or --branch.\\n\"\n+            \"remediation: --prs N[,M,...] for a PR that exists, --branch for a working branch that has no PR yet.\",\n+            file=sys.stderr,\n+        )\n+        sys.exit(1)\n+\n     repo = resolve_repo(args.repo)\n     bundle_dir = Path(args.bundle_dir).resolve() if args.bundle_dir else repo / \".prodyssey\" / \"self\"\n     data_dir = bundle_dir / \"data\"\n     manifest_js = data_dir / \"manifest.js\"\n \n+    if args.branch:\n+        entry = resolve_branch(repo, args.branch, args.base)\n+        files = split_diff_by_file(get_diff_text(repo, entry))\n+        stats = diff_stats(repo, entry[\"diff_base\"], entry[\"hash\"])\n+        out_path = write_branch_diff(bundle_dir, entry, files, stats)\n+        print(\n+            f\"branch {entry['branch']} vs {entry['base']}: \"\n+            f\"{stats['files']} file(s), +{stats['adds']}/-{stats['dels']} -> {out_path}\"\n+        )\n+        return\n+\n     pr_nums = sorted({int(x.strip()) for x in args.prs.split(\",\") if x.strip()})\n     if not pr_nums:\n         print(\"error: --prs must list at least one PR number.\\nremediation: pass --prs N[,M,...]\", file=sys.stderr)",
  "scripts/extract_story.py": "diff --git a/scripts/extract_story.py b/scripts/extract_story.py\nindex f0b5e2d..5364262 100755\n--- a/scripts/extract_story.py\n+++ b/scripts/extract_story.py\n@@ -92,6 +92,14 @@ def run_git(repo: Path, args: list[str]) -> str:\n     return subprocess.check_output([\"git\", \"-C\", str(repo)] + args, text=True)\n \n \n+def run_git_quiet(repo: Path, args: list[str]) -> str:\n+    \"\"\"run_git for a probe whose failure is expected and handled. Keeps git's\n+    own stderr off the user's terminal.\"\"\"\n+    return subprocess.check_output(\n+        [\"git\", \"-C\", str(repo)] + args, text=True, stderr=subprocess.DEVNULL\n+    )\n+\n+\n def get_remote_origin(repo: Path) -> str | None:\n     try:\n         return run_git(repo, [\"remote\", \"get-url\", \"origin\"]).strip()\n@@ -103,7 +111,10 @@ def detect_default_branch(repo: Path) -> str:\n     \"\"\"Detect the repo's default branch: origin/HEAD symref first, then try\n     `main` and `master` directly. Exits 1 with remediation if none resolve.\"\"\"\n     try:\n-        out = run_git(repo, [\"symbolic-ref\", \"--short\", \"refs/remotes/origin/HEAD\"]).strip()\n+        # This is a probe, and a repo with no origin/HEAD symref is the normal\n+        # case, not an error — swallow git's \"fatal:\" so it never reaches the\n+        # user's terminal on the way to the `main`/`master` fallback below.\n+        out = run_git_quiet(repo, [\"symbolic-ref\", \"--short\", \"refs/remotes/origin/HEAD\"]).strip()\n         if out.startswith(\"origin/\"):\n             out = out[len(\"origin/\"):]\n         if out:",
  "scripts/migrate_bundle.py": "diff --git a/scripts/migrate_bundle.py b/scripts/migrate_bundle.py\nindex ec209f5..c0b3304 100644\n--- a/scripts/migrate_bundle.py\n+++ b/scripts/migrate_bundle.py\n@@ -66,7 +66,11 @@ PLUGIN_ROOT = Path(__file__).resolve().parent.parent\n \n # Authored (irreplaceable) leaves in story.json, as dotted-path templates.\n # \"*\" stands for \"every item in this list\" when walking timeline/districts.\n-AUTHORED_TIMELINE_FIELDS = (\"tagline\", \"depth\")\n+# `intent` is the author's own words, captured by submit mode's interview, and\n+# `assessment` is the judgment written against them — both irreplaceable, and\n+# neither re-derivable from git. They sit here so the guard treats a migration\n+# that touches either one as a violation unless it declares them.\n+AUTHORED_TIMELINE_FIELDS = (\"tagline\", \"depth\", \"intent\", \"assessment\")\n AUTHORED_DISTRICT_FIELDS = (\"label\", \"kind\", \"blurb\")\n AUTHORED_LEVEL_FIELDS = (\n     \"narration\", \"voice\", \"detail\", \"problem\", \"solution\",\n@@ -146,10 +150,22 @@ def migrate_1_0_to_1_1(story: dict) -> dict:\n     return story\n \n \n+def migrate_1_1_to_1_2(story: dict) -> dict:\n+    \"\"\"Stamp only. 1.2 adds two OPTIONAL timeline fields written by submit\n+    mode — `intent` (the author's stated problem/approach/alternatives) and\n+    `assessment` (the judgment written against them). Neither is backfilled:\n+    `intent` is what a person said, and nothing but an interview can produce\n+    it, so an absent block stays absent rather than becoming an invented one.\n+    verify_bundle.py reports both as optional unless --require-review.\"\"\"\n+    story.setdefault(\"meta\", {})[\"schema_version\"] = \"1.2\"\n+    return story\n+\n+\n # Ordered list of (from, to, fn, touches, description). Append one entry per\n # future schema bump; never edit history in place.\n SCHEMA_MIGRATIONS: list[tuple[str, str, object, set[str], str]] = [\n     (\"1.0\", \"1.1\", migrate_1_0_to_1_1, set(), \"backfill timeline[].status where absent\"),\n+    (\"1.1\", \"1.2\", migrate_1_1_to_1_2, set(), \"stamp schema 1.2 (intent/assessment are optional, never backfilled)\"),\n ]\n \n \n@@ -424,8 +440,15 @@ def main() -> None:\n     bundle_json_path = bundle_dir / \"bundle.json\"\n     # `schema_mirror_drift` matters even when no ladder step ran: the mirror is\n     # wrong and only rewriting it clears verify_bundle.py's `bundle.schema`.\n+    # A viewer refresh counts too — the plugin just changed something in this\n+    # bundle, so leaving `generator_version` at the older value makes the stamp\n+    # claim a version that did not produce what is now on disk.\n     needs_bundle_json = (\n-        not bundle_json_path.exists() or layout_steps or data_steps or schema_mirror_drift\n+        not bundle_json_path.exists()\n+        or layout_steps\n+        or data_steps\n+        or schema_mirror_drift\n+        or report[\"viewer\"] == \"refreshed\"\n     )\n     if needs_bundle_json:\n         write_bundle_json(bundle_dir, final_format, report[\"data\"][\"to\"])",
  "scripts/render_review.py": "diff --git a/scripts/render_review.py b/scripts/render_review.py\nnew file mode 100644\nindex 0000000..b01c01b\n--- /dev/null\n+++ b/scripts/render_review.py\n@@ -0,0 +1,459 @@\n+#!/usr/bin/env python3\n+# /// script\n+# requires-python = \">=3.10\"\n+# dependencies = []\n+# ///\n+\"\"\"Render submit mode's `intent` and `assessment` blocks into two markdown\n+deliverables. Pure rendering — it makes no judgment, contacts no service, and\n+never calls `gh`. Claude authors both blocks (see\n+skills/odyssey/references/interview-guide.md and review-mode.md); this script\n+only lays them out.\n+\n+Two sources, one shape:\n+\n+  --prs N     reads timeline[N].intent / .assessment from <bundle-dir>/data/\n+              story.json, and writes <bundle-dir>/exports/pr-N-description.md\n+              and pr-N-assessment.md.\n+\n+  --branch    reads <bundle-dir>/exports/branch-<slug>/{intent,assessment}.json\n+              (the pre-submit staging dir extract_diffs.py --branch creates)\n+              and writes description.md / assessment.md beside them. Use this\n+              before the PR exists, when there is no number to file under.\n+\n+`description.md` follows references/pr-description-template.md — it is the PR\n+body, and it exists so a reviewer reads the author's argument before the diff.\n+An empty field drops its whole section rather than printing a placeholder.\n+\n+Usage:\n+    uv run render_review.py --bundle-dir <bundle> --prs 73\n+    uv run render_review.py --bundle-dir <bundle> --prs 73,75\n+    uv run render_review.py --repo <path> --bundle-dir <bundle> --branch\n+\"\"\"\n+from __future__ import annotations\n+\n+import argparse\n+import json\n+import re\n+import subprocess\n+import sys\n+from pathlib import Path\n+\n+VERDICT_LABEL = {\n+    \"sound\": \"Sound\",\n+    \"concerns\": \"Concerns\",\n+    \"rework\": \"Rework\",\n+}\n+TIER_LABEL = {\n+    \"routine\": \"Routine\",\n+    \"architectural\": \"Architectural\",\n+    \"sensitive\": \"Sensitive\",\n+}\n+PATTERN_LABEL = {\n+    \"conforms\": \"Conforms to an existing pattern\",\n+    \"new-valuable\": \"New pattern, and it earns its place\",\n+    \"duplicate\": \"Duplicates something the repo already has\",\n+    \"reinvention\": \"Reinvents a solved problem\",\n+}\n+DRIFT_LABEL = {\n+    \"out_of_scope\": \"Out of scope, but touched\",\n+    \"unaddressed_risk\": \"Risk with no guard\",\n+    \"adopted_alternative\": \"Rejected option adopted\",\n+    \"delta_shift\": \"District delta shifted\",\n+}\n+\n+\n+# ---- plumbing (duplicated from the sibling scripts — no cross-imports) ----\n+\n+def resolve_repo(repo_arg: str | None) -> Path:\n+    target = repo_arg or \".\"\n+    try:\n+        out = subprocess.check_output(\n+            [\"git\", \"-C\", target, \"rev-parse\", \"--show-toplevel\"],\n+            text=True,\n+            stderr=subprocess.PIPE,\n+        ).strip()\n+    except (subprocess.CalledProcessError, FileNotFoundError):\n+        print(\n+            f\"error: '{target}' is not inside a git repository.\\n\"\n+            \"remediation: run from inside a git checkout, or pass --repo <path-to-git-repo>\",\n+            file=sys.stderr,\n+        )\n+        sys.exit(1)\n+    return Path(out)\n+\n+\n+def slugify(name: str) -> str:\n+    \"\"\"Same rule as extract_diffs.py and SKILL.md's Hub resolution.\"\"\"\n+    out = re.sub(r\"[^a-z0-9]+\", \"-\", name.lower())\n+    return out.strip(\"-\") or \"branch\"\n+\n+\n+def branch_slug(repo: Path, ref: str) -> str:\n+    name = ref\n+    if ref == \"HEAD\":\n+        try:\n+            name = subprocess.check_output(\n+                [\"git\", \"-C\", str(repo), \"rev-parse\", \"--abbrev-ref\", \"HEAD\"],\n+                text=True,\n+                stderr=subprocess.DEVNULL,\n+            ).strip()\n+        except subprocess.CalledProcessError:\n+            name = \"\"\n+        if not name or name == \"HEAD\":\n+            try:\n+                name = subprocess.check_output(\n+                    [\"git\", \"-C\", str(repo), \"rev-parse\", \"--short=8\", \"HEAD\"],\n+                    text=True,\n+                    stderr=subprocess.DEVNULL,\n+                ).strip()\n+            except subprocess.CalledProcessError:\n+                name = \"\"\n+    return slugify(name)\n+\n+\n+def read_json(path: Path) -> dict | None:\n+    if not path.exists():\n+        return None\n+    try:\n+        data = json.loads(path.read_text())\n+    except json.JSONDecodeError:\n+        return None\n+    return data if isinstance(data, dict) else None\n+\n+\n+# ---- small markdown helpers ----\n+\n+def section(title: str, body: str) -> str:\n+    \"\"\"A heading plus body, or \"\" when the body is empty. This is what makes\n+    an unanswered interview question vanish instead of printing a placeholder\n+    that reads like the author had nothing to say.\"\"\"\n+    body = (body or \"\").strip()\n+    return f\"## {title}\\n\\n{body}\\n\" if body else \"\"\n+\n+\n+def bullets(items) -> str:\n+    if not isinstance(items, list):\n+        return \"\"\n+    return \"\\n\".join(f\"- {str(item).strip()}\" for item in items if str(item).strip())\n+\n+\n+def paragraphs(*values: str) -> str:\n+    return \"\\n\\n\".join(v.strip() for v in values if v and str(v).strip())\n+\n+\n+def evidence_list(items) -> str:\n+    if not isinstance(items, list) or not items:\n+        return \"\"\n+    return \" \".join(f\"`{str(i).strip()}`\" for i in items if str(i).strip())\n+\n+\n+# ---- description.md ----\n+\n+def render_description(title: str, intent: dict) -> str:\n+    alts = intent.get(\"alternatives\")\n+    if isinstance(alts, list) and alts:\n+        alt_body = \"\\n\".join(\n+            f\"- **{str(a.get('option', '')).strip()}** — rejected because \"\n+            f\"{str(a.get('rejected_because', 'no reason recorded')).strip().rstrip('.')}.\"\n+            for a in alts\n+            if isinstance(a, dict) and str(a.get(\"option\", \"\")).strip()\n+        )\n+    elif intent.get(\"source\") == \"author\":\n+        alt_body = \"None. The author reports no alternative was considered.\"\n+    else:\n+        alt_body = \"\"\n+\n+    focus = bullets(intent.get(\"reviewer_focus\"))\n+    unknowns = bullets(intent.get(\"unknowns\"))\n+    if unknowns:\n+        lead = \"The author flagged these parts as not fully understood:\"\n+        focus = paragraphs(focus, f\"{lead}\\n\\n{unknowns}\")\n+\n+    parts = [\n+        f\"# {title}\\n\" if title else \"\",\n+        section(\"Problem\", paragraphs(intent.get(\"problem\", \"\"), intent.get(\"why_now\", \"\"))),\n+        section(\"Why this approach\", intent.get(\"approach\", \"\")),\n+        section(\"Alternatives considered\", alt_body),\n+        section(\"Out of scope\", bullets(intent.get(\"out_of_scope\"))),\n+        section(\"Risks\", bullets(intent.get(\"risks\"))),\n+        section(\"How this was tested\", intent.get(\"testing\", \"\")),\n+        section(\"Where to focus\", focus),\n+    ]\n+\n+    notes = []\n+    if intent.get(\"source\") == \"inferred\":\n+        notes.append(\n+            \"Intent inferred from the PR body, the commit messages, and the \"\n+            \"branch name. Not stated by the author.\"\n+        )\n+    authorship = intent.get(\"authorship\")\n+    if authorship and authorship != \"human\":\n+        notes.append(f\"Authorship: {authorship}.\")\n+    if notes:\n+        parts.append(\"---\\n\\n\" + \"\\n\".join(f\"_{n}_\" for n in notes) + \"\\n\")\n+\n+    return \"\\n\".join(p for p in parts if p).rstrip() + \"\\n\"\n+\n+\n+# ---- assessment.md ----\n+\n+def render_question(heading: str, block, extra_label: str = \"\", extra_key: str = \"\") -> str:\n+    if not isinstance(block, dict):\n+        return \"\"\n+    body = [str(block.get(\"answer\", \"\")).strip()]\n+    if extra_key and str(block.get(extra_key, \"\")).strip():\n+        body.append(f\"**{extra_label}:** {str(block[extra_key]).strip()}\")\n+    ev = evidence_list(block.get(\"evidence\"))\n+    if ev:\n+        body.append(f\"Evidence: {ev}\")\n+    return section(heading, paragraphs(*body))\n+\n+\n+def render_findings(findings) -> str:\n+    if not isinstance(findings, list) or not findings:\n+        return \"\"\n+    order = {\"blocker\": 0, \"concern\": 1, \"note\": 2}\n+    rows = sorted(\n+        (f for f in findings if isinstance(f, dict)),\n+        key=lambda f: order.get(f.get(\"severity\"), 3),\n+    )\n+    lines = [\"| Severity | Finding | Evidence |\", \"|---|---|---|\"]\n+    for f in rows:\n+        severity = str(f.get(\"severity\", \"note\")).strip()\n+        claim = str(f.get(\"claim\", \"\")).strip().replace(\"|\", \"\\\\|\")\n+        ev = str(f.get(\"evidence\", \"\")).strip().replace(\"|\", \"\\\\|\")\n+        lines.append(f\"| {severity} | {claim} | {f'`{ev}`' if ev else '—'} |\")\n+    body = \"\\n\".join(lines)\n+    suggestions = [\n+        f\"- **{str(f.get('claim', '')).strip()}** — {str(f['suggestion']).strip()}\"\n+        for f in rows\n+        if str(f.get(\"suggestion\", \"\")).strip()\n+    ]\n+    if suggestions:\n+        body += \"\\n\\n**Suggestions**\\n\\n\" + \"\\n\".join(suggestions)\n+    return section(\"Findings\", body)\n+\n+\n+def render_boundary_checks(checks) -> str:\n+    if not isinstance(checks, list) or not checks:\n+        return \"\"\n+    lines = [\"| Result | Rule | Source | Evidence |\", \"|---|---|---|---|\"]\n+    for c in checks:\n+        if not isinstance(c, dict):\n+            continue\n+        result = str(c.get(\"result\", \"\")).strip()\n+        rule = str(c.get(\"rule\", \"\")).strip().replace(\"|\", \"\\\\|\")\n+        source = str(c.get(\"source\", \"\")).strip()\n+        ev = str(c.get(\"evidence\", \"\")).strip().replace(\"|\", \"\\\\|\")\n+        lines.append(\n+            f\"| {result} | {rule} | {f'`{source}`' if source else '—'} | {f'`{ev}`' if ev else '—'} |\"\n+        )\n+    return section(\"Boundary checks\", \"\\n\".join(lines))\n+\n+\n+def render_delta(delta) -> str:\n+    if not isinstance(delta, dict):\n+        return \"\"\n+    lines = []\n+    added = delta.get(\"districts_added\")\n+    if isinstance(added, list) and added:\n+        lines.append(\"**Districts added:** \" + \", \".join(f\"`{d}`\" for d in added))\n+    changed = delta.get(\"districts_changed\")\n+    if isinstance(changed, list) and changed:\n+        for c in changed:\n+            if not isinstance(c, dict):\n+                continue\n+            lines.append(\n+                f\"- `{c.get('id', '?')}`: {c.get('files_before', '?')} -> \"\n+                f\"{c.get('files_after', '?')} files\"\n+            )\n+    for key, label in ((\"edges_added\", \"Edges added\"), (\"edges_removed\", \"Edges removed\")):\n+        edges = delta.get(key)\n+        if isinstance(edges, list) and edges:\n+            lines.append(f\"**{label}:** \" + \", \".join(f\"`{e}`\" for e in edges))\n+    return section(\"District delta\", \"\\n\".join(lines))\n+\n+\n+def render_drift(drift) -> str:\n+    if not isinstance(drift, list) or not drift:\n+        return \"\"\n+    lines = [\"| Kind | What changed | Evidence |\", \"|---|---|---|\"]\n+    for d in drift:\n+        if not isinstance(d, dict):\n+            continue\n+        kind = DRIFT_LABEL.get(d.get(\"kind\"), str(d.get(\"kind\", \"\")).strip())\n+        claim = str(d.get(\"claim\", \"\")).strip().replace(\"|\", \"\\\\|\")\n+        ev = str(d.get(\"evidence\", \"\")).strip().replace(\"|\", \"\\\\|\")\n+        lines.append(f\"| {kind} | {claim} | {f'`{ev}`' if ev else '—'} |\")\n+    return section(\"Drift from the stated intent\", \"\\n\".join(lines))\n+\n+\n+def render_assessment(title: str, assessment: dict, intent: dict | None) -> str:\n+    verdict = assessment.get(\"verdict\", \"\")\n+    tier = assessment.get(\"risk_tier\", \"\")\n+    stage = assessment.get(\"stage\", \"pre\")\n+\n+    header = [\n+        f\"**Verdict:** {VERDICT_LABEL.get(verdict, verdict or 'not recorded')}\",\n+        f\"**Risk tier:** {TIER_LABEL.get(tier, tier or 'not recorded')}\",\n+        f\"**Stage:** {'post-merge' if stage == 'post' else 'pre-merge'}\",\n+    ]\n+    if isinstance(intent, dict) and intent.get(\"unknowns\"):\n+        header.append(\n+            f\"**Author-flagged unknowns:** {len(intent['unknowns'])} \"\n+            \"(raises the tier by one step)\"\n+        )\n+\n+    pattern = assessment.get(\"pattern\")\n+    pattern_heading = \"Question 3 — new pattern, duplicate, or reinvention?\"\n+    pattern_body = \"\"\n+    if isinstance(pattern, dict):\n+        verdict_line = PATTERN_LABEL.get(pattern.get(\"verdict\"), pattern.get(\"verdict\", \"\"))\n+        body = [f\"**{verdict_line}**\" if verdict_line else \"\", str(pattern.get(\"answer\", \"\")).strip()]\n+        dupes = pattern.get(\"duplicates\")\n+        if isinstance(dupes, list) and dupes:\n+            body.append(\"Duplicates: \" + \", \".join(f\"`{d}`\" for d in dupes))\n+        ev = evidence_list(pattern.get(\"evidence\"))\n+        if ev:\n+            body.append(f\"Evidence: {ev}\")\n+        pattern_body = section(pattern_heading, paragraphs(*body))\n+\n+    parts = [\n+        f\"# Assessment — {title}\\n\" if title else \"# Assessment\\n\",\n+        \"  \\n\".join(header) + \"\\n\",\n+        section(\"Summary\", assessment.get(\"summary\", \"\")),\n+        render_question(\"Question 1 — is this sensible?\", assessment.get(\"sensible\")),\n+        render_question(\n+            \"Question 2 — maintainability and readability\",\n+            assessment.get(\"maintainability\"),\n+            \"Constraint introduced\",\n+            \"constraint_introduced\",\n+        ),\n+        pattern_body,\n+        section(\"Will we regret this?\", assessment.get(\"regret_risk\", \"\")),\n+        render_findings(assessment.get(\"findings\")),\n+        render_boundary_checks(assessment.get(\"boundary_checks\")),\n+        render_delta(assessment.get(\"delta\")),\n+        render_drift(assessment.get(\"drift\")),\n+    ]\n+\n+    generated = assessment.get(\"generated\", \"\")\n+    footer = \"_Generated by prodyssey submit mode\"\n+    footer += f\" on {generated}._\" if generated else \"._\"\n+    parts.append(\"---\\n\\n\" + footer + \"\\n\")\n+\n+    return \"\\n\".join(p for p in parts if p).rstrip() + \"\\n\"\n+\n+\n+# ---- drivers ----\n+\n+def write_pair(out_dir: Path, prefix: str, title: str, intent: dict | None, assessment: dict | None) -> list[Path]:\n+    out_dir.mkdir(parents=True, exist_ok=True)\n+    written: list[Path] = []\n+    if intent:\n+        path = out_dir / f\"{prefix}description.md\"\n+        path.write_text(render_description(title, intent))\n+        written.append(path)\n+    if assessment:\n+        path = out_dir / f\"{prefix}assessment.md\"\n+        path.write_text(render_assessment(title, assessment, intent))\n+        written.append(path)\n+    return written\n+\n+\n+def main() -> None:\n+    parser = argparse.ArgumentParser(description=__doc__, formatter_class=argparse.RawDescriptionHelpFormatter)\n+    parser.add_argument(\"--repo\", default=None, help=\"path to the target git repo (default: cwd)\")\n+    parser.add_argument(\"--bundle-dir\", default=None, help=\"bundle dir (default: <repo>/.prodyssey/self)\")\n+    parser.add_argument(\"--prs\", default=None, help=\"comma-separated PR numbers, e.g. 73,75\")\n+    parser.add_argument(\n+        \"--branch\",\n+        nargs=\"?\",\n+        const=\"HEAD\",\n+        default=None,\n+        metavar=\"REF\",\n+        help=\"render the pre-submit staging dir for a working branch (default ref: HEAD). \"\n+        \"Mutually exclusive with --prs\",\n+    )\n+    args = parser.parse_args()\n+\n+    if bool(args.prs) == bool(args.branch):\n+        print(\n+            \"error: pass exactly one of --prs or --branch.\\n\"\n+            \"remediation: --prs N[,M,...] once the PR exists, --branch before it does.\",\n+            file=sys.stderr,\n+        )\n+        sys.exit(1)\n+\n+    repo = resolve_repo(args.repo)\n+    bundle_dir = Path(args.bundle_dir).resolve() if args.bundle_dir else repo / \".prodyssey\" / \"self\"\n+\n+    if args.branch:\n+        slug = branch_slug(repo, args.branch)\n+        stage_dir = bundle_dir / \"exports\" / f\"branch-{slug}\"\n+        if not stage_dir.exists():\n+            print(\n+                f\"error: {stage_dir} does not exist.\\n\"\n+                f\"remediation: run extract_diffs.py --branch {args.branch} first.\",\n+                file=sys.stderr,\n+            )\n+            sys.exit(1)\n+        intent = read_json(stage_dir / \"intent.json\")\n+        assessment = read_json(stage_dir / \"assessment.json\")\n+        if not intent and not assessment:\n+            print(\n+                f\"error: neither intent.json nor assessment.json found in {stage_dir}.\\n\"\n+                \"remediation: run submit mode's interview and assessment before rendering.\",\n+                file=sys.stderr,\n+            )\n+            sys.exit(1)\n+        title = (intent or {}).get(\"title\") or f\"Branch {slug}\"\n+        written = write_pair(stage_dir, \"\", title, intent, assessment)\n+        for path in written:\n+            print(f\"wrote {path}\")\n+        return\n+\n+    story = read_json(bundle_dir / \"data\" / \"story.json\")\n+    if story is None:\n+        print(\n+            f\"error: {bundle_dir}/data/story.json not found or not valid JSON.\\n\"\n+            \"remediation: run /prodyssey:baseline against this bundle first.\",\n+            file=sys.stderr,\n+        )\n+        sys.exit(1)\n+\n+    timeline = {item.get(\"pr\"): item for item in story.get(\"timeline\", []) if isinstance(item, dict)}\n+    pr_nums = sorted({int(x.strip()) for x in args.prs.split(\",\") if x.strip()})\n+    exports_dir = bundle_dir / \"exports\"\n+\n+    exit_code = 0\n+    for pr_num in pr_nums:\n+        entry = timeline.get(pr_num)\n+        if entry is None:\n+            print(\n+                f\"error: PR #{pr_num} is not in this bundle's timeline.\\n\"\n+                f\"remediation: run /prodyssey:generate --prs {pr_num} first.\",\n+                file=sys.stderr,\n+            )\n+            exit_code = 1\n+            continue\n+        intent = entry.get(\"intent\") if isinstance(entry.get(\"intent\"), dict) else None\n+        assessment = entry.get(\"assessment\") if isinstance(entry.get(\"assessment\"), dict) else None\n+        if not intent and not assessment:\n+            print(\n+                f\"error: PR #{pr_num} has neither an `intent` nor an `assessment` block.\\n\"\n+                f\"remediation: run /prodyssey:submit --prs {pr_num} first.\",\n+                file=sys.stderr,\n+            )\n+            exit_code = 1\n+            continue\n+        title = f\"PR #{pr_num} — {entry.get('title', '')}\".rstrip(\" —\")\n+        written = write_pair(exports_dir, f\"pr-{pr_num}-\", title, intent, assessment)\n+        for path in written:\n+            print(f\"PR #{pr_num}: wrote {path}\")\n+\n+    sys.exit(exit_code)\n+\n+\n+if __name__ == \"__main__\":\n+    main()",
  "scripts/verify_bundle.py": "diff --git a/scripts/verify_bundle.py b/scripts/verify_bundle.py\nindex a271bbe..c76a4d0 100755\n--- a/scripts/verify_bundle.py\n+++ b/scripts/verify_bundle.py\n@@ -36,6 +36,13 @@ Artifact key names (stable, used in --json output):\n     audio.<level>           - wav exists; only checked for levels with a non-empty\n                               `voice` script in story.json\n     diffs                   - data/diffs-pr{N}.js exists\n+    intent                  - submit mode's captured author intent: a dict with a\n+                              non-empty `problem`. \"empty\" when the dict is there\n+                              but `problem` is not.\n+    assessment              - submit mode's architecture assessment: a dict whose\n+                              `verdict` is sound/concerns/rework and which answers\n+                              all three questions. \"incomplete\" when a question\n+                              block is absent, \"wrong-verdict:<v>\" otherwise.\n \n   `asset.*` and `diagram.*` are always computed and reported, but which family is\n   REQUIRED for a passing/\"ok\" run is controlled by `--art image|diagram|both`\n@@ -43,13 +50,20 @@ Artifact key names (stable, used in --json output):\n   are listed under a per-section `\"_optional\"` key (JSON) or marked `(optional)`\n   in the table; their real status is still reported, not hidden.\n \n-Exit 0 iff every REQUIRED artifact is \"ok\" (see `--art` above). Never writes\n-anything.\n+  `intent`/`assessment` follow the same reported-but-optional rule, and are\n+  optional by DEFAULT — every PR generated before submit mode existed has\n+  neither, and a bundle must not start failing because the plugin grew a new\n+  mode. `--require-review` promotes them to required, which is what submit mode\n+  passes to confirm its own run landed.\n+\n+Exit 0 iff every REQUIRED artifact is \"ok\" (see `--art` and `--require-review`\n+above). Never writes anything.\n \n Usage:\n     uv run verify_bundle.py --bundle-dir <bundle>\n     uv run verify_bundle.py --bundle-dir <bundle> --prs 73,75 --json\n     uv run verify_bundle.py --bundle-dir <bundle> --art diagram --json\n+    uv run verify_bundle.py --bundle-dir <bundle> --prs 73 --require-review --json\n \"\"\"\n from __future__ import annotations\n \n@@ -64,6 +78,12 @@ from _bundle_meta import CURRENT_BUNDLE_FORMAT, SCHEMA_VERSION, SCHEMA_VERSION_K\n LEVEL_KEYS = [\"landscape\", \"problem_solution\", \"architecture\", \"file_changes\"]\n MIN_ASSET_BYTES = 1024\n DIAGRAM_TYPE_BY_LEVEL = {1: \"C4Container\", 2: \"sequenceDiagram\", 3: \"classDiagram\"}\n+# skills/odyssey/references/review-mode.md §8 and §3.\n+ASSESSMENT_VERDICTS = {\"sound\", \"concerns\", \"rework\"}\n+ASSESSMENT_QUESTIONS = (\"sensible\", \"maintainability\", \"pattern\")\n+# Key prefixes for submit mode's two artifacts. Optional unless --require-review;\n+# no other artifact key starts with either string.\n+REVIEW_PREFIXES = (\"intent\", \"assessment\")\n \n \n def count_inventory_contexts(text: str) -> int:\n@@ -213,6 +233,32 @@ def optional_prefixes_for_art(art: str) -> tuple[str, ...]:\n     return ()  # \"both\": nothing optional\n \n \n+def check_intent(entry: dict | None) -> str:\n+    \"\"\"\"ok\" when submit mode's interview left a `problem` behind. An `intent`\n+    block with no `problem` is a half-written interview, not a valid one.\"\"\"\n+    intent = (entry or {}).get(\"intent\")\n+    if not isinstance(intent, dict) or not intent:\n+        return \"missing\"\n+    return \"ok\" if intent.get(\"problem\") else \"empty\"\n+\n+\n+def check_assessment(entry: dict | None) -> str:\n+    \"\"\"\"ok\" when the block carries a known verdict and answers all three\n+    questions in review-mode.md §3. Shape only — nothing here judges whether\n+    the answers are any good.\"\"\"\n+    assessment = (entry or {}).get(\"assessment\")\n+    if not isinstance(assessment, dict) or not assessment:\n+        return \"missing\"\n+    verdict = assessment.get(\"verdict\")\n+    if verdict not in ASSESSMENT_VERDICTS:\n+        return f\"wrong-verdict:{verdict}\"\n+    for question in ASSESSMENT_QUESTIONS:\n+        block = assessment.get(question)\n+        if not isinstance(block, dict) or not block.get(\"answer\"):\n+            return \"incomplete\"\n+    return \"ok\"\n+\n+\n def check_pr(bundle_dir: Path, story: dict | None, adrs: dict | None, pr_num: int) -> dict[str, str]:\n     results: dict[str, str] = {}\n \n@@ -262,6 +308,9 @@ def check_pr(bundle_dir: Path, story: dict | None, adrs: dict | None, pr_num: in\n     diffs_path = bundle_dir / \"data\" / f\"diffs-pr{pr_num}.js\"\n     results[\"diffs\"] = \"ok\" if diffs_path.exists() else \"missing\"\n \n+    results[\"intent\"] = check_intent(entry)\n+    results[\"assessment\"] = check_assessment(entry)\n+\n     return results\n \n \n@@ -283,9 +332,18 @@ def main() -> None:\n         \"pre-diagram behavior), 'diagram' (diagram.* required, asset.* \"\n         \"informational), or 'both' (both required)\",\n     )\n+    parser.add_argument(\n+        \"--require-review\",\n+        action=\"store_true\",\n+        help=\"require submit mode's intent/assessment blocks for a passing run. \"\n+        \"Off by default so a bundle generated before submit mode existed keeps \"\n+        \"passing; submit mode passes it to confirm its own run landed\",\n+    )\n     args = parser.parse_args()\n \n     optional_prefixes = optional_prefixes_for_art(args.art)\n+    if not args.require_review:\n+        optional_prefixes += REVIEW_PREFIXES\n \n     bundle_dir = Path(args.bundle_dir).resolve()\n     if not bundle_dir.exists():\n@@ -324,9 +382,18 @@ def main() -> None:\n             pr_num: {**results, \"_optional\": optional_keys(results)}\n             for pr_num, results in prs_results.items()\n         }\n-        print(json.dumps({\"art\": args.art, \"baseline\": baseline_out, \"prs\": prs_out}, indent=2, ensure_ascii=False))\n+        # `art` and `require_review` both decide what counts as optional, so the\n+        # JSON reports each of them next to the results they shaped.\n+        payload = {\n+            \"art\": args.art,\n+            \"require_review\": args.require_review,\n+            \"baseline\": baseline_out,\n+            \"prs\": prs_out,\n+        }\n+        print(json.dumps(payload, indent=2, ensure_ascii=False))\n     else:\n-        print(f\"Baseline (--art {args.art})\")\n+        review_note = \", review required\" if args.require_review else \"\"\n+        print(f\"Baseline (--art {args.art}{review_note})\")\n         print(\"--------\")\n         for key, status in baseline.items():\n             print(f\"  {key:<20} {status:<12} (required)\")",
  "skills/odyssey/SKILL.md": "diff --git a/skills/odyssey/SKILL.md b/skills/odyssey/SKILL.md\nindex 2d8536d..c40099b 100644\n--- a/skills/odyssey/SKILL.md\n+++ b/skills/odyssey/SKILL.md\n@@ -2,17 +2,22 @@\n name: odyssey\n title: \"Codebase Odyssey Generator\"\n status: active\n-version: 0.1.0\n+version: 0.3.0\n description: >\n   Generate a narrated four-level codebase story bundle (landscape, problem/solution,\n   architecture, file-changes) with scene art, voice narration, and retro-extracted\n-  architecture decision records for any locally checked-out git repo. Also serves the\n-  bundle locally for viewing. Use when the user asks to \"generate codebase odyssey\",\n-  \"generate story for PR\", \"odyssey baseline\", \"prodyssey\", \"narrated PR story\", \"tell\n-  the story of this PR as scene art\", \"explain this PR as a story\", \"build the odyssey\n-  bundle\", \"refresh odyssey baseline\", \"view the odyssey bundle\", \"serve the bundle\",\n-  \"open the viewer\", \"start the odyssey server\", \"stop the odyssey server\", or invokes\n-  `/prodyssey:baseline`, `/prodyssey:generate`, or `/prodyssey:view`.\n+  architecture decision records for any locally checked-out git repo. Also interviews\n+  a change's author before the PR opens, assesses the change against the bundle, and\n+  serves the bundle locally for viewing. Use when the user asks to \"generate codebase\n+  odyssey\", \"generate story for PR\", \"odyssey baseline\", \"prodyssey\", \"narrated PR\n+  story\", \"tell the story of this PR as scene art\", \"explain this PR as a story\",\n+  \"build the odyssey bundle\", \"refresh odyssey baseline\", \"view the odyssey bundle\",\n+  \"serve the bundle\", \"open the viewer\", \"start the odyssey server\", \"stop the\n+  odyssey server\", \"publish the odyssey\", \"submit this PR\", \"review my PR\", \"review\n+  this branch before I open a PR\", \"architecture review\", \"should we merge this\",\n+  \"will we regret this change\", \"interview me about this change\", or invokes\n+  `/prodyssey:baseline`, `/prodyssey:generate`, `/prodyssey:view`,\n+  `/prodyssey:publish`, or `/prodyssey:submit`.\n ---\n \n # Codebase Odyssey Generator\n@@ -20,9 +25,16 @@ description: >\n Orchestration procedure for turning merged PRs of **any locally checked-out git\n repo** — the session's own repo, or any other checkout reached via `--repo` — into\n a portable bundle: four-level narrated story, scene art, TTS narration, and ADR\n-retro-extraction. This is a read-only, generate-only instrument against the target\n-repo — it never edits the target repo's source, only writes into its bundle\n-directory (`<bundle-dir>`, see Hub resolution below).\n+retro-extraction. It also runs Submit mode, which interviews a change's author,\n+assesses the change against that bundle, and opens the pull request.\n+\n+**This skill never edits the target repo's source.** It only writes into the\n+bundle directory (`<bundle-dir>`, see Hub resolution below). Baseline, Generate,\n+View, and Publish are read-only against the target beyond that. Submit mode is\n+the one exception, and it is narrow: after an explicit confirmation, it pushes\n+the branch and runs `gh pr create`. It still writes no source file, and it takes\n+no other outward action — no comments, no edits to an existing PR body, no\n+labels, no reviewers, no merges. See Submit mode.\n \n Where that bundle actually lands depends on whether the target is the session's\n own repo or a foreign one: every bundle lives under one parent,\n@@ -113,11 +125,13 @@ fi\n \n (`<repo-slug>`/`$SLUG` is computed only when the foreign path applies — see\n above.) `<bundle-dir>` is the only path Baseline, Generate, and Publish modes\n-ever write to; no mode references a literal bundle path directly.\n+ever write to; no mode references a literal bundle path directly. Submit mode\n+writes there too, and its `git push` / `gh pr create` are the only actions that\n+reach outside it.\n \n **Legacy layout detection.** Immediately after `BUNDLE_DIR` is computed above\n-— reached by all four modes, since Step 0's prereq gate is skipped by View and\n-Publish — check: if `$BUNDLE_DIR` does not exist but a legacy\n+— reached by all five modes, since Step 0's prereq gate is skipped by View,\n+Publish, and Submit — check: if `$BUNDLE_DIR` does not exist but a legacy\n `<target>/.odyssey/` does, STOP and tell the user their bundle predates the\n `.prodyssey/self` layout, printing the exact command:\n ```\n@@ -129,8 +143,8 @@ fix: auto-running `git mv` inside a user's repo contradicts this skill's own\n \"never edits the target repo's source\" posture; and without this check,\n Generate mode's auto-baseline check (below) would find nothing at the new\n path, announce \"No baseline found\", and silently regenerate over\n-hand-authored narrative at real Gemini API cost. This block is removable at\n-0.2.0, once legacy `.odyssey/` layouts are no longer expected in the wild.\n+hand-authored narrative at real Gemini API cost. This block is removable once\n+legacy `.odyssey/` layouts are no longer expected in the wild.\n \n Whenever `<bundle-dir>` resolves under `<hub>/.prodyssey/` and\n `<hub>/.prodyssey/` doesn't exist yet, create it (`mkdir -p`) and check\n@@ -160,11 +174,13 @@ suggested lines.\n \n ## Step 0 — Prereq gate (hard, before ANYTHING generative)\n \n-Applies to **baseline** and **generate** modes. **View and Publish modes are\n-exempt** — View only serves static files already on disk (needs neither `uv`\n-nor `GEMINI_API_KEY`); Publish only flattens/publishes what's already\n-generated (needs `uv` for its export scripts, but not `GEMINI_API_KEY`). See\n-each mode's own section below.\n+Applies to **baseline** and **generate** modes. **View, Publish, and Submit\n+modes are exempt** — View only serves static files already on disk (needs\n+neither `uv` nor `GEMINI_API_KEY`); Publish only flattens/publishes what's\n+already generated (needs `uv` for its export scripts, but not\n+`GEMINI_API_KEY`); Submit reads git and writes markdown (needs `uv` and a git\n+repo, but never calls Gemini — its narrative work is Claude's judgment, and it\n+generates no art or audio). See each mode's own section below.\n \n Run this before any other step, every baseline/generate invocation:\n \n@@ -197,12 +213,13 @@ Only after all three checks pass does mode dispatch begin.\n \n ## Mode dispatch\n \n-The invoking command passes a mode (`baseline`, `generate`, `view`, or\n-`publish`) plus forwarded args (`--repo`, `--store`, `--prs`, `--force`,\n+The invoking command passes a mode (`baseline`, `generate`, `view`, `publish`,\n+or `submit`) plus forwarded args (`--repo`, `--store`, `--prs`, `--force`,\n `--voice`, `--art`, `--dry-run`, `--port`, `--stop`, `--list`, `--format`,\n-`--style`).\n+`--style`, `--stage`, `--branch`, `--base`, `--draft`, `--no-create`,\n+`--non-interactive`).\n If invoked with no mode, ask the user whether they want `baseline`,\n-`generate`, `view`, or `publish`.\n+`generate`, `view`, `publish`, or `submit`.\n \n ## Baseline mode\n \n@@ -286,6 +303,11 @@ Per-PR narrative + ADR + art + audio sweep. Steps:\n       §3 for both. Ground every claim by reading the diff (from\n       `extract_diffs.py`'s output — run it first if the diff isn't extracted yet),\n       the touched files in `<target>`, and `<bundle-dir>/inventory.yaml`.\n+      **Read this PR's `intent` block first when it has one** — submit mode\n+      captured the author's stated problem, approach, and rejected\n+      alternatives, so do not re-derive them from the diff. See\n+      `references/story-mode.md`'s opening for what carries over and what\n+      does not.\n       Author the four levels (`landscape`, `problem_solution`, `architecture`,\n       `file_changes`), the tagline, and the `voice` scripts directly into\n       `data/story.json` for this PR. **`problem_solution` and `architecture`\n@@ -296,7 +318,11 @@ Per-PR narrative + ADR + art + audio sweep. Steps:\n       `kind` values per level and worked guidance.\n    2. **ADR retro-extraction**. Follow `references/decision-records-lite.md`.\n       Write/update `data/adrs.json` and `data/adrs.js`, and set this PR's `adrs[]`\n-      array in `story.json` to the resulting record ids.\n+      array in `story.json` to the resulting record ids. When the PR carries an\n+      `intent` block, §7 of that reference applies: take `alternatives` from\n+      the author instead of hunting for traces of them, and mark the record\n+      `provenance: authored`. An unmerged PR's record is `state: proposed`,\n+      not `approved` (§3.2).\n    3. **Diff extraction**:\n       ```bash\n       uv run \"${CLAUDE_PLUGIN_ROOT}/scripts/extract_diffs.py\" --repo <target> --bundle-dir <bundle-dir> --prs <N>\n@@ -611,6 +637,122 @@ produces are still valid deliverables — tell the user where they landed\n (`<bundle-dir>/exports/`) so they can open or share them another way instead\n of the run looking like it silently failed.\n \n+## Submit mode\n+\n+Interviews the author of a change, assesses that change against the bundle, and\n+opens the pull request. Submit mode is the author-side and reviewer-side half of\n+the plugin: every other mode narrates history, this one is used before the\n+history exists. It needs `uv` and a git repo, never `GEMINI_API_KEY`, and it\n+generates no art and no audio.\n+\n+Two references govern it, both loaded on demand:\n+`references/interview-guide.md` for what to ask and how, and\n+`references/review-mode.md` for the rubric, the verdicts, and the risk tiers.\n+\n+`--stage pre|post` selects the stage (default `pre`).\n+\n+### Pre stage\n+\n+1. **Resolve `<bundle-dir>`** per Hub resolution above, then **migrate the\n+   bundle**:\n+   ```bash\n+   uv run \"${CLAUDE_PLUGIN_ROOT}/scripts/migrate_bundle.py\" --bundle-dir <bundle-dir>\n+   ```\n+2. **Resolve the target**, in this order:\n+   - `--prs <N>` — the PR already exists. Use the existing open-PR path (see\n+     Generate mode step 3). Nothing gets created.\n+   - no `--prs` — the current branch. Ask `gh pr view --json number` for it\n+     first. If a PR already exists for this branch, adopt its number and\n+     continue as the case above; this is the re-run-after-review-feedback\n+     path. If not, this is a pre-submit run, and step 7 opens the PR.\n+3. **Extract the diff.** For a PR:\n+   ```bash\n+   uv run \"${CLAUDE_PLUGIN_ROOT}/scripts/extract_diffs.py\" --repo <target> --bundle-dir <bundle-dir> --prs <N>\n+   ```\n+   For a branch with no PR yet (`--base` defaults to the detected default\n+   branch):\n+   ```bash\n+   uv run \"${CLAUDE_PLUGIN_ROOT}/scripts/extract_diffs.py\" --repo <target> --bundle-dir <bundle-dir> --branch [<ref>] [--base <branch>]\n+   ```\n+   The branch form writes `<bundle-dir>/exports/branch-<slug>/diff.json` and\n+   touches nothing under `data/`.\n+4. **Gather the rest of the evidence before asking the author anything.** Read\n+   the touched districts in `<bundle-dir>/inventory.yaml`, every record in\n+   `<bundle-dir>/data/adrs.json` whose `problem` or `decision` covers those\n+   districts, the matching stack card per `references/stacks/README.md`, and\n+   the timeline entries for earlier PRs in the same districts. This order is\n+   not optional — `references/interview-guide.md` §2 depends on it.\n+5. **Interview the author** (Claude work, not a script). Follow\n+   `references/interview-guide.md`. Draft a hypothesis from step 4, ask only\n+   what the evidence cannot settle, then play the drafted `intent` back for\n+   confirmation. `--non-interactive`, or a session with no author present,\n+   takes the fallback in §6 of that file and sets `intent.source: \"inferred\"`.\n+6. **Assess** (Claude work, not a script). Follow `references/review-mode.md`:\n+   the three questions with evidence, the stack card's boundary greps, the\n+   district delta, the risk tier, `regret_risk`, and the verdict.\n+7. **Render, then open the PR.** Write `intent.json` and `assessment.json` into\n+   the branch staging directory (branch target) or the two blocks onto the\n+   timeline entry (PR target), then:\n+   ```bash\n+   uv run \"${CLAUDE_PLUGIN_ROOT}/scripts/render_review.py\" --repo <target> --bundle-dir <bundle-dir> {--prs <N> | --branch [<ref>]}\n+   ```\n+   If step 2 found no PR, open one — see **Submitting the PR** below. If a PR\n+   already exists, skip to step 8.\n+8. **File the results under the PR number.** Write `intent` and `assessment`\n+   onto that PR's timeline entry in `<bundle-dir>/data/story.json`, regenerate\n+   `data/story.js`, then re-render so the deliverables carry the number:\n+   ```bash\n+   uv run \"${CLAUDE_PLUGIN_ROOT}/scripts/render_review.py\" --repo <target> --bundle-dir <bundle-dir> --prs <N>\n+   ```\n+9. **Verify**:\n+   ```bash\n+   uv run \"${CLAUDE_PLUGIN_ROOT}/scripts/verify_bundle.py\" --bundle-dir <bundle-dir> --prs <N> --require-review --json\n+   ```\n+   Report the verdict, the risk tier, the finding count, the PR URL, and the\n+   paths of the two markdown files.\n+\n+### Submitting the PR\n+\n+Assess first, create second. The assessment is part of the author's decision\n+about whether to open the PR at all.\n+\n+1. **Make sure the branch is pushed.** If `git rev-parse --abbrev-ref\n+   --symbolic-full-name @{u}` finds no upstream, the branch needs\n+   `git push -u origin <branch>` before `gh pr create` can work.\n+2. **Confirm once, covering both outward actions.** Show the author the\n+   rendered description, the base branch, the verdict, and the risk tier. Then\n+   ask to \"push `<branch>` and open a PR against `<base>`\". Pushing a branch\n+   and opening a PR are public and hard to walk back. **Nothing fires without\n+   this confirmation** — not with `--force`, not in `--non-interactive`, which\n+   takes the `--no-create` path instead.\n+   - A `rework` verdict does not block anything. Offer three ways forward —\n+     open it, open it as a draft, or stop and fix first — and let the author\n+     pick. This mode reports; it never gates.\n+3. **Create it:**\n+   ```bash\n+   gh pr create --base <base> --head <branch> --title \"<title>\" --body-file <bundle-dir>/exports/branch-<slug>/description.md [--draft]\n+   ```\n+   Then read the number back with `gh pr view --json number`.\n+4. **Continue at pre-stage step 8** with that number.\n+\n+**Three cases end at the staging directory instead**, and all three are normal:\n+`--no-create`, `gh` missing or unauthenticated, and the author declining at\n+step 2. In each, tell the user where the files landed\n+(`<bundle-dir>/exports/branch-<slug>/`), print the exact `gh pr create` line\n+above so nothing is lost, and say that re-running `/prodyssey:submit` once the\n+PR exists files the content into `story.json`.\n+\n+### Post stage\n+\n+Runs after the PR merges. Same steps 1 through 4, then:\n+\n+5. **Compare the merged diff against the `intent` captured pre-merge** and\n+   write a second `assessment` with `stage: \"post\"` and a populated `drift`\n+   array. `references/review-mode.md` §7 holds the four drift kinds and the\n+   rule that matters most: **never rewrite the pre-stage `intent`.** Its value\n+   comes from being what the author said before the change shipped.\n+6. Render and verify as in pre-stage steps 8 and 9.\n+\n ## Notes\n \n - Narrative authoring and ADR extraction are Claude judgment work — never delegate\n@@ -619,11 +761,19 @@ of the run looking like it silently failed.\n   step further removed: the orchestrating Claude never writes `.mmd` files itself,\n   it delegates that to a per-PR subagent (see Generate mode, step 4) and only calls\n   a script (`build_diagrams.py`) to compile and validate the subagent's output into\n-  `data/diagrams.js`.\n+  `data/diagrams.js`. The author interview and the architecture assessment are the\n+  same kind of work — `render_review.py` lays out the result and judges none of it.\n - Never touch anything in `<target>` outside `<target>/.prodyssey/` and `<target>/.env`\n   (read-only check, never written by this skill) — `<hub>/.prodyssey/` is also a\n   sanctioned write location, for centrally-stored bundles and view-server bookkeeping.\n-- `story.json`'s `meta.schema_version` is `\"1.0\"` — `verify_bundle.py` gates on it.\n+  Submit mode's `git push` and `gh pr create` are the only actions that reach past\n+  this line, they never write a source file, and they only run after the explicit\n+  confirmation in Submit mode.\n+- `story.json`'s `meta.schema_version` is `\"1.2\"` — `verify_bundle.py` gates on it.\n+  `scripts/_bundle_meta.py` is the single source for that constant.\n+- Submit mode's `intent` and `assessment` live on the timeline entry, not in a file\n+  of their own. That is what puts them under `migrate_bundle.py`'s authored-field\n+  guard, and it is why the viewer needs no new global to render them.\n - View mode's PID/log files and the `active` symlink live under\n   `<hub>/.prodyssey/`, never inside a bundle directory — those two files plus\n   `active` are the only entries meant to stay out of the commit.",
  "skills/odyssey/references/baseline-derivation.md": "diff --git a/skills/odyssey/references/baseline-derivation.md b/skills/odyssey/references/baseline-derivation.md\nindex 120db76..6ec6b35 100644\n--- a/skills/odyssey/references/baseline-derivation.md\n+++ b/skills/odyssey/references/baseline-derivation.md\n@@ -79,6 +79,30 @@ vocabulary for architectural weight.\n      `map_quality: low` on the district-map output so downstream consumers\n      know not to over-trust it.\n \n+## 3a. District sensitivity (optional)\n+\n+Submit mode reads an optional `sensitivity` field on each district to set a\n+change's risk tier. Author it here, where the district's real contents are\n+already verified, instead of leaving submit mode to guess from path names.\n+\n+Three values:\n+\n+| Value | Assign it when the district holds |\n+|---|---|\n+| `sensitive` | authentication, authorization, permissions, billing or payments, data deletion, database migrations, infrastructure, or a security boundary |\n+| `architectural` | a module boundary, a dependency direction, a public interface, or a cross-cutting pattern that other districts depend on |\n+| `routine` | everything else |\n+\n+**Assign it from the files, not from the name.** A district called `auth` that\n+holds only login-page copy is `routine`. A district called `utils` that holds\n+the token verifier is `sensitive`. This is the same §1 discipline: check\n+before you claim.\n+\n+The field is optional, and omitting it costs little. Submit mode falls back to\n+reading the diff directly when a district carries no value, and\n+`references/review-mode.md` §4 holds that fallback. A bundle written before\n+this field existed keeps working.\n+\n ## 4. Writing the outputs\n \n ### `world.districts` in `data/story.json`\n@@ -93,10 +117,14 @@ doesn't exist yet — see SKILL.md Baseline mode step 1). Shape:\n   \"label\": \"<authored label>\",\n   \"kind\": \"core|tooling|quality|knowledge|product|governance|unknown\",\n   \"blurb\": \"<one-line, grounded in verified contents>\",\n-  \"root_paths\": [\"<dir>\", \"...\"]\n+  \"root_paths\": [\"<dir>\", \"...\"],\n+  \"sensitivity\": \"routine|architectural|sensitive\"\n }\n ```\n \n+`sensitivity` is optional — see §3a. Leave the key out rather than defaulting\n+it to `routine`, so an absent value stays distinguishable from a judged one.\n+\n Never overwrite a district entry that already has human-authored fields (a\n `blurb` that doesn't match the auto-generated pattern) — treat existing\n non-placeholder text as authored and leave it, per the same discipline\n@@ -112,6 +140,7 @@ contexts:\n     label: <authored label>\n     paths: [<dir>, ...]\n     summary: <one-line, grounded in verified contents>\n+    sensitivity: routine|architectural|sensitive   # optional, see §3a\n ```\n \n `contexts[].id` matches `world.districts[].id` in `story.json` — this is the",
  "skills/odyssey/references/decision-records-lite.md": "diff --git a/skills/odyssey/references/decision-records-lite.md b/skills/odyssey/references/decision-records-lite.md\nindex 248dc80..d6b3ad0 100644\n--- a/skills/odyssey/references/decision-records-lite.md\n+++ b/skills/odyssey/references/decision-records-lite.md\n@@ -67,6 +67,12 @@ section.\n    step exists in this lite model (that machinery is dropped). Set\n    `source_pr` and note in the body that the record is retroactively\n    extracted.\n+\n+   **Carve-out for an unmerged PR.** A decision in a PR that has not merged is\n+   a proposal, not an approved decision. Use `state: proposed` for a PR whose\n+   `timeline[].status` is `\"open\"`. Submit mode never writes an ADR at all —\n+   see §7 — so this case only arises when generate mode runs against an open\n+   PR. Change the state to `approved` on the next run after the PR merges.\n 3. **One record per structural decision** — module boundary, dependency\n    direction, data-flow choice, public interface, cross-cutting pattern.\n    Not every PR produces one; most PRs produce zero or one. Do not manufacture\n@@ -76,6 +82,11 @@ section.\n    actually consider. If you can't find any, either the PR isn't ADR-worthy or\n    the alternative is genuinely just \"do nothing,\" which is a legitimate\n    entry.\n+\n+   **Prefer the author's own answer when it exists.** See §7. An `intent`\n+   block on the timeline entry holds `alternatives` in this exact\n+   `{option, rejected_because}` shape, stated by the person who made the\n+   choice. Copy those instead of searching for traces of them.\n 5. **Examples never live in the register.** Sample/demo records belong in this\n    reference file, not in a real bundle's `data/adrs.json`.\n \n@@ -101,13 +112,40 @@ they map to; there is no separate provenance field on the record itself.\n ## 6. Workflow — retro-extraction from a merged PR\n \n 1. Read the PR's diff (`extract_diffs.py` output) and touched files.\n-2. Identify zero or more *structural* decisions in the diff (see §3.3).\n-3. For each: fill the shape in §1, using `references/adr-template.md` as the\n+2. Read this PR's `intent` block, if it has one (§7).\n+3. Identify zero or more *structural* decisions in the diff (see §3.3).\n+4. For each: fill the shape in §1, using `references/adr-template.md` as the\n    body skeleton. Ground `problem`/`decision`/`alternatives`/`forces` in what\n    the diff and surrounding code actually show — never speculate beyond the\n    evidence.\n-4. Assign the next free `ADR-NNNN` id.\n-5. Write/merge into `<bundle-dir>/data/adrs.json`, regenerate\n+5. Assign the next free `ADR-NNNN` id.\n+6. Write/merge into `<bundle-dir>/data/adrs.json`, regenerate\n    `data/adrs.js`, and set this PR's `adrs: [\"ADR-NNNN\", ...]` array in\n    `data/story.json` so story mode's level 3 can pull `alternatives`/`forces`\n    straight from these records (see `story-mode.md` §2).\n+\n+## 7. When the author already answered\n+\n+Submit mode (`/prodyssey:submit`) interviews the author before the PR opens,\n+and writes an `intent` block onto the PR's timeline entry. See\n+`references/interview-guide.md` §1 for the shape. When that block is present,\n+it changes this workflow in three ways:\n+\n+1. **`problem` and the body's `## Context` come from `intent.problem` and\n+   `intent.why_now`.** These are statements of fact by the person who made the\n+   change. Do not replace them with your reading of the diff.\n+2. **`alternatives` come from `intent.alternatives`.** The shape already\n+   matches, so copy the entries. This is the case §3.4's escape hatch exists\n+   for, and an interviewed PR does not need it.\n+3. **The record carries `provenance: authored` instead of `inferred`.** §5's\n+   default applies only to a record you reconstructed yourself. A reader needs\n+   to know which of the two they are looking at.\n+\n+Two rules do not relax. A record still needs a real *structural* decision\n+(§3.3) — an interview does not make a routine PR ADR-worthy. And an `intent`\n+block whose own `source` is `\"inferred\"` is not an author statement: it is\n+another reading of the evidence, so the record stays `provenance: inferred`.\n+\n+**Submit mode itself writes no ADR.** A pre-merge decision is a proposal, and\n+the register holds decisions. Generate mode writes the record after the PR\n+merges, reading the `intent` the interview captured.",
  "skills/odyssey/references/interview-guide.md": "diff --git a/skills/odyssey/references/interview-guide.md b/skills/odyssey/references/interview-guide.md\nnew file mode 100644\nindex 0000000..8d1e283\n--- /dev/null\n+++ b/skills/odyssey/references/interview-guide.md\n@@ -0,0 +1,150 @@\n+---\n+title: \"Interview Guide — author intent capture reference\"\n+type: reference\n+status: active\n+last_verified: 2026-08-04\n+owner: bjoerns\n+---\n+\n+# Interview Guide — author intent capture reference\n+\n+How to interview the author of a change, and how to write the result into the\n+PR's `intent` block in `data/story.json`. Submit mode runs this before it\n+assesses the change, and before it opens the pull request.\n+\n+A diff shows what changed. It does not show which problem the author solved,\n+which options the author rejected, or which parts the author does not\n+understand. Generate mode infers those three things from merged code, long\n+after the fact. The interview collects them while the author still knows them.\n+\n+## 1. What the interview collects\n+\n+One JSON object on this PR's timeline entry:\n+\n+```json\n+\"intent\": {\n+  \"captured\": \"2026-08-04\",\n+  \"source\": \"author\",\n+  \"authorship\": \"agent-assisted\",\n+  \"problem\": \"<the problem this change solves>\",\n+  \"why_now\": \"<why the problem is worth solving here, and now>\",\n+  \"approach\": \"<the chosen solution, and why this one>\",\n+  \"alternatives\": [\n+    {\"option\": \"<considered and rejected>\", \"rejected_because\": \"<why>\"}\n+  ],\n+  \"out_of_scope\": [\"<deliberately not done in this change>\"],\n+  \"risks\": [\"<what the author expects to be risky>\"],\n+  \"testing\": \"<how the author checked the change>\",\n+  \"reviewer_focus\": [\"<where the author wants a reviewer to look>\"],\n+  \"unknowns\": [\"<what the author cannot explain>\"]\n+}\n+```\n+\n+`source` is `author` after a real interview. It is `inferred` after the\n+fallback in §6. `authorship` is `human`, `agent-assisted`, or\n+`agent-generated`.\n+\n+`alternatives` uses the same `{option, rejected_because}` shape as a record in\n+`data/adrs.json`. This is deliberate. An alternative that the author states\n+here moves into an ADR with no rewrite, and it answers\n+`decision-records-lite.md` §3.4 without archaeology.\n+\n+## 2. Read the evidence before you ask anything\n+\n+**Never ask a question that the evidence already answers.** An author who\n+answers three questions you could have answered yourself stops giving you real\n+answers to the fourth. Read the diff, the touched districts in\n+`inventory.yaml`, the ADRs that cover those districts, the matching stack\n+card, and the timeline entries for earlier PRs in the same districts. Then\n+draft your own hypothesis of the author's intent. Ask only about the gaps.\n+\n+The gaps are usually the same four things, because a diff cannot carry them:\n+\n+1. **Why this problem, and why now.** Code shows the solution. It does not\n+   show the pressure that produced it.\n+2. **Which options the author rejected.** A rejected option leaves no trace in\n+   the diff. This is the field the retro-extraction path most often loses.\n+3. **What the author left out on purpose.** An absence looks the same as an\n+   oversight from the outside.\n+4. **Which parts worry the author.** A reviewer cannot read confidence off a\n+   diff.\n+\n+## 3. Question budget\n+\n+**Target six questions. Stop at eight.** A long interview trains the author to\n+answer fast and short, which is the opposite of the goal. Rank the gaps by\n+what the assessment cannot proceed without, and drop the rest.\n+\n+Use the `AskUserQuestion` tool for a closed question, where you can offer real\n+options:\n+\n+- which authorship mode applies (§4);\n+- which of the alternatives you found in the code the author actually\n+  considered;\n+- which areas the author wants a reviewer to look at.\n+\n+Use an ordinary conversational turn for an open question, where an option list\n+would put words in the author's mouth: the problem, the reason for the timing,\n+and the approach.\n+\n+Ask about the problem first. Every later question reads better once you know\n+what the author was trying to do.\n+\n+## 4. Authorship and unknowns\n+\n+These two fields carry the reason submit mode exists. Do not skip them.\n+\n+**Ask who wrote the code.** `human`, `agent-assisted`, or `agent-generated`.\n+This is not a judgment about the author. Agent-written code holds less human\n+intent than its diff suggests, and the assessment needs to know that before it\n+reads confidence into a clean-looking change.\n+\n+**Record what the author cannot explain, in the author's own words.** \"The\n+agent wrote that part, and I am not sure why\" is a result, not a failed\n+interview. Write it into `unknowns` verbatim. Do not paraphrase it into\n+something more confident, and do not fill the gap with your own reading of the\n+code.\n+\n+A non-empty `unknowns` array raises the risk tier by one step. See\n+`review-mode.md` §4.\n+\n+## 5. Play the draft back\n+\n+**Show the drafted `intent` to the author before you write it to disk.** You\n+are about to attribute statements to a person, in a file that the repo\n+commits, and that later drives narrative and ADR text. Show the full block,\n+ask for corrections, and apply them.\n+\n+Correct the author's words only for length. Do not improve the argument. If\n+the stated reason for the approach is thin, that is a finding for the\n+assessment, not a thing to fix in the quote.\n+\n+## 6. Non-interactive fallback\n+\n+Submit mode runs with `--non-interactive`, or reaches a session with no author\n+present. Harvest what you can from the PR body, the commit messages, the\n+branch name, and any linked issue. Then set `source: \"inferred\"`.\n+\n+The `inferred` value mirrors the `provenance: inferred` convention that\n+`decision-records-lite.md` §5 already uses. It marks the block as your reading\n+of the evidence, not the author's statement.\n+\n+Two rules hold in this mode:\n+\n+1. **Leave a field empty rather than guess it.** An empty `alternatives` array\n+   is honest. An invented alternative corrupts every ADR that later reads it.\n+2. **`unknowns` stays empty.** Only an author can report what an author does\n+   not understand. Never infer it.\n+\n+## 7. Anti-patterns\n+\n+- **A fixed questionnaire.** The same seven questions on every PR produce the\n+  same seven shallow answers. Select questions from the diff.\n+- **Asking for a summary of the change.** You have the diff. Summarizing it is\n+  your job, not the author's.\n+- **Writing intent the author did not state.** The `intent` block is a record\n+  of what a person said. Your reading of the change belongs in `assessment`.\n+- **Treating a short answer as a failed interview.** \"I do not know\" and \"no\n+  alternatives, this was the obvious fix\" are both real data. Record them.\n+- **Interviewing before reading the diff.** The whole method in §2 depends on\n+  this order.",
  "skills/odyssey/references/pr-description-template.md": "diff --git a/skills/odyssey/references/pr-description-template.md b/skills/odyssey/references/pr-description-template.md\nnew file mode 100644\nindex 0000000..378eef7\n--- /dev/null\n+++ b/skills/odyssey/references/pr-description-template.md\n@@ -0,0 +1,53 @@\n+<!-- PR body skeleton. Fields come from the `intent` block:\n+     references/interview-guide.md §1. scripts/render_review.py emits this\n+     shape. Keep the two in step when you change either one.\n+     Omit a section when its field is empty. Do not write a placeholder. -->\n+\n+## Problem\n+\n+<intent.problem — what this change solves. One paragraph.>\n+\n+<intent.why_now — why the problem is worth solving here, and now.>\n+\n+## Why this approach\n+\n+<intent.approach — the chosen solution, and the reason for choosing it.>\n+\n+## Alternatives considered\n+\n+<one line per intent.alternatives entry:>\n+- **<option>** — rejected because <rejected_because>.\n+\n+<When the array is empty and intent.source is \"author\", write:\n+ \"None. The author reports no alternative was considered.\">\n+\n+## Out of scope\n+\n+<one line per intent.out_of_scope entry. This section says what the diff\n+ deliberately does not do, which a reader cannot tell from an absence.>\n+\n+## Risks\n+\n+<one line per intent.risks entry.>\n+\n+## How this was tested\n+\n+<intent.testing.>\n+\n+## Where to focus\n+\n+<one line per intent.reviewer_focus entry. Put the reviewer's time where the\n+ author thinks it pays.>\n+\n+<When intent.unknowns is non-empty, add the entries here under the lead-in\n+ \"The author flagged these parts as not fully understood:\". This is the most\n+ useful line in the description, and it belongs where a reviewer reads it.>\n+\n+---\n+\n+<Footer, written by scripts/render_review.py, not by hand:>\n+\n+<`intent.source == \"inferred\"` → \"Intent inferred from the PR body, the commit\n+ messages, and the branch name. Not stated by the author.\">\n+\n+<`intent.authorship != \"human\"` → \"Authorship: <value>.\">",
  "skills/odyssey/references/review-mode.md": "diff --git a/skills/odyssey/references/review-mode.md b/skills/odyssey/references/review-mode.md\nnew file mode 100644\nindex 0000000..e3171c6\n--- /dev/null\n+++ b/skills/odyssey/references/review-mode.md\n@@ -0,0 +1,269 @@\n+---\n+title: \"Review Mode — pre-merge architecture assessment reference\"\n+type: reference\n+status: active\n+last_verified: 2026-08-04\n+owner: bjoerns\n+---\n+\n+# Review Mode — pre-merge architecture assessment reference\n+\n+How to assess a change against the bundle, and how to write the result into\n+the PR's `assessment` block in `data/story.json`. Submit mode runs this after\n+the interview in `interview-guide.md`, and before it opens the pull request.\n+\n+The assessment answers three questions about judgment. It does not hunt for\n+defects. A model already finds a missing null check, an unhandled error path,\n+or a naming inconsistency, and every repo has other tools for that work. What\n+no diff-reading tool can answer is whether the change belongs in this system.\n+That question needs the district map, the decision history, and the narrated\n+timeline that the bundle already holds. Use them.\n+\n+## 1. What the assessment produces\n+\n+One JSON object on this PR's timeline entry:\n+\n+```json\n+\"assessment\": {\n+  \"stage\": \"pre\",\n+  \"generated\": \"2026-08-04\",\n+  \"verdict\": \"concerns\",\n+  \"risk_tier\": \"architectural\",\n+  \"summary\": \"<two or three sentences a reviewer can read first>\",\n+  \"sensible\": {\n+    \"answer\": \"<...>\", \"evidence\": [\"<path:line or ADR id>\"]\n+  },\n+  \"maintainability\": {\n+    \"answer\": \"<...>\",\n+    \"constraint_introduced\": \"<the invariant this change establishes>\",\n+    \"evidence\": [\"<path:line>\"]\n+  },\n+  \"pattern\": {\n+    \"verdict\": \"duplicate\",\n+    \"answer\": \"<...>\",\n+    \"duplicates\": [\"ADR-0003\"],\n+    \"evidence\": [\"<path:line>\"]\n+  },\n+  \"findings\": [\n+    {\"severity\": \"concern\", \"claim\": \"<...>\", \"evidence\": \"scripts/foo.py:41\",\n+     \"district\": \"scripts\", \"suggestion\": \"<...>\"}\n+  ],\n+  \"boundary_checks\": [\n+    {\"rule\": \"<the rule text>\", \"source\": \"stacks/python-fastapi.md\",\n+     \"result\": \"violation\", \"evidence\": \"<the grep hit>\"}\n+  ],\n+  \"delta\": {\n+    \"districts_added\": [], \"districts_changed\": [],\n+    \"edges_added\": [], \"edges_removed\": []\n+  },\n+  \"regret_risk\": \"<one paragraph>\",\n+  \"drift\": []\n+}\n+```\n+\n+`stage` is `pre` or `post`. `drift` stays empty until the post stage. See §7.\n+\n+## 2. Evidence discipline\n+\n+This rule comes from `baseline-derivation.md` §1, and it holds here without\n+change: **never write a claim you did not check.**\n+\n+- Every entry in `findings` carries an `evidence` value. That value is a\n+  `path:line` reference, or an ADR id, or a district id from\n+  `inventory.yaml`. A finding with no evidence does not go in the array.\n+- Read the file before you write about it. A diff hunk shows you the change.\n+  It does not show you the surrounding code that the change has to live with.\n+- Report what you could not check. \"I did not verify the callers of this\n+  function\" is a useful sentence. A confident claim about callers you never\n+  read is not.\n+- Do not repeat the author's `intent` back as a finding. The interview already\n+  recorded it. The assessment adds your reading, or it adds nothing.\n+\n+## 3. The three questions\n+\n+Answer all three. Each answer needs evidence, or it does not get written.\n+\n+### 3.1 Is this sensible?\n+\n+Does the change solve the problem that `intent.problem` states? Is that\n+problem worth solving in this repo, at this layer, in this district?\n+\n+Read the two halves separately. A change can solve its stated problem\n+correctly and still be the wrong change, because the problem belongs\n+somewhere else. Say which of the two halves fails when one does.\n+\n+When `intent.source` is `inferred`, say so in the answer. You are assessing\n+the change against your own reading of the problem, not against the author's\n+statement, and a reviewer needs to know that.\n+\n+### 3.2 Does this help or hurt maintainability and readability?\n+\n+Name the invariant that the change establishes. `adr-template.md` already\n+carries this phrasing as **\"Constraint introduced: the invariant this decision\n+establishes\"**, and `constraint_introduced` holds it here.\n+\n+A change helps when it removes a special case, moves a decision to one place,\n+or makes an illegal state harder to write. A change hurts when it adds a\n+second place to change for one reason, widens a public interface without a\n+caller that needs it, or makes a rule that only its author knows.\n+\n+Judge the code the repo will keep, not the diff. A large diff that deletes a\n+duplicated path improves maintainability. A small diff that adds a\n+config-driven branch to a hot function can hurt it.\n+\n+### 3.3 New valuable pattern, duplicate, or reinvention?\n+\n+This is the question the bundle exists to answer. Pick one verdict:\n+\n+| Verdict | Meaning |\n+|---|---|\n+| `conforms` | The change uses a pattern the repo already has, the way the repo already uses it. This is the common, good case. |\n+| `new-valuable` | The change introduces a pattern the repo does not have, and the pattern earns its place. Say what it buys. |\n+| `duplicate` | The change builds a second implementation of something a district or an ADR already provides. |\n+| `reinvention` | The change solves a problem the repo already solved, in a different way, and does not say why the existing way fails. |\n+\n+**A `duplicate` or a `reinvention` verdict must cite what it duplicates.** Put\n+the ADR id or the district id in `duplicates`, and the file evidence in\n+`evidence`. A verdict with no citation is an accusation, and it wastes the\n+reviewer's time. When you cannot cite it, the verdict is not `duplicate`.\n+\n+Search three sources before you answer:\n+\n+1. `data/adrs.json` — every structural decision the repo already made. Match\n+   on `problem` and on `decision`, not on the title.\n+2. `inventory.yaml` — does a district already own this capability?\n+3. The touched districts in the code, for a function or a module that already\n+   does this work.\n+\n+**Degrade honestly.** A bundle with one or two PRs holds almost no decision\n+history. The honest answer is then \"insufficient history to judge\", and you\n+write that. Do not manufacture a verdict from a thin baseline. This follows\n+`baseline-derivation.md` §3.\n+\n+### 3.4 Then answer the real question\n+\n+Write `regret_risk`: one paragraph on what the team lives with if this change\n+merges as written. Not \"does this work\". \"What do we regret in six months.\"\n+\n+Name the cost in the terms the repo will feel it: a second place to change, a\n+dependency somebody has to maintain, a boundary that later work has to route\n+around, a rule with no test to hold it.\n+\n+## 4. Risk tier\n+\n+`risk_tier` is `routine`, `architectural`, or `sensitive`. Compute it from\n+three inputs, and take the highest:\n+\n+1. **District sensitivity.** Read the optional `sensitivity` field on each\n+   touched district in `inventory.yaml`. See `baseline-derivation.md` §3a.\n+2. **Diff heuristics**, for a bundle whose baseline carries no `sensitivity`\n+   values. Treat a change to authentication, authorization, billing, data\n+   deletion, a database migration, infrastructure, or a security boundary as\n+   `sensitive`. Treat a change to a module boundary, a dependency direction, a\n+   public interface, or a cross-cutting pattern as `architectural`.\n+3. **Author understanding.** A non-empty `intent.unknowns` array raises the\n+   result by one step, `routine` to `architectural`, or `architectural` to\n+   `sensitive`. Code that the author cannot explain costs the team more than\n+   the same code with an author who can.\n+\n+The tier sets how deep the assessment goes, and submit mode reports it. **It\n+is not a gate.** Nothing in this mode blocks a merge, and nothing refuses to\n+open a pull request.\n+\n+## 5. Boundary checks from the stack card\n+\n+Detect the stack with `stacks/README.md`, then read the matching card. Two\n+sections drive this step:\n+\n+- **`## Boundary Rules`** — each rule ships with a literal `grep` command. Run\n+  it against the touched paths. Write one entry in `boundary_checks` per rule,\n+  with `result` set to `pass` or `violation`, and the grep hit as `evidence`.\n+- **`## Review Checks`** — the stack's named smells. Check each one against\n+  the diff. A hit becomes a `findings` entry, not a `boundary_checks` entry.\n+\n+Read `## Reference Structure` and `## ADR Topics` for context.\n+\n+**Never read `## Corpus Load`.** Those sections point at\n+`corpus/principles/**/*.yaml`, which this plugin does not ship. The paths are\n+dead. `README.md`'s extraction manifest records why.\n+\n+## 6. District delta\n+\n+`delta` records the change to the world map, before against after:\n+\n+- `districts_added` — a district that this change creates.\n+- `districts_changed` — `{\"id\": ..., \"files_before\": N, \"files_after\": N}`.\n+- `edges_added` and `edges_removed` — an import edge between districts that\n+  the change creates or deletes, as `\"<from> -> <to>\"`.\n+\n+Derive the edges the way `baseline-derivation.md` §1 does, with a grep in both\n+directions. Do not claim an edge you did not check.\n+\n+A new edge between two districts is the highest-value line in the whole\n+assessment. It is the moment a repo's shape changes, and it is nearly\n+invisible in a diff.\n+\n+## 7. The post stage\n+\n+The post stage runs after the PR merges. It reads the `intent` that the pre\n+stage captured, compares it against the merged diff, and writes `drift`.\n+\n+Each entry is `{\"kind\": ..., \"claim\": ..., \"evidence\": ...}`. Four kinds:\n+\n+| `kind` | What it reports |\n+|---|---|\n+| `out_of_scope` | The merged diff touches something `intent.out_of_scope` said it would not. |\n+| `unaddressed_risk` | An `intent.risks` entry with no test, no guard, and no follow-up in the merged code. |\n+| `adopted_alternative` | The merged code uses an option that `intent.alternatives` listed as rejected. |\n+| `delta_shift` | The district delta that landed differs from the delta the pre stage projected. |\n+\n+**Never rewrite the pre-stage `intent`.** The value of the post stage comes\n+from comparing what shipped against what the author said before it shipped. An\n+`intent` block edited after the fact reports nothing. Write a new\n+`assessment` with `stage: \"post\"`, and leave `intent` alone.\n+\n+Drift is not blame. A change of plan during review is normal and often right.\n+The entry records that the plan changed, so that the narrative and the ADRs\n+built later describe the change that merged.\n+\n+## 8. Verdict\n+\n+`verdict` is one value:\n+\n+| Verdict | Meaning |\n+|---|---|\n+| `sound` | The three questions all answer well. No blocker, and no unresolved concern. |\n+| `concerns` | The change works, and something in it costs the team. The `findings` array says what. |\n+| `rework` | A finding with `severity: blocker`, or a `duplicate` or `reinvention` verdict with a citation. |\n+\n+`severity` on a finding is `blocker`, `concern`, or `note`.\n+\n+A `rework` verdict does not stop submit mode from opening the pull request.\n+The author reads the verdict, and the author decides. See the Submit mode\n+section of `SKILL.md`.\n+\n+## 9. What this mode does not do\n+\n+- **No defect hunting.** No missing null check, no naming, no unhandled error\n+  path, no test-coverage gap. Other tools own that work, and a review that\n+  mixes commodity findings with judgment buries the judgment.\n+- **No score.** No number, no grade, no pass rate. The verdict and the\n+  findings carry the result.\n+- **No gate.** Nothing here blocks a merge or refuses to open a PR.\n+- **No corpus.** See §5.\n+- **No new ADR.** A pre-merge decision is a proposal. Generate mode writes the\n+  record after the PR merges. See `decision-records-lite.md` §3.2.\n+\n+## 10. Workflow\n+\n+1. Read the diff, `inventory.yaml`, `data/adrs.json`, the matching stack card,\n+   and the timeline entries for earlier PRs in the touched districts.\n+2. Read this PR's `intent` block, or run the interview first.\n+3. Answer §3.1, §3.2, and §3.3, with evidence for each.\n+4. Run the stack card's boundary greps (§5). Record the results.\n+5. Compute the district delta (§6).\n+6. Compute the risk tier (§4).\n+7. Write `regret_risk`, then pick the verdict (§8).\n+8. Write the block into this PR's timeline entry in `data/story.json`.\n+9. Render the deliverables:\n+   `uv run scripts/render_review.py --bundle-dir <bundle-dir> --prs <N>`.",
  "skills/odyssey/references/story-mode.md": "diff --git a/skills/odyssey/references/story-mode.md b/skills/odyssey/references/story-mode.md\nindex 0ab3e43..bed6208 100644\n--- a/skills/odyssey/references/story-mode.md\n+++ b/skills/odyssey/references/story-mode.md\n@@ -23,6 +23,30 @@ not the codebase's current state). This applies to `narration`, `problem`/\n `solution`, `beats`, and `voice` alike — the register (plain vs.\n developer-precise) is unchanged, only the tense/certainty.\n \n+**When the PR carries an `intent` block, read it first.** Submit mode\n+(`/prodyssey:submit`) interviews the author before the PR opens and writes\n+what they said onto the timeline entry — see `references/interview-guide.md`\n+§1. Everything below still applies to how you write. What changes is where the\n+content comes from:\n+\n+- `intent.problem` and `intent.why_now` are the problem. Level 2's `problem`\n+  and its `background` beat start there, and you do not re-derive them from\n+  the diff. Rewrite for register and length, never for meaning.\n+- `intent.approach` is the solution, and `intent.alternatives` supplies level\n+  3's rejected options directly.\n+- `intent.out_of_scope` is the honest boundary of the change, which a diff\n+  cannot show. It belongs in the level 3 narrative when it explains a\n+  surprising absence.\n+- `intent.unknowns` is not narrative material. It is a review signal, it lives\n+  in the assessment, and it does not belong in a story a reader trusts.\n+\n+Still ground every *mechanism* claim in the diff. The author states the intent.\n+Only the code states what the change actually does, and the two can disagree —\n+that disagreement is exactly what submit mode's post stage reports.\n+\n+An `intent` block whose `source` is `\"inferred\"` was not stated by anyone. Treat\n+it as one more reading of the evidence, at the same weight as your own.\n+\n ## 1. Framework origin\n \n The four-part structure is Geoffrey Litt's **explain-diff** framework",
  "viewer/index.html": "diff --git a/viewer/index.html b/viewer/index.html\nindex b10e8aa..14a7fca 100644\n--- a/viewer/index.html\n+++ b/viewer/index.html\n@@ -292,6 +292,9 @@\n \n   .alt-section-label{font:10.5px 'JetBrains Mono',monospace;letter-spacing:.1em;color:var(--ink-dim);text-transform:uppercase;margin-bottom:8px;}\n   .alt-row{display:flex;gap:12px;flex-wrap:wrap;}\n+  /* A `display:` rule beats the plain `hidden` attribute, so the ADR sheet's\n+     \"+N more\" disclosure needs this to stay hidden until the button is clicked. */\n+  .alt-row[hidden]{display:none;}\n   .alt-card{width:240px;background:#fff;border:1px dashed oklch(0.6 0.13 25);border-radius:8px;padding:10px 12px;}\n   .alt-card .alt-opt{font-size:12px;font-weight:600;color:oklch(0.3 0.02 250);}\n   .alt-card .alt-rej{font-size:11px;color:oklch(0.45 0.14 25);margin-top:5px;line-height:1.4;}\n@@ -304,6 +307,16 @@\n   .adr-badge:hover{background:oklch(0.94 0.04 60);border-color:oklch(0.48 0.13 60);}\n   .no-adrs{font:11.5px system-ui,sans-serif;color:var(--ink-dim);}\n \n+  /* Submit-mode assessment badge: same row as the ADR badges, deliberately a\n+     different hue so a verdict never reads as an ADR id. */\n+  .assessment-badge{\n+    font:10.5px 'JetBrains Mono',monospace;padding:4px 10px;border-radius:3px;cursor:pointer;\n+    border:1px solid oklch(0.55 0.12 265);color:oklch(0.44 0.12 265);background:oklch(0.97 0.02 265);\n+    display:inline-flex;align-items:center;gap:6px;\n+  }\n+  .assessment-badge:hover{background:oklch(0.94 0.04 265);border-color:oklch(0.48 0.13 265);}\n+  .assessment-badge .ab-dot{width:6px;height:6px;border-radius:50%;background:currentColor;}\n+\n   .beats-stack{display:flex;flex-direction:column;gap:10px;}\n   .beat-card.fixed-card{height:180px;}\n   .beat-card .fc-kicker{color:oklch(0.5 0.02 250);}\n@@ -453,18 +466,42 @@\n   /* ================= ADR SHEET (same shadcn-Sheet family as the comments drawer,\n      just wider — reuses .drawer-header/.drawer-close/.drawer-body). Shares\n      #drawer-scrim with the comments drawer; JS enforces only one open at a time. ================= */\n-  #adr-sheet{\n+  #adr-sheet,\n+  #assessment-sheet{\n     position:fixed;top:0;right:0;bottom:0;width:clamp(480px, 40vw, 720px);max-width:92vw;background:#fff;\n     border-left:1px solid var(--line);border-radius:14px 0 0 14px;box-shadow:-12px 0 34px rgba(20,25,35,0.2);\n     transform:translateX(100%);transition:transform .25s ease;z-index:41;\n     display:flex;flex-direction:column;min-height:0;\n   }\n-  #adr-sheet.open{transform:translateX(0);}\n-  #adr-sheet .drawer-header{align-items:flex-start;}\n+  #adr-sheet.open,\n+  #assessment-sheet.open{transform:translateX(0);}\n+  #adr-sheet .drawer-header,\n+  #assessment-sheet .drawer-header{align-items:flex-start;}\n   .adr-sheet-meta{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:6px;}\n   .adr-state-badge{font:10px 'JetBrains Mono',monospace;letter-spacing:.04em;padding:3px 9px;border-radius:10px;border:1px solid;text-transform:uppercase;white-space:nowrap;}\n   .adr-sheet-sourceline{font:11px 'JetBrains Mono',monospace;color:var(--ink-dim);}\n-  #adr-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+  #adr-sheet .drawer-body,\n+  #assessment-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+\n+  /* Assessment-only blocks. Everything else in the sheet reuses .adr-block,\n+     .ab-kicker, .force-chip, .alt-section-label, and .adr-body-md. */\n+  .asm-finding{border-radius:8px;border:1px solid var(--line);background:#fff;padding:10px 12px;border-left:3px solid var(--line);}\n+  .asm-finding.blocker{border-left-color:oklch(0.55 0.19 25);}\n+  .asm-finding.concern{border-left-color:oklch(0.66 0.15 70);}\n+  .asm-finding.note{border-left-color:oklch(0.7 0.05 250);}\n+  .asm-finding .asm-sev{font:700 9.5px 'JetBrains Mono',monospace;letter-spacing:.1em;text-transform:uppercase;color:var(--ink-dim);}\n+  .asm-finding.blocker .asm-sev{color:oklch(0.5 0.18 25);}\n+  .asm-finding.concern .asm-sev{color:oklch(0.5 0.14 70);}\n+  .asm-finding .asm-claim{font-size:12.5px;line-height:1.5;color:oklch(0.28 0.02 250);margin-top:5px;}\n+  .asm-finding .asm-ev{font:10.5px 'JetBrains Mono',monospace;color:var(--ink-dim);margin-top:6px;word-break:break-all;}\n+  .asm-finding .asm-fix{font-size:12px;line-height:1.5;color:oklch(0.4 0.02 250);margin-top:6px;}\n+  .asm-stack{display:flex;flex-direction:column;gap:8px;}\n+  .asm-stack[hidden]{display:none;}\n+  .asm-check{display:flex;gap:8px;align-items:baseline;font-size:12px;line-height:1.5;color:oklch(0.3 0.02 250);}\n+  .asm-check .asm-res{font:700 9.5px 'JetBrains Mono',monospace;letter-spacing:.06em;text-transform:uppercase;white-space:nowrap;}\n+  .asm-check.pass .asm-res{color:oklch(0.45 0.13 145);}\n+  .asm-check.violation .asm-res{color:oklch(0.5 0.18 25);}\n+  .asm-delta{font:11.5px 'JetBrains Mono',monospace;color:oklch(0.3 0.02 250);line-height:1.7;}\n \n   .adr-block{border-radius:10px;border:1px solid var(--line);background:#fff;padding:14px 16px;}\n   .adr-block.problem{border-left:3px solid oklch(0.6 0.16 25);}\n@@ -636,6 +673,22 @@\n     <div class=\"drawer-body\" id=\"adr-sheet-body\"></div>\n   </div>\n \n+  <!-- Submit-mode assessment sheet (same Sheet family as the ADR sheet above,\n+       reachable from the Assessment badge on level 3). Reads the timeline\n+       entry's own `assessment`/`intent` blocks — there is no window.ASSESSMENTS,\n+       because both live inline in story.json. Shares #drawer-scrim; only one\n+       sheet is ever open. -->\n+  <div id=\"assessment-sheet\" role=\"dialog\" aria-label=\"Architecture assessment\">\n+    <div class=\"drawer-header\">\n+      <div>\n+        <h2 id=\"assessment-sheet-title\">Assessment</h2>\n+        <div class=\"adr-sheet-meta\" id=\"assessment-sheet-meta\"></div>\n+      </div>\n+      <button class=\"drawer-close\" id=\"assessment-sheet-close-btn\" title=\"Close (Esc)\">&times;</button>\n+    </div>\n+    <div class=\"drawer-body\" id=\"assessment-sheet-body\"></div>\n+  </div>\n+\n   <!-- Narration audio dialog (shadcn Dialog look, centered). Shares #drawer-scrim above;\n        mutually exclusive with the comments drawer / ADR sheet — see openAudioDialog. -->\n   <div id=\"audio-dialog-layer\">\n@@ -749,7 +802,9 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     // ADR sheet: mutually exclusive with the comments drawer (see openCommentsDrawer/openAdrSheet).\n     adrOpen: false,\n     activeAdrId: null,\n-    // Audio dialog: mutually exclusive with both sheets above (see openAudioDialog).\n+    // Submit-mode assessment sheet: mutually exclusive with both panels above.\n+    assessmentOpen: false,\n+    // Audio dialog: mutually exclusive with every sheet above (see openAudioDialog).\n     audioDialogOpen: false,\n     // true while a level/PR content transition animation is in flight — guards re-entrant\n     // nav input (arrow mashing, double rail clicks) from stacking transitions.\n@@ -1620,6 +1675,15 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n         ? adrs.map(a => `<button class=\"adr-badge\" data-adr-id=\"${escapeHtml(a)}\">${escapeHtml(a)}</button>`).join('')\n         : `<span class=\"no-adrs\">No ADRs recorded for this PR.</span>`;\n \n+      // Assessment badge, only when submit mode actually left one behind. A PR\n+      // generated before submit mode existed simply shows no badge.\n+      const asm = p.assessment;\n+      const assessmentHtml = (asm && asm.verdict)\n+        ? `<button class=\"assessment-badge\" id=\"assessment-badge\" title=\"Architecture assessment (submit mode)\">\n+             <span class=\"ab-dot\"></span>Assessment: ${escapeHtml(asm.verdict)}\n+           </button>`\n+        : '';\n+\n       const boxesHtml = DISTRICTS.map(d => {\n         const isTouched = !!touched[d.id];\n         const color = KIND_COLOR[d.kind] || KIND_COLOR.tooling;\n@@ -1640,13 +1704,15 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n       setMainHtml(main, `\n         <div class=\"level-stack narrow\">\n           ${heroFrame(p.pr, 3, `PR #${p.pr} architecture diagram`)}\n-          <div class=\"adr-row\">${adrHtml}</div>\n+          <div class=\"adr-row\">${adrHtml}${assessmentHtml}</div>\n           <div class=\"district-boxes\">${boxesHtml}</div>\n           ${beatsHtml}\n         </div>`);\n       main.querySelectorAll('.adr-badge[data-adr-id]').forEach(btn => {\n         btn.addEventListener('click', () => openAdrSheet(btn.dataset.adrId));\n       });\n+      const asmBtn = main.querySelector('#assessment-badge');\n+      if (asmBtn) asmBtn.addEventListener('click', openAssessmentSheet);\n \n     } else {\n       const fc = content.file_changes;\n@@ -1887,6 +1953,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   function openAudioDialog(){\n     if (state.commentsOpen) state.commentsOpen = false;\n     if (state.adrOpen) state.adrOpen = false;\n+    if (state.assessmentOpen) state.assessmentOpen = false;\n     state.audioDialogOpen = true;\n     renderAudioDialogContent();\n     const dialog = document.getElementById('audio-dialog');\n@@ -2117,6 +2184,10 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n       state.level = 1;\n       state.focusDistrict = null;\n       state.draftMergeMethod = null;\n+      // The assessment sheet renders the CURRENT pr(), so leaving it open across\n+      // a PR switch would show the new PR's data under the old PR's scroll\n+      // position — and level resets to 1, where its badge is not even visible.\n+      if (state.assessmentOpen) closeAssessmentSheet();\n       closeMergeMenu();\n       stopAudio();\n       renderAll();\n@@ -2148,19 +2219,22 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   }\n \n   /* ============================================================\n-     COMMENTS DRAWER + ADR SHEET (shadcn Sheet look, recreated in vanilla JS/CSS)\n-     Both panels share one #drawer-scrim; opening either one closes the other first, so\n-     at most one sheet is ever open — this is the \"only one sheet at a time\" rule.\n+     COMMENTS DRAWER + ADR SHEET + ASSESSMENT SHEET (shadcn Sheet look, recreated\n+     in vanilla JS/CSS). All three panels share one #drawer-scrim; opening any one\n+     closes the others first, so at most one sheet is ever open — this is the\n+     \"only one sheet at a time\" rule.\n      ============================================================ */\n   function renderSheetVisibility(){\n     document.getElementById('comments-drawer').classList.toggle('open', state.commentsOpen);\n     document.getElementById('adr-sheet').classList.toggle('open', state.adrOpen);\n+    document.getElementById('assessment-sheet').classList.toggle('open', state.assessmentOpen);\n     const scrim = document.getElementById('drawer-scrim');\n-    scrim.classList.toggle('open', state.commentsOpen || state.adrOpen || state.audioDialogOpen);\n+    scrim.classList.toggle('open', state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen);\n     scrim.classList.toggle('audio-dim', state.audioDialogOpen);\n   }\n   function openCommentsDrawer(){\n     if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.commentsOpen = true;\n     renderSheetVisibility();\n@@ -2172,6 +2246,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   }\n   function openAdrSheet(id){\n     if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.adrOpen = true;\n     state.activeAdrId = id;\n@@ -2182,9 +2257,22 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     state.adrOpen = false;\n     renderSheetVisibility();\n   }\n+  function openAssessmentSheet(){\n+    if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n+    state.assessmentOpen = true;\n+    renderAssessmentSheet();\n+    renderSheetVisibility();\n+  }\n+  function closeAssessmentSheet(){\n+    state.assessmentOpen = false;\n+    renderSheetVisibility();\n+  }\n   function closeAnyOpenSheet(){\n     if (state.commentsOpen) closeCommentsDrawer();\n     else if (state.adrOpen) closeAdrSheet();\n+    else if (state.assessmentOpen) closeAssessmentSheet();\n     else if (state.audioDialogOpen) stopAudio();\n   }\n \n@@ -2352,6 +2440,201 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n     }\n   }\n \n+  /* ---------- Submit-mode assessment sheet ----------\n+     Reads pr().assessment / pr().intent straight off window.STORY. No new\n+     global and no new data file: both blocks live inline on the timeline\n+     entry, which is what gives them migrate_bundle.py's authored-field guard.\n+     Verdict/tier vocabulary: skills/odyssey/references/review-mode.md §8, §4. */\n+  const VERDICT_COLOR = {\n+    sound:    ['oklch(0.94 0.06 145)', 'oklch(0.4 0.13 145)', 'oklch(0.6 0.12 145)'],\n+    concerns: ['oklch(0.95 0.06 70)',  'oklch(0.44 0.13 70)', 'oklch(0.65 0.13 70)'],\n+    rework:   ['oklch(0.95 0.05 25)',  'oklch(0.45 0.17 25)', 'oklch(0.62 0.16 25)'],\n+  };\n+  const PATTERN_LABEL = {\n+    conforms: 'Conforms to an existing pattern',\n+    'new-valuable': 'New pattern, and it earns its place',\n+    duplicate: 'Duplicates something the repo already has',\n+    reinvention: 'Reinvents a solved problem',\n+  };\n+  const DRIFT_LABEL = {\n+    out_of_scope: 'Out of scope, but touched',\n+    unaddressed_risk: 'Risk with no guard',\n+    adopted_alternative: 'Rejected option adopted',\n+    delta_shift: 'District delta shifted',\n+  };\n+\n+  function verdictBadgeHtml(v){\n+    const [bg, fg, border] = VERDICT_COLOR[v] || ['oklch(0.96 0.02 250)', 'var(--ink-dim)', 'var(--line)'];\n+    return `<span class=\"adr-state-badge\" style=\"background:${bg};color:${fg};border-color:${border};\">${escapeHtml((v || 'unknown').toUpperCase())}</span>`;\n+  }\n+\n+  function asmQuestionHtml(kicker, block, extraLabel, extraKey){\n+    if (!block || !block.answer) return '';\n+    const ev = (block.evidence || []).filter(Boolean);\n+    return `\n+      <div class=\"adr-block\">\n+        <div class=\"ab-kicker\" style=\"color:var(--accent-text);\">${escapeHtml(kicker)}</div>\n+        <p>${escapeHtml(block.answer)}</p>\n+        ${extraKey && block[extraKey] ? `<p style=\"margin-top:8px;\"><strong>${escapeHtml(extraLabel)}:</strong> ${escapeHtml(block[extraKey])}</p>` : ''}\n+        ${ev.length ? `<div class=\"chip-row\" style=\"margin-top:10px;\">${ev.map(e => `<span class=\"force-chip\">${escapeHtml(e)}</span>`).join('')}</div>` : ''}\n+      </div>`;\n+  }\n+\n+  function renderAssessmentSheet(){\n+    const p = pr();\n+    const a = p && p.assessment;\n+    const intent = (p && p.intent) || null;\n+    const titleEl = document.getElementById('assessment-sheet-title');\n+    const metaEl = document.getElementById('assessment-sheet-meta');\n+    const body = document.getElementById('assessment-sheet-body');\n+\n+    if (!a){\n+      titleEl.textContent = 'Assessment';\n+      metaEl.innerHTML = '';\n+      body.innerHTML = `\n+        <div class=\"empty-card\">\n+          <div class=\"empty-detail\">No assessment recorded for this PR. Run <code>/prodyssey:submit</code> against it.</div>\n+        </div>`;\n+      return;\n+    }\n+\n+    titleEl.textContent = `PR #${p.pr} — assessment`;\n+    const metaBits = [\n+      a.risk_tier ? `risk: ${a.risk_tier}` : '',\n+      a.stage === 'post' ? 'post-merge' : 'pre-merge',\n+      a.generated ? a.generated : '',\n+    ].filter(Boolean).join(' · ');\n+    metaEl.innerHTML = verdictBadgeHtml(a.verdict)\n+      + (metaBits ? `<span class=\"adr-sheet-sourceline\">${escapeHtml(metaBits)}</span>` : '');\n+\n+    const summaryHtml = a.summary ? `\n+      <div class=\"adr-block decision\">\n+        <div class=\"ab-kicker\">Summary</div>\n+        <p>${escapeHtml(a.summary)}</p>\n+      </div>` : '';\n+\n+    // The author's own words come first — the assessment is a response to them.\n+    let intentHtml = '';\n+    if (intent && intent.problem){\n+      const flags = [\n+        intent.source === 'inferred' ? 'intent inferred, not stated by the author' : '',\n+        intent.authorship && intent.authorship !== 'human' ? `authorship: ${intent.authorship}` : '',\n+        (intent.unknowns || []).length ? `${intent.unknowns.length} author-flagged unknown(s)` : '',\n+      ].filter(Boolean);\n+      intentHtml = `\n+        <div class=\"adr-block problem\">\n+          <div class=\"ab-kicker\">The author's stated problem</div>\n+          <p>${escapeHtml(intent.problem)}</p>\n+          ${flags.length ? `<div class=\"chip-row\" style=\"margin-top:10px;\">${flags.map(f => `<span class=\"force-chip\">${escapeHtml(f)}</span>`).join('')}</div>` : ''}\n+        </div>`;\n+    }\n+\n+    let patternHtml = '';\n+    if (a.pattern && a.pattern.answer){\n+      const label = PATTERN_LABEL[a.pattern.verdict] || a.pattern.verdict || '';\n+      const dupes = (a.pattern.duplicates || []).filter(Boolean);\n+      patternHtml = asmQuestionHtml('Q3 — pattern, duplicate, or reinvention', a.pattern);\n+      patternHtml = patternHtml.replace(\n+        '<p>',\n+        `${label ? `<p style=\"margin-bottom:8px;\"><strong>${escapeHtml(label)}</strong></p>` : ''}<p>`\n+      );\n+      if (dupes.length){\n+        patternHtml = patternHtml.replace(\n+          '</div>\\n      </div>',\n+          `</div>\\n        <div class=\"asm-delta\" style=\"margin-top:8px;\">duplicates: ${dupes.map(d => escapeHtml(d)).join(', ')}</div>\\n      </div>`\n+        );\n+      }\n+    }\n+\n+    const findings = a.findings || [];\n+    const order = { blocker: 0, concern: 1, note: 2 };\n+    const sorted = findings.slice().sort((x, y) => (order[x.severity] ?? 3) - (order[y.severity] ?? 3));\n+    const findingCard = (f) => `\n+      <div class=\"asm-finding ${escapeHtml(f.severity || 'note')}\">\n+        <div class=\"asm-sev\">${escapeHtml(f.severity || 'note')}${f.district ? ` · ${escapeHtml(f.district)}` : ''}</div>\n+        <div class=\"asm-claim\">${escapeHtml(f.claim || '')}</div>\n+        ${f.evidence ? `<div class=\"asm-ev\">${escapeHtml(f.evidence)}</div>` : ''}\n+        ${f.suggestion ? `<div class=\"asm-fix\">${escapeHtml(f.suggestion)}</div>` : ''}\n+      </div>`;\n+    let findingsHtml = '';\n+    if (sorted.length){\n+      // Same +N more disclosure the ADR sheet uses for rejected alternatives.\n+      const visible = sorted.slice(0, 3);\n+      const hidden = sorted.slice(3);\n+      findingsHtml = `\n+        <div>\n+          <div class=\"alt-section-label\">Findings</div>\n+          <div class=\"asm-stack\">${visible.map(findingCard).join('')}</div>\n+          ${hidden.length ? `\n+            <button class=\"link-btn\" id=\"asm-findings-more-btn\" style=\"margin-top:8px;\">+${hidden.length} more</button>\n+            <div class=\"asm-stack\" id=\"asm-findings-hidden\" hidden style=\"margin-top:8px;\">${hidden.map(findingCard).join('')}</div>\n+          ` : ''}\n+        </div>`;\n+    }\n+\n+    const checks = a.boundary_checks || [];\n+    const checksHtml = checks.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">Boundary checks</div>\n+        <div class=\"asm-stack\">${checks.map(c => `\n+          <div class=\"asm-check ${escapeHtml(c.result || '')}\">\n+            <span class=\"asm-res\">${escapeHtml(c.result || '?')}</span>\n+            <span>${escapeHtml(c.rule || '')}${c.source ? ` <span class=\"adr-sheet-sourceline\">(${escapeHtml(c.source)})</span>` : ''}</span>\n+          </div>`).join('')}</div>\n+      </div>` : '';\n+\n+    const d = a.delta || {};\n+    const deltaLines = []\n+      .concat((d.districts_added || []).map(x => `+ district ${x}`))\n+      .concat((d.districts_changed || []).map(x => `~ ${x.id}: ${x.files_before} → ${x.files_after} files`))\n+      .concat((d.edges_added || []).map(x => `+ edge ${x}`))\n+      .concat((d.edges_removed || []).map(x => `- edge ${x}`));\n+    const deltaHtml = deltaLines.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">District delta</div>\n+        <div class=\"asm-delta\">${deltaLines.map(l => escapeHtml(l)).join('<br>')}</div>\n+      </div>` : '';\n+\n+    const drift = a.drift || [];\n+    const driftHtml = drift.length ? `\n+      <div>\n+        <div class=\"alt-section-label\">Drift from the stated intent</div>\n+        <div class=\"asm-stack\">${drift.map(x => `\n+          <div class=\"asm-finding concern\">\n+            <div class=\"asm-sev\">${escapeHtml(DRIFT_LABEL[x.kind] || x.kind || '')}</div>\n+            <div class=\"asm-claim\">${escapeHtml(x.claim || '')}</div>\n+            ${x.evidence ? `<div class=\"asm-ev\">${escapeHtml(x.evidence)}</div>` : ''}\n+          </div>`).join('')}</div>\n+      </div>` : '';\n+\n+    const regretHtml = a.regret_risk ? `\n+      <div class=\"adr-block\">\n+        <div class=\"ab-kicker\" style=\"color:oklch(0.5 0.15 25);\">Will we regret this?</div>\n+        <p>${escapeHtml(a.regret_risk)}</p>\n+      </div>` : '';\n+\n+    body.innerHTML = `\n+      ${summaryHtml}\n+      ${intentHtml}\n+      ${asmQuestionHtml('Q1 — is this sensible?', a.sensible)}\n+      ${asmQuestionHtml('Q2 — maintainability and readability', a.maintainability, 'Constraint introduced', 'constraint_introduced')}\n+      ${patternHtml}\n+      ${regretHtml}\n+      ${findingsHtml}\n+      ${checksHtml}\n+      ${deltaHtml}\n+      ${driftHtml}\n+    `;\n+\n+    const moreBtn = document.getElementById('asm-findings-more-btn');\n+    if (moreBtn){\n+      moreBtn.addEventListener('click', () => {\n+        document.getElementById('asm-findings-hidden').hidden = false;\n+        moreBtn.hidden = true;\n+      });\n+    }\n+  }\n+\n   document.getElementById('tb-prev').addEventListener('click', () => selectPR(state.prIndex - 1));\n   document.getElementById('tb-next').addEventListener('click', () => selectPR(state.prIndex + 1));\n \n@@ -2371,6 +2654,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   document.getElementById('btn-comments-toggle').addEventListener('click', openCommentsDrawer);\n   document.getElementById('drawer-close-btn').addEventListener('click', closeCommentsDrawer);\n   document.getElementById('adr-sheet-close-btn').addEventListener('click', closeAdrSheet);\n+  document.getElementById('assessment-sheet-close-btn').addEventListener('click', closeAssessmentSheet);\n   document.getElementById('drawer-scrim').addEventListener('click', closeAnyOpenSheet);\n   document.getElementById('btn-add-comment').addEventListener('click', addCommentOnly);\n   commentInput.addEventListener('keydown', (e) => { if (e.key === 'Enter'){ e.preventDefault(); addCommentOnly(); } });\n@@ -2393,7 +2677,7 @@ window.DIAGRAMS = window.DIAGRAMS || {};\n   window.addEventListener('keydown', (e) => {\n     // Escape must close whichever sheet is open, even while the comment input is\n     // focused — checked before the input/textarea early-return below so it always works.\n-    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n+    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n \n     const tag = document.activeElement && document.activeElement.tagName;\n     if (tag === 'TEXTAREA' || tag === 'INPUT') return;"
};
