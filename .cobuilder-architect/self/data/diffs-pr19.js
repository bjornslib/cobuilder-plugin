window.DIFFS_BY_PR = window.DIFFS_BY_PR || {};
window.DIFFS_BY_PR[19] = {
  ".cobuilder-architect/self/bundle.json": "diff --git a/.cobuilder-architect/self/bundle.json b/.cobuilder-architect/self/bundle.json\nindex a23bf1c..ac198ed 100644\n--- a/.cobuilder-architect/self/bundle.json\n+++ b/.cobuilder-architect/self/bundle.json\n@@ -6,7 +6,8 @@\n     \"cobuilder-architect\": \"0.4.0\",\n     \"cobuilder-pr\": \"0.5.0\",\n     \"cobuilder-artifact\": \"0.4.0\",\n-    \"unknown-plugin\": \"0.0.0\"\n+    \"unknown-plugin\": \"0.0.0\",\n+    \"pr\": \"0.5.0\"\n   },\n-  \"migrated_at\": \"2026-08-27\"\n+  \"migrated_at\": \"2026-09-01\"\n }",
  ".cobuilder-architect/self/data/adrs.js": "diff --git a/.cobuilder-architect/self/data/adrs.js b/.cobuilder-architect/self/data/adrs.js\nindex 9ed6903..6a4db01 100644\n--- a/.cobuilder-architect/self/data/adrs.js\n+++ b/.cobuilder-architect/self/data/adrs.js\n@@ -1 +1 @@\n-window.ADRS = {\"ADR-0001\": {\"id\": \"ADR-0001\", \"title\": \"Flatten the bundle viewer into one self-contained file for Claude Artifact publishing\", \"state\": \"approved\", \"source_pr\": 2, \"problem\": \"The bundle viewer (viewer/index.html) depends on sibling <script src> data loading, relative asset/audio paths, and two external CDN requests — none of which survive being published as a Claude Artifact, which is a single file under a CSP that blocks every external request.\", \"decision\": \"export_artifact.py inlines story/manifest/diff/ADR data as literal window globals, rewrites the two relative-asset lookups and the narration-audio src to pull from embedded data-URI maps, and drops both CDN tags — recompressing scene art from PNG to JPEG (1400px/q78, retried at tighter tiers if needed) to fit the 16 MiB cap, since audio's ~33% base64 inflation makes it the tighter constraint of the two.\", \"alternatives\": [{\"option\": \"Publish the raw .odyssey bundle directory as-is\", \"rejected_because\": \"Artifacts are one file with no sibling files and no external requests — the viewer's <script src=\\\"../data/story.js\\\"> tags and relative asset paths simply don't resolve outside a real bundle directory.\"}, {\"option\": \"Keep images as lossless PNG\", \"rejected_because\": \"Source PNGs run ~5MB each at the 2K generation size — three of them alone (~15MB, ~20MB after base64) already exceed the 16 MiB cap before audio is even considered.\"}], \"forces\": [\"Claude Artifacts enforce a strict CSP: no external stylesheets/scripts/fetch, one self-contained file, 16 MiB rendered size cap.\", \"The Motion animation library already degrades gracefully when window.Motion is undefined, so dropping its CDN tag costs only micro-animations, not correctness.\", \"Diffs of HTML files can contain a literal </script> substring, which silently truncates an inlined <script> block unless escaped.\"], \"delivers\": {\"capability\": \"One command turns any generated PR narrative into a shareable, self-contained web page — no server, no bundle directory, no sibling files required.\", \"benefit\": \"PR stories can be shared with anyone with the link, not only teammates with the repo checked out and a local viewer server running.\", \"beneficiary\": [\"developer\", \"operator\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #2. The plugin already had a bundled viewer\\nfor local use (`python3 -m http.server`), but no way to share a single PR's\\nstory outside that setup. Claude Artifacts looked like the natural target,\\nbut the viewer was never built to be self-contained.\\n\\n## Options considered\\n\\n1. **Publish the raw `.odyssey` bundle directory as-is.** Rejected —\\n   Artifacts are one file with no sibling files and no external requests;\\n   the viewer's relative paths and CDN tags don't resolve in that sandbox.\\n2. **Keep images as lossless PNG (chosen: recompress instead).** Rejected —\\n   source PNGs run ~5MB each; three of them alone would blow the budget\\n   before audio is even considered.\\n3. **Inline everything, recompress images, drop CDN deps (chosen).** Verified\\n   by actually publishing a flattened export this session — it rendered\\n   correctly, full nav/audio/ADR sheet all worked.\\n\\n## Decision\\n\\n`export_artifact.py` inlines `window.STORY`/`ODYSSEY`/`DIFFS_BY_PR`/`ADRS`\\nplus `ODYSSEY_ASSETS`/`ODYSSEY_AUDIO` data-URI maps, rewrites the viewer's\\nthree relative-path touch points to read from those maps, and drops the\\nGoogle Fonts + Motion CDN tags.\\n\\n## Consequences\\n\\n- **Positive:** any generated PR can be published as a shareable Artifact\\n  with one command.\\n- **Constraint introduced:** the export is scoped to one PR at a time —\\n  multiple PRs' images+audio together would risk the 16 MiB cap.\\n- **Negative / accepted:** scene art loses some fidelity (PNG → JPEG,\\n  downscaled) in the published copy; the local viewer still shows full\\n  quality.\\n\\n## Value delivered\\n\\n- **New capability:** turn a generated bundle into a link, not just a\\n  locally-served page.\\n- **Benefit:** removes the repo-checkout requirement from sharing a story.\\n- **Beneficiary:** developer, operator\\n\\n## Maps to\\n\\nDistrict `scripts` from `.odyssey/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"scripts\", \"unanchored\": true, \"modules\": [\"scripts\"], \"rule\": \"Artifact export inlines bundle data and drops CDN tags so a PR story is one self-contained file.\"}, \"approved_by\": \"merge of PR #2\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-07-24\", \"by\": \"merge of PR #2\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0002\": {\"id\": \"ADR-0002\", \"title\": \"Track publish provenance with a commit SHA + content hash, not by re-threading git history\", \"state\": \"approved\", \"source_pr\": 2, \"problem\": \"Once a PR artifact is published, re-running /prodyssey:publish needs to know whether to mint a new Artifact call or leave the existing one alone — but story.json never persisted a stable per-PR reference to compare against, and open PRs additionally move underneath an already-published artifact as new commits land.\", \"decision\": \"extract_story.py now writes entry[\\\"commit\\\"] (a value it already computed internally for size/touched and previously discarded); export_artifact.py combines it with a sha256 content hash of the PR's timeline entry, referenced ADRs, and diff into publish-manifest.json, and republishes only when either signal changed, passing the previously recorded artifact_url back to the Artifact tool so a republish updates the same link instead of minting a new one.\", \"alternatives\": [{\"option\": \"Always republish on every /prodyssey:publish invocation\", \"rejected_because\": \"Wastes an Artifact call (and mints a confusing new URL) for a PR that hasn't changed since it was last published.\"}, {\"option\": \"Track staleness by content hash alone, no commit SHA\", \"rejected_because\": \"Persisting the commit SHA turned out to be a ~4 line addition to a function extract_story.py already runs — and it catches a case content-hash alone can't distinguish as clearly: new commits landing on an open PR's branch.\"}], \"forces\": [\"extract_story.py already resolves and discards a merge-commit or branch-head SHA per PR while computing size/touched — persisting it cost nothing new to compute.\", \"Open-PR entries are explicitly not immutable (story-mode.md, generate mode notes) — narration and diff both refresh as new commits land, so publish-time staleness has to track the same thing.\", \"The Artifact tool mints a new URL unless the same file path is republished in-session or an existing url is passed explicitly — losing track of a PR's artifact_url means every republish orphans the last one.\"], \"delivers\": {\"capability\": \"Re-running /prodyssey:publish is safe and idempotent — unchanged PRs report \\\"already up to date\\\" instead of spamming new Artifact links, and the index artifact always reflects the full, current set.\", \"benefit\": \"Publishing becomes a pipeline you can re-run freely rather than a one-shot action you have to remember not to repeat.\", \"beneficiary\": [\"developer\", \"operator\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #2. Once the artifact-export mechanism\\n(ADR-0001) existed, the next question was what happens on a second\\n`/prodyssey:publish` run — especially for an open PR, whose diff and\\nnarrative are explicitly not settled history.\\n\\n## Options considered\\n\\n1. **Always republish.** Rejected — wasteful, and risks orphaning the\\n   previous Artifact URL if it isn't reused.\\n2. **Content hash only.** Rejected — works, but investigation showed the\\n   commit SHA was already computed and thrown away one function over, so\\n   adding it was nearly free and catches branch-moved-but-text-unchanged\\n   cases more directly.\\n3. **Commit SHA + content hash together (chosen).**\\n\\n## Decision\\n\\n`extract_story.py` persists `entry[\\\"commit\\\"]`. `export_artifact.py` computes\\na sha256 `source_hash` over the PR's timeline entry + referenced ADRs + diff,\\nand records both plus the export's file/byte metadata into\\n`exports/publish-manifest.json`. `record_publish.py` writes the Artifact\\ntool's returned URL back into the same file once Claude has it.\\n\\n## Consequences\\n\\n- **Positive:** idempotent republishing; the index artifact can always\\n  rebuild from a single source of truth instead of guessing what's changed.\\n- **Constraint introduced:** `exports/publish-manifest.json` is now load-\\n  bearing state, not disposable build output — it's tracked in git\\n  alongside `data/`/`assets/`, not gitignored.\\n- **Negative / accepted:** a PR generated before this change has no\\n  `commit` field until it's regenerated — handled as \\\"unknown,\\\" not an\\n  error.\\n\\n## Value delivered\\n\\n- **New capability:** safe, repeatable publishing with no manual bookkeeping\\n  of what's already live.\\n- **Benefit:** removes the operational burden of tracking Artifact URLs by\\n  hand across sessions.\\n- **Beneficiary:** developer, operator\\n\\n## Maps to\\n\\nDistrict `scripts` from `.odyssey/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"scripts\", \"unanchored\": true, \"modules\": [\"scripts\"], \"rule\": \"Republish a PR artifact only when its commit SHA or content hash has changed.\"}, \"approved_by\": \"merge of PR #2\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-07-24\", \"by\": \"merge of PR #2\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0003\": {\"id\": \"ADR-0003\", \"title\": \"Unify bundle storage under .prodyssey/, self-analysis into .prodyssey/self/\", \"state\": \"approved\", \"source_pr\": 3, \"problem\": \"Two separate storage roots existed for the same kind of output: self-analysis bundles lived at `<target>/.odyssey/`, while foreign-repo bundles were cached centrally at `<hub>/.prodyssey/<repo-slug>/`. Every script's `--bundle-dir` default, every skill reference, and every command had to carry two path conventions, and there was no single root to write one `.gitignore` line against for hub-local bookkeeping (`.view-server.pid`, `.view-server.log`, the `active` symlink).\", \"decision\": \"Collapse both roots into one: self-analysis bundles now default to `<target>/.prodyssey/self/`, a sibling of the existing `<hub>/.prodyssey/<repo-slug>/` cache directories, with `self` reserved as a slug no repo-derived hash can ever collide with. Every script's `--bundle-dir` default, the skill's Hub-resolution rule, and the `.gitignore` guidance were rewritten around this single root — and a legacy-layout detector stops any mode with the exact `git mv .odyssey .prodyssey/self` command instead of silently treating the old path as \\\"no baseline found\\\".\", \"alternatives\": [{\"option\": \"Keep two storage roots, just document the split more clearly\", \"rejected_because\": \"Leaves the actual maintenance cost in place — ~10 places across scripts, SKILL.md, README, and CLAUDE.md still hardcode two different default paths — and still gives no single root for one shared .gitignore rule.\"}, {\"option\": \"Auto-migrate a legacy `.odyssey/` bundle on next run (run `git mv` for the user)\", \"rejected_because\": \"Contradicts the skill's standing posture of never editing the target repo's source outside the bundle directory itself; a legacy layout gets a printed command for the user to run, not an automatic move on their behalf.\"}], \"forces\": [\"Every one of the plugin's scripts (plus SKILL.md, README.md, CLAUDE.md) hardcoded `.odyssey` as the self-analysis default, duplicating the same path assumption in roughly ten places.\", \"A legacy `.odyssey/` bundle must never be silently reinterpreted as \\\"no baseline exists\\\" and regenerated — that would burn real Gemini API cost re-deriving content a maintainer already authored by hand.\", \"The `.prodyssey/` bookkeeping entries (`.view-server.pid`, `.view-server.log`, the `active` symlink) need a `.gitignore` line that ignores only those three files, never `.prodyssey/` as a whole, since real bundle directories sit right next to them and are meant to be committed.\"], \"delivers\": {\"capability\": \"One storage root for every bundle, self-analysis or foreign-repo, with one `.gitignore` rule and one path convention across the whole plugin instead of two.\", \"benefit\": \"Removes a whole class of \\\"which root is this bundle actually in\\\" bugs and doc-drift before they can occur, and gives centrally-cached and self-analysis bundles the same on-disk shape.\", \"beneficiary\": [\"developer\", \"the plugin itself (maintainers and future sessions working in this repo)\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #3. The plugin had grown two separate bundle-storage roots as the central-cache feature (`<hub>/.prodyssey/<repo-slug>/`) was added alongside the original self-analysis location (`<target>/.odyssey/`), and the split had started costing real maintenance overhead across scripts, skill references, and commands.\\n\\n## Options considered\\n\\n1. **Document the split better, keep two roots.** Rejected — doesn't remove the duplicated path-default maintenance burden or unlock a single `.gitignore` line.\\n2. **Auto-migrate legacy bundles via `git mv` on next run.** Rejected — an automatic move inside the user's repo contradicts the skill's \\\"never edits target repo source\\\" rule; detect-and-instruct instead.\\n3. **Unify under `<target>/.prodyssey/self/` (chosen).**\\n\\n## Decision\\n\\nEvery script's `--bundle-dir` default becomes `<repo>/.prodyssey/self`. The skill's Hub-resolution rule is rewritten around one root with `self` as a reserved slug. A legacy-layout check runs before any mode's other work: if the new path is missing but `<target>/.odyssey/` exists, the mode stops and prints the exact `git mv` command rather than proceeding.\\n\\n## Consequences\\n\\n- **Positive:** one path convention everywhere; central and self-analysis bundles are now structurally identical, just at different subpaths of the same root.\\n- **Constraint introduced:** `self` can never be used as a foreign-repo slug — the slug derivation must guarantee this.\\n- **Negative / accepted:** any bundle generated before this PR needs a one-time manual `git mv`, not handled automatically.\\n\\n## Value delivered\\n\\n- **New capability:** a single, consistent bundle-storage root for the whole plugin.\\n- **Benefit:** removes duplicated path-convention maintenance and enables one shared `.gitignore` rule.\\n- **Beneficiary:** developer, the plugin itself\\n\\n## Maps to\\n\\nDistrict `.prodyssey` from `.prodyssey/self/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \".prodyssey\", \"unanchored\": true, \"modules\": [\".prodyssey\"], \"rule\": \"Self-analysis and foreign-repo bundles share one root, with self reserved as a slug.\"}, \"approved_by\": \"merge of PR #3\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-07-28\", \"by\": \"merge of PR #3\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0004\": {\"id\": \"ADR-0004\", \"title\": \"Keep the ste-writing skill dev-local, out of the plugin's install surface\", \"state\": \"approved\", \"source_pr\": 3, \"problem\": \"The repo needed a controlled-language (ASD-STE100) writing skill and linter to keep its own docs and PR narratives in a consistent register, but the plugin's `skills/` directory is exactly what ships to anyone who runs `/plugin install prodyssey@prodyssey` — adding a general-purpose writing-style skill there would silently grow every installer's surface with a skill that has nothing to do with generating codebase odysseys.\", \"decision\": \"`ste-writing` lives under `.claude/skills/ste-writing/`, a location outside the plugin manifest's `skills/` tree entirely, so an install of `prodyssey@prodyssey` still gets exactly the two skills the plugin has always shipped (odyssey, and mermaid one PR later) and never a third.\", \"alternatives\": [{\"option\": \"Ship it under skills/ste-writing/ alongside odyssey\", \"rejected_because\": \"Every installer of the plugin would receive a general writing-style skill unrelated to codebase odysseys, contradicting the plugin's already-explicit \\\"no agents, no hooks, no MCP servers\\\" minimal-install-surface stance.\"}], \"forces\": [\"The plugin's minimal-surface intent is an explicit, pre-existing design constraint (no agents/hooks/MCP servers, deliberately so it never touches another session's permission surface), and `skills/` is auto-discovered by the manifest with no allowlist to exclude an individual skill.\", \"The repo's own writing standard (ASD-STE100 for README, CLAUDE.md, reference docs, commit/PR bodies) still needs an available skill and linter for sessions working inside this repo, independent of what the plugin ships downstream.\"], \"delivers\": {\"capability\": \"This repo can enforce a documentation writing standard on itself without changing what any plugin installer receives.\", \"benefit\": \"Keeps two separate concerns — how prodyssey ships, versus how this repo writes its own docs — from leaking into each other.\", \"beneficiary\": [\"developer\", \"plugin installers (install surface stays unaffected)\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #3. The repo adopted an ASD-STE100 writing standard for its own prose (README, CLAUDE.md, reference docs, commit/PR bodies) and needed a skill plus linter to enforce it, at the same time the plugin's `skills/` directory already had an established minimal-surface rule.\\n\\n## Options considered\\n\\n1. **Ship under `skills/ste-writing/`.** Rejected — grows every installer's surface with an unrelated skill.\\n2. **Place under `.claude/skills/ste-writing/`, dev-local only (chosen).**\\n\\n## Decision\\n\\n`ste-writing` and its linter (`.claude/skills/ste-writing/ste-lint.py`) live outside the plugin manifest's discovered tree. CLAUDE.md documents explicitly that an install of `prodyssey@prodyssey` gets exactly the skills under `skills/` in this repo, never `.claude/skills/`.\\n\\n## Consequences\\n\\n- **Positive:** plugin install surface stays exactly as minimal as before this PR.\\n- **Constraint introduced:** any future repo-local-only tooling should default to `.claude/skills/`, not `skills/`, unless it's meant to ship.\\n- **Negative / accepted:** ste-writing isn't available to other repos that install the prodyssey plugin — it's specific to this repo's own documentation practice.\\n\\n## Value delivered\\n\\n- **New capability:** a self-enforced writing standard for this repo's own docs.\\n- **Benefit:** keeps shipping surface and internal tooling decisions independent.\\n- **Beneficiary:** developer, plugin installers\\n\\n## Maps to\\n\\nDistrict `skills` from `.prodyssey/self/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\".claude/skills/ste-writing\"], \"rule\": \"Repo-local writing tools live under .claude/skills/, never under the plugin skills/ tree.\"}, \"approved_by\": \"merge of PR #3\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-07-28\", \"by\": \"merge of PR #3\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0005\": {\"id\": \"ADR-0005\", \"title\": \"Diagram authoring delegated to a per-PR subagent, never the orchestrating Claude or a script\", \"state\": \"approved\", \"source_pr\": 4, \"problem\": \"Adding Mermaid diagrams as a visual family alongside Gemini scene art meant deciding who writes the actual `.mmd` diagram sources. The plugin already held a hard rule that scripts never author judgment-shaped content (narrative, ADRs) — only move data — and diagram content (what to show, how to group it, what level of detail) is exactly that kind of judgment call. But authoring three diagrams per PR directly in the orchestrating Claude's own context has a discoverability cost too: the Mermaid authoring rules live in a reference set of over fifty files, one per diagram type, that only makes sense loaded on demand, not kept resident in the orchestrator's context for every PR whether or not it needs diagrams.\", \"decision\": \"Generate mode spawns one subagent per PR for diagram authoring. Its prompt requires it to invoke `Skill(\\\"prodyssey:mermaid\\\")` first (falling back to reading `skills/mermaid/SKILL.md` directly if the skill doesn't resolve by name in that session), hands it the PR's timeline entry, extracted diff, and inventory as grounding, and requires it to write exactly three files — `pr{N}-level1.mmd` (`C4Container`), `pr{N}-level2.mmd` (`sequenceDiagram`), `pr{N}-level3.mmd` (`classDiagram`) — returning only their paths. `build_diagrams.py` then compiles those files into `data/diagrams.js` and validates each one's required diagram type and bracket balance; a validation failure is sent back to the same subagent to fix, never hand-patched by the orchestrator.\", \"alternatives\": [{\"option\": \"Have the orchestrating Claude author the .mmd files itself, inline\", \"rejected_because\": \"Keeps the entire Mermaid reference set (block, C4, sequence, class, and dozens more diagram types) loaded against every PR's context regardless of whether that PR needs diagrams at all, and duplicates authoring-rule maintenance between the odyssey skill and the diagram content.\"}, {\"option\": \"Have build_diagrams.py generate diagrams programmatically from the diff\", \"rejected_because\": \"Breaks the plugin's standing invariant that scripts only move data — deciding what a diagram should show is the same kind of judgment call already reserved for Claude in narrative and ADR authoring, not something a deterministic script can produce.\"}], \"forces\": [\"The plugin's existing convention, already true for narrative and ADRs: a mechanical script only compiles and validates content a subagent already wrote — it never authors that content itself.\", \"A new `--art both|diagram|image` flag controls which visual family a sweep produces per PR; `--art diagram` skips Gemini entirely, and since a diagram is plain text rather than base64 image data, it meaningfully relieves the 16 MiB Artifact-publish budget from ADR-0001.\", \"verify_bundle.py must report `diagram.level-*` status regardless of which family actually gated a given sweep, so a later `--art both` re-run or a publish-mode budget check can see the true state of both families, not just whichever one last ran.\"], \"delivers\": {\"capability\": \"PRs can get architecture diagrams at zero Gemini cost, authored with full access to a large, specialized Mermaid ruleset, without that ruleset bloating every other PR's authoring context.\", \"benefit\": \"Diagrams and scene art become independently selectable per sweep, and diagram-only PRs get meaningfully easier to fit under the artifact-publish size cap.\", \"beneficiary\": [\"developer\", \"operator (whoever runs /prodyssey:publish under the 16 MiB cap)\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #4. Mermaid diagrams were added as a second visual family for levels 1-3, alongside the existing Gemini scene art from earlier PRs, and needed an authoring path consistent with the plugin's judgment-vs-mechanical split already established for narrative and ADRs.\\n\\n## Options considered\\n\\n1. **Orchestrator authors .mmd inline.** Rejected — forces the full Mermaid reference set into every PR's context and duplicates authoring-rule ownership.\\n2. **Script-generates diagrams from the diff.** Rejected — violates the scripts-never-author-judgment-content rule.\\n3. **Per-PR subagent invoking the mermaid skill (chosen).**\\n\\n## Decision\\n\\nOne subagent per PR, told to invoke `Skill(\\\"prodyssey:mermaid\\\")` and write exactly three typed `.mmd` files; `build_diagrams.py` compiles and validates them mechanically, sending failures back to the same subagent rather than patching them directly.\\n\\n## Consequences\\n\\n- **Positive:** diagram authoring inherits the same judgment/mechanical split already proven for narrative and ADRs; Gemini cost and the 16 MiB artifact budget both become easier to manage for diagram-only sweeps.\\n- **Constraint introduced:** the orchestrating Claude must never write or hand-patch a `.mmd` file directly — all fixes route back through the subagent.\\n- **Negative / accepted:** one extra subagent spawn per PR when diagrams are requested, versus authoring inline.\\n\\n## Value delivered\\n\\n- **New capability:** independent, zero-Gemini-cost architecture diagrams per PR.\\n- **Benefit:** smaller authoring context per sweep and an easier artifact-publish budget.\\n- **Beneficiary:** developer, operator\\n\\n## Maps to\\n\\nDistrict `skills` from `.prodyssey/self/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\"skills/odyssey\"], \"rule\": \"The orchestrating Claude never writes or hand-patches a .mmd file. A per-PR subagent authors them.\"}, \"approved_by\": \"merge of PR #4\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-08-02\", \"by\": \"merge of PR #4\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0006\": {\"id\": \"ADR-0006\", \"title\": \"Three-phase, guard-checked bundle self-migration instead of one version-gated ladder\", \"state\": \"approved\", \"source_pr\": 4, \"problem\": \"A bundle drifts from what the plugin currently produces in three genuinely different ways — a derived file going stale (`viewer/index.html`, nothing authored to preserve), the directory layout changing (new files or directories), and `story.json`'s data shape changing — and a single migration mechanism gated on one version number can't handle all three at once without either under-firing (an unconditional-refresh case wrongly skipped because a version check said \\\"already current\\\") or over-firing (an in-place JSON transform touching hand-authored narrative and paid TTS/art content it has no business touching).\", \"decision\": \"`migrate_bundle.py` runs three independent phases in order, against every mode's bundle-dir, before anything else reads it: an unconditional viewer refresh with no version check at all, then an ordered `LAYOUT_MIGRATIONS` ladder keyed on `bundle.json`'s `bundle_format` integer, then an ordered `SCHEMA_MIGRATIONS` ladder keyed on `story.json`'s own `meta.schema_version` — never `bundle.json`'s mirror of it, which could run ahead and deadlock the ladder. Each schema migration declares a `touches` set of dotted field paths; the whole ladder runs once in memory over a deepcopy, and any authored field outside `touches` that changed anywhere in the run stops the write entirely — nothing reaches disk, so there is no restore step, only \\\"no write happened.\\\"\", \"alternatives\": [{\"option\": \"Gate the viewer refresh on a version check, same as the other two phases\", \"rejected_because\": \"This is the exact bug that motivated the whole mechanism — a bundle's viewer had gone stale and silently dropped diagram support because nothing forced a refresh after the viewer changed without an accompanying version bump.\"}, {\"option\": \"Skip the authored-field guard; trust each migration step to only touch what it claims\", \"rejected_because\": \"story.json holds authored, irreplaceable narrative text and paid Gemini TTS/art content in the same document as derived fields — a migration is only safe to run unattended if it structurally cannot touch the authored half by accident, which requires checking every field, not trusting the step's own claim.\"}], \"forces\": [\"`viewer/index.html` is a pure build artifact with nothing authored to preserve, while `story.json` mixes derived fields with hand-authored narrative and paid Gemini TTS/art content in the same file — one migration strategy can't treat both the same way.\", \"`bundle.json`'s schema_version is only a mirror; `migrate_bundle.py` must read `story.json`'s own `meta.schema_version` as the source of truth, or a mirror that ran ahead of the real data would make every ladder step report \\\"already current\\\" while the underlying shape stayed old, with no command able to repair it.\", \"All four modes — Baseline, Generate, View, Publish — need this to run first and unconditionally, so a stale bundle self-heals before any other step reads it; it cannot be an opt-in flag a session might forget to pass.\"], \"delivers\": {\"capability\": \"A bundle generated by an older plugin version upgrades itself in place, automatically, the moment any command touches it — no manual migration step, and no risk of silently losing hand-authored narrative or paid audio/art content in the process.\", \"benefit\": \"Removes an entire class of \\\"why does my bundle look wrong\\\" support burden as the plugin's bundle shape keeps evolving.\", \"beneficiary\": [\"developer\", \"operator (anyone running an older bundle against a newer plugin checkout)\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #4. As the bundle format gained diagrams and other changes, older committed bundles (including this repo's own `.prodyssey/self/` and its test-fixture bundles) needed a way to self-heal to the current shape, motivated directly by a real incident: a bundle's viewer had gone stale and silently lost diagram support.\\n\\n## Options considered\\n\\n1. **One version-gated ladder covering everything, including the viewer.** Rejected — reproduces the exact incident that motivated this work.\\n2. **In-place JSON transforms with no authored-field guard.** Rejected — unsafe next to hand-authored narrative and paid TTS/art content living in the same file.\\n3. **Three independent phases, one unconditional, two version-gated, with a guard on the data ladder (chosen).**\\n\\n## Decision\\n\\n`migrate_bundle.py` always refreshes the viewer, then steps the layout ladder from `bundle_format`, then steps the schema ladder from `story.json`'s own `meta.schema_version`, comparing every authored field before and after each schema step and refusing to write if anything outside the declared `touches` set changed.\\n\\n## Consequences\\n\\n- **Positive:** every mode self-heals a stale bundle before doing anything else; hand-authored content is structurally protected from an in-place migration bug.\\n- **Constraint introduced:** adding a new schema migration means declaring an accurate `touches` set — an incomplete one causes the guard to reject a legitimate migration, not silently under-check it.\\n- **Negative / accepted:** a failed guard means \\\"no write happened,\\\" not an automatic fix — the maintainer has to widen `touches` or fix the migration function by hand.\\n\\n## Value delivered\\n\\n- **New capability:** automatic, safe bundle self-migration across all four modes.\\n- **Benefit:** removes drift-related support burden without risking paid content.\\n- **Beneficiary:** developer, operator\\n\\n## Maps to\\n\\nDistrict `scripts` from `.prodyssey/self/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"scripts\", \"unanchored\": true, \"modules\": [\"scripts\"], \"rule\": \"migrate_bundle.py always refreshes the viewer, then steps the layout and schema ladders with an authored-field guard.\"}, \"approved_by\": \"merge of PR #4\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-08-02\", \"by\": \"merge of PR #4\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0007\": {\"id\": \"ADR-0007\", \"title\": \"Anchor the GEMINI_API_KEY / .env lookup to the hub's cwd, never to a --repo target\", \"state\": \"approved\", \"source_pr\": 5, \"problem\": \"generate_prompts.py and generate_audio.py called load_dotenv(repo / \\\".env\\\"), where repo is whatever path --repo passes. load_dotenv merges every assignment in that file into os.environ (subject only to override=False protecting a variable already set), so a foreign, untrusted --repo target's .env could set HTTPS_PROXY, HTTP_PROXY, REQUESTS_CA_BUNDLE, SSL_CERT_FILE, or any GOOGLE_*/GEMINI_* variable the google-genai SDK reads, and capture the session's own GEMINI_API_KEY on the next request.\", \"decision\": \"Both scripts call load_dotenv(find_dotenv(usecwd=True)) with no path argument, which searches upward from the process's current working directory instead of the target repo or the script's own directory. Since SKILL.md always runs these scripts from inside the hub, this resolves the hub's own .env and never touches the target's.\", \"alternatives\": [{\"option\": \"load_dotenv(repo / \\\".env\\\") — the original code\", \"rejected_because\": \"Treats the analyzed repo as a trusted source of environment configuration, when --repo is explicitly documented as pointing at foreign, untrusted checkouts.\"}, {\"option\": \"load_dotenv() with no arguments (python-dotenv's plain default)\", \"rejected_because\": \"find_dotenv()'s default is usecwd=False, which walks up from the calling frame's own file — the scripts' directory under scripts/, not the working directory. That happens to work only in this dev checkout because the scripts live inside the hub; for an installed plugin, CLAUDE_PLUGIN_ROOT sits under ~/.claude/plugins/cache/, nowhere near the user's repo, so the key would never be found even when the user placed it exactly where README.md and SKILL.md say to.\"}], \"forces\": [\"--repo is documented to accept any local checkout, including ones the user does not control or trust.\", \"SKILL.md's own Step 0 prereq gate already states the key belongs to <hub>, never <target> — the fix has to make the code match that documented boundary, not just patch the immediate leak.\", \"python-dotenv's find_dotenv() has two different default search origins (usecwd=False walks from the caller's file; usecwd=True walks from the process cwd) that look interchangeable but are not, once scripts run from an installed plugin path instead of a dev checkout.\"], \"delivers\": {\"capability\": \"A --repo target can never influence which environment variables the session's own API calls run with, regardless of what that target's .env contains.\", \"benefit\": \"The user's GEMINI_API_KEY, and any other environment variable an untrusted repo could otherwise poison (HTTPS_PROXY, SSL_CERT_FILE, etc.), stays confined to the hub the user actually controls.\", \"beneficiary\": [\"operator\", \"developer\"]}, \"body\": \"## Context\\n\\ngenerate_prompts.py and generate_audio.py both authenticate to Google's Gemini API using GEMINI_API_KEY, and both previously loaded that key's .env file from the --repo target rather than the hub. --repo is documented (SKILL.md's Target resolution section) to accept any local checkout, which by construction includes repos the user does not control.\\n\\n## Options considered\\n\\n1. Original: load_dotenv(repo / \\\".env\\\") against the target — rejected, trusts an untrusted repo's file to set process environment variables.\\n2. load_dotenv() with no path — rejected, python-dotenv's usecwd=False default resolves relative to the scripts' own directory, which only coincides with the hub in a dev checkout, not an installed plugin.\\n3. Chosen: load_dotenv(find_dotenv(usecwd=True)) — resolves from the process working directory, which SKILL.md's procedure always leaves at the hub before invoking any script.\\n\\n## Decision\\n\\nAnchor .env resolution to cwd via find_dotenv(usecwd=True) at all three call sites, and never construct a path into --repo target for this purpose. SKILL.md's Step 0 prereq gate now states the cwd constraint explicitly.\\n\\n## Consequences\\n\\nA foreign --repo target's .env is now fully inert as far as this plugin's own process environment is concerned. Verified against a hostile fixture repo whose .env set a decoy GEMINI_API_KEY plus HTTPS_PROXY and a marker variable — none of it reached os.environ regardless of --repo target.\\n\\n## Value delivered\\n\\nCloses a credential-exfiltration path that existed for any user who pointed --repo at a repo they didn't fully trust.\\n\\n## Maps to\\n\\nscripts\\n\", \"maps_to\": {\"district\": \"scripts\", \"unanchored\": true, \"modules\": [\"scripts\"], \"rule\": \"Gemini .env lookup walks from the hub cwd and never from a --repo target.\"}, \"approved_by\": \"merge of PR #5\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-08-05\", \"by\": \"merge of PR #5\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0008\": {\"id\": \"ADR-0008\", \"title\": \"Cap and lock third-party dependencies in the three Gemini-calling scripts\", \"state\": \"approved\", \"source_pr\": 5, \"problem\": \"export_artifact.py, generate_audio.py, and generate_prompts.py declare PEP 723 dependencies (pillow, google-genai, python-dotenv) with no upper version bound. Two of the three read GEMINI_API_KEY and send authenticated requests to Google, so an untested major release of any dependency could load silently on the next uv run, with the key already resident in process memory, and no lockfile pinned the resolved versions or their transitive packages.\", \"decision\": \"Add upper bounds to each dependency (pillow<13, google-genai<2, python-dotenv<2) and generate a uv lockfile beside each of the three scripts (scripts/<name>.py.lock), pinning every transitive package with a hash. The other eight scripts in the plugin use only the standard library and need neither bound nor lockfile.\", \"alternatives\": [{\"option\": \"Leave dependencies unbounded, matching the plugin's existing PEP 723 scripts\", \"rejected_because\": \"An unbounded dependency on a script that authenticates to an external API with a live secret can silently pick up a compromised or breaking major release on the very next run.\"}, {\"option\": \"Pin exact versions in the PEP 723 header instead of adding a lockfile\", \"rejected_because\": \"A version pin alone does not pin or hash transitive dependencies, so a compromised transitive package could still load; a lockfile is what makes a tampered resolution fail closed instead of executing.\"}], \"forces\": [\"generate_prompts.py and generate_audio.py send the user's GEMINI_API_KEY to Google on every call — a compromised or unexpectedly-behaving dependency has a live credential to work with.\", \"uv 0.5.14 (the version available when the version caps were first added) has no `uv lock --script` subcommand, so the caps landed one commit ahead of the lockfiles, after an upgrade to uv 0.12.1 made per-script locking possible.\", \"Locking only the three scripts that declare third-party dependencies keeps the other eight, stdlib-only scripts free of lockfile maintenance they don't need.\"], \"delivers\": {\"capability\": \"A `uv run` of any of the three key-handling scripts resolves to the same, hash-verified set of packages every time, and fails closed rather than silently executing a tampered or unexpectedly major-bumped dependency.\", \"benefit\": \"The blast radius of a supply-chain compromise in pillow, google-genai, or python-dotenv is contained to versions the maintainer has actually reviewed, on the two scripts that hold a live external credential.\", \"beneficiary\": [\"operator\", \"developer\"]}, \"body\": \"## Context\\n\\nThree of the plugin's eleven PEP 723 scripts declare third-party dependencies. Two of those three (generate_prompts.py, generate_audio.py) read GEMINI_API_KEY and call Google's API on every run; the third (export_artifact.py) depends on pillow alone, for image recompression. None had an upper version bound or a lockfile.\\n\\n## Options considered\\n\\n1. Leave dependencies unbounded — rejected, given the live credential two of the three scripts hold.\\n2. Exact-pin versions in the PEP 723 header, no lockfile — rejected, does not pin or hash transitive packages.\\n3. Chosen: upper-bound each direct dependency, and lock all three scripts once uv 0.12.1 made `uv lock --script` available.\\n\\n## Decision\\n\\npillow<13 (resolves to 12.3.0), google-genai<2 (resolves to 1.75.0), python-dotenv<2 (resolves to 1.2.2). Each of the three scripts gets a sibling .lock file pinning every transitive package with a hash. Refresh a lockfile with `uv lock --script scripts/<name>.py` after any change to that script's dependency block.\\n\\n## Consequences\\n\\nA `uv run` against a tampered or altered lockfile now fails the hash check instead of executing. The other eight, stdlib-only scripts carry no lockfile and need none.\\n\\n## Value delivered\\n\\nRemoves the single largest unreviewed-upgrade surface on the two scripts that hold a live Google API credential.\\n\\n## Maps to\\n\\nscripts\\n\", \"maps_to\": {\"district\": \"scripts\", \"unanchored\": true, \"modules\": [\"scripts\"], \"rule\": \"The three Gemini-calling scripts declare upper bounds and a hash-pinned lockfile.\"}, \"approved_by\": \"merge of PR #5\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-08-05\", \"by\": \"merge of PR #5\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0009\": {\"id\": \"ADR-0009\", \"title\": \"Submit mode ends the pre stage by opening the real PR, never a synthetic key\", \"state\": \"approved\", \"source_pr\": 6, \"problem\": \"story.json's timeline keys every entry on an integer `pr` field, and verify_bundle.py, record_publish.py, manifest.js, and the viewer all depend on that being the change's real, permanent PR number. Submit mode interviews the author and assesses the change before the PR exists, so at that point there is no such number to key on yet.\", \"decision\": \"Submit mode's pre stage stages its output — description, intent, and assessment — under exports/branch-<slug>/, keyed by branch name, and only writes the real story.json timeline entry after `gh pr create` returns an actual PR number, ending the pre stage by opening the PR for real rather than by staging a placeholder.\", \"alternatives\": [{\"option\": \"Mint a synthetic PR key for the story.json timeline entry instead of ending the pre stage by opening the real PR\", \"rejected_because\": \"It would have been hard to reliably connect what the author said in the interview back to the actual PR number once one existed. Opening the PR as the flow's last step keeps the interview answers and the real PR tied together from the start.\"}], \"forces\": [\"story.json's schema keys the timeline on an integer `pr`, with four consumers (verify_bundle.py, record_publish.py, manifest.js, the viewer) that all assume it is real and stable.\", \"The interview and assessment both have to happen before the PR exists, since that is the only point intent is still recoverable — but nothing before `gh pr create` returns has a real PR number to attach them to.\", \"Pushing a branch and opening a PR are the only actions submit mode takes outside .prodyssey/, and they only run after an explicit confirmation — so the design has to tolerate a run that stages content and stops without ever creating a PR.\"], \"delivers\": {\"capability\": \"An author's stated intent and a bundle-grounded assessment attach to a PR from the moment that PR exists, with no reconciliation step and no synthetic key ever entering story.json.\", \"benefit\": \"Every timeline consumer keeps its existing assumption that `pr` is the real, permanent number, so nothing downstream needs to learn a second, temporary key scheme.\", \"beneficiary\": [\"developer\", \"operator\"]}, \"body\": \"## Context\\n\\nSubmit mode is the one Odyssey mode that runs before the history it would narrate exists. It interviews the author and assesses the change against the bundle's decision history while the change is still an uncommitted branch, but story.json's timeline schema was designed around merged PRs that already have a permanent, real `pr` number.\\n\\n## Options considered\\n\\n1. **Mint a synthetic PR key for the timeline entry.** Rejected — a placeholder key created before the PR exists would need to be reliably reconciled with the real PR number once `gh pr create` returned one, and the author's own interview answers could drift from the PR they end up describing.\\n2. **Chosen: stage pre-PR output under exports/branch-<slug>/, and end the pre stage by actually opening the PR.** description.json, intent.json, and assessment.json live under a branch-keyed staging directory until `gh pr create` returns a number; only then does the real story.json timeline entry get written, with `intent` and `assessment` attached to it directly.\\n\\n## Decision\\n\\nSubmit mode's pre stage never writes a placeholder or synthetic key into story.json. It stages everything it can produce before the PR exists under exports/branch-<slug>/, and treats `gh pr create` succeeding as the point the flow's real timeline entry can be written. Pushing and opening the PR are gated on an explicit confirmation; `--non-interactive` cannot reach this step at all.\\n\\n## Consequences\\n\\n- **Positive:** No reconciliation step exists between a staged interview and the PR it describes — they are the same object from the moment the PR is real.\\n- **Constraint introduced:** A branch that never becomes a PR can never enter the timeline; its staged content stays under exports/branch-<slug>/ indefinitely.\\n- **Negative / accepted:** A run that stops before confirmation produces no timeline entry at all, even though the interview and assessment work already happened — that work is not wasted (render_review.py can still render it from the staging directory), but it is not yet part of the bundle's narrated history.\\n\\n## Value delivered\\n\\n- **New capability:** An author's stated intent and a structured assessment attach to a PR from the moment it exists, with no separate keying scheme to maintain.\\n- **Benefit:** verify_bundle.py, record_publish.py, manifest.js, and the viewer keep their existing assumption that `pr` is real and permanent, so none of them needed to change to support submit mode.\\n- **Beneficiary:** developer, operator.\\n\\n## Maps to\\n\\nDistrict `commands` from .prodyssey/self/inventory.yaml.\\n\", \"maps_to\": {\"district\": \"commands\", \"unanchored\": true, \"modules\": [\"commands\"], \"rule\": \"Submit mode never writes a synthetic PR key. The pre stage ends by opening the real PR.\"}, \"approved_by\": \"merge of PR #6\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-08-04\", \"by\": \"merge of PR #6\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0010\": {\"id\": \"ADR-0010\", \"title\": \"Interview self-consistency check compares blind author accounts by LLM judgment, not keyword matching\", \"state\": \"approved\", \"source_pr\": 9, \"problem\": \"Submit mode's interview had no way to catch an author's own account of a change being internally incoherent, or diverging from what the diff shows, before intent was written to disk.\", \"decision\": \"Ask the problem and approach questions blind, before showing Claude's diff-derived hypothesis, then compare all three accounts by LLM judgment; raise a surviving material mismatch to the author with a resolve-now-or-log choice.\", \"alternatives\": [{\"option\": \"Keyword/regex comparison of the two author answers\", \"rejected_because\": \"Cannot separate a register difference (same understanding at two altitudes) from a real material disagreement — interview-guide.md §3a states this explicitly as the reason LLM judgment is required instead.\"}, {\"option\": \"Silently write any mismatch into intent.unknowns without asking the author\", \"rejected_because\": \"Treats a possible misunderstanding as settled without giving the author a chance to resolve it in the moment; §3a instead offers a choice between working through it now or logging it.\"}, {\"option\": \"Add a new intent schema field or a new interview stage for this check\", \"rejected_because\": \"The check reorders two questions §3 already budgets (problem, approach) rather than growing the schema or the stage count.\"}], \"forces\": [\"Submit mode's intent block feeds ADRs and narrative downstream (story-mode.md), so an unreconciled misunderstanding written to disk propagates.\", \"The question budget in §3 caps interviews at 6-8 topics; a new stage would compete with that budget.\", \"The interviewing Claude already drafts a hypothesis from the diff before asking anything (§2); showing it early would anchor the author's answers instead of testing them independently.\"], \"delivers\": {\"capability\": \"The interview can detect, before a PR opens, when the author's own account of a change disagrees with itself or with the diff.\", \"benefit\": \"Catches an unsettled mental model or a factually incorrect claim before it is committed to intent, assessment, and later narrative/ADR text.\", \"beneficiary\": [\"developer\", \"operator\"]}, \"body\": \"## Context\\n\\nSubmit mode's interview drafts a private hypothesis from the diff, then asks\\nthe author a budget of questions and plays the draft back for confirmation.\\nNothing in that flow checked whether the author's own account of a change\\nwas internally coherent, or whether it matched what the diff actually\\nshowed. This surfaced live, during a real PR interview, when the author's\\nown answer materially diverged from the diff (retroactively extracted from\\nPR #9, still open at extraction time).\\n\\n## Options considered\\n\\n1. **Keyword/regex comparison of the two author answers.** Rejected —\\n   cannot separate a register difference (the same understanding stated at\\n   two altitudes) from a real material disagreement.\\n2. **Silently write any mismatch into `intent.unknowns` without asking the\\n   author.** Rejected — treats a possible misunderstanding as settled\\n   without giving the author a chance to resolve it in the moment.\\n3. **Add a new `intent` schema field, or a new interview stage, for this\\n   check (chosen: neither).** Rejected as unnecessary — the check reorders\\n   two questions §3 already budgets (`problem`, `approach`) instead of\\n   growing the schema or the stage count.\\n4. **Ask the problem and approach questions blind, before showing the\\n   diff-derived hypothesis, then compare all three accounts by LLM\\n   judgment (chosen).** Read for meaning, not for words, so a register\\n   difference does not read as a disagreement.\\n\\n## Decision\\n\\nAsk the problem and approach questions back to back, blind, before showing\\nthe hypothesis drafted from the diff. Compare all three accounts —\\nproblem answer, approach answer, and the hypothesis — by judgment. A\\nsurviving material mismatch (checked against the diff first, since the\\nhypothesis is the newer, less-tested reading) is raised to the author\\ndirectly, with a choice: work through it now, or log it. An unresolved\\nmismatch lands in the existing `unknowns` field, in the author's own\\nwords, the same way an author-reported gap already does.\\n\\n## Consequences\\n\\n- **Positive:** Catches an author's own unsettled understanding of a\\n  change, or a stated claim the diff does not support, before the PR\\n  opens — not after.\\n- **Constraint introduced:** The interviewing Claude must not show its\\n  drafted hypothesis until both the problem and approach questions are\\n  answered. Showing it earlier would anchor the author's answers instead\\n  of testing them independently.\\n- **Negative / accepted:** The check is a judgment call with no mechanical\\n  backstop. Its accuracy depends on how carefully the interviewing session\\n  compares the three accounts; a careless comparison can miss a real\\n  mismatch or over-flag a harmless register difference.\\n\\n## Value delivered\\n\\n- **New capability:** The interview can detect, before a PR opens, when\\n  the author's own account of a change disagrees with itself or with the\\n  diff.\\n- **Benefit:** An unsettled mental model or a factually incorrect claim\\n  gets caught before it is committed to `intent`, `assessment`, and later\\n  narrative/ADR text that downstream readers trust.\\n- **Beneficiary:** developer, operator.\\n\\n## Maps to\\n\\nDistrict `skills` from `.prodyssey/self/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\"skills/odyssey\"], \"rule\": \"Interview problem and approach answers are compared by judgment, not by keyword match.\"}, \"approved_by\": \"merge of PR #9\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle as proposed while PR #9 was still open.\"}, {\"state\": \"approved\", \"date\": \"2026-08-06\", \"by\": \"merge of PR #9\", \"note\": \"PR #9 has since merged. Recorded as approved on export.\"}], \"provenance\": \"inferred\"}, \"ADR-0011\": {\"id\": \"ADR-0011\", \"title\": \"Design mode: capture intent before the code exists\", \"state\": \"decided\", \"source_pr\": null, \"problem\": \"The plugin captures author intent at submit time, after the change exists. Nothing captures it at design time, when the engineer still holds the alternatives, the constraints, and the doubts.\", \"decision\": \"Add /cobuilder-architect:design, a seven-stage interview-led design mode that writes docs/architecture/designs/<name>/, drafts a proposed ADR, and joins N pull requests through an epics array and the branch name design/<name>[/<epic-slug>].\", \"alternatives\": [{\"option\": \"Build design mode in archkit, next to its existing design mode\", \"rejected_because\": \"The interview discipline, the bundle, the diagram pipeline, and the viewer all live here. archkit's contribution is one skill call.\"}, {\"option\": \"Keep archkit an optional dependency and degrade when absent\", \"rejected_because\": \"A design mode whose substance is optional degrades silently into a chat.\"}, {\"option\": \"Ask the engineer which options they rejected, as an interview question\", \"rejected_because\": \"A rejected option is an outcome of the design process, not an input to it.\"}, {\"option\": \"Mint a synthetic PR key so a design enters the story.json timeline directly\", \"rejected_because\": \"story.json keys on an integer pr, and four readers depend on it.\"}, {\"option\": \"Keep a scalar branch field and accept one PR per design\", \"rejected_because\": \"An initiative with N epics produces N pull requests. A 1:1 join cannot express that.\"}], \"forces\": [\"story.json keys on an integer pr, and a design has no PR number\", \"interview-guide.md caps the interview at six topics\", \"the plugin ships no agents, hooks, or MCP servers\", \"cobuilder-factory owns epic decomposition; design mode must not grow a second planning tier\"], \"delivers\": {\"capability\": \"An engineer can design a change with the plugin before writing it, and the design carries through to the pull requests that implement it.\", \"benefit\": \"Intent, alternatives, and constraints are recorded while the author still knows them, so generate mode stops reconstructing them from merged code.\", \"beneficiary\": [\"developer\", \"reviewer\", \"the-business\"]}, \"body\": \"## Context\\n\\nSubmit mode interviews an author before the pull request opens. That is already\\nlater than the moment the alternatives are still open. Design mode reaches that\\nmoment. The design stays off the timeline until submit mode files it under the\\nreal number that `gh pr create` returns.\\n\\n## Options considered\\n\\n1. **Build it in archkit.** Rejected. This plugin holds the interview, the\\n   bundle, the diagram subagent, and the viewer.\\n2. **Keep the architecture skill optional.** Rejected. The corpus is the design\\n   content, not an enhancement.\\n3. **Ask which options they rejected.** Rejected. Stages 3 and 4 produce those.\\n4. **Mint a synthetic PR key.** Rejected. Four readers assume `pr` is real.\\n5. **One branch per design.** Rejected. N epics need N pull requests.\\n\\n## Decision\\n\\n`/cobuilder-architect:design` runs seven stages and writes\\n`docs/architecture/designs/<name>/`. The ADR is `state: decided`. Stage 4 is\\nthe challenge gate. Stage 7 asks whether this is one PR or several, captures\\nepic slugs if several, and creates the first local branch only.\\n\\n`goal.json` holds an `epics` array. The branch form is `design/<name>` or\\n`design/<name>/<epic-slug>`. Submit mode strips `design/`, takes the first\\nsegment as the name, and measures drift per epic.\\n\\n## Consequences\\n\\n- **Positive:** rejected options exist before any code exists.\\n- **Constraint introduced:** design mode does not decompose epics.\\n- **Negative / accepted:** stage 7 does nothing under `--non-interactive`.\\n\\n## Value delivered\\n\\n- **New capability:** design a change before writing it.\\n- **Benefit:** generate mode stops guessing intent.\\n- **Beneficiary:** developer, reviewer, the-business.\\n\\n## Maps to\\n\\nDistrict `skills`. Unanchored until a boundary.yaml exists.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\"skills/odyssey\", \"commands\"], \"rule\": \"A design lives under docs/architecture/designs/<name>/ and never enters story.json until submit files a real PR number.\"}, \"history\": [{\"state\": \"decided\", \"date\": \"2026-08-19\", \"note\": \"Amended from the hand-authored draft to use docs/, epics, and the cobuilder-architect command prefix. Chosen on this branch; not approved until a human merges.\"}], \"provenance\": \"authored\"}, \"ADR-0012\": {\"id\": \"ADR-0012\", \"title\": \"CoBuilder Implement: a design becomes verified code\", \"state\": \"approved\", \"source_pr\": null, \"problem\": \"Software Factory gates every design decision before code exists, then accepts a slice on the implementer's own word. CoBuilder Guardian scores work independently against a rubric the implementer never reads, but runs only on the pi SDK, DOT pipelines, beads, and Hindsight. Neither can take an approved architecture design and turn it into verified code in an arbitrary coding harness, so the designs cobuilder-architect now produces have nothing that consumes them.\", \"decision\": \"Ship cobuilder-implement as a separate plugin: a three-tier build (initiative, epic, slice) behind four gates, where a validator that did not write the code executes the test suite, then judges whether the tests cover the declared outcomes, and scores every slice and epic at 0.90 or better against a rubric written before the code existed.\", \"alternatives\": [{\"option\": \"One agent writes the rubric, the tests, the code, and the self-report in a single context, and a human skims the log at the end\", \"rejected_because\": \"It abandons independent scoring, which the outcome requires. The agent that wrote the code judges its own test run.\"}, {\"option\": \"Delete the validator and get blindness from context amnesia: one agent writes the rubric, the harness clears context, then the same agent implements and self-scores twice, voiding on a mismatch above 0.15\", \"rejected_because\": \"Erasure blindness cannot be verified from outside the model, and two runs of one policy give correlated error rather than independent verification.\"}, {\"option\": \"Treat rubrics as a cross-repo append-only ledger and cluster criterion fingerprints into a shared genome that primes the validator with a prior\", \"rejected_because\": \"It needs a periodic clustering job across repos, which breaks the no-external-service rule, and it solves a volume problem this work does not have yet.\"}, {\"option\": \"Forbid the validator from reading the implementation and score only from a test runner's exit codes\", \"rejected_because\": \"It scores whether tests pass and never asks whether the tests are worth passing. The validator reads the code after it executes the suite, and judges test adequacy against the specs.\"}, {\"option\": \"Commit a hash of every test file at G3 and check it before scoring\", \"rejected_because\": \"It changes the G3 gate contract to close a hole that a validator judging test adequacy already covers.\"}, {\"option\": \"Score a criterion called 'no undeclared test dependencies' to catch a fixture added after G3\", \"rejected_because\": \"Enumerating a test's transitive dependencies is not free in a dynamically imported language.\"}, {\"option\": \"Strip the validator's retry feedback to pass or fail per criterion, so accumulated feedback cannot leak the rubric's shape\", \"rejected_because\": \"Rich feedback is mandatory for the implementing agent to improve within its attempts. Starving it pushes slices into escalation, which costs more than the leak.\"}, {\"option\": \"Extend Software Factory with a verification step per slice\", \"rejected_because\": \"A rubric written after the code describes the code, so it always passes.\"}, {\"option\": \"Port CoBuilder Guardian and replace its dispatch layer with subagents\", \"rejected_because\": \"The guardian's ceremony was proportionate to its machinery. Remove the machinery and the ceremony stays without its justification.\"}, {\"option\": \"Accept a slice at 0.70, the guardian's shipped threshold\", \"rejected_because\": \"A 0.70 gate accepts three criteria in ten being half met, and half met is where the observed bugs came from.\"}], \"forces\": [\"the skill must run in any harness that can spawn a subagent, with no SDK, no pipeline runtime, and no external service\", \"an implementing agent that reads the rubric writes to the rubric, not to the requirement\", \"an agent that wrote code cannot score it, because it reads its own intent into the code\", \"a skill directory is not a workflow discovery path, but a plugin's workflows directory is\", \"process weight that does not scale down makes small work cost more than the work\", \"ADR-0015 makes the architecture modes self-only, so a build workflow cannot live inside cobuilder-architect\"], \"delivers\": {\"capability\": \"An approved architecture design becomes working code in any coding harness that can spawn a subagent, with every slice and epic gated on an independent score rather than a self-report.\", \"benefit\": \"The defects a self-report hides are found at the slice boundary, while the diff is still small enough to fix cheaply. The design that cobuilder-architect captured stops at nothing.\", \"beneficiary\": [\"developer\", \"reviewer\", \"validator-agent\", \"the-business\"]}, \"body\": \"# ADR-0012 — CoBuilder Implement: a design becomes verified code\\n\\n## Context\\n\\nTwo workflows exist, and each holds half of what a build needs.\\n\\n**Software Factory** makes every important decision before implementation code\\nexists. Four gates run in order, and a human approves each one. It then builds\\nvertical slices, and each slice ends in a working state. Its weakness sits at\\nthe end of a slice. The agent proves the slice works by running it and showing\\nthe result, so the agent that built the slice decides what counts as proof.\\n\\n**CoBuilder Guardian** answers that weakness. It writes acceptance rubrics\\nbefore implementation starts, stores them where the implementer cannot read\\nthem, and scores the result. Its weakness is the runtime. It needs the pi SDK,\\nDOT pipelines, beads, and Hindsight, and none of that runs in another harness.\\n\\nThe valuable half of the guardian needs no runtime. Four ideas carry: write\\nthe rubric before the code, hide it from the implementer, score with an agent\\nthat did not build the thing, and route every gap instead of dropping it.\\n\\nADR-0011 added design mode, so designs now exist and carry intent,\\nalternatives, and constraints that nobody has to reconstruct later. ADR-0015\\nmade the architecture modes self-only, so a build workflow cannot live inside\\nthis plugin. The design therefore has nothing that consumes it.\\n\\n## Options considered\\n\\nStage 4 of design mode contested the stated approach against five frames. The\\nfull rejected set sits in the frontmatter. Three shaped the result.\\n\\n1. **Collapse to one agent, one gate, one log.** Rejected. It abandons\\n   independent scoring, which the outcome requires.\\n\\n2. **Delete the validator, and get blindness from context amnesia.** Rejected\\n   as a trap. It looks right because it assumes away the hard part: blindness\\n   by erasure cannot be verified from outside the model, and two scoring runs\\n   of one policy produce correlated error, not independent verification.\\n\\n3. **Forbid the validator from reading the implementation (chosen, with a\\n   change).** The inversion frame showed that a validator which re-reads the\\n   code and asks whether it looks correct will score a plausible function with\\n   a swallowed exception at 1.0. Its proposed fix was to score only from exit\\n   codes. That fix scores whether tests pass and never asks whether the tests\\n   are worth passing. The decision takes the diagnosis and rejects the cure:\\n   the validator executes the suite first, then reads the code, and judges\\n   whether the tests are sensible and cover the outcomes the specs declared.\\n\\n## Decision\\n\\nShip `cobuilder-implement` as a plugin of its own. It needs a git repository,\\na test command, and the ability to spawn a subagent. It needs nothing else.\\n\\n### Three tiers\\n\\nA feature is an epic, one to one. An epic is a vertical slice of the product,\\nand **it must always be testable**. If nobody can state how to prove an epic\\nworks, it is not an epic yet, and the Business Spec is not finished. An epic\\nholds the slices that build it.\\n\\n### Four gates, two of them repeating\\n\\n| Gate | Name | Cadence | Produces |\\n|---|---|---|---|\\n| G1 | Business Spec | once | the spec, the epics, one skeleton Technical Spec per epic |\\n| G2 | Technical Spec | per epic | the detailed spec, written just in time and research verified |\\n| G3 | Slice Plan and Rubrics | per epic | the slice list, and the blind epic and slice rubrics |\\n| G4 | Acceptance | per slice, then per epic | work scored at 0.90 or better |\\n\\nG2 carries Software Factory's architecture gate and its program design gate for\\none epic. **An epic gate is a complete Software Factory run.** The Business\\nSpec is a new tier above Software Factory's scope, which that workflow never\\nhad an opinion about.\\n\\n### The validator's mandate\\n\\nThe validator did not write the code, and it never wrote the tests. It works in\\ntwo steps, in this order.\\n\\n1. **Execute.** Run the suite in a fresh process. Score from what the run\\n   reports, never from a claimed result.\\n2. **Read, then judge.** Read the implementation and the tests, and judge\\n   whether the tests are sensible, and whether they cover the outcomes the\\n   Business Spec and the Technical Spec declared for this epic and this slice.\\n   A test that passes without exercising the declared outcome is not a pass.\\n\\nThe second step is what stops a rubric from being satisfied by dead code, a\\nstub return, or a mocked response.\\n\\n### Scoring\\n\\nCriteria score `0.0`, `0.5`, or `1.0`. A wider scale lets a validator split the\\ndifference on anything ambiguous, and a marginal slice then drifts past the\\ngate. Every score cites evidence: a test name, captured output, or a file and a\\nline. A score without evidence is an opinion and does not count.\\n\\nA slice passes at 0.90 or better with no critical criterion below 1.0. Below\\nthat it returns to the implementing agent with **full feedback**, up to three\\nattempts, then escalates. An epic gate is hard: below 0.90 a remediation slice\\nreturns the epic feedback to the implementing agent until the epic closes, and\\na human check-in runs after every third remediation.\\n\\n**The threshold does not move in reaction to a failed attempt.** A moved\\ngoalpost converts the score into noise.\\n\\n### The void rule\\n\\nA rigged pass voids the run instead of scoring it, and the void costs no\\nattempt, because what was measured was not the thing. The triggers are a test\\nedited, skipped, weakened, or narrowed; a test whose dependency was swapped or\\nnarrowed in a sibling file; a test that would also pass against the\\npre-change code; and an implementer that read the rubric.\\n\\nThe sibling-file trigger exists because the obvious rule misses the obvious\\nattack. An implementing agent does not need to edit a test to defeat it. It can\\nadd a helper the test imports and narrow what the test exercises, and the diff\\nto the test file stays empty.\\n\\n### The record\\n\\nEach scored slice appends one line to a per-epic ledger that nothing rewrites.\\nThe line carries the epic, the slice, the per-criterion score, and **the\\nvalidator's stated reason for each score**. A session that resumes a stalled\\ninitiative can then see why a slice sits at 0.72 without re-invoking the\\nvalidator.\\n\\n### The light path\\n\\nA single-epic initiative collapses the two specs into one document and skips\\nthe refute panel, the skeptic, and the epic rubric. Research still runs when\\nthe spec names a new library, a new external API, or a version change, because\\nthat risk does not scale with the epic count. **The slice loop and the 0.90\\ngate never switch off.**\\n\\n### Out of scope\\n\\nOpening a pull request or pushing a branch, which stays submit mode's job.\\nChanging design mode. Any code dependency between the two plugins. Agents,\\nhooks, and MCP servers.\\n\\n## Consequences\\n\\n- **Positive.** No slice is declared done by the agent that built it. The\\n  pattern that catches a plausible but wrong pass is structural, and it does\\n  not depend on the implementer's care.\\n\\n- **Positive.** A validator that judges test adequacy catches the failure the\\n  void rule cannot express: criteria written at a level that dead code\\n  satisfies.\\n\\n- **Positive.** The skill runs in any harness with a subagent. A scripted\\n  runner adds deterministic control flow, and the manual three-role mode is the\\n  portable fallback.\\n\\n- **Constraint introduced.** No slice or epic is accepted on the word of the\\n  agent that built it. A validator that did not write the code executes the\\n  tests, judges their adequacy against the declared outcomes, and scores at\\n  0.90 or better against a rubric written before the code existed.\\n\\n- **Constraint introduced.** The threshold is 0.90, and it does not move in\\n  reaction to a failed attempt. A different value is set only when a human asks\\n  for it, and the reason is recorded.\\n\\n- **Risk carried, accepted.** Blindness is honour based. Nothing mechanically\\n  stops an implementer reading the rubric directory. The void rule punishes the\\n  read after the fact, and detection depends on the validator noticing.\\n\\n- **Risk carried.** At volume, a validator pool statistically absorbs the shape\\n  of passing rubrics, so prompt discipline leaks. The fix proposed for this\\n  needs a cross-repo clustering job, which the no-external-service rule\\n  forbids, so the risk stays open and named.\\n\\n- **Risk carried.** Just-in-time detailing can redefine a contract another epic\\n  depends on. A skeleton names the contracts an epic exposes or consumes, and\\n  those freeze at G1. The rule is a rule, not a mechanism, and it depends on\\n  each skeleton naming its contracts honestly.\\n\\n- **Risk carried.** The validator now does two jobs, and the second is\\n  judgment. Judgment scored on a three-value scale may not repeat run to run.\\n  Consistency is an unknown to measure on real slices, not to assume.\\n\\n- **Negative, accepted.** A multi-epic initiative runs one refute panel, plus a\\n  research panel and a skeptic per epic, before the first slice starts. The\\n  light path removes this for single-epic work, and nothing removes it for\\n  multi-epic work.\\n\\n## Value delivered\\n\\n- **New capability.** An approved architecture design becomes working code in\\n  any coding harness that can spawn a subagent, with every slice and epic gated\\n  on an independent score rather than a self-report.\\n- **Benefit.** The defects a self-report hides are found at the slice boundary,\\n  while the diff is still small enough to fix cheaply. The intent that\\n  cobuilder-architect captured now reaches the code.\\n- **Beneficiary.** The developer building the change, the reviewer reading it,\\n  the validating agent that finally has a rubric and a mandate, and the\\n  business that inherits fewer escaped defects.\\n\\n## Maps to\\n\\nDistrict `skills` from `.cobuilder-architect/self/inventory.yaml`, and the\\nplugin that ships outside this repository. The join to this repository is one\\nfile, and [ADR-0013](ADR-0013-design-mode-implement-join.md) governs it.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\"skills\"], \"rule\": \"No slice or epic is accepted on the word of the agent that built it. A validator that did not write the code executes the tests, judges their adequacy against the declared outcomes, and scores at 0.90 or better against a rubric written before the code existed.\"}, \"approved_by\": \"bjoerns\", \"history\": [{\"state\": \"decided\", \"date\": \"2026-08-19\", \"note\": \"Recorded from design mode. Stage 4 produced seven rejected options. Chosen on this branch; not approved until a human merges.\"}, {\"state\": \"approved\", \"date\": \"2026-08-19\", \"by\": \"bjoerns\", \"note\": \"Approved in the design review session, before merge.\"}], \"related\": [\"docs/architecture/designs/cobuilder-implement/intent.json\"]}, \"ADR-0013\": {\"id\": \"ADR-0013\", \"title\": \"Design mode and cobuilder-implement: split the first gate, join through a file\", \"state\": \"approved\", \"source_pr\": null, \"problem\": \"Design mode and cobuilder-implement both capture intent before code exists. Left alone, each grows a weaker copy of the other: design mode grows a planning tier, and the build workflow grows an architecture interview. ADR-0011 also joins a design to its pull request through the branch name, and that join is one to one, which a Business Spec with several epics cannot express.\", \"decision\": \"Split the first gate. Design mode owns framing, exploration, and the challenge, and produces the ADR and intent.json. cobuilder-implement owns epic decomposition and everything downstream. The two join through docs/architecture/designs/<name>/intent.json, a file contract and not a code dependency, and goal.json carries an epics array so one design can track many pull requests.\", \"alternatives\": [{\"option\": \"Design mode replaces the whole first gate, including epic decomposition\", \"rejected_because\": \"Design mode would grow a second, weaker planning tier. Decomposition into testable epics feeds the rubrics, and it is the build workflow's competence.\"}, {\"option\": \"cobuilder-implement authors its own Business Spec and ignores design mode\", \"rejected_because\": \"It rebuilds the interview, the corpus, the divergent exploration, and the challenge stage that design mode already has.\"}, {\"option\": \"Make design mode a hard dependency of cobuilder-implement\", \"rejected_because\": \"The build workflow must run in any harness. A dependency on this plugin and its corpus ends that.\"}, {\"option\": \"Join the two through a code call, such as a skill invocation\", \"rejected_because\": \"A code call couples release cycles. A file on disk lets either side run alone, and lets a third tool read the same contract.\"}, {\"option\": \"Keep the scalar branch field in goal.json and accept one pull request per design\", \"rejected_because\": \"An initiative with several epics produces several pull requests, and a one-to-one branch join cannot express that.\"}], \"forces\": [\"ADR-0011 is amendable rather than superseded, because no pull request has merged it\", \"story.json keys on an integer pr, so a design never enters the timeline until a pull request exists\", \"cobuilder-implement must carry no dependency on this plugin\", \"ADR-0014 moved authored source into docs/, so the design directory is the contract's home\", \"design mode's challenge stage contests the approach, and never the epic decomposition, because it does not produce one\"], \"delivers\": {\"capability\": \"An engineer designs a change in cobuilder-architect, builds it with cobuilder-implement, and each epic's pull request points back at the design that produced it.\", \"benefit\": \"Neither tool carries a weaker copy of the other's job, and drift is measured per epic against that epic's slice of the design rather than against a whole record no single pull request was going to satisfy.\", \"beneficiary\": [\"developer\", \"reviewer\", \"the-business\"]}, \"body\": \"# ADR-0013 — Design mode and cobuilder-implement: split the first gate, join through a file\\n\\n## Context\\n\\nADR-0011 added `/cobuilder-architect:design`, a seven-stage interview-led design\\nmode. It grounds itself in the bundle, interviews the engineer, explores\\ndivergent options, challenges the stated approach, and drafts a record with\\ndiagrams and an `intent.json`.\\n\\nADR-0012 added cobuilder-implement, whose first gate writes a Business Spec,\\nchallenges it, and decomposes it into testable epics.\\n\\nBoth capture intent before the code exists. The overlap is real, and it is not\\naccidental. Without a decision, each one grows a weaker copy of the other half.\\n\\nTwo further facts shape the answer. Design mode's challenge stage contests\\n**the approach**, and it never contests an epic decomposition, because it does\\nnot produce one. And ADR-0011 joins a design to its pull request through the\\nbranch name, one to one. A Business Spec with several epics produces several\\npull requests, so a scalar branch field cannot express the result.\\n\\n## Options considered\\n\\n1. **Design mode replaces the whole first gate.** Rejected. Decomposition into\\n   testable epics feeds the rubrics, and it belongs to the workflow that writes\\n   them. Moving it here grows a second planning tier.\\n\\n2. **cobuilder-implement ignores design mode.** Rejected. It rebuilds an\\n   interview, a corpus, a divergent exploration, and a challenge stage that\\n   already exist.\\n\\n3. **Split the first gate, and join through a file (chosen).** Design mode runs\\n   its seven stages. cobuilder-implement reads the result and decomposes it.\\n   Neither imports the other.\\n\\n## Decision\\n\\n### The first gate splits in two\\n\\n**Design (optional), supplied by this plugin.** It produces the record, the\\ndiagrams, and `intent.json`. Its last stage asks only whether the change is one\\npull request or several, captures the epic slugs when the answer is several,\\nand creates the first branch. **Design mode does no epic decomposition.**\\n\\n**Decomposition, always run by cobuilder-implement.** It turns the design into\\nordered epics, each with testable acceptance criteria, declared dependencies,\\nand named cross-epic contracts. It writes the epics back into `goal.json`.\\n\\ncobuilder-implement asks once whether design mode is available and wanted, and\\nrecords the answer. When the answer is no, it drafts the Business Spec itself.\\n\\n### The challenge retargets rather than repeats\\n\\nDesign mode's challenge stage contests the approach. cobuilder-implement's\\nrefute panel therefore contests what that stage never saw: whether the epics\\nare genuinely independent, whether each one is testable, whether one is\\nmissing, and whether the declared cross-epic contracts hold. No signal is paid\\nfor twice.\\n\\n### The join is one file\\n\\n```\\ndocs/architecture/designs/<name>/intent.json\\n```\\n\\ncobuilder-implement reads it. Design mode writes it. Neither side calls the\\nother, so either runs alone, and a third tool can read the same file.\\n\\n### goal.json carries an epics array\\n\\nThe scalar `branch` field retires.\\n\\n```json\\n\\\"epics\\\": [\\n  {\\\"id\\\": \\\"E1\\\", \\\"slug\\\": \\\"guest-checkout\\\",\\n   \\\"outcome\\\": \\\"<testable criterion>\\\",\\n   \\\"branch\\\": \\\"design/checkout/guest-checkout\\\",\\n   \\\"pr\\\": 42, \\\"state\\\": \\\"merged\\\"},\\n  {\\\"id\\\": \\\"E2\\\", \\\"slug\\\": \\\"saved-cards\\\",\\n   \\\"outcome\\\": \\\"<...>\\\", \\\"branch\\\": null, \\\"pr\\\": null, \\\"state\\\": \\\"planned\\\"}\\n],\\n\\\"stage\\\": \\\"partially-delivered\\\"\\n```\\n\\nDesign mode writes the slugs. cobuilder-implement writes `outcome`, because\\nonly decomposition knows the testable criterion.\\n\\n### Branch convention\\n\\n`design/<design-name>/<epic-slug>`. Git allows a slash, so the join is a prefix\\nparse. Submit mode removes `design/`, reads the first segment as the design\\nname, and reads the rest as the epic slug. A single-epic design keeps the plain\\n`design/<name>` form with one entry and no second segment, so the common case\\nadds nothing to remember. The authoritative fallback stays a scan of every\\n`goal.json` for a matching `epics[].branch`.\\n\\n### Submit mode records the design\\n\\nSubmit mode writes `intent.design = {name, epic}` onto the timeline entry, so a\\npull request points back at its design and the viewer can group several pull\\nrequests under one. Drift is measured per epic, against that epic's slice of\\nthe design. `goal.stage` gains `partially-delivered` and `delivered`, and a\\ndesign is delivered when every epic has a merged pull request.\\n\\n## Consequences\\n\\n- **Positive.** Neither tool duplicates the other. Design mode keeps its corpus\\n  and its interview. cobuilder-implement keeps its harness independence.\\n\\n- **Positive.** Drift becomes measurable per epic. A per-epic baseline is\\n  fairer than a whole record that no single pull request was going to satisfy.\\n\\n- **Constraint introduced.** The two plugins join through\\n  `docs/architecture/designs/<name>/intent.json` and never through a code call.\\n  Design mode does no epic decomposition, and cobuilder-implement does no\\n  architecture interview.\\n\\n- **Constraint introduced.** The contract path appears in two plans. **A change\\n  to the path in one plan must change the other in the same commit, or the\\n  contract breaks silently.** The failure mode is not an error. The reader\\n  finds no file and falls back to drafting a Business Spec from scratch, which\\n  looks like normal operation. A reader that finds no file must report that it\\n  looked and found nothing.\\n\\n- **Amends ADR-0011.** That record is not approved, so this one amends it\\n  rather than superseding it. The amended parts are the scalar `branch` field,\\n  the one-design-to-one-pull-request assumption, and the addition of the\\n  one-or-several question at the last stage.\\n\\n- **Constraint introduced.** A branch may carry more than one design. The\\n  `epics` array models one design across many pull requests. It does not model\\n  many designs inside one pull request, and the branch parse returns the first\\n  match. Submit mode therefore treats design resolution as a judgment, not a\\n  lookup. When a branch matches one design but the diff also touches a second\\n  design's declared modules, submit mode names both and asks the author which\\n  designs this pull request delivers. It never picks silently.\\n\\n- **Risk carried.** The split depends on design mode never growing a\\n  decomposition step. A future session that adds one recreates the duplication\\n  this record exists to prevent.\\n\\n## Value delivered\\n\\n- **New capability.** An engineer designs a change in cobuilder-architect,\\n  builds it with cobuilder-implement, and every epic's pull request points back\\n  at the design.\\n- **Benefit.** One design tracks to completion across many pull requests, and\\n  neither tool carries a weaker copy of the other's job.\\n- **Beneficiary.** The developer designing and building, the reviewer tracing a\\n  pull request to its design, and the business reading a delivered design.\\n\\n## Maps to\\n\\nDistricts `skills` and `docs` from `.cobuilder-architect/self/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\"skills\", \"docs\"], \"rule\": \"The two plugins join through docs/architecture/designs/<name>/intent.json and never through a code call. Design mode does no epic decomposition; cobuilder-implement does no architecture interview.\"}, \"approved_by\": \"bjoerns\", \"history\": [{\"state\": \"decided\", \"date\": \"2026-08-19\", \"note\": \"Recorded from the cobuilder-implement design. Amends ADR-0011's one-design-to-one-pull-request assumption. Chosen on this branch; not approved until a human merges.\"}, {\"state\": \"approved\", \"date\": \"2026-08-19\", \"by\": \"bjoerns\", \"note\": \"Approved in the design review session, before merge.\"}], \"related\": [\"docs/architecture/designs/cobuilder-implement/intent.json\"]}, \"ADR-0014\": {\"id\": \"ADR-0014\", \"title\": \"Authored source lives in docs/; the bundle is derived\", \"state\": \"decided\", \"source_pr\": null, \"problem\": \"The bundle directory had become a document store. ADRs lived only as JSON, designs and PR write-ups sat under exports/, and a future session could not tell authored text from a compiled projection.\", \"decision\": \"Keep authored source in docs/ (ADRs, designs, reviews, pull-request files). Rebuild data/adrs.json, adrs.js, and designs.js from that tree by full rebuild, never merge. exports/ keeps only publish artifacts and the gitignored diff cache.\", \"alternatives\": [{\"option\": \"Keep a lite JSON ADR store next to the full markdown store\", \"rejected_because\": \"Two stores drift. The lite schema existed because records described a foreign repo; architecture modes are now self-only, so that premise is gone.\"}, {\"option\": \"Leave designs and PR documents in exports/\", \"rejected_because\": \"exports/ is the publish pipeline. Mixing authored intent with generated HTML hides the documents from ordinary review.\"}, {\"option\": \"Merge new ADRs into data/adrs.json in place\", \"rejected_because\": \"build_diagrams.py already shows the rule: the authored files are the source, and the projection is a full rebuild.\"}], \"forces\": [\"migrate_bundle.py's authored-field guard does not cover adrs.json\", \"the viewer reads window.ADRS and must keep that shape\", \"foreign fixtures must not grow a docs/ tree\"], \"delivers\": {\"capability\": \"A reviewer reads ADRs, designs, and PR write-ups as ordinary markdown in docs/.\", \"benefit\": \"Paid bundle content stays derived, and a session cannot silently edit JSON that was supposed to be compiled.\", \"beneficiary\": [\"developer\", \"reviewer\"]}, \"body\": \"## Context\\n\\n`build_diagrams.py` already treats `.mmd` files as the source and `diagrams.js`\\nas a full rebuild. ADRs and designs did not follow that rule. The lite JSON\\nstore was justified by foreign-repo history. Phase 2 removed that premise.\\n\\n## Options considered\\n\\n1. **Two ADR stores.** Rejected. They drift.\\n2. **Keep designs in exports/.** Rejected. That directory is the publish\\n   pipeline.\\n3. **Full rebuild from docs/** (chosen). Same rule as diagrams.\\n\\n## Decision\\n\\n`docs/architecture/adr/` is the ADR register. `docs/architecture/designs/`\\nholds design directories. `docs/pull-requests/` holds description and\\nassessment files. `build_adrs.py` and `build_designs.py` rebuild the self\\nbundle only. A foreign `--bundle-dir` is refused. `exports/` keeps\\n`pr-N.html`, `index.html`, `publish-manifest.json`, and `branch-*/diff.json`.\\n\\n## Consequences\\n\\n- **Positive:** authored text is reviewable in git as markdown.\\n- **Constraint introduced:** generate and submit write markdown, then compile.\\n- **Negative / accepted:** the two committed fixtures keep their JSON ADRs\\n  and grow no `docs/` tree.\\n\\n## Value delivered\\n\\n- **New capability:** one organising rule for every authored document.\\n- **Benefit:** the bundle stops being a document store.\\n- **Beneficiary:** developer, reviewer.\\n\\n## Maps to\\n\\nDistrict `scripts`. Unanchored until a boundary.yaml exists.\\n\", \"maps_to\": {\"district\": \"scripts\", \"unanchored\": true, \"modules\": [\"scripts\", \"docs/architecture\"], \"rule\": \"Authored source lives in docs/. Derived projections live in the bundle. build_adrs.py and build_designs.py are self-only full rebuilds.\"}, \"history\": [{\"state\": \"decided\", \"date\": \"2026-08-19\", \"note\": \"Recorded with the cobuilder-architect merge. Chosen on this branch; not approved until a human merges.\"}], \"provenance\": \"authored\"}, \"ADR-0015\": {\"id\": \"ADR-0015\", \"title\": \"Four shipped skills, architecture modes self-only\", \"state\": \"decided\", \"source_pr\": null, \"problem\": \"Design mode needs the architecture corpus, and the install-surface claim that the plugin ships exactly two skills was already false once mermaid existed. Keeping architecture in a second repo, or keeping ste-writing repo-local, left design mode without a corpus it could edit.\", \"decision\": \"Merge archkit main at 61f3c3e into this plugin, ship four skills (odyssey, architecture, mermaid, ste-writing), rename the plugin and bundle directory to cobuilder-architect, and make the six architecture modes self-only. Odyssey's five modes keep --repo.\", \"alternatives\": [{\"option\": \"Keep archkit a soft, optional dependency\", \"rejected_because\": \"A design mode whose corpus is optional degrades into a chat.\"}, {\"option\": \"Give architecture modes --repo as well\", \"rejected_because\": \"docs/ is a sanctioned write location for this repo only. A foreign checkout must not receive architecture documents from this plugin.\"}, {\"option\": \"Keep ste-writing under .claude/skills/ only\", \"rejected_because\": \"ADR procedural text and skill prose now run through that skill on every design and generate pass. A repo-local copy is invisible to an installer.\"}], \"forces\": [\"the real install constraint is no agents, no hooks, and no MCP servers\", \"ADR-0004 said ste-writing stays off the install surface\", \"GitHub redirects a renamed repository, so existing marketplace installs should keep resolving\"], \"delivers\": {\"capability\": \"One plugin covers design, submit, and review, with the corpus in the same install.\", \"benefit\": \"Design mode can reason with a corpus it can edit, and an installer gets a predictable surface with no extra permission hooks.\", \"beneficiary\": [\"developer\", \"plugin installers\"]}, \"body\": \"## Context\\n\\nADR-0004 kept `ste-writing` off the install surface so an installer received\\nonly odyssey and mermaid. Design mode needs the architecture skill in the\\nsame plugin. The two-skills sentence in CLAUDE.md was already stale.\\n\\n## Options considered\\n\\n1. **Optional archkit.** Rejected. Design without a corpus is a chat.\\n2. **Architecture `--repo`.** Rejected. `docs/` is self-only.\\n3. **Keep ste-writing repo-local.** Rejected. Design and generate invoke it.\\n4. **Merge, ship four skills, self-only architecture** (chosen).\\n\\n## Decision\\n\\nThe plugin is `cobuilder-architect` version 0.4.0. It ships `odyssey`,\\n`architecture`, `mermaid`, and `ste-writing`. Architecture commands refuse\\n`--repo` and `--store`. Odyssey keeps both. A leftover `.prodyssey/` store\\nis moved to `.cobuilder-architect/` before any mode resolves the bundle dir.\\n\\nThe install constraint that remains is: no agents, no hooks, no MCP servers.\\n\\n## Consequences\\n\\n- **Positive:** design, submit, and review share one install.\\n- **Constraint introduced:** architecture analysis of a foreign repo is out\\n  of scope.\\n- **Negative / accepted:** ADR-0004's \\\"never a third skill\\\" clause is no\\n  longer current. The record stays in the register as history.\\n\\n## Value delivered\\n\\n- **New capability:** one plugin for the architecture lifecycle except build.\\n- **Benefit:** the corpus and the mode that uses it cannot skew across repos.\\n- **Beneficiary:** developer, plugin installers.\\n\\n## Maps to\\n\\nDistrict `skills`. Unanchored until a boundary.yaml exists.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\"skills\", \".claude-plugin\"], \"rule\": \"The plugin ships four skills and no agents, hooks, or MCP servers. Architecture modes refuse a foreign target.\"}, \"history\": [{\"state\": \"decided\", \"date\": \"2026-08-19\", \"note\": \"Recorded with the cobuilder-architect merge. ADR-0004's install-surface clause no longer holds. Chosen on this branch; not approved until a human merges.\"}], \"provenance\": \"authored\"}, \"ADR-0016\": {\"id\": \"ADR-0016\", \"title\": \"Five sibling plugins, with the bundle as the only seam\", \"state\": \"approved\", \"source_pr\": 11, \"problem\": \"One plugin now carries the whole architecture lifecycle. Its four skills, thirteen commands, and twenty-two scripts serve four separate jobs, and a user who wants one job installs all four.\", \"decision\": \"Split the plugin into five sibling plugins in one marketplace repository, and let them integrate only through the bundle directory on disk.\", \"alternatives\": [{\"option\": \"Keep one plugin and organise inside it with directories and modes\", \"rejected_because\": \"The install surface is the unit a user chooses. A directory is not installable, so a user who wants only pull-request narration still takes the whole corpus, the viewer, and the paid-art pipeline.\"}, {\"option\": \"Two plugins, one per pillar, with view and publish duplicated into both\", \"rejected_because\": \"The viewer reads projections that both pillars write. Two copies of one viewer drift, and the Designs / Pull-requests toggle then renders differently depending on which plugin last refreshed it.\"}, {\"option\": \"Let plugins call each other's scripts directly through a shared path\", \"rejected_because\": \"Not possible. A cached plugin cannot read outside its own directory, and ${CLAUDE_PLUGIN_ROOT} resolves only to that plugin's own cache.\"}, {\"option\": \"Abandon the plugin format and ship one cloned repository with entry-point scripts\", \"rejected_because\": \"It removes version skew by removing the install surface, which is the product. `/plugin install` and nothing else is a deliberate constraint of this project.\"}], \"forces\": [\"The install surface is fixed: no agents, no hooks, no MCP servers. A plugin installs with /plugin install and nothing else.\", \"A cached plugin cannot reference files outside its own directory. The dependencies field guarantees co-installation, not file access.\", \"A user can hold four different plugin versions on one machine at the same time.\", \"The bundle holds authored prose, paid images, and paid audio. A wrong write is not recoverable by regeneration.\", \"There is no CI, no test suite, and no package manager in this repository.\"], \"delivers\": {\"capability\": \"A user installs one stage of the architecture lifecycle without installing the other three.\", \"benefit\": \"Each pillar releases on its own cadence, and a team that wants only PR narration does not carry the review corpus, the viewer, and the paid-art pipeline.\", \"beneficiary\": [\"operator\", \"developer\"]}, \"body\": \"# ADR-0016 — Five sibling plugins, with the bundle as the only seam\\n\\n## Context\\n\\nThe plugin ships as one unit. It holds 4 skills, 13 commands, 22 Python\\nscripts, and 1 HTML viewer. Those parts serve four separate jobs: design\\na change, build it, submit and narrate the pull request, and render the\\nresult. A user who wants one job installs all four.\\n\\nThe names have drifted with the scope. The same short words mean\\ndifferent things in the two skill families, and `CLAUDE.md` now carries a\\nvocabulary table to keep them apart. A vocabulary table is a symptom. The\\nwords collide because one install surface holds four bounded contexts.\\n\\nThree facts constrain any split.\\n\\nThe install surface is fixed. No agents, no hooks, and no MCP servers.\\nThis is deliberate, so the plugin never touches another session's\\npermission surface.\\n\\nA cached plugin cannot read outside its own directory. The reference\\ndocumentation states that paths which traverse outside the plugin root do\\nnot work after installation, because those files are not copied to the\\ncache. `${CLAUDE_PLUGIN_ROOT}` resolves to one plugin's own cache and\\nnothing above it. The `dependencies` field in `plugin.json` guarantees\\nco-installation. It does not grant file access.\\n\\nThe output directory holds content that a rerun cannot replace. It carries\\nauthored narrative prose, paid image generation, and paid text-to-speech\\naudio. `migrate_bundle.py` already treats this as the primary risk. It\\nruns its whole ladder in memory over a deepcopy and refuses to write when\\nan authored field changes outside a declared `touches` set.\\n\\n## Options considered\\n\\nFive options came out of a divergent exploration across five frames,\\nfollowed by an isolated critic pass. `references/divergent-exploration.md`\\ngoverns the procedure.\\n\\n1. **Abandon the plugin format.** Ship one cloned repository, invoke entry\\n   points by path, and let a normal Python import handle shared modules.\\n   Version skew becomes impossible by construction. Rejected: it removes\\n   skew by removing the install surface, and the install surface is the\\n   product.\\n\\n2. **Keep one plugin, organise internally.** Rejected: a directory is not\\n   installable. This is the status quo, and it is what the split exists to\\n   change.\\n\\n3. **Two plugins, one per pillar.** Duplicate the viewer into each.\\n   Rejected: both pillars write projections the one viewer reads. Two\\n   copies drift, and the Designs / Pull-requests toggle then depends on\\n   which plugin last refreshed the file.\\n\\n4. **Four plugins plus a shared non-plugin directory.** Chosen, and\\n   extended to five. See the Decision below.\\n\\n5. **Five plugins with a write-log ledger** that records which plugin\\n   wrote what, so a person can reconstruct the causal chain after a\\n   failure. Kept, but demoted. It is forensics, not a contract. It reports\\n   a mismatch after the mismatch has already been written. ADR-0017 folds\\n   the useful part of it into the compatibility record.\\n\\nThe critic flagged one trap. An option that shares code by symlink and\\nadds a single scalar version comparison scores well on fit, and it hides\\nthe hard part. A scalar comparison cannot express a sequenced migration\\nladder with per-step `touches` sets. It would give false confidence while\\nthe ladder logic still diverges between two installed copies. ADR-0017\\nrecords what replaces it.\\n\\n## Decision\\n\\nSplit into five sibling plugins in one marketplace repository. None is\\nnested inside another.\\n\\n| Plugin | Owns |\\n|---|---|\\n| `cobuilder-architect` | design, describe, decisions, review, maintenance, debug, baseline |\\n| `cobuilder-pr` | generate, assess, review |\\n| `cobuilder-artifact` | view, publish, and the viewer |\\n| `cobuilder-implement` | builds an epic from a design |\\n| `cobuilder-full-lifecycle` | one orientation skill, and the other four as dependencies |\\n\\nEvery plugin name carries the `cobuilder-` prefix. Duplicate-name\\nvalidation in `marketplace.json` is scoped to one marketplace file, so two\\nmarketplaces can each publish a plugin named `architect`. Skill namespacing\\nruns off the bare `name` field, so that collision would be real.\\n\\n**The bundle is the only seam.** No plugin reads or imports another\\nplugin's files. `cobuilder-architect` writes `designs.js` and\\n`adrs.js`. `cobuilder-pr` writes `story.json`, the diffs, the diagrams, and\\nthe audio. `cobuilder-artifact` reads all of it and renders.\\n`cobuilder-implement` hands off through a git branch and a real pull\\nrequest, which is a file-and-state seam, not a call.\\n\\n**A handoff is allowed. A file reference is not.** The lifecycle does hand\\nwork across pillars. A finished design hands off to `cobuilder-implement`,\\nand a finished implementation hands off to `cobuilder-pr:generate`. That\\nhandoff names the next mode and stops. The mode reads the design directory\\nand the bundle for itself. The plugin that handed off never resolves a path\\ninto the other plugin's root, so the handoff survives a version skew that a\\npath reference would not. This is the line the split enforces: a named mode\\nis a public interface, and a file path is an internal one.\\n\\nIn domain-driven terms the bundle is a Published Language, not a shared\\nkernel. Its shape is documented, versioned, and the contract between\\ncontexts. `corpus/principles/ddd/bounded_context_integration.yaml` names\\nthe anti-pattern this avoids: one shared domain package that erases the\\nboundaries. The shape belongs to the bundle. It does not belong to any\\nplugin that writes into it.\\n\\n**Out of scope for this record.** How the plugins share Python and shared\\nskills, and how they stay compatible across versions, is ADR-0017. That\\nseparation is deliberate. This record establishes that the seam is the\\nbundle. ADR-0017 makes the seam safe.\\n\\n## Consequences\\n\\n- **Positive:** A user installs one stage without the other three.\\n  `cobuilder-full-lifecycle` still installs the whole family in one\\n  command.\\n- **Positive:** The vocabulary collision resolves structurally. The\\n  architect pillar's `review` and the pull-request pillar's `review` are\\n  different commands in different namespaces, not two meanings of one\\n  word.\\n- **Constraint introduced:** No plugin reads or imports another plugin's\\n  files. A plugin may hand off to another plugin's mode by name, and the\\n  state that crosses the handoff travels in the bundle and in `docs/`.\\n- **Negative / accepted:** Four plugins can be four different versions on\\n  one machine. Today one plugin writes the bundle, so this failure mode\\n  does not exist. The split creates it. ADR-0017 answers it, and this\\n  record does not stand without that one.\\n- **Negative / accepted:** Shared code must be duplicated into each\\n  plugin's cache. There is no shared runtime copy, and there cannot be\\n  one.\\n\\n## Value delivered\\n\\n- **New capability:** A user installs one stage of the architecture\\n  lifecycle without installing the other three.\\n- **Benefit:** Each pillar releases on its own cadence. A team that wants\\n  only pull-request narration does not carry the review corpus, the\\n  viewer, and the paid-art pipeline.\\n- **Beneficiary:** operator, developer.\\n\\n## Maps to\\n\\nContext `cobuilder-packaging`, modules `.claude-plugin/`, `commands/`,\\n`skills/`, `scripts/`, `viewer/`.\\n\\n**Flagged:** `docs/architecture/contexts/` does not exist yet, so this\\nrecord has no `boundary.yaml` to anchor against. Run\\n`/cobuilder-architect:describe` for the `cobuilder-packaging` context and\\nwrite the boundary record before this ADR moves to `approved`. The\\ninvariant above is the rule that record must carry.\\n\\n## Addendum (2026-08-26) — the cobuilder- prefix drops, except on the umbrella\\n\\n### Context\\n\\nThe table above named every plugin with the `cobuilder-` prefix. That\\nprefix duplicates the marketplace repository's own name,\\n`cobuilder-plugin` — a plugin's slash-command namespace is its own\\n`plugin.json` `name` field, so every command in this family repeated the\\nfamily's own identity on top of the repo's: `/cobuilder-architect:design`,\\n`/cobuilder-pr:generate`, and so on.\\n\\nPR #17 renamed the four non-umbrella plugins to drop that prefix:\\n`cobuilder-architect` to `architect`, `cobuilder-pr` to `pr`,\\n`cobuilder-artifact` to `artifact`, `cobuilder-implement` to `implement`.\\n`cobuilder-full-lifecycle` keeps its name, because it is the umbrella\\nplugin the repository's own `cobuilder-` prefix already names — renaming\\nit too would leave nothing in the family carrying that identity at all.\\nCommands now read `/architect:design`, `/pr:generate`,\\n`/artifact:publish`, `/implement:start`.\\n\\n### A second collision the first rename did not anticipate\\n\\nTwo of the four renamed plugins already had a skill folder named after\\nwhat the plugin now also became: `implement` (plugin) shipped a skill\\nfolder `skills/implement/`, and `artifact` (plugin) shipped a skill folder\\n`skills/artifact/`. Before PR #17, those strings were distinct\\n(`cobuilder-implement` the plugin, `implement` the skill) and addressed\\nunambiguously as `cobuilder-implement:implement`. After the rename, the\\nplugin's own name and its skill's name became the identical string in\\nboth cases, and `Skill()` resolution inside Claude Code broke: a call\\nmeant for the skill returned the plugin's thin `commands/*.md` dispatcher\\nfile instead, or failed outright as `Unknown skill`. This was reproduced\\ndirectly, independent of any one installer's environment, so it is a\\nproperty of the name collision itself, not of a stale cache.\\n\\n**A plugin's own name must never equal one of its own skill folder names,\\nor one of its own command file basenames.** This constraint did not exist\\nbefore this addendum, because no plugin had shared a name with anything\\nit shipped until this rename created two. Check it explicitly before any\\nfuture plugin rename.\\n\\n### Fix\\n\\n- `implement`'s skill folder `skills/implement/` becomes `skills/build/`\\n  (frontmatter `name: build`). Its command file `commands/implement.md`\\n  becomes `commands/start.md`, so the slash command becomes\\n  `/implement:start` instead of the collision-prone `/implement:implement`.\\n- `artifact`'s skill folder `skills/artifact/` becomes\\n  `skills/cobuilder-artifacts/` (frontmatter `name: cobuilder-artifacts`).\\n  Unlike the plugin names above, this skill name keeps a `cobuilder-`\\n  flavor deliberately: \\\"artifacts\\\" is an overloaded word on its own — this\\n  skill publishes to Claude's own Artifacts feature — so the prefix\\n  disambiguates the skill's internal identifier even though the plugin's\\n  public, short name stays `artifact`. The plugin's own commands are\\n  unaffected: `/artifact:view` and `/artifact:publish` are unchanged.\\n- `cobuilder-full-lifecycle`'s skill folder `skills/orientation/` becomes\\n  `skills/cobuilder-full/` (frontmatter `name: cobuilder-full`). This one\\n  is a consistency rename, not a collision fix — `orientation` never\\n  collided with the plugin's own name — done at the same time so every\\n  plugin in the family follows the same naming discipline.\\n\\n### Consequences\\n\\n- **Positive:** The naming convention this addendum states is now\\n  checkable by inspection: no plugin's own name may equal a skill folder\\n  or command basename it ships.\\n- **Negative / accepted:** This convention has no automated test yet. A\\n  future plugin, or a future rename of an existing one, can reintroduce\\n  the same collision silently. A cheap follow-up would assert this\\n  invariant in `tests/test_plugin_manifests.py`.\\n- **Negative / accepted:** Anyone who enabled a plugin under its old,\\n  `cobuilder-`-prefixed name — in a different repository's own\\n  `.claude/settings.json`, or in `~/.claude/settings.json` — keeps that\\n  stale entry until they update it by hand. This repository's own rename\\n  cannot reach a settings file it does not own.\\n\", \"maps_to\": {\"context\": \"cobuilder-packaging\", \"modules\": [\".claude-plugin/\", \"commands/\", \"skills/\", \"scripts/\", \"viewer/\"], \"rule\": \"No plugin reads or imports another plugin's files. A plugin may hand off to another plugin's mode by name, and the state that crosses the handoff travels in the bundle and in docs/, never through a file path into another plugin's root.\"}, \"approved_by\": \"bjornslib\", \"history\": [{\"state\": \"tentative\", \"date\": \"2026-08-20\"}, {\"state\": \"decided\", \"date\": \"2026-08-21\"}, {\"state\": \"approved\", \"date\": \"2026-08-21\"}], \"related\": [\"docs/architecture/adr/ADR-0017-vendored-shared-code-and-bundle-compatibility.md\"]}, \"ADR-0017\": {\"id\": \"ADR-0017\", \"title\": \"Vendored shared code, and a compatibility gate the bundle owns\", \"state\": \"approved\", \"source_pr\": 11, \"problem\": \"The split of ADR-0016 gives four plugins their own vendored copy of the bundle-writing code, and a user can hold four different versions at once. Three writers already stamp a schema version into the bundle with no check at all.\", \"decision\": \"Vendor shared code from one marketplace-level shared/ directory by symlink, and move the compatibility gate out of skill prose into a single required function that every writer calls before it writes.\", \"alternatives\": [{\"option\": \"Copy-paste the shared modules into each plugin and keep them in step by hand\", \"rejected_because\": \"Nothing keeps them in step. A stale ladder meeting a newer bundle is the exact path that discards paid art and authored prose.\"}, {\"option\": \"One scalar version comparison in each entry point\", \"rejected_because\": \"A scalar cannot express a sequenced ladder with per-step touches sets. Two copies can agree on the number and still disagree on what a step is allowed to change.\"}, {\"option\": \"A write-log ledger as the compatibility mechanism\", \"rejected_because\": \"It records a mismatch after the mismatch is written. It is forensics, not a contract. The useful half is kept as the generators map below.\"}, {\"option\": \"Rely on the skill prose that already says migrate_bundle.py runs first\", \"rejected_because\": \"Prose in one plugin's SKILL.md cannot bind another plugin. Across a plugin boundary the invariant has to be code.\"}], \"forces\": [\"A cached plugin cannot read a sibling plugin's files. Vendoring is the only way to share code.\", \"A symlink elsewhere within the same marketplace is dereferenced at install, and the target content is copied into the cache in its place.\", \"The bundle holds authored prose, paid images, and paid audio. A wrong write is not recoverable.\", \"There is no CI and no test suite, so the gate must be runtime code, not a build check.\", \"migrate_bundle.py already refuses on an unknown schema or a newer bundle_format, so the posture exists and needs extending, not inventing.\"], \"delivers\": {\"capability\": \"Four independently-installed plugins write into one bundle without a stale copy silently destroying authored or paid content.\", \"benefit\": \"The failure mode the split introduces becomes a loud refusal that names the plugin needing an update, instead of a quiet overwrite discovered weeks later.\", \"beneficiary\": [\"operator\", \"developer\", \"validator-agent\"]}, \"body\": \"# ADR-0017 — Vendored shared code, and a compatibility gate the bundle owns\\n\\n## Context\\n\\nADR-0016 splits one plugin into five. Three of them write into one bundle\\ndirectory. Four Python modules and two skills are needed by more than one\\nplugin.\\n\\n| Shared item | Needed by |\\n|---|---|\\n| `_bundle_meta.py` — `SCHEMA_VERSION`, `SCHEMA_VERSION_KNOWN`, `CURRENT_BUNDLE_FORMAT` | architect, pr, artifact |\\n| `_manifest.py`, `migrate_bundle.py`, `verify_bundle.py` | architect, pr, artifact |\\n| `ste-writing` skill | all five |\\n| `mermaid` skill | pr |\\n\\nA cached plugin cannot read a sibling plugin's files, so a shared runtime\\ncopy is not available. Every plugin that needs a module gets its own copy\\nof it. A user can then hold four different versions of that copy on one\\nmachine at the same time.\\n\\nThree facts about the current code decide the shape of the answer.\\n\\n**The refusal posture already exists.** `migrate_bundle.py` lines 351 to\\n372 exit with status 1 when `bundle_format` is newer than\\n`CURRENT_BUNDLE_FORMAT`, and again when `schema_version` is not one the\\nladder knows how to read. Both messages name the remediation.\\n`verify_bundle.py` computes a `too-new` status for the same two fields.\\n\\n**Three writers bypass that posture.** `extract_story.py` line 599 sets\\n`meta.schema_version` to its own compiled constant with no check.\\n`export_artifact.py` line 428 and `_manifest.py` line 76 stamp the same\\nconstant. None of the three reads `bundle.json` first. Today this is safe\\nonly because the skill prose runs `migrate_bundle.py` before anything\\nelse. After the split, that invariant would live as prose in four\\n`SKILL.md` files owned by four plugins. Prose in one plugin cannot bind\\nanother.\\n\\n**`generator_version` becomes wrong on the day of the split.**\\n`migrate_bundle.py` line 238 writes one scalar, read from the running\\nplugin's own `plugin.json`. With one writer that is accurate. With four\\nwriters it records whichever plugin migrated last and silently misreports\\nthe other three.\\n\\n## Options considered\\n\\n1. **Copy-paste the shared modules into each plugin.** Rejected. Nothing\\n   keeps them in step. The inversion frame produced the failure recipe\\n   directly: an old `cobuilder-pr` with `SCHEMA_VERSION_KNOWN` frozen at\\n   `{1.0, 1.1}` runs its stale ladder against a bundle a newer\\n   `cobuilder-architect` already advanced, and drops the fields it does\\n   not recognise.\\n\\n2. **Symlink the shared modules, and add one scalar version comparison to\\n   each entry point.** Rejected as a trap. It scores well because the\\n   symlink half is right and the check sounds like defence in depth. A\\n   scalar cannot express a sequenced ladder with per-step `touches` sets.\\n   Two copies can agree on the version number and still disagree on what a\\n   step may change. Its correct half is absorbed into option 4.\\n\\n3. **A write-log ledger.** Each script appends what it wrote, and a person\\n   reconstructs the causal chain afterwards. Kept in part, demoted as a\\n   mechanism. It reports the mismatch after the mismatch is on disk. Its\\n   useful half is the plugin identity, which option 4 keeps as a map.\\n\\n4. **Vendor by symlink, and let the bundle own the gate.** Chosen. See\\n   below.\\n\\n5. **Cut all five plugins from one marketplace tag.** Kept as the release\\n   discipline underneath option 4. It makes skew within a tag impossible,\\n   which narrows what the gate has to catch. It is a policy and not\\n   enforceable by the install mechanism, so it supports the gate and does\\n   not replace it.\\n\\n## Decision\\n\\n**Vendor shared code from one place.** A `shared/` directory at the\\nmarketplace repository root holds `bundle-core/` (the four modules) and\\nthe two shared skills. It is not itself a plugin. Each plugin symlinks\\nwhat it needs. The documented dereference copies the target content into\\nthat plugin's cache at install, so every plugin ships byte-identical code\\nand no plugin reads outside itself at runtime.\\n\\n**Move the gate from prose into code.** `_bundle_meta.py` gains one\\nfunction:\\n\\n```python\\ndef require_compatible(bundle_dir: Path, plugin: str) -> None:\\n    \\\"\\\"\\\"Refuse to write when this copy is older than the bundle requires.\\\"\\\"\\\"\\n```\\n\\nEvery script that writes into a bundle calls it before its first write.\\nThat is `extract_story.py`, `export_artifact.py`, `_manifest.py`,\\n`build_adrs.py`, `build_designs.py`, and both existing gate holders. The\\ninvariant stops being a sentence in a `SKILL.md` that one plugin cannot\\nenforce on another, and becomes a call in the module every plugin\\nvendors.\\n\\n**The bundle carries the floor, not the plugin.** `bundle.json` gains\\n`min_reader_schema`: the schema version of the newest migration that has\\never touched this bundle. `require_compatible()` refuses when that floor\\nexceeds the caller's own compiled `SCHEMA_VERSION`, before any ladder step\\nruns. This inverts who decides compatibility. A plugin no longer asks\\nwhether its own number looks recent. It asks the bundle what the bundle\\ndemands. That is the same posture as a wire format, and it is the only\\nversion of the check that keeps working when nobody can enumerate the\\nreaders.\\n\\n**`generator_version` becomes `generators`, a map.** Keyed by plugin name,\\nvalued by version. Each writer updates only its own key. The scalar is\\nkept as a mirror of the last writer for one migration cycle, then dropped.\\nThis is the surviving half of the ledger option, at the cost of one field\\ninstead of a second log file.\\n\\nThese changes are a `story.json` and `bundle.json` shape change, so they\\nstep `SCHEMA_VERSION` to `1.3` and take a `SCHEMA_MIGRATIONS` entry with a\\n`touches` set, per ADR-0006. `min_reader_schema` defaults to the bundle's\\ncurrent `schema_version` for a bundle that predates the field, so no\\nexisting bundle is locked out by its own new floor.\\n\\n**Out of scope.** Signing, checksums, or any attempt to verify that a\\nvendored copy was not edited after install. The threat model here is\\nversion skew between honest copies, not tampering.\\n\\n## Consequences\\n\\n- **Positive:** A stale plugin meeting a newer bundle stops before it\\n  writes, and the error names the plugin that needs updating.\\n- **Positive:** The gate holds for a plugin this repository does not ship\\n  and did not review, because the floor lives in the data and the check\\n  lives in the module that plugin vendored.\\n- **Constraint introduced:** A script that writes into a bundle calls\\n  `require_compatible()` first and refuses to write when the bundle's\\n  `min_reader_schema` exceeds its own compiled `SCHEMA_VERSION`.\\n- **Negative / accepted:** Every plugin ships its own copy of the same\\n  four modules. There is no single running copy and there cannot be one.\\n- **Negative / accepted:** A vendored copy goes stale the moment its\\n  plugin is not reinstalled. The gate turns that into a refusal rather\\n  than a corruption, and it does not prevent it.\\n- **Negative / accepted:** The one-tag release discipline is policy.\\n  Nothing in the install mechanism enforces it.\\n\\n## Value delivered\\n\\n- **New capability:** Four independently-installed plugins write into one\\n  bundle without a stale copy silently destroying authored or paid\\n  content.\\n- **Benefit:** The failure mode the split introduces becomes a loud\\n  refusal that names the plugin needing an update, instead of a quiet\\n  overwrite found weeks later, after the paid art and the authored\\n  narrative are already gone.\\n- **Beneficiary:** operator, developer, validator-agent.\\n\\n## Maps to\\n\\nContext `cobuilder-packaging`, modules `scripts/_bundle_meta.py`,\\n`scripts/migrate_bundle.py`, `scripts/verify_bundle.py`,\\n`scripts/extract_story.py`, `scripts/export_artifact.py`,\\n`scripts/_manifest.py`.\\n\\n**Flagged:** as with ADR-0016, `docs/architecture/contexts/` does not\\nexist. The `cobuilder-packaging` boundary record must carry the invariant\\nabove before this record moves to `approved`.\\n\\n## Unverified before implementation\\n\\nThe symlink dereference is documented for sharing files within a\\nmarketplace, and the example given is a meta-plugin's `skills/` directory.\\nIt is not stated for a directory of Python that a skill later runs through\\n`${CLAUDE_PLUGIN_ROOT}`. Install a two-plugin draft from a local\\nmarketplace and read what lands in the cache before this decision leaves\\n`tentative`. If the dereference does not cover it, the fallback is a copy\\nstep before publish. That changes the mechanism and none of the\\ncompatibility reasoning above.\\n\", \"maps_to\": {\"context\": \"cobuilder-packaging\", \"modules\": [\"scripts/_bundle_meta.py\", \"scripts/migrate_bundle.py\", \"scripts/verify_bundle.py\", \"scripts/extract_story.py\", \"scripts/export_artifact.py\", \"scripts/_manifest.py\"], \"rule\": \"A script that writes into a bundle calls require_compatible() first and refuses to write when the bundle's min_reader_schema exceeds its own compiled SCHEMA_VERSION.\"}, \"approved_by\": \"bjornslib\", \"history\": [{\"state\": \"tentative\", \"date\": \"2026-08-20\"}, {\"state\": \"decided\", \"date\": \"2026-08-21\"}, {\"state\": \"approved\", \"date\": \"2026-08-21\"}], \"related\": [\"docs/architecture/adr/ADR-0016-five-sibling-plugins-bundle-as-seam.md\"]}, \"ADR-0018\": {\"id\": \"ADR-0018\", \"title\": \"One lifecycle surface, and a record index that joins it\", \"state\": \"approved\", \"source_pr\": 11, \"problem\": \"The lifecycle produces 41 artifacts across seven reading surfaces. The joins between them are declared in the authored source and resolved nowhere, so a decision is reachable only when a change happens to cite it.\", \"decision\": \"Extend the viewer to one surface covering designs, decisions, contexts, builds, and pull requests, and give it a single derived index that resolves every join between them.\", \"alternatives\": [{\"option\": \"A separate page per artifact family\", \"rejected_because\": \"A reader does not open eleven files to review a pull request. They open one page. Seven surfaces already exist and adding more widens the problem the index exists to close.\"}, {\"option\": \"A local SQLite store for the records\", \"rejected_because\": \"The viewer cannot read it. Six script tags assign window globals and export_artifact.py inlines each as literal JSON. Reaching SQLite needs a WASM build, which breaks the buildless viewer and the artifact size budget.\"}, {\"option\": \"A Postgres service behind the plugin\", \"rejected_because\": \"It is a service. The install surface is /plugin install and nothing else, with no agents, no hooks, and no MCP servers.\"}, {\"option\": \"One file per entity type beside the existing projections\", \"rejected_because\": \"It matches the pattern in data/ and it leaves the joins unresolved, which is the actual problem. One index that references everything is what makes a record reachable.\"}, {\"option\": \"Leave adrs.js and designs.js in place and add the index beside them\", \"rejected_because\": \"Two sources for the same records drift. The index subsumes both.\"}], \"forces\": [\"The viewer loads sibling script tags that assign window globals. That is its only input channel.\", \"export_artifact.py inlines each projection as literal JSON into a published page.\", \"Authored source lives in docs/ and derived projections live in the bundle. A projection is a full rebuild, never a merge.\", \"A derived index goes stale the moment an authored file changes.\", \"17 of 17 ADRs declare a maps_to join. Two resolve to a context. One record is reachable from nowhere.\"], \"delivers\": {\"capability\": \"A person browses the decisions, the contexts, the builds, and the pull requests of a repo in one place, and follows a join from any one of them to the others.\", \"benefit\": \"A decision stops being reachable by accident. The cost of finding why something was built the way it was drops from a search to a click.\", \"beneficiary\": [\"operator\", \"developer\"]}, \"body\": \"# ADR-0018 — One lifecycle surface, and a record index that joins it\\n\\n## Context\\n\\nThe lifecycle produces 41 artifacts. They land on seven reading surfaces:\\nthe viewer, chat, a git diff, a GitHub pull request, a standalone report\\nfile, a published page, and nothing at all.\\n\\nTwo surfaces carry eleven artifacts each. The git diff can be answered. The\\nviewer, which is the surface built for a person to sit and read, cannot.\\n\\nThe joins between the records are worse. Measured against the tree on\\n2026-08-21:\\n\\n- 17 of 17 ADRs declare a `maps_to` block.\\n- 2 resolve to a bounded context. The other 15 carry `unanchored: true` and\\n  point at a district, which is the inferred concept rather than the\\n  verified one.\\n- 16 are reachable through an `.adr-badge` chip. ADR-0013 is reachable from\\n  nowhere.\\n- A boundary record declares `governed_by`, the reverse of that join.\\n  Nothing populates it.\\n\\nReachability today is incidental. A record is visible because some pull\\nrequest or design happens to cite it. There is no index. A reader cannot\\nbrowse the decisions, only stumble on one.\\n\\n## Options considered\\n\\n1. **A separate page per artifact family.** Rejected. Seven surfaces already\\n   exist, and more of them widen the problem.\\n\\n2. **A local SQLite store.** A file and not a service, in the Python\\n   standard library, with real query semantics. Rejected for one reason:\\n   the viewer cannot read it. `viewer/index.html` loads six\\n   `<script src=\\\"../data/*.js\\\">` tags that assign window globals, and\\n   `export_artifact.py` inlines each as literal JSON. Reaching SQLite needs\\n   a WASM build, which breaks the buildless viewer and pushes against the\\n   16 MiB artifact budget. It would serve scripts and not the viewer, which\\n   is two stores for one set of facts.\\n\\n3. **A Postgres service.** Rejected. It is a service, and the install\\n   surface forbids one.\\n\\n4. **One file per entity type.** Rejected. It matches the pattern in `data/`\\n   and leaves every join unresolved.\\n\\n5. **One index that references everything.** Chosen.\\n\\n## Decision\\n\\n**The viewer becomes one surface with five modes.** Designs and Pull\\nrequests exist. Decisions, Contexts, and Builds are added.\\n\\n- **Decisions** is the index of ADRs. Its value is not the list. It is the\\n  anchor column, which shows at a glance which records resolve to a verified\\n  context and which do not.\\n- **Contexts** renders `canvas.md` and `boundary.yaml`. Boundary violations\\n  lead, because each one is recorded as an ADR candidate. The view also\\n  lists the districts no context covers, which is the unverified half of\\n  the map. Nobody runs Describe to reach this view. Design mode calls it\\n  when a design needs a verified region to anchor against, so a context\\n  bundle is a by-product of designing rather than a step a person schedules.\\n- **Builds** renders a feature's gate documents. `00-status.md` is not a\\n  document to display. It is the state that drives the view, so approval\\n  marks and slice scores become chrome. **Builds also carries a Backlog\\n  lane**, showing every epic with a null branch and every slice not yet\\n  scored, ranked by the count of ADRs that depend on the epic. The lane is a\\n  query over the index and not a file, so no backlog document exists to go\\n  stale. This is what makes an epic the backlog record and removes the need\\n  for a separate store of planned work. The view always presents work\\n  epic-first, with each epic's slices nested under it, never as a flat\\n  slice list with an epic column.\\n\\n**One index, `data/index.json` plus `index.js`.** It references every entity\\nand resolves the joins. `build_index.py` rebuilds it in full from `docs/`\\nplus git, and **it subsumes `build_adrs.py` and `build_designs.py`** rather\\nthan sitting beside them. Two sources for one set of records would drift.\\n\\n**Every entity carries a stable id.** Most already do. Two do not:\\n\\n| Entity | Id | Status |\\n|---|---|---|\\n| `adr` | `ADR-NNNN` | stable |\\n| `design`, `context` | directory name | stable |\\n| `epic` | `<design>/<epic-id>` | must be scoped, because `E1` repeats |\\n| `pull_request` | integer | stable |\\n| `district` | `world.districts[].id` | already stable, newly joined |\\n| `boundary_rule` | `<context>/<kind>/<n>` | new |\\n| `comment` | ulid | new, see ADR-0019 |\\n\\n**A district joins to a context, and that join is what gives a district\\nweight.** A district is inferred and a context is verified, and until now\\nthey were two vocabularies for one idea with nothing between them. That is\\nwhy 15 of 17 ADRs anchor to a district and are recorded as `unanchored`:\\nthe anchor they do have resolves to nothing.\\n\\nSo a boundary record declares `verifies: [<district-id>]`, naming the\\ndistricts the context covers. The index resolves the edge in both\\ndirections. Three facts follow that nothing states today:\\n\\n- An ADR anchored to a district reaches a context through that district,\\n  once one covers it. The existing anchors start resolving without a\\n  rewrite.\\n- A district with no context is **unverified coverage**, and the list of\\n  them is the describe backlog, ranked by how many ADRs point at each.\\n- A district is now the coarse map and a context is the surveyed part of\\n  it. The inferred layer stops being a parallel vocabulary and becomes the\\n  first pass of the verified one.\\n\\n**The index carries the state of a pull request, and realisation is derived\\nfrom it.** An ADR's own state machine describes the lifecycle of the\\n*decision*: `idea → tentative → decided → approved → challenged`. It says\\nnothing about whether the code matches the decision yet, and it should not.\\nThose are two axes, and collapsing them into one enum breaks both.\\n\\nSo the index holds `pull_request.state` as `open`, `merged`, or `closed`, and\\nan ADR reaches its pull request two ways: directly through `source_pr`, or\\nthrough the design and epic that delivered it. A merged pull request means the\\ndecision shipped. Nothing declares that. It is computed.\\n\\nThis is why no new ADR state is added. `approved` stays the most final state a\\nperson grants, and shipped is a fact the index reads off git.\\n\\nA boundary rule has three kinds, and one entity carries all three with a\\n`kind` discriminator: a `forbidden_dependencies` edge prohibition, a\\n`modules[].rule` invariant with its inbound and outbound lists, and a\\n`context_map[]` integration pattern. They answer one question, which is what\\nmay cross this boundary.\\n\\n**The index records what it was built from.** A `sources` block holds a\\ncontent hash per `docs/` subtree plus the git head. Any mode that reads the\\nindex compares the hashes first and rebuilds on a mismatch, in the same\\nplace `migrate_bundle.py` already runs before every mode.\\n\\n**Out of scope.** The reply channel is ADR-0019. This record covers what is\\nrendered and how the records join, not how a reader answers back.\\n\\n## Consequences\\n\\n- **Positive:** A decision becomes reachable by browsing rather than by\\n  accident, and the anchor column makes an unresolved join visible instead\\n  of silent.\\n- **Positive:** Identity is the durable half of this decision and the engine\\n  is the reversible half. With stable ids, a local SQLite index can be added\\n  later as a build-time projection beside `index.json` without touching one\\n  authored record.\\n- **Constraint introduced:** Every record carries a stable id, and\\n  `data/index.json` is the single derived projection that resolves the joins\\n  between them.\\n- **Negative / accepted:** A sixth build script, and the two it replaces\\n  must be retired in the same change rather than left in place.\\n- **Positive:** An approved but unshipped decision becomes visible, because\\n  the index can show an ADR whose pull request is still open. Today nothing\\n  distinguishes a decision that describes a target state from one the code\\n  already meets. ADR-0016 is exactly that case.\\n- **Negative / accepted:** A JSON index answers a lookup by id and a one-hop\\n  join by loading the file. It cannot answer an arbitrary traversal without\\n  loading everything. That is the threshold that would later earn SQLite.\\n\\n## Value delivered\\n\\n- **New capability:** A person browses the decisions, the contexts, the\\n  builds, and the pull requests of a repo in one place, and follows a join\\n  from any one of them to the others.\\n- **Benefit:** A decision stops being reachable by accident. The cost of\\n  finding why something was built a certain way drops from a search to a\\n  click.\\n- **Beneficiary:** operator, developer.\\n\\n## Maps to\\n\\nContext `cobuilder-packaging`, modules `viewer/index.html`,\\n`scripts/build_adrs.py`, `scripts/build_designs.py`, `docs/architecture`.\\nSee `docs/architecture/contexts/cobuilder-packaging/boundary.yaml`.\\n\\n## Unverified before implementation\\n\\nA prototype of the Decisions mode exists at\\n`.lavish/decisions-register.html`, generated from the real `adrs.json`. It\\nproves the data supports the view. It does not prove the viewer can carry\\nfive modes without the mode switch becoming the navigation problem this\\nrecord set out to solve.\\n\", \"maps_to\": {\"context\": \"cobuilder-packaging\", \"modules\": [\"viewer/index.html\", \"scripts/build_adrs.py\", \"scripts/build_designs.py\", \"docs/architecture\"], \"rule\": \"Every record carries a stable id, data/index.json is the single derived projection that resolves the joins between them, a boundary record declares the districts it verifies, and realisation is derived from a pull request state rather than declared on a record.\"}, \"approved_by\": \"bjornslib\", \"history\": [{\"state\": \"tentative\", \"date\": \"2026-08-21\"}, {\"state\": \"decided\", \"date\": \"2026-08-21\"}, {\"state\": \"approved\", \"date\": \"2026-08-21\"}], \"related\": [\"docs/plans/cobuilder-family/02a-artifact-map.md\", \"docs/plans/cobuilder-family/02c-record-model.md\"]}, \"ADR-0019\": {\"id\": \"ADR-0019\", \"title\": \"Anchored comments, kept as a durable ledger\", \"state\": \"approved\", \"source_pr\": 11, \"problem\": \"The viewer is the largest reading surface in the system and carries no way to answer. A person reads a whole pull request and has nowhere to put a reaction the agent will ever see.\", \"decision\": \"Compute the anchor from the live DOM at annotation time, and append every comment to a durable append-only ledger with a stable id and an open, answered, or resolved state.\", \"alternatives\": [{\"option\": \"Write a data-fb attribute into every commentable element at build time\", \"rejected_because\": \"It forces the generator to decide in advance what is commentable, it pollutes the markup, and it cannot express a selection that crosses part of an element.\"}, {\"option\": \"Adopt the lavish CLI\", \"rejected_because\": \"It needs Node 22, npx on every call, a background Express server, and a blocking foreground poll. Against an install surface of /plugin install and nothing else, that is disqualifying.\"}, {\"option\": \"Keep the clipboard hand-off that the viewer already ships\", \"rejected_because\": \"It works and it needs a manual paste every time. It is kept as the fallback, not as the mechanism.\"}, {\"option\": \"Publish the bundle and use the Artifact platform's own comment threads\", \"rejected_because\": \"It is a real answer and it needs no new code, but it makes commenting a deliberate act rather than an available one. Kept as the second transport, not the only one.\"}, {\"option\": \"Delete a comment from the store once it is delivered, as the lavish CLI does\", \"rejected_because\": \"It destroys the only record of what a person said. A read must never delete.\"}], \"forces\": [\"The viewer already ships a comments drawer with localStorage persistence and a markdown export. It must be upgraded, not replaced.\", \"A comment is the first record that is neither authored in docs/ nor derivable from it. No rebuild can reconstruct one.\", \"view mode serves static files with python3 -m http.server. A write endpoint makes it a small application.\", \"A reaction without its quote forces the agent to reconstruct what it referred to, which is the problem this plugin exists to remove.\", \"The install surface forbids a hook and an MCP server, so a background command is the only wake path available.\"], \"delivers\": {\"capability\": \"A reader points at one paragraph of a generated page and asks a question the agent will find.\", \"benefit\": \"The largest reading surface in the system stops being one-directional, and the reaction survives the session that produced it.\", \"beneficiary\": [\"operator\", \"developer\"]}, \"body\": \"# ADR-0019 — Anchored comments, kept as a durable ledger\\n\\n## Context\\n\\nEleven artifacts converge on the viewer, more than on any other surface\\nexcept a git diff. A reader can answer a git diff. A reader cannot answer\\nthe viewer.\\n\\nThe viewer is not empty on this point. `viewer/index.html` line 705 already\\nships a comments drawer. It takes a comment tagged to a whole level, keeps a\\nlist, persists to `localStorage` under `odyssey-f:<repo>:<pr>`, and offers\\n**Copy review as markdown**. That is a working clipboard transport, and it\\nis the thing to upgrade rather than to replace.\\n\\nTwo problems remain. The anchor is a whole level, which is too coarse to\\npoint at a claim. And the clipboard needs a person to paste, so the agent\\nlearns nothing unless somebody carries it across.\\n\\nThe lavish CLI solves the anchoring problem well and the persistence problem\\nbadly. Read from `dist/cli.mjs`, its `selector(el)` walks up to five\\nancestors at annotation time, short-circuits on a real `id`, and falls back\\nto `:nth-of-type`. A text selection captures a real DOM range, with a node\\npath and a character offset at each end. Its `takeFeedback()` at line 7522\\nthen returns the queued prompts and clears them in the same transaction.\\nVerified on one machine: all 18 sessions in `~/.lavish-axi/state.json` hold\\n`prompts: []`. The only lifecycle it ships anywhere belongs to\\nauto-detected layout warnings, not to anything a person wrote.\\n\\n## Options considered\\n\\n1. **A `data-fb` attribute written into the page at build time.** Rejected.\\n   It forces the generator to decide in advance what is commentable, it\\n   pollutes the markup, and it cannot express a selection that crosses part\\n   of an element.\\n\\n2. **Adopt the lavish CLI.** Rejected. Node 22, `npx` on every call, a\\n   background Express server on port 4387, a blocking foreground poll, and a\\n   dependency tree including Express, Excalidraw, Tailwind, and Mermaid. Its\\n   `share` path uploads to a third-party host. The install surface forbids\\n   all of it.\\n\\n3. **Keep the clipboard hand-off.** Kept as the fallback, not as the\\n   mechanism. It needs a manual paste every time.\\n\\n4. **Publish the bundle and use the Artifact platform's comment threads.** A\\n   real answer that needs no new code. Kept as the second transport. It\\n   makes commenting deliberate rather than available.\\n\\n5. **Compute the anchor live, and append to a durable ledger.** Chosen.\\n\\n## Decision\\n\\n**Take the anchor from the lavish CLI. Keep the ledger it does not have.**\\n\\nThe anchor is computed from the live DOM when a reader selects something,\\nnot written into the page when the page is built. Any element and any\\nmid-sentence selection becomes annotatable with no prior authoring.\\n\\nOne line is appended per thread to `<bundle-dir>/feedback/threads.ndjson`:\\n\\n```json\\n{\\\"id\\\":\\\"01JCXZ...\\\", \\\"state\\\":\\\"open\\\",\\n \\\"entity\\\":{\\\"type\\\":\\\"pull_request\\\",\\\"id\\\":11,\\\"level\\\":2},\\n \\\"anchor\\\":{\\\"selector\\\":\\\"div.level-stack > p:nth-of-type(2)\\\",\\n           \\\"range\\\":{\\\"start\\\":[0,58],\\\"end\\\":[0,101]},\\n           \\\"quote\\\":\\\"each carries its own vendored copy of the code\\\"},\\n \\\"body\\\":\\\"is this still true after the split?\\\",\\n \\\"author\\\":\\\"human\\\", \\\"ts\\\":\\\"2026-08-21T14:02:00Z\\\",\\n \\\"queue_key\\\":\\\"pr11/l2/p2\\\"}\\n```\\n\\n**The drawer opens on the click, and the text stays in view.** Today the\\ndrawer is a mode a reader enters with the `c` key, and it covers what they\\nwere reading. Reversed: a click on any element on the page opens the drawer\\nwith that element already anchored, and the page shifts rather than hides\\nbehind it. A reader must see the sentence they are commenting on while they\\nwrite the comment. Commenting becomes the available action rather than a\\nmode, which is the whole difference between a channel a person uses and one\\nthey remember exists.\\n\\n- `state` is `open`, `answered`, or `resolved`.\\n- `quote` is the fallback when a selector goes stale. It is also what makes\\n  the comment readable on its own.\\n- `queue_key` deduplicates a re-annotated spot before it is sent.\\n- **A read never deletes.** The ledger is append-only and each reader tracks\\n  its own offset. This is the single point where this record departs from\\n  the mechanism it borrows from.\\n\\n**The agent answers in the thread, not in chat.** A reply is another line in\\nthe same ledger, carrying `\\\"author\\\": \\\"agent\\\"` and `\\\"replies_to\\\": \\\"<thread\\nid>\\\"`, and the drawer renders it under the comment that prompted it. The\\nreader sees the answer beside the sentence they asked about, which is where\\nthe question came from and where its answer belongs. A state change is a\\nthird line naming only `id`, `state`, and `ts`.\\n\\n**The ledger keeps the history and a projection keeps the current state.**\\nThe append-only rule is about not destroying what a person said. It is not a\\nreason to make every reader recompute a state that a file can hold.\\n`feedback/threads-state.json` maps each thread id to its current state, who\\nmoved it, when, and the reply count. It is rebuilt in full from the ledger\\nafter each append, and deleting it costs nothing. The ledger is the truth and\\nthe field is the cache, so a writer appends first and projects second, and a\\ndisagreement between the two is a bug in the projection. This is the same\\nderived-projection discipline `index.json` follows.\\n\\nThis turns a one-way channel into a conversation. Today an answer arrives in\\na chat session that the reader may not be looking at, and it loses the anchor\\nthe question carried. **Threaded replies in the viewer are a required part of\\nthe reply channel, and E6 delivers the record shape and the ledger they need.\\nRendering a thread as a conversation is deliberately deferred past E6**, so\\nthe first version ships the durable record rather than the interface. The\\nrecord shape is designed for it now, because retrofitting `replies_to` onto a\\nledger with real content in it is the change this deferral must not cost.\\n\\n**Machine findings stay in a separate channel.** An automatically detected\\nproblem must not compete with human intent in one inbox.\\n\\n**Two transports, one record.** A published page carries the Artifact\\nplatform's own threads and needs no new code. The local viewer needs\\n`view` mode to accept a `POST /feedback` and append. That turns a static\\nfile server into a small application, which is the real cost of the local\\nhalf.\\n\\n**The write endpoint lands with the anchor, in the same epic.** It was open\\nwhether to ship the anchor first and let a published page carry the threads\\nuntil the endpoint arrived. It does not, for two reasons. A published page\\nis a deliberate act and the local viewer is where the reading happens, so\\nthe deferred half is the half that matters. And a comment written into\\n`localStorage` before the ledger exists is a record with no id and no\\nmigration guard, which is the loss this record was written to prevent. The\\n`POST /feedback` endpoint is therefore part of E6 and not a follow-on.\\n\\n**The agent finds new threads through a background command.** The install\\nsurface forbids a hook and an MCP server, so a background command that\\nblocks until the ledger grows is the only wake path available. It must also\\nexit on a timeout, so a session never waits forever on a reader who left.\\n\\n**A comment is the first record that is neither authored source nor\\nderivable from it.** No rebuild can reconstruct one. It joins the paid art\\nand the authored narrative as content a rebuild would destroy, so it comes\\nunder `migrate_bundle.py`'s guard from the first commit rather than once one\\nis worth losing.\\n\\n## Consequences\\n\\n- **Positive:** The largest reading surface in the system stops being\\n  one-directional.\\n- **Positive:** A reaction carries its quote, so the agent never has to\\n  reconstruct what it referred to.\\n- **Constraint introduced:** A comment is appended to\\n  `feedback/threads.ndjson` with a stable id and a state, and a read of that\\n  ledger never deletes from it.\\n- **Negative / accepted:** `view` mode stops being a bare file server.\\n- **Negative / accepted:** A computed selector can go stale when a page is\\n  regenerated. `quote` is the recovery path, and it is a human-readable one\\n  rather than an automatic one.\\n\\n## Value delivered\\n\\n- **New capability:** A reader points at one paragraph of a generated page\\n  and asks a question the agent will find.\\n- **Benefit:** The reaction survives the session that produced it, which is\\n  what the mechanism this borrows from gets wrong.\\n- **Beneficiary:** operator, developer.\\n\\n## Maps to\\n\\nContext `cobuilder-packaging`, modules `viewer/index.html`,\\n`scripts/migrate_bundle.py`.\\n\\n## Unverified before implementation\\n\\nThe selector walk and the range capture were read from `dist/cli.mjs` around\\nlines 5037 and 5354, and the drain behaviour at line 7522. None of it was\\nrun. The Artifact platform's comment threads are documented and this session\\nhas not carried one end to end.\\n\", \"maps_to\": {\"context\": \"cobuilder-packaging\", \"modules\": [\"viewer/index.html\", \"scripts/migrate_bundle.py\"], \"rule\": \"A comment is appended to feedback/threads.ndjson with a stable id and a state, and a read of that ledger never deletes from it.\"}, \"approved_by\": \"bjornslib\", \"history\": [{\"state\": \"tentative\", \"date\": \"2026-08-21\"}, {\"state\": \"decided\", \"date\": \"2026-08-21\"}, {\"state\": \"approved\", \"date\": \"2026-08-21\"}], \"related\": [\"docs/plans/cobuilder-family/02b-view-designs.md\", \"docs/plans/cobuilder-family/02c-record-model.md\"]}, \"ADR-0020\": {\"id\": \"ADR-0020\", \"title\": \"Viewer parts and an author-time build\", \"state\": \"decided\", \"source_pr\": 11, \"problem\": \"viewer/index.html is 4917 lines in one file: 881 lines of CSS and 3872 lines of JavaScript in a single IIFE holding 145 functions. Five view modes interleave through 50 scattered mode-predicate calls, 45 innerHTML string blocks carry no shared template, and one card fragment is copy-pasted 18 times. The file must ship as one self-contained file, so nobody has been able to split it.\", \"decision\": \"Author the viewer as parts under viewer/src/, and compile them into the committed viewer/index.html with build_viewer.py. The build runs when an engineer changes the viewer, never at install and never in the browser, and a test fails when the committed file does not equal a fresh build.\", \"alternatives\": [{\"option\": \"Leave the viewer as one hand-edited file and remove only the duplication\", \"rejected_because\": \"It delivers easier maintenance without smaller parts. The file stays near 4000 lines, so a reader still holds five interleaved modes in one scroll, and the next mode is as invasive to add as the last one was.\"}, {\"option\": \"Split the viewer into real sibling files loaded by script and link tags\", \"rejected_because\": \"ADR-0001 forbids it. A published Artifact is a single file under a CSP that blocks every external request, and export_artifact.py inlines the data because sibling files do not survive publishing.\"}, {\"option\": \"Generate index.html into the bundle at migrate time and never commit it\", \"rejected_because\": \"/plugin install runs no build, so an installed plugin would ship no viewer at all. migrate_bundle.py and export_artifact.py both read the plugin's own viewer/index.html as an existing file.\"}, {\"option\": \"Adopt a bundler such as esbuild or Vite\", \"rejected_because\": \"It adds a Node toolchain to a repository of prose, Python scripts, and one HTML file. Concatenation of ordered parts is the whole requirement, and PEP 723 already covers every other script here.\"}], \"forces\": [\"ADR-0001: the published viewer is one self-contained file under a CSP that blocks every external request.\", \"The install surface is /plugin install and nothing else, with no agents, no hooks, and no MCP servers. Install runs no build.\", \"export_artifact.py rewrites the viewer by verbatim string replacement and hard-errors with not-found-verbatim when the literal text moves. Any refactor can break publishing silently until somebody publishes.\", \"migrate_bundle.py copies the plugin's viewer/index.html into every bundle unconditionally, so the built file must exist in the plugin.\", \"A generated file that is also committed goes stale the moment somebody edits the output instead of the parts.\"], \"delivers\": {\"capability\": \"An engineer changes one view mode by opening one file of a few hundred lines, instead of finding it inside 3872 lines of one function scope.\", \"benefit\": \"The cost of a viewer change stops scaling with the size of the whole viewer. A sixth mode becomes a new part plus one registry entry, rather than another pass of scattered predicate calls.\", \"beneficiary\": [\"developer\", \"validator-agent\"]}, \"body\": \"# ADR-0020 — Viewer parts and an author-time build\\n\\n## Context\\n\\n`viewer/index.html` is the largest authored file in the repository. Measured\\non 2026-08-24:\\n\\n| Property | Value |\\n|---|---|\\n| Total lines | 4917 |\\n| CSS | 881 lines |\\n| JavaScript | 3872 lines, one IIFE, 145 functions |\\n| Mode-predicate calls | 50, across five view modes |\\n| `innerHTML` string assignments | 45 |\\n| `<template>` elements | 0 |\\n| The `empty-card` fragment | repeated 16 times, `empty-detail` 18 times |\\n| Longest functions | 231, 222, and 219 lines |\\n\\nThe five view modes do not live in five places. They interleave. A single\\nfunction, `renderLevelRail`, carries the same rail-item template four times\\nand differs only in the data behind it. Three separate functions of over\\n200 lines each render main content for one mode apiece.\\n\\nTwo facts have kept this file whole. `/plugin install` runs no build step,\\nso whatever the plugin ships is what runs. And ADR-0001 records that a\\npublished Artifact is one file under a CSP that blocks every external\\nrequest, which is why `export_artifact.py` inlines the data rather than\\nloading it. A viewer split into sibling files loaded at runtime cannot be\\npublished.\\n\\nA third fact makes the situation worse rather than merely awkward.\\n`export_artifact.py` edits the viewer by verbatim string replacement and\\nfails with a not-found-verbatim error when a literal moves. The publish\\npipeline is coupled to exact source text inside the viewer. Nobody sees\\nthat coupling until a publish fails, so the file resists refactoring by\\npunishing it late.\\n\\nTwo regressions this month show the cost. A `[hidden]` attribute was\\ndefeated by a `display:flex` rule 100 lines away, and the caption it was\\nmeant to hide stayed visible in four of five modes. Separately, the\\n`#adr-sheet` and `#assessment-sheet` markup blocks were deleted while\\ntheir CSS and every JavaScript reference survived, so `renderSheetVisibility`\\nthrew on every call. Neither defect is exotic. Both are what a 4917-line\\nfile with no seams produces.\\n\\n## Options considered\\n\\n1. **Remove the duplication in place, and keep one hand-edited file.**\\n   A template helper and a mode registry would cut real repetition, and\\n   the convention would survive untouched. Rejected because it delivers\\n   half the ask. The file stays near 4000 lines, a reader still holds\\n   five interleaved modes in one scroll, and \\\"smaller parts\\\" is not\\n   delivered at all.\\n\\n2. **Split into sibling files loaded by `<script>` and `<link>`.**\\n   Rejected. ADR-0001 already settled this. A published Artifact has no\\n   sibling files.\\n\\n3. **Generate the viewer into the bundle at migrate time, and commit no\\n   output.** The cleanest source tree of the four. Rejected because\\n   `/plugin install` runs no build, so an installed plugin would carry no\\n   viewer. Both `migrate_bundle.py` and `export_artifact.py` read the\\n   plugin's own `viewer/index.html` as a file that already exists.\\n\\n4. **Adopt a bundler.** Rejected. It puts a Node toolchain into a\\n   repository of prose, Python scripts, and one HTML file, to do a job\\n   that is ordered concatenation.\\n\\n5. **Author-time build with a committed output.** Chosen.\\n\\n## Decision\\n\\nThe viewer is authored as ordered parts under\\n`plugins/cobuilder-artifact/viewer/src/`. `build_viewer.py` concatenates\\nthem into `plugins/cobuilder-artifact/viewer/index.html`, which stays\\ncommitted and stays the file that ships.\\n\\n**Build-free is narrowed, not abandoned.** The property that matters is\\nthat no build runs at install and no build runs in the browser. Both hold.\\nWhat ends is build-free authoring, which is the half that has been paying\\nfor itself in defects rather than in simplicity.\\n\\n**The output is guarded, not trusted.** A committed generated file goes\\nstale the moment somebody edits the output. A test rebuilds from the parts\\nand fails when the result differs from the committed file. That test is\\nthe whole reason this arrangement is safe.\\n\\n**The exporter seam becomes declared.** `export_artifact.py` stops\\nmatching incidental literals and matches named markers that the parts\\nemit on purpose. The coupling between the viewer and the publish pipeline\\nbecomes a stated contract rather than an accident of formatting.\\n\\n**Out of scope.** This record decides the packaging of the viewer source\\nand the guard on its output. It does not decide the part boundaries, the\\ntemplate mechanism, or the shape of the mode registry. Those belong to the\\n`maintainable-viewer` design, which is deliberately deferred and sits in\\nthe backlog as four planned epics.\\n\\n## Consequences\\n\\n- **Positive:** A view mode becomes one file of a few hundred lines. A\\n  sixth mode is a new part plus one registry entry.\\n- **Positive:** The publish coupling stops being invisible. A moved marker\\n  fails a test instead of failing a publish.\\n- **Constraint introduced:** `viewer/index.html` is a build artifact\\n  compiled from `viewer/src/`. An engineer edits a part, never the output,\\n  and a test fails when the committed output does not equal a fresh build.\\n- **Negative / accepted:** A generated file is committed, so every viewer\\n  change shows a large diff in the output beside a small diff in a part. A\\n  reviewer must learn to read the part and ignore the output.\\n- **Negative / accepted:** An engineer who edits `index.html` directly\\n  loses that edit at the next build. The guard test converts the loss into\\n  a failure, but it cannot prevent the mistake.\\n- **Negative / accepted:** One more script to maintain, and one more test\\n  that must run before the viewer is trusted.\\n\\n## Value delivered\\n\\n- **New capability:** An engineer changes one view mode by opening one\\n  file of a few hundred lines, instead of locating it inside 3872 lines of\\n  a single function scope.\\n- **Benefit:** The cost of a viewer change stops scaling with the size of\\n  the whole viewer.\\n- **Beneficiary:** developer, validator-agent.\\n\\n## Maps to\\n\\nContext `cobuilder-packaging`, modules\\n`plugins/cobuilder-artifact/viewer/index.html`,\\n`plugins/cobuilder-artifact/scripts/export_artifact.py`,\\n`shared/migrate_bundle.py`. See\\n`docs/architecture/contexts/cobuilder-packaging/boundary.yaml`.\\n\\n## Unverified before implementation\\n\\nThe part boundaries in the backlog are drawn from the file's own 35 banner\\ncomments and have not been tested by an actual split. A section that reads\\nas self-contained may still close over state defined elsewhere in the\\nIIFE, because everything currently shares one scope. The first epic exists\\nto find that out.\\n\\nNo measurement yet supports the claim that ordered concatenation is\\nsufficient. If any part must be reordered at build time rather than\\nconcatenated in filename order, this record needs revisiting.\\n\", \"maps_to\": {\"context\": \"cobuilder-packaging\", \"modules\": [\"plugins/cobuilder-artifact/viewer/index.html\", \"plugins/cobuilder-artifact/scripts/export_artifact.py\", \"shared/migrate_bundle.py\"], \"rule\": \"viewer/index.html is a build artifact compiled from viewer/src/. An engineer edits a part, never the output, and a test fails when the committed output does not equal a fresh build.\"}, \"history\": [{\"state\": \"tentative\", \"date\": \"2026-08-24\"}, {\"state\": \"decided\", \"date\": \"2026-08-24\"}], \"related\": [\"docs/architecture/contexts/cobuilder-packaging/boundary.yaml\", \"docs/architecture/designs/maintainable-viewer/goal.json\"]}, \"ADR-0021\": {\"id\": \"ADR-0021\", \"title\": \"book-index.md gains a nano tier, and full-book loading stops being unconditional\", \"state\": \"decided\", \"source_pr\": null, \"problem\": \"book-index.md escalates straight from the Tier 1 corpus heuristic to one full vendored book (300-1000 lines). Upstream ciembor/agent-rules-books has since published nano (~20-40 line) and mini (~80-150 line) tiers for the same 14 books, and books/README.md's manifest still only vendors full. Nothing in this plugin uses the smaller tiers.\", \"decision\": \"Vendor nano.md and mini.md alongside the existing full.md for each of the 14 books. book-index.md's escalation rule changes: after Tier 1 narrows the candidate set, load a minimum of three nano-tier excerpts, then escalate any one of those books to mini or full only when its principles are judged to matter for the task at hand. Full-tier loading is never unconditional. The existing 'one primary + one optional companion' cap is rewritten to state this new ceiling explicitly. This applies to Design mode's Stage 1 (Ground) and Stage 3 (Explore) grounding, and to Review/Maintenance mode's corpus-to-book escalation. Addendum (2026-08-26): Review/Maintenance's mandatory 14-file security corpus load gets the same cheap-signal-before-expensive-read shape, but with the opposite default bias and no vendoring — read each security corpus file's first ~30 lines (id/name/tags/summary, already structurally separate from its worked examples) unconditionally for all 14, never skipping a category's summary, then read the rest in full unless the summary clearly shows the category has no applicable surface area in the codebase under review. Ambiguous cases default to reading the full file, not to skipping it.\", \"alternatives\": [{\"option\": \"Ship the engineer's original approach unmodified: load 3 nanos, then always load the full book, unconditionally\", \"rejected_because\": \"Making the full-book read unconditional defeats the point of a cheap tier — nano/mini exist to let a session skip the expensive read, not precede it. It also leaves the existing 1-primary+1-companion cap silently violated with no stated replacement ceiling.\"}, {\"option\": \"Signed evidence ledger: an append-only book-loads.jsonl entry, hash-pinned, gating the full-book read behind three committed nano entries\", \"rejected_because\": \"Solves auditability, a problem nobody named. Adds a new shim and a new file surface on top of the 42-file vendoring cost already at issue, without reducing token cost or file count.\"}, {\"option\": \"Content-addressed cold storage: ship only a book-id-to-hash index, materialize nano/mini/full into a per-session scratch path on first read, evict by LRU\", \"rejected_because\": \"Sized for a corpus of 140+ books; this repo vendors 14. Conflicts with the plugin family's vendor-in-tree convention — no plugin ships infrastructure beyond the files it carries itself (ADR-0017).\"}, {\"option\": \"Live-fetch the nano tier from upstream raw.githubusercontent.com URLs at task time instead of vendoring it\", \"rejected_because\": \"The divergent-exploration frame that proposed this was blocked by a security classifier. Fetching remote content into prompt context at runtime is an injection and supply-chain risk this plugin family's self-only, no-network-dependency design deliberately avoids.\"}, {\"option\": \"Pre-concatenated per-book ladder file: nano, then mini, then a <!-- FULL:path --> marker, in one file with a shared stop-early instruction\", \"rejected_because\": \"Collapses the vendored surface back to 14 files and scored highest with the critic, but its 'stop reading early' discipline has no mechanical consumer. This repo's own CLAUDE.md names exactly this failure mode from the Gate 4b history: a documented process step with no mechanical consumer gets skipped, however well it is documented. Rejected in favor of keeping separate tier files, at the cost of accepting the same unenforced-step risk in the chosen design instead.\"}, {\"option\": \"Ad hoc subagent fan-out: spawn one short-lived Task-tool call per candidate book, each reads that book's full text and returns only a verdict, keeping full text out of the orchestrating context entirely\", \"rejected_because\": \"Scored well on isolating context growth, but ADR-0015's rule is that this plugin ships skills and no agents, hooks, or MCP servers. The pattern could only ever be an ad hoc in-turn Task-tool call, never installed infrastructure, and was declined even in that ad hoc form to keep this design's scope to the vendored-file mechanism.\"}, {\"option\": \"Panic-grep pre-filter: one hand-written 'panic:' frontmatter line per nano file, grepped across all 14 before any tier loads\", \"rejected_because\": \"The critic's starred non-obvious survivor — cheap, and the only candidate that stays fully inside the existing 1+1 cap. Deferred rather than bundled into this design; it does not conflict with the chosen mechanism and can be proposed separately if the 3-nano step proves too slow in practice.\"}], \"forces\": [\"All loading happens through an LLM agent's own Read tool calls, directly into the same context window the rest of the task uses. There is no retrieval index or RAG layer today.\", \"book-index.md already caps loading at one primary book plus one optional companion, and the stated 3-nano-plus-full approach exceeds that cap as written.\", \"The plugin ships no agents, hooks, or MCP servers (ADR-0015), which rules out shipping the subagent-fan-out alternative as installed infrastructure.\", \"books/README.md documents a scripts/sync-books.sh drift-check script that does not exist anywhere in the repo — the vendoring pipeline was already incomplete before this decision, though fixing it is out of scope here.\", \"CLAUDE.md's own Gate 4b history: a process step with no mechanical consumer gets skipped, however well it is documented.\"], \"delivers\": {\"capability\": \"A design or review session can ground itself against three books' worth of signal for close to the token cost of one, and escalate to deeper tiers only for the book that actually turns out to matter.\", \"benefit\": \"The vendored nano/mini tiers upstream already publishes stop sitting unused, and the escalation ladder starts trading cost for depth instead of jumping straight to the most expensive read available.\", \"beneficiary\": [\"developer\", \"validator-agent\"]}, \"body\": \"# ADR-0021 — book-index.md gains a nano tier, and full-book loading stops being unconditional\\n\\n## Context\\n\\n`book-index.md` is Tier 2 of a two-tier escalation: a cheap corpus heuristic\\n(Tier 1) picks a symptom, and Tier 2 loads one full vendored book, a\\n300-1000 line markdown file distilled from a canonical software-engineering\\nbook. `books/README.md` vendors these full-length files from\\n`ciembor/agent-rules-books`, at a pinned commit that predates that upstream\\nrepo's own addition of `nano` (about 20-40 lines) and `mini` (about\\n80-150 lines) tiers per book. Fourteen books are vendored today. None of\\nthem have a nano or mini counterpart in this repo, and nothing in\\n`book-index.md`'s escalation rule accounts for them.\\n\\nSeparately, `books/README.md` documents `scripts/sync-books.sh` as the way\\nto check for upstream drift. That script does not exist anywhere in this\\nrepository. The vendoring pipeline was already incomplete before this\\ndecision. Fixing it is explicitly out of scope here.\\n\\n## Options considered\\n\\n1. **3 nanos, then always the full book, unconditionally (the engineer's\\n   original approach).** Rejected. Making the full-book read unconditional\\n   defeats the reason a cheap tier exists — nano and mini are supposed to\\n   let a session skip the expensive read when the topic turns out not to\\n   matter, not run ahead of it every time. It also silently breaks the\\n   existing \\\"one primary plus one optional companion\\\" cap with no stated\\n   replacement.\\n2. **Signed evidence ledger.** Rejected. Solves an auditability problem\\n   nobody raised, and adds its own file and shim surface on top of the\\n   vendoring cost already under discussion.\\n3. **Content-addressed cold storage.** Rejected. Solves for a corpus two\\n   orders of magnitude larger than the 14 books this repo actually vendors,\\n   and conflicts with the vendor-in-tree convention ADR-0017 already\\n   established for shared code.\\n4. **Live-fetch nano tier from upstream at task time.** Rejected outright —\\n   the divergent-exploration branch that proposed it was blocked by a\\n   security classifier. Fetching remote content into prompt context at\\n   runtime is exactly the risk this plugin family's self-only design avoids.\\n5. **Pre-concatenated per-book ladder file.** Scored highest with the\\n   critic pass — it collapses the vendored surface back to 14 files and\\n   makes the full-book section physically follow the mini section in the\\n   same document. Rejected anyway, because its \\\"stop reading before the\\n   FULL marker\\\" discipline is enforced by nothing but instructional prose,\\n   and this repo's own CLAUDE.md names that exact failure mode from the\\n   Gate 4b history.\\n6. **Ad hoc subagent fan-out.** Rejected as out of scope. ADR-0015 fixes\\n   the install surface at no agents, hooks, or MCP servers. The pattern\\n   could only ever run as an in-turn `Agent` tool call, never as shipped\\n   infrastructure, and the engineer declined to add even that much for\\n   this design.\\n7. **Panic-grep pre-filter (chosen: deferred, not rejected on merit).** The\\n   critic's starred non-obvious survivor. Cheap, and the only candidate\\n   that stays inside the existing cap unmodified. Left out of this design's\\n   scope rather than bundled in, because it is orthogonal to the chosen\\n   mechanism and can be proposed on its own later.\\n8. **3 nanos, then judgment-gated escalation to mini or full (chosen).**\\n   See Decision.\\n\\n## Decision\\n\\nVendor `nano.md` and `mini.md` alongside the existing `full.md` for each of\\nthe 14 books already listed in `books/README.md`'s manifest — 42 files\\ntotal, in the same `references/books/` layout used today. Re-vendoring the\\nexisting `full/` files against upstream's current commit, and fixing\\n`scripts/sync-books.sh`, are both explicitly out of scope for this design.\\n\\nChange `book-index.md`'s escalation rule: once Tier 1's corpus heuristic\\nnarrows the candidate books for a task, a design or review-mode session\\nloads a minimum of three `nano` excerpts. It may then escalate any one of\\nthose three to `mini` or `full` only when that book's principles are\\njudged to matter for the specific problem at hand. A book that does not\\nclear that judgment stays at the nano tier for the rest of the task.\\nFull-tier loading is never automatic.\\n\\nThis changes book-index.md's own combination rule. The prior \\\"at most one\\nprimary book, plus one optional companion\\\" cap is rewritten to state the\\nnew ceiling directly: a minimum of three nano loads, plus judgment-gated\\nmini or full escalation per book, replacing the old 1+1 count entirely for\\nbook loading specifically.\\n\\nApplies to Design mode's Stage 1 (Ground) and Stage 3 (Explore) grounding,\\nand to Review and Maintenance mode's existing corpus-to-book escalation\\npath. See the Addendum below for Review/Maintenance's separate, mandatory\\n14-file security corpus load, which was originally out of scope for this\\nADR and was folded in on 2026-08-26.\\n\\n## Consequences\\n\\n- **Positive:** The nano and mini tiers upstream already publishes get\\n  used. A session can ground itself against three books' worth of signal\\n  for close to the cost of reading one, and only pays the full-book cost\\n  for the book that turns out to matter.\\n- **Constraint introduced:** A design or review-mode session must load a\\n  minimum of three nano-tier excerpts before it may escalate any one book\\n  to mini or full tier. Full-tier loading is judgment-gated per book,\\n  never unconditional.\\n- **Negative / accepted:** The judgment gate has no mechanical enforcement.\\n  The engineer explicitly chose this over the mechanically-stronger\\n  ladder-file alternative (option 5), accepting the risk that, under time\\n  pressure, a session quietly reverts to reading only `full`, the way this\\n  repo's own Gate 4b history shows an unenforced step tends to erode. The\\n  vendored surface also triples, from 14 files to 42, with the sync\\n  pipeline that would check them against upstream still broken.\\n\\n## Value delivered\\n\\n- **New capability:** A design or review session can ground itself\\n  against three books' worth of signal for close to the token cost of\\n  one, and escalate to deeper tiers only for the book that actually turns\\n  out to matter.\\n- **Benefit:** The vendored nano/mini tiers upstream already publishes\\n  stop sitting unused, and the escalation ladder starts trading cost for\\n  depth instead of jumping straight to the most expensive read available.\\n- **Beneficiary:** developer, validator-agent.\\n\\n## Addendum (2026-08-26) — security corpus applicability read\\n\\n### Context\\n\\nReview and Maintenance mode load all 14 `references/corpus/principles/security/*.yaml`\\nfiles unconditionally, in full, on every run — 2179 lines total, larger\\nthan a single vendored full book. Each file front-loads its checkable\\nsignal (`id`, `name`, `category`, `canonical_tags`, `sources`, `summary`)\\nbefore its worked before/after code examples; across all 14 files, that\\nmetadata-plus-summary block ends between line 17 and line 24. The examples\\nthat follow account for most of each file's length.\\n\\nJudgment-gating whether to read a security category at all — the same\\nmechanism this ADR gives books — was considered and rejected for security\\nspecifically. A missed vulnerability in a report that carries compliance\\nweight (GDPR, SOC2, per this skill's own Impact Taxonomy) is a worse\\nfailure than a design session that under-read one book. Security is\\nalready the highest-weighted category in the scoring rubric (25%). The\\nexisting \\\"load all 14, mandatory, no toggle\\\" rule is correct in its\\n\\\"never silently skip a category\\\" half. It has never been scrutinized on\\nits \\\"always read the full 150-180 lines regardless of relevance\\\" half.\\n\\n### Decision\\n\\nRead the first ~30 lines of all 14 security corpus files unconditionally,\\nevery review or maintenance run — this covers the metadata-plus-summary\\nblock for every file with margin, and costs about 420 lines total instead\\nof 2179. A category's summary is never skipped; this is what preserves the\\nexisting audit-completeness guarantee.\\n\\nFor each file, read the remainder in full — the worked examples — unless\\nthe summary clearly shows the category has no applicable surface area in\\nthe codebase under review (for example, `file_upload_api_hardening.yaml`'s\\nsummary describes upload-endpoint risk, and the codebase has no upload\\nendpoints). **Ambiguous cases default to reading the full file, not to\\nskipping it.** This is the opposite default from book escalation on\\npurpose: a book's cost of being wrong is worse engineering advice, and a\\nsecurity category's cost of being wrong is a missed finding in a\\ncompliance-weighted report.\\n\\nThis is a same-file partial read, not a separate vendored tier. No new\\nfiles are created, and `books/README.md`'s manifest is untouched — this\\naddendum applies only to `references/corpus/principles/security/`, never\\nto `references/books/`.\\n\\n### Consequences\\n\\n- **Positive:** Unconditional cost drops from 2179 lines to about 420 for\\n  the categories that turn out not to apply, while every category's\\n  summary is still read on every run, so the \\\"never silently skip\\\" audit\\n  guarantee is unchanged.\\n- **Constraint introduced:** A review or maintenance session must read the\\n  first ~30 lines of all 14 security corpus files before it may skip any\\n  one file's remainder. Skipping a file's remainder requires the summary\\n  to clearly rule out applicability. Ambiguity defaults to reading in\\n  full.\\n- **Negative / accepted:** Like the book-escalation rule above, this gate\\n  has no mechanical enforcement — nothing stops a session from reading all\\n  14 in full regardless (safe, if wasteful) or, worse, judging a category\\n  inapplicable when it was actually ambiguous (the failure mode the\\n  default-to-full bias exists to make less likely, not impossible).\\n\\n## Maps to\\n\\nDistrict `skills` (unanchored — no formal bounded context exists for this\\narea yet). Modules `plugins/cobuilder-architect/skills/architecture/references/book-index.md`,\\n`.../references/books`, `.../references/design-mode.md`,\\n`.../SKILL.md`, `.../references/corpus/principles/security`. See this\\nADR's `maps_to.rule` for both invariants.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\"plugins/cobuilder-architect/skills/architecture/references/book-index.md\", \"plugins/cobuilder-architect/skills/architecture/references/books\", \"plugins/cobuilder-architect/skills/architecture/references/design-mode.md\", \"plugins/cobuilder-architect/skills/architecture/SKILL.md\", \"plugins/cobuilder-architect/skills/architecture/references/corpus/principles/security\"], \"rule\": \"A design or review-mode session must load a minimum of three nano-tier book excerpts before it may escalate any one book to mini or full tier. Full-tier loading is judgment-gated per book, never unconditional, and the total load per task is bounded by the ceiling this ADR states — not by the prior one-primary-plus-one-companion cap, which this ADR supersedes for book loading. A review or maintenance session must read the first ~30 lines of all 14 security corpus files unconditionally, never skipping a category's summary, and may read a file's remainder in full only when the summary shows the category applies or the applicability is ambiguous — never when judgment alone says to skip.\"}, \"history\": [{\"state\": \"decided\", \"date\": \"2026-08-25\", \"note\": \"Chosen on this branch; not approved until a human merges.\"}, {\"state\": \"decided\", \"date\": \"2026-08-26\", \"note\": \"Addendum: extended to Review/Maintenance's mandatory 14-file security corpus load, using a same-file partial-read gate instead of separate vendored tiers.\"}], \"related\": [\"docs/architecture/designs/book-index-tiering/goal.json\"]}};\n+window.ADRS = {\"ADR-0001\": {\"id\": \"ADR-0001\", \"title\": \"Flatten the bundle viewer into one self-contained file for Claude Artifact publishing\", \"state\": \"approved\", \"source_pr\": 2, \"problem\": \"The bundle viewer (viewer/index.html) depends on sibling <script src> data loading, relative asset/audio paths, and two external CDN requests — none of which survive being published as a Claude Artifact, which is a single file under a CSP that blocks every external request.\", \"decision\": \"export_artifact.py inlines story/manifest/diff/ADR data as literal window globals, rewrites the two relative-asset lookups and the narration-audio src to pull from embedded data-URI maps, and drops both CDN tags — recompressing scene art from PNG to JPEG (1400px/q78, retried at tighter tiers if needed) to fit the 16 MiB cap, since audio's ~33% base64 inflation makes it the tighter constraint of the two.\", \"alternatives\": [{\"option\": \"Publish the raw .odyssey bundle directory as-is\", \"rejected_because\": \"Artifacts are one file with no sibling files and no external requests — the viewer's <script src=\\\"../data/story.js\\\"> tags and relative asset paths simply don't resolve outside a real bundle directory.\"}, {\"option\": \"Keep images as lossless PNG\", \"rejected_because\": \"Source PNGs run ~5MB each at the 2K generation size — three of them alone (~15MB, ~20MB after base64) already exceed the 16 MiB cap before audio is even considered.\"}], \"forces\": [\"Claude Artifacts enforce a strict CSP: no external stylesheets/scripts/fetch, one self-contained file, 16 MiB rendered size cap.\", \"The Motion animation library already degrades gracefully when window.Motion is undefined, so dropping its CDN tag costs only micro-animations, not correctness.\", \"Diffs of HTML files can contain a literal </script> substring, which silently truncates an inlined <script> block unless escaped.\"], \"delivers\": {\"capability\": \"One command turns any generated PR narrative into a shareable, self-contained web page — no server, no bundle directory, no sibling files required.\", \"benefit\": \"PR stories can be shared with anyone with the link, not only teammates with the repo checked out and a local viewer server running.\", \"beneficiary\": [\"developer\", \"operator\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #2. The plugin already had a bundled viewer\\nfor local use (`python3 -m http.server`), but no way to share a single PR's\\nstory outside that setup. Claude Artifacts looked like the natural target,\\nbut the viewer was never built to be self-contained.\\n\\n## Options considered\\n\\n1. **Publish the raw `.odyssey` bundle directory as-is.** Rejected —\\n   Artifacts are one file with no sibling files and no external requests;\\n   the viewer's relative paths and CDN tags don't resolve in that sandbox.\\n2. **Keep images as lossless PNG (chosen: recompress instead).** Rejected —\\n   source PNGs run ~5MB each; three of them alone would blow the budget\\n   before audio is even considered.\\n3. **Inline everything, recompress images, drop CDN deps (chosen).** Verified\\n   by actually publishing a flattened export this session — it rendered\\n   correctly, full nav/audio/ADR sheet all worked.\\n\\n## Decision\\n\\n`export_artifact.py` inlines `window.STORY`/`ODYSSEY`/`DIFFS_BY_PR`/`ADRS`\\nplus `ODYSSEY_ASSETS`/`ODYSSEY_AUDIO` data-URI maps, rewrites the viewer's\\nthree relative-path touch points to read from those maps, and drops the\\nGoogle Fonts + Motion CDN tags.\\n\\n## Consequences\\n\\n- **Positive:** any generated PR can be published as a shareable Artifact\\n  with one command.\\n- **Constraint introduced:** the export is scoped to one PR at a time —\\n  multiple PRs' images+audio together would risk the 16 MiB cap.\\n- **Negative / accepted:** scene art loses some fidelity (PNG → JPEG,\\n  downscaled) in the published copy; the local viewer still shows full\\n  quality.\\n\\n## Value delivered\\n\\n- **New capability:** turn a generated bundle into a link, not just a\\n  locally-served page.\\n- **Benefit:** removes the repo-checkout requirement from sharing a story.\\n- **Beneficiary:** developer, operator\\n\\n## Maps to\\n\\nDistrict `scripts` from `.odyssey/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"scripts\", \"unanchored\": true, \"modules\": [\"scripts\"], \"rule\": \"Artifact export inlines bundle data and drops CDN tags so a PR story is one self-contained file.\"}, \"approved_by\": \"merge of PR #2\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-07-24\", \"by\": \"merge of PR #2\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0002\": {\"id\": \"ADR-0002\", \"title\": \"Track publish provenance with a commit SHA + content hash, not by re-threading git history\", \"state\": \"approved\", \"source_pr\": 2, \"problem\": \"Once a PR artifact is published, re-running /prodyssey:publish needs to know whether to mint a new Artifact call or leave the existing one alone — but story.json never persisted a stable per-PR reference to compare against, and open PRs additionally move underneath an already-published artifact as new commits land.\", \"decision\": \"extract_story.py now writes entry[\\\"commit\\\"] (a value it already computed internally for size/touched and previously discarded); export_artifact.py combines it with a sha256 content hash of the PR's timeline entry, referenced ADRs, and diff into publish-manifest.json, and republishes only when either signal changed, passing the previously recorded artifact_url back to the Artifact tool so a republish updates the same link instead of minting a new one.\", \"alternatives\": [{\"option\": \"Always republish on every /prodyssey:publish invocation\", \"rejected_because\": \"Wastes an Artifact call (and mints a confusing new URL) for a PR that hasn't changed since it was last published.\"}, {\"option\": \"Track staleness by content hash alone, no commit SHA\", \"rejected_because\": \"Persisting the commit SHA turned out to be a ~4 line addition to a function extract_story.py already runs — and it catches a case content-hash alone can't distinguish as clearly: new commits landing on an open PR's branch.\"}], \"forces\": [\"extract_story.py already resolves and discards a merge-commit or branch-head SHA per PR while computing size/touched — persisting it cost nothing new to compute.\", \"Open-PR entries are explicitly not immutable (story-mode.md, generate mode notes) — narration and diff both refresh as new commits land, so publish-time staleness has to track the same thing.\", \"The Artifact tool mints a new URL unless the same file path is republished in-session or an existing url is passed explicitly — losing track of a PR's artifact_url means every republish orphans the last one.\"], \"delivers\": {\"capability\": \"Re-running /prodyssey:publish is safe and idempotent — unchanged PRs report \\\"already up to date\\\" instead of spamming new Artifact links, and the index artifact always reflects the full, current set.\", \"benefit\": \"Publishing becomes a pipeline you can re-run freely rather than a one-shot action you have to remember not to repeat.\", \"beneficiary\": [\"developer\", \"operator\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #2. Once the artifact-export mechanism\\n(ADR-0001) existed, the next question was what happens on a second\\n`/prodyssey:publish` run — especially for an open PR, whose diff and\\nnarrative are explicitly not settled history.\\n\\n## Options considered\\n\\n1. **Always republish.** Rejected — wasteful, and risks orphaning the\\n   previous Artifact URL if it isn't reused.\\n2. **Content hash only.** Rejected — works, but investigation showed the\\n   commit SHA was already computed and thrown away one function over, so\\n   adding it was nearly free and catches branch-moved-but-text-unchanged\\n   cases more directly.\\n3. **Commit SHA + content hash together (chosen).**\\n\\n## Decision\\n\\n`extract_story.py` persists `entry[\\\"commit\\\"]`. `export_artifact.py` computes\\na sha256 `source_hash` over the PR's timeline entry + referenced ADRs + diff,\\nand records both plus the export's file/byte metadata into\\n`exports/publish-manifest.json`. `record_publish.py` writes the Artifact\\ntool's returned URL back into the same file once Claude has it.\\n\\n## Consequences\\n\\n- **Positive:** idempotent republishing; the index artifact can always\\n  rebuild from a single source of truth instead of guessing what's changed.\\n- **Constraint introduced:** `exports/publish-manifest.json` is now load-\\n  bearing state, not disposable build output — it's tracked in git\\n  alongside `data/`/`assets/`, not gitignored.\\n- **Negative / accepted:** a PR generated before this change has no\\n  `commit` field until it's regenerated — handled as \\\"unknown,\\\" not an\\n  error.\\n\\n## Value delivered\\n\\n- **New capability:** safe, repeatable publishing with no manual bookkeeping\\n  of what's already live.\\n- **Benefit:** removes the operational burden of tracking Artifact URLs by\\n  hand across sessions.\\n- **Beneficiary:** developer, operator\\n\\n## Maps to\\n\\nDistrict `scripts` from `.odyssey/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"scripts\", \"unanchored\": true, \"modules\": [\"scripts\"], \"rule\": \"Republish a PR artifact only when its commit SHA or content hash has changed.\"}, \"approved_by\": \"merge of PR #2\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-07-24\", \"by\": \"merge of PR #2\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0003\": {\"id\": \"ADR-0003\", \"title\": \"Unify bundle storage under .prodyssey/, self-analysis into .prodyssey/self/\", \"state\": \"approved\", \"source_pr\": 3, \"problem\": \"Two separate storage roots existed for the same kind of output: self-analysis bundles lived at `<target>/.odyssey/`, while foreign-repo bundles were cached centrally at `<hub>/.prodyssey/<repo-slug>/`. Every script's `--bundle-dir` default, every skill reference, and every command had to carry two path conventions, and there was no single root to write one `.gitignore` line against for hub-local bookkeeping (`.view-server.pid`, `.view-server.log`, the `active` symlink).\", \"decision\": \"Collapse both roots into one: self-analysis bundles now default to `<target>/.prodyssey/self/`, a sibling of the existing `<hub>/.prodyssey/<repo-slug>/` cache directories, with `self` reserved as a slug no repo-derived hash can ever collide with. Every script's `--bundle-dir` default, the skill's Hub-resolution rule, and the `.gitignore` guidance were rewritten around this single root — and a legacy-layout detector stops any mode with the exact `git mv .odyssey .prodyssey/self` command instead of silently treating the old path as \\\"no baseline found\\\".\", \"alternatives\": [{\"option\": \"Keep two storage roots, just document the split more clearly\", \"rejected_because\": \"Leaves the actual maintenance cost in place — ~10 places across scripts, SKILL.md, README, and CLAUDE.md still hardcode two different default paths — and still gives no single root for one shared .gitignore rule.\"}, {\"option\": \"Auto-migrate a legacy `.odyssey/` bundle on next run (run `git mv` for the user)\", \"rejected_because\": \"Contradicts the skill's standing posture of never editing the target repo's source outside the bundle directory itself; a legacy layout gets a printed command for the user to run, not an automatic move on their behalf.\"}], \"forces\": [\"Every one of the plugin's scripts (plus SKILL.md, README.md, CLAUDE.md) hardcoded `.odyssey` as the self-analysis default, duplicating the same path assumption in roughly ten places.\", \"A legacy `.odyssey/` bundle must never be silently reinterpreted as \\\"no baseline exists\\\" and regenerated — that would burn real Gemini API cost re-deriving content a maintainer already authored by hand.\", \"The `.prodyssey/` bookkeeping entries (`.view-server.pid`, `.view-server.log`, the `active` symlink) need a `.gitignore` line that ignores only those three files, never `.prodyssey/` as a whole, since real bundle directories sit right next to them and are meant to be committed.\"], \"delivers\": {\"capability\": \"One storage root for every bundle, self-analysis or foreign-repo, with one `.gitignore` rule and one path convention across the whole plugin instead of two.\", \"benefit\": \"Removes a whole class of \\\"which root is this bundle actually in\\\" bugs and doc-drift before they can occur, and gives centrally-cached and self-analysis bundles the same on-disk shape.\", \"beneficiary\": [\"developer\", \"the plugin itself (maintainers and future sessions working in this repo)\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #3. The plugin had grown two separate bundle-storage roots as the central-cache feature (`<hub>/.prodyssey/<repo-slug>/`) was added alongside the original self-analysis location (`<target>/.odyssey/`), and the split had started costing real maintenance overhead across scripts, skill references, and commands.\\n\\n## Options considered\\n\\n1. **Document the split better, keep two roots.** Rejected — doesn't remove the duplicated path-default maintenance burden or unlock a single `.gitignore` line.\\n2. **Auto-migrate legacy bundles via `git mv` on next run.** Rejected — an automatic move inside the user's repo contradicts the skill's \\\"never edits target repo source\\\" rule; detect-and-instruct instead.\\n3. **Unify under `<target>/.prodyssey/self/` (chosen).**\\n\\n## Decision\\n\\nEvery script's `--bundle-dir` default becomes `<repo>/.prodyssey/self`. The skill's Hub-resolution rule is rewritten around one root with `self` as a reserved slug. A legacy-layout check runs before any mode's other work: if the new path is missing but `<target>/.odyssey/` exists, the mode stops and prints the exact `git mv` command rather than proceeding.\\n\\n## Consequences\\n\\n- **Positive:** one path convention everywhere; central and self-analysis bundles are now structurally identical, just at different subpaths of the same root.\\n- **Constraint introduced:** `self` can never be used as a foreign-repo slug — the slug derivation must guarantee this.\\n- **Negative / accepted:** any bundle generated before this PR needs a one-time manual `git mv`, not handled automatically.\\n\\n## Value delivered\\n\\n- **New capability:** a single, consistent bundle-storage root for the whole plugin.\\n- **Benefit:** removes duplicated path-convention maintenance and enables one shared `.gitignore` rule.\\n- **Beneficiary:** developer, the plugin itself\\n\\n## Maps to\\n\\nDistrict `.prodyssey` from `.prodyssey/self/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \".prodyssey\", \"unanchored\": true, \"modules\": [\".prodyssey\"], \"rule\": \"Self-analysis and foreign-repo bundles share one root, with self reserved as a slug.\"}, \"approved_by\": \"merge of PR #3\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-07-28\", \"by\": \"merge of PR #3\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0004\": {\"id\": \"ADR-0004\", \"title\": \"Keep the ste-writing skill dev-local, out of the plugin's install surface\", \"state\": \"approved\", \"source_pr\": 3, \"problem\": \"The repo needed a controlled-language (ASD-STE100) writing skill and linter to keep its own docs and PR narratives in a consistent register, but the plugin's `skills/` directory is exactly what ships to anyone who runs `/plugin install prodyssey@prodyssey` — adding a general-purpose writing-style skill there would silently grow every installer's surface with a skill that has nothing to do with generating codebase odysseys.\", \"decision\": \"`ste-writing` lives under `.claude/skills/ste-writing/`, a location outside the plugin manifest's `skills/` tree entirely, so an install of `prodyssey@prodyssey` still gets exactly the two skills the plugin has always shipped (odyssey, and mermaid one PR later) and never a third.\", \"alternatives\": [{\"option\": \"Ship it under skills/ste-writing/ alongside odyssey\", \"rejected_because\": \"Every installer of the plugin would receive a general writing-style skill unrelated to codebase odysseys, contradicting the plugin's already-explicit \\\"no agents, no hooks, no MCP servers\\\" minimal-install-surface stance.\"}], \"forces\": [\"The plugin's minimal-surface intent is an explicit, pre-existing design constraint (no agents/hooks/MCP servers, deliberately so it never touches another session's permission surface), and `skills/` is auto-discovered by the manifest with no allowlist to exclude an individual skill.\", \"The repo's own writing standard (ASD-STE100 for README, CLAUDE.md, reference docs, commit/PR bodies) still needs an available skill and linter for sessions working inside this repo, independent of what the plugin ships downstream.\"], \"delivers\": {\"capability\": \"This repo can enforce a documentation writing standard on itself without changing what any plugin installer receives.\", \"benefit\": \"Keeps two separate concerns — how prodyssey ships, versus how this repo writes its own docs — from leaking into each other.\", \"beneficiary\": [\"developer\", \"plugin installers (install surface stays unaffected)\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #3. The repo adopted an ASD-STE100 writing standard for its own prose (README, CLAUDE.md, reference docs, commit/PR bodies) and needed a skill plus linter to enforce it, at the same time the plugin's `skills/` directory already had an established minimal-surface rule.\\n\\n## Options considered\\n\\n1. **Ship under `skills/ste-writing/`.** Rejected — grows every installer's surface with an unrelated skill.\\n2. **Place under `.claude/skills/ste-writing/`, dev-local only (chosen).**\\n\\n## Decision\\n\\n`ste-writing` and its linter (`.claude/skills/ste-writing/ste-lint.py`) live outside the plugin manifest's discovered tree. CLAUDE.md documents explicitly that an install of `prodyssey@prodyssey` gets exactly the skills under `skills/` in this repo, never `.claude/skills/`.\\n\\n## Consequences\\n\\n- **Positive:** plugin install surface stays exactly as minimal as before this PR.\\n- **Constraint introduced:** any future repo-local-only tooling should default to `.claude/skills/`, not `skills/`, unless it's meant to ship.\\n- **Negative / accepted:** ste-writing isn't available to other repos that install the prodyssey plugin — it's specific to this repo's own documentation practice.\\n\\n## Value delivered\\n\\n- **New capability:** a self-enforced writing standard for this repo's own docs.\\n- **Benefit:** keeps shipping surface and internal tooling decisions independent.\\n- **Beneficiary:** developer, plugin installers\\n\\n## Maps to\\n\\nDistrict `skills` from `.prodyssey/self/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\".claude/skills/ste-writing\"], \"rule\": \"Repo-local writing tools live under .claude/skills/, never under the plugin skills/ tree.\"}, \"approved_by\": \"merge of PR #3\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-07-28\", \"by\": \"merge of PR #3\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0005\": {\"id\": \"ADR-0005\", \"title\": \"Diagram authoring delegated to a per-PR subagent, never the orchestrating Claude or a script\", \"state\": \"approved\", \"source_pr\": 4, \"problem\": \"Adding Mermaid diagrams as a visual family alongside Gemini scene art meant deciding who writes the actual `.mmd` diagram sources. The plugin already held a hard rule that scripts never author judgment-shaped content (narrative, ADRs) — only move data — and diagram content (what to show, how to group it, what level of detail) is exactly that kind of judgment call. But authoring three diagrams per PR directly in the orchestrating Claude's own context has a discoverability cost too: the Mermaid authoring rules live in a reference set of over fifty files, one per diagram type, that only makes sense loaded on demand, not kept resident in the orchestrator's context for every PR whether or not it needs diagrams.\", \"decision\": \"Generate mode spawns one subagent per PR for diagram authoring. Its prompt requires it to invoke `Skill(\\\"prodyssey:mermaid\\\")` first (falling back to reading `skills/mermaid/SKILL.md` directly if the skill doesn't resolve by name in that session), hands it the PR's timeline entry, extracted diff, and inventory as grounding, and requires it to write exactly three files — `pr{N}-level1.mmd` (`C4Container`), `pr{N}-level2.mmd` (`sequenceDiagram`), `pr{N}-level3.mmd` (`classDiagram`) — returning only their paths. `build_diagrams.py` then compiles those files into `data/diagrams.js` and validates each one's required diagram type and bracket balance; a validation failure is sent back to the same subagent to fix, never hand-patched by the orchestrator.\", \"alternatives\": [{\"option\": \"Have the orchestrating Claude author the .mmd files itself, inline\", \"rejected_because\": \"Keeps the entire Mermaid reference set (block, C4, sequence, class, and dozens more diagram types) loaded against every PR's context regardless of whether that PR needs diagrams at all, and duplicates authoring-rule maintenance between the odyssey skill and the diagram content.\"}, {\"option\": \"Have build_diagrams.py generate diagrams programmatically from the diff\", \"rejected_because\": \"Breaks the plugin's standing invariant that scripts only move data — deciding what a diagram should show is the same kind of judgment call already reserved for Claude in narrative and ADR authoring, not something a deterministic script can produce.\"}], \"forces\": [\"The plugin's existing convention, already true for narrative and ADRs: a mechanical script only compiles and validates content a subagent already wrote — it never authors that content itself.\", \"A new `--art both|diagram|image` flag controls which visual family a sweep produces per PR; `--art diagram` skips Gemini entirely, and since a diagram is plain text rather than base64 image data, it meaningfully relieves the 16 MiB Artifact-publish budget from ADR-0001.\", \"verify_bundle.py must report `diagram.level-*` status regardless of which family actually gated a given sweep, so a later `--art both` re-run or a publish-mode budget check can see the true state of both families, not just whichever one last ran.\"], \"delivers\": {\"capability\": \"PRs can get architecture diagrams at zero Gemini cost, authored with full access to a large, specialized Mermaid ruleset, without that ruleset bloating every other PR's authoring context.\", \"benefit\": \"Diagrams and scene art become independently selectable per sweep, and diagram-only PRs get meaningfully easier to fit under the artifact-publish size cap.\", \"beneficiary\": [\"developer\", \"operator (whoever runs /prodyssey:publish under the 16 MiB cap)\"]}, \"body\": \"## Context\\n\\nRetroactively extracted from PR #4. Mermaid diagrams were added as a second visual family for levels 1-3, alongside the existing Gemini scene art from earlier PRs, and needed an authoring path consistent with the plugin's judgment-vs-mechanical split already established for narrative and ADRs.\\n\\n## Options considered\\n\\n1. **Orchestrator authors .mmd inline.** Rejected — forces the full Mermaid reference set into every PR's context and duplicates authoring-rule ownership.\\n2. **Script-generates diagrams from the diff.** Rejected — violates the scripts-never-author-judgment-content rule.\\n3. **Per-PR subagent invoking the mermaid skill (chosen).**\\n\\n## Decision\\n\\nOne subagent per PR, told to invoke `Skill(\\\"prodyssey:mermaid\\\")` and write exactly three typed `.mmd` files; `build_diagrams.py` compiles and validates them mechanically, sending failures back to the same subagent rather than patching them directly.\\n\\n## Consequences\\n\\n- **Positive:** diagram authoring inherits the same judgment/mechanical split already proven for narrative and ADRs; Gemini cost and the 16 MiB artifact budget both become easier to manage for diagram-only sweeps.\\n- **Constraint introduced:** the orchestrating Claude must never write or hand-patch a `.mmd` file directly — all fixes route back through the subagent.\\n- **Negative / accepted:** one extra subagent spawn per PR when diagrams are requested, versus authoring inline.\\n\\n## Value delivered\\n\\n- **New capability:** independent, zero-Gemini-cost architecture diagrams per PR.\\n- **Benefit:** smaller authoring context per sweep and an easier artifact-publish budget.\\n- **Beneficiary:** developer, operator\\n\\n## Maps to\\n\\nDistrict `skills` from `.prodyssey/self/inventory.yaml`.\\n\", \"maps_to\": {\"district\": \"skills\", \"unanchored\": true, \"modules\": [\"skills/odyssey\"], \"rule\": \"The orchestrating Claude never writes or hand-patches a .mmd file. A per-PR subagent authors them.\"}, \"approved_by\": \"merge of PR #4\", \"history\": [{\"state\": \"decided\", \"date\": \"unrecorded\", \"source\": \".cobuilder-architect/self/data/adrs.json\", \"note\": \"Retro-extracted from the self-bundle.\"}, {\"state\": \"approved\", \"date\": \"2026-08-02\", \"by\": \"merge of PR #4\", \"note\": \"Approved by the merge that shipped the decision.\"}], \"provenance\": \"inferred\"}, \"ADR-0006\": {\"id\": \"ADR-0006\", \"title\": \"Three-phase, guard-checked bundle self-migration instead of one version-gated ladder\", \"state\": \"approved\", \"source_pr\": 4, \"problem\": \"A bundle drifts from what the plugin currently produces in three genuinely different ways — a derived file going stale (`viewer/index.html`, nothing authored to preserve), the directory layout changing (new files or directories), and `story.json`'s data shape changing — and a single migration mechanism gated on one version number can't handle all three at once without either under-firing (an unconditional-refresh case wrongly skipped because a version check said \\\"already current\\\") or over-firing (an in-place JSON transform touching hand-authored narrative and paid TTS/art content it has no business touching).\", \"decision\": \"`migrate_bundle.py` runs three independent phases in order, against every mode's bundle-dir, before anything else reads it: an unconditional viewer refresh with no version check at all, then an ordered `LAYOUT_MIGRATIONS` ladder keyed on `bundle.json`'s `bundle_format` integer, then an ordered `SCHEMA_MIGRATIONS` ladder keyed on `story.json`'s own `meta.schema_version` — never `bundle.json`'s mirror of it, which could run ahead and deadlock the ladder. Each schema migration declares a `touches` set of dotted field paths; the whole ladder runs once in memory over a deepcopy, and any authored field outside `touches` that changed anywhere in the run stops the write entirely — nothing reaches disk, so there is no restore step, only \\\"no write happened.\\\"\", \"alternatives\": [{\"option\": \"Gate the viewer refresh on a version check, same as the other two phases\", \"rejected_because\": \"This is the exact bug that motivated the whole mechanism — a bundle's viewer had gone stale and silently dropped diagram support because nothing forced a refresh after the viewer changed without an accompanying version bump.\"}, {\"option\": \"Skip the authored-field guard; trust each migrat\n… [truncated by pr: diff exceeds 200KB on very few lines]",
  ".cobuilder-architect/self/data/adrs.json": "diff --git a/.cobuilder-architect/self/data/adrs.json b/.cobuilder-architect/self/data/adrs.json\nindex 15ef44b..e8efd41 100644\n--- a/.cobuilder-architect/self/data/adrs.json\n+++ b/.cobuilder-architect/self/data/adrs.json\n@@ -1327,5 +1327,62 @@\n     \"related\": [\n       \"docs/architecture/designs/book-index-tiering/goal.json\"\n     ]\n+  },\n+  \"ADR-0022\": {\n+    \"id\": \"ADR-0022\",\n+    \"title\": \"Gate documents projected into the record index\",\n+    \"state\": \"decided\",\n+    \"source_pr\": null,\n+    \"problem\": \"Gate 3 program-design docs and Gate 4b epic technical-design docs live under docs/plans/<slug>/, but the viewer's index (ADR-0018) tracks only adr, design, epic, slice, and pull_request entities. A reviewer watching a build's progress in the viewer sees the Gate Rail's approval state but never the document behind a gate, and the workaround — a one-off Claude Artifact publish — drifts from the source and is undiscoverable from the viewer itself.\",\n+    \"decision\": \"Add two new index entities, program_design and epic_design, projected from docs/plans/<slug>/03-program-design.md and epic-<id>-design.md by shared/build_index.py, and surface them as a clickable state on the Builds view's existing Gate Rail cards and epic cards, rather than as a new viewer tab or a continued ad hoc Artifact-publish workflow.\",\n+    \"alternatives\": [\n+      {\n+        \"option\": \"A new top-level Plans or Gate Docs tab in the viewer\",\n+        \"rejected_because\": \"ADR-0018 decided one lifecycle surface with a derived record index specifically to stop the reading-surface count growing per artifact type. A gate doc is scoped to a build already shown in the Builds view.\"\n+      },\n+      {\n+        \"option\": \"Parse gate docs client-side in the viewer at render time, skipping the index\",\n+        \"rejected_because\": \"The viewer is one committed HTML file with no filesystem access beyond its bundled data/*.js scripts. ADR-0018's record index exists to resolve joins once, not to be bypassed per artifact type.\"\n+      },\n+      {\n+        \"option\": \"Execute ADR-0020's viewer-parts split as part of this change\",\n+        \"rejected_because\": \"ADR-0020 is decided but explicitly not executed yet. Starting that execution as a side effect of this narrower change is scope it does not own.\"\n+      },\n+      {\n+        \"option\": \"Keep publishing gate docs as one-off Claude Artifacts\",\n+        \"rejected_because\": \"This is the workaround that exposed the gap: no rebuild hook ties it to docs/plans/, so it drifts, and it is not discoverable from the viewer itself.\"\n+      }\n+    ],\n+    \"forces\": [\n+      \"The Builds view (renderBuildsMainContent) already renders a Gate Rail keyed on feature_slug and epic cards keyed on epic id — both keys a gate doc can reuse without a new join table.\",\n+      \"shared/build_index.py already has a field-projection pattern (GOAL_FIELDS/EPIC_FIELDS/INTENT_FIELDS/ASSESSMENT_FIELDS plus project_fields) that a new entity type should extend, not replace.\",\n+      \"The viewer ships as one self-contained file today (ADR-0001, narrowed by ADR-0020), so any UI change lands in plugins/artifact/viewer/index.html directly.\"\n+    ],\n+    \"delivers\": {\n+      \"capability\": \"A reviewer reads a build's Gate 3 program design or Gate 4b epic design from inside the odyssey viewer, without leaving it for a one-off Artifact or a raw file.\",\n+      \"benefit\": \"The gate doc a reviewer reads is always the current one, rebuilt with the rest of the index, instead of a snapshot that can silently drift from docs/plans/.\",\n+      \"beneficiary\": [\n+        \"operator\",\n+        \"developer\"\n+      ]\n+    },\n+    \"body\": \"# ADR-0022 — Gate documents projected into the record index\\n\\n## Context\\n\\n`implement`'s build skill produces two authored documents per feature\\nthat the viewer never reads: `docs/plans/<slug>/03-program-design.md`\\n(Gate 3, one per feature) and `docs/plans/<slug>/epic-<id>-design.md`\\n(Gate 4b, one per multi-slice epic). `shared/build_index.py`'s\\n`resolve_feature_gates()` already parses `00-status.md` to show each\\ngate's approval state on the Builds view's Gate Rail, but it has never\\nread the gate document's content. A colleague session approving Gate 3\\nasked to see the program design in the viewer and found nothing indexed\\nfor it — a real gap, not a bug, because the index's five entity types\\n(`adr`, `design`, `epic`, `slice`, `pull_request`) have no sixth for a\\ngate document. The immediate fix was publishing the doc as a standalone\\nClaude Artifact, which reads well once but has no rebuild hook back to\\n`docs/plans/` and is not discoverable from the viewer itself.\\n\\n## Options considered\\n\\n1. **A new top-level viewer tab for plans or gate docs.** Rejected.\\n   ADR-0018 decided one lifecycle surface precisely to stop the reading\\n   surface count growing with every new artifact type. A gate doc has no\\n   independent existence — it is always scoped to one build, which the\\n   Builds view already renders.\\n2. **Client-side parsing in the viewer at render time.** Rejected. The\\n   viewer is one committed HTML file with access only to its bundled\\n   `data/*.js` scripts, never the raw repository tree, so this option is\\n   not mechanically available even before weighing it against ADR-0018.\\n3. **Execute ADR-0020's viewer-parts split now, and add gate docs to the\\n   new structure.** Rejected. ADR-0020 is decided, not executed — CLAUDE.md\\n   is explicit that `viewer/src/` does not exist yet and the viewer is\\n   still one committed file. Bundling that execution into this narrower\\n   change would make this decision own scope it did not ask for.\\n4. **Two new index entities, surfaced on the existing Gate Rail and epic\\n   cards (chosen).** `program_design` keys on `feature_slug`, matching\\n   `joins.feature_gates`. `epic_design` keys on epic id, matching\\n   `entities.epic`. Both follow the existing `project_fields(source,\\n   fields)` pattern. The viewer makes a Gate Rail card and, where an\\n   approved 4b design exists, an epic card, clickable — opening a\\n   read-only sheet modeled on the existing `openAdrSheet`.\\n\\n## Decision\\n\\nAdd `program_design` and `epic_design` as index entities in\\n`shared/build_index.py`, projected from `docs/plans/<slug>/*.md`. Surface\\nthem as a clickable state on `plugins/artifact/viewer/index.html`'s\\nexisting Gate Rail cards and epic cards, in `renderBuildsMainContent`.\\nNo new join table: both entities key on values the Builds view already\\ncarries. Out of scope: a new viewer tab, editing a gate doc from the\\nviewer, and any change to how `00-status.md`'s own approval-state line is\\nparsed or displayed.\\n\\n## Consequences\\n\\n- **Positive:** A gate document becomes readable from the one surface\\n  already built to show a build's progress, kept current by the same\\n  `build_index.py` rebuild every other entity relies on.\\n- **Constraint introduced:** A gate document is indexed and read-only from\\n  the viewer, joined to its build by the key the build's own Gate Rail or\\n  epic card already carries. It never gets a new top-level reading\\n  surface.\\n- **Negative / accepted:** The sheet pattern (`openAdrSheet`) was built for\\n  ADR-length text. A long program-design doc may not read as well in the\\n  same component; this is deferred to implementation-time verification\\n  against a real doc, not resolved here.\\n\\n## Value delivered\\n\\n- **New capability:** A reviewer reads a build's Gate 3 or Gate 4b document\\n  from inside the odyssey viewer.\\n- **Benefit:** The document a reviewer sees is always the current one,\\n  rebuilt with the index, rather than a one-off Artifact snapshot that can\\n  drift from `docs/plans/` with no warning.\\n- **Beneficiary:** operator, developer.\\n\\n## Maps to\\n\\nContext `viewer`, module(s) `shared/build_index.py`,\\n`plugins/artifact/viewer/index.html`. See the boundary record's rule and\\nthe context canvas.\\n\",\n+    \"maps_to\": {\n+      \"context\": \"viewer\",\n+      \"modules\": [\n+        \"shared/build_index.py\",\n+        \"plugins/artifact/viewer/index.html\"\n+      ],\n+      \"rule\": \"A gate document is indexed and read-only from the viewer, joined to its build by the same key (feature_slug or epic id) the build's own Gate Rail or epic card already carries. Never a new top-level reading surface for it.\"\n+    },\n+    \"history\": [\n+      {\n+        \"state\": \"decided\",\n+        \"date\": \"2026-09-01\"\n+      }\n+    ],\n+    \"related\": [\n+      \"docs/architecture/designs/gate-doc-surfacing/goal.json\"\n+    ]\n   }\n }",
  ".cobuilder-architect/self/data/designs.js": "diff --git a/.cobuilder-architect/self/data/designs.js b/.cobuilder-architect/self/data/designs.js\nindex d3135b0..9d0fdac 100644\n--- a/.cobuilder-architect/self/data/designs.js\n+++ b/.cobuilder-architect/self/data/designs.js\n@@ -1 +1 @@\n-window.DESIGNS = {\"book-index-tiering\": {\"goal\": {\"name\": \"book-index-tiering\", \"title\": \"book-index.md gains a nano tier, and full-book loading stops being unconditional\", \"created\": \"2026-08-25\", \"outcome\": \"book-index.md offers nano, mini, and full tiers for all 14 vendored books, with a 3-nano-minimum judgment-gated escalation rule; Review/Maintenance's mandatory 14-file security corpus load gets the same cheap-signal-before-expensive-read shape, opposite default bias, no vendoring.\", \"done_when\": [\"books/README.md's manifest lists nano.md, mini.md, and full.md for each of the 14 books\", \"book-index.md states the 3-nano-minimum, judgment-gated escalation rule, replacing the prior 1-primary-plus-1-companion cap for book loading\", \"design-mode.md's Stage 1/3 grounding section and the architecture skill's Review/Maintenance corpus-to-book escalation both reference the new rule\", \"SKILL.md's Review mode security corpus step reads the first ~30 lines of all 14 security files unconditionally, then reads a file's remainder in full unless its summary clearly rules out applicability, defaulting to full on ambiguity\"], \"abort_if\": [], \"min_work\": {\"derived_from\": \"1 district (skills), 2 colliding ADRs (ADR-0015, ADR-0017), 2 boundary rules (book-index.md's prior 1+1 cap, Review mode's mandatory-14-file security rule)\", \"alternatives_explored\": 7, \"boundary_rules_checked\": true, \"challenge_stage_run\": true}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"slug\": \"book-index-tiering\", \"branch\": \"design/book-index-tiering\", \"pr\": 16, \"state\": \"open\", \"outcome\": \"book-index.md, books/README.md, design-mode.md, and SKILL.md's Review mode security corpus step all reflect ADR-0021's two escalation rules (book tiering, and the security corpus applicability read)\"}], \"stage\": \"review\", \"supersedes\": null, \"adr\": \"ADR-0021\", \"adrs\": [\"ADR-0021\"], \"rounds\": [{\"n\": 1, \"changed\": false, \"feedback_class\": \"approved\"}, {\"n\": 2, \"changed\": true, \"feedback_class\": \"material\", \"note\": \"Folded in the security corpus applicability read after a live discussion of whether the mandatory 14-file security load deserved the same scrutiny as book loading; resolved with an opposite default bias (full-on-ambiguity, not skip-on-ambiguity).\"}]}, \"intent\": {\"problem\": \"book-index.md's escalation goes straight from the Tier 1 corpus heuristic to one full vendored book, 300-1000 lines. Upstream ciembor/agent-rules-books has since published nano (about 20-40 lines) and mini (about 80-150 lines) tiers for the same 14 books, and books/README.md's manifest still only vendors full. Nothing in this plugin uses the smaller tiers. Separately, books/README.md documents a scripts/sync-books.sh drift-check script that does not exist anywhere in the repository.\", \"approach\": \"Vendor nano.md and mini.md alongside the existing full.md for each of the 14 books, as separate files (42 total). Change book-index.md's escalation rule: after Tier 1 narrows candidates, load a minimum of three nano-tier excerpts, then escalate any one of those books to mini or full only when its principles are judged to matter for the task at hand. Full-tier loading is never unconditional. Rewrite the existing 'one primary book plus one optional companion' cap to state this new ceiling explicitly. Applies to Design mode's Stage 1 (Ground) and Stage 3 (Explore) grounding, and to Review/Maintenance mode's corpus-to-book escalation. Addendum: Review/Maintenance's mandatory 14-file security corpus load gets the same cheap-signal-before-expensive-read shape, with no vendoring (the existing YAML files already separate a metadata-plus-summary block, ~17-24 lines, from worked examples). Read the first ~30 lines of all 14 unconditionally, every run, never skipping a category's summary; read a file's remainder in full unless the summary clearly rules out applicability, defaulting to full on ambiguity — the opposite bias from book escalation, because a missed security finding is worse than a wasted read.\", \"alternatives\": [{\"option\": \"3 nanos, then always the full book, unconditionally (the engineer's original approach)\", \"rejected_because\": \"Making the full-book read unconditional defeats the reason a cheap tier exists, and it silently breaks the existing 1+1 cap with no stated replacement.\"}, {\"option\": \"Signed evidence ledger gating the full-book read behind three committed nano entries\", \"rejected_because\": \"Solves an auditability problem nobody named. Adds a new shim and file surface without reducing token cost or file count.\"}, {\"option\": \"Content-addressed cold storage with per-session materialization and LRU eviction\", \"rejected_because\": \"Sized for a corpus of 140+ books; this repo vendors 14. Conflicts with the plugin family's vendor-in-tree convention (ADR-0017).\"}, {\"option\": \"Live-fetch the nano tier from upstream URLs at task time instead of vendoring it\", \"rejected_because\": \"The frame that proposed this was blocked by a security classifier. Fetching remote content into prompt context at runtime is an injection and supply-chain risk this plugin family's self-only design avoids.\"}, {\"option\": \"Pre-concatenated per-book ladder file with a stop-early marker\", \"rejected_because\": \"Scored highest with the critic and collapses vendoring back to 14 files, but its stop-early discipline has no mechanical consumer. This repo's own CLAUDE.md names exactly this failure mode from the Gate 4b history.\"}, {\"option\": \"Ad hoc subagent fan-out, one Task-tool call per book, verdict-only return\", \"rejected_because\": \"ADR-0015 fixes the install surface at no agents, hooks, or MCP servers. The pattern could only ever run as an in-turn call, never shipped infrastructure, and was declined even in that form to keep this design's scope to the vendored-file mechanism.\"}, {\"option\": \"Panic-grep pre-filter: one hand-written panic line per nano file, grepped before any tier loads\", \"rejected_because\": \"The critic's starred non-obvious survivor, and the only option that stays inside the existing cap unmodified. Deferred rather than bundled in, since it is orthogonal to the chosen mechanism.\"}, {\"option\": \"Judgment-gate the security corpus the same symmetric way as book escalation (skip-on-ambiguity)\", \"rejected_because\": \"A missed security finding in a compliance-weighted report is a worse failure than a design session under-reading one book. Security is already the highest-weighted category in the scoring rubric. The chosen rule keeps a mandatory summary read for every category and defaults to full on ambiguity instead.\"}], \"authorship\": \"agent-generated\", \"captured\": \"2026-08-25\", \"design_name\": \"book-index-tiering\", \"out_of_scope\": [\"Fixing or creating scripts/sync-books.sh\", \"Re-vendoring the existing full/ books against upstream's current commit\", \"The panic-grep pre-filter and the subagent fan-out pattern, both declined for this design\", \"Vendoring separate nano/mini files for the security corpus — it uses a same-file partial read instead, since each YAML already separates metadata/summary from examples\"], \"reviewer_focus\": [\"Whether the judgment-gated escalation language in book-index.md is specific enough to actually change agent behavior, or vague enough to be silently ignored\", \"Whether the rewritten cap conflicts with any other reference file that still assumes the old 1+1 cap\", \"Whether the security corpus's opposite default bias (full-on-ambiguity) is stated clearly enough that a session doesn't quietly collapse it into the same skip-on-ambiguity behavior as book escalation\"], \"risks\": [\"The judgment gate ('escalate to mini/full only when it matters') has no mechanical enforcement. This repo's own Gate 4b history shows an unenforced step tends to erode under time pressure.\", \"Vendoring surface triples from 14 files to 42, while the sync pipeline that would check them against upstream (scripts/sync-books.sh) is already missing.\", \"The security corpus applicability read also has no mechanical enforcement. Its default-to-full bias on ambiguity is meant to make a false 'not applicable' verdict less likely, not impossible.\"], \"source\": \"design\", \"testing\": \"No code changes; this design writes docs/ artifacts only. Verification is: book-index.md's escalation rule reads unambiguously, and the new cap language explicitly supersedes the old 1+1 cap rather than leaving both stated at once.\", \"unknowns\": [\"Whether upstream's nano/mini files exist for all 14 currently-vendored books at a syncable commit — not verified in this design\"], \"why_now\": \"The escalation ladder is missing two rungs that upstream already provides for free. Every task that reaches Tier 2 today pays the full-book cost with no cheaper option available first.\"}, \"assessment\": {\"verdict\": \"concerns\", \"findings\": [{\"kind\": \"prediction\", \"question\": \"sensible\", \"citation\": \"district:skills\", \"text\": \"The approach solves the stated problem: the escalation ladder currently skips two tiers upstream already publishes. The problem belongs in the skills district, where book-index.md already lives, not in a new seam.\"}, {\"kind\": \"prediction\", \"question\": \"maintainability\", \"constraint_introduced\": \"A design or review-mode session must load a minimum of three nano-tier book excerpts before it may escalate any one book to mini or full tier. Full-tier loading is judgment-gated per book, never unconditional.\", \"text\": \"This establishes a real invariant on book-index.md's own escalation rule, replacing the prior 1-primary-plus-1-companion cap for book loading specifically.\"}, {\"kind\": \"prediction\", \"question\": \"new_pattern_or_duplicate\", \"verdict_detail\": \"new_pattern\", \"citation\": \"district:skills\", \"text\": \"No ADR or existing file in docs/architecture/adr/ or inventory.yaml covers tiered reference-doc escalation today. This is a new pattern for this repo, not a duplicate or reinvention.\"}], \"captured\": \"2026-08-25\", \"regret_risk\": \"Judgment-gated escalation has no mechanical check. This repo's own CLAUDE.md names exactly this failure mode from the Gate 4b history: a process step with no mechanical consumer gets skipped, however well it is documented. If that happens here, the 42-file vendoring cost is paid while sessions silently keep reading only full, the same as today, and the nano/mini tiers sit unread.\", \"stage\": \"design\"}, \"narrative\": {\"landscape\": {\"tagline\": \"Three cheap reads before one expensive one.\", \"narration\": \"book-index.md picks one book to load per task, and today it only owns one size: the full one. Upstream already publishes two smaller ones. This design wires them in.\"}, \"problem_solution\": {\"problem\": \"book-index.md's escalation jumps straight from a cheap corpus heuristic to a 300-1000 line full book. Upstream ciembor/agent-rules-books now ships nano and mini tiers for the same 14 books, and none of them are vendored or used here.\", \"solution\": \"Vendor nano.md and mini.md alongside each book's existing full.md. Require a minimum of three nano loads before any book may be judgment-escalated to mini or full, and make full-tier loading conditional instead of automatic.\", \"narration\": \"The fix is not a bigger corpus. It is using the sizes that already exist instead of reaching for the biggest one every time.\", \"beats\": [{\"kind\": \"grounding\", \"text\": \"Grounding found the escalation policy itself violates its own stated cap, and a documented sync script that was never actually written.\"}, {\"kind\": \"divergence\", \"text\": \"Six divergent frames produced a ledger, a cold-storage index, a subagent-fan-out pattern, a live-fetch approach that a security classifier blocked outright, a pre-concatenated ladder file, and a panic-grep pre-filter.\"}, {\"kind\": \"challenge\", \"text\": \"The critic scored the ladder file highest, then flagged that its own stop-early discipline has no mechanical enforcement — the same failure mode this repo's Gate 4b history already lived through. The engineer chose to accept that same risk in a simpler, separate-files form instead of adopting the ladder file's mechanism.\"}, {\"kind\": \"decision\", \"text\": \"3 nanos minimum, judgment-gated escalation to mini or full, separate files per tier, the old 1+1 cap rewritten to say so explicitly.\"}, {\"kind\": \"addendum\", \"text\": \"Answering a follow-up question about Review mode's mandatory 14-file security corpus load surfaced the same shape of problem — 2179 lines read unconditionally, every run, larger than a single full book. The fix folded into this design instead of spawning a new one: read each file's first ~30 lines (metadata plus summary, already structurally separate from its examples) unconditionally, then read the remainder in full unless the summary clearly rules out applicability — defaulting to full, not to skipping, on ambiguity. The opposite bias from book escalation, deliberately, because a missed security finding costs more than a wasted read.\"}], \"alternatives\": [{\"option\": \"3 nanos, then always the full book, unconditionally\", \"rejected_because\": \"Defeats the point of a cheap tier, and breaks the existing cap with no replacement stated.\"}, {\"option\": \"Signed evidence ledger\", \"rejected_because\": \"Solves auditability, a problem nobody named, at the cost of a new file surface.\"}, {\"option\": \"Content-addressed cold storage\", \"rejected_because\": \"Sized for 140+ books; this repo has 14.\"}, {\"option\": \"Live-fetch from upstream URLs at task time\", \"rejected_because\": \"Blocked by a security classifier — a real injection and supply-chain risk.\"}, {\"option\": \"Pre-concatenated ladder file with a stop-early marker\", \"rejected_because\": \"Scored highest, but its stop-early rule has no mechanical consumer.\"}, {\"option\": \"Ad hoc subagent fan-out\", \"rejected_because\": \"Would need to ship as an agent, which ADR-0015 forbids.\"}, {\"option\": \"Panic-grep pre-filter\", \"rejected_because\": \"The critic's starred survivor, deferred rather than bundled into this design.\"}, {\"option\": \"Judgment-gate the security corpus the same symmetric way as books (skip-on-ambiguity)\", \"rejected_because\": \"A missed finding in a compliance-weighted report costs more than a wasted read; security stays biased toward reading in full.\"}]}, \"architecture\": {\"narration\": \"book-index.md becomes the single owner of a three-tier vendored shelf, with an explicit, stated ceiling on how much of it any one task may read. The security corpus gets the same shape of gate without a new file format.\", \"beats\": [{\"kind\": \"component\", \"text\": \"references/books/<name>/{nano,mini,full}.md — 42 files, replacing the current 14 full-only files.\"}, {\"kind\": \"rule\", \"text\": \"book-index.md: load a minimum of 3 nano excerpts; escalate any one book to mini or full only when its principles are judged to matter; full is never automatic.\"}, {\"kind\": \"consumer\", \"text\": \"Design mode Stage 1 (Ground) and Stage 3 (Explore), and Review/Maintenance's corpus-to-book escalation, both read the new rule instead of the old 1+1 cap.\"}, {\"kind\": \"rule\", \"text\": \"SKILL.md's Review mode security step: read the first ~30 lines of all 14 security corpus files unconditionally; read a file's remainder in full unless its summary clearly rules out applicability; ambiguity defaults to full, not to skipping.\"}]}}, \"diagrams\": {\"1\": \"C4Container\\n    title ADR-0021 book-index.md tiering: nano, mini, full escalation\\n\\n    Person(session, \\\"Design or Review/Maintenance session\\\", \\\"Reads book-index.md through its own Read tool calls\\\")\\n\\n    Container_Boundary(t1, \\\"Tier 1 (unchanged)\\\") {\\n        Container(corpus, \\\"corpus-index.md\\\", \\\"vendored YAML + markdown\\\", \\\"Cheap heuristic; narrows candidate books before any book file is read\\\")\\n    }\\n\\n    Container_Boundary(t2, \\\"Tier 2 - book-index.md (this design)\\\") {\\n        Container(index, \\\"book-index.md\\\", \\\"vendored markdown\\\", \\\"Escalation rule: load minimum 3 nano excerpts, then judgment-gate mini/full per book. Replaces the old 1-primary-plus-1-companion cap\\\")\\n        Container(books, \\\"references/books/<name>/{nano,mini,full}.md\\\", \\\"42 vendored files, 14 books\\\", \\\"Three tiers per book: nano ~20-40 lines, mini ~80-150 lines, full 300-1000 lines\\\")\\n    }\\n\\n    Container_Boundary(consumers, \\\"Consumers of the escalation rule\\\") {\\n        Container(design_stage, \\\"design-mode.md Stage 1 (Ground) / Stage 3 (Explore)\\\", \\\"reference\\\", \\\"Grounds and explores using book-index.md's tiered rule\\\")\\n        Container(review_stage, \\\"Review/Maintenance corpus-to-book escalation\\\", \\\"architecture skill mode\\\", \\\"Escalates from corpus-index.md through book-index.md; separate mandatory 14-file security corpus load is untouched by this design\\\")\\n    }\\n\\n    Rel(session, corpus, \\\"Consults first\\\", \\\"Tier 1 heuristic\\\")\\n    Rel(corpus, index, \\\"Narrows candidate books, hands off to\\\")\\n    Rel(design_stage, index, \\\"Invokes escalation rule\\\")\\n    Rel(review_stage, index, \\\"Invokes escalation rule\\\")\\n    Rel(index, books, \\\"Loads >= 3 nano, then judgment-gated mini/full per book\\\")\\n\", \"2\": \"sequenceDiagram\\n    participant Session as Design-mode session (Stage 1: Ground)\\n    participant Corpus as corpus-index.md\\n    participant Index as book-index.md\\n    participant BookA as clean-architecture (nano/mini/full)\\n    participant BookB as domain-driven-design-distilled (nano/mini/full)\\n    participant BookC as refactoring (nano/mini/full)\\n\\n    Session->>Corpus: Consult Tier 1 heuristic for the task\\n    Corpus-->>Session: Narrows candidates to 3 books (A, B, C)\\n    Session->>Index: Escalate per new rule (ADR-0021)\\n\\n    Index->>BookA: Read nano.md (~20-40 lines)\\n    BookA-->>Index: Excerpt returned\\n    Index->>BookB: Read nano.md (~20-40 lines)\\n    BookB-->>Index: Excerpt returned\\n    Index->>BookC: Read nano.md (~20-40 lines)\\n    BookC-->>Index: Excerpt returned\\n\\n    Note over Index: Minimum of 3 nano loads satisfied.\\\\nJudge each book's principles against the task.\\n\\n    Index->>Index: Judge Book A's principles matter for this task\\n    Index->>BookA: Escalate - read mini.md or full.md\\n    BookA-->>Index: Deeper tier content returned\\n\\n    Index->>Index: Judge Book B's principles do not clear the bar\\n    Note over Index,BookB: Book B stays at nano tier. No further Read call.\\n\\n    Index->>Index: Judge Book C's principles do not clear the bar\\n    Note over Index,BookC: Book C stays at nano tier. No further Read call.\\n\\n    Index-->>Session: Grounding = 3 nanos + 1 escalated book (A)\\n\", \"3\": \"classDiagram\\n    class Book {\\n        +name string\\n        +nanoPath string\\n        +miniPath string\\n        +fullPath string\\n        +tierLoaded Tier\\n        +escalated bool\\n    }\\n\\n    class Tier {\\n        <<enumeration>>\\n        NANO\\n        MINI\\n        FULL\\n    }\\n\\n    class EscalationRule {\\n        +minNanoLoads int = 3\\n        +escalationMode string = \\\"judgment-gated\\\"\\n        +cap string = \\\"supersedes prior 1-primary-plus-1-companion cap\\\"\\n        +selectCandidates(corpusResult) Book[]\\n        +loadNano(book) void\\n        +judgePrinciplesMatter(book, task) bool\\n        +escalate(book, targetTier) void\\n    }\\n\\n    class CorpusIndex {\\n        +narrowCandidates(task) Book[]\\n    }\\n\\n    class BookIndex {\\n        +rule EscalationRule\\n        +run(task) GroundingResult\\n    }\\n\\n    class GroundingResult {\\n        +nanoOnlyBooks Book[]\\n        +escalatedBooks Book[]\\n    }\\n\\n    CorpusIndex --> BookIndex : hands off narrowed candidates\\n    BookIndex --> EscalationRule : applies\\n    EscalationRule --> Book : loads/escalates\\n    Book --> Tier : tierLoaded\\n    BookIndex --> GroundingResult : produces\\n\\n    note for Book \\\"escalated=false -> nano-only for rest of task\\\\nescalated=true -> mini or full, judgment-gated per book\\\"\\n    note for EscalationRule \\\"Full-tier loading is never unconditional.\\\\nApplies to Design Stage 1/Stage 3 and\\\\nReview/Maintenance corpus-to-book escalation.\\\\nDoes NOT apply to the separate mandatory\\\\n14-file security corpus load.\\\"\\n\"}, \"pr_draft\": \"## Problem\\n\\n`book-index.md`'s escalation goes straight from the Tier 1 corpus heuristic\\nto one full vendored book, 300-1000 lines. Upstream\\n`ciembor/agent-rules-books` has since published `nano` (about 20-40 lines)\\nand `mini` (about 80-150 lines) tiers for the same 14 books, and\\n`books/README.md`'s manifest still only vendors `full`. Nothing in this\\nplugin uses the smaller tiers. Separately, `books/README.md` documents a\\n`scripts/sync-books.sh` drift-check script that does not exist anywhere in\\nthe repository.\\n\\nThe escalation ladder is missing two rungs that upstream already provides\\nfor free. Every task that reaches Tier 2 today pays the full-book cost with\\nno cheaper option available first.\\n\\n## Why this approach\\n\\nVendor `nano.md` and `mini.md` alongside the existing `full.md` for each of\\nthe 14 books, as separate files (42 total). Change `book-index.md`'s\\nescalation rule: after Tier 1 narrows candidates, load a minimum of three\\nnano-tier excerpts, then escalate any one of those books to mini or full\\nonly when its principles are judged to matter for the task at hand.\\nFull-tier loading is never unconditional. Rewrite the existing \\\"one primary\\nbook plus one optional companion\\\" cap to state this new ceiling explicitly.\\nApplies to Design mode's Stage 1 (Ground) and Stage 3 (Explore) grounding,\\nand to Review/Maintenance mode's corpus-to-book escalation.\\n\\n**Addendum.** Review/Maintenance's mandatory 14-file security corpus load\\ngets the same cheap-signal-before-expensive-read shape, with no vendoring:\\neach YAML already separates its metadata-plus-summary block (~17-24 lines)\\nfrom its worked examples. Read the first ~30 lines of all 14\\nunconditionally, every run, never skipping a category's summary; read a\\nfile's remainder in full unless the summary clearly rules out\\napplicability, defaulting to full on ambiguity — the opposite bias from\\nbook escalation, because a missed security finding costs more than a\\nwasted read.\\n\\n## Alternatives considered\\n\\n- **3 nanos, then always the full book, unconditionally (the engineer's\\n  original approach)** — rejected because making the full-book read\\n  unconditional defeats the reason a cheap tier exists, and it silently\\n  breaks the existing 1+1 cap with no stated replacement.\\n- **Signed evidence ledger gating the full-book read behind three\\n  committed nano entries** — rejected because it solves an auditability\\n  problem nobody named, and adds a new shim and file surface without\\n  reducing token cost or file count.\\n- **Content-addressed cold storage with per-session materialization and\\n  LRU eviction** — rejected because it is sized for a corpus of 140+\\n  books; this repo vendors 14. Conflicts with the plugin family's\\n  vendor-in-tree convention (ADR-0017).\\n- **Live-fetch the nano tier from upstream URLs at task time** — rejected\\n  because the frame that proposed this was blocked by a security\\n  classifier. Fetching remote content into prompt context at runtime is an\\n  injection and supply-chain risk this plugin family's self-only design\\n  avoids.\\n- **Pre-concatenated per-book ladder file with a stop-early marker** —\\n  rejected because its stop-early discipline has no mechanical consumer.\\n  This repo's own CLAUDE.md names exactly this failure mode from the\\n  Gate 4b history, even though this option scored highest with the critic.\\n- **Ad hoc subagent fan-out, one Task-tool call per book, verdict-only\\n  return** — rejected because ADR-0015 fixes the install surface at no\\n  agents, hooks, or MCP servers, and the pattern was declined even as an\\n  in-turn call to keep this design's scope to the vendored-file mechanism.\\n- **Panic-grep pre-filter** — rejected for this design's scope, though it\\n  is the critic's starred non-obvious survivor and the only option that\\n  stays inside the existing cap unmodified. Deferred, not disqualified.\\n- **Judgment-gate the security corpus the same symmetric way as book\\n  escalation (skip-on-ambiguity)** — rejected because a missed security\\n  finding in a compliance-weighted report is a worse failure than a design\\n  session under-reading one book. Security stays biased toward reading in\\n  full.\\n\\n## Out of scope\\n\\n- Fixing or creating `scripts/sync-books.sh`.\\n- Re-vendoring the existing `full/` books against upstream's current\\n  commit.\\n- The panic-grep pre-filter and the subagent fan-out pattern, both\\n  declined for this design.\\n- Vendoring separate nano/mini files for the security corpus — it uses a\\n  same-file partial read instead, since each YAML already separates\\n  metadata/summary from examples.\\n\\n## Risks\\n\\n- The judgment gate (\\\"escalate to mini/full only when it matters\\\") has no\\n  mechanical enforcement. This repo's own Gate 4b history shows an\\n  unenforced step tends to erode under time pressure.\\n- Vendoring surface triples from 14 files to 42, while the sync pipeline\\n  that would check them against upstream (`scripts/sync-books.sh`) is\\n  already missing.\\n- The security corpus applicability read also has no mechanical\\n  enforcement. Its default-to-full bias on ambiguity is meant to make a\\n  false \\\"not applicable\\\" verdict less likely, not impossible.\\n\\n## How this was tested\\n\\nNo code changes; this design writes `docs/` artifacts only. Verification\\nis: `book-index.md`'s escalation rule reads unambiguously, and the new cap\\nlanguage explicitly supersedes the old 1+1 cap rather than leaving both\\nstated at once.\\n\\n## Where to focus\\n\\n- Whether the judgment-gated escalation language in `book-index.md` is\\n  specific enough to actually change agent behavior, or vague enough to be\\n  silently ignored.\\n- Whether the rewritten cap conflicts with any other reference file that\\n  still assumes the old 1+1 cap.\\n- Whether the security corpus's opposite default bias (full-on-ambiguity)\\n  is stated clearly enough that a session doesn't quietly collapse it into\\n  the same skip-on-ambiguity behavior as book escalation.\\n\\nThe author flagged these parts as not fully understood:\\n- Whether upstream's nano/mini files exist for all 14 currently-vendored\\n  books at a syncable commit — not verified in this design.\\n\\n---\\n\\nAuthorship: agent-generated.\\n\"}, \"build-workflow-polish\": {\"goal\": {\"name\": \"build-workflow-polish\", \"title\": \"Fix four friction points found using the plugin family on a real project\", \"created\": \"2026-08-27\", \"outcome\": \"An engineer running architect:design into implement:start never loses the handoff, 04-slices.md is always written in the shape verify_gate.py actually parses, a validated slice is committed without an extra prompt, and the bundle viewer borrows the tab-based Diagram / Data flow / Data types layout a colleague prototyped.\", \"done_when\": [], \"abort_if\": [], \"min_work\": {\"derived_from\": \"Direct feedback from using cobuilder-plugin end to end on a separate real project on 2026-08-26/27. Four independent gaps surfaced, none touching the same file: architect:design's Stage 7 (references/design-mode.md) ends the mode without naming /implement:start as the next step; a session drafted 04-slices.md in a table shape slice_table.py's _ROW_RE_6COL/_ROW_RE_4COL do not match, so verify_gate.py and build_index.py silently failed to read it; build/SKILL.md's 'After each slice' step asks the user whether to commit instead of committing once VALIDATE clears the 0.90 threshold; and a colleague's diagram-viewer.html (scratchpad artifact, not in this repo) demonstrated a tabbed Diagram/Data-flow/Data-types layout with a collapsible left rail worth adopting for viewer/index.html.\", \"challenge_stage_run\": false, \"note\": \"Stage 0 and stage 1 grounding did not run. Stages 2 through 7 are deliberately deferred. This design is a backlog entry, not an approved design. It carries no intent.json, narrative.json, assessment.json, or diagrams, and it must not be implemented until design mode runs to completion for each epic that needs it.\"}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"Stage 7 in architect's references/design-mode.md ends design mode after creating the first branch, with no pointer to what runs next. Add one line after the branch is created: 'Design mode's work ends here. To build this design's epics, run /implement:start.' Do not have design mode invoke implement:start itself — architecture modes are self-only in scope, and implement already owns Gate 1's design-record lookup (build/SKILL.md's 'Optional: design mode record' section). This is a one-line prose addition to a mode-governing reference file, so it still needs the Gate-selection exception's behavioral rubric, not a skip.\"}, {\"id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"04-slices.md's Gate 4a instructions in build/SKILL.md describe the table only through the worked example, and never say the table must match shared/slice_table.py's exact column shape (six columns: #, Epic, Slice, Ends with, Score, State, with the epic-header row's precise empty-cell pattern) or point to that module as the source of truth. Add an explicit sentence and a link, so an authoring session does not freehand a table shape verify_gate.py silently fails to parse. Consider also having verify_gate.py surface parse_table()'s `unparsed` list by name when it is non-empty, instead of only reporting downstream symptoms like a missing epic.\"}, {\"id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"build/SKILL.md's 'After each slice' section (the four numbered steps following the red-green-validate loop) records the score and asks 'Proceed to the next slice, or adjust direction?' but never commits. In practice this produces a spurious 'want to commit this checkpoint first?' prompt after every slice instead of an automatic commit. Change step 2 (or add a step) so that once VALIDATE scores a slice at or above 0.90, the orchestrator commits the slice's changes to git immediately, before asking whether to proceed — no confirmation question for the commit itself, only for what comes after. A score below threshold still routes through the existing gap decision tree in references/validation-scoring.md, and does not commit. Below-threshold slices accepted with reservations after three attempts should also commit, since that path already represents a final accepted state, not a pending one.\"}, {\"id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A colleague's scratch artifact (/private/tmp/.../diagram-viewer.html, session-local, not part of this repo) uses a top-of-panel tab switcher across three views — Diagram, Data flow, Data types — instead of stacking them, plus a right-hand info rail. Evaluate adopting the same top-tab pattern for viewer/index.html's per-PR diagram levels (currently level 1/2/3 switching, per ADR-0020's viewer/src/ plan), and separately evaluate narrowing the left-hand navigation rail so it takes less width once tabs handle top-level switching. This touches only the viewer, so it can run through architect:design on its own without waiting on E1-E3.\"}], \"stage\": \"backlog\", \"supersedes\": null, \"adr\": null, \"adrs\": [], \"rounds\": []}}, \"cobuilder-implement\": {\"goal\": {\"name\": \"cobuilder-implement\", \"title\": \"CoBuilder Implement — a design becomes verified code\", \"created\": \"2026-08-19\", \"outcome\": \"The architecture design produced by cobuilder-architect carries directly into implementation: its intent is decomposed into testable epics, and each epic ships as vertical slices that an independent validator scores at 0.90 or better before the next one starts.\", \"done_when\": [\"the skill runs G1 through G4 and stops at each gate for approval\", \"a slice is scored at 0.90 or better by a validator that executes the tests and never wrote the code\", \"an epic below 0.90 loops back to GREEN and closes\", \"cobuilder-implement builds one real change end to end under its own gates\"], \"abort_if\": [\"0.90 proves unreachable in practice, so most real slices escalate rather than pass, which makes the threshold theatre\"], \"min_work\": {\"derived_from\": \"6 districts (skills, scripts, commands, viewer, docs, .cobuilder-architect), 3 related ADRs, 1 boundary rule on install surface\", \"alternatives_explored\": 5, \"boundary_rules_checked\": true, \"challenge_stage_run\": true}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Shares a branch and a pull request with the design-mode design. See ADR-0013: a branch may carry more than one design.\"}, {\"id\": \"E2\", \"branch\": \"docs/post-pr11-cleanup\", \"pr\": 12, \"state\": \"open\", \"note\": \"Gate 4b is enforced instead of merely specified. The per-epic technical solution design already exists in SKILL.md section 4b, with a six-section template, and five downstream files reference the artifact. Nothing checks that it was written. Checked on 2026-08-24: zero epic-<id>-design.md files exist, and six epics shipped without one. Four failures allow that. Gate 4's only exit check counts rubrics, so a Gate 4 that skips 4b still passes. slice-loop.js builds the epic-design path and never checks the file exists, so the red and green agents are told to read a file that is absent and quietly fall back to 03-program-design.md. rubric-authoring.md tells the author to update the technical design first, and nobody checks that either. No test asserts the artifact. The root cause is that 4b is the only Gate 4 sub-step whose output nothing downstream mechanically requires: the loop cannot score without rubrics or iterate without a slice plan, but it can read an absent design. This epic adds the exit check, the existence guard in the workflow, a test, and sub-step tracking in 00-status.md so an approved Gate 4 cannot hide a missing 4b. Raised 2026-08-22, re-scoped 2026-08-24 from 'write the process' to 'enforce the process' after reading the skill.\"}], \"stage\": \"approved\", \"supersedes\": null, \"adr\": \"ADR-0012\", \"adrs\": [\"ADR-0012\", \"ADR-0011\", \"ADR-0015\"], \"rounds\": [{\"n\": 1, \"changed\": true, \"feedback_class\": \"material\"}]}, \"intent\": {\"problem\": \"Software Factory gates the design, then accepts a slice on the implementer's own word. CoBuilder Guardian scores independently, but runs only on the pi SDK. Neither turns an approved design into verified code in an arbitrary harness, so the designs cobuilder-architect produces have nothing that consumes them.\", \"approach\": \"Three tiers and four gates. An initiative holds epics, a feature is an epic, and every epic must be testable. An epic holds vertical slices. Three separate agents build each slice: one writes failing tests, one writes the minimal code and never edits a test, and one validates. The validator executes the suite, then reads the code and judges whether the tests cover the declared outcomes. A slice needs 0.90 or better. The epic gate is hard, and remediation returns to the implementing agent until it closes.\", \"alternatives\": [{\"option\": \"One agent does the rubric, the tests, the code, and the self-report in a single context, and a human skims the log at the end\", \"rejected_because\": \"It abandons independent scoring, which the outcome requires. The agent that wrote the code judges its own test run, which is the most gameable pattern available.\"}, {\"option\": \"Delete the validator. Get blindness from context amnesia: one agent writes the rubric, the harness clears context, and the same agent implements and self-scores twice, voiding on a mismatch above 0.15\", \"rejected_because\": \"Erasure blindness cannot be verified from outside the model, and two runs of one policy give correlated error rather than independent verification. It assumes away the hard part.\"}, {\"option\": \"Treat rubrics as a cross-repo append-only ledger, cluster criterion fingerprints into a shared genome, and prime the validator with a prior score distribution\", \"rejected_because\": \"It needs a periodic clustering job across repos, which breaks the rule that this skill uses no external service and no pipeline runtime. It also solves a volume problem this work does not have yet.\"}, {\"option\": \"Forbid the validator from reading the implementation at all, and score only from a test runner's exit codes\", \"rejected_because\": \"It scores whether tests pass and never asks whether the tests are worth passing. The validator must also judge test adequacy against the specs, so it reads the code after it executes the suite.\"}, {\"option\": \"Commit a hash of every test file at G3 and check it before scoring\", \"rejected_because\": \"It changes the G3 gate contract for a hole that a validator judging test adequacy already covers. The void triggers widen in prose instead.\"}, {\"option\": \"Score a criterion called 'no undeclared test dependencies' to catch a fixture added after G3\", \"rejected_because\": \"Enumerating a test's transitive dependencies is not free in a dynamically imported language. The same reasoning as the hash check applies.\"}, {\"option\": \"Strip the validator's retry feedback to pass or fail per criterion, so accumulated feedback cannot leak the rubric's shape\", \"rejected_because\": \"Rich feedback is mandatory for GREEN to improve within its attempts. Starving it pushes slices into escalation, which costs more than the leak.\"}], \"authorship\": \"bjoerns, with Claude drafting and challenging\", \"captured\": \"2026-08-19\", \"design\": {\"name\": \"cobuilder-implement\", \"epic\": null}, \"design_name\": \"cobuilder-implement\", \"out_of_scope\": [\"opening a pull request or pushing a branch — that stays submit mode's job in cobuilder-architect\", \"changing design mode, which this design consumes and does not modify\", \"any code dependency between the two plugins in either direction\", \"agents, hooks, and MCP servers, so the plugin never touches another session's permission surface\", \"running the skill on a real initiative, which follows the build\"], \"reviewer_focus\": [\"the validator's mandate — it executes the suite, then reads the code and judges whether the tests cover the declared outcomes\", \"the epic gate and its remediation loop, which has no automatic escalation and relies on a human check-in every three remediations\", \"the light path, which removes the panels for a single-epic initiative but never the slice loop or the 0.90 gate\", \"the join contract path, which is written in two plans and breaks silently if one moves\"], \"risks\": [\"blindness is honour-based: nothing mechanically stops an implementer reading the rubric directory, and detection depends on the validator noticing\", \"at volume, a validator pool statistically absorbs the shape of passing rubrics, so prompt discipline leaks and no filesystem permission was ever there to catch it\", \"just-in-time detailing can redefine a cross-epic contract another epic depends on; the frozen-contract rule is a rule, not a mechanism\", \"the ledger records what agents claim, so a rigged run writes a clean-looking entry; it buys auditability, not integrity\", \"a validator that must judge test adequacy does more work per slice, which raises cost and slows every gate\"], \"source\": \"authored\", \"testing\": \"The skill is prose plus two workflow scripts, so verification is static plus one end-to-end run. Parse both scripts wrapped in an async function, because the runtime wraps them and a bare top-level return fails a plain module check. Confirm every reference named in SKILL.md exists and is reachable. Grep for retired names. Then run the skill against a throwaway feature and confirm it reaches the G3 rubric gate check and stops for approval. The stop condition is self-hosting: it builds one real change end to end under its own gates.\", \"unknowns\": [\"whether 0.90 is reachable often enough in practice that escalation stays the exception\", \"whether a validator judging test adequacy scores consistently enough run to run for the number to mean anything\", \"whether the manual three-role mode degrades acceptably in a harness with no scripted runner\", \"how many remediation rounds a real epic needs before the human check-in fires\"], \"why_now\": \"Design mode shipped, so designs now carry intent that nobody has to reconstruct. The half that turns a design into code does not exist, so the intent stops at the branch name.\"}, \"assessment\": {\"verdict\": \"proceed\", \"findings\": [{\"kind\": \"prediction\", \"id\": \"F1\", \"severity\": \"medium\", \"title\": \"Blindness has no mechanical enforcement\", \"detail\": \"Rubrics live in .cobuilder/rubrics/ inside the repo, so nothing stops an implementing agent reading them. Enforcement is prompt discipline plus a void rule applied after the fact, and detection depends on the validator noticing. The engineer accepted this risk deliberately, choosing in-repo storage over a second repository.\", \"cites\": \"ADR-0012\", \"mitigation\": \"Three layers rather than one: the prompt forbids the read, the implementer never receives the rubric path, and a detected read voids the score at no attempt cost instead of lowering it.\"}, {\"kind\": \"prediction\", \"id\": \"F2\", \"severity\": \"high\", \"title\": \"The join contract path is written in two plans and can break silently\", \"detail\": \"docs/architecture/designs/<name>/intent.json is the only coupling between cobuilder-architect and cobuilder-implement. It appears in this design and in the design-mode plan. A move in one without the other leaves the reader plugin looking at an absent file, and the failure mode is a silent fallback to drafting a Business Spec from scratch rather than an error.\", \"cites\": \"ADR-0013\", \"mitigation\": \"Record the coupling as a constraint introduced in the ADR, so a future session changing either plan is told the other moves with it. A missing file must report that it looked and found nothing, never fall through silently.\"}, {\"kind\": \"prediction\", \"id\": \"F3\", \"severity\": \"medium\", \"title\": \"Cross-epic contracts freeze by rule, not by mechanism\", \"detail\": \"Just-in-time Technical Specs only work when epics are separable. A skeleton names the contracts an epic exposes or consumes, and those freeze at G1. Nothing checks that a later detailing pass honoured them, and nothing checks that the skeleton named them honestly in the first place.\", \"cites\": \"district: docs\", \"mitigation\": \"A change to a frozen contract is a backtrack to G1 with re-approval. The rule is stated, and the risk of an unhonest skeleton stays open.\"}, {\"kind\": \"prediction\", \"id\": \"F4\", \"severity\": \"medium\", \"title\": \"The validator now carries two jobs, and the second is subjective\", \"detail\": \"The validator executes the suite, then reads the code and judges whether the tests are sensible and cover the declared outcomes. Execution is objective. Test adequacy is judgment, and judgment scored on a three-value scale may not repeat run to run. If the second job scores inconsistently, the 0.90 number stops meaning one thing.\", \"cites\": \"ADR-0012\", \"mitigation\": \"Every criterion cites evidence — a test name, captured output, or a file and line. A score without evidence does not count. Consistency is listed as an unknown to be measured on real slices, not assumed.\"}, {\"kind\": \"prediction\", \"id\": \"F5\", \"severity\": \"low\", \"title\": \"The ledger records claims, not facts\", \"detail\": \"An append-only record of each scored slice, with the validator's stated reason per criterion, makes a stalled initiative resumable and makes a score explicable. It does not make a score true. A rigged run writes a plausible entry.\", \"cites\": \"district: docs\", \"mitigation\": \"State the limit where the ledger is defined. It buys auditability, and it is not a fix for validation integrity.\"}, {\"kind\": \"prediction\", \"id\": \"F6\", \"severity\": \"low\", \"title\": \"Ceremony may exceed the work on small initiatives\", \"detail\": \"A multi-epic initiative runs one refute panel, plus a research panel and a skeptic per epic, before the first slice starts. On a two-day feature that is heavier than the feature.\", \"cites\": \"ADR-0012\", \"mitigation\": \"The light path collapses a single-epic initiative to one spec and removes the panels, the skeptic, and the epic rubric. The slice loop and the 0.90 gate never switch off.\"}], \"boundary_checks\": [\"no agents, no hooks, and no MCP servers, so the plugin never touches another session's permission surface\", \"no code dependency on cobuilder-architect in either direction; the join is one file\", \"no external service and no pipeline runtime; the manual three-role mode is the portable fallback\"], \"drift\": [], \"regret_risk\": \"low\", \"risk_tier\": \"medium\", \"stage\": \"design\", \"summary\": \"The design is internally consistent and its constraints are honest. Every finding below is a prediction, because no code exists yet. The two that matter are the honour-based blindness the engineer accepted knowingly, and the cross-plan contract path that breaks silently if one plan moves without the other.\"}, \"narrative\": {\"landscape\": {\"tagline\": \"A design becomes verified code, one slice at a time.\", \"narration\": \"cobuilder-architect can now capture a design before any code exists, but nothing on the other side of that record turns it into working software under independent checks. This design adds the missing half: four approval gates before implementation starts, and a validator that never wrote the code deciding whether each slice is actually done.\"}, \"problem_solution\": {\"problem\": \"Software Factory gates a design, then accepts a finished slice on the implementer's own word. CoBuilder Guardian scores independently, but it only runs on the pi SDK, DOT pipelines, beads, and Hindsight. Neither one takes an approved architecture design and turns it into verified code in an arbitrary coding harness, so the designs `cobuilder-architect` now produces have nothing that consumes them.\", \"solution\": \"Three tiers and four gates. An initiative holds epics, a feature is an epic, and every epic must be testable. An epic holds vertical slices. Three separate agents build each slice: one writes failing tests, one writes the minimal code and never edits a test, and one validates. The validator executes the suite, then reads the code and judges whether the tests cover the declared outcomes. A slice needs a score of `0.90` or better. The epic gate is hard, and remediation returns to the implementing agent until it closes.\", \"narration\": \"Design mode already writes down what a change is for and why, before the change exists. This design gives that record somewhere to go. It borrows the gate discipline of Software Factory, which settles every hard decision in prose while it is still cheap to change, and it borrows the independent scoring of CoBuilder Guardian, which never lets the agent that wrote the code grade its own test run. Joining the two means a design can walk all the way to working software, with a validator checking every step who was never shown the code while writing the checklist.\", \"beats\": [{\"kind\": \"background\", \"text\": \"`cobuilder-architect`'s design mode shipped first, so a design now carries intent, alternatives, and constraints that nobody has to reconstruct later (ADR-0011). ADR-0015 then made the architecture modes self-only, which means a build workflow cannot live inside `cobuilder-architect` itself. The design that mode produces has nothing downstream that reads it and turns it into code.\"}, {\"kind\": \"background\", \"text\": \"Software Factory's weakness sits at the end of a slice: the agent that built the slice proves it works by running it and showing the result, so the same agent decides what counts as proof. CoBuilder Guardian answers that weakness by writing acceptance rubrics before implementation starts, storing them where the implementer cannot read them, and scoring the result with an agent that did not write the code. Its weakness is the runtime: it needs the pi SDK, DOT pipelines, beads, and Hindsight, none of which runs in another harness.\"}, {\"kind\": \"intuition\", \"text\": \"Take a single epic with three slices. Slice one gets a rubric written before any code exists, hidden from the implementer. The GREEN agent writes the minimal code, the validator runs the test suite it never touched, reads the resulting code, and scores it against the rubric. A score of `0.85` sends the slice back to GREEN with the validator's stated reasons; a score of `0.90` or better closes it and starts the next slice. Nothing in that loop lets the agent that wrote the code also decide it is finished.\"}], \"alternatives\": [{\"option\": \"One agent does the rubric, the tests, the code, and the self-report in a single context, and a human skims the log at the end\", \"rejected\": \"It abandons independent scoring, which the outcome requires. The agent that wrote the code judges its own test run, which is the most gameable pattern available.\"}, {\"option\": \"Delete the validator. Get blindness from context amnesia: one agent writes the rubric, the harness clears context, and the same agent implements and self-scores twice, voiding on a mismatch above 0.15\", \"rejected\": \"Erasure blindness cannot be verified from outside the model, and two runs of one policy give correlated error rather than independent verification. It assumes away the hard part.\"}, {\"option\": \"Treat rubrics as a cross-repo append-only ledger, cluster criterion fingerprints into a shared genome, and prime the validator with a prior score distribution\", \"rejected\": \"It needs a periodic clustering job across repos, which breaks the rule that this skill uses no external service and no pipeline runtime. It also solves a volume problem this work does not have yet.\"}, {\"option\": \"Forbid the validator from reading the implementation at all, and score only from a test runner's exit codes\", \"rejected\": \"It scores whether tests pass and never asks whether the tests are worth passing. The validator must also judge test adequacy against the specs, so it reads the code after it executes the suite.\"}, {\"option\": \"Commit a hash of every test file at G3 and check it before scoring\", \"rejected\": \"It changes the G3 gate contract for a hole that a validator judging test adequacy already covers. The void triggers widen in prose instead.\"}, {\"option\": \"Score a criterion called 'no undeclared test dependencies' to catch a fixture added after G3\", \"rejected\": \"Enumerating a test's transitive dependencies is not free in a dynamically imported language. The same reasoning as the hash check applies.\"}, {\"option\": \"Strip the validator's retry feedback to pass or fail per criterion, so accumulated feedback cannot leak the rubric's shape\", \"rejected\": \"Rich feedback is mandatory for GREEN to improve within its attempts. Starving it pushes slices into escalation, which costs more than the leak.\"}]}, \"architecture\": {\"narration\": \"No plugin here takes a code dependency on another. `cobuilder-implement` joins `cobuilder-architect` through one file, `docs/architecture/designs/<name>/intent.json`, and joins nothing else. The install surface stays fixed: no agents, no hooks, no MCP servers, so the plugin never touches another session's permission surface. Everything else — the gates, the rubric store, the validator's mandate — is prose plus two workflow scripts that any harness able to spawn a subagent can run.\", \"beats\": [{\"kind\": \"forces\", \"text\": \"The skill must run in any harness that can spawn a subagent, with no SDK, no pipeline runtime, and no external service. An implementing agent that reads the rubric writes to the rubric, not to the requirement. An agent that wrote code cannot score it, because it reads its own intent into the code. ADR-0015 makes the architecture modes self-only, so a build workflow cannot live inside `cobuilder-architect`.\"}, {\"kind\": \"contract\", \"text\": \"`docs/architecture/designs/<name>/intent.json` is the only coupling between the two plugins, in either direction. `cobuilder-implement` reads it as the hypothesis for the initiative it builds. No shared code, no shared runtime, and no arrow from either plugin to the other's files.\"}, {\"kind\": \"boundary\", \"text\": \"Blindness is honour-based inside one repository: rubrics live in `.cobuilder/rubrics/`, and nothing mechanically stops an implementing agent from reading them. Enforcement is three layers — the prompt forbids the read, the implementer never receives the rubric path, and a detected read voids the score at no attempt cost rather than lowering it. Opening a pull request or pushing a branch stays submit mode's job in `cobuilder-architect`, never this plugin's.\"}]}}, \"diagrams\": {\"1\": \"C4Container\\n    title \\\"cobuilder-implement design: two plugins joined by one file, no code dependency\\\"\\n\\n    Person(engineer, \\\"Engineer\\\", \\\"Approves gates, runs both plugins in the same coding harness\\\")\\n\\n    Container_Boundary(target, \\\"Target repo\\\") {\\n        Container(intentfile, \\\"intent.json\\\", \\\"JSON file\\\", \\\"docs/architecture/designs/<name>/intent.json — the only join between the two plugins\\\")\\n    }\\n\\n    Container_Boundary(architect, \\\"cobuilder-architect plugin — designs\\\") {\\n        Container(designmode, \\\"design mode\\\", \\\"skill\\\", \\\"Interviews the engineer, writes intent.json and the ADR before code exists\\\")\\n    }\\n\\n    Container_Boundary(implement, \\\"cobuilder-implement plugin — builds\\\") {\\n        Container(gates, \\\"G1-G4 gates\\\", \\\"skill\\\", \\\"Business Spec, Technical Spec, Slice Plan and Rubrics, Acceptance\\\")\\n        Container(validator, \\\"VALIDATE subagent\\\", \\\"subagent\\\", \\\"Executes tests, then reads code, scores against a blind rubric\\\")\\n    }\\n\\n    Rel(engineer, designmode, \\\"Runs design mode on\\\")\\n    Rel(designmode, intentfile, \\\"Writes\\\")\\n    Rel(engineer, gates, \\\"Runs G1 to start build on\\\")\\n    Rel(gates, intentfile, \\\"Reads at G1 to ground the Business Spec\\\")\\n    Rel(gates, validator, \\\"Spawns per slice\\\")\\n\\n    UpdateRelStyle(designmode, intentfile, $offsetX=\\\"10\\\", $offsetY=\\\"-10\\\")\\n    UpdateRelStyle(gates, intentfile, $offsetX=\\\"-10\\\", $offsetY=\\\"10\\\")\\n\", \"2\": \"sequenceDiagram\\n    participant Orchestrator\\n    participant RED as RED subagent\\n    participant GREEN as GREEN subagent\\n    participant VALIDATE as VALIDATE subagent\\n    participant Suite as Test suite (fresh process)\\n    participant Ledger as Per-epic ledger\\n\\n    Orchestrator->>RED: Spawn for this slice\\n    RED->>RED: Write failing tests against the blind rubric's declared outcomes\\n    RED-->>Orchestrator: Failing tests committed\\n\\n    Orchestrator->>GREEN: Spawn with failing tests\\n    GREEN->>GREEN: Write minimal code, never edit a test\\n    GREEN-->>Orchestrator: Implementation ready\\n\\n    Orchestrator->>VALIDATE: Spawn to score the slice\\n    VALIDATE->>Suite: Execute the suite in a fresh process\\n    Suite-->>VALIDATE: Pass/fail results, no claimed result accepted\\n    VALIDATE->>VALIDATE: Read the code and the tests, judge adequacy against specs\\n\\n    alt Score >= 0.90, no critical criterion below 1.0\\n        VALIDATE->>Ledger: Append per-criterion score and stated reason\\n        VALIDATE-->>Orchestrator: Slice accepted\\n    else Score below 0.90\\n        VALIDATE->>Ledger: Append per-criterion score and stated reason\\n        VALIDATE-->>Orchestrator: Return to GREEN with full feedback\\n        Orchestrator->>GREEN: Retry (up to 3 attempts total)\\n        Note over Orchestrator,GREEN: After 3 attempts, escalate to a human check-in\\n    else Rigged pass detected (edited/skipped/weakened test, swapped dependency, rubric read)\\n        VALIDATE->>Ledger: Append VOID and stated reason\\n        VALIDATE-->>Orchestrator: Void, no attempt cost\\n        Orchestrator->>RED: Return to RED to rewrite the tests\\n    end\\n\", \"3\": \"classDiagram\\n    class Initiative {\\n        +businessSpec BusinessSpec\\n        +epics Epic[]\\n    }\\n\\n    class BusinessSpec {\\n        <<visible>>\\n        +problem string\\n        +epicList string[]\\n    }\\n\\n    class Epic {\\n        +name string\\n        +mustBeTestable bool\\n        +technicalSpec TechnicalSpec\\n        +epicRubric Rubric\\n        +slices Slice[]\\n    }\\n\\n    class TechnicalSpec {\\n        <<visible>>\\n        +writtenJustInTime bool\\n        +researchVerified bool\\n    }\\n\\n    class Slice {\\n        +blindRubric Rubric\\n        +score float\\n        +attempts int\\n        +maxAttempts int\\n    }\\n\\n    class Rubric {\\n        <<blind, implementer never reads it>>\\n        +criteria Criterion[]\\n    }\\n\\n    class Criterion {\\n        +score float\\n        +evidence string\\n        +critical bool\\n    }\\n\\n    class LedgerEntry {\\n        <<append-only>>\\n        +epic string\\n        +slice string\\n        +perCriterionScore float\\n        +validatorReason string\\n    }\\n\\n    class G1_BusinessSpec {\\n        <<gate, once>>\\n        +producesEpics() void\\n    }\\n\\n    class G2_TechnicalSpec {\\n        <<gate, per epic>>\\n        +producesTechnicalSpec() void\\n    }\\n\\n    class G3_SlicePlanAndRubrics {\\n        <<gate, per epic>>\\n        +producesSliceList() void\\n        +producesBlindRubrics() void\\n    }\\n\\n    class G4_Acceptance {\\n        <<gate, per slice then per epic>>\\n        +threshold float\\n    }\\n\\n    Initiative \\\"1\\\" --> \\\"*\\\" Epic : decomposes into\\n    Epic \\\"1\\\" --> \\\"1\\\" TechnicalSpec : holds\\n    Epic \\\"1\\\" --> \\\"1\\\" Rubric : holds epic rubric\\n    Epic \\\"1\\\" --> \\\"*\\\" Slice : holds\\n    Slice \\\"1\\\" --> \\\"1\\\" Rubric : holds blind rubric\\n    Rubric \\\"1\\\" --> \\\"*\\\" Criterion : contains\\n    Slice \\\"1\\\" --> \\\"*\\\" LedgerEntry : scored into\\n\\n    G1_BusinessSpec ..> Initiative : attaches to\\n    G1_BusinessSpec ..> Epic : attaches to\\n    G2_TechnicalSpec ..> TechnicalSpec : attaches to\\n    G3_SlicePlanAndRubrics ..> Slice : attaches to\\n    G3_SlicePlanAndRubrics ..> Rubric : attaches to\\n    G4_Acceptance ..> Slice : attaches to\\n    G4_Acceptance ..> Epic : attaches to\\n\\n    note for Rubric \\\"BLIND: the implementer never reads Rubric or Criterion.\\\\nBusinessSpec and TechnicalSpec stay visible.\\\"\\n\"}, \"pr_draft\": \"# CoBuilder Implement — a design becomes verified code\\n\\n## Problem\\n\\nSoftware Factory gates the design, then accepts a slice on the implementer's own word. CoBuilder Guardian scores independently, but runs only on the pi SDK. Neither turns an approved design into verified code in an arbitrary harness, so the designs cobuilder-architect produces have nothing that consumes them.\\n\\nDesign mode shipped, so designs now carry intent that nobody has to reconstruct. The half that turns a design into code does not exist, so the intent stops at the branch name.\\n\\n## Why this approach\\n\\nThree tiers and four gates. An initiative holds epics, a feature is an epic, and every epic must be testable. An epic holds vertical slices. Three separate agents build each slice: one writes failing tests, one writes the minimal code and never edits a test, and one validates. The validator executes the suite, then reads the code and judges whether the tests cover the declared outcomes. A slice needs 0.90 or better. The epic gate is hard, and remediation returns to the implementing agent until it closes.\\n\\n## Alternatives considered\\n\\n- **One agent does the rubric, the tests, the code, and the self-report in a single context, and a human skims the log at the end** — rejected because it abandons independent scoring, which the outcome requires. The agent that wrote the code judges its own test run, which is the most gameable pattern available.\\n- **Delete the validator. Get blindness from context amnesia: one agent writes the rubric, the harness clears context, and the same agent implements and self-scores twice, voiding on a mismatch above 0.15** — rejected because erasure blindness cannot be verified from outside the model, and two runs of one policy give correlated error rather than independent verification. It assumes away the hard part.\\n- **Treat rubrics as a cross-repo append-only ledger, cluster criterion fingerprints into a shared genome, and prime the validator with a prior score distribution** — rejected because it needs a periodic clustering job across repos, which breaks the rule that this skill uses no external service and no pipeline runtime. It also solves a volume problem this work does not have yet.\\n- **Forbid the validator from reading the implementation at all, and score only from a test runner's exit codes** — rejected because it scores whether tests pass and never asks whether the tests are worth passing. The validator must also judge test adequacy against the specs, so it reads the code after it executes the suite.\\n- **Commit a hash of every test file at G3 and check it before scoring** — rejected because it changes the G3 gate contract for a hole that a validator judging test adequacy already covers. The void triggers widen in prose instead.\\n- **Score a criterion called 'no undeclared test dependencies' to catch a fixture added after G3** — rejected because enumerating a test's transitive dependencies is not free in a dynamically imported language. The same reasoning as the hash check applies.\\n- **Strip the validator's retry feedback to pass or fail per criterion, so accumulated feedback cannot leak the rubric's shape** — rejected because rich feedback is mandatory for GREEN to improve within its attempts. Starving it pushes slices into escalation, which costs more than the leak.\\n\\n## Out of scope\\n\\n- opening a pull request or pushing a branch — that stays submit mode's job in cobuilder-architect\\n- changing design mode, which this design consumes and does not modify\\n- any code dependency between the two plugins in either direction\\n- agents, hooks, and MCP servers, so the plugin never touches another session's permission surface\\n- running the skill on a real initiative, which follows the build\\n\\n## Risks\\n\\n- blindness is honour-based: nothing mechanically stops an implementer reading the rubric directory, and detection depends on the validator noticing\\n- at volume, a validator pool statistically absorbs the shape of passing rubrics, so prompt discipline leaks and no filesystem permission was ever there to catch it\\n- just-in-time detailing can redefine a cross-epic contract another epic depends on; the frozen-contract rule is a rule, not a mechanism\\n- the ledger records what agents claim, so a rigged run writes a clean-looking entry; it buys auditability, not integrity\\n- a validator that must judge test adequacy does more work per slice, which raises cost and slows every gate\\n\\n## How this was tested\\n\\nThe skill is prose plus two workflow scripts, so verification is static plus one end-to-end run. Parse both scripts wrapped in an async function, because the runtime wraps them and a bare top-level return fails a plain module check. Confirm every reference named in SKILL.md exists and is reachable. Grep for retired names. Then run the skill against a throwaway feature and confirm it reaches the G3 rubric gate check and stops for approval. The stop condition is self-hosting: it builds one real change end to end under its own gates.\\n\\n## Where to focus\\n\\n- the validator's mandate — it executes the suite, then reads the code and judges whether the tests cover the declared outcomes\\n- the epic gate and its remediation loop, which has no automatic escalation and relies on a human check-in every three remediations\\n- the light path, which removes the panels for a single-epic initiative but never the slice loop or the 0.90 gate\\n- the join contract path, which is written in two plans and breaks silently if one moves\\n\\nThe author flagged these parts as not fully understood:\\n\\n- whether 0.90 is reachable often enough in practice that escalation stays the exception\\n- whether a validator judging test adequacy scores consistently enough run to run for the number to mean anything\\n- whether the manual three-role mode degrades acceptably in a harness with no scripted runner\\n- how many remediation rounds a real epic needs before the human check-in fires\\n\"}, \"design-mode\": {\"goal\": {\"name\": \"design-mode\", \"title\": \"Design mode for cobuilder-architect\", \"created\": \"2026-08-19\", \"outcome\": \"An engineer can design a change with the plugin before writing it, and the resulting intent, ADR, and diagrams carry through to the pull request that implements it.\", \"done_when\": [\"/cobuilder-architect:design runs the seven stages and writes a design directory\", \"submit mode resolves a design from the branch name and skips the cold interview\", \"the viewer lists and renders designs alongside the PR timeline\"], \"abort_if\": [\"the challenge stage cannot be made to produce rejected options reliably, leaving every ADR with a single considered option\", \"the architecture skill cannot be merged, leaving design mode without a corpus to reason with\"], \"min_work\": {\"derived_from\": \"3 districts (skills, scripts, viewer), 2 colliding ADRs, 1 boundary rule on install surface\", \"alternatives_explored\": 3, \"boundary_rules_checked\": true, \"challenge_stage_run\": true}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Design mode as a pre-code interview and challenge gate. Shares a branch and a pull request with the cobuilder-implement design. See ADR-0013: a branch may carry more than one design.\"}], \"stage\": \"implemented\", \"supersedes\": null, \"adr\": \"ADR-0011\", \"adrs\": [\"ADR-0011\", \"ADR-0014\", \"ADR-0015\"], \"rounds\": [{\"n\": 1, \"changed\": true, \"feedback_class\": \"material\"}, {\"n\": 2, \"changed\": true, \"feedback_class\": \"material\"}, {\"n\": 3, \"changed\": true, \"feedback_class\": \"cosmetic\"}]}, \"intent\": {\"problem\": \"The plugin captures author intent at submit time, after the change exists. Nothing captures it at design time, when the engineer still holds the alternatives, the constraints, and the doubts. Generate mode then reconstructs intent from merged code, which is the guessing the plugin was built to stop.\", \"approach\": \"A seventh mode, /prodyssey:design, running seven stages: name and outcome, ground, interview, explore, challenge, draft, review, branch. It writes a named design directory under exports/, never the timeline, and ends by creating a branch that submit mode later resolves by name.\", \"alternatives\": [{\"option\": \"Put design mode in archkit instead, next to its existing design mode\", \"rejected_because\": \"The interview discipline, the bundle, the diagram pipeline, and the viewer all live in prodyssey. archkit's contribution is a skill call. Building it in archkit would mean porting four subsystems to reach one.\"}, {\"option\": \"Keep archkit a soft, optional dependency and degrade when it is absent\", \"rejected_because\": \"A design mode whose substance is optional degrades silently into a chat. The corpus and the divergent exploration are the design content, not an enhancement to it.\"}, {\"option\": \"Ask the engineer which options they rejected, as an interview question\", \"rejected_because\": \"Nobody can name a rejected option before the design exists. A rejected option is an outcome of the design process, not an input to it. Stages 3 and 4 produce them instead.\"}, {\"option\": \"Mint a synthetic PR key so a design can enter the story.json timeline directly\", \"rejected_because\": \"story.json keys on an integer pr, and verify_bundle.py, record_publish.py, manifest.js, and the viewer all depend on it. Submit mode already stages outside the timeline for this reason.\"}, {\"option\": \"Review designs in a Lavish surface rather than the bundle viewer\", \"rejected_because\": \"Element-level annotation is a better review loop, but it adds an external dependency to a plugin that deliberately has none. The viewer already renders assessments and is committed with the repo.\"}], \"authorship\": \"agent-assisted\", \"captured\": \"2026-08-19\", \"design_name\": \"design-mode\", \"out_of_scope\": [\"opening a pull request — that stays submit mode's job\", \"pushing a branch or any other remote action\", \"element-level annotation or a send-to-agent channel in the viewer\", \"renaming the repository\", \"deciding the fate of the archkit repository\"], \"reviewer_focus\": [\"stage 4, the challenge gate — it is the sole producer of rejected options\", \"section 3, the branch-name lookup that joins a design to its pull request\", \"the CLAUDE.md passages that the merge deliberately breaks\"], \"risks\": [\"the agreeable design: exploration finds nothing contradicting the engineer, and the ADR records one choice instead of a decision\", \"a design-stage prediction read as a verified observation, because it cites an ADR id rather than a path:line\", \"the design and its branch diverge silently after stage 7\", \"the duplicate-design subagent reports no match and the engineer trusts it\"], \"source\": \"design\", \"testing\": \"Not yet built. This design directory is itself the first test of the layout: it was authored by hand against the proposed shape.\", \"unknowns\": [\"whether a semantic duplicate-design search is accurate enough that an engineer can trust a no-match answer\", \"how much the 3.1 MB architecture skill slows a plugin install in practice\", \"whether material and cosmetic review feedback can be classified reliably enough to route automatically\"], \"why_now\": \"Submit mode's intent block already has the right shape for a design. The bundle already holds districts, ADRs, and a timeline to design against. The archkit architecture skill already holds the corpus and the divergent exploration that a design needs. The three parts exist and are not connected.\"}, \"assessment\": {\"verdict\": \"proceed\", \"findings\": [{\"kind\": \"observation\", \"id\": \"F1\", \"severity\": \"low\", \"title\": \"Design mode shipped and moved, as goal.json's outcome describes\", \"detail\": \"plugins/cobuilder-architect/commands/design.md exists and dispatches to the architecture skill. No skills/odyssey directory or root-level commands/ directory exists anywhere in the tree today. The mode is not present under plugins/cobuilder-pr, which now ships five modes (baseline, review, generate, view, publish) with no design mode among them. This matches CLAUDE.md's Recent history entry: design mode moved from the odyssey skill into the architecture skill, leaving odyssey with five modes.\", \"cites\": \"plugins/cobuilder-architect/commands/design.md; absence of skills/odyssey and commands/ at the repository root\", \"mitigation\": \"None needed. This is the shipped, intended state.\"}, {\"kind\": \"observation\", \"id\": \"F2\", \"severity\": \"low\", \"title\": \"commands/explore-design.md is confirmed deleted\", \"detail\": \"No file named explore-design.md exists anywhere in the current tree. git log on docs/architecture/designs/design-mode/adr-draft.md and the surrounding commit history are consistent with CLAUDE.md's Recent history note that explore-design.md was deleted as a duplicate of design.md.\", \"cites\": \"find . -iname 'explore-design*' returns no results\", \"mitigation\": \"None needed.\"}, {\"kind\": \"drift\", \"id\": \"F3\", \"severity\": \"medium\", \"title\": \"ADR-0011's maps_to.modules names two paths that predate the five-plugin split\", \"detail\": \"ADR-0011's frontmatter lists maps_to.modules as skills/odyssey and commands. Neither path exists today. The command that implements this decision lives at plugins/cobuilder-architect/commands/design.md, and the skill it dispatches to is plugins/cobuilder-architect/skills/architecture/. The ADR was decided on 2026-08-19, before ADR-0016 and ADR-0017 split the single plugin into five and moved every root-level skills/ and commands/ directory into per-plugin roots. Nothing updated the ADR's maps_to block afterward.\", \"cites\": \"docs/architecture/adr/ADR-0011-design-mode.md, maps_to.modules; plugins/cobuilder-architect/commands/design.md\", \"mitigation\": \"Update ADR-0011's maps_to.modules to plugins/cobuilder-architect/commands and plugins/cobuilder-architect/skills/architecture, or mark the field historical and add a current-location note.\"}, {\"kind\": \"drift\", \"id\": \"F4\", \"severity\": \"low\", \"title\": \"intent.json's approach paragraph names a dropped plugin name and a rejected output location\", \"detail\": \"intent.json's approach field reads: \\\"A seventh mode, /prodyssey:design ... It writes a named design directory under exports/, never the timeline.\\\" The plugin was renamed from prodyssey to cobuilder-architect, and the shipped command is /cobuilder-architect:design. The shipped output location is docs/architecture/designs/<name>/, per goal.json's done_when and ADR-0011's decision text, not exports/. This sentence was captured on 2026-08-19, the same day as the ADR, and reads as an early draft overtaken by the decision recorded in the same design.\", \"cites\": \"docs/architecture/designs/design-mode/intent.json, approach field; docs/architecture/designs/design-mode/goal.json, done_when[0]\", \"mitigation\": \"Either update the approach sentence to match the shipped command and path, or add a note that intent.json is a point-in-time capture and the ADR supersedes it on naming and location.\"}, {\"kind\": \"observation\", \"id\": \"F5\", \"severity\": \"low\", \"title\": \"The design's pr-draft.md is now projected into the record index\", \"detail\": \"shared/build_index.py reads pr_draft_path = path.parent / 'pr-draft.md' at line 431, inside the function that walks each design directory. This design directory has no pr-draft.md of its own to project, but the code path that was missing is present in the script this branch ships.\", \"cites\": \"shared/build_index.py:431\", \"mitigation\": \"None needed for this design. Worth reconfirming the same line exists in each plugin's vendored shared/build_index.py copy before this fix is treated as universal.\"}, {\"kind\": \"observation\", \"id\": \"F6\", \"severity\": \"low\", \"title\": \"adr-draft.md is gone, consistent with promotion to ADR-0011\", \"detail\": \"docs/architecture/designs/design-mode/ holds no adr-draft.md today. The file was deleted on 2026-08-25, as part of the change on this branch that retires adr-draft.md as a documented artifact type, with the matching line removed from CLAUDE.md and README.md in the same change. The draft was ADR-0011, already promoted to docs/architecture/adr/ADR-0011-design-mode.md. It was stale on the same two counts F4 records for intent.json: it named /prodyssey:design and it named exports/ as the output location. git status --short confirms the deletion is uncommitted at the time of writing.\", \"cites\": \"git log --oneline -- docs/architecture/designs/design-mode/adr-draft.md\", \"mitigation\": \"None needed. Flagging only the unverified deletion commit for completeness.\"}, {\"kind\": \"observation\", \"id\": \"F7\", \"severity\": \"medium\", \"title\": \"Epic E1's pull request is open, and the index cannot resolve that state\", \"detail\": \"goal.json.epics[0] declares pr: 11 and state: 'open'. gh pr view 11 confirms pull request 11 is OPEN on GitHub today, so the declared state matches reality. But shared/build_index.py's collect_pull_requests() (line 641) reads only entries already present in data/story.json's timeline, which holds narrated, merged pull requests. refine_epic_status() (line 791) looks up each epic's pull request number in that same narrowed set and only overwrites the placeholder status when a match exists. Pull request 11 has not been narrated, so no match exists, and the index cannot derive its open state from the bundle alone. The goal.json field is the only place that state is recorded correctly right now.\", \"cites\": \"shared/build_index.py:641-665 (collect_pull_requests), shared/build_index.py:791-797 (refine_epic_status); goal.json epics[0]; gh pr view 11\", \"mitigation\": \"Either narrate pull request 11 once it is available to review mode, so the index can pick up its real state, or extend refine_epic_status to fall back to a live gh pr view lookup for epics whose pull request never entered the timeline.\"}], \"boundary_checks\": [{\"rule\": \"No agents, no hooks, no MCP servers in the install surface\", \"result\": \"holds\", \"note\": \"Design mode is a skill dispatch from a command file, the same shape as every other architecture mode.\"}, {\"rule\": \"Architecture modes are self-only and take no --repo\", \"result\": \"holds\", \"note\": \"plugins/cobuilder-architect/commands/design.md carries no --repo handling, consistent with the other five architecture commands.\"}, {\"rule\": \"A design lives under docs/architecture/designs/<name>/ and never enters story.json until a real PR number exists\", \"result\": \"holds\", \"note\": \"This design's own directory is exactly that shape, and its pull request, 11, is a real number returned by gh pr create, not a synthetic key.\"}], \"drift\": [{\"kind\": \"drift\", \"title\": \"ADR-0011's maps_to.modules names pre-split paths\", \"detail\": \"See F3. skills/odyssey and commands no longer exist as top-level paths after the five-plugin split (ADR-0016, ADR-0017). The ADR's own maps_to block was never updated to name plugins/cobuilder-architect/commands and plugins/cobuilder-architect/skills/architecture.\"}, {\"kind\": \"drift\", \"title\": \"intent.json still names the pre-rename plugin and a rejected output root\", \"detail\": \"See F4. The approach field says /prodyssey:design writing under exports/. The shipped command is /cobuilder-architect:design, writing under docs/architecture/designs/<name>/.\"}, {\"kind\": \"drift\", \"title\": \"Epic E1's real-world pull request state is invisible to the generated index\", \"detail\": \"See F7. Pull request 11 is open on GitHub and correctly recorded as such in goal.json, but shared/build_index.py can only refine an epic's status from pull requests already narrated into data/story.json's timeline. An open, un-narrated pull request leaves the index with only its initial placeholder, not the live GitHub state.\"}], \"regret_risk\": \"low\", \"risk_tier\": \"medium\", \"stage\": \"retrospective\", \"summary\": \"This assessment was written on 2026-08-25, after design mode shipped, because the file was found missing from the design directory. It is a retrospective, not a design-stage prediction. Every finding below states what a check against the tree today found, not what an author foresaw before the code existed. Design mode shipped, moved from the odyssey skill into the architecture skill, and its command now lives at plugins/cobuilder-architect/commands/design.md. The design's own record carries two stale paths from before the five-plugin split, and its lone epic sits in a state the index cannot yet resolve because its pull request is open, not merged.\"}, \"narrative\": {\"landscape\": {\"narration\": \"This design adds a seventh Odyssey mode so an engineer can record intent, alternatives, and constraints before any code exists, then carry that record through to the pull requests that implement it.\", \"tagline\": \"Design before the change exists.\"}, \"problem_solution\": {\"narration\": \"Submit mode already captures intent, but only after the change exists. The alternatives are already closed. Design mode reaches the earlier moment, writes a named design under docs/, and joins N pull requests through an epics array.\", \"problem\": \"The plugin captures author intent at submit time, after the change exists. Nothing captures it at design time, when the engineer still holds the alternatives, the constraints, and the doubts.\", \"solution\": \"A seven-stage /cobuilder-architect:design mode writes docs/architecture/designs/<name>/, drafts a decided ADR, and joins pull requests through design/<name>[/<epic-slug>].\", \"beats\": [{\"kind\": \"background\", \"text\": \"Submit mode already interviews an author before the PR opens. That is later than the moment the alternatives are still open. Generate mode then reconstructs intent from merged code.\"}, {\"kind\": \"intuition\", \"text\": \"Name the outcome first. Search existing designs by meaning. Interview five topics. Explore. Challenge until rejected options exist. Draft the ADR. Review. Create the first local branch only.\"}], \"alternatives\": [{\"option\": \"Build design mode in archkit\", \"rejected\": \"The interview, bundle, diagrams, and viewer live here.\"}, {\"option\": \"Keep the architecture skill optional\", \"rejected\": \"A design without a corpus degrades into a chat.\"}, {\"option\": \"Ask which options they rejected\", \"rejected\": \"A rejected option is an outcome of explore and challenge, not an interview input.\"}, {\"option\": \"Mint a synthetic PR key\", \"rejected\": \"story.json and four readers require a real integer pr.\"}, {\"option\": \"One branch per design\", \"rejected\": \"N epics produce N pull requests.\"}]}, \"architecture\": {\"narration\": \"Authored source lives in docs/. The bundle is a full rebuild. Architecture modes are self-only. Odyssey keeps --repo. One design lists every ADR it produced, and drift is measured per epic.\", \"beats\": [{\"kind\": \"forces\", \"text\": \"story.json keys on an integer pr. The plugin ships no agents, hooks, or MCP servers. cobuilder-factory owns epic decomposition.\"}, {\"kind\": \"contract\", \"text\": \"docs/architecture/designs/<name>/ is the design home. build_adrs.py and build_designs.py rebuild the self bundle only. Submit strips design/ and loads intent.json as the hypothesis.\"}, {\"kind\": \"boundary\", \"text\": \"Design mode does not take --repo, does not push, does not open a PR, and does not decompose epics. A design never enters the timeline until gh pr create returns a number.\"}]}}, \"diagrams\": {\"1\": \"C4Container\\ntitle cobuilder-architect after design mode\\n\\nPerson(eng, \\\"Engineer\\\", \\\"Designs, then submits\\\")\\nPerson(rev, \\\"Reviewer\\\", \\\"Reads docs/ and the viewer\\\")\\n\\nSystem_Boundary(plugin, \\\"cobuilder-architect\\\") {\\n  Container(design, \\\"Design mode\\\", \\\"odyssey\\\", \\\"Seven stages, self-only\\\")\\n  Container(submit, \\\"Submit mode\\\", \\\"odyssey\\\", \\\"Interview, assess, open PR\\\")\\n  Container(odyssey, \\\"Odyssey five\\\", \\\"odyssey\\\", \\\"Baseline, generate, view, publish\\\")\\n  Container(arch, \\\"Architecture six\\\", \\\"architecture\\\", \\\"Self-only review and decisions\\\")\\n}\\n\\nSystem_Ext(docs, \\\"docs/\\\", \\\"Authored ADRs, designs, PR write-ups\\\")\\nSystem_Ext(bundle, \\\".cobuilder-architect/\\\", \\\"Derived projections and binary\\\")\\n\\nRel(eng, design, \\\"Names the outcome\\\")\\nRel(design, docs, \\\"Writes designs/ and a decided ADR\\\")\\nRel(design, arch, \\\"Stage 3 explore, stage 4 challenge\\\")\\nRel(eng, submit, \\\"Confirms the PR\\\")\\nRel(submit, docs, \\\"Reads the design, writes pull-requests/\\\")\\nRel(odyssey, bundle, \\\"Rebuilds adrs.js and designs.js\\\")\\nRel(rev, bundle, \\\"Opens the designs view\\\")\\nRel(rev, docs, \\\"Reviews markdown\\\")\\n\"}}, \"inflight-record-store\": {\"goal\": {\"name\": \"inflight-record-store\", \"title\": \"A record store for in-flight pull requests\", \"created\": \"2026-08-25\", \"outcome\": \"A reader on the main branch sees every piece of in-flight work across all open pull requests, and no pull request owner hits a merge conflict caused by that view.\", \"done_when\": [], \"abort_if\": [], \"min_work\": {\"derived_from\": \"collect_pull_requests() in shared/build_index.py reads data/story.json, which holds narrated, merged pull requests only. Eight epics in goal.json files point at pull request 11, which is not a pull_request entity, so refine_epic_status() cannot derive its state and leaves a hardcoded placeholder. A daily agent that pulls every branch and writes one consolidated view onto main guarantees a merge conflict for every pull request owner, because each of them touches the same generated file. File-based single-document storage cannot carry concurrent in-flight state. This constraint makes the work a program, not a single fix. The role model is beads (bd), which stores one record per file with append-only semantics and a defined merge strategy.\", \"challenge_stage_run\": false, \"note\": \"Stage 0 and stage 1 grounding did not run. Stages 2 through 7 are deliberately deferred. This design is a backlog entry, not an approved design. It carries no intent.json, narrative.json, assessment.json, or diagrams, and it must not be implemented until design mode runs to completion.\"}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Open pull requests become entities. collect_pull_requests() merges gh pr list --state all metadata over the narrated story.json entities. An open pull request gets an entity with state: \\\"open\\\" and a narrated: false flag. This removes the placeholder in refine_epic_status(). It reads metadata only, checks out no branch, and creates no conflict. This is the tracer bullet, and it is useful on its own. It does not depend on E2, E3, or E4, and can ship alone.\"}, {\"id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A record store with a merge strategy. One file per record, append-only, with a defined resolution rule for a concurrent write. Modelled on beads. This epic decides the storage shape and nothing else.\"}, {\"id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Cross-branch collection without a conflict. Records written by a session on a branch reach the shared view without any branch writing a shared generated file.\"}, {\"id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The active-work view. The viewer renders in-flight work beside merged history, so a reader sees one timeline rather than two.\"}], \"stage\": \"backlog\", \"supersedes\": null, \"adr\": null, \"adrs\": [\"ADR-0018\"], \"rounds\": []}}, \"lean-bundle-diffs\": {\"goal\": {\"name\": \"lean-bundle-diffs\", \"title\": \"Stop committing per-PR diff dumps into the bundle\", \"created\": \"2026-08-26\", \"outcome\": \"A fresh clone of this repo is not weighed down by every merged PR's full diff text, and the viewer still shows a diff for any PR a reader opens.\", \"done_when\": [], \"abort_if\": [], \"min_work\": {\"derived_from\": \"data/diffs-pr{N}.js under .cobuilder-architect/ is a per-PR export of extract_diffs.py's output, committed today the same way story.js and adrs.js are. Measured on 2026-08-26: the repo's clone size is ~40 MB, and the largest single files are diffs-pr11.js (6.4 MB) and several more in the hundreds of KB to low MB range, per-PR and permanent. Unlike the narrative and ADRs, a diff is fully reproducible at any time from git diff <merge-base>..<head> as long as history is not rewritten, so committing it buys nothing a live git command could not supply on demand.\", \"challenge_stage_run\": false, \"note\": \"Stage 0 and stage 1 grounding did not run. Stages 2 through 7 are deliberately deferred. This design is a backlog entry, not an approved design. It carries no intent.json, narrative.json, assessment.json, or diagrams, and it must not be implemented until design mode runs to completion.\"}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Decide the replacement shape: the viewer reads a diff by running git diff against the PR's recorded commit SHAs at serve time (view mode already runs from inside the repo), versus keeping diffs-pr{N}.js exactly as it is today but gitignoring it, regenerating it from git on demand the way exports/branch-*/diff.json already works for generate mode. Either removes the committed weight; they differ in whether a served bundle still needs a live git checkout next to it.\"}, {\"id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Whichever shape E1 picks, implement it in extract_diffs.py and, if the viewer starts requesting diffs at view time instead of reading a static file, in artifact's serve_bundle.py. Update verify_bundle.py's expectations accordingly.\"}, {\"id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Migrate the existing committed bundles (self, and the two fixture bundles) so history stops growing: remove the checked-in diffs-pr{N}.js files, or move them under .gitignore, per E1's decision. This does not touch story.json's authored fields, so it is a bundle-file change, not a schema migration.\"}], \"stage\": \"backlog\", \"supersedes\": null, \"adr\": null, \"adrs\": [], \"rounds\": []}}, \"maintainable-viewer\": {\"goal\": {\"name\": \"maintainable-viewer\", \"title\": \"The viewer becomes parts, templates, and mode modules\", \"created\": \"2026-08-24\", \"outcome\": \"An engineer changes one view mode of the bundle viewer by opening one file of a few hundred lines, and the file that ships is still one self-contained HTML page that publishes as an Artifact unchanged.\", \"done_when\": [\"viewer/index.html is produced by build_viewer.py from ordered parts under viewer/src/, and a test fails when the committed output does not equal a fresh build\", \"no part exceeds roughly 400 lines, and each of the five view modes lives in its own part\", \"the rail-item, empty-card, and empty-detail fragments have one definition each, instead of 4, 16, and 18 copies\", \"the 50 scattered mode-predicate calls collapse into one mode registry, and adding a sixth mode touches one new part plus one registry entry\", \"export_artifact.py matches named markers the parts emit on purpose, instead of incidental literals, and publishing a PR still succeeds byte-for-byte equivalently\", \"the five modes render identically before and after, proven by measurement in a browser rather than by reading the code\"], \"abort_if\": [\"the single IIFE proves not separable, because sections close over shared state in a way that ordered concatenation cannot preserve without rewriting the state model\", \"ordered concatenation turns out to be insufficient and the build needs real dependency resolution, which would reopen the bundler option ADR-0020 rejected\"], \"min_work\": {\"derived_from\": \"4917 measured lines (881 CSS, 3872 JS, 145 functions, 50 mode-predicate calls, 45 innerHTML blocks, 0 templates), 4 related ADRs, 1 bounded context, 35 existing banner-comment sections\", \"challenge_stage_run\": false, \"note\": \"Stage 0 and stage 1 grounding ran on 2026-08-24 and produced the measurements above. Stages 2 through 7 are deliberately deferred at the engineer's request. This design is a backlog entry, not an approved design. It carries no intent.json, narrative.json, assessment.json, or diagrams, and it must not be implemented until design mode runs to completion.\"}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The build exists and is guarded. build_viewer.py concatenates ordered parts under viewer/src/ into the committed viewer/index.html, and a test rebuilds from the parts and fails on any difference. The first split is mechanical only, cut at the file's own 35 banner comments, with no line rewritten. This epic is the tracer bullet: it proves or disproves that the single IIFE separates by concatenation alone, which is the abort condition the whole design rests on.\"}, {\"id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The exporter seam becomes declared. export_artifact.py stops matching incidental literals and matches named markers the parts emit deliberately. Today it hard-errors with not-found-verbatim when a literal moves, so the publish pipeline breaks silently under any refactor until somebody publishes. This must land before the parts are rewritten, not after, or every later epic risks breaking publishing without a test noticing.\"}, {\"id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"One definition per fragment. A template layer replaces the 45 innerHTML string blocks, and the rail-item, empty-card, and empty-detail fragments collapse from 4, 16, and 18 copies to one each. The viewer uses no template element today, so the mechanism is a design choice this epic makes and does not inherit.\"}, {\"id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Each mode owns its own part. The five view modes stop interleaving: the 50 scattered mode-predicate calls collapse into one registry, and renderContextsMainContent, renderBuildsMainContent, and renderMainContent stop being three functions of over 200 lines that live beside each other. Adding a sixth mode becomes one new part plus one registry entry.\"}], \"stage\": \"backlog\", \"supersedes\": null, \"adr\": \"ADR-0020\", \"adrs\": [\"ADR-0020\", \"ADR-0001\", \"ADR-0018\", \"ADR-0019\"], \"rounds\": []}}, \"plugin-split\": {\"goal\": {\"name\": \"plugin-split\", \"title\": \"Five sibling plugins, with the bundle as the only seam\", \"created\": \"2026-08-20\", \"outcome\": \"A user installs one stage of the architecture lifecycle without the other three, and four independently-installed plugins write into one bundle without a stale copy destroying authored or paid content.\", \"done_when\": [\"five plugin manifests validate, and cobuilder-full-lifecycle installs the other four\", \"shared/ is vendored by symlink and an installed copy resolves it from its own cache\", \"require_compatible() gates every script that writes into a bundle\", \"bundle.json carries min_reader_schema and a generators map instead of one scalar\"], \"abort_if\": [\"the marketplace symlink dereference does not cover a directory of Python that a skill later runs, and the copy-before-publish fallback proves worse than staying one plugin\"], \"min_work\": {\"derived_from\": \"6 districts (skills, scripts, commands, viewer, docs, .cobuilder-architect), 4 related ADRs, 1 boundary rule on install surface\", \"alternatives_explored\": 5, \"boundary_rules_checked\": true, \"challenge_stage_run\": true}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"One plugin becomes five. The whole packaging split: the mode renames inside today's single plugin, the deleted duplicate explore-design command, the 33 cross-pillar references, the five manifests, and the two ports. The rename is a rotation, not a split: odyssey submit becomes cobuilder-pr:generate, and the old odyssey generate, which narrates merged history, becomes cobuilder-pr:review. Four slices.\"}, {\"id\": \"E2\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Shared code survives an install. Prove the marketplace symlink dereference, then vendor the four bundle modules and the two shared skills. One slice, because it is a spike whose result decides whether the rest of the design stands. goal.json.abort_if names its failure.\"}, {\"id\": \"E3\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"The seam is version-safe. require_compatible(), min_reader_schema, the generators map, bundle format 3, and schema 1.3, so two plugins at different versions cannot corrupt one bundle. Two slices.\"}, {\"id\": \"E4\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"The record index. build_index.py replaces the two projection scripts, emits every entity with a stable id, and resolves every join including an ADR to its pull request. Two slices.\"}, {\"id\": \"E5\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"One lifecycle surface. The Decisions, Contexts, and Builds modes, the Backlog lane, and moving generated pages out of the foreign directory into the bundle. Two slices (10, 11).\"}, {\"id\": \"E6\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"The reply channel. The append-only ledger and its projection, the anchor computed from the live DOM, the write endpoint, and the background wake command. Three slices (12, 13, 14).\"}, {\"id\": \"E7\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Threads read as conversations. The drawer renders a reply under the comment it answers. Deferred past E6 on purpose: E6 ships the record shape, so this epic adds no migration and carries no slice yet.\"}, {\"id\": \"E8\", \"branch\": \"rename-plugins-drop-cobuilder-prefix\", \"pr\": 17, \"state\": \"merged\", \"note\": \"The cobuilder- prefix drops from the four non-umbrella plugins named in E1's table (cobuilder-architect, cobuilder-pr, cobuilder-artifact, cobuilder-implement become architect, pr, artifact, implement), since it duplicates the marketplace repo's own name. cobuilder-full-lifecycle keeps its prefix, being the umbrella the repo's own name already carries. Merged as PR #17, then a same-epic follow-up (uncommitted at the time this entry was written) fixes a second-order bug the rename introduced: two of the four renamed plugins (implement, artifact) already shipped a skill folder named after themselves, and once the plugin took that same name too, Skill() resolution inside Claude Code broke -- reproduced directly, not environment-specific. Fixed by renaming the colliding skill folders (implement's skill to build, its command to start; artifact's skill to cobuilder-artifacts) and, for consistency, cobuilder-full-lifecycle's orientation skill to cobuilder-full. See ADR-0016's addendum.\"}], \"stage\": \"implemented\", \"supersedes\": null, \"adr\": \"ADR-0016\", \"adrs\": [\"ADR-0016\", \"ADR-0017\", \"ADR-0015\", \"ADR-0006\"], \"rounds\": [{\"n\": 1, \"changed\": true, \"feedback_class\": \"material\"}]}, \"intent\": {\"problem\": \"One plugin carries the whole architecture lifecycle. Its four skills, thirteen commands, and twenty-two scripts serve four separate jobs, and a user who wants one job installs all four. The names have drifted with the scope: the same short words mean different things across the two skill families, and CLAUDE.md now carries a vocabulary table to keep them apart. A vocabulary table is a symptom. The words collide because one install surface holds four bounded contexts.\", \"approach\": \"Five sibling plugins in one marketplace repository, none nested inside another: cobuilder-architect, cobuilder-pr, cobuilder-artifact, cobuilder-implement, and cobuilder-full-lifecycle, which ships one orientation skill and lists the other four as dependencies. No plugin reads or imports another plugin's files, though a plugin may hand off to another plugin's mode by name. They integrate only by reading and writing the bundle directory, which becomes a Published Language rather than a shared kernel. Shared Python and shared skills travel by a marketplace-level shared/ directory, symlinked into each plugin and dereferenced into its own cache at install. The compatibility gate moves out of skill prose into require_compatible(), a function in the module every plugin vendors, and bundle.json carries the floor that gate reads.\", \"alternatives\": [{\"option\": \"Keep one plugin and organise internally with directories and modes\", \"rejected\": \"A directory is not installable. The install surface is the unit a user chooses, so a user who wants only PR narration still takes the review corpus, the viewer, and the paid-art pipeline.\"}, {\"option\": \"Two plugins, one per pillar, with the viewer duplicated into both\", \"rejected\": \"Both pillars write projections the one viewer reads. Two copies drift, and the Designs toggle then renders differently depending on which plugin last refreshed the file.\"}, {\"option\": \"Let plugins reach each other's scripts through a shared path\", \"rejected\": \"Not possible. A cached plugin cannot read outside its own directory, and CLAUDE_PLUGIN_ROOT resolves only to that plugin's own cache.\"}, {\"option\": \"Abandon the plugin format and ship one cloned repository with entry-point scripts\", \"rejected\": \"It removes version skew by removing the install surface, and the install surface is the product.\"}, {\"option\": \"Symlink the shared modules and add one scalar version comparison per entry point\", \"rejected\": \"A scalar cannot express a sequenced ladder with per-step touches sets. Two copies can agree on the number and still disagree on what a step may change, which is false confidence rather than a gate.\"}, {\"option\": \"A write-log ledger as the compatibility mechanism\", \"rejected\": \"It records a mismatch after the mismatch is on disk. Kept in part: its useful half is the plugin identity, which survives as the generators map.\"}], \"authorship\": \"agent-assisted\", \"captured\": \"2026-08-20\", \"design_name\": \"plugin-split\", \"doubts\": [\"The symlink dereference is documented for sharing files within a marketplace, and the worked example is a meta-plugin's skills/ directory. It is not stated for a directory of Python that a skill later runs through CLAUDE_PLUGIN_ROOT.\", \"The one-tag release discipline is policy. Nothing in the install mechanism enforces it, so the gate has to hold on its own.\"], \"source\": \"design\", \"why_now\": \"The pieces the split needs are all present and none of them are wired. The architecture skill merged in. Design mode and cobuilder-implement are designed and joined through intent.json. The viewer already has a Designs and Pull-requests toggle, so it already reads projections written by what would become two different plugins, and neither writer knows the other exists. The seam is real before the split, which is why the split is a packaging change rather than a rewrite.\"}, \"assessment\": {\"verdict\": \"proceed\", \"findings\": [{\"kind\": \"skill-gap\", \"id\": \"F1\", \"severity\": \"high\", \"title\": \"Architecture design mode does not know design directories exist\", \"detail\": \"skills/architecture/SKILL.md's Design Mode section names one output: an ADR markdown document, optionally with an HTML version. The design directory — goal.json, intent.json, narrative.json, assessment.json, diagrams/ — came from the odyssey design mode and is specified in skills/odyssey/references/design-mode.md. Session decision 3 folds odyssey design into the architect pillar, but the architecture skill was never given that responsibility. Running the mode therefore produced two well-formed ADRs and no design at all. The Designs view stayed empty because nothing in the mode it ran was ever told to fill it.\", \"cites\": \"skills/architecture/SKILL.md, Design Mode, Output\", \"mitigation\": \"Design mode's Output section must name the design directory as a first-class output alongside the ADR, and reference the seven stages in design-mode.md rather than stopping at the corpus chain. This is merge debt from folding the two skills together, not a new feature.\"}, {\"kind\": \"skill-gap\", \"id\": \"F2\", \"severity\": \"high\", \"title\": \"narrative.json is the only design artifact with no projection and no validation\", \"detail\": \"build_designs.py projects goal, intent, and assessment through explicit field tuples (GOAL_FIELDS, INTENT_FIELDS, ASSESSMENT_FIELDS) and validates their shape. narrative.json is attached raw at line 277 with one isinstance check. Its four-level structure — landscape, problem_solution, architecture, file_changes — is written down nowhere: not in the script, not in the architecture skill, not in a template. An author has to read design-mode/narrative.json and infer the shape. A missing or misshapen level fails silently in the viewer rather than loudly in the build.\", \"cites\": \"scripts/build_designs.py:277, viewer/index.html:816\", \"mitigation\": \"Give narrative.json a NARRATIVE_FIELDS projection and a per-level check, the same treatment the other three artifacts already get. Add the level shape to the design-mode reference so it can be authored without reverse-engineering an example.\"}, {\"kind\": \"skill-gap\", \"id\": \"F3\", \"severity\": \"medium\", \"title\": \"verify_bundle checks PRs level by level and designs file-by-existence\", \"detail\": \"check_designs() returns ok when data/designs.js parses as an object assignment. It never looks inside a design. A design carrying only goal.json passes as ok, which is exactly what happened here. The per-PR checks verify each of the four narrative levels, each asset, and each diagram individually. Two artifact families feed the same four-level viewer under two completely different standards of proof.\", \"cites\": \"scripts/verify_bundle.py:215-226\", \"mitigation\": \"Extend check_designs to report per-design completeness in the same vocabulary the PR checks use, with narrative levels required and intent, assessment, and diagrams optional.\"}, {\"kind\": \"skill-gap\", \"id\": \"F4\", \"severity\": \"low\", \"title\": \"The corpus path in the skill body does not match the corpus on disk\", \"detail\": \"SKILL.md and corpus-index.md both reference corpus/principles/... The files live at references/corpus/principles/... A first read from the skill base directory finds nothing.\", \"cites\": \"skills/architecture/SKILL.md, Design Mode corpus chain\", \"mitigation\": \"Make the paths relative to the skill base directory, or state the base the paths are relative to.\"}, {\"kind\": \"prediction\", \"id\": \"F5\", \"severity\": \"high\", \"title\": \"The compatibility gate is only as good as its least-updated vendored copy\", \"detail\": \"require_compatible() lives in a module each plugin vendors. A plugin forked before the function existed carries no gate, and the safety property collapses without a signal. The one-tag release discipline narrows this to cross-tag skew but does not close it, because nothing in the install mechanism enforces a shared tag.\", \"cites\": \"ADR-0017\", \"mitigation\": \"Have the writer record its own key in the generators map on every write. A bundle whose generators map holds a plugin that never advanced its key is then visible evidence of an ungated writer, rather than an invisible one.\"}, {\"kind\": \"prediction\", \"id\": \"F6\", \"severity\": \"medium\", \"title\": \"Neither ADR can reach approved, because the context they map to does not exist\", \"detail\": \"ADR-0016 and ADR-0017 both anchor maps_to at context cobuilder-packaging. docs/architecture/contexts/ is empty, so there is no boundary.yaml carrying the invariant either record establishes. The decision-records standard requires that anchor.\", \"cites\": \"ADR-0016, ADR-0017, Maps to\", \"mitigation\": \"Run /cobuilder-architect:describe for cobuilder-packaging before either record leaves tentative. That is epic E5.\"}], \"boundary_checks\": [{\"rule\": \"No agents, no hooks, no MCP servers in the install surface\", \"result\": \"holds\", \"note\": \"The split adds manifests and symlinks only.\"}, {\"rule\": \"Authored source lives in docs/, derived projections in the bundle\", \"result\": \"holds\", \"note\": \"shared/ is authored source at the marketplace root, outside both.\"}, {\"rule\": \"Architecture modes are self-only and refuse a foreign target\", \"result\": \"holds\", \"note\": \"The split does not move any mode across that line.\"}], \"drift\": [{\"kind\": \"drift\", \"title\": \"Two design modes still coexist\", \"detail\": \"skills/odyssey/references/design-mode.md and skills/architecture/SKILL.md both define a design mode. Decision 3 folds one into the other. Until that lands, which mode a user gets depends on which command they type, and only one of them writes a design directory.\"}], \"regret_risk\": \"medium\", \"risk_tier\": \"high\", \"stage\": \"design\", \"summary\": \"The split is sound and the compatibility gate is the load-bearing half. Two findings are about the change itself. Four are about the skill mode that produced it: running /cobuilder-architect:design against this design surfaced that architecture's design mode writes an ADR and nothing else, so every design-directory artifact the viewer renders had to be written by hand afterwards.\"}, \"narrative\": {\"landscape\": {\"tagline\": \"One install surface, four jobs.\", \"narration\": \"The plugin grew until one install carried the whole architecture lifecycle: design a change, build it, submit and narrate the pull request, render the result. Four jobs, one manifest. The first sign was not a bug. It was a vocabulary table appearing in CLAUDE.md to stop the same short words meaning two things, because review meant one thing in the architecture skill and another in the odyssey skill. This design splits the install surface along the seam the words were already drawing.\"}, \"problem_solution\": {\"problem\": \"One plugin holds four bounded contexts. A user who wants pull-request narration installs the review corpus, the viewer, and the paid-art pipeline as well. The names collide because nothing in the packaging keeps the contexts apart, so the prose has to.\", \"solution\": \"Five sibling plugins in one marketplace repository, integrating only through the bundle directory on disk. No plugin reads or imports another plugin's files, though a plugin may hand off to another plugin's mode by name. The bundle becomes a Published Language: its shape is documented and versioned, and it belongs to no plugin that writes into it.\", \"narration\": \"The split is easy to state and the cost is easy to miss. Today one plugin writes the bundle, so the bundle cannot disagree with itself. Four plugins can be four different versions on one machine, and each carries its own vendored copy of the code that decides what a bundle is allowed to contain. That copy is the whole risk, because the bundle holds authored prose, paid images, and paid audio that no rerun can replace.\", \"beats\": [{\"kind\": \"background\", \"text\": \"A cached plugin cannot read outside its own directory. CLAUDE_PLUGIN_ROOT resolves to one plugin's own cache and nothing above it. The dependencies field guarantees co-installation, not file access. So shared code must be copied, not referenced.\"}, {\"kind\": \"discovery\", \"text\": \"The refusal posture already exists. migrate_bundle.py exits with status 1 on a bundle_format or schema_version newer than it knows, and names the remediation. What was missing was not the idea of refusing.\"}, {\"kind\": \"discovery\", \"text\": \"Three writers bypass it entirely. extract_story.py overwrites meta.schema_version with its own compiled constant, and export_artifact.py and _manifest.py stamp the same constant. None of them reads bundle.json first. Today that is safe only because the skill prose runs migrate first.\"}, {\"kind\": \"turn\", \"text\": \"Prose in one plugin's SKILL.md cannot bind another plugin. Across a plugin boundary the invariant has to be code, so the gate moves into require_compatible() in the module every plugin vendors.\"}, {\"kind\": \"turn\", \"text\": \"The bundle carries the floor, not the plugin. A plugin stops asking whether its own version looks recent and asks the bundle what the bundle demands. That is the only form of the check that still works when nobody can enumerate the readers.\"}], \"alternatives\": [{\"option\": \"Keep one plugin, organise internally\", \"rejected\": \"A directory is not installable.\"}, {\"option\": \"Two plugins, viewer duplicated into both\", \"rejected\": \"Both pillars write projections one viewer reads. Two copies drift.\"}, {\"option\": \"Reach a sibling plugin's files at a shared path\", \"rejected\": \"Not possible after install. The docs are explicit.\"}, {\"option\": \"Abandon the plugin format, ship a cloned repo\", \"rejected\": \"It removes skew by removing the install surface, which is the product.\"}, {\"option\": \"Symlink plus one scalar version compare\", \"rejected\": \"A scalar cannot express a sequenced ladder with per-step touches sets. False confidence.\"}, {\"option\": \"A write-log ledger as the mechanism\", \"rejected\": \"It reports the mismatch after it is written. Kept as the generators map.\"}]}, \"architecture\": {\"narration\": \"Three plugins write projections into one versioned directory, one reads all of them, and no arrow runs from any plugin to any other. That absence is the design. cobuilder-implement is the exception in kind, not in mechanism: it hands off through a git branch and a real pull request, which is still a file-and-state seam rather than a call.\", \"beats\": [{\"kind\": \"forces\", \"text\": \"The install surface is fixed: no agents, no hooks, no MCP servers. There is no CI, no test suite, and no package manager, so any gate has to be runtime code rather than a build check.\"}, {\"kind\": \"boundary\", \"text\": \"shared/ sits at the marketplace root and is not itself a plugin. Each plugin symlinks what it needs, and the documented dereference copies the target content into that plugin's own cache at install. Every plugin ships byte-identical code and no plugin reads outside itself at runtime.\"}, {\"kind\": \"boundary\", \"text\": \"bundle.json gains min_reader_schema, the schema version of the newest migration that has ever touched this bundle. require_compatible() refuses when that floor exceeds the caller's own compiled SCHEMA_VERSION, before any ladder step runs.\"}, {\"kind\": \"consequence\", \"text\": \"generator_version becomes generators, a map keyed by plugin. One scalar written from the running plugin's own manifest is accurate with one writer and silently wrong with four.\"}, {\"kind\": \"open\", \"text\": \"The symlink dereference is documented for a meta-plugin's skills/ directory, not for a directory of Python a skill later runs. Install a two-plugin draft from a local marketplace and read the cache before this leaves tentative. The fallback is a copy step before publish.\"}]}}}, \"ubiquitous-language\": {\"goal\": {\"name\": \"ubiquitous-language\", \"title\": \"A mandated ubiquitous language across the plugin family\", \"created\": \"2026-08-25\", \"outcome\": \"One term means one thing across every plugin, skill, design, and ADR in the repo, and a new or colliding term cannot reach a merged pull request undetected.\", \"done_when\": [], \"abort_if\": [\"a first mechanical check of an existing term set produces mostly false positives, showing that a script cannot tell a real term from ordinary prose\", \"design authors route around the language stage on three or more designs in a row, showing that enforcement is worked around rather than followed\"], \"min_work\": {\"derived_from\": \"A case-insensitive grep for ubiquitous|domain-driven|bounded context|aggregate|domain event|glossary|ddd returns zero hits in plugins/cobuilder-architect/skills/architecture/references/design-mode.md and zero hits in plugins/cobuilder-implement/skills/implement/SKILL.md. plugins/cobuilder-architect/skills/architecture/references/corpus/principles/ddd/ holds 11 files, loaded by design mode's Ground stage and by review mode, but nothing checks the result. architecture-documentation.md section 3 requires a context canvas to carry a ubiquitous language of 5 to 15 terms that disambiguates homonyms shared with other contexts, and describe mode obeys it. docs/architecture/contexts/cobuilder-packaging/canvas.md section 3 defines Skill as five skills, architecture, odyssey, mermaid, ste-writing, and collaborate-with-user, and three of those five names are now wrong. Its boundary.yaml public_interface still lists commands/submit.md and commands/explore-design.md, both deleted, and scripts/_bundle_meta, which moved to shared/. The canvas also states that Skill and command carry the same meaning across the whole repo, with no homonym conflict against CLAUDE.md's vocabulary table, and that claim is now false because CLAUDE.md's table and the canvas section do not know about each other. CLAUDE.md itself records the lesson: a process step with no mechanical consumer gets skipped, however well it is documented, citing Gate 4b, which ran for zero of five multi-slice epics before it gained a mechanical check.\", \"challenge_stage_run\": false, \"note\": \"Stages 2 through 7 are deliberately deferred. This design is a backlog entry, not an approved design. It carries no intent.json, narrative.json, assessment.json, or diagrams, and it must not be implemented until design mode runs to completion.\"}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"One language record, and it is the tracer bullet. docs/architecture/language.md becomes the single source of truth for a term, its owning context, its meaning, and what it must not be confused with. It is seeded from CLAUDE.md's existing Vocabulary table and from cobuilder-packaging's canvas section 3. shared/build_index.py projects it into the record index, so the viewer can render it. This epic is independently shippable and does not depend on E2 through E5.\"}, {\"id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Design mode gains a language stage. A design that introduces a new term must declare it or reuse an existing entry from the language record. A collision with an existing term becomes a challenge-stage finding. This fits design mode's existing no-citation-no-challenge rule, because the language record gives the challenge stage something concrete to cite.\"}, {\"id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The mechanical consumer, in cobuilder-implement. A Gate 4b technical solution design that names a concept absent from the language record fails the gate. This epic is what stops the whole program from rotting the way canvas section 3 did. E1 and E2 without E3 are documentation waiting to go stale.\"}, {\"id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A verifier that exits non-zero. A script finds a term used in a design or an ADR and absent from the language record, and fails the check. It is modeled on plugins/cobuilder-implement/scripts/verify_gate.py, which exits non-zero on any failed Gate 4 sub-step. The honest open question is that deciding what counts as a term mechanically, rather than by judgement, is the hard part of this epic and may need its own exploration.\"}, {\"id\": \"E5\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Repair the existing drift. Refresh cobuilder-packaging's canvas section 3 and its boundary.yaml public_interface against the shipped five-plugin tree, delete the now-false homonym comment, and reconcile the two glossaries into E1's record.\"}], \"stage\": \"backlog\", \"supersedes\": null, \"adr\": null, \"adrs\": [], \"rounds\": []}}};\n+window.DESIGNS = {\"book-index-tiering\": {\"goal\": {\"name\": \"book-index-tiering\", \"title\": \"book-index.md gains a nano tier, and full-book loading stops being unconditional\", \"created\": \"2026-08-25\", \"outcome\": \"book-index.md offers nano, mini, and full tiers for all 14 vendored books, with a 3-nano-minimum judgment-gated escalation rule; Review/Maintenance's mandatory 14-file security corpus load gets the same cheap-signal-before-expensive-read shape, opposite default bias, no vendoring.\", \"done_when\": [\"books/README.md's manifest lists nano.md, mini.md, and full.md for each of the 14 books\", \"book-index.md states the 3-nano-minimum, judgment-gated escalation rule, replacing the prior 1-primary-plus-1-companion cap for book loading\", \"design-mode.md's Stage 1/3 grounding section and the architecture skill's Review/Maintenance corpus-to-book escalation both reference the new rule\", \"SKILL.md's Review mode security corpus step reads the first ~30 lines of all 14 security files unconditionally, then reads a file's remainder in full unless its summary clearly rules out applicability, defaulting to full on ambiguity\"], \"abort_if\": [], \"min_work\": {\"derived_from\": \"1 district (skills), 2 colliding ADRs (ADR-0015, ADR-0017), 2 boundary rules (book-index.md's prior 1+1 cap, Review mode's mandatory-14-file security rule)\", \"alternatives_explored\": 7, \"boundary_rules_checked\": true, \"challenge_stage_run\": true}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"slug\": \"book-index-tiering\", \"branch\": \"design/book-index-tiering\", \"pr\": 16, \"state\": \"open\", \"outcome\": \"book-index.md, books/README.md, design-mode.md, and SKILL.md's Review mode security corpus step all reflect ADR-0021's two escalation rules (book tiering, and the security corpus applicability read)\"}], \"stage\": \"review\", \"supersedes\": null, \"adr\": \"ADR-0021\", \"adrs\": [\"ADR-0021\"], \"rounds\": [{\"n\": 1, \"changed\": false, \"feedback_class\": \"approved\"}, {\"n\": 2, \"changed\": true, \"feedback_class\": \"material\", \"note\": \"Folded in the security corpus applicability read after a live discussion of whether the mandatory 14-file security load deserved the same scrutiny as book loading; resolved with an opposite default bias (full-on-ambiguity, not skip-on-ambiguity).\"}]}, \"intent\": {\"problem\": \"book-index.md's escalation goes straight from the Tier 1 corpus heuristic to one full vendored book, 300-1000 lines. Upstream ciembor/agent-rules-books has since published nano (about 20-40 lines) and mini (about 80-150 lines) tiers for the same 14 books, and books/README.md's manifest still only vendors full. Nothing in this plugin uses the smaller tiers. Separately, books/README.md documents a scripts/sync-books.sh drift-check script that does not exist anywhere in the repository.\", \"approach\": \"Vendor nano.md and mini.md alongside the existing full.md for each of the 14 books, as separate files (42 total). Change book-index.md's escalation rule: after Tier 1 narrows candidates, load a minimum of three nano-tier excerpts, then escalate any one of those books to mini or full only when its principles are judged to matter for the task at hand. Full-tier loading is never unconditional. Rewrite the existing 'one primary book plus one optional companion' cap to state this new ceiling explicitly. Applies to Design mode's Stage 1 (Ground) and Stage 3 (Explore) grounding, and to Review/Maintenance mode's corpus-to-book escalation. Addendum: Review/Maintenance's mandatory 14-file security corpus load gets the same cheap-signal-before-expensive-read shape, with no vendoring (the existing YAML files already separate a metadata-plus-summary block, ~17-24 lines, from worked examples). Read the first ~30 lines of all 14 unconditionally, every run, never skipping a category's summary; read a file's remainder in full unless the summary clearly rules out applicability, defaulting to full on ambiguity — the opposite bias from book escalation, because a missed security finding is worse than a wasted read.\", \"alternatives\": [{\"option\": \"3 nanos, then always the full book, unconditionally (the engineer's original approach)\", \"rejected_because\": \"Making the full-book read unconditional defeats the reason a cheap tier exists, and it silently breaks the existing 1+1 cap with no stated replacement.\"}, {\"option\": \"Signed evidence ledger gating the full-book read behind three committed nano entries\", \"rejected_because\": \"Solves an auditability problem nobody named. Adds a new shim and file surface without reducing token cost or file count.\"}, {\"option\": \"Content-addressed cold storage with per-session materialization and LRU eviction\", \"rejected_because\": \"Sized for a corpus of 140+ books; this repo vendors 14. Conflicts with the plugin family's vendor-in-tree convention (ADR-0017).\"}, {\"option\": \"Live-fetch the nano tier from upstream URLs at task time instead of vendoring it\", \"rejected_because\": \"The frame that proposed this was blocked by a security classifier. Fetching remote content into prompt context at runtime is an injection and supply-chain risk this plugin family's self-only design avoids.\"}, {\"option\": \"Pre-concatenated per-book ladder file with a stop-early marker\", \"rejected_because\": \"Scored highest with the critic and collapses vendoring back to 14 files, but its stop-early discipline has no mechanical consumer. This repo's own CLAUDE.md names exactly this failure mode from the Gate 4b history.\"}, {\"option\": \"Ad hoc subagent fan-out, one Task-tool call per book, verdict-only return\", \"rejected_because\": \"ADR-0015 fixes the install surface at no agents, hooks, or MCP servers. The pattern could only ever run as an in-turn call, never shipped infrastructure, and was declined even in that form to keep this design's scope to the vendored-file mechanism.\"}, {\"option\": \"Panic-grep pre-filter: one hand-written panic line per nano file, grepped before any tier loads\", \"rejected_because\": \"The critic's starred non-obvious survivor, and the only option that stays inside the existing cap unmodified. Deferred rather than bundled in, since it is orthogonal to the chosen mechanism.\"}, {\"option\": \"Judgment-gate the security corpus the same symmetric way as book escalation (skip-on-ambiguity)\", \"rejected_because\": \"A missed security finding in a compliance-weighted report is a worse failure than a design session under-reading one book. Security is already the highest-weighted category in the scoring rubric. The chosen rule keeps a mandatory summary read for every category and defaults to full on ambiguity instead.\"}], \"authorship\": \"agent-generated\", \"captured\": \"2026-08-25\", \"design_name\": \"book-index-tiering\", \"out_of_scope\": [\"Fixing or creating scripts/sync-books.sh\", \"Re-vendoring the existing full/ books against upstream's current commit\", \"The panic-grep pre-filter and the subagent fan-out pattern, both declined for this design\", \"Vendoring separate nano/mini files for the security corpus — it uses a same-file partial read instead, since each YAML already separates metadata/summary from examples\"], \"reviewer_focus\": [\"Whether the judgment-gated escalation language in book-index.md is specific enough to actually change agent behavior, or vague enough to be silently ignored\", \"Whether the rewritten cap conflicts with any other reference file that still assumes the old 1+1 cap\", \"Whether the security corpus's opposite default bias (full-on-ambiguity) is stated clearly enough that a session doesn't quietly collapse it into the same skip-on-ambiguity behavior as book escalation\"], \"risks\": [\"The judgment gate ('escalate to mini/full only when it matters') has no mechanical enforcement. This repo's own Gate 4b history shows an unenforced step tends to erode under time pressure.\", \"Vendoring surface triples from 14 files to 42, while the sync pipeline that would check them against upstream (scripts/sync-books.sh) is already missing.\", \"The security corpus applicability read also has no mechanical enforcement. Its default-to-full bias on ambiguity is meant to make a false 'not applicable' verdict less likely, not impossible.\"], \"source\": \"design\", \"testing\": \"No code changes; this design writes docs/ artifacts only. Verification is: book-index.md's escalation rule reads unambiguously, and the new cap language explicitly supersedes the old 1+1 cap rather than leaving both stated at once.\", \"unknowns\": [\"Whether upstream's nano/mini files exist for all 14 currently-vendored books at a syncable commit — not verified in this design\"], \"why_now\": \"The escalation ladder is missing two rungs that upstream already provides for free. Every task that reaches Tier 2 today pays the full-book cost with no cheaper option available first.\"}, \"assessment\": {\"verdict\": \"concerns\", \"findings\": [{\"kind\": \"prediction\", \"question\": \"sensible\", \"citation\": \"district:skills\", \"text\": \"The approach solves the stated problem: the escalation ladder currently skips two tiers upstream already publishes. The problem belongs in the skills district, where book-index.md already lives, not in a new seam.\"}, {\"kind\": \"prediction\", \"question\": \"maintainability\", \"constraint_introduced\": \"A design or review-mode session must load a minimum of three nano-tier book excerpts before it may escalate any one book to mini or full tier. Full-tier loading is judgment-gated per book, never unconditional.\", \"text\": \"This establishes a real invariant on book-index.md's own escalation rule, replacing the prior 1-primary-plus-1-companion cap for book loading specifically.\"}, {\"kind\": \"prediction\", \"question\": \"new_pattern_or_duplicate\", \"verdict_detail\": \"new_pattern\", \"citation\": \"district:skills\", \"text\": \"No ADR or existing file in docs/architecture/adr/ or inventory.yaml covers tiered reference-doc escalation today. This is a new pattern for this repo, not a duplicate or reinvention.\"}], \"captured\": \"2026-08-25\", \"regret_risk\": \"Judgment-gated escalation has no mechanical check. This repo's own CLAUDE.md names exactly this failure mode from the Gate 4b history: a process step with no mechanical consumer gets skipped, however well it is documented. If that happens here, the 42-file vendoring cost is paid while sessions silently keep reading only full, the same as today, and the nano/mini tiers sit unread.\", \"stage\": \"design\"}, \"narrative\": {\"landscape\": {\"tagline\": \"Three cheap reads before one expensive one.\", \"narration\": \"book-index.md picks one book to load per task, and today it only owns one size: the full one. Upstream already publishes two smaller ones. This design wires them in.\"}, \"problem_solution\": {\"problem\": \"book-index.md's escalation jumps straight from a cheap corpus heuristic to a 300-1000 line full book. Upstream ciembor/agent-rules-books now ships nano and mini tiers for the same 14 books, and none of them are vendored or used here.\", \"solution\": \"Vendor nano.md and mini.md alongside each book's existing full.md. Require a minimum of three nano loads before any book may be judgment-escalated to mini or full, and make full-tier loading conditional instead of automatic.\", \"narration\": \"The fix is not a bigger corpus. It is using the sizes that already exist instead of reaching for the biggest one every time.\", \"beats\": [{\"kind\": \"grounding\", \"text\": \"Grounding found the escalation policy itself violates its own stated cap, and a documented sync script that was never actually written.\"}, {\"kind\": \"divergence\", \"text\": \"Six divergent frames produced a ledger, a cold-storage index, a subagent-fan-out pattern, a live-fetch approach that a security classifier blocked outright, a pre-concatenated ladder file, and a panic-grep pre-filter.\"}, {\"kind\": \"challenge\", \"text\": \"The critic scored the ladder file highest, then flagged that its own stop-early discipline has no mechanical enforcement — the same failure mode this repo's Gate 4b history already lived through. The engineer chose to accept that same risk in a simpler, separate-files form instead of adopting the ladder file's mechanism.\"}, {\"kind\": \"decision\", \"text\": \"3 nanos minimum, judgment-gated escalation to mini or full, separate files per tier, the old 1+1 cap rewritten to say so explicitly.\"}, {\"kind\": \"addendum\", \"text\": \"Answering a follow-up question about Review mode's mandatory 14-file security corpus load surfaced the same shape of problem — 2179 lines read unconditionally, every run, larger than a single full book. The fix folded into this design instead of spawning a new one: read each file's first ~30 lines (metadata plus summary, already structurally separate from its examples) unconditionally, then read the remainder in full unless the summary clearly rules out applicability — defaulting to full, not to skipping, on ambiguity. The opposite bias from book escalation, deliberately, because a missed security finding costs more than a wasted read.\"}], \"alternatives\": [{\"option\": \"3 nanos, then always the full book, unconditionally\", \"rejected_because\": \"Defeats the point of a cheap tier, and breaks the existing cap with no replacement stated.\"}, {\"option\": \"Signed evidence ledger\", \"rejected_because\": \"Solves auditability, a problem nobody named, at the cost of a new file surface.\"}, {\"option\": \"Content-addressed cold storage\", \"rejected_because\": \"Sized for 140+ books; this repo has 14.\"}, {\"option\": \"Live-fetch from upstream URLs at task time\", \"rejected_because\": \"Blocked by a security classifier — a real injection and supply-chain risk.\"}, {\"option\": \"Pre-concatenated ladder file with a stop-early marker\", \"rejected_because\": \"Scored highest, but its stop-early rule has no mechanical consumer.\"}, {\"option\": \"Ad hoc subagent fan-out\", \"rejected_because\": \"Would need to ship as an agent, which ADR-0015 forbids.\"}, {\"option\": \"Panic-grep pre-filter\", \"rejected_because\": \"The critic's starred survivor, deferred rather than bundled into this design.\"}, {\"option\": \"Judgment-gate the security corpus the same symmetric way as books (skip-on-ambiguity)\", \"rejected_because\": \"A missed finding in a compliance-weighted report costs more than a wasted read; security stays biased toward reading in full.\"}]}, \"architecture\": {\"narration\": \"book-index.md becomes the single owner of a three-tier vendored shelf, with an explicit, stated ceiling on how much of it any one task may read. The security corpus gets the same shape of gate without a new file format.\", \"beats\": [{\"kind\": \"component\", \"text\": \"references/books/<name>/{nano,mini,full}.md — 42 files, replacing the current 14 full-only files.\"}, {\"kind\": \"rule\", \"text\": \"book-index.md: load a minimum of 3 nano excerpts; escalate any one book to mini or full only when its principles are judged to matter; full is never automatic.\"}, {\"kind\": \"consumer\", \"text\": \"Design mode Stage 1 (Ground) and Stage 3 (Explore), and Review/Maintenance's corpus-to-book escalation, both read the new rule instead of the old 1+1 cap.\"}, {\"kind\": \"rule\", \"text\": \"SKILL.md's Review mode security step: read the first ~30 lines of all 14 security corpus files unconditionally; read a file's remainder in full unless its summary clearly rules out applicability; ambiguity defaults to full, not to skipping.\"}]}}, \"diagrams\": {\"1\": \"C4Container\\n    title ADR-0021 book-index.md tiering: nano, mini, full escalation\\n\\n    Person(session, \\\"Design or Review/Maintenance session\\\", \\\"Reads book-index.md through its own Read tool calls\\\")\\n\\n    Container_Boundary(t1, \\\"Tier 1 (unchanged)\\\") {\\n        Container(corpus, \\\"corpus-index.md\\\", \\\"vendored YAML + markdown\\\", \\\"Cheap heuristic; narrows candidate books before any book file is read\\\")\\n    }\\n\\n    Container_Boundary(t2, \\\"Tier 2 - book-index.md (this design)\\\") {\\n        Container(index, \\\"book-index.md\\\", \\\"vendored markdown\\\", \\\"Escalation rule: load minimum 3 nano excerpts, then judgment-gate mini/full per book. Replaces the old 1-primary-plus-1-companion cap\\\")\\n        Container(books, \\\"references/books/<name>/{nano,mini,full}.md\\\", \\\"42 vendored files, 14 books\\\", \\\"Three tiers per book: nano ~20-40 lines, mini ~80-150 lines, full 300-1000 lines\\\")\\n    }\\n\\n    Container_Boundary(consumers, \\\"Consumers of the escalation rule\\\") {\\n        Container(design_stage, \\\"design-mode.md Stage 1 (Ground) / Stage 3 (Explore)\\\", \\\"reference\\\", \\\"Grounds and explores using book-index.md's tiered rule\\\")\\n        Container(review_stage, \\\"Review/Maintenance corpus-to-book escalation\\\", \\\"architecture skill mode\\\", \\\"Escalates from corpus-index.md through book-index.md; separate mandatory 14-file security corpus load is untouched by this design\\\")\\n    }\\n\\n    Rel(session, corpus, \\\"Consults first\\\", \\\"Tier 1 heuristic\\\")\\n    Rel(corpus, index, \\\"Narrows candidate books, hands off to\\\")\\n    Rel(design_stage, index, \\\"Invokes escalation rule\\\")\\n    Rel(review_stage, index, \\\"Invokes escalation rule\\\")\\n    Rel(index, books, \\\"Loads >= 3 nano, then judgment-gated mini/full per book\\\")\\n\", \"2\": \"sequenceDiagram\\n    participant Session as Design-mode session (Stage 1: Ground)\\n    participant Corpus as corpus-index.md\\n    participant Index as book-index.md\\n    participant BookA as clean-architecture (nano/mini/full)\\n    participant BookB as domain-driven-design-distilled (nano/mini/full)\\n    participant BookC as refactoring (nano/mini/full)\\n\\n    Session->>Corpus: Consult Tier 1 heuristic for the task\\n    Corpus-->>Session: Narrows candidates to 3 books (A, B, C)\\n    Session->>Index: Escalate per new rule (ADR-0021)\\n\\n    Index->>BookA: Read nano.md (~20-40 lines)\\n    BookA-->>Index: Excerpt returned\\n    Index->>BookB: Read nano.md (~20-40 lines)\\n    BookB-->>Index: Excerpt returned\\n    Index->>BookC: Read nano.md (~20-40 lines)\\n    BookC-->>Index: Excerpt returned\\n\\n    Note over Index: Minimum of 3 nano loads satisfied.\\\\nJudge each book's principles against the task.\\n\\n    Index->>Index: Judge Book A's principles matter for this task\\n    Index->>BookA: Escalate - read mini.md or full.md\\n    BookA-->>Index: Deeper tier content returned\\n\\n    Index->>Index: Judge Book B's principles do not clear the bar\\n    Note over Index,BookB: Book B stays at nano tier. No further Read call.\\n\\n    Index->>Index: Judge Book C's principles do not clear the bar\\n    Note over Index,BookC: Book C stays at nano tier. No further Read call.\\n\\n    Index-->>Session: Grounding = 3 nanos + 1 escalated book (A)\\n\", \"3\": \"classDiagram\\n    class Book {\\n        +name string\\n        +nanoPath string\\n        +miniPath string\\n        +fullPath string\\n        +tierLoaded Tier\\n        +escalated bool\\n    }\\n\\n    class Tier {\\n        <<enumeration>>\\n        NANO\\n        MINI\\n        FULL\\n    }\\n\\n    class EscalationRule {\\n        +minNanoLoads int = 3\\n        +escalationMode string = \\\"judgment-gated\\\"\\n        +cap string = \\\"supersedes prior 1-primary-plus-1-companion cap\\\"\\n        +selectCandidates(corpusResult) Book[]\\n        +loadNano(book) void\\n        +judgePrinciplesMatter(book, task) bool\\n        +escalate(book, targetTier) void\\n    }\\n\\n    class CorpusIndex {\\n        +narrowCandidates(task) Book[]\\n    }\\n\\n    class BookIndex {\\n        +rule EscalationRule\\n        +run(task) GroundingResult\\n    }\\n\\n    class GroundingResult {\\n        +nanoOnlyBooks Book[]\\n        +escalatedBooks Book[]\\n    }\\n\\n    CorpusIndex --> BookIndex : hands off narrowed candidates\\n    BookIndex --> EscalationRule : applies\\n    EscalationRule --> Book : loads/escalates\\n    Book --> Tier : tierLoaded\\n    BookIndex --> GroundingResult : produces\\n\\n    note for Book \\\"escalated=false -> nano-only for rest of task\\\\nescalated=true -> mini or full, judgment-gated per book\\\"\\n    note for EscalationRule \\\"Full-tier loading is never unconditional.\\\\nApplies to Design Stage 1/Stage 3 and\\\\nReview/Maintenance corpus-to-book escalation.\\\\nDoes NOT apply to the separate mandatory\\\\n14-file security corpus load.\\\"\\n\"}, \"pr_draft\": \"## Problem\\n\\n`book-index.md`'s escalation goes straight from the Tier 1 corpus heuristic\\nto one full vendored book, 300-1000 lines. Upstream\\n`ciembor/agent-rules-books` has since published `nano` (about 20-40 lines)\\nand `mini` (about 80-150 lines) tiers for the same 14 books, and\\n`books/README.md`'s manifest still only vendors `full`. Nothing in this\\nplugin uses the smaller tiers. Separately, `books/README.md` documents a\\n`scripts/sync-books.sh` drift-check script that does not exist anywhere in\\nthe repository.\\n\\nThe escalation ladder is missing two rungs that upstream already provides\\nfor free. Every task that reaches Tier 2 today pays the full-book cost with\\nno cheaper option available first.\\n\\n## Why this approach\\n\\nVendor `nano.md` and `mini.md` alongside the existing `full.md` for each of\\nthe 14 books, as separate files (42 total). Change `book-index.md`'s\\nescalation rule: after Tier 1 narrows candidates, load a minimum of three\\nnano-tier excerpts, then escalate any one of those books to mini or full\\nonly when its principles are judged to matter for the task at hand.\\nFull-tier loading is never unconditional. Rewrite the existing \\\"one primary\\nbook plus one optional companion\\\" cap to state this new ceiling explicitly.\\nApplies to Design mode's Stage 1 (Ground) and Stage 3 (Explore) grounding,\\nand to Review/Maintenance mode's corpus-to-book escalation.\\n\\n**Addendum.** Review/Maintenance's mandatory 14-file security corpus load\\ngets the same cheap-signal-before-expensive-read shape, with no vendoring:\\neach YAML already separates its metadata-plus-summary block (~17-24 lines)\\nfrom its worked examples. Read the first ~30 lines of all 14\\nunconditionally, every run, never skipping a category's summary; read a\\nfile's remainder in full unless the summary clearly rules out\\napplicability, defaulting to full on ambiguity — the opposite bias from\\nbook escalation, because a missed security finding costs more than a\\nwasted read.\\n\\n## Alternatives considered\\n\\n- **3 nanos, then always the full book, unconditionally (the engineer's\\n  original approach)** — rejected because making the full-book read\\n  unconditional defeats the reason a cheap tier exists, and it silently\\n  breaks the existing 1+1 cap with no stated replacement.\\n- **Signed evidence ledger gating the full-book read behind three\\n  committed nano entries** — rejected because it solves an auditability\\n  problem nobody named, and adds a new shim and file surface without\\n  reducing token cost or file count.\\n- **Content-addressed cold storage with per-session materialization and\\n  LRU eviction** — rejected because it is sized for a corpus of 140+\\n  books; this repo vendors 14. Conflicts with the plugin family's\\n  vendor-in-tree convention (ADR-0017).\\n- **Live-fetch the nano tier from upstream URLs at task time** — rejected\\n  because the frame that proposed this was blocked by a security\\n  classifier. Fetching remote content into prompt context at runtime is an\\n  injection and supply-chain risk this plugin family's self-only design\\n  avoids.\\n- **Pre-concatenated per-book ladder file with a stop-early marker** —\\n  rejected because its stop-early discipline has no mechanical consumer.\\n  This repo's own CLAUDE.md names exactly this failure mode from the\\n  Gate 4b history, even though this option scored highest with the critic.\\n- **Ad hoc subagent fan-out, one Task-tool call per book, verdict-only\\n  return** — rejected because ADR-0015 fixes the install surface at no\\n  agents, hooks, or MCP servers, and the pattern was declined even as an\\n  in-turn call to keep this design's scope to the vendored-file mechanism.\\n- **Panic-grep pre-filter** — rejected for this design's scope, though it\\n  is the critic's starred non-obvious survivor and the only option that\\n  stays inside the existing cap unmodified. Deferred, not disqualified.\\n- **Judgment-gate the security corpus the same symmetric way as book\\n  escalation (skip-on-ambiguity)** — rejected because a missed security\\n  finding in a compliance-weighted report is a worse failure than a design\\n  session under-reading one book. Security stays biased toward reading in\\n  full.\\n\\n## Out of scope\\n\\n- Fixing or creating `scripts/sync-books.sh`.\\n- Re-vendoring the existing `full/` books against upstream's current\\n  commit.\\n- The panic-grep pre-filter and the subagent fan-out pattern, both\\n  declined for this design.\\n- Vendoring separate nano/mini files for the security corpus — it uses a\\n  same-file partial read instead, since each YAML already separates\\n  metadata/summary from examples.\\n\\n## Risks\\n\\n- The judgment gate (\\\"escalate to mini/full only when it matters\\\") has no\\n  mechanical enforcement. This repo's own Gate 4b history shows an\\n  unenforced step tends to erode under time pressure.\\n- Vendoring surface triples from 14 files to 42, while the sync pipeline\\n  that would check them against upstream (`scripts/sync-books.sh`) is\\n  already missing.\\n- The security corpus applicability read also has no mechanical\\n  enforcement. Its default-to-full bias on ambiguity is meant to make a\\n  false \\\"not applicable\\\" verdict less likely, not impossible.\\n\\n## How this was tested\\n\\nNo code changes; this design writes `docs/` artifacts only. Verification\\nis: `book-index.md`'s escalation rule reads unambiguously, and the new cap\\nlanguage explicitly supersedes the old 1+1 cap rather than leaving both\\nstated at once.\\n\\n## Where to focus\\n\\n- Whether the judgment-gated escalation language in `book-index.md` is\\n  specific enough to actually change agent behavior, or vague enough to be\\n  silently ignored.\\n- Whether the rewritten cap conflicts with any other reference file that\\n  still assumes the old 1+1 cap.\\n- Whether the security corpus's opposite default bias (full-on-ambiguity)\\n  is stated clearly enough that a session doesn't quietly collapse it into\\n  the same skip-on-ambiguity behavior as book escalation.\\n\\nThe author flagged these parts as not fully understood:\\n- Whether upstream's nano/mini files exist for all 14 currently-vendored\\n  books at a syncable commit — not verified in this design.\\n\\n---\\n\\nAuthorship: agent-generated.\\n\"}, \"build-workflow-polish\": {\"goal\": {\"name\": \"build-workflow-polish\", \"title\": \"Fix four friction points found using the plugin family on a real project\", \"created\": \"2026-08-27\", \"outcome\": \"An engineer running architect:design into implement:start never loses the handoff, 04-slices.md is always written in the shape verify_gate.py actually parses, a validated slice is committed without an extra prompt, and the bundle viewer borrows the tab-based Diagram / Data flow / Data types layout a colleague prototyped.\", \"done_when\": [], \"abort_if\": [], \"min_work\": {\"derived_from\": \"Direct feedback from using cobuilder-plugin end to end on a separate real project on 2026-08-26/27. Four independent gaps surfaced, none touching the same file: architect:design's Stage 7 (references/design-mode.md) ends the mode without naming /implement:start as the next step; a session drafted 04-slices.md in a table shape slice_table.py's _ROW_RE_6COL/_ROW_RE_4COL do not match, so verify_gate.py and build_index.py silently failed to read it; build/SKILL.md's 'After each slice' step asks the user whether to commit instead of committing once VALIDATE clears the 0.90 threshold; and a colleague's diagram-viewer.html (scratchpad artifact, not in this repo) demonstrated a tabbed Diagram/Data-flow/Data-types layout with a collapsible left rail worth adopting for viewer/index.html.\", \"challenge_stage_run\": false, \"note\": \"Stage 0 and stage 1 grounding did not run. Stages 2 through 7 are deliberately deferred. This design is a backlog entry, not an approved design. It carries no intent.json, narrative.json, assessment.json, or diagrams, and it must not be implemented until design mode runs to completion for each epic that needs it.\"}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"Stage 7 in architect's references/design-mode.md ends design mode after creating the first branch, with no pointer to what runs next. Add one line after the branch is created: 'Design mode's work ends here. To build this design's epics, run /implement:start.' Do not have design mode invoke implement:start itself — architecture modes are self-only in scope, and implement already owns Gate 1's design-record lookup (build/SKILL.md's 'Optional: design mode record' section). This is a one-line prose addition to a mode-governing reference file, so it still needs the Gate-selection exception's behavioral rubric, not a skip.\"}, {\"id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"04-slices.md's Gate 4a instructions in build/SKILL.md describe the table only through the worked example, and never say the table must match shared/slice_table.py's exact column shape (six columns: #, Epic, Slice, Ends with, Score, State, with the epic-header row's precise empty-cell pattern) or point to that module as the source of truth. Add an explicit sentence and a link, so an authoring session does not freehand a table shape verify_gate.py silently fails to parse. Consider also having verify_gate.py surface parse_table()'s `unparsed` list by name when it is non-empty, instead of only reporting downstream symptoms like a missing epic.\"}, {\"id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"build/SKILL.md's 'After each slice' section (the four numbered steps following the red-green-validate loop) records the score and asks 'Proceed to the next slice, or adjust direction?' but never commits. In practice this produces a spurious 'want to commit this checkpoint first?' prompt after every slice instead of an automatic commit. Change step 2 (or add a step) so that once VALIDATE scores a slice at or above 0.90, the orchestrator commits the slice's changes to git immediately, before asking whether to proceed — no confirmation question for the commit itself, only for what comes after. A score below threshold still routes through the existing gap decision tree in references/validation-scoring.md, and does not commit. Below-threshold slices accepted with reservations after three attempts should also commit, since that path already represents a final accepted state, not a pending one.\"}, {\"id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A colleague's scratch artifact (/private/tmp/.../diagram-viewer.html, session-local, not part of this repo) uses a top-of-panel tab switcher across three views — Diagram, Data flow, Data types — instead of stacking them, plus a right-hand info rail. Evaluate adopting the same top-tab pattern for viewer/index.html's per-PR diagram levels (currently level 1/2/3 switching, per ADR-0020's viewer/src/ plan), and separately evaluate narrowing the left-hand navigation rail so it takes less width once tabs handle top-level switching. This touches only the viewer, so it can run through architect:design on its own without waiting on E1-E3.\"}], \"stage\": \"backlog\", \"supersedes\": null, \"adr\": null, \"adrs\": [], \"rounds\": []}}, \"cobuilder-implement\": {\"goal\": {\"name\": \"cobuilder-implement\", \"title\": \"CoBuilder Implement — a design becomes verified code\", \"created\": \"2026-08-19\", \"outcome\": \"The architecture design produced by cobuilder-architect carries directly into implementation: its intent is decomposed into testable epics, and each epic ships as vertical slices that an independent validator scores at 0.90 or better before the next one starts.\", \"done_when\": [\"the skill runs G1 through G4 and stops at each gate for approval\", \"a slice is scored at 0.90 or better by a validator that executes the tests and never wrote the code\", \"an epic below 0.90 loops back to GREEN and closes\", \"cobuilder-implement builds one real change end to end under its own gates\"], \"abort_if\": [\"0.90 proves unreachable in practice, so most real slices escalate rather than pass, which makes the threshold theatre\"], \"min_work\": {\"derived_from\": \"6 districts (skills, scripts, commands, viewer, docs, .cobuilder-architect), 3 related ADRs, 1 boundary rule on install surface\", \"alternatives_explored\": 5, \"boundary_rules_checked\": true, \"challenge_stage_run\": true}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Shares a branch and a pull request with the design-mode design. See ADR-0013: a branch may carry more than one design.\"}, {\"id\": \"E2\", \"branch\": \"docs/post-pr11-cleanup\", \"pr\": 12, \"state\": \"open\", \"note\": \"Gate 4b is enforced instead of merely specified. The per-epic technical solution design already exists in SKILL.md section 4b, with a six-section template, and five downstream files reference the artifact. Nothing checks that it was written. Checked on 2026-08-24: zero epic-<id>-design.md files exist, and six epics shipped without one. Four failures allow that. Gate 4's only exit check counts rubrics, so a Gate 4 that skips 4b still passes. slice-loop.js builds the epic-design path and never checks the file exists, so the red and green agents are told to read a file that is absent and quietly fall back to 03-program-design.md. rubric-authoring.md tells the author to update the technical design first, and nobody checks that either. No test asserts the artifact. The root cause is that 4b is the only Gate 4 sub-step whose output nothing downstream mechanically requires: the loop cannot score without rubrics or iterate without a slice plan, but it can read an absent design. This epic adds the exit check, the existence guard in the workflow, a test, and sub-step tracking in 00-status.md so an approved Gate 4 cannot hide a missing 4b. Raised 2026-08-22, re-scoped 2026-08-24 from 'write the process' to 'enforce the process' after reading the skill.\"}], \"stage\": \"approved\", \"supersedes\": null, \"adr\": \"ADR-0012\", \"adrs\": [\"ADR-0012\", \"ADR-0011\", \"ADR-0015\"], \"rounds\": [{\"n\": 1, \"changed\": true, \"feedback_class\": \"material\"}]}, \"intent\": {\"problem\": \"Software Factory gates the design, then accepts a slice on the implementer's own word. CoBuilder Guardian scores independently, but runs only on the pi SDK. Neither turns an approved design into verified code in an arbitrary harness, so the designs cobuilder-architect produces have nothing that consumes them.\", \"approach\": \"Three tiers and four gates. An initiative holds epics, a feature is an epic, and every epic must be testable. An epic holds vertical slices. Three separate agents build each slice: one writes failing tests, one writes the minimal code and never edits a test, and one validates. The validator executes the suite, then reads the code and judges whether the tests cover the declared outcomes. A slice needs 0.90 or better. The epic gate is hard, and remediation returns to the implementing agent until it closes.\", \"alternatives\": [{\"option\": \"One agent does the rubric, the tests, the code, and the self-report in a single context, and a human skims the log at the end\", \"rejected_because\": \"It abandons independent scoring, which the outcome requires. The agent that wrote the code judges its own test run, which is the most gameable pattern available.\"}, {\"option\": \"Delete the validator. Get blindness from context amnesia: one agent writes the rubric, the harness clears context, and the same agent implements and self-scores twice, voiding on a mismatch above 0.15\", \"rejected_because\": \"Erasure blindness cannot be verified from outside the model, and two runs of one policy give correlated error rather than independent verification. It assumes away the hard part.\"}, {\"option\": \"Treat rubrics as a cross-repo append-only ledger, cluster criterion fingerprints into a shared genome, and prime the validator with a prior score distribution\", \"rejected_because\": \"It needs a periodic clustering job across repos, which breaks the rule that this skill uses no external service and no pipeline runtime. It also solves a volume problem this work does not have yet.\"}, {\"option\": \"Forbid the validator from reading the implementation at all, and score only from a test runner's exit codes\", \"rejected_because\": \"It scores whether tests pass and never asks whether the tests are worth passing. The validator must also judge test adequacy against the specs, so it reads the code after it executes the suite.\"}, {\"option\": \"Commit a hash of every test file at G3 and check it before scoring\", \"rejected_because\": \"It changes the G3 gate contract for a hole that a validator judging test adequacy already covers. The void triggers widen in prose instead.\"}, {\"option\": \"Score a criterion called 'no undeclared test dependencies' to catch a fixture added after G3\", \"rejected_because\": \"Enumerating a test's transitive dependencies is not free in a dynamically imported language. The same reasoning as the hash check applies.\"}, {\"option\": \"Strip the validator's retry feedback to pass or fail per criterion, so accumulated feedback cannot leak the rubric's shape\", \"rejected_because\": \"Rich feedback is mandatory for GREEN to improve within its attempts. Starving it pushes slices into escalation, which costs more than the leak.\"}], \"authorship\": \"bjoerns, with Claude drafting and challenging\", \"captured\": \"2026-08-19\", \"design\": {\"name\": \"cobuilder-implement\", \"epic\": null}, \"design_name\": \"cobuilder-implement\", \"out_of_scope\": [\"opening a pull request or pushing a branch — that stays submit mode's job in cobuilder-architect\", \"changing design mode, which this design consumes and does not modify\", \"any code dependency between the two plugins in either direction\", \"agents, hooks, and MCP servers, so the plugin never touches another session's permission surface\", \"running the skill on a real initiative, which follows the build\"], \"reviewer_focus\": [\"the validator's mandate — it executes the suite, then reads the code and judges whether the tests cover the declared outcomes\", \"the epic gate and its remediation loop, which has no automatic escalation and relies on a human check-in every three remediations\", \"the light path, which removes the panels for a single-epic initiative but never the slice loop or the 0.90 gate\", \"the join contract path, which is written in two plans and breaks silently if one moves\"], \"risks\": [\"blindness is honour-based: nothing mechanically stops an implementer reading the rubric directory, and detection depends on the validator noticing\", \"at volume, a validator pool statistically absorbs the shape of passing rubrics, so prompt discipline leaks and no filesystem permission was ever there to catch it\", \"just-in-time detailing can redefine a cross-epic contract another epic depends on; the frozen-contract rule is a rule, not a mechanism\", \"the ledger records what agents claim, so a rigged run writes a clean-looking entry; it buys auditability, not integrity\", \"a validator that must judge test adequacy does more work per slice, which raises cost and slows every gate\"], \"source\": \"authored\", \"testing\": \"The skill is prose plus two workflow scripts, so verification is static plus one end-to-end run. Parse both scripts wrapped in an async function, because the runtime wraps them and a bare top-level return fails a plain module check. Confirm every reference named in SKILL.md exists and is reachable. Grep for retired names. Then run the skill against a throwaway feature and confirm it reaches the G3 rubric gate check and stops for approval. The stop condition is self-hosting: it builds one real change end to end under its own gates.\", \"unknowns\": [\"whether 0.90 is reachable often enough in practice that escalation stays the exception\", \"whether a validator judging test adequacy scores consistently enough run to run for the number to mean anything\", \"whether the manual three-role mode degrades acceptably in a harness with no scripted runner\", \"how many remediation rounds a real epic needs before the human check-in fires\"], \"why_now\": \"Design mode shipped, so designs now carry intent that nobody has to reconstruct. The half that turns a design into code does not exist, so the intent stops at the branch name.\"}, \"assessment\": {\"verdict\": \"proceed\", \"findings\": [{\"kind\": \"prediction\", \"id\": \"F1\", \"severity\": \"medium\", \"title\": \"Blindness has no mechanical enforcement\", \"detail\": \"Rubrics live in .cobuilder/rubrics/ inside the repo, so nothing stops an implementing agent reading them. Enforcement is prompt discipline plus a void rule applied after the fact, and detection depends on the validator noticing. The engineer accepted this risk deliberately, choosing in-repo storage over a second repository.\", \"cites\": \"ADR-0012\", \"mitigation\": \"Three layers rather than one: the prompt forbids the read, the implementer never receives the rubric path, and a detected read voids the score at no attempt cost instead of lowering it.\"}, {\"kind\": \"prediction\", \"id\": \"F2\", \"severity\": \"high\", \"title\": \"The join contract path is written in two plans and can break silently\", \"detail\": \"docs/architecture/designs/<name>/intent.json is the only coupling between cobuilder-architect and cobuilder-implement. It appears in this design and in the design-mode plan. A move in one without the other leaves the reader plugin looking at an absent file, and the failure mode is a silent fallback to drafting a Business Spec from scratch rather than an error.\", \"cites\": \"ADR-0013\", \"mitigation\": \"Record the coupling as a constraint introduced in the ADR, so a future session changing either plan is told the other moves with it. A missing file must report that it looked and found nothing, never fall through silently.\"}, {\"kind\": \"prediction\", \"id\": \"F3\", \"severity\": \"medium\", \"title\": \"Cross-epic contracts freeze by rule, not by mechanism\", \"detail\": \"Just-in-time Technical Specs only work when epics are separable. A skeleton names the contracts an epic exposes or consumes, and those freeze at G1. Nothing checks that a later detailing pass honoured them, and nothing checks that the skeleton named them honestly in the first place.\", \"cites\": \"district: docs\", \"mitigation\": \"A change to a frozen contract is a backtrack to G1 with re-approval. The rule is stated, and the risk of an unhonest skeleton stays open.\"}, {\"kind\": \"prediction\", \"id\": \"F4\", \"severity\": \"medium\", \"title\": \"The validator now carries two jobs, and the second is subjective\", \"detail\": \"The validator executes the suite, then reads the code and judges whether the tests are sensible and cover the declared outcomes. Execution is objective. Test adequacy is judgment, and judgment scored on a three-value scale may not repeat run to run. If the second job scores inconsistently, the 0.90 number stops meaning one thing.\", \"cites\": \"ADR-0012\", \"mitigation\": \"Every criterion cites evidence — a test name, captured output, or a file and line. A score without evidence does not count. Consistency is listed as an unknown to be measured on real slices, not assumed.\"}, {\"kind\": \"prediction\", \"id\": \"F5\", \"severity\": \"low\", \"title\": \"The ledger records claims, not facts\", \"detail\": \"An append-only record of each scored slice, with the validator's stated reason per criterion, makes a stalled initiative resumable and makes a score explicable. It does not make a score true. A rigged run writes a plausible entry.\", \"cites\": \"district: docs\", \"mitigation\": \"State the limit where the ledger is defined. It buys auditability, and it is not a fix for validation integrity.\"}, {\"kind\": \"prediction\", \"id\": \"F6\", \"severity\": \"low\", \"title\": \"Ceremony may exceed the work on small initiatives\", \"detail\": \"A multi-epic initiative runs one refute panel, plus a research panel and a skeptic per epic, before the first slice starts. On a two-day feature that is heavier than the feature.\", \"cites\": \"ADR-0012\", \"mitigation\": \"The light path collapses a single-epic initiative to one spec and removes the panels, the skeptic, and the epic rubric. The slice loop and the 0.90 gate never switch off.\"}], \"boundary_checks\": [\"no agents, no hooks, and no MCP servers, so the plugin never touches another session's permission surface\", \"no code dependency on cobuilder-architect in either direction; the join is one file\", \"no external service and no pipeline runtime; the manual three-role mode is the portable fallback\"], \"drift\": [], \"regret_risk\": \"low\", \"risk_tier\": \"medium\", \"stage\": \"design\", \"summary\": \"The design is internally consistent and its constraints are honest. Every finding below is a prediction, because no code exists yet. The two that matter are the honour-based blindness the engineer accepted knowingly, and the cross-plan contract path that breaks silently if one plan moves without the other.\"}, \"narrative\": {\"landscape\": {\"tagline\": \"A design becomes verified code, one slice at a time.\", \"narration\": \"cobuilder-architect can now capture a design before any code exists, but nothing on the other side of that record turns it into working software under independent checks. This design adds the missing half: four approval gates before implementation starts, and a validator that never wrote the code deciding whether each slice is actually done.\"}, \"problem_solution\": {\"problem\": \"Software Factory gates a design, then accepts a finished slice on the implementer's own word. CoBuilder Guardian scores independently, but it only runs on the pi SDK, DOT pipelines, beads, and Hindsight. Neither one takes an approved architecture design and turns it into verified code in an arbitrary coding harness, so the designs `cobuilder-architect` now produces have nothing that consumes them.\", \"solution\": \"Three tiers and four gates. An initiative holds epics, a feature is an epic, and every epic must be testable. An epic holds vertical slices. Three separate agents build each slice: one writes failing tests, one writes the minimal code and never edits a test, and one validates. The validator executes the suite, then reads the code and judges whether the tests cover the declared outcomes. A slice needs a score of `0.90` or better. The epic gate is hard, and remediation returns to the implementing agent until it closes.\", \"narration\": \"Design mode already writes down what a change is for and why, before the change exists. This design gives that record somewhere to go. It borrows the gate discipline of Software Factory, which settles every hard decision in prose while it is still cheap to change, and it borrows the independent scoring of CoBuilder Guardian, which never lets the agent that wrote the code grade its own test run. Joining the two means a design can walk all the way to working software, with a validator checking every step who was never shown the code while writing the checklist.\", \"beats\": [{\"kind\": \"background\", \"text\": \"`cobuilder-architect`'s design mode shipped first, so a design now carries intent, alternatives, and constraints that nobody has to reconstruct later (ADR-0011). ADR-0015 then made the architecture modes self-only, which means a build workflow cannot live inside `cobuilder-architect` itself. The design that mode produces has nothing downstream that reads it and turns it into code.\"}, {\"kind\": \"background\", \"text\": \"Software Factory's weakness sits at the end of a slice: the agent that built the slice proves it works by running it and showing the result, so the same agent decides what counts as proof. CoBuilder Guardian answers that weakness by writing acceptance rubrics before implementation starts, storing them where the implementer cannot read them, and scoring the result with an agent that did not write the code. Its weakness is the runtime: it needs the pi SDK, DOT pipelines, beads, and Hindsight, none of which runs in another harness.\"}, {\"kind\": \"intuition\", \"text\": \"Take a single epic with three slices. Slice one gets a rubric written before any code exists, hidden from the implementer. The GREEN agent writes the minimal code, the validator runs the test suite it never touched, reads the resulting code, and scores it against the rubric. A score of `0.85` sends the slice back to GREEN with the validator's stated reasons; a score of `0.90` or better closes it and starts the next slice. Nothing in that loop lets the agent that wrote the code also decide it is finished.\"}], \"alternatives\": [{\"option\": \"One agent does the rubric, the tests, the code, and the self-report in a single context, and a human skims the log at the end\", \"rejected\": \"It abandons independent scoring, which the outcome requires. The agent that wrote the code judges its own test run, which is the most gameable pattern available.\"}, {\"option\": \"Delete the validator. Get blindness from context amnesia: one agent writes the rubric, the harness clears context, and the same agent implements and self-scores twice, voiding on a mismatch above 0.15\", \"rejected\": \"Erasure blindness cannot be verified from outside the model, and two runs of one policy give correlated error rather than independent verification. It assumes away the hard part.\"}, {\"option\": \"Treat rubrics as a cross-repo append-only ledger, cluster criterion fingerprints into a shared genome, and prime the validator with a prior score distribution\", \"rejected\": \"It needs a periodic clustering job across repos, which breaks the rule that this skill uses no external service and no pipeline runtime. It also solves a volume problem this work does not have yet.\"}, {\"option\": \"Forbid the validator from reading the implementation at all, and score only from a test runner's exit codes\", \"rejected\": \"It scores whether tests pass and never asks whether the tests are worth passing. The validator must also judge test adequacy against the specs, so it reads the code after it executes the suite.\"}, {\"option\": \"Commit a hash of every test file at G3 and check it before scoring\", \"rejected\": \"It changes the G3 gate contract for a hole that a validator judging test adequacy already covers. The void triggers widen in prose instead.\"}, {\"option\": \"Score a criterion called 'no undeclared test dependencies' to catch a fixture added after G3\", \"rejected\": \"Enumerating a test's transitive dependencies is not free in a dynamically imported language. The same reasoning as the hash check applies.\"}, {\"option\": \"Strip the validator's retry feedback to pass or fail per criterion, so accumulated feedback cannot leak the rubric's shape\", \"rejected\": \"Rich feedback is mandatory for GREEN to improve within its attempts. Starving it pushes slices into escalation, which costs more than the leak.\"}]}, \"architecture\": {\"narration\": \"No plugin here takes a code dependency on another. `cobuilder-implement` joins `cobuilder-architect` through one file, `docs/architecture/designs/<name>/intent.json`, and joins nothing else. The install surface stays fixed: no agents, no hooks, no MCP servers, so the plugin never touches another session's permission surface. Everything else — the gates, the rubric store, the validator's mandate — is prose plus two workflow scripts that any harness able to spawn a subagent can run.\", \"beats\": [{\"kind\": \"forces\", \"text\": \"The skill must run in any harness that can spawn a subagent, with no SDK, no pipeline runtime, and no external service. An implementing agent that reads the rubric writes to the rubric, not to the requirement. An agent that wrote code cannot score it, because it reads its own intent into the code. ADR-0015 makes the architecture modes self-only, so a build workflow cannot live inside `cobuilder-architect`.\"}, {\"kind\": \"contract\", \"text\": \"`docs/architecture/designs/<name>/intent.json` is the only coupling between the two plugins, in either direction. `cobuilder-implement` reads it as the hypothesis for the initiative it builds. No shared code, no shared runtime, and no arrow from either plugin to the other's files.\"}, {\"kind\": \"boundary\", \"text\": \"Blindness is honour-based inside one repository: rubrics live in `.cobuilder/rubrics/`, and nothing mechanically stops an implementing agent from reading them. Enforcement is three layers — the prompt forbids the read, the implementer never receives the rubric path, and a detected read voids the score at no attempt cost rather than lowering it. Opening a pull request or pushing a branch stays submit mode's job in `cobuilder-architect`, never this plugin's.\"}]}}, \"diagrams\": {\"1\": \"C4Container\\n    title \\\"cobuilder-implement design: two plugins joined by one file, no code dependency\\\"\\n\\n    Person(engineer, \\\"Engineer\\\", \\\"Approves gates, runs both plugins in the same coding harness\\\")\\n\\n    Container_Boundary(target, \\\"Target repo\\\") {\\n        Container(intentfile, \\\"intent.json\\\", \\\"JSON file\\\", \\\"docs/architecture/designs/<name>/intent.json — the only join between the two plugins\\\")\\n    }\\n\\n    Container_Boundary(architect, \\\"cobuilder-architect plugin — designs\\\") {\\n        Container(designmode, \\\"design mode\\\", \\\"skill\\\", \\\"Interviews the engineer, writes intent.json and the ADR before code exists\\\")\\n    }\\n\\n    Container_Boundary(implement, \\\"cobuilder-implement plugin — builds\\\") {\\n        Container(gates, \\\"G1-G4 gates\\\", \\\"skill\\\", \\\"Business Spec, Technical Spec, Slice Plan and Rubrics, Acceptance\\\")\\n        Container(validator, \\\"VALIDATE subagent\\\", \\\"subagent\\\", \\\"Executes tests, then reads code, scores against a blind rubric\\\")\\n    }\\n\\n    Rel(engineer, designmode, \\\"Runs design mode on\\\")\\n    Rel(designmode, intentfile, \\\"Writes\\\")\\n    Rel(engineer, gates, \\\"Runs G1 to start build on\\\")\\n    Rel(gates, intentfile, \\\"Reads at G1 to ground the Business Spec\\\")\\n    Rel(gates, validator, \\\"Spawns per slice\\\")\\n\\n    UpdateRelStyle(designmode, intentfile, $offsetX=\\\"10\\\", $offsetY=\\\"-10\\\")\\n    UpdateRelStyle(gates, intentfile, $offsetX=\\\"-10\\\", $offsetY=\\\"10\\\")\\n\", \"2\": \"sequenceDiagram\\n    participant Orchestrator\\n    participant RED as RED subagent\\n    participant GREEN as GREEN subagent\\n    participant VALIDATE as VALIDATE subagent\\n    participant Suite as Test suite (fresh process)\\n    participant Ledger as Per-epic ledger\\n\\n    Orchestrator->>RED: Spawn for this slice\\n    RED->>RED: Write failing tests against the blind rubric's declared outcomes\\n    RED-->>Orchestrator: Failing tests committed\\n\\n    Orchestrator->>GREEN: Spawn with failing tests\\n    GREEN->>GREEN: Write minimal code, never edit a test\\n    GREEN-->>Orchestrator: Implementation ready\\n\\n    Orchestrator->>VALIDATE: Spawn to score the slice\\n    VALIDATE->>Suite: Execute the suite in a fresh process\\n    Suite-->>VALIDATE: Pass/fail results, no claimed result accepted\\n    VALIDATE->>VALIDATE: Read the code and the tests, judge adequacy against specs\\n\\n    alt Score >= 0.90, no critical criterion below 1.0\\n        VALIDATE->>Ledger: Append per-criterion score and stated reason\\n        VALIDATE-->>Orchestrator: Slice accepted\\n    else Score below 0.90\\n        VALIDATE->>Ledger: Append per-criterion score and stated reason\\n        VALIDATE-->>Orchestrator: Return to GREEN with full feedback\\n        Orchestrator->>GREEN: Retry (up to 3 attempts total)\\n        Note over Orchestrator,GREEN: After 3 attempts, escalate to a human check-in\\n    else Rigged pass detected (edited/skipped/weakened test, swapped dependency, rubric read)\\n        VALIDATE->>Ledger: Append VOID and stated reason\\n        VALIDATE-->>Orchestrator: Void, no attempt cost\\n        Orchestrator->>RED: Return to RED to rewrite the tests\\n    end\\n\", \"3\": \"classDiagram\\n    class Initiative {\\n        +businessSpec BusinessSpec\\n        +epics Epic[]\\n    }\\n\\n    class BusinessSpec {\\n        <<visible>>\\n        +problem string\\n        +epicList string[]\\n    }\\n\\n    class Epic {\\n        +name string\\n        +mustBeTestable bool\\n        +technicalSpec TechnicalSpec\\n        +epicRubric Rubric\\n        +slices Slice[]\\n    }\\n\\n    class TechnicalSpec {\\n        <<visible>>\\n        +writtenJustInTime bool\\n        +researchVerified bool\\n    }\\n\\n    class Slice {\\n        +blindRubric Rubric\\n        +score float\\n        +attempts int\\n        +maxAttempts int\\n    }\\n\\n    class Rubric {\\n        <<blind, implementer never reads it>>\\n        +criteria Criterion[]\\n    }\\n\\n    class Criterion {\\n        +score float\\n        +evidence string\\n        +critical bool\\n    }\\n\\n    class LedgerEntry {\\n        <<append-only>>\\n        +epic string\\n        +slice string\\n        +perCriterionScore float\\n        +validatorReason string\\n    }\\n\\n    class G1_BusinessSpec {\\n        <<gate, once>>\\n        +producesEpics() void\\n    }\\n\\n    class G2_TechnicalSpec {\\n        <<gate, per epic>>\\n        +producesTechnicalSpec() void\\n    }\\n\\n    class G3_SlicePlanAndRubrics {\\n        <<gate, per epic>>\\n        +producesSliceList() void\\n        +producesBlindRubrics() void\\n    }\\n\\n    class G4_Acceptance {\\n        <<gate, per slice then per epic>>\\n        +threshold float\\n    }\\n\\n    Initiative \\\"1\\\" --> \\\"*\\\" Epic : decomposes into\\n    Epic \\\"1\\\" --> \\\"1\\\" TechnicalSpec : holds\\n    Epic \\\"1\\\" --> \\\"1\\\" Rubric : holds epic rubric\\n    Epic \\\"1\\\" --> \\\"*\\\" Slice : holds\\n    Slice \\\"1\\\" --> \\\"1\\\" Rubric : holds blind rubric\\n    Rubric \\\"1\\\" --> \\\"*\\\" Criterion : contains\\n    Slice \\\"1\\\" --> \\\"*\\\" LedgerEntry : scored into\\n\\n    G1_BusinessSpec ..> Initiative : attaches to\\n    G1_BusinessSpec ..> Epic : attaches to\\n    G2_TechnicalSpec ..> TechnicalSpec : attaches to\\n    G3_SlicePlanAndRubrics ..> Slice : attaches to\\n    G3_SlicePlanAndRubrics ..> Rubric : attaches to\\n    G4_Acceptance ..> Slice : attaches to\\n    G4_Acceptance ..> Epic : attaches to\\n\\n    note for Rubric \\\"BLIND: the implementer never reads Rubric or Criterion.\\\\nBusinessSpec and TechnicalSpec stay visible.\\\"\\n\"}, \"pr_draft\": \"# CoBuilder Implement — a design becomes verified code\\n\\n## Problem\\n\\nSoftware Factory gates the design, then accepts a slice on the implementer's own word. CoBuilder Guardian scores independently, but runs only on the pi SDK. Neither turns an approved design into verified code in an arbitrary harness, so the designs cobuilder-architect produces have nothing that consumes them.\\n\\nDesign mode shipped, so designs now carry intent that nobody has to reconstruct. The half that turns a design into code does not exist, so the intent stops at the branch name.\\n\\n## Why this approach\\n\\nThree tiers and four gates. An initiative holds epics, a feature is an epic, and every epic must be testable. An epic holds vertical slices. Three separate agents build each slice: one writes failing tests, one writes the minimal code and never edits a test, and one validates. The validator executes the suite, then reads the code and judges whether the tests cover the declared outcomes. A slice needs 0.90 or better. The epic gate is hard, and remediation returns to the implementing agent until it closes.\\n\\n## Alternatives considered\\n\\n- **One agent does the rubric, the tests, the code, and the self-report in a single context, and a human skims the log at the end** — rejected because it abandons independent scoring, which the outcome requires. The agent that wrote the code judges its own test run, which is the most gameable pattern available.\\n- **Delete the validator. Get blindness from context amnesia: one agent writes the rubric, the harness clears context, and the same agent implements and self-scores twice, voiding on a mismatch above 0.15** — rejected because erasure blindness cannot be verified from outside the model, and two runs of one policy give correlated error rather than independent verification. It assumes away the hard part.\\n- **Treat rubrics as a cross-repo append-only ledger, cluster criterion fingerprints into a shared genome, and prime the validator with a prior score distribution** — rejected because it needs a periodic clustering job across repos, which breaks the rule that this skill uses no external service and no pipeline runtime. It also solves a volume problem this work does not have yet.\\n- **Forbid the validator from reading the implementation at all, and score only from a test runner's exit codes** — rejected because it scores whether tests pass and never asks whether the tests are worth passing. The validator must also judge test adequacy against the specs, so it reads the code after it executes the suite.\\n- **Commit a hash of every test file at G3 and check it before scoring** — rejected because it changes the G3 gate contract for a hole that a validator judging test adequacy already covers. The void triggers widen in prose instead.\\n- **Score a criterion called 'no undeclared test dependencies' to catch a fixture added after G3** — rejected because enumerating a test's transitive dependencies is not free in a dynamically imported language. The same reasoning as the hash check applies.\\n- **Strip the validator's retry feedback to pass or fail per criterion, so accumulated feedback cannot leak the rubric's shape** — rejected because rich feedback is mandatory for GREEN to improve within its attempts. Starving it pushes slices into escalation, which costs more than the leak.\\n\\n## Out of scope\\n\\n- opening a pull request or pushing a branch — that stays submit mode's job in cobuilder-architect\\n- changing design mode, which this design consumes and does not modify\\n- any code dependency between the two plugins in either direction\\n- agents, hooks, and MCP servers, so the plugin never touches another session's permission surface\\n- running the skill on a real initiative, which follows the build\\n\\n## Risks\\n\\n- blindness is honour-based: nothing mechanically stops an implementer reading the rubric directory, and detection depends on the validator noticing\\n- at volume, a validator pool statistically absorbs the shape of passing rubrics, so prompt discipline leaks and no filesystem permission was ever there to catch it\\n- just-in-time detailing can redefine a cross-epic contract another epic depends on; the frozen-contract rule is a rule, not a mechanism\\n- the ledger records what agents claim, so a rigged run writes a clean-looking entry; it buys auditability, not integrity\\n- a validator that must judge test adequacy does more work per slice, which raises cost and slows every gate\\n\\n## How this was tested\\n\\nThe skill is prose plus two workflow scripts, so verification is static plus one end-to-end run. Parse both scripts wrapped in an async function, because the runtime wraps them and a bare top-level return fails a plain module check. Confirm every reference named in SKILL.md exists and is reachable. Grep for retired names. Then run the skill against a throwaway feature and confirm it reaches the G3 rubric gate check and stops for approval. The stop condition is self-hosting: it builds one real change end to end under its own gates.\\n\\n## Where to focus\\n\\n- the validator's mandate — it executes the suite, then reads the code and judges whether the tests cover the declared outcomes\\n- the epic gate and its remediation loop, which has no automatic escalation and relies on a human check-in every three remediations\\n- the light path, which removes the panels for a single-epic initiative but never the slice loop or the 0.90 gate\\n- the join contract path, which is written in two plans and breaks silently if one moves\\n\\nThe author flagged these parts as not fully understood:\\n\\n- whether 0.90 is reachable often enough in practice that escalation stays the exception\\n- whether a validator judging test adequacy scores consistently enough run to run for the number to mean anything\\n- whether the manual three-role mode degrades acceptably in a harness with no scripted runner\\n- how many remediation rounds a real epic needs before the human check-in fires\\n\"}, \"design-mode\": {\"goal\": {\"name\": \"design-mode\", \"title\": \"Design mode for cobuilder-architect\", \"created\": \"2026-08-19\", \"outcome\": \"An engineer can design a change with the plugin before writing it, and the resulting intent, ADR, and diagrams carry through to the pull request that implements it.\", \"done_when\": [\"/cobuilder-architect:design runs the seven stages and writes a design directory\", \"submit mode resolves a design from the branch name and skips the cold interview\", \"the viewer lists and renders designs alongside the PR timeline\"], \"abort_if\": [\"the challenge stage cannot be made to produce rejected options reliably, leaving every ADR with a single considered option\", \"the architecture skill cannot be merged, leaving design mode without a corpus to reason with\"], \"min_work\": {\"derived_from\": \"3 districts (skills, scripts, viewer), 2 colliding ADRs, 1 boundary rule on install surface\", \"alternatives_explored\": 3, \"boundary_rules_checked\": true, \"challenge_stage_run\": true}, \"limits\": {\"warn_after_rounds\": 3, \"cutoff_rounds\": 6}, \"epics\": [{\"id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Design mode as a pre-code interview and challenge gate. Shares a branch and a pull request with the cobuilder-implement design. See ADR-0013: a branch may carry more than one design.\"}], \"stage\": \"implemented\", \"supersedes\": null, \"adr\": \"ADR-0011\", \"adrs\": [\"ADR-0011\", \"ADR-0014\", \"ADR-0015\"], \"rounds\": [{\"n\": 1, \"changed\": true, \"feedback_class\": \"material\"}, {\"n\": 2, \"changed\": true, \"feedback_class\": \"material\"}, {\"n\": 3, \"changed\": true, \"feedback_class\": \"cosmetic\"}]}, \"intent\": {\"problem\": \"The plugin captures author intent at submit time, after the change exists. Nothing captures it at design time, when the engineer still holds the alternatives, the constraints, and the doubts. Generate mode then reconstructs intent from merged code, which is the guessing the plugin was built to stop.\", \"approach\": \"A seventh mode, /prodyssey:design, running seven stages: name and outcome, ground, interview, explore, challenge, draft, review, branch. It writes a named design directory under exports/, never the timeline, and ends by creating a branch that submit mode later resolves by name.\", \"alternatives\": [{\"option\": \"Put design mode in archkit instead, next to its existing design mode\", \"rejected_because\": \"The interview discipline, the bundle, the diagram pipeline, and the viewer all live in prodyssey. archkit's contribution is a skill call. Building it in archkit would mean porting four subsystems to reach one.\"}, {\"option\": \"Keep archkit a soft, optional dependency and degrade when it is absent\", \"rejected_because\": \"A design mode whose substance is optional degrades silently into a chat. The corpus and the divergent exploration are the design content, not an enhancement to it.\"}, {\"option\": \"Ask the engineer which options they rejected, as an interview question\", \"rejected_because\": \"Nobody can name a rejected option before the design exists. A rejected option is an outcome of the design process, not an input to it. Stages 3 and 4 produce them instead.\"}, {\"option\": \"Mint a synthetic PR key so a design can enter the story.json timeline directly\", \"rejected_because\": \"story.json keys on an integer pr, and verify_bundle.py, record_publish.py, manifest.js, and the viewer all depend on it. Submit mode already stages outside the timeline for this reason.\"}, {\"option\": \"Review designs in a Lavish surface rather than the bundle viewer\", \"rejected_because\": \"Element-level annotation is a better review loop, but it adds an external dependency to a plugin that deliberately has none. The viewer already renders assessments and is committed with the repo.\"}], \"authorship\": \"agent-assisted\", \"captured\": \"2026-08-19\", \"design_name\": \"design-mode\", \"out_of_scope\": [\"opening a pull request — that stays submit mode's job\", \"pushing a branch or any other remote action\", \"element-level annotation or a send-to-agent channel in the viewer\", \"renaming the repository\", \"deciding the fate of the archkit repository\"], \"reviewer_focus\": [\"stage 4, the challenge gate — it is the sole producer of rejected options\", \"section 3, the branch-name lookup that joins a design to its pull request\", \"the CLAUDE.md passages that the merge deliberately breaks\"], \"risks\": [\"the agreeable design: exploration finds nothing contradicting the engineer, and the ADR records one choice instead of a decision\", \"a design-stage prediction read as a verified observation, because it cites an ADR id rather than a path:line\", \"the design and its branch diverge silently after stage 7\", \"the duplicate-design subagent reports no match and the engineer trusts it\"], \"source\": \"design\", \"testing\": \"Not yet built. This design directory is itself the first test of the layout: it was authored by hand against the proposed shape.\", \"unknowns\": [\"whether a semantic duplicate-design search is accurate enough that an engineer can trust a no-match answer\", \"how much the 3.1 MB architecture skill slows a plugin install in practice\", \"whether material and cosmetic review feedback can be classified reliably enough to route automatically\"], \"why_now\": \"Submit mode's intent block already has the right shape for a design. The bundle already holds districts, ADRs, and a timeline to design against. The archkit architecture skill already holds the corpus and the divergent exploration that a design needs. The three parts exist and are not connected.\"}, \"assessment\": {\"verdict\": \"proceed\", \"findings\": [{\"kind\": \"observation\", \"id\": \"F1\", \"severity\": \"low\", \"title\": \"Design mode shipped and moved, as goal.json's outcome describes\", \"detail\": \"plugins/cobuilder-architect/commands/design.md exists and dispatches to the architecture skill. No skills/odyssey directory or root-level commands/ directory exists anywhere in the tree today. The mode is not present under plugins/cobuilder-pr, which now ships five modes (baseline, review, generate, view, publish) with no design mode among them. This matches CLAUDE.md's Recent history entry: design mode moved from the odyssey skill into the architecture skill, leaving odyssey with five modes.\", \"cites\": \"plugins/cobuilder-architect/commands/design.md; absence of skills/odyssey and commands/ at the repository root\", \"mitigation\": \"None needed. This is the shipped, intended state.\"}, {\"kind\": \"observation\", \"id\": \"F2\", \"severity\": \"low\", \"title\": \"commands/explore-design.md is confirmed deleted\", \"detail\": \"No file named explore-design.md exists anywhere in the current tree. git log on docs/architecture/designs/design-mode/adr-draft.md and the surrounding commit history are consistent with CLAUDE.md's Recent history note that explore-design.md was deleted as a duplicate of design.md.\", \"cites\": \"find . -iname 'explore-design*' returns no results\", \"mitigation\": \"None needed.\"}, {\"kind\": \"drift\", \"id\": \"F3\", \"severity\": \"medium\", \"title\": \"ADR-0011's maps_to.modules names two paths that predate the five-plugin split\", \"detail\": \"ADR-0011's frontmatter lists maps_to.modules as skills/odyssey and commands. Neither path exists today. The command that implements this decision lives at plugins/cobuilder-architect/commands/design.md, and the skill it dispatches to is plugins/cobuilder-architect/skills/architecture/. The ADR was decided on 2026-08-19, before ADR-0016 and ADR-0017 split the single plugin into five and moved every root-level skills/ and commands/ directory into per-plugin roots. Nothing updated the ADR's maps_to block afterward.\", \"cites\": \"docs/architecture/adr/ADR-0011-design-mode.md, maps_to.modules; plugins/cobuilder-architect/commands/design.md\", \"mitigation\": \"Update ADR-0011's maps_to.modules to plugins/cobuilder-architect/commands and plugins/cobuilder-architect/skills/architecture, or mark the field historical and add a current-location note.\"}, {\"kind\": \"drift\", \"id\": \"F4\", \"severity\": \"low\", \"title\": \"intent.json's approach paragraph names a dropped plugin name and a rejected output location\", \"detail\": \"intent.json's approach field reads: \\\"A seventh mode, /prodyssey:design ... It writes a named design directory under exports/, never the timeline.\\\" The plugin was renamed from prodyssey to cobuilder-architect, and the shipped command is /cobuilder-architect:design. The shipped output location is docs/architecture/designs/<name>/, per goal.json's done_when and ADR-0011's decision text, not exports/. This sentence was captured on 2026-08-19, the same day as the ADR, and reads as an early draft overtaken by the decision recorded in the same design.\", \"cites\": \"docs/architecture/designs/design-mode/intent.json, approach field; docs/architecture/designs/design-mode/goal.json, done_when[0]\", \"mitigation\": \"Either update the approach sentence to match the shipped command and path, or add a note that intent.json is a point-in-time capture and the ADR supersedes it on naming and location.\"}, {\"kind\": \"observation\", \"id\": \"F5\", \"severity\": \"low\", \"title\": \"The design's pr-draft.md is now projected into the record index\", \"detail\": \"shared/build_index.py reads pr_draft_path = path.parent / 'pr-draft.md' at line 431, inside the function that walks each design directory. This design directory has no pr-draft.md of its own to project, but the code path that was missing is present in the script this branch ships.\", \"cites\": \"shared/build_index.py:431\", \"mitigation\": \"None needed for this design. Worth reconfirming the same line exists in each plugin's vendored shared/build_index.py copy before this fix is treated as universal.\"}, {\"kind\": \"observation\", \"id\": \"F6\", \"severity\": \"low\", \"title\": \"adr-draft.md is gone, consistent with promotion to ADR-0011\", \"detail\": \"docs/architecture/designs/design-mode/ holds no adr-draft.md today. The file was deleted on 2026-08-25, as part of the change on this branch that retires adr-draft.md as a documented artifact type, with the matching line removed from CLAUDE.md and README.md in the same change. The draft was ADR-0011, already promoted to docs/architecture/adr/ADR-0011-design-mode.md. It was stale on the same two counts F4 records for intent.json: it named /prodyssey:design and it named exports/ as the output location. git status --short confirms the deletion is uncommitted at the time of writing.\", \"cites\": \"git log --oneline -- docs/architecture/designs/design-mode/adr-draft.md\", \"mitigation\": \"None needed. Flagging only the unverified deletion commit for completeness.\"}, {\"kind\": \"observation\", \"id\": \"F7\", \"severity\": \"medium\", \"title\": \"Epic E1's pull request is open, and the index cannot resolve that state\", \"detail\": \"goal.json.epics[0] declares pr: 11 and state: 'open'. gh pr view 11 confirms pull request 11 is OPEN on GitHub today, so the declared state matches reality. But shared/build_index.py's collect_pull_requests() (line 641) reads only entries already present in data/story.json's timeline, which holds narrated, merged pull requests. refine_epic_status() (line 791) looks up each epic's pull request number in that same narrowed set and only overwrites the placeholder status when a match exists. Pull request 11 has not been narrated, so no match exists, and the index cannot derive its open state from the bundle alone. The goal.json field is the only place that state is recorded correctly right now.\", \"cites\": \"shared/build_index.py:641-665 (collect_pull_requests), shared/build_index.py:791-797 (refine_epic_status); goal.json epics[0]; gh pr view 11\", \"mitigation\": \"Either narrate pull request 11 once it is available to review mode, so the index can pick up its real state, or extend refine_epic_status to fall back to a live gh pr view lookup for epics whose pull request never entered the timeline.\"}], \"boundary_checks\": [{\"rule\": \"No agents, no hooks, no MCP servers in the install surface\", \"result\": \"holds\", \"note\": \"Design mode is a skill dispatch from a command file, the same shape as every other architecture mode.\"}, {\"rule\": \"Architecture modes are self-only and take no --repo\", \"result\": \"holds\", \"note\": \"plugins/cobuilder-architect/commands/design.md carries no --repo handling, consistent with the other five architecture commands.\"}, {\"rule\": \"A design lives under docs/architecture/designs/<name>/ and never enters story.json until a real PR number exists\", \"result\": \"holds\", \"note\": \"This design's own directory is exactly that shape, and its pull request, 11, is a real number returned by gh pr create, not a synthetic key.\"}], \"drift\": [{\"kind\": \"drift\", \"title\": \"ADR-0011's maps_to.modules names pre-split paths\", \"detail\": \"See F3. skills/odyssey and commands no longer exist as top-level paths after the five-plugin split (ADR-0016, ADR-0017). The ADR's own maps_to block was never updated to name plugins/cobuilder-architect/commands and plugins/cobuilder-architect/skills/architecture.\"}, {\"kind\": \"drift\", \"title\": \"intent.json still names the pre-rename plugin and a rejected output root\", \"detail\": \"See F4. The approach field says /prodyssey:design writing under exports/. The shipped command is /cobuilder-architect:design, writing under docs/architecture/designs/<name>/.\"}, {\"kind\": \"drift\", \"title\": \"Epic E1's real-world pull request state is invisible to the generated index\", \"detail\": \"See F7. Pull request 11 is open on GitHub and correctly recorded as such in goal.json, but shared/build_index.py can only refine an epic's status from pull requests already narrated into data/story.json's timeline. An open, un-narrated pull request leaves the index with only its initial placeholder, not the live GitHub state.\"}], \"regret_risk\": \"low\", \"risk_tier\": \"medium\", \"stage\": \"retrospective\", \"summary\": \"This assessment was written on 2026-08-25, after design mode shipped, because the file was found missing from the design directory. It is a retrospective, not a design-stage prediction. Every finding below states what a check against the tree today found, not what an author foresaw before the code existed. Design mode shipped, moved from the odyssey skill into the architecture skill, and its command now lives at plugins/cobuilder-architect/commands/design.md. The design's own record carries two stale paths from before the five-plugin split, and its lone epic sits in a state the index cannot yet resolve because its pull request is open, not merged.\"}, \"narrative\": {\"landscape\": {\"narration\": \"This design adds a seventh Odyssey mode so an engineer can record intent, alternatives, and constraints before any code exists, then carry that record through to the pull requests that implement it.\", \"tagline\": \"Design before the change exists.\"}, \"problem_solution\": {\"narration\": \"Submit mode already captures intent, but only after the change exists. The alternatives are already closed. Design mode reaches the earlier moment, writes a named design under docs/, and joins N pull requests through an epics array.\", \"problem\": \"The plugin captures author intent at submit time, after the change exists. Nothing captures it at design time, when the engineer still holds the alternatives, the constraints, and the doubts.\", \"solution\": \"A seven-stage /cobuilder-architect:design mode writes docs/architecture/designs/<name>/, drafts a decided ADR, and joins pull requests through design/<name>[/<epic-slug>].\", \"beats\": [{\"kind\": \"background\", \"text\": \"Submit mode already interviews an author before the PR opens. That is later than the moment the alternatives are still open. Generate mode then reconstructs intent from merged code.\"}, {\"kind\": \"intuition\", \"text\": \"Name the outcome first. Search existing designs by meaning. Interview five topics. Explore. Challenge until rejected options exist. Draft the ADR. Review. Create the first local branch only.\"}], \"alternatives\": [{\"option\": \"Build design mode in archkit\", \"rejected\": \"The interview, bundle, diagrams, and viewer live here.\"}, {\"option\": \"Keep the architecture skill optional\", \"rejected\": \"A design without a corpus degrades into a chat.\"}, {\"option\": \"Ask which options they rejected\", \"rejected\": \"A rejected option is an outcome of explore and challenge, not an interview input.\"}, {\"option\": \"Mint a synthetic PR key\", \"rejected\": \"story.json and four readers require a real integer pr.\"}, {\"option\": \"One branch per design\", \"rejected\": \"N epics produce N pull requests.\"}]}, \"architecture\": {\"narration\": \"Authored source lives in docs/. The bundle is a full rebuild. Architecture modes are self-only. Odyssey keeps --repo. One design lists every ADR it produced, and drift is measured per epic.\", \"beats\": [{\"kind\": \"forces\", \"text\": \"story.json keys on an integer pr. The plugin ships no agents, hooks, or MCP servers. cobuilder-factory owns epic decomposition.\"}, {\"kind\": \"contract\", \"text\": \"docs/architecture/designs/<name>/ is the design home. build_adrs.py and build_designs.py rebuild the self bundle only. Submit strips design/ and loads intent.json as the hypothesis.\"}, {\"kind\": \"boundary\", \"text\": \"Design mode does not take --repo, does not push, does not open a PR, and does not decompose epics. A design never enters the timeline until gh pr create returns a number.\"}]}}, \"diagrams\": {\"1\": \"C4Container\\ntitle cobuilder-architect after design mode\\n\\nPerson(eng, \\\"Engineer\\\", \\\"Designs, then submits\\\")\\nPerson(rev, \\\"Reviewer\\\", \\\"Reads docs/ and the viewer\\\")\\n\\nSystem_Boundary(plugin, \\\"cobuilder-architect\\\") {\\n  Container(design, \\\"Design mode\\\", \\\"odyssey\\\", \\\"Seven stages, self-only\\\")\\n  Container(submit, \\\"Submit mode\\\", \\\"odyssey\\\", \\\"Interview, assess, open PR\\\")\\n  Container(odyssey, \\\"Odyssey five\\\", \\\"odyssey\\\", \\\"Baseline, generate, view, publish\\\")\\n  Container(arch, \\\"Architecture six\\\", \\\"architecture\\\", \\\"Self-only review and decisions\\\")\\n}\\n\\nSystem_Ext(docs, \\\"docs/\\\", \\\"Authored ADRs, designs, PR write-ups\\\")\\nSystem_Ext(bundle, \\\".cobuilder-architect/\\\", \\\"Derived projections and binary\\\")\\n\\nRel(eng, design, \\\"Names the outcome\\\")\\nRel(design, docs, \\\"Writes designs/ and a decided ADR\\\")\\nRel(design, arch, \\\"Stage 3 explore, stage 4 challenge\\\")\\nRel(eng, submit, \\\"Confirms the PR\\\")\\nRel(submit, docs, \\\"Reads the design, writes pull-requests/\\\")\\nRel(odyssey, bundle, \\\"Rebuilds adrs.js and designs.js\\\")\\nRel(rev, bundle, \\\"Opens the designs view\\\")\\nRel(rev, docs, \\\"Reviews markdown\\\")\\n\"}}, \"gate-doc-surfacing\": {\"goal\": {\"name\": \"gate-doc-surfacing\", \"title\": \"Surface Gate 3 and Gate 4b docs in the Builds view\", \"created\": \"2026-09-01\", \"outcome\": \"Gate documents (03-program-design.md, epic-<id>-design.md) are readable from the viewer's Builds view, not just from docs/plans/ or a one-off Artifact publish.\", \"done_when\": [\"Gate Rail cards in the Builds view open a sheet showing the gate's doc content\", \"Epic cards with an approved 4b design show a design-doc chip that opens the same sheet type\", \"data/index.json carries program_design and epic_design entities projected from docs/plans/<slug>/03-program-design.md and epic-<id>-des\n… [truncated by pr: diff exceeds 200KB on very few lines]",
  ".cobuilder-architect/self/data/index.js": "diff --git a/.cobuilder-architect/self/data/index.js b/.cobuilder-architect/self/data/index.js\nindex da4f39b..79bf958 100644\n--- a/.cobuilder-architect/self/data/index.js\n+++ b/.cobuilder-architect/self/data/index.js\n@@ -1 +1 @@\n-window.INDEX = {\"schema_version\": \"1.3\", \"sources\": {\"git_head\": \"55395917fd9a0dd54b272e76799d319f0a5e174d\", \"trees\": {\"docs/architecture/adr\": \"sha256:fd9dd693addcaf68e2bcc92bf905004dfcf546721de125b51b7d5e9e758ba8a1\", \"docs/architecture/designs\": \"sha256:83935e6bf2bb91a878f8883ea5198777c99bf2252273ac7fcba639bd723d366f\", \"docs/architecture/contexts\": \"sha256:4626a327bd28df972d97d40dc1325c28808eb48789649b87eadd218b3528c907\", \"docs/plans\": \"sha256:c6ed98e3846b908d8cfd216b6ff873473172ec2559e6e6ac6a4141253205efd1\"}}, \"entities\": {\"adr\": [{\"id\": \"ADR-0001\", \"title\": \"Flatten the bundle viewer into one self-contained file for Claude Artifact publishing\", \"state\": \"approved\"}, {\"id\": \"ADR-0002\", \"title\": \"Track publish provenance with a commit SHA + content hash, not by re-threading git history\", \"state\": \"approved\"}, {\"id\": \"ADR-0003\", \"title\": \"Unify bundle storage under .prodyssey/, self-analysis into .prodyssey/self/\", \"state\": \"approved\"}, {\"id\": \"ADR-0004\", \"title\": \"Keep the ste-writing skill dev-local, out of the plugin's install surface\", \"state\": \"approved\"}, {\"id\": \"ADR-0005\", \"title\": \"Diagram authoring delegated to a per-PR subagent, never the orchestrating Claude or a script\", \"state\": \"approved\"}, {\"id\": \"ADR-0006\", \"title\": \"Three-phase, guard-checked bundle self-migration instead of one version-gated ladder\", \"state\": \"approved\"}, {\"id\": \"ADR-0007\", \"title\": \"Anchor the GEMINI_API_KEY / .env lookup to the hub's cwd, never to a --repo target\", \"state\": \"approved\"}, {\"id\": \"ADR-0008\", \"title\": \"Cap and lock third-party dependencies in the three Gemini-calling scripts\", \"state\": \"approved\"}, {\"id\": \"ADR-0009\", \"title\": \"Submit mode ends the pre stage by opening the real PR, never a synthetic key\", \"state\": \"approved\"}, {\"id\": \"ADR-0010\", \"title\": \"Interview self-consistency check compares blind author accounts by LLM judgment, not keyword matching\", \"state\": \"approved\"}, {\"id\": \"ADR-0011\", \"title\": \"Design mode: capture intent before the code exists\", \"state\": \"decided\"}, {\"id\": \"ADR-0012\", \"title\": \"CoBuilder Implement: a design becomes verified code\", \"state\": \"approved\"}, {\"id\": \"ADR-0013\", \"title\": \"Design mode and cobuilder-implement: split the first gate, join through a file\", \"state\": \"approved\"}, {\"id\": \"ADR-0014\", \"title\": \"Authored source lives in docs/; the bundle is derived\", \"state\": \"decided\"}, {\"id\": \"ADR-0015\", \"title\": \"Four shipped skills, architecture modes self-only\", \"state\": \"decided\"}, {\"id\": \"ADR-0016\", \"title\": \"Five sibling plugins, with the bundle as the only seam\", \"state\": \"approved\"}, {\"id\": \"ADR-0017\", \"title\": \"Vendored shared code, and a compatibility gate the bundle owns\", \"state\": \"approved\"}, {\"id\": \"ADR-0018\", \"title\": \"One lifecycle surface, and a record index that joins it\", \"state\": \"approved\"}, {\"id\": \"ADR-0019\", \"title\": \"Anchored comments, kept as a durable ledger\", \"state\": \"approved\"}, {\"id\": \"ADR-0020\", \"title\": \"Viewer parts and an author-time build\", \"state\": \"decided\"}, {\"id\": \"ADR-0021\", \"title\": \"book-index.md gains a nano tier, and full-book loading stops being unconditional\", \"state\": \"decided\"}], \"design\": [{\"id\": \"book-index-tiering\", \"name\": \"book-index-tiering\", \"outcome\": \"book-index.md offers nano, mini, and full tiers for all 14 vendored books, with a 3-nano-minimum judgment-gated escalation rule; Review/Maintenance's mandatory 14-file security corpus load gets the same cheap-signal-before-expensive-read shape, opposite default bias, no vendoring.\", \"stage\": \"review\"}, {\"id\": \"build-workflow-polish\", \"name\": \"build-workflow-polish\", \"outcome\": \"An engineer running architect:design into implement:start never loses the handoff, 04-slices.md is always written in the shape verify_gate.py actually parses, a validated slice is committed without an extra prompt, and the bundle viewer borrows the tab-based Diagram / Data flow / Data types layout a colleague prototyped.\", \"stage\": \"backlog\"}, {\"id\": \"cobuilder-implement\", \"name\": \"cobuilder-implement\", \"outcome\": \"The architecture design produced by cobuilder-architect carries directly into implementation: its intent is decomposed into testable epics, and each epic ships as vertical slices that an independent validator scores at 0.90 or better before the next one starts.\", \"stage\": \"approved\"}, {\"id\": \"design-mode\", \"name\": \"design-mode\", \"outcome\": \"An engineer can design a change with the plugin before writing it, and the resulting intent, ADR, and diagrams carry through to the pull request that implements it.\", \"stage\": \"implemented\"}, {\"id\": \"inflight-record-store\", \"name\": \"inflight-record-store\", \"outcome\": \"A reader on the main branch sees every piece of in-flight work across all open pull requests, and no pull request owner hits a merge conflict caused by that view.\", \"stage\": \"backlog\"}, {\"id\": \"lean-bundle-diffs\", \"name\": \"lean-bundle-diffs\", \"outcome\": \"A fresh clone of this repo is not weighed down by every merged PR's full diff text, and the viewer still shows a diff for any PR a reader opens.\", \"stage\": \"backlog\"}, {\"id\": \"maintainable-viewer\", \"name\": \"maintainable-viewer\", \"outcome\": \"An engineer changes one view mode of the bundle viewer by opening one file of a few hundred lines, and the file that ships is still one self-contained HTML page that publishes as an Artifact unchanged.\", \"stage\": \"backlog\"}, {\"id\": \"plugin-split\", \"name\": \"plugin-split\", \"outcome\": \"A user installs one stage of the architecture lifecycle without the other three, and four independently-installed plugins write into one bundle without a stale copy destroying authored or paid content.\", \"stage\": \"implemented\"}, {\"id\": \"ubiquitous-language\", \"name\": \"ubiquitous-language\", \"outcome\": \"One term means one thing across every plugin, skill, design, and ADR in the repo, and a new or colliding term cannot reach a merged pull request undetected.\", \"stage\": \"backlog\"}], \"epic\": [{\"id\": \"book-index-tiering/E1\", \"design\": \"book-index-tiering\", \"epic_id\": \"E1\", \"branch\": \"design/book-index-tiering\", \"pr\": 16, \"state\": \"open\", \"note\": null}, {\"id\": \"build-workflow-polish/E1\", \"design\": \"build-workflow-polish\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"Stage 7 in architect's references/design-mode.md ends design mode after creating the first branch, with no pointer to what runs next. Add one line after the branch is created: 'Design mode's work ends here. To build this design's epics, run /implement:start.' Do not have design mode invoke implement:start itself — architecture modes are self-only in scope, and implement already owns Gate 1's design-record lookup (build/SKILL.md's 'Optional: design mode record' section). This is a one-line prose addition to a mode-governing reference file, so it still needs the Gate-selection exception's behavioral rubric, not a skip.\"}, {\"id\": \"build-workflow-polish/E2\", \"design\": \"build-workflow-polish\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"04-slices.md's Gate 4a instructions in build/SKILL.md describe the table only through the worked example, and never say the table must match shared/slice_table.py's exact column shape (six columns: #, Epic, Slice, Ends with, Score, State, with the epic-header row's precise empty-cell pattern) or point to that module as the source of truth. Add an explicit sentence and a link, so an authoring session does not freehand a table shape verify_gate.py silently fails to parse. Consider also having verify_gate.py surface parse_table()'s `unparsed` list by name when it is non-empty, instead of only reporting downstream symptoms like a missing epic.\"}, {\"id\": \"build-workflow-polish/E3\", \"design\": \"build-workflow-polish\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"build/SKILL.md's 'After each slice' section (the four numbered steps following the red-green-validate loop) records the score and asks 'Proceed to the next slice, or adjust direction?' but never commits. In practice this produces a spurious 'want to commit this checkpoint first?' prompt after every slice instead of an automatic commit. Change step 2 (or add a step) so that once VALIDATE scores a slice at or above 0.90, the orchestrator commits the slice's changes to git immediately, before asking whether to proceed — no confirmation question for the commit itself, only for what comes after. A score below threshold still routes through the existing gap decision tree in references/validation-scoring.md, and does not commit. Below-threshold slices accepted with reservations after three attempts should also commit, since that path already represents a final accepted state, not a pending one.\"}, {\"id\": \"build-workflow-polish/E4\", \"design\": \"build-workflow-polish\", \"epic_id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A colleague's scratch artifact (/private/tmp/.../diagram-viewer.html, session-local, not part of this repo) uses a top-of-panel tab switcher across three views — Diagram, Data flow, Data types — instead of stacking them, plus a right-hand info rail. Evaluate adopting the same top-tab pattern for viewer/index.html's per-PR diagram levels (currently level 1/2/3 switching, per ADR-0020's viewer/src/ plan), and separately evaluate narrowing the left-hand navigation rail so it takes less width once tabs handle top-level switching. This touches only the viewer, so it can run through architect:design on its own without waiting on E1-E3.\"}, {\"id\": \"cobuilder-implement/E1\", \"design\": \"cobuilder-implement\", \"epic_id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Shares a branch and a pull request with the design-mode design. See ADR-0013: a branch may carry more than one design.\"}, {\"id\": \"cobuilder-implement/E2\", \"design\": \"cobuilder-implement\", \"epic_id\": \"E2\", \"branch\": \"docs/post-pr11-cleanup\", \"pr\": 12, \"state\": \"open\", \"note\": \"Gate 4b is enforced instead of merely specified. The per-epic technical solution design already exists in SKILL.md section 4b, with a six-section template, and five downstream files reference the artifact. Nothing checks that it was written. Checked on 2026-08-24: zero epic-<id>-design.md files exist, and six epics shipped without one. Four failures allow that. Gate 4's only exit check counts rubrics, so a Gate 4 that skips 4b still passes. slice-loop.js builds the epic-design path and never checks the file exists, so the red and green agents are told to read a file that is absent and quietly fall back to 03-program-design.md. rubric-authoring.md tells the author to update the technical design first, and nobody checks that either. No test asserts the artifact. The root cause is that 4b is the only Gate 4 sub-step whose output nothing downstream mechanically requires: the loop cannot score without rubrics or iterate without a slice plan, but it can read an absent design. This epic adds the exit check, the existence guard in the workflow, a test, and sub-step tracking in 00-status.md so an approved Gate 4 cannot hide a missing 4b. Raised 2026-08-22, re-scoped 2026-08-24 from 'write the process' to 'enforce the process' after reading the skill.\"}, {\"id\": \"design-mode/E1\", \"design\": \"design-mode\", \"epic_id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Design mode as a pre-code interview and challenge gate. Shares a branch and a pull request with the cobuilder-implement design. See ADR-0013: a branch may carry more than one design.\"}, {\"id\": \"inflight-record-store/E1\", \"design\": \"inflight-record-store\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Open pull requests become entities. collect_pull_requests() merges gh pr list --state all metadata over the narrated story.json entities. An open pull request gets an entity with state: \\\"open\\\" and a narrated: false flag. This removes the placeholder in refine_epic_status(). It reads metadata only, checks out no branch, and creates no conflict. This is the tracer bullet, and it is useful on its own. It does not depend on E2, E3, or E4, and can ship alone.\"}, {\"id\": \"inflight-record-store/E2\", \"design\": \"inflight-record-store\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A record store with a merge strategy. One file per record, append-only, with a defined resolution rule for a concurrent write. Modelled on beads. This epic decides the storage shape and nothing else.\"}, {\"id\": \"inflight-record-store/E3\", \"design\": \"inflight-record-store\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Cross-branch collection without a conflict. Records written by a session on a branch reach the shared view without any branch writing a shared generated file.\"}, {\"id\": \"inflight-record-store/E4\", \"design\": \"inflight-record-store\", \"epic_id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The active-work view. The viewer renders in-flight work beside merged history, so a reader sees one timeline rather than two.\"}, {\"id\": \"lean-bundle-diffs/E1\", \"design\": \"lean-bundle-diffs\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Decide the replacement shape: the viewer reads a diff by running git diff against the PR's recorded commit SHAs at serve time (view mode already runs from inside the repo), versus keeping diffs-pr{N}.js exactly as it is today but gitignoring it, regenerating it from git on demand the way exports/branch-*/diff.json already works for generate mode. Either removes the committed weight; they differ in whether a served bundle still needs a live git checkout next to it.\"}, {\"id\": \"lean-bundle-diffs/E2\", \"design\": \"lean-bundle-diffs\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Whichever shape E1 picks, implement it in extract_diffs.py and, if the viewer starts requesting diffs at view time instead of reading a static file, in artifact's serve_bundle.py. Update verify_bundle.py's expectations accordingly.\"}, {\"id\": \"lean-bundle-diffs/E3\", \"design\": \"lean-bundle-diffs\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Migrate the existing committed bundles (self, and the two fixture bundles) so history stops growing: remove the checked-in diffs-pr{N}.js files, or move them under .gitignore, per E1's decision. This does not touch story.json's authored fields, so it is a bundle-file change, not a schema migration.\"}, {\"id\": \"maintainable-viewer/E1\", \"design\": \"maintainable-viewer\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The build exists and is guarded. build_viewer.py concatenates ordered parts under viewer/src/ into the committed viewer/index.html, and a test rebuilds from the parts and fails on any difference. The first split is mechanical only, cut at the file's own 35 banner comments, with no line rewritten. This epic is the tracer bullet: it proves or disproves that the single IIFE separates by concatenation alone, which is the abort condition the whole design rests on.\"}, {\"id\": \"maintainable-viewer/E2\", \"design\": \"maintainable-viewer\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The exporter seam becomes declared. export_artifact.py stops matching incidental literals and matches named markers the parts emit deliberately. Today it hard-errors with not-found-verbatim when a literal moves, so the publish pipeline breaks silently under any refactor until somebody publishes. This must land before the parts are rewritten, not after, or every later epic risks breaking publishing without a test noticing.\"}, {\"id\": \"maintainable-viewer/E3\", \"design\": \"maintainable-viewer\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"One definition per fragment. A template layer replaces the 45 innerHTML string blocks, and the rail-item, empty-card, and empty-detail fragments collapse from 4, 16, and 18 copies to one each. The viewer uses no template element today, so the mechanism is a design choice this epic makes and does not inherit.\"}, {\"id\": \"maintainable-viewer/E4\", \"design\": \"maintainable-viewer\", \"epic_id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Each mode owns its own part. The five view modes stop interleaving: the 50 scattered mode-predicate calls collapse into one registry, and renderContextsMainContent, renderBuildsMainContent, and renderMainContent stop being three functions of over 200 lines that live beside each other. Adding a sixth mode becomes one new part plus one registry entry.\"}, {\"id\": \"plugin-split/E1\", \"design\": \"plugin-split\", \"epic_id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"One plugin becomes five. The whole packaging split: the mode renames inside today's single plugin, the deleted duplicate explore-design command, the 33 cross-pillar references, the five manifests, and the two ports. The rename is a rotation, not a split: odyssey submit becomes cobuilder-pr:generate, and the old odyssey generate, which narrates merged history, becomes cobuilder-pr:review. Four slices.\"}, {\"id\": \"plugin-split/E2\", \"design\": \"plugin-split\", \"epic_id\": \"E2\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Shared code survives an install. Prove the marketplace symlink dereference, then vendor the four bundle modules and the two shared skills. One slice, because it is a spike whose result decides whether the rest of the design stands. goal.json.abort_if names its failure.\"}, {\"id\": \"plugin-split/E3\", \"design\": \"plugin-split\", \"epic_id\": \"E3\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"The seam is version-safe. require_compatible(), min_reader_schema, the generators map, bundle format 3, and schema 1.3, so two plugins at different versions cannot corrupt one bundle. Two slices.\"}, {\"id\": \"plugin-split/E4\", \"design\": \"plugin-split\", \"epic_id\": \"E4\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"The record index. build_index.py replaces the two projection scripts, emits every entity with a stable id, and resolves every join including an ADR to its pull request. Two slices.\"}, {\"id\": \"plugin-split/E5\", \"design\": \"plugin-split\", \"epic_id\": \"E5\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"One lifecycle surface. The Decisions, Contexts, and Builds modes, the Backlog lane, and moving generated pages out of the foreign directory into the bundle. Two slices (10, 11).\"}, {\"id\": \"plugin-split/E6\", \"design\": \"plugin-split\", \"epic_id\": \"E6\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"The reply channel. The append-only ledger and its projection, the anchor computed from the live DOM, the write endpoint, and the background wake command. Three slices (12, 13, 14).\"}, {\"id\": \"plugin-split/E7\", \"design\": \"plugin-split\", \"epic_id\": \"E7\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Threads read as conversations. The drawer renders a reply under the comment it answers. Deferred past E6 on purpose: E6 ships the record shape, so this epic adds no migration and carries no slice yet.\"}, {\"id\": \"plugin-split/E8\", \"design\": \"plugin-split\", \"epic_id\": \"E8\", \"branch\": \"rename-plugins-drop-cobuilder-prefix\", \"pr\": 17, \"state\": \"merged\", \"note\": \"The cobuilder- prefix drops from the four non-umbrella plugins named in E1's table (cobuilder-architect, cobuilder-pr, cobuilder-artifact, cobuilder-implement become architect, pr, artifact, implement), since it duplicates the marketplace repo's own name. cobuilder-full-lifecycle keeps its prefix, being the umbrella the repo's own name already carries. Merged as PR #17, then a same-epic follow-up (uncommitted at the time this entry was written) fixes a second-order bug the rename introduced: two of the four renamed plugins (implement, artifact) already shipped a skill folder named after themselves, and once the plugin took that same name too, Skill() resolution inside Claude Code broke -- reproduced directly, not environment-specific. Fixed by renaming the colliding skill folders (implement's skill to build, its command to start; artifact's skill to cobuilder-artifacts) and, for consistency, cobuilder-full-lifecycle's orientation skill to cobuilder-full. See ADR-0016's addendum.\"}, {\"id\": \"ubiquitous-language/E1\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"One language record, and it is the tracer bullet. docs/architecture/language.md becomes the single source of truth for a term, its owning context, its meaning, and what it must not be confused with. It is seeded from CLAUDE.md's existing Vocabulary table and from cobuilder-packaging's canvas section 3. shared/build_index.py projects it into the record index, so the viewer can render it. This epic is independently shippable and does not depend on E2 through E5.\"}, {\"id\": \"ubiquitous-language/E2\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Design mode gains a language stage. A design that introduces a new term must declare it or reuse an existing entry from the language record. A collision with an existing term becomes a challenge-stage finding. This fits design mode's existing no-citation-no-challenge rule, because the language record gives the challenge stage something concrete to cite.\"}, {\"id\": \"ubiquitous-language/E3\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The mechanical consumer, in cobuilder-implement. A Gate 4b technical solution design that names a concept absent from the language record fails the gate. This epic is what stops the whole program from rotting the way canvas section 3 did. E1 and E2 without E3 are documentation waiting to go stale.\"}, {\"id\": \"ubiquitous-language/E4\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A verifier that exits non-zero. A script finds a term used in a design or an ADR and absent from the language record, and fails the check. It is modeled on plugins/cobuilder-implement/scripts/verify_gate.py, which exits non-zero on any failed Gate 4 sub-step. The honest open question is that deciding what counts as a term mechanically, rather than by judgement, is the hard part of this epic and may need its own exploration.\"}, {\"id\": \"ubiquitous-language/E5\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E5\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Repair the existing drift. Refresh cobuilder-packaging's canvas section 3 and its boundary.yaml public_interface against the shipped five-plugin tree, delete the now-false homonym comment, and reconcile the two glossaries into E1's record.\"}], \"context\": [{\"id\": \"cobuilder-packaging\", \"name\": \"Plugin packaging and dispatch\", \"path\": \".\", \"verifies\": [\"commands\", \"skills\", \"scripts\", \"viewer\", \"docs\"]}], \"district\": [{\"id\": \"skills\", \"label\": \"Skills\", \"paths\": [\"skills\"]}, {\"id\": \"scripts\", \"label\": \"Scripts\", \"paths\": [\"scripts\"]}, {\"id\": \"commands\", \"label\": \"Commands\", \"paths\": [\"commands\"]}, {\"id\": \"viewer\", \"label\": \"Viewer\", \"paths\": [\"viewer\"]}, {\"id\": \"docs\", \"label\": \"Docs\", \"paths\": [\"docs\"]}, {\"id\": \".cobuilder-architect\", \"label\": \"Bundle Store\", \"paths\": [\".cobuilder-architect\"]}], \"boundary_rule\": [{\"id\": \"cobuilder-packaging/forbidden-dependency/1\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"another plugin's files (post-ADR-0016 split)\", \"why\": \"ADR-0016: 'No plugin reads or imports another plugin's files. A plugin may hand off to another plugin's mode by name, and the state that crosses the handoff travels in the bundle and in docs/.' Not yet enforceable -- only one plugin (cobuilder-architect) exists today, so there is no sibling to violate this against. Recorded now so the rule is in place before the split lands.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/2\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"skills/odyssey/SKILL.md\", \"from\": \"skills/architecture\", \"why\": \"SMELL: skills/architecture/SKILL.md lines 83-84 hardcode the path 'skills/odyssey/SKILL.md' inside Design mode's Hub-resolution step. Verified by grep. Harmless today (both skills ship in one plugin), but this is exactly the edge ADR-0016 forbids once architecture and odyssey become separate plugins (cobuilder-architect and cobuilder-pr). Needs an ADR-sanctioned fix (e.g. duplicate the Hub-resolution text, or move it to a shared vendored module) before the split, or it becomes real drift the day the split ships.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/3\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"skills/architecture/SKILL.md\", \"from\": \"skills/odyssey\", \"why\": \"SMELL: skills/odyssey/SKILL.md lines 32-33 name and point at 'skills/architecture/SKILL.md' when describing where Design mode lives. Same coupling as the entry above, reversed direction. Verified by grep.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/4\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"skills/architecture/references/*\", \"from\": \"scripts (build_designs.py, build_adrs.py, validate_decision_state.py)\", \"why\": \"SMELL: three scripts embed a literal 'skills/architecture/references/...' path inside a validation-failure error message (build_designs.py:299, build_adrs.py:217, validate_decision_state.py:433). Text, not an import, so it does not execute a cross-plugin call today. Still a coupling a post-split remediation hint cannot resolve, because the reading plugin (e.g. cobuilder-pr) cannot open a path inside cobuilder-architect's cache.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/5\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"skills/odyssey/references/*\", \"from\": \"scripts (build_diagrams.py, render_review.py, verify_bundle.py)\", \"why\": \"SMELL: same pattern, reversed. build_diagrams.py:9 and :273, render_review.py:9, and verify_bundle.py:86 embed 'skills/odyssey/references/...' in comments or error text. Verified by grep.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/6\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"scripts/*.py (as a Python import, not a shell invocation)\", \"from\": \"viewer/index.html\", \"why\": \"The viewer never imports or executes anything under scripts/. Its one mention of 'scripts/' (line 854) is a code comment naming the script that produces data/diagrams.js, not a reference the browser resolves. Recorded as a confirmed absence, not a smell -- this is the rule holding, not breaking.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/1\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"plugin-manifest\", \"path\": \".claude-plugin/\", \"public_interface\": [\"plugin.json\", \"marketplace.json\"], \"allowed_inbound\": [], \"allowed_outbound\": [], \"rule\": \"Static JSON only. No module in this repo reads or writes it at runtime.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/2\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"commands\", \"path\": \"commands/\", \"members\": [\"baseline.md\", \"generate.md\", \"view.md\", \"publish.md\", \"submit.md\", \"design.md\", \"review.md\", \"maintenance.md\", \"decisions.md\", \"describe.md\", \"debug.md\", \"explore-design.md\"], \"public_interface\": [], \"allowed_inbound\": [], \"allowed_outbound\": [\"skills-architecture\", \"skills-odyssey\"], \"rule\": \"Each command file makes exactly one Skill(...) dispatch call and nothing else. Verified: every commands/*.md greps to exactly one Skill(\\\"architecture\\\"|\\\"odyssey\\\", ...) line.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/3\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-architecture\", \"path\": \"skills/architecture/\", \"public_interface\": [\"Skill(\\\"architecture\\\", args=...)\"], \"allowed_inbound\": [\"commands\"], \"allowed_outbound\": [\"scripts\", \"skills-mermaid\", \"skills-ste-writing\"], \"rule\": \"SMELL: also reaches skills-odyssey via a hardcoded SKILL.md path reference (see forbidden_dependencies). Should be [] toward skills-odyssey once ADR-0016 lands.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/4\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-odyssey\", \"path\": \"skills/odyssey/\", \"public_interface\": [\"Skill(\\\"odyssey\\\", args=...)\"], \"allowed_inbound\": [\"commands\"], \"allowed_outbound\": [\"scripts\", \"skills-mermaid\", \"skills-ste-writing\"], \"rule\": \"SMELL: also reaches skills-architecture via a hardcoded SKILL.md path reference (see forbidden_dependencies). Should be [] toward skills-architecture once ADR-0016 lands.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/5\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-mermaid\", \"path\": \"skills/mermaid/\", \"public_interface\": [\"Skill(\\\"mermaid\\\")\", \"Skill(\\\"cobuilder-architect:mermaid\\\")\"], \"allowed_inbound\": [\"skills-architecture\", \"skills-odyssey\"], \"allowed_outbound\": [], \"rule\": \"Diagram-authoring rules only. Invoked by a per-PR/per-design subagent, never by the orchestrating Claude directly, per CLAUDE.md.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/6\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-ste-writing\", \"path\": \"skills/ste-writing/\", \"public_interface\": [\"Skill(\\\"ste-writing\\\")\", \"Skill(\\\"cobuilder-architect:ste-writing\\\")\", \"ste-lint.py\"], \"allowed_inbound\": [\"skills-architecture\", \"skills-odyssey\"], \"allowed_outbound\": [], \"rule\": \"Writing-style rules and a rules-only linter. No dependency on any other module verified.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/7\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-collaborate-with-user\", \"path\": \"skills/collaborate-with-user/\", \"public_interface\": [\"Skill(\\\"collaborate-with-user\\\")\"], \"allowed_inbound\": [], \"allowed_outbound\": [], \"rule\": \"References skills/architecture/SKILL.md's 'Report Generation' section in prose (line 95) to say it does not duplicate that convention. This is documentation cross-talk, not a code or Skill-dispatch edge -- no forbidden_dependencies entry warranted.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/8\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"scripts\", \"path\": \"scripts/\", \"members\": [\"_bundle_meta.py\", \"_manifest.py\", \"build_adrs.py\", \"build_designs.py\", \"build_diagrams.py\", \"compute_scores.py\", \"export_artifact.py\", \"export_index.py\", \"extract_diffs.py\", \"extract_story.py\", \"generate_audio.py\", \"generate_prompts.py\", \"html_to_pdf.py\", \"migrate_bundle.py\", \"record_publish.py\", \"render_review.py\", \"validate_decision_state.py\", \"verify_bundle.py\"], \"public_interface\": [\"_bundle_meta.SCHEMA_VERSION\", \"_bundle_meta.SCHEMA_VERSION_KNOWN\", \"_bundle_meta.CURRENT_BUNDLE_FORMAT\", \"_manifest.rewrite_manifest\"], \"allowed_inbound\": [\"skills-architecture\", \"skills-odyssey\"], \"allowed_outbound\": [], \"rule\": \"Every script that reads or writes bundle data imports _bundle_meta.SCHEMA_VERSION for its own version stamp. _bundle_meta.py itself is a leaf: stdlib-only, no imports beyond __future__. ADR-0017's require_compatible() gate does not exist in this file yet -- verified absent.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/9\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"viewer\", \"path\": \"viewer/\", \"members\": [\"index.html\"], \"public_interface\": [\"window.STORY, window.ODYSSEY, window.DIFFS, window.ADRS, window.DIAGRAMS, window.DESIGNS (populated by sibling <script src> tags)\"], \"allowed_inbound\": [], \"allowed_outbound\": [], \"rule\": \"viewer/index.html never reaches into scripts/ (grep-verified: the one 'scripts/' mention is a code comment, not a live reference). It is not self-contained -- see CLAUDE.md 'The viewer is not self-contained' -- and depends on being served from the bundle root, a sibling of viewer/.\"}}, {\"id\": \"cobuilder-packaging/context-map/1\", \"context\": \"cobuilder-packaging\", \"kind\": \"context-map\", \"detail\": {\"with\": \"bundle-directory\", \"pattern\": \"open-host-service\", \"note\": \"Not yet a documented bounded context. Scripts write it, the viewer reads it; neither reads the other's source. Data-only, versioned by bundle.json / story.json meta.schema_version.\"}}], \"pull_request\": [{\"id\": 1, \"title\": \"Master\", \"state\": \"merged\", \"commit\": \"7bd668f\", \"date\": \"2026-07-22\"}, {\"id\": 2, \"title\": \"docs: add CLAUDE.md with codebase orientation + artifact feasibility findings\", \"state\": \"merged\", \"commit\": \"96c1531\", \"date\": \"2026-07-24\"}, {\"id\": 3, \"title\": \"Unify bundle storage under .prodyssey/, self-analysis into .prodyssey/self/\", \"state\": \"merged\", \"commit\": \"fb4be22\", \"date\": \"2026-07-28\"}, {\"id\": 4, \"title\": \"Mermaid diagrams for levels 1-3, and self-upgrading bundles\", \"state\": \"merged\", \"commit\": \"065654d\", \"date\": \"2026-08-02\"}, {\"id\": 5, \"title\": \"Close the API-key trust boundary, and fix three correctness bugs\", \"state\": \"merged\", \"commit\": \"789081f\", \"date\": \"2026-08-05\"}, {\"id\": 6, \"title\": \"Pr Odyssey Improvements 7Suouc\", \"state\": \"merged\", \"commit\": \"7473b7f\", \"date\": \"2026-08-05\"}, {\"id\": 7, \"title\": \"Pr Odyssey Improvements 7Suouc\", \"state\": \"merged\", \"commit\": \"630aa4f\", \"date\": \"2026-08-05\"}, {\"id\": 8, \"title\": \"Ste Writing Style Claude Fd5Fzs\", \"state\": \"merged\", \"commit\": \"d9a81d7\", \"date\": \"2026-08-06\"}, {\"id\": 9, \"title\": \"Deduplicate rewrite_manifest(); add interview self-consistency check; STE clarity pass\", \"state\": \"merged\", \"commit\": \"139de94\", \"date\": \"2026-08-06\"}, {\"id\": 10, \"title\": \"Swift Best Practices Fbx48M\", \"state\": \"merged\", \"commit\": \"6b2fce6\", \"date\": \"2026-08-06\"}, {\"id\": 11, \"title\": \"Design cobuilder-architect and cobuilder-implement, split repo into five plugins and skills\", \"state\": \"merged\", \"commit\": \"a389892\", \"date\": \"2026-08-25\"}, {\"id\": 12, \"title\": \"Retire adr-draft, close Gate 4b retrospectively, and record two ADR maintenance rules\", \"state\": \"merged\", \"commit\": \"be93c04\", \"date\": \"2026-08-25\"}, {\"id\": 16, \"title\": \"book-index.md gains a nano tier, and the security corpus stops reading unconditionally in full\", \"state\": \"open\", \"commit\": \"6bad882c30e7ea84a2e1d950e604859ffd211595\", \"date\": \"2026-08-26\"}, {\"id\": 17, \"title\": \"Rename plugins to drop the cobuilder- prefix; fix Mermaid click/copy-paste in the feedback drawer\", \"state\": \"open\", \"commit\": \"1d0c5ceb27a7df8dfaf4e3d36e2ebf9993a27735\", \"date\": \"2026-08-26\"}], \"slice\": [{\"id\": \"cobuilder-family/4\", \"feature\": \"cobuilder-family\", \"n\": 4, \"title\": \"Renames inside today's single plugin\", \"ends_with\": \"Six architecture modes, `explore-design` deleted, `submit` renamed to `generate`, and the old `generate` renamed to `review`\", \"score\": \"1.00\", \"attempts\": 2, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/5\", \"feature\": \"cobuilder-family\", \"n\": 5, \"title\": \"The 33 cross-pillar references are fixed\", \"ends_with\": \"The grep regression test passes while the repo is still one plugin\", \"score\": \"1.00\", \"attempts\": 3, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/6\", \"feature\": \"cobuilder-family\", \"n\": 6, \"title\": \"Five plugins, five manifests\", \"ends_with\": \"`claude plugin validate` passes on each, and the family installs\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/7\", \"feature\": \"cobuilder-family\", \"n\": 7, \"title\": \"The two ports land\", \"ends_with\": \"`cobuilder-implement` renamed and shipped, `collaborate-with-user` folded in, the orientation skill written\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/1\", \"feature\": \"cobuilder-family\", \"n\": 1, \"title\": \"**Tracer bullet: two plugins, one shared module**\", \"ends_with\": \"Two throwaway plugins install from a local marketplace and both resolve the shared module from their own cache\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/2\", \"feature\": \"cobuilder-family\", \"n\": 2, \"title\": \"The compatibility gate exists\", \"ends_with\": \"`require_compatible()` and `stamp_generator()` pass their tests, called by nothing yet\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/3\", \"feature\": \"cobuilder-family\", \"n\": 3, \"title\": \"The gate is wired and the bundle migrates\", \"ends_with\": \"Format 3, `min_reader_schema`, the generators map, every writer gated\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/8\", \"feature\": \"cobuilder-family\", \"n\": 8, \"title\": \"The index holds the entities\", \"ends_with\": \"`build_index.py` emits every entity, and the two scripts it replaces are gone\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/9\", \"feature\": \"cobuilder-family\", \"n\": 9, \"title\": \"The index resolves the joins\", \"ends_with\": \"`adr_to_pull_request`, `slice_to_epic`, `district_uncovered`, and freshness\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/10\", \"feature\": \"cobuilder-family\", \"n\": 10, \"title\": \"The Decisions and Contexts modes\", \"ends_with\": \"A person browses every decision and every context, with the anchor distinction visible\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/11\", \"feature\": \"cobuilder-family\", \"n\": 11, \"title\": \"The Builds mode and the Backlog lane\", \"ends_with\": \"Gates driven by the status document, the Backlog lane computed from the index, pages moved into the bundle\", \"score\": \"0.92\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/12\", \"feature\": \"cobuilder-family\", \"n\": 12, \"title\": \"The ledger and its projection\", \"ends_with\": \"Append, fold, and project, with no server involved\", \"score\": \"0.92\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/13\", \"feature\": \"cobuilder-family\", \"n\": 13, \"title\": \"The anchor and the write endpoint\", \"ends_with\": \"Click to anchor a sentence, `POST /feedback` appends, the drawer keeps the text in view\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/14\", \"feature\": \"cobuilder-family\", \"n\": 14, \"title\": \"The wake command and the whole loop\", \"ends_with\": \"The background command blocks and returns new threads, and the loop runs once end to end\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}], \"publication\": [{\"id\": \"exports/pr-2.html\", \"pull_request\": 2, \"artifact_url\": \"https://claude.ai/code/artifact/ea2c0f34-c5e5-46f7-b619-2bb7c72b5583\", \"published_at\": \"2026-07-22T09:56:34Z\"}, {\"id\": \"exports/index.html\", \"pull_request\": null, \"artifact_url\": \"https://claude.ai/code/artifact/4bf5fff8-f70e-4e68-8553-2b8a254c5e79\", \"published_at\": \"2026-07-22T09:56:48Z\"}]}, \"joins\": {\"adr_to_pull_request\": {\"ADR-0001\": {\"pr\": 2, \"via\": \"direct\", \"path\": []}, \"ADR-0002\": {\"pr\": 2, \"via\": \"direct\", \"path\": []}, \"ADR-0003\": {\"pr\": 3, \"via\": \"direct\", \"path\": []}, \"ADR-0004\": {\"pr\": 3, \"via\": \"direct\", \"path\": []}, \"ADR-0005\": {\"pr\": 4, \"via\": \"direct\", \"path\": []}, \"ADR-0006\": {\"pr\": 4, \"via\": \"direct\", \"path\": []}, \"ADR-0007\": {\"pr\": 5, \"via\": \"direct\", \"path\": []}, \"ADR-0008\": {\"pr\": 5, \"via\": \"direct\", \"path\": []}, \"ADR-0009\": {\"pr\": 6, \"via\": \"direct\", \"path\": []}, \"ADR-0010\": {\"pr\": 9, \"via\": \"direct\", \"path\": []}, \"ADR-0011\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"cobuilder-implement/E1\", \"cobuilder-implement/E2\", \"design-mode/E1\"]}, \"ADR-0012\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"cobuilder-implement/E1\", \"cobuilder-implement/E2\"]}, \"ADR-0013\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"cobuilder-implement/E1\", \"cobuilder-implement/E2\"]}, \"ADR-0014\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"design-mode/E1\"]}, \"ADR-0015\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"cobuilder-implement/E1\", \"cobuilder-implement/E2\", \"design-mode/E1\", \"plugin-split/E1\", \"plugin-split/E2\", \"plugin-split/E3\", \"plugin-split/E4\", \"plugin-split/E5\", \"plugin-split/E6\", \"plugin-split/E7\", \"plugin-split/E8\"]}, \"ADR-0016\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0017\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0018\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0019\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0020\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0021\": {\"pr\": 16, \"via\": \"epic\", \"path\": [\"book-index-tiering/E1\"]}}, \"epic_to_pull_request\": {\"book-index-tiering/E1\": 16, \"cobuilder-implement/E1\": 11, \"cobuilder-implement/E2\": 12, \"design-mode/E1\": 11, \"plugin-split/E1\": 11, \"plugin-split/E2\": 11, \"plugin-split/E3\": 11, \"plugin-split/E4\": 11, \"plugin-split/E5\": 11, \"plugin-split/E6\": 11, \"plugin-split/E8\": 18}, \"epic_status\": {\"book-index-tiering/E1\": \"open\", \"build-workflow-polish/E1\": \"unstarted\", \"build-workflow-polish/E2\": \"unstarted\", \"build-workflow-polish/E3\": \"unstarted\", \"build-workflow-polish/E4\": \"unstarted\", \"cobuilder-implement/E1\": \"merged\", \"cobuilder-implement/E2\": \"merged\", \"design-mode/E1\": \"merged\", \"inflight-record-store/E1\": \"unstarted\", \"inflight-record-store/E2\": \"unstarted\", \"inflight-record-store/E3\": \"unstarted\", \"inflight-record-store/E4\": \"unstarted\", \"lean-bundle-diffs/E1\": \"unstarted\", \"lean-bundle-diffs/E2\": \"unstarted\", \"lean-bundle-diffs/E3\": \"unstarted\", \"maintainable-viewer/E1\": \"unstarted\", \"maintainable-viewer/E2\": \"unstarted\", \"maintainable-viewer/E3\": \"unstarted\", \"maintainable-viewer/E4\": \"unstarted\", \"plugin-split/E1\": \"merged\", \"plugin-split/E2\": \"merged\", \"plugin-split/E3\": \"merged\", \"plugin-split/E4\": \"merged\", \"plugin-split/E5\": \"merged\", \"plugin-split/E6\": \"merged\", \"plugin-split/E7\": \"unstarted\", \"plugin-split/E8\": \"open\", \"ubiquitous-language/E1\": \"unstarted\", \"ubiquitous-language/E2\": \"unstarted\", \"ubiquitous-language/E3\": \"unstarted\", \"ubiquitous-language/E4\": \"unstarted\", \"ubiquitous-language/E5\": \"unstarted\"}, \"slice_to_epic\": {\"cobuilder-family/4\": \"plugin-split/E1\", \"cobuilder-family/5\": \"plugin-split/E1\", \"cobuilder-family/6\": \"plugin-split/E1\", \"cobuilder-family/7\": \"plugin-split/E1\", \"cobuilder-family/1\": \"plugin-split/E2\", \"cobuilder-family/2\": \"plugin-split/E3\", \"cobuilder-family/3\": \"plugin-split/E3\", \"cobuilder-family/8\": \"plugin-split/E4\", \"cobuilder-family/9\": \"plugin-split/E4\", \"cobuilder-family/10\": \"plugin-split/E5\", \"cobuilder-family/11\": \"plugin-split/E5\", \"cobuilder-family/12\": \"plugin-split/E6\", \"cobuilder-family/13\": \"plugin-split/E6\", \"cobuilder-family/14\": \"plugin-split/E6\"}, \"slice_to_epic_unresolved\": {}, \"context_verifies_district\": {\"cobuilder-packaging\": [\"commands\", \"skills\", \"scripts\", \"viewer\", \"docs\"]}, \"district_uncovered\": [\".cobuilder-architect\"], \"adr_to_context\": {\"ADR-0016\": \"cobuilder-packaging\", \"ADR-0017\": \"cobuilder-packaging\", \"ADR-0018\": \"cobuilder-packaging\", \"ADR-0019\": \"cobuilder-packaging\", \"ADR-0020\": \"cobuilder-packaging\"}, \"adr_to_district\": {\"ADR-0001\": \"scripts\", \"ADR-0002\": \"scripts\", \"ADR-0003\": \".prodyssey\", \"ADR-0004\": \"skills\", \"ADR-0005\": \"skills\", \"ADR-0006\": \"scripts\", \"ADR-0007\": \"scripts\", \"ADR-0008\": \"scripts\", \"ADR-0009\": \"commands\", \"ADR-0010\": \"skills\", \"ADR-0011\": \"skills\", \"ADR-0012\": \"skills\", \"ADR-0013\": \"skills\", \"ADR-0014\": \"scripts\", \"ADR-0015\": \"skills\", \"ADR-0021\": \"skills\"}, \"feature_gates\": {\"cobuilder-family\": [{\"n\": 1, \"name\": \"Product\", \"state\": \"APPROVED 2026-08-20\"}, {\"n\": 2, \"name\": \"Architecture\", \"state\": \"APPROVED 2026-08-21\"}, {\"n\": 3, \"name\": \"Program Design\", \"state\": \"APPROVED 2026-08-21\"}, {\"n\": 4, \"name\": \"Slice plan, epic designs, and rubrics\", \"state\": \"APPROVED 2026-08-25\"}], \"skill-collision-fix\": [{\"n\": 1, \"name\": \"3\", \"state\": \"n/a, superseded by direct conversation record\"}, {\"n\": 4, \"name\": \"Slice plan, epic designs, and rubrics\", \"state\": \"in progress\"}]}}};\n+window.INDEX = {\"schema_version\": \"1.3\", \"sources\": {\"git_head\": \"651b07af45d87e17e7d855ed04d271d5f12f618d\", \"trees\": {\"docs/architecture/adr\": \"sha256:ff9214a45fa93be6d1db6859475dceb268c9816a137ae97da4580533b18f55cf\", \"docs/architecture/designs\": \"sha256:babc2ac67e7550b96634728f816cef8ac238900fceb023b7acc5e4b7d1f369c9\", \"docs/architecture/contexts\": \"sha256:4626a327bd28df972d97d40dc1325c28808eb48789649b87eadd218b3528c907\", \"docs/plans\": \"sha256:c74faf0d87d977f583e8fbd67d00138b8ee695a145112fae6f391b4107aca33e\"}}, \"entities\": {\"adr\": [{\"id\": \"ADR-0001\", \"title\": \"Flatten the bundle viewer into one self-contained file for Claude Artifact publishing\", \"state\": \"approved\"}, {\"id\": \"ADR-0002\", \"title\": \"Track publish provenance with a commit SHA + content hash, not by re-threading git history\", \"state\": \"approved\"}, {\"id\": \"ADR-0003\", \"title\": \"Unify bundle storage under .prodyssey/, self-analysis into .prodyssey/self/\", \"state\": \"approved\"}, {\"id\": \"ADR-0004\", \"title\": \"Keep the ste-writing skill dev-local, out of the plugin's install surface\", \"state\": \"approved\"}, {\"id\": \"ADR-0005\", \"title\": \"Diagram authoring delegated to a per-PR subagent, never the orchestrating Claude or a script\", \"state\": \"approved\"}, {\"id\": \"ADR-0006\", \"title\": \"Three-phase, guard-checked bundle self-migration instead of one version-gated ladder\", \"state\": \"approved\"}, {\"id\": \"ADR-0007\", \"title\": \"Anchor the GEMINI_API_KEY / .env lookup to the hub's cwd, never to a --repo target\", \"state\": \"approved\"}, {\"id\": \"ADR-0008\", \"title\": \"Cap and lock third-party dependencies in the three Gemini-calling scripts\", \"state\": \"approved\"}, {\"id\": \"ADR-0009\", \"title\": \"Submit mode ends the pre stage by opening the real PR, never a synthetic key\", \"state\": \"approved\"}, {\"id\": \"ADR-0010\", \"title\": \"Interview self-consistency check compares blind author accounts by LLM judgment, not keyword matching\", \"state\": \"approved\"}, {\"id\": \"ADR-0011\", \"title\": \"Design mode: capture intent before the code exists\", \"state\": \"decided\"}, {\"id\": \"ADR-0012\", \"title\": \"CoBuilder Implement: a design becomes verified code\", \"state\": \"approved\"}, {\"id\": \"ADR-0013\", \"title\": \"Design mode and cobuilder-implement: split the first gate, join through a file\", \"state\": \"approved\"}, {\"id\": \"ADR-0014\", \"title\": \"Authored source lives in docs/; the bundle is derived\", \"state\": \"decided\"}, {\"id\": \"ADR-0015\", \"title\": \"Four shipped skills, architecture modes self-only\", \"state\": \"decided\"}, {\"id\": \"ADR-0016\", \"title\": \"Five sibling plugins, with the bundle as the only seam\", \"state\": \"approved\"}, {\"id\": \"ADR-0017\", \"title\": \"Vendored shared code, and a compatibility gate the bundle owns\", \"state\": \"approved\"}, {\"id\": \"ADR-0018\", \"title\": \"One lifecycle surface, and a record index that joins it\", \"state\": \"approved\"}, {\"id\": \"ADR-0019\", \"title\": \"Anchored comments, kept as a durable ledger\", \"state\": \"approved\"}, {\"id\": \"ADR-0020\", \"title\": \"Viewer parts and an author-time build\", \"state\": \"decided\"}, {\"id\": \"ADR-0021\", \"title\": \"book-index.md gains a nano tier, and full-book loading stops being unconditional\", \"state\": \"decided\"}, {\"id\": \"ADR-0022\", \"title\": \"Gate documents projected into the record index\", \"state\": \"decided\"}], \"design\": [{\"id\": \"book-index-tiering\", \"name\": \"book-index-tiering\", \"outcome\": \"book-index.md offers nano, mini, and full tiers for all 14 vendored books, with a 3-nano-minimum judgment-gated escalation rule; Review/Maintenance's mandatory 14-file security corpus load gets the same cheap-signal-before-expensive-read shape, opposite default bias, no vendoring.\", \"stage\": \"review\"}, {\"id\": \"build-workflow-polish\", \"name\": \"build-workflow-polish\", \"outcome\": \"An engineer running architect:design into implement:start never loses the handoff, 04-slices.md is always written in the shape verify_gate.py actually parses, a validated slice is committed without an extra prompt, and the bundle viewer borrows the tab-based Diagram / Data flow / Data types layout a colleague prototyped.\", \"stage\": \"backlog\"}, {\"id\": \"cobuilder-implement\", \"name\": \"cobuilder-implement\", \"outcome\": \"The architecture design produced by cobuilder-architect carries directly into implementation: its intent is decomposed into testable epics, and each epic ships as vertical slices that an independent validator scores at 0.90 or better before the next one starts.\", \"stage\": \"approved\"}, {\"id\": \"design-mode\", \"name\": \"design-mode\", \"outcome\": \"An engineer can design a change with the plugin before writing it, and the resulting intent, ADR, and diagrams carry through to the pull request that implements it.\", \"stage\": \"implemented\"}, {\"id\": \"gate-doc-surfacing\", \"name\": \"gate-doc-surfacing\", \"outcome\": \"Gate documents (03-program-design.md, epic-<id>-design.md) are readable from the viewer's Builds view, not just from docs/plans/ or a one-off Artifact publish.\", \"stage\": \"decided\"}, {\"id\": \"inflight-record-store\", \"name\": \"inflight-record-store\", \"outcome\": \"A reader on the main branch sees every piece of in-flight work across all open pull requests, and no pull request owner hits a merge conflict caused by that view.\", \"stage\": \"backlog\"}, {\"id\": \"lean-bundle-diffs\", \"name\": \"lean-bundle-diffs\", \"outcome\": \"A fresh clone of this repo is not weighed down by every merged PR's full diff text, and the viewer still shows a diff for any PR a reader opens.\", \"stage\": \"backlog\"}, {\"id\": \"maintainable-viewer\", \"name\": \"maintainable-viewer\", \"outcome\": \"An engineer changes one view mode of the bundle viewer by opening one file of a few hundred lines, and the file that ships is still one self-contained HTML page that publishes as an Artifact unchanged.\", \"stage\": \"backlog\"}, {\"id\": \"plugin-split\", \"name\": \"plugin-split\", \"outcome\": \"A user installs one stage of the architecture lifecycle without the other three, and four independently-installed plugins write into one bundle without a stale copy destroying authored or paid content.\", \"stage\": \"implemented\"}, {\"id\": \"ubiquitous-language\", \"name\": \"ubiquitous-language\", \"outcome\": \"One term means one thing across every plugin, skill, design, and ADR in the repo, and a new or colliding term cannot reach a merged pull request undetected.\", \"stage\": \"backlog\"}], \"epic\": [{\"id\": \"book-index-tiering/E1\", \"design\": \"book-index-tiering\", \"epic_id\": \"E1\", \"branch\": \"design/book-index-tiering\", \"pr\": 16, \"state\": \"open\", \"note\": null}, {\"id\": \"build-workflow-polish/E1\", \"design\": \"build-workflow-polish\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"Stage 7 in architect's references/design-mode.md ends design mode after creating the first branch, with no pointer to what runs next. Add one line after the branch is created: 'Design mode's work ends here. To build this design's epics, run /implement:start.' Do not have design mode invoke implement:start itself — architecture modes are self-only in scope, and implement already owns Gate 1's design-record lookup (build/SKILL.md's 'Optional: design mode record' section). This is a one-line prose addition to a mode-governing reference file, so it still needs the Gate-selection exception's behavioral rubric, not a skip.\"}, {\"id\": \"build-workflow-polish/E2\", \"design\": \"build-workflow-polish\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"04-slices.md's Gate 4a instructions in build/SKILL.md describe the table only through the worked example, and never say the table must match shared/slice_table.py's exact column shape (six columns: #, Epic, Slice, Ends with, Score, State, with the epic-header row's precise empty-cell pattern) or point to that module as the source of truth. Add an explicit sentence and a link, so an authoring session does not freehand a table shape verify_gate.py silently fails to parse. Consider also having verify_gate.py surface parse_table()'s `unparsed` list by name when it is non-empty, instead of only reporting downstream symptoms like a missing epic.\"}, {\"id\": \"build-workflow-polish/E3\", \"design\": \"build-workflow-polish\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"completed\", \"note\": \"build/SKILL.md's 'After each slice' section (the four numbered steps following the red-green-validate loop) records the score and asks 'Proceed to the next slice, or adjust direction?' but never commits. In practice this produces a spurious 'want to commit this checkpoint first?' prompt after every slice instead of an automatic commit. Change step 2 (or add a step) so that once VALIDATE scores a slice at or above 0.90, the orchestrator commits the slice's changes to git immediately, before asking whether to proceed — no confirmation question for the commit itself, only for what comes after. A score below threshold still routes through the existing gap decision tree in references/validation-scoring.md, and does not commit. Below-threshold slices accepted with reservations after three attempts should also commit, since that path already represents a final accepted state, not a pending one.\"}, {\"id\": \"build-workflow-polish/E4\", \"design\": \"build-workflow-polish\", \"epic_id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A colleague's scratch artifact (/private/tmp/.../diagram-viewer.html, session-local, not part of this repo) uses a top-of-panel tab switcher across three views — Diagram, Data flow, Data types — instead of stacking them, plus a right-hand info rail. Evaluate adopting the same top-tab pattern for viewer/index.html's per-PR diagram levels (currently level 1/2/3 switching, per ADR-0020's viewer/src/ plan), and separately evaluate narrowing the left-hand navigation rail so it takes less width once tabs handle top-level switching. This touches only the viewer, so it can run through architect:design on its own without waiting on E1-E3.\"}, {\"id\": \"cobuilder-implement/E1\", \"design\": \"cobuilder-implement\", \"epic_id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Shares a branch and a pull request with the design-mode design. See ADR-0013: a branch may carry more than one design.\"}, {\"id\": \"cobuilder-implement/E2\", \"design\": \"cobuilder-implement\", \"epic_id\": \"E2\", \"branch\": \"docs/post-pr11-cleanup\", \"pr\": 12, \"state\": \"open\", \"note\": \"Gate 4b is enforced instead of merely specified. The per-epic technical solution design already exists in SKILL.md section 4b, with a six-section template, and five downstream files reference the artifact. Nothing checks that it was written. Checked on 2026-08-24: zero epic-<id>-design.md files exist, and six epics shipped without one. Four failures allow that. Gate 4's only exit check counts rubrics, so a Gate 4 that skips 4b still passes. slice-loop.js builds the epic-design path and never checks the file exists, so the red and green agents are told to read a file that is absent and quietly fall back to 03-program-design.md. rubric-authoring.md tells the author to update the technical design first, and nobody checks that either. No test asserts the artifact. The root cause is that 4b is the only Gate 4 sub-step whose output nothing downstream mechanically requires: the loop cannot score without rubrics or iterate without a slice plan, but it can read an absent design. This epic adds the exit check, the existence guard in the workflow, a test, and sub-step tracking in 00-status.md so an approved Gate 4 cannot hide a missing 4b. Raised 2026-08-22, re-scoped 2026-08-24 from 'write the process' to 'enforce the process' after reading the skill.\"}, {\"id\": \"design-mode/E1\", \"design\": \"design-mode\", \"epic_id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Design mode as a pre-code interview and challenge gate. Shares a branch and a pull request with the cobuilder-implement design. See ADR-0013: a branch may carry more than one design.\"}, {\"id\": \"gate-doc-surfacing/E1\", \"design\": \"gate-doc-surfacing\", \"epic_id\": \"E1\", \"branch\": \"design/gate-doc-surfacing\", \"pr\": null, \"state\": \"completed\", \"note\": \"shared/build_index.py: add PROGRAM_DESIGN_FIELDS and EPIC_DESIGN_FIELDS projection tuples plus project_program_design/project_epic_design, following the project_fields(source, fields) pattern used by project_goal/project_intent/project_assessment. Parse docs/plans/<slug>/03-program-design.md and epic-<id>-design.md; emit program_design and epic_design entities into data/index.json. Extend feature_gates join entries with a doc reference where one exists.\", \"design_doc\": \"gate-doc-surfacing/E1\"}, {\"id\": \"gate-doc-surfacing/E2\", \"design\": \"gate-doc-surfacing\", \"epic_id\": \"E2\", \"branch\": \"design/gate-doc-surfacing\", \"pr\": null, \"state\": \"completed\", \"note\": \"plugins/artifact/viewer/index.html: make gate-rail-grid cards (renderBuildsMainContent, ~line 3249) clickable, opening a gate-doc sheet modeled on openAdrSheet. Add a design-doc chip to renderEpicCard (~line 3282) for epics with an epic_design entity.\", \"design_doc\": \"gate-doc-surfacing/E2\"}, {\"id\": \"inflight-record-store/E1\", \"design\": \"inflight-record-store\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Open pull requests become entities. collect_pull_requests() merges gh pr list --state all metadata over the narrated story.json entities. An open pull request gets an entity with state: \\\"open\\\" and a narrated: false flag. This removes the placeholder in refine_epic_status(). It reads metadata only, checks out no branch, and creates no conflict. This is the tracer bullet, and it is useful on its own. It does not depend on E2, E3, or E4, and can ship alone.\"}, {\"id\": \"inflight-record-store/E2\", \"design\": \"inflight-record-store\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A record store with a merge strategy. One file per record, append-only, with a defined resolution rule for a concurrent write. Modelled on beads. This epic decides the storage shape and nothing else.\"}, {\"id\": \"inflight-record-store/E3\", \"design\": \"inflight-record-store\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Cross-branch collection without a conflict. Records written by a session on a branch reach the shared view without any branch writing a shared generated file.\"}, {\"id\": \"inflight-record-store/E4\", \"design\": \"inflight-record-store\", \"epic_id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The active-work view. The viewer renders in-flight work beside merged history, so a reader sees one timeline rather than two.\"}, {\"id\": \"lean-bundle-diffs/E1\", \"design\": \"lean-bundle-diffs\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Decide the replacement shape: the viewer reads a diff by running git diff against the PR's recorded commit SHAs at serve time (view mode already runs from inside the repo), versus keeping diffs-pr{N}.js exactly as it is today but gitignoring it, regenerating it from git on demand the way exports/branch-*/diff.json already works for generate mode. Either removes the committed weight; they differ in whether a served bundle still needs a live git checkout next to it.\"}, {\"id\": \"lean-bundle-diffs/E2\", \"design\": \"lean-bundle-diffs\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Whichever shape E1 picks, implement it in extract_diffs.py and, if the viewer starts requesting diffs at view time instead of reading a static file, in artifact's serve_bundle.py. Update verify_bundle.py's expectations accordingly.\"}, {\"id\": \"lean-bundle-diffs/E3\", \"design\": \"lean-bundle-diffs\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Migrate the existing committed bundles (self, and the two fixture bundles) so history stops growing: remove the checked-in diffs-pr{N}.js files, or move them under .gitignore, per E1's decision. This does not touch story.json's authored fields, so it is a bundle-file change, not a schema migration.\"}, {\"id\": \"maintainable-viewer/E1\", \"design\": \"maintainable-viewer\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The build exists and is guarded. build_viewer.py concatenates ordered parts under viewer/src/ into the committed viewer/index.html, and a test rebuilds from the parts and fails on any difference. The first split is mechanical only, cut at the file's own 35 banner comments, with no line rewritten. This epic is the tracer bullet: it proves or disproves that the single IIFE separates by concatenation alone, which is the abort condition the whole design rests on.\"}, {\"id\": \"maintainable-viewer/E2\", \"design\": \"maintainable-viewer\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The exporter seam becomes declared. export_artifact.py stops matching incidental literals and matches named markers the parts emit deliberately. Today it hard-errors with not-found-verbatim when a literal moves, so the publish pipeline breaks silently under any refactor until somebody publishes. This must land before the parts are rewritten, not after, or every later epic risks breaking publishing without a test noticing.\"}, {\"id\": \"maintainable-viewer/E3\", \"design\": \"maintainable-viewer\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"One definition per fragment. A template layer replaces the 45 innerHTML string blocks, and the rail-item, empty-card, and empty-detail fragments collapse from 4, 16, and 18 copies to one each. The viewer uses no template element today, so the mechanism is a design choice this epic makes and does not inherit.\"}, {\"id\": \"maintainable-viewer/E4\", \"design\": \"maintainable-viewer\", \"epic_id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Each mode owns its own part. The five view modes stop interleaving: the 50 scattered mode-predicate calls collapse into one registry, and renderContextsMainContent, renderBuildsMainContent, and renderMainContent stop being three functions of over 200 lines that live beside each other. Adding a sixth mode becomes one new part plus one registry entry.\"}, {\"id\": \"plugin-split/E1\", \"design\": \"plugin-split\", \"epic_id\": \"E1\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"One plugin becomes five. The whole packaging split: the mode renames inside today's single plugin, the deleted duplicate explore-design command, the 33 cross-pillar references, the five manifests, and the two ports. The rename is a rotation, not a split: odyssey submit becomes cobuilder-pr:generate, and the old odyssey generate, which narrates merged history, becomes cobuilder-pr:review. Four slices.\"}, {\"id\": \"plugin-split/E2\", \"design\": \"plugin-split\", \"epic_id\": \"E2\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"Shared code survives an install. Prove the marketplace symlink dereference, then vendor the four bundle modules and the two shared skills. One slice, because it is a spike whose result decides whether the rest of the design stands. goal.json.abort_if names its failure.\"}, {\"id\": \"plugin-split/E3\", \"design\": \"plugin-split\", \"epic_id\": \"E3\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"The seam is version-safe. require_compatible(), min_reader_schema, the generators map, bundle format 3, and schema 1.3, so two plugins at different versions cannot corrupt one bundle. Two slices.\"}, {\"id\": \"plugin-split/E4\", \"design\": \"plugin-split\", \"epic_id\": \"E4\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"The record index. build_index.py replaces the two projection scripts, emits every entity with a stable id, and resolves every join including an ADR to its pull request. Two slices.\"}, {\"id\": \"plugin-split/E5\", \"design\": \"plugin-split\", \"epic_id\": \"E5\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"One lifecycle surface. The Decisions, Contexts, and Builds modes, the Backlog lane, and moving generated pages out of the foreign directory into the bundle. Two slices (10, 11).\"}, {\"id\": \"plugin-split/E6\", \"design\": \"plugin-split\", \"epic_id\": \"E6\", \"branch\": \"design/design-mode\", \"pr\": 11, \"state\": \"open\", \"note\": \"The reply channel. The append-only ledger and its projection, the anchor computed from the live DOM, the write endpoint, and the background wake command. Three slices (12, 13, 14).\"}, {\"id\": \"plugin-split/E7\", \"design\": \"plugin-split\", \"epic_id\": \"E7\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Threads read as conversations. The drawer renders a reply under the comment it answers. Deferred past E6 on purpose: E6 ships the record shape, so this epic adds no migration and carries no slice yet.\"}, {\"id\": \"plugin-split/E8\", \"design\": \"plugin-split\", \"epic_id\": \"E8\", \"branch\": \"rename-plugins-drop-cobuilder-prefix\", \"pr\": 17, \"state\": \"merged\", \"note\": \"The cobuilder- prefix drops from the four non-umbrella plugins named in E1's table (cobuilder-architect, cobuilder-pr, cobuilder-artifact, cobuilder-implement become architect, pr, artifact, implement), since it duplicates the marketplace repo's own name. cobuilder-full-lifecycle keeps its prefix, being the umbrella the repo's own name already carries. Merged as PR #17, then a same-epic follow-up (uncommitted at the time this entry was written) fixes a second-order bug the rename introduced: two of the four renamed plugins (implement, artifact) already shipped a skill folder named after themselves, and once the plugin took that same name too, Skill() resolution inside Claude Code broke -- reproduced directly, not environment-specific. Fixed by renaming the colliding skill folders (implement's skill to build, its command to start; artifact's skill to cobuilder-artifacts) and, for consistency, cobuilder-full-lifecycle's orientation skill to cobuilder-full. See ADR-0016's addendum.\"}, {\"id\": \"ubiquitous-language/E1\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E1\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"One language record, and it is the tracer bullet. docs/architecture/language.md becomes the single source of truth for a term, its owning context, its meaning, and what it must not be confused with. It is seeded from CLAUDE.md's existing Vocabulary table and from cobuilder-packaging's canvas section 3. shared/build_index.py projects it into the record index, so the viewer can render it. This epic is independently shippable and does not depend on E2 through E5.\"}, {\"id\": \"ubiquitous-language/E2\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E2\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Design mode gains a language stage. A design that introduces a new term must declare it or reuse an existing entry from the language record. A collision with an existing term becomes a challenge-stage finding. This fits design mode's existing no-citation-no-challenge rule, because the language record gives the challenge stage something concrete to cite.\"}, {\"id\": \"ubiquitous-language/E3\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E3\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"The mechanical consumer, in cobuilder-implement. A Gate 4b technical solution design that names a concept absent from the language record fails the gate. This epic is what stops the whole program from rotting the way canvas section 3 did. E1 and E2 without E3 are documentation waiting to go stale.\"}, {\"id\": \"ubiquitous-language/E4\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E4\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"A verifier that exits non-zero. A script finds a term used in a design or an ADR and absent from the language record, and fails the check. It is modeled on plugins/cobuilder-implement/scripts/verify_gate.py, which exits non-zero on any failed Gate 4 sub-step. The honest open question is that deciding what counts as a term mechanically, rather than by judgement, is the hard part of this epic and may need its own exploration.\"}, {\"id\": \"ubiquitous-language/E5\", \"design\": \"ubiquitous-language\", \"epic_id\": \"E5\", \"branch\": null, \"pr\": null, \"state\": \"planned\", \"note\": \"Repair the existing drift. Refresh cobuilder-packaging's canvas section 3 and its boundary.yaml public_interface against the shipped five-plugin tree, delete the now-false homonym comment, and reconcile the two glossaries into E1's record.\"}], \"context\": [{\"id\": \"cobuilder-packaging\", \"name\": \"Plugin packaging and dispatch\", \"path\": \".\", \"verifies\": [\"commands\", \"skills\", \"scripts\", \"viewer\", \"docs\"]}], \"district\": [{\"id\": \"skills\", \"label\": \"Skills\", \"paths\": [\"skills\"]}, {\"id\": \"scripts\", \"label\": \"Scripts\", \"paths\": [\"scripts\"]}, {\"id\": \"commands\", \"label\": \"Commands\", \"paths\": [\"commands\"]}, {\"id\": \"viewer\", \"label\": \"Viewer\", \"paths\": [\"viewer\"]}, {\"id\": \"docs\", \"label\": \"Docs\", \"paths\": [\"docs\"]}, {\"id\": \".cobuilder-architect\", \"label\": \"Bundle Store\", \"paths\": [\".cobuilder-architect\"]}], \"boundary_rule\": [{\"id\": \"cobuilder-packaging/forbidden-dependency/1\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"another plugin's files (post-ADR-0016 split)\", \"why\": \"ADR-0016: 'No plugin reads or imports another plugin's files. A plugin may hand off to another plugin's mode by name, and the state that crosses the handoff travels in the bundle and in docs/.' Not yet enforceable -- only one plugin (cobuilder-architect) exists today, so there is no sibling to violate this against. Recorded now so the rule is in place before the split lands.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/2\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"skills/odyssey/SKILL.md\", \"from\": \"skills/architecture\", \"why\": \"SMELL: skills/architecture/SKILL.md lines 83-84 hardcode the path 'skills/odyssey/SKILL.md' inside Design mode's Hub-resolution step. Verified by grep. Harmless today (both skills ship in one plugin), but this is exactly the edge ADR-0016 forbids once architecture and odyssey become separate plugins (cobuilder-architect and cobuilder-pr). Needs an ADR-sanctioned fix (e.g. duplicate the Hub-resolution text, or move it to a shared vendored module) before the split, or it becomes real drift the day the split ships.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/3\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"skills/architecture/SKILL.md\", \"from\": \"skills/odyssey\", \"why\": \"SMELL: skills/odyssey/SKILL.md lines 32-33 name and point at 'skills/architecture/SKILL.md' when describing where Design mode lives. Same coupling as the entry above, reversed direction. Verified by grep.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/4\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"skills/architecture/references/*\", \"from\": \"scripts (build_designs.py, build_adrs.py, validate_decision_state.py)\", \"why\": \"SMELL: three scripts embed a literal 'skills/architecture/references/...' path inside a validation-failure error message (build_designs.py:299, build_adrs.py:217, validate_decision_state.py:433). Text, not an import, so it does not execute a cross-plugin call today. Still a coupling a post-split remediation hint cannot resolve, because the reading plugin (e.g. cobuilder-pr) cannot open a path inside cobuilder-architect's cache.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/5\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"skills/odyssey/references/*\", \"from\": \"scripts (build_diagrams.py, render_review.py, verify_bundle.py)\", \"why\": \"SMELL: same pattern, reversed. build_diagrams.py:9 and :273, render_review.py:9, and verify_bundle.py:86 embed 'skills/odyssey/references/...' in comments or error text. Verified by grep.\"}}, {\"id\": \"cobuilder-packaging/forbidden-dependency/6\", \"context\": \"cobuilder-packaging\", \"kind\": \"forbidden-dependency\", \"detail\": {\"target\": \"scripts/*.py (as a Python import, not a shell invocation)\", \"from\": \"viewer/index.html\", \"why\": \"The viewer never imports or executes anything under scripts/. Its one mention of 'scripts/' (line 854) is a code comment naming the script that produces data/diagrams.js, not a reference the browser resolves. Recorded as a confirmed absence, not a smell -- this is the rule holding, not breaking.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/1\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"plugin-manifest\", \"path\": \".claude-plugin/\", \"public_interface\": [\"plugin.json\", \"marketplace.json\"], \"allowed_inbound\": [], \"allowed_outbound\": [], \"rule\": \"Static JSON only. No module in this repo reads or writes it at runtime.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/2\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"commands\", \"path\": \"commands/\", \"members\": [\"baseline.md\", \"generate.md\", \"view.md\", \"publish.md\", \"submit.md\", \"design.md\", \"review.md\", \"maintenance.md\", \"decisions.md\", \"describe.md\", \"debug.md\", \"explore-design.md\"], \"public_interface\": [], \"allowed_inbound\": [], \"allowed_outbound\": [\"skills-architecture\", \"skills-odyssey\"], \"rule\": \"Each command file makes exactly one Skill(...) dispatch call and nothing else. Verified: every commands/*.md greps to exactly one Skill(\\\"architecture\\\"|\\\"odyssey\\\", ...) line.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/3\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-architecture\", \"path\": \"skills/architecture/\", \"public_interface\": [\"Skill(\\\"architecture\\\", args=...)\"], \"allowed_inbound\": [\"commands\"], \"allowed_outbound\": [\"scripts\", \"skills-mermaid\", \"skills-ste-writing\"], \"rule\": \"SMELL: also reaches skills-odyssey via a hardcoded SKILL.md path reference (see forbidden_dependencies). Should be [] toward skills-odyssey once ADR-0016 lands.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/4\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-odyssey\", \"path\": \"skills/odyssey/\", \"public_interface\": [\"Skill(\\\"odyssey\\\", args=...)\"], \"allowed_inbound\": [\"commands\"], \"allowed_outbound\": [\"scripts\", \"skills-mermaid\", \"skills-ste-writing\"], \"rule\": \"SMELL: also reaches skills-architecture via a hardcoded SKILL.md path reference (see forbidden_dependencies). Should be [] toward skills-architecture once ADR-0016 lands.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/5\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-mermaid\", \"path\": \"skills/mermaid/\", \"public_interface\": [\"Skill(\\\"mermaid\\\")\", \"Skill(\\\"cobuilder-architect:mermaid\\\")\"], \"allowed_inbound\": [\"skills-architecture\", \"skills-odyssey\"], \"allowed_outbound\": [], \"rule\": \"Diagram-authoring rules only. Invoked by a per-PR/per-design subagent, never by the orchestrating Claude directly, per CLAUDE.md.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/6\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-ste-writing\", \"path\": \"skills/ste-writing/\", \"public_interface\": [\"Skill(\\\"ste-writing\\\")\", \"Skill(\\\"cobuilder-architect:ste-writing\\\")\", \"ste-lint.py\"], \"allowed_inbound\": [\"skills-architecture\", \"skills-odyssey\"], \"allowed_outbound\": [], \"rule\": \"Writing-style rules and a rules-only linter. No dependency on any other module verified.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/7\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"skills-collaborate-with-user\", \"path\": \"skills/collaborate-with-user/\", \"public_interface\": [\"Skill(\\\"collaborate-with-user\\\")\"], \"allowed_inbound\": [], \"allowed_outbound\": [], \"rule\": \"References skills/architecture/SKILL.md's 'Report Generation' section in prose (line 95) to say it does not duplicate that convention. This is documentation cross-talk, not a code or Skill-dispatch edge -- no forbidden_dependencies entry warranted.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/8\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"scripts\", \"path\": \"scripts/\", \"members\": [\"_bundle_meta.py\", \"_manifest.py\", \"build_adrs.py\", \"build_designs.py\", \"build_diagrams.py\", \"compute_scores.py\", \"export_artifact.py\", \"export_index.py\", \"extract_diffs.py\", \"extract_story.py\", \"generate_audio.py\", \"generate_prompts.py\", \"html_to_pdf.py\", \"migrate_bundle.py\", \"record_publish.py\", \"render_review.py\", \"validate_decision_state.py\", \"verify_bundle.py\"], \"public_interface\": [\"_bundle_meta.SCHEMA_VERSION\", \"_bundle_meta.SCHEMA_VERSION_KNOWN\", \"_bundle_meta.CURRENT_BUNDLE_FORMAT\", \"_manifest.rewrite_manifest\"], \"allowed_inbound\": [\"skills-architecture\", \"skills-odyssey\"], \"allowed_outbound\": [], \"rule\": \"Every script that reads or writes bundle data imports _bundle_meta.SCHEMA_VERSION for its own version stamp. _bundle_meta.py itself is a leaf: stdlib-only, no imports beyond __future__. ADR-0017's require_compatible() gate does not exist in this file yet -- verified absent.\"}}, {\"id\": \"cobuilder-packaging/module-invariant/9\", \"context\": \"cobuilder-packaging\", \"kind\": \"module-invariant\", \"detail\": {\"id\": \"viewer\", \"path\": \"viewer/\", \"members\": [\"index.html\"], \"public_interface\": [\"window.STORY, window.ODYSSEY, window.DIFFS, window.ADRS, window.DIAGRAMS, window.DESIGNS (populated by sibling <script src> tags)\"], \"allowed_inbound\": [], \"allowed_outbound\": [], \"rule\": \"viewer/index.html never reaches into scripts/ (grep-verified: the one 'scripts/' mention is a code comment, not a live reference). It is not self-contained -- see CLAUDE.md 'The viewer is not self-contained' -- and depends on being served from the bundle root, a sibling of viewer/.\"}}, {\"id\": \"cobuilder-packaging/context-map/1\", \"context\": \"cobuilder-packaging\", \"kind\": \"context-map\", \"detail\": {\"with\": \"bundle-directory\", \"pattern\": \"open-host-service\", \"note\": \"Not yet a documented bounded context. Scripts write it, the viewer reads it; neither reads the other's source. Data-only, versioned by bundle.json / story.json meta.schema_version.\"}}], \"pull_request\": [{\"id\": 1, \"title\": \"Master\", \"state\": \"merged\", \"commit\": \"7bd668f\", \"date\": \"2026-07-22\"}, {\"id\": 2, \"title\": \"docs: add CLAUDE.md with codebase orientation + artifact feasibility findings\", \"state\": \"merged\", \"commit\": \"96c1531\", \"date\": \"2026-07-24\"}, {\"id\": 3, \"title\": \"Unify bundle storage under .prodyssey/, self-analysis into .prodyssey/self/\", \"state\": \"merged\", \"commit\": \"fb4be22\", \"date\": \"2026-07-28\"}, {\"id\": 4, \"title\": \"Mermaid diagrams for levels 1-3, and self-upgrading bundles\", \"state\": \"merged\", \"commit\": \"065654d\", \"date\": \"2026-08-02\"}, {\"id\": 5, \"title\": \"Close the API-key trust boundary, and fix three correctness bugs\", \"state\": \"merged\", \"commit\": \"789081f\", \"date\": \"2026-08-05\"}, {\"id\": 6, \"title\": \"Pr Odyssey Improvements 7Suouc\", \"state\": \"merged\", \"commit\": \"7473b7f\", \"date\": \"2026-08-05\"}, {\"id\": 7, \"title\": \"Pr Odyssey Improvements 7Suouc\", \"state\": \"merged\", \"commit\": \"630aa4f\", \"date\": \"2026-08-05\"}, {\"id\": 8, \"title\": \"Ste Writing Style Claude Fd5Fzs\", \"state\": \"merged\", \"commit\": \"d9a81d7\", \"date\": \"2026-08-06\"}, {\"id\": 9, \"title\": \"Deduplicate rewrite_manifest(); add interview self-consistency check; STE clarity pass\", \"state\": \"merged\", \"commit\": \"139de94\", \"date\": \"2026-08-06\"}, {\"id\": 10, \"title\": \"Swift Best Practices Fbx48M\", \"state\": \"merged\", \"commit\": \"6b2fce6\", \"date\": \"2026-08-06\"}, {\"id\": 11, \"title\": \"Design cobuilder-architect and cobuilder-implement, split repo into five plugins and skills\", \"state\": \"merged\", \"commit\": \"a389892\", \"date\": \"2026-08-25\"}, {\"id\": 12, \"title\": \"Retire adr-draft, close Gate 4b retrospectively, and record two ADR maintenance rules\", \"state\": \"merged\", \"commit\": \"be93c04\", \"date\": \"2026-08-25\"}, {\"id\": 16, \"title\": \"book-index.md gains a nano tier, and the security corpus stops reading unconditionally in full\", \"state\": \"open\", \"commit\": \"6bad882c30e7ea84a2e1d950e604859ffd211595\", \"date\": \"2026-08-26\"}, {\"id\": 17, \"title\": \"Rename plugins to drop the cobuilder- prefix; fix Mermaid click/copy-paste in the feedback drawer\", \"state\": \"open\", \"commit\": \"1d0c5ceb27a7df8dfaf4e3d36e2ebf9993a27735\", \"date\": \"2026-08-26\"}], \"slice\": [{\"id\": \"cobuilder-family/4\", \"feature\": \"cobuilder-family\", \"n\": 4, \"title\": \"Renames inside today's single plugin\", \"ends_with\": \"Six architecture modes, `explore-design` deleted, `submit` renamed to `generate`, and the old `generate` renamed to `review`\", \"score\": \"1.00\", \"attempts\": 2, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/5\", \"feature\": \"cobuilder-family\", \"n\": 5, \"title\": \"The 33 cross-pillar references are fixed\", \"ends_with\": \"The grep regression test passes while the repo is still one plugin\", \"score\": \"1.00\", \"attempts\": 3, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/6\", \"feature\": \"cobuilder-family\", \"n\": 6, \"title\": \"Five plugins, five manifests\", \"ends_with\": \"`claude plugin validate` passes on each, and the family installs\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/7\", \"feature\": \"cobuilder-family\", \"n\": 7, \"title\": \"The two ports land\", \"ends_with\": \"`cobuilder-implement` renamed and shipped, `collaborate-with-user` folded in, the orientation skill written\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/1\", \"feature\": \"cobuilder-family\", \"n\": 1, \"title\": \"**Tracer bullet: two plugins, one shared module**\", \"ends_with\": \"Two throwaway plugins install from a local marketplace and both resolve the shared module from their own cache\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/2\", \"feature\": \"cobuilder-family\", \"n\": 2, \"title\": \"The compatibility gate exists\", \"ends_with\": \"`require_compatible()` and `stamp_generator()` pass their tests, called by nothing yet\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/3\", \"feature\": \"cobuilder-family\", \"n\": 3, \"title\": \"The gate is wired and the bundle migrates\", \"ends_with\": \"Format 3, `min_reader_schema`, the generators map, every writer gated\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/8\", \"feature\": \"cobuilder-family\", \"n\": 8, \"title\": \"The index holds the entities\", \"ends_with\": \"`build_index.py` emits every entity, and the two scripts it replaces are gone\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/9\", \"feature\": \"cobuilder-family\", \"n\": 9, \"title\": \"The index resolves the joins\", \"ends_with\": \"`adr_to_pull_request`, `slice_to_epic`, `district_uncovered`, and freshness\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/10\", \"feature\": \"cobuilder-family\", \"n\": 10, \"title\": \"The Decisions and Contexts modes\", \"ends_with\": \"A person browses every decision and every context, with the anchor distinction visible\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/11\", \"feature\": \"cobuilder-family\", \"n\": 11, \"title\": \"The Builds mode and the Backlog lane\", \"ends_with\": \"Gates driven by the status document, the Backlog lane computed from the index, pages moved into the bundle\", \"score\": \"0.92\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/12\", \"feature\": \"cobuilder-family\", \"n\": 12, \"title\": \"The ledger and its projection\", \"ends_with\": \"Append, fold, and project, with no server involved\", \"score\": \"0.92\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/13\", \"feature\": \"cobuilder-family\", \"n\": 13, \"title\": \"The anchor and the write endpoint\", \"ends_with\": \"Click to anchor a sentence, `POST /feedback` appends, the drawer keeps the text in view\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"cobuilder-family/14\", \"feature\": \"cobuilder-family\", \"n\": 14, \"title\": \"The wake command and the whole loop\", \"ends_with\": \"The background command blocks and returns new threads, and the loop runs once end to end\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"gate-doc-surfacing/1\", \"feature\": \"gate-doc-surfacing\", \"n\": 1, \"title\": \"Tracer bullet: constants and empty entity lists\", \"ends_with\": \"`data/index.json` carries `program_design` and `epic_design` keys (empty lists when no gate docs exist yet)\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"gate-doc-surfacing/2\", \"feature\": \"gate-doc-surfacing\", \"n\": 2, \"title\": \"Real parsing: project Gate 3 and Gate 4b docs\", \"ends_with\": \"A repo with a `03-program-design.md` and an `epic-*-design.md` produces populated `program_design`/`epic_design` entities, and `joins.feature_gates[slug][3].doc` points at the projected id\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"gate-doc-surfacing/3\", \"feature\": \"gate-doc-surfacing\", \"n\": 3, \"title\": \"Edge cases: missing files, malformed epic id, no plans dir\", \"ends_with\": \"A plan directory with no gate docs, and a repo with no `docs/plans/` at all, both still produce a clean index build with empty (not missing) entity keys\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"gate-doc-surfacing/4\", \"feature\": \"gate-doc-surfacing\", \"n\": 4, \"title\": \"Tracer bullet: clickable Gate Rail card opens an empty sheet\", \"ends_with\": \"Clicking a Gate Rail card with a `doc` opens a sheet showing the gate's title and an empty body; a card with no `doc` stays non-interactive\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"gate-doc-surfacing/5\", \"feature\": \"gate-doc-surfacing\", \"n\": 5, \"title\": \"Real content: render the gate doc body, add the epic chip\", \"ends_with\": \"The sheet renders `body_md` through the existing `renderMarkdown()`; an epic card with a `design_doc` shows a chip that opens the same sheet scoped to that epic\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}, {\"id\": \"gate-doc-surfacing/6\", \"feature\": \"gate-doc-surfacing\", \"n\": 6, \"title\": \"Edge cases: no entity, mutual exclusivity with other sheets\", \"ends_with\": \"A Gate Rail card whose gate has no matching doc opens nothing; opening the gate-doc sheet closes any open ADR/assessment/comments sheet, and vice versa\", \"score\": \"1.00\", \"attempts\": 1, \"state\": \"completed\"}], \"publication\": [{\"id\": \"exports/pr-2.html\", \"pull_request\": 2, \"artifact_url\": \"https://claude.ai/code/artifact/ea2c0f34-c5e5-46f7-b619-2bb7c72b5583\", \"published_at\": \"2026-07-22T09:56:34Z\"}, {\"id\": \"exports/index.html\", \"pull_request\": null, \"artifact_url\": \"https://claude.ai/code/artifact/4bf5fff8-f70e-4e68-8553-2b8a254c5e79\", \"published_at\": \"2026-07-22T09:56:48Z\"}], \"program_design\": [{\"id\": \"cobuilder-family\", \"feature_slug\": \"cobuilder-family\", \"gate\": 3, \"title\": \"Program Design: the cobuilder family\", \"body_md\": \"# Program Design: the cobuilder family\\n\\nGate 3. The decisions an agent would otherwise make silently while\\nimplementing Gate 2. Every one of them is cheap to change here and expensive\\nto change once a slice has landed.\\n\\nGoverned by ADR-0016, ADR-0017, ADR-0018, and ADR-0019. Where this document\\nand an ADR disagree, the ADR wins and this document is wrong.\\n\\n## Files\\n\\n### The repository becomes a marketplace\\n\\n```\\n.claude-plugin/marketplace.json        one file, five plugin entries\\nshared/                                 vendored by symlink into each plugin\\n  _bundle_meta.py\\n  _manifest.py\\n  migrate_bundle.py\\n  verify_bundle.py\\n  skills/ste-writing/\\n  skills/mermaid/\\nplugins/\\n  cobuilder-architect/\\n  cobuilder-pr/\\n  cobuilder-implement/\\n  cobuilder-artifact/\\n  cobuilder-full-lifecycle/\\ntests/                                  new, see Test plan\\n```\\n\\n`shared/` sits at the marketplace root and each plugin carries a symlink to\\nit at `<plugin>/shared`. The install copy dereferences the link, so each\\nplugin's cache holds its own real copy. This is the mechanism ADR-0017\\nchose and the one E2 exists to prove.\\n\\n### Where each existing file lands\\n\\n| From | To | Why |\\n|---|---|---|\\n| `skills/architecture/` | `cobuilder-architect` | design, review, maintenance, decisions, describe, debug — six modes, since `explore-design` is deleted |\\n| `skills/odyssey/` | `cobuilder-pr` | baseline, generate, review. `view` and `publish` go to `cobuilder-artifact` instead |\\n| `skills/ste-writing/`, `skills/mermaid/` | `shared/skills/` | both are cited from both pillars, which is exactly what vendoring is for |\\n| `.claude/skills/collaborate-with-user/` | `cobuilder-artifact`, folded into its `SKILL.md` and `references/` | it presents a decision, which is the artifact pillar's job |\\n| `viewer/index.html` | `cobuilder-artifact` | the reading surface |\\n| `extract_story.py`, `extract_diffs.py`, `generate_prompts.py`, `generate_audio.py`, `build_diagrams.py`, `render_review.py` | `cobuilder-pr` | all six only run inside the per-PR sweep or submit |\\n| `build_adrs.py`, `build_designs.py` | deleted | `build_index.py` replaces both, see below |\\n| `validate_decision_state.py`, `compute_scores.py`, `html_to_pdf.py` | `cobuilder-architect` | decisions mode and the two review reports |\\n| `export_artifact.py`, `export_index.py`, `record_publish.py` | `cobuilder-artifact` | publish |\\n| `_bundle_meta.py`, `_manifest.py`, `migrate_bundle.py`, `verify_bundle.py` | `shared/` | every plugin that touches a bundle needs all four |\\n| `~/.claude/skills/cobuilder-factory/` | `cobuilder-implement` | the port and the rename |\\n\\n### New files\\n\\n| File | Plugin | Purpose |\\n|---|---|---|\\n| `scripts/build_index.py` | `cobuilder-artifact` | builds `data/index.json` and `index.js` |\\n| `scripts/serve_bundle.py` | `cobuilder-artifact` | replaces `python3 -m http.server`, adds `POST /feedback` |\\n| `scripts/watch_feedback.py` | `cobuilder-artifact` | the background wake command |\\n| `skills/cobuilder-full/SKILL.md` | `cobuilder-full-lifecycle` | names the four modes and when to reach for each |\\n\\n**`build_index.py` lives in the artifact plugin and reads what the other\\npillars wrote.** Not only `cobuilder-architect`'s ADRs, designs, and\\ncontexts. Also `cobuilder-pr`'s `story.json`, its assessments, and its\\n`docs/pull-requests/`, and `cobuilder-implement`'s `docs/plans/`. That is not\\na seam violation. Every one of those paths is inside the *target repo* or\\ninside the bundle. None resolves into another plugin's root. This is the\\ndistinction ADR-0016 draws, and it is the one an implementing agent is most\\nlikely to get wrong.\\n\\nSettled at Gate 3 review: the index is a repo-wide artifact rather than one\\npillar's output, so it belongs with the pillar that presents the repo.\\n\\n### `cobuilder-artifact` owns the authoring skill as well as the viewer\\n\\n`collaborate-with-user` does not travel as a separate skill directory. Its\\nrules become part of `cobuilder-artifact`'s own `SKILL.md`, with the long\\nmaterial in `references/`: the composition rules, the theme-token rules, the\\nhonesty rule, and the relationship to the architecture skill's two report\\ntemplates. One plugin then owns both halves of presenting something to a\\nperson — the page it builds and the answer it collects.\\n\\n**The agent doing this work must invoke the `skill-development` skill first.**\\nFolding a standalone `SKILL.md` into another plugin's skill is exactly the\\njob that has established practice, and this build should follow it rather\\nthan reinvent a layout.\\n\\n### Pages leave `.lavish/`\\n\\n`.lavish/` is another tool's directory and this plugin's pages have no\\nbusiness in it. A generated page belongs in the bundle, because it is derived\\nfrom an authored markdown file and a rebuild can reproduce it:\\n\\n```\\n<bundle-dir>/pages/<slug>.html          the generated page\\n<bundle-dir>/exports/publish-manifest.json   already records the URL\\n```\\n\\nThe markdown stays the source of record in `docs/`, unchanged. Moving the\\nexisting six pages is part of E5, and each keeps its published URL, because\\na URL is bound to the artifact rather than to the local path.\\n\\n## Types and signatures\\n\\n### `shared/_bundle_meta.py`\\n\\n```python\\nSCHEMA_VERSION = \\\"1.3\\\"                            # was \\\"1.2\\\"\\nSCHEMA_VERSION_KNOWN = {\\\"1.0\\\", \\\"1.1\\\", \\\"1.2\\\", \\\"1.3\\\"}\\nCURRENT_BUNDLE_FORMAT = 3                         # was 2\\n\\nclass BundleIncompatible(RuntimeError):\\n    \\\"\\\"\\\"Raised when a bundle demands a reader this plugin cannot be.\\\"\\\"\\\"\\n\\ndef require_compatible(bundle_dir: Path, plugin: str) -> None:\\n    \\\"\\\"\\\"Refuse to write into a bundle this plugin is too old to read.\\n\\n    Reads <bundle_dir>/bundle.json. Raises BundleIncompatible when\\n    min_reader_schema > SCHEMA_VERSION, or when bundle_format >\\n    CURRENT_BUNDLE_FORMAT. Returns None and is silent otherwise.\\n    A missing bundle.json is a new bundle and is compatible.\\n    \\\"\\\"\\\"\\n\\ndef stamp_generator(bundle_dir: Path, plugin: str, version: str) -> None:\\n    \\\"\\\"\\\"Record this plugin's version in bundle.json's generators map.\\\"\\\"\\\"\\n```\\n\\nEvery script that writes into a bundle calls `require_compatible()` as its\\nfirst statement after argument parsing. Today three scripts stamp\\n`SCHEMA_VERSION` with no check at all: `extract_story.py:599`,\\n`export_artifact.py:428`, and `_manifest.py:76`.\\n\\n### `bundle.json`, at format 3\\n\\n```json\\n{\\n  \\\"bundle_format\\\": 3,\\n  \\\"schema_version\\\": \\\"1.3\\\",\\n  \\\"min_reader_schema\\\": \\\"1.3\\\",\\n  \\\"generators\\\": {\\n    \\\"cobuilder-pr\\\": \\\"0.5.0\\\",\\n    \\\"cobuilder-artifact\\\": \\\"0.5.0\\\"\\n  },\\n  \\\"migrated_at\\\": \\\"2026-08-21T09:00:00Z\\\"\\n}\\n```\\n\\n`generator_version`, a scalar, becomes `generators`, a map keyed by plugin.\\n`min_reader_schema` is the floor a reader must meet and is raised only by a\\nmigration that makes a bundle unreadable to an older plugin. A migration that\\nonly adds an optional field leaves it alone.\\n\\n### `scripts/build_index.py`\\n\\n```python\\ndef build_index(repo: Path, bundle_dir: Path) -> dict:\\n    \\\"\\\"\\\"Full rebuild. Reads docs/ plus git. Never merges, never authors.\\\"\\\"\\\"\\n\\ndef collect_adrs(repo: Path)      -> list[dict]\\ndef collect_designs(repo: Path)   -> list[dict]\\ndef collect_contexts(repo: Path)  -> list[dict]   # includes boundary rules\\ndef collect_districts(bundle: Path) -> list[dict] # from inventory.yaml\\ndef collect_plans(repo: Path)     -> list[dict]   # gates and slices\\ndef collect_pull_requests(repo: Path) -> list[dict]  # git plus gh\\ndef resolve_joins(entities: dict) -> dict\\ndef compute_sources(repo: Path)   -> dict         # freshness\\n\\ndef is_stale(index: dict, repo: Path) -> bool:\\n    \\\"\\\"\\\"True when any tracked subtree hash or the git head has moved.\\\"\\\"\\\"\\n```\\n\\n`index.json`:\\n\\n```json\\n{\\n  \\\"schema_version\\\": \\\"1.3\\\",\\n  \\\"sources\\\": {\\n    \\\"git_head\\\": \\\"642ecb3\\\",\\n    \\\"trees\\\": {\\\"docs/architecture/adr\\\": \\\"sha256:…\\\",\\n              \\\"docs/architecture/designs\\\": \\\"sha256:…\\\",\\n              \\\"docs/architecture/contexts\\\": \\\"sha256:…\\\",\\n              \\\"docs/plans\\\": \\\"sha256:…\\\"}\\n  },\\n  \\\"entities\\\": {\\n    \\\"adr\\\": [...], \\\"design\\\": [...], \\\"epic\\\": [...],\\n    \\\"context\\\": [...], \\\"district\\\": [...], \\\"boundary_rule\\\": [...],\\n    \\\"pull_request\\\": [...], \\\"slice\\\": [...], \\\"publication\\\": [...]\\n  },\\n  \\\"joins\\\": {\\n    \\\"adr_to_context\\\":  {\\\"ADR-0016\\\": \\\"cobuilder-packaging\\\"},\\n    \\\"adr_to_district\\\": {\\\"ADR-0007\\\": \\\"viewer\\\"},\\n    \\\"context_verifies_district\\\": {\\\"cobuilder-packaging\\\": [\\\"packaging\\\"]},\\n    \\\"adr_to_pull_request\\\": {\\\"ADR-0016\\\": {\\\"pr\\\": 11, \\\"via\\\": \\\"epic\\\",\\n                                         \\\"path\\\": [\\\"plugin-split/E1\\\"]}},\\n    \\\"slice_to_epic\\\": {\\\"cobuilder-family/3\\\": \\\"plugin-split/E1\\\"},\\n    \\\"epic_to_pull_request\\\": {\\\"plugin-split/E1\\\": 11},\\n    \\\"district_uncovered\\\": [\\\"narrative\\\", \\\"audio\\\"]\\n  }\\n}\\n```\\n\\n`joins` is materialised rather than computed in the viewer. A join resolved\\nonce in Python is a join the viewer cannot get wrong, and `district_uncovered`\\nis the describe backlog ADR-0018 names.\\n\\n### An ADR is not an epic\\n\\nThey are different things and neither contains the other.\\n\\nAn **ADR** is a decision. It outlives the work that carried it out, it can be\\nchallenged years later, and its state machine describes the life of the\\ndecision. An **epic** is a unit of work with a branch and an end.\\n\\nThe relation is many-to-many. One epic can carry several decisions: E1 lands\\nthe renames and every ADR the renames settle. One decision can take several\\nepics: ADR-0016 needs E1, E1b, E2, and E3 before the code matches it. That is\\nexactly why the code-matches-the-decision axis is derived rather than stored\\non the ADR.\\n\\n### How `adr_to_pull_request` is derived\\n\\nTwo paths, tried in order. Nothing is authored.\\n\\n1. **Direct.** The ADR's frontmatter carries `source_pr`. Retro-extraction\\n   sets it, because the record was written from a merged pull request. Done.\\n2. **Through the design and the epic.** Otherwise:\\n\\n```\\nADR-NNNN\\n  → design            the design whose adr list names it, or whose\\n                      directory the ADR's `related` paths point into\\n  → epic              goal.json.epics[] entries belonging to that design\\n  → branch            epics[].branch\\n  → pull_request      the PR whose head branch equals that branch,\\n                      from `gh pr list --head <branch> --state all`\\n```\\n\\n`via` records which path resolved, and `path` records the epics it went\\nthrough. A reader can then see *why* an ADR is attached to a pull request,\\nwhich matters when the answer is wrong.\\n\\n**The gap this exposes: a slice names no epic today.** `04-slices.md` lists\\nslices in build order and nothing ties a slice to the epic it advances. The\\njoin therefore stops at the epic and cannot reach a slice. Gate 4 fixes this\\nat the source: **every slice line declares `epic: <design>/<epic-id>`.** The\\nalternative is to guess from the files a slice touches, which is a heuristic\\nin a chain built to avoid them.\\n\\n**What stays underivable.** An epic with no branch has no pull request, and\\nthat is correct rather than missing — it is work that has not started. The\\nindex reports it as unstarted and does not search for a PR.\\n\\n### `scripts/serve_bundle.py`\\n\\n```python\\ndef serve(bundle_dir: Path, port: int, allow_write: bool) -> None:\\n    \\\"\\\"\\\"Static files, plus POST /feedback when allow_write is true.\\n\\n    Binds 127.0.0.1 only. Never binds a routable address.\\n    \\\"\\\"\\\"\\n\\ndef append_thread(bundle_dir: Path, record: dict) -> str:\\n    \\\"\\\"\\\"Validate, assign a ulid, append one line, return the id.\\n    Opens with 'a' and one write call, so a concurrent append cannot\\n    interleave. Never rewrites and never truncates.\\n    \\\"\\\"\\\"\\n```\\n\\nAccepted routes: `GET` for any file under the bundle root, and\\n`POST /feedback`. Nothing else. A `DELETE` is not implemented, because a read\\nnever deletes and neither does anything else.\\n\\n### `scripts/watch_feedback.py`\\n\\n```python\\ndef watch(bundle_dir: Path, since: int, timeout: int = 900) -> list[dict]:\\n    \\\"\\\"\\\"Block until threads.ndjson grows past byte offset `since`, or the\\n    timeout expires. Print the new lines as JSON and exit 0. Exit 2 on\\n    timeout with no new lines, so a caller can tell the two apart.\\n    \\\"\\\"\\\"\\n```\\n\\nPolls the file size once a second. It does not watch the filesystem, because\\na watcher is a dependency and this is a local file the same machine writes.\\n\\n### The comment record\\n\\nExactly as ADR-0019 specifies, including the agent reply that carries\\n`\\\"author\\\": \\\"agent\\\"` and `\\\"replies_to\\\": \\\"<thread id>\\\"`, and the state change\\nthat carries only `id`, `state`, and `ts`. **The ledger is append-only, so a\\nstate is the last line that names it, not a field edited in place.**\\n\\n```python\\ndef thread_view(bundle_dir: Path, thread_id: str) -> dict:\\n    \\\"\\\"\\\"Fold every line naming this id into one thread: the root comment,\\n    its replies in append order, and the transition history.\\\"\\\"\\\"\\n\\ndef project_threads(bundle_dir: Path) -> dict:\\n    \\\"\\\"\\\"Fold the whole ledger once into feedback/threads-state.json:\\n    {thread_id: {state, updated_by, updated_at, reply_count, offset}}.\\n    Rewritten in full after each append. Never authored.\\n    \\\"\\\"\\\"\\n```\\n\\n**The current state is a field, and the log is still append-only.** The\\nearlier draft had every reader fold the file to learn a state, and that was\\nthe wrong trade. `threads-state.json` is a projection rebuilt from the\\nledger, so a reader gets the state in one lookup and keeps the audit trail\\nthat says who moved it and when. The projection is derived and disposable —\\ndelete it and the next append rebuilds it from the ledger.\\n\\nThe rule the projection must not break: **the ledger is the truth and the\\nfield is the cache.** A writer appends to the ledger first and projects\\nsecond. A projection that disagrees with the ledger is a bug in the\\nprojection, and `project_threads()` is cheap enough to rebuild whenever the\\nanswer is in doubt. This is the same shape as `index.json` — derived, full\\nrebuild, never merged — so it needs no new idea, only the same discipline.\\n\\nE6 ships `thread_view()`, `project_threads()`, and the reply record. It does\\nnot ship the conversation interface — the drawer lists replies flat.\\nRendering a thread as a conversation is a later slice, and the record shape\\nexists now so that slice adds no migration.\\n\\n## Call stack\\n\\n### Publishing a design, end to end\\n\\n```\\n/cobuilder-architect:design\\n  → migrate_bundle.py --bundle-dir …            (shared, unconditional)\\n  → require_compatible(bundle, \\\"cobuilder-architect\\\")\\n  → interview, explore, challenge                (Claude judgment)\\n  → write docs/architecture/designs/<name>/*.json\\n  → write docs/architecture/adr/ADR-NNNN-*.md\\n  → when no context covers the region:\\n        describe procedure, in-process\\n        → write docs/architecture/contexts/<id>/{canvas.md,boundary.yaml}\\n        → boundary.verifies = [districts covered]\\n  → validate_decision_state.py\\n  → hand off: name cobuilder-implement, stop\\n```\\n\\n### Building the index and reading it\\n\\n```\\nany cobuilder-artifact mode\\n  → migrate_bundle.py\\n  → is_stale(index, repo)?\\n        yes → build_index.py\\n                collect_* ×6  →  resolve_joins  →  compute_sources\\n                write data/index.json and data/index.js\\n  → serve_bundle.py --allow-write\\n        viewer/index.html\\n          <script src=\\\"../data/index.js\\\">  →  window.INDEX\\n          renderMode(mode)  →  Designs | Pull requests | Decisions\\n                               | Contexts | Builds\\n```\\n\\n### A comment, end to end\\n\\n```\\nreader clicks any element in the viewer\\n  → onSelect(event)\\n      computeSelector(el)     walk ≤5 ancestors, short-circuit on an id\\n      captureRange(selection) node path plus character offset per end\\n      openDrawer(anchor)      shifts the page, does not cover it\\n  → reader types, submits\\n      POST /feedback  →  append_thread()  →  ulid returned\\n      fallback when the server is read-only: localStorage, then the\\n      existing \\\"Copy review as markdown\\\" hand-off\\n  → agent side\\n      watch_feedback.py --since <offset>\\n        blocks → prints new lines → exits 0\\n        harness re-invokes the session with the threads in hand\\n      agent acts, appends a reply line, appends a state line\\n```\\n\\n## Test plan\\n\\n**This repository has no test suite today.** Gate 3 introduces one, because\\nthe red-green-validate loop of `cobuilder-implement` cannot run without it.\\n`tests/` at the marketplace root, run with\\n`uv run --with pytest pytest tests/`. No CI. No package manager. The\\nconstraint that this repo stays prose plus PEP-723 scripts holds, and pytest\\narrives through `--with` rather than through a lockfile.\\n\\n### Compatibility gate\\n\\n- `test_require_compatible_passes_on_equal_schema`\\n- `test_require_compatible_raises_when_bundle_demands_newer_reader` —\\n  `min_reader_schema` `1.4` against a `1.3` plugin raises `BundleIncompatible`\\n- `test_require_compatible_raises_on_newer_bundle_format`\\n- `test_require_compatible_allows_missing_bundle_json` — a new bundle\\n- `test_write_scripts_call_require_compatible_first` — greps each writer for\\n  the call. Crude, and it is the test that actually prevents the regression.\\n- `test_stamp_generator_preserves_other_plugins` — writing as\\n  `cobuilder-pr` must not drop `cobuilder-artifact`'s entry\\n\\n### Migration to format 3\\n\\n- `test_scalar_generator_version_becomes_a_map`\\n- `test_migration_refuses_when_an_authored_field_changes_outside_touches`\\n- `test_migration_writes_a_backup_before_it_writes_the_new_story`\\n- `test_viewer_refreshes_unconditionally` — no version gate, the bug that\\n  motivated the mechanism\\n\\n### The index\\n\\n- `test_index_resolves_an_adr_to_its_context`\\n- `test_index_resolves_an_adr_to_a_context_through_a_district` — the join\\n  ADR-0018 adds\\n- `test_district_with_no_context_appears_in_district_uncovered`\\n- `test_epic_id_is_scoped_to_its_design` — two designs both carrying `E1`\\n- `test_is_stale_detects_a_changed_adr`\\n- `test_is_stale_detects_a_moved_git_head`\\n- `test_index_is_a_full_rebuild` — deleting an ADR removes it from the index\\n- `test_build_index_never_writes_into_docs`\\n\\n### The ledger\\n\\n- `test_append_thread_assigns_a_ulid_and_returns_it`\\n- `test_append_thread_never_rewrites_an_existing_line`\\n- `test_two_concurrent_appends_both_survive`\\n- `test_state_change_is_a_new_line_not_an_edit`\\n- `test_read_does_not_delete` — the point of departure from the lavish CLI\\n- `test_thread_view_folds_a_reply_under_its_root`\\n- `test_an_agent_reply_carries_author_agent_and_replies_to`\\n- `test_projection_state_equals_the_last_transition_in_the_ledger`\\n- `test_deleting_the_projection_rebuilds_it_from_the_ledger`\\n- `test_projection_records_who_moved_the_state_and_when`\\n- `test_watch_exits_2_on_timeout_with_no_new_lines`\\n- `test_serve_rejects_a_post_to_any_path_other_than_feedback`\\n- `test_serve_binds_loopback_only`\\n\\n### The split itself\\n\\n- `test_no_plugin_path_reference_crosses_a_pillar` — greps every plugin for\\n  `${CLAUDE_PLUGIN_ROOT}/skills/<other pillar>`. This is the E1b regression\\n  guard, and it is the single most valuable test in the list.\\n- `test_every_plugin_manifest_validates` — `claude plugin validate` on each\\n- `test_no_page_is_written_into_dot_lavish`\\n- `test_slice_declares_an_epic` — every line in `04-slices.md` names one\\n- `test_adr_reaches_a_pull_request_through_its_epic` — the two-path derivation\\n- `test_an_epic_with_no_branch_is_reported_unstarted_not_missing`\\n- `test_shared_symlink_dereferences_to_a_real_directory` — E2's proof,\\n  written as a test rather than as a one-off check\\n\\n## Found while writing this document\\n\\n**`explore-design` and `design` are the same command, and the command goes.**\\n`commands/explore-design.md:27` dispatches `Skill(\\\"architecture\\\", args=\\\"design\\n$ARGUMENTS\\\")`, which is character-for-character what `commands/design.md`\\ndispatches. Divergent exploration is stage 3 of Design mode and runs either\\nway, so the second command adds a name and no behaviour. **Decided: delete\\nthe command. Divergent exploration stays an agent-invoked step inside Design\\nmode and is not a user-facing entry point.** `cobuilder-architect` ships six\\nmodes. E1 carries the deletion.\\n\\n**The backlog already exists and nobody was reading it.** Deferred work has a\\nhome, and it is `goal.json.epics[]`. Each entry carries an `id`, a `branch`,\\na `pr`, and a `state`, and an epic with a null branch is work that is planned\\nand not started. That is a backlog item. Nothing new is needed to hold one.\\n\\nSo the three levels are:\\n\\n| Level | Where it lives | Lifetime |\\n|---|---|---|\\n| **Epic** | `goal.json.epics[]` in a design | survives the session, survives the branch |\\n| **Slice** | `04-slices.md` for a feature, one line each in build order | survives the session, dies with the feature |\\n| **Task** | inside a slice, in the session doing it | dies with the session, deliberately |\\n\\nA task below slice level is not durable and should not be. A slice is small\\nenough that its tasks are the work of one sitting, and writing them down\\ncosts more than redoing them.\\n\\n**The Builds view gains a Backlog lane**, and it is a query rather than a\\nfile: every epic with a null branch, plus every slice not yet scored, ranked\\nby the count of ADRs that depend on the epic. Nothing authors it, and nothing\\ncan go stale against a list it computes.\\n\\n`plugin-split/goal.json` is now the real list. It held five epics from the\\nfirst draft while `02-architecture.md` had grown to eight. It carries all\\nnine, including **E8, rendering a thread as a conversation** — which is the\\nanswer to where that deferred work went.\\n\\n**Rendering a thread as a conversation needs no ADR.** ADR-0019 already\\ndecides the reply channel, the record shape, and that `replies_to` exists.\\nRendering those replies as a thread is deferred work under that decision, not\\na competing option, and a record with no rejected alternative is a task in\\nADR clothing. One thing would earn its own record: if the viewer's threads\\nand the Artifact platform's threads have to merge into one inbox, that is a\\nreal decision with real alternatives. It is not in scope for E6.\\n\\n## Least confident decisions\\n\\n1. **`serve_bundle.py` replaces `python3 -m http.server` outright.** A\\n   smaller change keeps the stock server for reading and adds the write\\n   endpoint only under a flag. That leaves two servers to keep in step. One\\n   server with `--allow-write` off by default is simpler and it is still a\\n   real application where there used to be none.\\n\\n2. **Materialised `joins` in `index.json`.** It duplicates what the entities\\n   already state, and it goes stale in exactly the same rebuild the entities\\n   do, so the duplication costs size rather than correctness. The argument\\n   against is that a viewer bug and an index bug now look identical.\\n\\n3. **pytest through `uv run --with`.** It keeps the no-package-manager\\n   constraint and it re-resolves pytest on every run. A slow test command is\\n   a test command people skip.\\n\\n4. **A projection file beside the ledger.** It is a second file that can\\n   disagree with the first. The alternative is a fold on every read, which\\n   was the earlier draft and which made the common operation the expensive\\n   one. Two files with a stated direction of truth is the better trade, and\\n   it is the trade `index.json` already makes.\\n\\n5. **`shared/` as a sibling of `plugins/` rather than duplicated content in\\n   git.** Everything rests on the symlink dereference. If E2 finds it does\\n   not work for a directory of Python, the fallback is a copy step before\\n   publish, and that fallback is a build step in a repo that has none.\\n\", \"approved_date\": null, \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/03-program-design.md\"}, {\"id\": \"gate-doc-surfacing\", \"feature_slug\": \"gate-doc-surfacing\", \"gate\": 3, \"title\": \"Program Design: Gate doc surfacing\", \"body_md\": \"# Program Design: Gate doc surfacing\\n\\n## Files\\n- `shared/build_index.py` — modified. New constants `PROGRAM_DESIGN_FIELDS`,\\n  `EPIC_DESIGN_FIELDS`; new functions `discover_plan_gate_docs()`,\\n  `project_program_design()`, `project_epic_design()`; `collect_designs()`\\n  or a new `collect_gate_docs()` wires the new entities into the index;\\n  `resolve_feature_gates()` attaches `doc` per gate.\\n- `plugins/artifact/viewer/index.html` — modified. New functions\\n  `openGateDocSheet(kind, id)`, `closeGateDocSheet()`,\\n  `renderGateDocSheet()`. New markup block for the sheet (siblings of\\n  `#adr-sheet`). `renderBuildsMainContent()`'s `gate-rail-grid` and\\n  `renderEpicCard()` gain click wiring.\\n- `tests/test_build_index_gate_docs.py` — new. Covers the projection\\n  functions and `resolve_feature_gates()`'s new `doc` field.\\n\\n## Types & signatures\\n\\n```python\\n# shared/build_index.py\\nPROGRAM_DESIGN_FIELDS: tuple[str, ...]\\nEPIC_DESIGN_FIELDS: tuple[str, ...]\\n\\ndef discover_plan_gate_docs(repo: Path) -> list[dict]:\\n    \\\"\\\"\\\"One dict per docs/plans/<slug>/{03-program-design.md, epic-*-design.md}\\n    found, with slug, kind ('program'|'epic'), epic_id (if kind == 'epic'),\\n    path, and raw markdown text.\\\"\\\"\\\"\\n\\ndef project_program_design(feature_slug: str, path: Path, text: str) -> dict:\\n    \\\"\\\"\\\"-> {\\\"id\\\": feature_slug, \\\"feature_slug\\\", \\\"gate\\\": 3, \\\"title\\\",\\n    \\\"body_md\\\", \\\"approved_date\\\", \\\"source_path\\\"}, fields filtered through\\n    project_fields(..., PROGRAM_DESIGN_FIELDS).\\\"\\\"\\\"\\n\\ndef project_epic_design(epic_id: str, feature_slug: str, path: Path, text: str) -> dict:\\n    \\\"\\\"\\\"-> {\\\"id\\\": epic_id, \\\"epic_id\\\", \\\"feature_slug\\\", \\\"title\\\", \\\"body_md\\\",\\n    \\\"approved_date\\\", \\\"source_path\\\"}, filtered through\\n    project_fields(..., EPIC_DESIGN_FIELDS).\\\"\\\"\\\"\\n```\\n\\n```javascript\\n// plugins/artifact/viewer/index.html\\nfunction openGateDocSheet(kind, id) { /* kind: 'program' | 'epic' */ }\\nfunction closeGateDocSheet() { }\\nfunction renderGateDocSheet() { /* reads window.INDEX.entities.program_design\\n  or .epic_design by id, renders via renderMarkdown() into the sheet body */ }\\n```\\n\\n## Call stack\\n\\nIndex build (top to bottom):\\n```\\nmain()\\n  build_index(repo)\\n    collect_designs(repo)          # unchanged\\n    discover_plan_gate_docs(repo)  # new\\n      project_program_design()     # new, one call per feature with a Gate 3 doc\\n      project_epic_design()        # new, one call per epic-*-design.md found\\n    resolve_joins(repo)\\n      resolve_feature_gates(repo)  # extended: attaches doc id where found\\n```\\n\\nViewer click (top to bottom):\\n```\\ngate-card click / epic design-doc chip click\\n  openGateDocSheet(kind, id)\\n    state.gateDocOpen = true; state.activeGateDocKind = kind; state.activeGateDocId = id\\n    renderGateDocSheet()\\n      lookup window.INDEX.entities.program_design[id] or .epic_design[id]\\n      renderMarkdown(record.body_md)   # reused, unchanged\\n    renderSheetVisibility()            # existing mutual-exclusivity function, extended\\n```\\n\\n## Test plan\\n- `test_build_index_gate_docs.py::test_project_program_design_reads_title_and_body`\\n- `test_build_index_gate_docs.py::test_project_epic_design_parses_epic_id_from_filename`\\n- `test_build_index_gate_docs.py::test_discover_plan_gate_docs_skips_plan_dir_with_no_gate_docs`\\n- `test_build_index_gate_docs.py::test_resolve_feature_gates_attaches_doc_id_when_program_design_exists`\\n- `test_build_index_gate_docs.py::test_resolve_feature_gates_gate3_doc_absent_when_no_program_design_md`\\n- `test_build_index_gate_docs.py::test_build_index_end_to_end_index_json_carries_new_entity_keys`\\n\\nManual dogfood check (no automated frontend test harness in this repo):\\nopen the rebuilt viewer, navigate to Builds, click this feature's own Gate\\n3 card once `03-program-design.md` exists (after this gate is approved),\\nconfirm the sheet shows this document's content.\\n\\n## Least confident decisions\\n- Whether `collect_designs()` should absorb the new entities directly, or\\n  a separate `collect_gate_docs()` function is cleaner — deferred to\\n  implementation; either satisfies the same `data/index.json` shape.\\n- Whether the sheet reuses the *same* DOM nodes as `#adr-sheet` with a mode\\n  flag, or gets its own sibling `#gate-doc-sheet` block — leaning toward a\\n  sibling block, since `renderAdrSheet` and a future `renderGateDocSheet`\\n  read different record shapes and a shared node risks stale content on a\\n  fast open/open across the two kinds.\\n\", \"approved_date\": null, \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/gate-doc-surfacing/03-program-design.md\"}], \"epic_design\": [{\"id\": \"cobuilder-family/E1\", \"epic_id\": \"E1\", \"feature_slug\": \"cobuilder-family\", \"title\": \"Epic Technical Solution Design: E1 — One plugin becomes five\", \"body_md\": \"# Epic Technical Solution Design: E1 — One plugin becomes five\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E1\\n\\nThis design was written on 2026-08-25, after this epic's four slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE1 turns one plugin into five sibling plugins under `plugins/`, each with\\nits own `.claude-plugin/plugin.json`, `commands/`, `skills/`, and `scripts/`.\\nIt renames three modes inside the single plugin first, deletes the\\nduplicate `explore-design` command, fixes 33 cross-pillar references, then\\nsplits the tree into five manifests, and ports two skills in from outside\\nthe repository. ADR-0016 decided the five-way split. ADR-0017 decided the\\nvendoring mechanism E1 relies on but does not itself implement (E2 proves\\nit, E3 gates it).\\n\\n## Files Touched\\n\\nThe whole split landed in commit `4156641` (\\\"Slice 10 change\\\"), together\\nwith parts of E3, E4, and half of E5. `git show --stat 4156641` lists the\\nfull file move. The load-bearing paths today are:\\n\\n- `.claude-plugin/marketplace.json` — one file, five plugin entries.\\n- `plugins/cobuilder-architect/.claude-plugin/plugin.json`,\\n  `plugins/cobuilder-pr/.claude-plugin/plugin.json`,\\n  `plugins/cobuilder-implement/.claude-plugin/plugin.json`,\\n  `plugins/cobuilder-artifact/.claude-plugin/plugin.json`,\\n  `plugins/cobuilder-full-lifecycle/.claude-plugin/plugin.json` — one\\n  manifest per plugin.\\n- `plugins/cobuilder-architect/commands/{design,review,maintenance,\\n  decisions,describe,debug}.md` — six modes, `explore-design.md` deleted.\\n- `plugins/cobuilder-architect/skills/architecture/` — moved from the\\n  repository root, unchanged in content apart from path updates.\\n- `plugins/cobuilder-pr/skills/odyssey/` — the renamed modes: the old\\n  `submit` became `generate`, and the old `generate` became `review`.\\n- `plugins/cobuilder-artifact/scripts/{serve_bundle.py,export_artifact.py,\\n  export_index.py,record_publish.py}` and\\n  `plugins/cobuilder-artifact/viewer/index.html`.\\n- `plugins/cobuilder-full-lifecycle/skills/cobuilder-full/SKILL.md` and\\n  `plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py`.\\n- Each plugin carries a `shared` symlink at its own root, verified by\\n  `test_shared_is_a_symlink_in_the_source_tree` in\\n  `tests/test_plugin_manifests.py`.\\n\\n## Types & Signatures\\n\\nE1 carries no new Python functions of its own. Its unit of work is the\\nmanifest and the command dispatch line, not a function signature. The\\ncontract each command file holds is a one-line `Skill(...)` dispatch, for\\nexample `plugins/cobuilder-architect/commands/design.md` dispatching\\n`Skill(\\\"architecture\\\", args=\\\"design $ARGUMENTS\\\")`. `tests/test_commands.py`\\nasserts this shape directly:\\n\\n```python\\ndef test_command_dispatches_to_a_declared_mode(command_path): ...\\ndef test_explore_design_command_does_not_exist(): ...\\ndef test_no_two_commands_in_the_same_plugin_dispatch_identically(): ...\\ndef test_cobuilder_pr_ships_its_own_review_command(): ...\\ndef test_architecture_skill_declares_exactly_six_modes(): ...\\n```\\n\\n## Slice Decomposition\\n\\nPer `docs/plans/cobuilder-family/04-slices.md`, in build order:\\n\\n1. **Slice 4 — renames inside today's single plugin.** No dependency. Six\\n   architecture modes land, `explore-design` is deleted, `submit` becomes\\n   `generate`, and the old `generate` becomes `review`. Completed on attempt\\n   2, score 1.00.\\n2. **Slice 5 — the 33 cross-pillar references are fixed.** Depends on\\n   slice 4 naming the modes the references must point to. The regression\\n   test runs and passes while the repository is still one plugin, so it\\n   guards the split rather than reporting on it. Completed on attempt 3,\\n   score 1.00.\\n3. **Slice 6 — five plugins, five manifests.** Depends on slices 4 and 5.\\n   `claude plugin validate` passes on each, and the family installs.\\n   Completed, score 1.00.\\n4. **Slice 7 — the two ports land.** Depends on slice 6 existing so the\\n   ported skills have a plugin to land in. `cobuilder-implement` is renamed\\n   and shipped, `collaborate-with-user` folds into `cobuilder-artifact`,\\n   and the orientation skill is written. Completed, score 1.00.\\n\\n## Test Plan\\n\\n- `tests/test_plugin_manifests.py` — `test_manifest_parses_and_has_\\n  required_fields`, `test_no_agent_hook_or_mcp_server` (asserts none of the\\n  five plugins declares an agent, a hook, or an MCP server, per the\\n  installation rule in `CLAUDE.md`), `test_marketplace_lists_all_five_\\n  plugins`, `test_umbrella_plugin_depends_on_the_other_four`, and\\n  `test_shared_is_a_symlink_in_the_source_tree`.\\n- `tests/test_commands.py` — the eight assertions listed above, covering\\n  the rename, the deletion, and the one-dispatch-per-command rule.\\n- `tests/test_pillar_boundaries.py` — `test_no_cross_pillar_references_in_\\n  repo` is the regression test slice 5 exists to write. It greps every\\n  plugin for `${CLAUDE_PLUGIN_ROOT}/skills/<other pillar>` and fails on a\\n  hit. The file also carries thirteen evasion tests (`test_evasion_*`)\\n  covering relative paths, backslashes, mixed case, and files outside the\\n  original allowlist.\\n- `tests/test_script_references.py` — `test_runnable_commands_name_\\n  existing_scripts` checks that a script path named in prose actually\\n  exists on disk after the move.\\n\\n## Risks & Open Questions\\n\\n- **The split landed inside a much larger commit.** `4156641` also carries\\n  parts of E3, E4, and half of E5. There is no commit boundary that isolates\\n  E1 on its own, so `git show --stat` on that one commit is the closest\\n  thing to an E1-only diff, and it is not a clean one.\\n- **`plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py` exists.**\\n  `03-program-design.md`'s Files section proposed `scripts/watch_feedback.py`\\n  under `cobuilder-artifact`. The shipped location is under\\n  `cobuilder-full-lifecycle` instead. This is a placement drift from the\\n  program design, not a defect, and no ADR records the reason for the\\n  change.\\n- **No commit is titled after E1 or after any of its four slices by name.**\\n  The slice table's \\\"completed\\\" status and the rubric scores are the only\\n  record that ties specific work to specific slice numbers. A future\\n  session auditing this epic has to read `04-slices.md` and the rubric\\n  files, not `git log`, to find slice boundaries.\\n\", \"approved_date\": null, \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E1-design.md\"}, {\"id\": \"cobuilder-family/E3\", \"epic_id\": \"E3\", \"feature_slug\": \"cobuilder-family\", \"title\": \"Epic Technical Solution Design: E3 — The seam is version-safe\", \"body_md\": \"# Epic Technical Solution Design: E3 — The seam is version-safe\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E3\\n\\nThis design was written on 2026-08-25, after this epic's two slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE3 makes the bundle safe to write from five independently versioned\\nplugins. It adds `require_compatible()` and `stamp_generator()`, raises\\n`bundle_format` to 3 and `schema_version` to 1.3, replaces the scalar\\n`generator_version` with a `generators` map keyed by plugin name, and gates\\nevery bundle-writing script behind the compatibility check. ADR-0017 governs\\nthis epic.\\n\\n## Files Touched\\n\\n- `shared/_bundle_meta.py` — `SCHEMA_VERSION = \\\"1.3\\\"`,\\n  `SCHEMA_VERSION_KNOWN = {\\\"1.0\\\", \\\"1.1\\\", \\\"1.2\\\", \\\"1.3\\\"}`,\\n  `CURRENT_BUNDLE_FORMAT = 3`, the `BundleIncompatible` exception, and both\\n  new functions.\\n- `shared/migrate_bundle.py` — the layout and schema ladders that carry a\\n  bundle from format 2 to format 3, and the scalar-to-map migration for\\n  `generators`.\\n- Every script that writes into a bundle now calls `require_compatible()`\\n  first: `shared/build_index.py`, `plugins/cobuilder-artifact/scripts/\\n  export_artifact.py`, `plugins/cobuilder-artifact/scripts/serve_bundle.py`\\n  (through `shared/ledger.py`), and `plugins/cobuilder-pr/scripts/\\n  extract_story.py`.\\n- `shared/ledger.py` — the append-only comment ledger this epic's gate\\n  gets wired in front of, in the sense that every write path funnels\\n  through `LedgerPaths` and the compatibility check runs before it.\\n\\n## Types & Signatures\\n\\nRead directly from `shared/_bundle_meta.py`:\\n\\n```python\\ndef read_plugin_version() -> str: ...\\ndef read_plugin_name() -> str: ...\\n\\nclass BundleIncompatible(RuntimeError): ...\\n\\ndef _version_tuple(version: str) -> tuple[int, ...]: ...\\n\\ndef require_compatible(bundle_dir: Path, plugin: str) -> None:\\n    \\\"\\\"\\\"Raise BundleIncompatible when the bundle's min_reader_schema exceeds\\n    this plugin's SCHEMA_VERSION, or when bundle_format exceeds\\n    CURRENT_BUNDLE_FORMAT. Silent otherwise. A missing bundle.json is a new\\n    bundle and passes.\\\"\\\"\\\"\\n\\ndef stamp_generator(bundle_dir: Path, plugin: str, version: str) -> None:\\n    \\\"\\\"\\\"Write this plugin's version into bundle.json's generators map,\\n    without touching another plugin's existing entry.\\\"\\\"\\\"\\n```\\n\\n`bundle.json` at format 3 carries `generators` as a map, for example\\n`{\\\"cobuilder-pr\\\": \\\"0.5.0\\\", \\\"cobuilder-artifact\\\": \\\"0.5.0\\\"}`, replacing the\\nsingle `generator_version` scalar the program design's Types and\\nsignatures section shows as the format-2 shape.\\n\\n## Slice Decomposition\\n\\nPer `04-slices.md`:\\n\\n1. **Slice 2 — the compatibility gate exists.** No dependency on E1 or E4.\\n   `require_compatible()` and `stamp_generator()` pass their own tests,\\n   called by nothing yet. Completed, score 1.00.\\n2. **Slice 3 — the gate is wired and the bundle migrates.** Depends on\\n   slice 2. Format 3, `min_reader_schema`, the `generators` map, and every\\n   writer gated. Completed, score 1.00.\\n\\nBoth slices land before E1's slice 6 splits the tree into five plugins, per\\n`04-slices.md`'s stated build order: \\\"Slices 2 through 5 stay inside\\ntoday's single plugin,\\\" so the compatibility gate is proven before the\\ndirectory move that would make debugging both changes at once harder.\\n\\n## Test Plan\\n\\n- `tests/test_bundle_meta.py` — eleven tests, including\\n  `test_newer_min_reader_schema_raises`, `test_equal_min_reader_schema_\\n  passes`, `test_version_comparison_is_numeric_not_string`,\\n  `test_version_comparison_older_dotted_passes`, `test_newer_bundle_format_\\n  raises`, `test_missing_bundle_json_passes_silently`, `test_stamp_\\n  generator_preserves_other_plugin`, `test_stamp_generator_overwrites_own_\\n  prior_entry`, `test_stamp_generator_keeps_old_scalar_field`, and\\n  `test_stamp_generator_creates_bundle_json_when_missing`.\\n- `tests/test_migrate_bundle.py` — `test_build_generators_map_preserves_\\n  old_scalar`, `test_build_generators_map_keeps_existing_map_entries`,\\n  `test_full_migration_turns_scalar_into_map`, `test_guard_rejects_\\n  undeclared_authored_field_change`, `test_data_ladder_stops_before_later_\\n  steps_on_violation`, `test_main_writes_nothing_on_guard_violation`,\\n  `test_backup_written_before_new_story`, `test_viewer_refreshes_even_at_\\n  current_version`, and `test_viewer_refresh_is_not_version_gated_in_\\n  source` — this last pair is the regression test for the exact bug\\n  `CLAUDE.md`'s Bundle versioning section names: a version gate on the\\n  viewer refresh that once let a stale viewer silently drop diagram\\n  support.\\n- `tests/test_build_index.py` — `test_calls_compatibility_gate_before_\\n  first_write` confirms `build_index.py` itself calls `require_compatible()`\\n  before it writes, closing the loop between E3's gate and E4's index.\\n\\n## Risks & Open Questions\\n\\n- **The program design lists three specific call sites as ungated before\\n  this epic** — `extract_story.py:599`, `export_artifact.py:428`, and\\n  `_manifest.py:76`. This design did not re-verify each of the three by\\n  line number against the tree as built. `test_write_scripts_call_require_\\n  compatible_first`, named in `03-program-design.md`'s test plan, is the\\n  test that would settle this, and its presence or absence under those\\n  exact names was not confirmed while writing this document.\\n- **`min_reader_schema` has never been raised in anger.** Every migration\\n  shipped so far adds an optional field, so the floor has stayed at 1.3\\n  since it was introduced. Whether a future migration correctly raises it,\\n  rather than leaving it stale the way an old ADR's `maps_to.modules` block\\n  went stale (see `docs/architecture/designs/design-mode/assessment.json`,\\n  finding F3), is untested because no such migration exists yet.\\n\", \"approved_date\": null, \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E3-design.md\"}, {\"id\": \"cobuilder-family/E4\", \"epic_id\": \"E4\", \"feature_slug\": \"cobuilder-family\", \"title\": \"Epic Technical Solution Design: E4 — The record index\", \"body_md\": \"# Epic Technical Solution Design: E4 — The record index\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E4\\n\\nThis design was written on 2026-08-25, after this epic's two slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE4 replaces the two separate projection scripts (`build_adrs.py` and\\n`build_designs.py`) with one `shared/build_index.py` that emits every\\nentity — ADR, design, epic, context, district, boundary rule, pull request,\\nslice, publication — with a stable id, and resolves the joins between them,\\nincluding an ADR reaching its pull request through a design and an epic.\\nADR-0018 governs this epic.\\n\\n## Files Touched\\n\\n- `shared/build_index.py` — the whole collect-and-resolve pipeline, 1163\\n  lines. `build_adrs.py` and `build_designs.py` no longer exist anywhere in\\n  the tree.\\n- `docs/architecture/designs/plugin-split/goal.json` and\\n  `docs/plans/cobuilder-family/04-slices.md` — the source data `collect_\\n  designs()` and `collect_plans()` read.\\n- `.cobuilder-architect/self/data/index.json` and `index.js` — the\\n  generated output, checked in as the self-bundle's own projection.\\n\\n## Types & Signatures\\n\\nRead directly from `shared/build_index.py`. Selected signatures, in the\\norder the pipeline calls them:\\n\\n```python\\ndef collect_adrs(repo: Path) -> tuple[list[dict], dict[str, dict], list[str]]: ...\\ndef collect_designs(repo: Path) -> tuple[list[dict], ...]: ...\\ndef collect_contexts(repo: Path) -> tuple[list[dict], list[dict], list[str]]: ...\\ndef collect_districts(bundle_dir: Path) -> tuple[list[dict], list[str]]: ...\\ndef collect_plans(repo: Path) -> tuple[list[dict], list[str]]: ...\\ndef collect_pull_requests(bundle_dir: Path) -> tuple[list[dict], list[str]]: ...\\ndef collect_publications(bundle_dir: Path) -> tuple[list[dict], list[str]]: ...\\n\\ndef gh_pr_for_branch(branch: str, warnings: list[str], gh_state: dict) -> int | None: ...\\ndef resolve_epic_pull_requests(...) -> tuple[dict[str, int], dict[str, str]]: ...\\ndef refine_epic_status(epic_status: dict[str, str], epic_to_pr: dict[str, int],\\n                        pull_requests: list[dict]) -> None: ...\\ndef adrs_reaching_design(adr_id: str, adr_record: dict,\\n                          design_records: dict[str, dict]) -> list[str]: ...\\ndef resolve_adr_to_pull_request(...) -> ...\\ndef resolve_slice_to_epic(...) -> ...\\ndef resolve_context_district_joins(...) -> ...\\ndef resolve_joins(...) -> dict: ...\\n\\ndef hash_tree(path: Path) -> str: ...\\ndef git_head(repo: Path) -> str | None: ...\\ndef compute_sources(repo: Path) -> dict: ...\\ndef is_stale(index: dict, repo: Path) -> bool: ...\\ndef build_index(repo: Path, bundle_dir: Path) -> tuple[dict, dict, dict, list[str]]: ...\\n```\\n\\n`collect_pull_requests(bundle_dir: Path)` reads `data/story.json`:\\n\\n```python\\ndef collect_pull_requests(bundle_dir: Path) -> tuple[list[dict], list[str]]:\\n    story_path = bundle_dir / \\\"data\\\" / \\\"story.json\\\"\\n    ...\\n    timeline = raw.get(\\\"timeline\\\")\\n    ...\\n    for entry in timeline:\\n        if not isinstance(entry, dict) or entry.get(\\\"pr\\\") is None:\\n            continue\\n        entities.append({\\\"id\\\": entry[\\\"pr\\\"], \\\"title\\\": entry.get(\\\"title\\\"),\\n                          \\\"state\\\": entry.get(\\\"status\\\"), ...})\\n    return entities, []\\n```\\n\\n`refine_epic_status` then tries to overwrite the placeholder state:\\n\\n```python\\ndef refine_epic_status(epic_status: dict[str, str], epic_to_pr: dict[str, int],\\n                        pull_requests: list[dict]) -> None:\\n    \\\"\\\"\\\"Replace the placeholder 'open' status with the real pull request state.\\\"\\\"\\\"\\n    state_by_number = {pr[\\\"id\\\"]: pr.get(\\\"state\\\") for pr in pull_requests}\\n    for epic_id, pr_number in epic_to_pr.items():\\n        state = state_by_number.get(pr_number)\\n        if state:\\n            epic_status[epic_id] = str(state).lower()\\n```\\n\\n## Slice Decomposition\\n\\nPer `04-slices.md`:\\n\\n1. **Slice 8 — the index holds the entities.** No dependency beyond E3's\\n   compatibility gate. `build_index.py` emits every entity, and the two\\n   scripts it replaces are gone. Completed, score 1.00.\\n2. **Slice 9 — the index resolves the joins.** Depends on slice 8's entity\\n   set existing to join against. `adr_to_pull_request`, `slice_to_epic`,\\n   `district_uncovered`, and freshness (`is_stale`). Completed, score 1.00.\\n\\n## Test Plan\\n\\n`tests/test_build_index.py` carries twenty tests. The ones specific to\\neach slice:\\n\\n- Slice 8: `test_every_entity_type_appears_with_correct_count`,\\n  `test_deleting_a_source_document_removes_it_on_rebuild` (proves the index\\n  is a full rebuild, not a merge), `test_writes_nothing_into_docs`,\\n  `test_calls_compatibility_gate_before_first_write`, and\\n  `test_slice_8_entities_still_present_alongside_joins`.\\n- Slice 9: `test_adr_reaches_pull_request_direct_path`, `test_adr_reaches_\\n  pull_request_through_design_and_epic`, `test_adr_with_no_reachable_\\n  design_is_unresolved_not_guessed`, `test_epic_with_no_branch_is_\\n  unstarted`, `test_epic_with_branch_but_no_pull_request_is_reported_not_\\n  errored`, `test_district_and_context_resolve_both_ways`, `test_uncovered_\\n  district_is_listed`, `test_every_slice_declares_an_existing_epic`,\\n  `test_a_bare_epic_header_id_is_unresolved_not_guessed`, `test_an_epic_\\n  header_naming_an_undeclared_epic_is_unresolved`, `test_all_fourteen_\\n  real_slices_resolve_from_the_scoped_id`, `test_changing_an_authored_\\n  document_marks_the_index_stale`, `test_moving_the_git_head_marks_the_\\n  index_stale`, `test_epic_id_scoped_to_design_no_collision`, and\\n  `test_gate_source_scan`.\\n\\nNo test in the suite asserts the open-pull-request behavior described\\nbelow under Risks. `test_epic_with_branch_but_no_pull_request_is_reported_\\nnot_errored` covers an epic with no matching pull request at all, which is\\na different case from an epic whose pull request exists but is open, not\\nmerged.\\n\\n## Risks & Open Questions\\n\\n- **An open pull request does not resolve to a real state.**\\n  `collect_pull_requests()` reads only `data/story.json`, which holds\\n  narrated merged pull requests. An open pull request is never narrated\\n  into `story.json`, so it never becomes a `pull_request` entity.\\n  `resolve_epic_pull_requests()` still finds the PR number through `gh pr\\n  list --head <branch>`, and sets a hardcoded placeholder state of\\n  `\\\"open\\\"`. `refine_epic_status()` then tries to look that number up in\\n  `pull_requests`, finds nothing, and the placeholder survives unchanged.\\n  Verified by reading `collect_pull_requests` and `refine_epic_status`\\n  together in `shared/build_index.py`. This is the exact behavior the\\n  epic's own PR (pull request 11, still open at the time of writing) hits\\n  today: `resolve_epic_pull_requests` reports E1 through E6 as `\\\"open\\\"` by\\n  construction, not because the placeholder was refined against a real\\n  state.\\n- **Closing this gap needs a decision, not a fix.** Either `collect_pull_\\n  requests()` gains a second, `gh`-backed path for a pull request with no\\n  narration yet, or the placeholder stays and the index documents it as a\\n  known limit. This design does not resolve which.\\n- **`hash_tree()` and `is_stale()` were read but not exercised by hand**\\n  against a real edit during this write-up. The corresponding tests,\\n  `test_changing_an_authored_document_marks_the_index_stale` and `test_\\n  moving_the_git_head_marks_the_index_stale`, stand in for that\\n  verification.\\n\", \"approved_date\": null, \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E4-design.md\"}, {\"id\": \"cobuilder-family/E5\", \"epic_id\": \"E5\", \"feature_slug\": \"cobuilder-family\", \"title\": \"Epic Technical Solution Design: E5 — One lifecycle surface\", \"body_md\": \"# Epic Technical Solution Design: E5 — One lifecycle surface\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E5\\n\\nThis design was written on 2026-08-25, after this epic's two slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE5 adds three viewer modes — Decisions, Contexts, and Builds — on top of\\nthe index E4 built, adds a computed Backlog lane inside the Builds mode,\\nand moves the six generated pages out of `.lavish/` into the bundle's own\\n`pages/` directory. ADR-0018 governs the record index and lifecycle surface\\nthis epic presents.\\n\\n## Files Touched\\n\\n- `plugins/cobuilder-artifact/viewer/index.html` — the five-mode top-left\\n  switcher (Designs, Pull requests, Decisions, Contexts, Builds), each\\n  mode's render function, and the Backlog lane computation.\\n- `scripts/build_builds_view.py` — reads the status document and the index\\n  to render the Builds page: `read_epics`, `read_rubrics`, `read_plan`,\\n  `current_doc`, `render`, and `main`.\\n- `.cobuilder-architect/self/pages/{builds-view.html,decisions-register.html,\\n  view-designs.html,artifact-transport-map.html,cobuilder-vocabulary.html,\\n  gate1-cobuilder-family.html}` — the six pages, now living in the bundle\\n  instead of `.lavish/`. `.lavish/` still holds its own copies as of this\\n  writing; `git status --short` does not list `.lavish/*.html` as deleted,\\n  so the move added a bundle copy without yet removing the `.lavish/`\\n  originals.\\n- `docs/architecture/contexts/cobuilder-packaging/{canvas.md,boundary.yaml}`\\n  — the one bounded context the Contexts mode renders today.\\n\\n## Types & Signatures\\n\\n`scripts/build_builds_view.py`:\\n\\n```python\\ndef read_epics(designs_dir: Path, slices_md: str) -> list[dict]: ...\\ndef read_rubrics(rubrics_dir: Path) -> tuple[dict, dict]: ...\\ndef read_plan(plan_dir: Path, designs_dir: Path, rubrics_dir: Path) -> dict: ...\\ndef current_doc(gates: list[dict]) -> tuple[str, str, bool]: ...\\ndef render(page: Path, plan_dir: Path, designs_dir: Path, rubrics_dir: Path) -> None: ...\\ndef main() -> None: ...\\n```\\n\\nThe viewer's Builds mode reads `window.INDEX.entities.epic` and\\n`window.INDEX.joins` directly, per the in-page label at line 3305 of\\n`plugins/cobuilder-artifact/viewer/index.html`: \\\"The Backlog lane is a\\ndynamic query over `window.INDEX.entities.epic` and `window.INDEX.joins`.\\nNo authored backlog file feeds it.\\\" The Backlog lane holds every epic with\\na null branch, plus every slice not yet scored, matching\\n`03-program-design.md`'s stated rule.\\n\\n## Slice Decomposition\\n\\nPer `04-slices.md`:\\n\\n1. **Slice 10 — the Decisions and Contexts modes.** Depends on E4's index\\n   existing to read from. A person browses every decision and every\\n   context, with the anchor distinction (a decision an ADR cites versus a\\n   decision it merely touches) visible. Completed, score 1.00.\\n2. **Slice 11 — the Builds mode and the Backlog lane.** Depends on slice\\n   10's mode-switcher scaffold and on E4's `epic_to_pull_request` and\\n   `slice_to_epic` joins. Gates driven by the status document, the Backlog\\n   lane computed from the index, and pages moved into the bundle.\\n   Completed, score 0.92 — the one slice in this feature that scored below\\n   1.00, though it is still recorded as completed rather than escalated.\\n\\n## Test Plan\\n\\n- `tests/test_viewer_modes.py` — `test_viewer_contains_all_five_mode_\\n  buttons`, `test_decisions_mode_lists_all_records_and_anchor_distinction`,\\n  `test_contexts_mode_leads_with_violations_and_uncovered_districts`,\\n  `test_contexts_mode_renders_boundary_record_as_readable_rules`, and\\n  `test_export_artifact_parses_updated_viewer` — this last test guards\\n  against the exact regression `CLAUDE.md`'s Publish mode notes describe:\\n  an artifact export that cannot parse a viewer carrying new mode markup.\\n- No test file name in `tests/` targets `scripts/build_builds_view.py` by\\n  name, and no `test_builds_*` or `test_backlog_*` function exists in the\\n  suite as collected. The Builds mode and the Backlog lane are exercised\\n  indirectly, through `test_viewer_contains_all_five_mode_buttons` and the\\n  index tests in `tests/test_build_index.py` that the lane's query depends\\n  on, but neither a dedicated unit test nor an assertion on `read_plan`'s\\n  or `current_doc`'s return shape was found.\\n\\n## Risks & Open Questions\\n\\n- **`scripts/build_builds_view.py` carries no direct test coverage.**\\n  This is a genuine gap, not a design choice recorded anywhere. A change to\\n  `current_doc`'s gate-ordering logic or to `read_rubrics`'s score parsing\\n  could regress silently until a person looks at the rendered page.\\n- **`.lavish/` still holds the six pages the move was meant to retire.**\\n  `git status --short` shows no deletion for any `.lavish/*.html` file, so\\n  either the removal step of slice 11 has not run, or it ran in a commit\\n  this design could not locate. The bundle's `pages/` directory and\\n  `.lavish/` currently disagree about which copy is the source of record.\\n- **The 0.92 score on slice 11 has no recorded reason in `00-status.md`'s\\n  Escalated section**, because the slice was accepted rather than\\n  escalated. What specifically fell short of 1.00 was not found in any\\n  file this design reviewed.\\n\", \"approved_date\": null, \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E5-design.md\"}, {\"id\": \"cobuilder-family/E6\", \"epic_id\": \"E6\", \"feature_slug\": \"cobuilder-family\", \"title\": \"Epic Technical Solution Design: E6 — The reply channel\", \"body_md\": \"# Epic Technical Solution Design: E6 — The reply channel\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E6\\n\\nThis design was written on 2026-08-25, after this epic's three slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE6 adds an append-only comment ledger and its projection, an anchor\\ncomputed from the live DOM so a reader can click any element and attach a\\ncomment to it, a `POST /feedback` write endpoint, and a background wake\\ncommand an agent polls for new threads. ADR-0019 governs the ledger's\\nrecord shape.\\n\\n## Files Touched\\n\\n- `shared/ledger.py` — the whole ledger: append, fold, project, and the\\n  blocking watch function. 260-plus lines.\\n- `plugins/cobuilder-artifact/scripts/serve_bundle.py` — extends\\n  `http.server.SimpleHTTPRequestHandler` with a `POST /feedback` route,\\n  binds `127.0.0.1` only, and returns 403 when `--allow-write` is off.\\n- `plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py` — the\\n  background wake command. This is a placement difference from\\n  `03-program-design.md`, which names `scripts/watch_feedback.py` under\\n  `cobuilder-artifact`. The shipped file lives under\\n  `cobuilder-full-lifecycle` instead.\\n- `plugins/cobuilder-artifact/viewer/index.html` — `computeSelector()`,\\n  `captureRange()`, and `openDrawer()`, the client side of the anchor and\\n  comment flow the program design's call stack section names.\\n\\n## Types & Signatures\\n\\nRead directly from `shared/ledger.py`:\\n\\n```python\\nclass LedgerPaths: ...\\n\\ndef append_line(path: Path, obj: dict[str, Any]) -> None: ...\\ndef read_ledger(path: Path) -> list[dict[str, Any]]: ...\\ndef fold_threads(lines: list[dict[str, Any]]) -> dict[str, dict[str, Any]]: ...\\ndef project_threads(lines: list[dict[str, Any]]) -> dict[str, dict[str, Any]]: ...\\ndef write_projection(path: Path, projection: dict[str, dict[str, Any]]) -> None: ...\\ndef load_projection(path: Path) -> dict[str, dict[str, Any]]: ...\\n\\ndef append_comment(paths, anchor: str, text: str, author: str = \\\"human\\\") -> str: ...\\ndef append_reply(paths, thread_ulid: str, text: str, author: str = \\\"human\\\",\\n                  replies_to: str | None = None) -> str: ...\\ndef append_state(paths, thread_ulid: str, state: str) -> None: ...\\n\\ndef get_thread(paths: LedgerPaths, thread_ulid: str) -> dict[str, Any] | None: ...\\ndef get_all_threads(paths: LedgerPaths) -> dict[str, dict[str, Any]]: ...\\ndef rebuild_projection(paths: LedgerPaths) -> dict[str, dict[str, Any]]: ...\\n\\ndef watch_feedback(ledger_path: Path, since_offset: int = 0,\\n                    timeout_seconds: float = 30.0) -> tuple[list[dict[str, Any]], int]:\\n    \\\"\\\"\\\"Block until new lines appear past since_offset. Returns\\n    (new_lines, new_offset). Times out with ([], since_offset) after\\n    timeout_seconds. The caller (watch_feedback.py) exits 2 on an empty\\n    result and 0 otherwise.\\\"\\\"\\\"\\n```\\n\\n`plugins/cobuilder-artifact/scripts/serve_bundle.py`:\\n\\n```python\\nclass FeedbackHandler(SimpleHTTPRequestHandler):\\n    ledger_paths: LedgerPaths | None = None\\n    allow_write: bool = False\\n    def do_POST(self) -> None: ...   # only /feedback; 403 when read-only,\\n                                       # 400 on missing anchor or text,\\n                                       # 201 with {\\\"ulid\\\": ..., \\\"status\\\": \\\"appended\\\"}\\n\\ndef serve_bundle(bundle_dir: Path, port: int = 0, allow_write: bool = False) -> int: ...\\ndef main() -> None: ...\\n```\\n\\n`plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py`'s `main()`\\ncalls `watch_feedback(paths.ledger, args.since, args.timeout)`, prints each\\nnew line as one JSON object per line, and exits 0 or 2 depending on\\nwhether any line came back.\\n\\n## Slice Decomposition\\n\\nPer `04-slices.md`:\\n\\n1. **Slice 12 — the ledger and its projection.** No server involved.\\n   Append, fold, and project are file operations, tested without HTTP.\\n   Completed, score 0.92.\\n2. **Slice 13 — the anchor and the write endpoint.** Depends on slice 12's\\n   ledger existing for `serve_bundle.py` to append into. Click to anchor a\\n   sentence, `POST /feedback` appends, and the drawer keeps the text in\\n   view. Completed, score 1.00.\\n3. **Slice 14 — the wake command and the whole loop.** Depends on slices\\n   12 and 13. The background command blocks and returns new threads, and\\n   the loop runs once end to end. Completed, score 1.00.\\n\\n## Test Plan\\n\\n- `tests/test_ledger.py` — twelve tests: `test_append_comment_assigns_\\n  ulid_and_returns_it`, `test_append_thread_never_rewrites_an_existing_\\n  line`, `test_two_concurrent_appends_both_survive`, `test_state_change_\\n  is_a_new_line_not_an_edit`, `test_read_does_not_delete`, `test_thread_\\n  view_folds_a_reply_under_its_root`, `test_an_agent_reply_carries_author_\\n  agent_and_replies_to`, `test_projection_state_equals_the_last_\\n  transition_in_the_ledger`, `test_deleting_the_projection_rebuilds_it_\\n  from_the_ledger`, `test_projection_records_who_moved_the_state_and_\\n  when`, `test_get_thread_returns_none_for_missing`, and `test_\\n  projection_is_disposable_full_rebuild`.\\n- `tests/test_server_watch.py` — its tests are methods inside three\\n  classes, so a bare `def test` search at the start of a line misses them.\\n  Read in full, it carries `TestServer` (`test_post_feedback_appends_to_\\n  ledger`, `test_post_feedback_returns_403_when_read_only`, `test_post_\\n  feedback_rejects_other_paths`, `test_post_feedback_validates_required_\\n  fields`, `test_server_binds_loopback_only`), `TestWatchCommand`\\n  (`test_watch_exits_0_on_new_lines`, `test_watch_exits_2_on_timeout`,\\n  `test_watch_tracks_offset_per_reader`), and `TestEndToEndLoop`\\n  (`test_full_loop_comment_to_reply`, covering comment, wake, agent reply,\\n  state change, and the projection in one run). This covers every case\\n  `03-program-design.md`'s test plan names for the ledger's server and\\n  watch behavior.\\n\\n## Risks & Open Questions\\n\\n- **`test_full_loop_comment_to_reply` carries a self-correcting bug in its\\n  own body**, not in the code it tests: it first checks\\n  `hasattr(sys.modules[__name__], 'get_all_threads')`, which is always\\n  false because `get_all_threads` was never imported at module level, then\\n  falls through to a local `from ledger import get_all_threads` on the\\n  next line and proceeds correctly. The test passes, but the dead\\n  `hasattr` branch reads as leftover exploratory code that a review should\\n  clean up.\\n- **The wake command's placement under `cobuilder-full-lifecycle` rather\\n  than `cobuilder-artifact`** was not explained by any commit message or\\n  ADR this design reviewed. `cobuilder-artifact` owns `serve_bundle.py`,\\n  the other half of the same feature, so the two scripts a full loop needs\\n  now live in two different plugins.\\n- **`select.select()` on a plain file descriptor, inside `watch_feedback`,\\n  is a Linux-and-BSD idiom that does not reliably signal on every\\n  filesystem.** The function falls back to a 0.5-second poll when `select`\\n  raises `OSError`, so the polling path is exercised whenever the fast\\n  path is unavailable, but no test in the suite forces that fallback path\\n  specifically.\\n\", \"approved_date\": null, \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E6-design.md\"}, {\"id\": \"gate-doc-surfacing/E1\", \"epic_id\": \"E1\", \"feature_slug\": \"gate-doc-surfacing\", \"title\": \"Epic Technical Solution Design: E1 — Index gate docs into data/index.json\", \"body_md\": \"# Epic Technical Solution Design: E1 — Index gate docs into data/index.json\\n\\nFeature: gate-doc-surfacing\\nEpic ID: E1\\n\\n## Scope and Intent\\nProject `docs/plans/<slug>/03-program-design.md` and\\n`docs/plans/<slug>/epic-<id>-design.md` files into two new\\n`data/index.json` entity arrays, `program_design` and `epic_design`, so the\\nviewer has something to read. Attach a `doc` reference to the matching\\nGate 3 entry in `joins.feature_gates`, and a `design_doc` reference to the\\nmatching epic in `entities.epic`. No viewer changes in this epic — that is\\nE2.\\n\\n## Files Touched\\n- `shared/build_index.py` — add `PROGRAM_DESIGN_FIELDS`,\\n  `EPIC_DESIGN_FIELDS`, `discover_plan_gate_docs()`,\\n  `project_program_design()`, `project_epic_design()`; wire the new\\n  entities into the top-level `build_index()`/`main()` flow the same way\\n  `collect_designs()` already is; extend `resolve_feature_gates()` and the\\n  epic entity emission in `collect_designs()` with the new `doc`/`design_doc`\\n  references.\\n- `tests/test_build_index_gate_docs.py` — new test file.\\n\\n## Types & Signatures\\n\\n```python\\nPROGRAM_DESIGN_FIELDS: tuple[str, ...] = (\\n    \\\"id\\\", \\\"feature_slug\\\", \\\"gate\\\", \\\"title\\\", \\\"body_md\\\", \\\"approved_date\\\", \\\"source_path\\\",\\n)\\nEPIC_DESIGN_FIELDS: tuple[str, ...] = (\\n    \\\"id\\\", \\\"epic_id\\\", \\\"feature_slug\\\", \\\"title\\\", \\\"body_md\\\", \\\"approved_date\\\", \\\"source_path\\\",\\n)\\n\\ndef discover_plan_gate_docs(repo: Path) -> list[dict]:\\n    \\\"\\\"\\\"Walk docs/plans/*/. Return one dict per gate doc found:\\n    {\\\"kind\\\": \\\"program\\\", \\\"feature_slug\\\": str, \\\"path\\\": Path, \\\"text\\\": str}\\n    or\\n    {\\\"kind\\\": \\\"epic\\\", \\\"epic_id\\\": str, \\\"feature_slug\\\": str, \\\"path\\\": Path, \\\"text\\\": str}.\\n    An epic id is parsed from the filename epic-<id>-design.md; a file\\n    whose id segment fails to parse is skipped, not raised on — this\\n    function never crashes a full index rebuild over one malformed name.\\n    \\\"\\\"\\\"\\n\\ndef project_program_design(feature_slug: str, path: Path, text: str) -> dict:\\n    \\\"\\\"\\\"Extract a title (first '# ' heading) and approved_date (from the\\n    matching 00-status.md's Gate 3 line, if resolvable) alongside the raw\\n    body. Returns a dict filtered through project_fields(..., PROGRAM_DESIGN_FIELDS).\\\"\\\"\\\"\\n\\ndef project_epic_design(epic_id: str, feature_slug: str, path: Path, text: str) -> dict:\\n    \\\"\\\"\\\"Same shape, keyed on epic_id instead of feature_slug.\\\"\\\"\\\"\\n```\\n\\n`id` for a `program_design` entity is the bare `feature_slug` (one Gate 3\\ndoc per feature, so no collision risk). `id` for an `epic_design` entity is\\nthe bare `epic_id` parsed from its filename — note this is the *unscoped*\\nepic id (`E1`), not the `<design>/<epic-id>` scoped id `entities.epic`\\nuses, because a technical design file lives under one `docs/plans/<slug>/`\\ndirectory already, so `feature_slug` carries the scoping context instead.\\n`entities.epic[].design_doc` stores the *scoped* lookup path\\n(`epic_design[<feature_slug>/<epic_id>]` is wrong; the correct join is:\\nlook up `epic_design` by its own `epic_id` key, disambiguated by also\\nmatching `feature_slug`, since two features could each have an `E1`).\\nImplementation note: to avoid this exact ambiguity, `epic_design`'s\\n`id` field is actually `f\\\"{feature_slug}/{epic_id}\\\"`, matching\\n`entities.epic`'s own `<design>/<epic-id>` scoping convention. This\\nsupersedes the plain `epic_id` framing above — keep them consistent\\nduring implementation; the test plan below locks this down.\\n\\n## Slice Decomposition\\n1. **Slice 1 (tracer bullet).** Add the constants and empty-list wiring so\\n   `data/index.json` always carries `program_design: []` and\\n   `epic_design: []`, even with zero gate docs on disk. No dependency.\\n2. **Slice 2 (real parsing).** Implement `discover_plan_gate_docs()`,\\n   `project_program_design()`, `project_epic_design()`, and the\\n   `feature_gates`/`epic` join attachment. Depends on slice 1's constants.\\n3. **Slice 3 (edge cases).** Missing `docs/plans/` entirely, a plan\\n   directory with only some gate docs, a malformed epic filename. Depends\\n   on slice 2.\\n\\n## Test Plan\\n`tests/test_build_index_gate_docs.py`:\\n- `test_project_program_design_reads_title_and_body` — feed a small\\n  markdown fixture, assert `title`/`body_md`/`source_path` match.\\n- `test_project_epic_design_id_is_scoped_feature_slug_and_epic_id` — locks\\n  down the `<feature_slug>/<epic_id>` id convention from the note above.\\n- `test_discover_plan_gate_docs_skips_malformed_epic_filename` — a file\\n  named `epic-design.md` (no id segment) is skipped, not raised on.\\n- `test_discover_plan_gate_docs_no_plans_dir_returns_empty_list` — repo\\n  with no `docs/plans/` at all.\\n- `test_resolve_feature_gates_attaches_doc_when_program_design_exists`.\\n- `test_resolve_feature_gates_gate3_doc_key_absent_when_no_md_file`.\\n- `test_build_index_end_to_end_index_json_has_program_design_and_epic_design_keys`\\n  — run against this repo's own tree; assert both keys exist, and (once\\n  this feature's own gate docs land) that this feature's own entities are\\n  among them.\\n\\n## Risks & Open Questions\\n- The id-scoping ambiguity flagged above (`epic_id` alone vs.\\n  `<feature_slug>/<epic_id>`) must be resolved consistently between the\\n  producer (`project_epic_design`) and the consumer\\n  (`entities.epic[].design_doc` lookup in E2) — the test plan pins the\\n  chosen shape so E2 cannot silently diverge from it.\\n- `approved_date` sourcing from `00-status.md`'s Gate 3/4b lines duplicates\\n  what `resolve_feature_gates()` already parses there. Acceptable\\n  duplication since the two functions read the same file for different\\n  fields, not the same field twice.\\n\", \"approved_date\": null, \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/gate-doc-surfacing/epic-E1-design.md\"}, {\"id\": \"gate-doc-surfacing/E2\", \"epic_id\": \"E2\", \"feature_slug\": \"gate-doc-surfacing\", \"title\": \"Epic Technical Solution Design: E2 — Surface gate docs in the Builds view\", \"body_md\": \"# Epic Technical Solution Design: E2 — Surface gate docs in the Builds view\\n\\nFeature: gate-doc-surfacing\\nEpic ID: E2\\n\\n## Scope and Intent\\nMake the Builds view's Gate Rail cards and epic design chips clickable,\\nopening a read-only sheet that shows a `program_design` or `epic_design`\\nentity's content. Depends on E1 shipping the entities first — this epic\\nreads `window.INDEX.entities.program_design`/`.epic_design`, both empty\\narrays until E1 lands, so E2's tracer bullet is safe to build against a\\nfixture even before E1 slice 2 completes.\\n\\n## Files Touched\\n- `plugins/artifact/viewer/index.html` — new sheet markup block (sibling of\\n  `#adr-sheet`, ~line 1022), new functions `openGateDocSheet(kind, id)`,\\n  `closeGateDocSheet()`, `renderGateDocSheet()`; extend\\n  `closeAnyOpenSheet()` (~line 4003) and `renderSheetVisibility()`\\n  (~line 3960) for mutual exclusivity; wire click handlers into\\n  `renderBuildsMainContent()`'s `gate-rail-grid` (~line 3249) and\\n  `renderEpicCard()` (~line 3282).\\n\\n## Types & Signatures\\n\\n```javascript\\n// state additions, alongside existing state.adrOpen / state.activeAdrId\\nstate.gateDocOpen = false;\\nstate.activeGateDocKind = null; // 'program' | 'epic'\\nstate.activeGateDocId = null;   // matches program_design/epic_design id\\n\\nfunction openGateDocSheet(kind, id){\\n  if (state.commentsOpen) state.commentsOpen = false;\\n  if (state.adrOpen) state.adrOpen = false;\\n  if (state.assessmentOpen) state.assessmentOpen = false;\\n  if (state.audioDialogOpen) stopAudio();\\n  state.gateDocOpen = true;\\n  state.activeGateDocKind = kind;\\n  state.activeGateDocId = id;\\n  renderGateDocSheet();\\n  renderSheetVisibility();\\n}\\nfunction closeGateDocSheet(){\\n  state.gateDocOpen = false;\\n  renderSheetVisibility();\\n}\\nfunction renderGateDocSheet(){\\n  const index = window.INDEX || {};\\n  const entities = index.entities || {};\\n  const table = state.activeGateDocKind === 'program'\\n    ? (entities.program_design || {})\\n    : (entities.epic_design || {});\\n  const record = table[state.activeGateDocId];\\n  // ... title/meta/body, reusing renderMarkdown() for record.body_md,\\n  // and the \\\"record not captured\\\" empty-card fallback openAdrSheet uses\\n  // when id/record is missing.\\n}\\n```\\n\\n**Resolved during Gate 4b:** every existing `window.INDEX.entities.<type>`\\nis array-shaped (confirmed against `entities.adr`/`entities.epic` in the\\ncurrent `data/index.json`), unlike the separate dict-shaped `window.ADRS`.\\n`program_design`/`epic_design` follow the same array convention as every\\nother entity, for consistency. `renderGateDocSheet` looks a record up with\\n`(entities.program_design || []).find(r => r.id === id)` (or the\\n`epic_design` equivalent) rather than a dict key access.\\n\\n## Slice Decomposition\\n1. **Slice 4 (tracer bullet).** Sheet markup + `openGateDocSheet`/\\n   `closeGateDocSheet`/`renderSheetVisibility` wiring, gate-rail-grid card\\n   click handler. Renders title only, empty body — proves the plumbing\\n   without depending on E1's real content.\\n2. **Slice 5 (real content).** `renderGateDocSheet` renders `body_md` via\\n   `renderMarkdown()`. Epic card design-doc chip added to `renderEpicCard`.\\n   Depends on E1 slice 2 (real entities) for a meaningful demo, but the\\n   render code itself only depends on slice 4's plumbing.\\n3. **Slice 6 (edge cases).** No-`doc` gate card stays non-interactive\\n   (no click handler attached at all, not a handler that no-ops); mutual\\n   exclusivity with `#adr-sheet`/`#assessment-sheet`/comments drawer/audio\\n   dialog, both directions.\\n\\n## Test Plan\\nNo JS test harness exists in this repo for the viewer (it is one static\\nHTML file with no build step, per ADR-0001/ADR-0020). Verification is\\nmanual, via the `run` skill or a direct `python3 -m http.server` serve of\\n`.cobuilder-architect/self/`, exercising:\\n- a Gate Rail card with a `doc` opens the sheet and shows body content\\n  (slice 4+5)\\n- a Gate Rail card with no `doc` has no click affordance (slice 6)\\n- opening the gate-doc sheet while the ADR sheet is open closes the ADR\\n  sheet, and vice versa (slice 6)\\n- an epic card with `design_doc` shows a chip; clicking it opens the sheet\\n  scoped to that epic's doc (slice 5)\\n\\n## Risks & Open Questions\\n- No JS test harness means slices 4-6 lean on manual dogfood verification\\n  more than the repo's usual pytest-backed rigor. Rubric criteria for this\\n  epic's slices are written as concrete manual-check steps, not automated\\n  assertions, for this reason.\\n\", \"approved_date\": null, \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/gate-doc-surfacing/epic-E2-design.md\"}]}, \"joins\": {\"adr_to_pull_request\": {\"ADR-0001\": {\"pr\": 2, \"via\": \"direct\", \"path\": []}, \"ADR-0002\": {\"pr\": 2, \"via\": \"direct\", \"path\": []}, \"ADR-0003\": {\"pr\": 3, \"via\": \"direct\", \"path\": []}, \"ADR-0004\": {\"pr\": 3, \"via\": \"direct\", \"path\": []}, \"ADR-0005\": {\"pr\": 4, \"via\": \"direct\", \"path\": []}, \"ADR-0006\": {\"pr\": 4, \"via\": \"direct\", \"path\": []}, \"ADR-0007\": {\"pr\": 5, \"via\": \"direct\", \"path\": []}, \"ADR-0008\": {\"pr\": 5, \"via\": \"direct\", \"path\": []}, \"ADR-0009\": {\"pr\": 6, \"via\": \"direct\", \"path\": []}, \"ADR-0010\": {\"pr\": 9, \"via\": \"direct\", \"path\": []}, \"ADR-0011\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"cobuilder-implement/E1\", \"cobuilder-implement/E2\", \"design-mode/E1\"]}, \"ADR-0012\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"cobuilder-implement/E1\", \"cobuilder-implement/E2\"]}, \"ADR-0013\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"cobuilder-implement/E1\", \"cobuilder-implement/E2\"]}, \"ADR-0014\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"design-mode/E1\"]}, \"ADR-0015\": {\"pr\": 11, \"via\": \"epic\", \"path\": [\"cobuilder-implement/E1\", \"cobuilder-implement/E2\", \"design-mode/E1\", \"plugin-split/E1\", \"plugin-split/E2\", \"plugin-split/E3\", \"plugin-split/E4\", \"plugin-split/E5\", \"plugin-split/E6\", \"plugin-split/E7\", \"plugin-split/E8\"]}, \"ADR-0016\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0017\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0018\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0019\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0020\": {\"pr\": 11, \"via\": \"direct\", \"path\": []}, \"ADR-0021\": {\"pr\": 16, \"via\": \"epic\", \"path\": [\"book-index-tiering/E1\"]}, \"ADR-0022\": {\"pr\": null, \"via\": \"epic\", \"path\": [\"gate-doc-surfacing/E1\", \"gate-doc-surfacing/E2\"]}}, \"epic_to_pull_request\": {\"book-index-tiering/E1\": 16, \"cobuilder-implement/E1\": 11, \"cobuilder-implement/E2\": 12, \"design-mode/E1\": 11, \"plugin-split/E1\": 11, \"plugin-split/E2\": 11, \"plugin-split/E3\": 11, \"plugin-split/E4\": 11, \"plugin-split/E5\": 11, \"plugin-split/E6\": 11, \"plugin-split/E8\": 18}, \"epic_status\": {\"book-index-tiering/E1\": \"open\", \"build-workflow-polish/E1\": \"unstarted\", \"build-workflow-polish/E2\": \"unstarted\", \"build-workflow-polish/E3\": \"unstarted\", \"build-workflow-polish/E4\": \"unstarted\", \"cobuilder-implement/E1\": \"merged\", \"cobuilder-implement/E2\": \"merged\", \"design-mode/E1\": \"merged\", \"gate-doc-surfacing/E1\": \"no-pull-request\", \"gate-doc-surfacing/E2\": \"no-pull-request\", \"inflight-record-store/E1\": \"unstarted\", \"inflight-record-store/E2\": \"unstarted\", \"inflight-record-store/E3\": \"unstarted\", \"inflight-record-store/E4\": \"unstarted\", \"lean-bundle-diffs/E1\": \"unstarted\", \"lean-bundle-diffs/E2\": \"unstarted\", \"lean-bundle-diffs/E3\": \"unstarted\", \"maintainable-viewer/E1\": \"unstarted\", \"maintainable-viewer/E2\": \"unstarted\", \"maintainable-viewer/E3\": \"unstarted\", \"maintainable-viewer/E4\": \"unstarted\", \"plugin-split/E1\": \"merged\", \"plugin-split/E2\": \"merged\", \"plugin-split/E3\": \"merged\", \"plugin-split/E4\": \"merged\", \"plugin-split/E5\": \"merged\", \"plugin-split/E6\": \"merged\", \"plugin-split/E7\": \"unstarted\", \"plugin-split/E8\": \"open\", \"ubiquitous-language/E1\": \"unstarted\", \"ubiquitous-language/E2\": \"unstarted\", \"ubiquitous-language/E3\": \"unstarted\", \"ubiquitous-language/E4\": \"unstarted\", \"ubiquitous-language/E5\": \"unstarted\"}, \"slice_to_epic\": {\"cobuilder-family/4\": \"plugin-split/E1\", \"cobuilder-family/5\": \"plugin-split/E1\", \"cobuilder-family/6\": \"plugin-split/E1\", \"cobuilder-family/7\": \"plugin-split/E1\", \"cobuilder-family/1\": \"plugin-split/E2\", \"cobuilder-family/2\": \"plugin-split/E3\", \"cobuilder-family/3\": \"plugin-split/E3\", \"cobuilder-family/8\": \"plugin-split/E4\", \"cobuilder-family/9\": \"plugin-split/E4\", \"cobuilder-family/10\": \"plugin-split/E5\", \"cobuilder-family/11\": \"plugin-split/E5\", \"cobuilder-family/12\": \"plugin-split/E6\", \"cobuilder-family/13\": \"plugin-split/E6\", \"cobuilder-family/14\": \"plugin-split/E6\", \"gate-doc-surfacing/1\": \"gate-doc-surfacing/E1\", \"gate-doc-surfacing/2\": \"gate-doc-surfacing/E1\", \"gate-doc-surfacing/3\": \"gate-doc-surfacing/E1\", \"gate-doc-surfacing/4\": \"gate-doc-surfacing/E2\", \"gate-doc-surfacing/5\": \"gate-doc-surfacing/E2\", \"gate-doc-surfacing/6\": \"gate-doc-surfacing/E2\"}, \"slice_to_epic_unresolved\": {}, \"context_verifies_district\": {\"cobuilder-packaging\": [\"commands\", \"skills\", \"scripts\", \"viewer\", \"docs\"]}, \"district_uncovered\": [\".cobuilder-architect\"], \"adr_to_context\": {\"ADR-0016\": \"cobuilder-packaging\", \"ADR-0017\": \"cobuilder-packaging\", \"ADR-0018\": \"cobuilder-packaging\", \"ADR-0019\": \"cobuilder-packaging\", \"ADR-0020\": \"cobuilder-packaging\", \"ADR-0022\": \"viewer\"}, \"adr_to_district\": {\"ADR-0001\": \"scripts\", \"ADR-0002\": \"scripts\", \"ADR-0003\": \".prodyssey\", \"ADR-0004\": \"skills\", \"ADR-0005\": \"skills\", \"ADR-0006\": \"scripts\", \"ADR-0007\": \"scripts\", \"ADR-0008\": \"scripts\", \"ADR-0009\": \"commands\", \"ADR-0010\": \"skills\", \"ADR-0011\": \"skills\", \"ADR-0012\": \"skills\", \"ADR-0013\": \"skills\", \"ADR-0014\": \"scripts\", \"ADR-0015\": \"skills\", \"ADR-0021\": \"skills\"}, \"feature_gates\": {\"cobuilder-family\": [{\"n\": 1, \"name\": \"Product\", \"state\": \"APPROVED 2026-08-20\"}, {\"n\": 2, \"name\": \"Architecture\", \"state\": \"APPROVED 2026-08-21\"}, {\"n\": 3, \"name\": \"Program Design\", \"state\": \"APPROVED 2026-08-21\", \"doc\": \"cobuilder-family\"}, {\"n\": 4, \"name\": \"Slice plan, epic designs, and rubrics\", \"state\": \"APPROVED 2026-08-25\"}], \"gate-doc-surfacing\": [{\"n\": 1, \"name\": \"Product\", \"state\": \"APPROVED 2026-09-01\"}, {\"n\": 2, \"name\": \"Architecture\", \"state\": \"APPROVED 2026-09-01\"}, {\"n\": 3, \"name\": \"Program Design\", \"state\": \"APPROVED 2026-09-01\", \"doc\": \"gate-doc-surfacing\"}, {\"n\": 4, \"name\": \"Slice plan, epic designs, and rubrics\", \"state\": \"APPROVED 2026-09-01\"}], \"skill-collision-fix\": [{\"n\": 1, \"name\": \"3\", \"state\": \"n/a, superseded by direct conversation record\"}, {\"n\": 4, \"name\": \"Slice plan, epic designs, and rubrics\", \"state\": \"in progress\"}]}}};",
  ".cobuilder-architect/self/data/index.json": "diff --git a/.cobuilder-architect/self/data/index.json b/.cobuilder-architect/self/data/index.json\nindex 61c6d0b..0217870 100644\n--- a/.cobuilder-architect/self/data/index.json\n+++ b/.cobuilder-architect/self/data/index.json\n@@ -1,12 +1,12 @@\n {\n   \"schema_version\": \"1.3\",\n   \"sources\": {\n-    \"git_head\": \"55395917fd9a0dd54b272e76799d319f0a5e174d\",\n+    \"git_head\": \"651b07af45d87e17e7d855ed04d271d5f12f618d\",\n     \"trees\": {\n-      \"docs/architecture/adr\": \"sha256:fd9dd693addcaf68e2bcc92bf905004dfcf546721de125b51b7d5e9e758ba8a1\",\n-      \"docs/architecture/designs\": \"sha256:83935e6bf2bb91a878f8883ea5198777c99bf2252273ac7fcba639bd723d366f\",\n+      \"docs/architecture/adr\": \"sha256:ff9214a45fa93be6d1db6859475dceb268c9816a137ae97da4580533b18f55cf\",\n+      \"docs/architecture/designs\": \"sha256:babc2ac67e7550b96634728f816cef8ac238900fceb023b7acc5e4b7d1f369c9\",\n       \"docs/architecture/contexts\": \"sha256:4626a327bd28df972d97d40dc1325c28808eb48789649b87eadd218b3528c907\",\n-      \"docs/plans\": \"sha256:c6ed98e3846b908d8cfd216b6ff873473172ec2559e6e6ac6a4141253205efd1\"\n+      \"docs/plans\": \"sha256:c74faf0d87d977f583e8fbd67d00138b8ee695a145112fae6f391b4107aca33e\"\n     }\n   },\n   \"entities\": {\n@@ -115,6 +115,11 @@\n         \"id\": \"ADR-0021\",\n         \"title\": \"book-index.md gains a nano tier, and full-book loading stops being unconditional\",\n         \"state\": \"decided\"\n+      },\n+      {\n+        \"id\": \"ADR-0022\",\n+        \"title\": \"Gate documents projected into the record index\",\n+        \"state\": \"decided\"\n       }\n     ],\n     \"design\": [\n@@ -142,6 +147,12 @@\n         \"outcome\": \"An engineer can design a change with the plugin before writing it, and the resulting intent, ADR, and diagrams carry through to the pull request that implements it.\",\n         \"stage\": \"implemented\"\n       },\n+      {\n+        \"id\": \"gate-doc-surfacing\",\n+        \"name\": \"gate-doc-surfacing\",\n+        \"outcome\": \"Gate documents (03-program-design.md, epic-<id>-design.md) are readable from the viewer's Builds view, not just from docs/plans/ or a one-off Artifact publish.\",\n+        \"stage\": \"decided\"\n+      },\n       {\n         \"id\": \"inflight-record-store\",\n         \"name\": \"inflight-record-store\",\n@@ -246,6 +257,26 @@\n         \"state\": \"open\",\n         \"note\": \"Design mode as a pre-code interview and challenge gate. Shares a branch and a pull request with the cobuilder-implement design. See ADR-0013: a branch may carry more than one design.\"\n       },\n+      {\n+        \"id\": \"gate-doc-surfacing/E1\",\n+        \"design\": \"gate-doc-surfacing\",\n+        \"epic_id\": \"E1\",\n+        \"branch\": \"design/gate-doc-surfacing\",\n+        \"pr\": null,\n+        \"state\": \"completed\",\n+        \"note\": \"shared/build_index.py: add PROGRAM_DESIGN_FIELDS and EPIC_DESIGN_FIELDS projection tuples plus project_program_design/project_epic_design, following the project_fields(source, fields) pattern used by project_goal/project_intent/project_assessment. Parse docs/plans/<slug>/03-program-design.md and epic-<id>-design.md; emit program_design and epic_design entities into data/index.json. Extend feature_gates join entries with a doc reference where one exists.\",\n+        \"design_doc\": \"gate-doc-surfacing/E1\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing/E2\",\n+        \"design\": \"gate-doc-surfacing\",\n+        \"epic_id\": \"E2\",\n+        \"branch\": \"design/gate-doc-surfacing\",\n+        \"pr\": null,\n+        \"state\": \"completed\",\n+        \"note\": \"plugins/artifact/viewer/index.html: make gate-rail-grid cards (renderBuildsMainContent, ~line 3249) clickable, opening a gate-doc sheet modeled on openAdrSheet. Add a design-doc chip to renderEpicCard (~line 3282) for epics with an epic_design entity.\",\n+        \"design_doc\": \"gate-doc-surfacing/E2\"\n+      },\n       {\n         \"id\": \"inflight-record-store/E1\",\n         \"design\": \"inflight-record-store\",\n@@ -1033,6 +1064,66 @@\n         \"score\": \"1.00\",\n         \"attempts\": 1,\n         \"state\": \"completed\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing/1\",\n+        \"feature\": \"gate-doc-surfacing\",\n+        \"n\": 1,\n+        \"title\": \"Tracer bullet: constants and empty entity lists\",\n+        \"ends_with\": \"`data/index.json` carries `program_design` and `epic_design` keys (empty lists when no gate docs exist yet)\",\n+        \"score\": \"1.00\",\n+        \"attempts\": 1,\n+        \"state\": \"completed\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing/2\",\n+        \"feature\": \"gate-doc-surfacing\",\n+        \"n\": 2,\n+        \"title\": \"Real parsing: project Gate 3 and Gate 4b docs\",\n+        \"ends_with\": \"A repo with a `03-program-design.md` and an `epic-*-design.md` produces populated `program_design`/`epic_design` entities, and `joins.feature_gates[slug][3].doc` points at the projected id\",\n+        \"score\": \"1.00\",\n+        \"attempts\": 1,\n+        \"state\": \"completed\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing/3\",\n+        \"feature\": \"gate-doc-surfacing\",\n+        \"n\": 3,\n+        \"title\": \"Edge cases: missing files, malformed epic id, no plans dir\",\n+        \"ends_with\": \"A plan directory with no gate docs, and a repo with no `docs/plans/` at all, both still produce a clean index build with empty (not missing) entity keys\",\n+        \"score\": \"1.00\",\n+        \"attempts\": 1,\n+        \"state\": \"completed\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing/4\",\n+        \"feature\": \"gate-doc-surfacing\",\n+        \"n\": 4,\n+        \"title\": \"Tracer bullet: clickable Gate Rail card opens an empty sheet\",\n+        \"ends_with\": \"Clicking a Gate Rail card with a `doc` opens a sheet showing the gate's title and an empty body; a card with no `doc` stays non-interactive\",\n+        \"score\": \"1.00\",\n+        \"attempts\": 1,\n+        \"state\": \"completed\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing/5\",\n+        \"feature\": \"gate-doc-surfacing\",\n+        \"n\": 5,\n+        \"title\": \"Real content: render the gate doc body, add the epic chip\",\n+        \"ends_with\": \"The sheet renders `body_md` through the existing `renderMarkdown()`; an epic card with a `design_doc` shows a chip that opens the same sheet scoped to that epic\",\n+        \"score\": \"1.00\",\n+        \"attempts\": 1,\n+        \"state\": \"completed\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing/6\",\n+        \"feature\": \"gate-doc-surfacing\",\n+        \"n\": 6,\n+        \"title\": \"Edge cases: no entity, mutual exclusivity with other sheets\",\n+        \"ends_with\": \"A Gate Rail card whose gate has no matching doc opens nothing; opening the gate-doc sheet closes any open ADR/assessment/comments sheet, and vice versa\",\n+        \"score\": \"1.00\",\n+        \"attempts\": 1,\n+        \"state\": \"completed\"\n       }\n     ],\n     \"publication\": [\n@@ -1048,6 +1139,91 @@\n         \"artifact_url\": \"https://claude.ai/code/artifact/4bf5fff8-f70e-4e68-8553-2b8a254c5e79\",\n         \"published_at\": \"2026-07-22T09:56:48Z\"\n       }\n+    ],\n+    \"program_design\": [\n+      {\n+        \"id\": \"cobuilder-family\",\n+        \"feature_slug\": \"cobuilder-family\",\n+        \"gate\": 3,\n+        \"title\": \"Program Design: the cobuilder family\",\n+        \"body_md\": \"# Program Design: the cobuilder family\\n\\nGate 3. The decisions an agent would otherwise make silently while\\nimplementing Gate 2. Every one of them is cheap to change here and expensive\\nto change once a slice has landed.\\n\\nGoverned by ADR-0016, ADR-0017, ADR-0018, and ADR-0019. Where this document\\nand an ADR disagree, the ADR wins and this document is wrong.\\n\\n## Files\\n\\n### The repository becomes a marketplace\\n\\n```\\n.claude-plugin/marketplace.json        one file, five plugin entries\\nshared/                                 vendored by symlink into each plugin\\n  _bundle_meta.py\\n  _manifest.py\\n  migrate_bundle.py\\n  verify_bundle.py\\n  skills/ste-writing/\\n  skills/mermaid/\\nplugins/\\n  cobuilder-architect/\\n  cobuilder-pr/\\n  cobuilder-implement/\\n  cobuilder-artifact/\\n  cobuilder-full-lifecycle/\\ntests/                                  new, see Test plan\\n```\\n\\n`shared/` sits at the marketplace root and each plugin carries a symlink to\\nit at `<plugin>/shared`. The install copy dereferences the link, so each\\nplugin's cache holds its own real copy. This is the mechanism ADR-0017\\nchose and the one E2 exists to prove.\\n\\n### Where each existing file lands\\n\\n| From | To | Why |\\n|---|---|---|\\n| `skills/architecture/` | `cobuilder-architect` | design, review, maintenance, decisions, describe, debug — six modes, since `explore-design` is deleted |\\n| `skills/odyssey/` | `cobuilder-pr` | baseline, generate, review. `view` and `publish` go to `cobuilder-artifact` instead |\\n| `skills/ste-writing/`, `skills/mermaid/` | `shared/skills/` | both are cited from both pillars, which is exactly what vendoring is for |\\n| `.claude/skills/collaborate-with-user/` | `cobuilder-artifact`, folded into its `SKILL.md` and `references/` | it presents a decision, which is the artifact pillar's job |\\n| `viewer/index.html` | `cobuilder-artifact` | the reading surface |\\n| `extract_story.py`, `extract_diffs.py`, `generate_prompts.py`, `generate_audio.py`, `build_diagrams.py`, `render_review.py` | `cobuilder-pr` | all six only run inside the per-PR sweep or submit |\\n| `build_adrs.py`, `build_designs.py` | deleted | `build_index.py` replaces both, see below |\\n| `validate_decision_state.py`, `compute_scores.py`, `html_to_pdf.py` | `cobuilder-architect` | decisions mode and the two review reports |\\n| `export_artifact.py`, `export_index.py`, `record_publish.py` | `cobuilder-artifact` | publish |\\n| `_bundle_meta.py`, `_manifest.py`, `migrate_bundle.py`, `verify_bundle.py` | `shared/` | every plugin that touches a bundle needs all four |\\n| `~/.claude/skills/cobuilder-factory/` | `cobuilder-implement` | the port and the rename |\\n\\n### New files\\n\\n| File | Plugin | Purpose |\\n|---|---|---|\\n| `scripts/build_index.py` | `cobuilder-artifact` | builds `data/index.json` and `index.js` |\\n| `scripts/serve_bundle.py` | `cobuilder-artifact` | replaces `python3 -m http.server`, adds `POST /feedback` |\\n| `scripts/watch_feedback.py` | `cobuilder-artifact` | the background wake command |\\n| `skills/cobuilder-full/SKILL.md` | `cobuilder-full-lifecycle` | names the four modes and when to reach for each |\\n\\n**`build_index.py` lives in the artifact plugin and reads what the other\\npillars wrote.** Not only `cobuilder-architect`'s ADRs, designs, and\\ncontexts. Also `cobuilder-pr`'s `story.json`, its assessments, and its\\n`docs/pull-requests/`, and `cobuilder-implement`'s `docs/plans/`. That is not\\na seam violation. Every one of those paths is inside the *target repo* or\\ninside the bundle. None resolves into another plugin's root. This is the\\ndistinction ADR-0016 draws, and it is the one an implementing agent is most\\nlikely to get wrong.\\n\\nSettled at Gate 3 review: the index is a repo-wide artifact rather than one\\npillar's output, so it belongs with the pillar that presents the repo.\\n\\n### `cobuilder-artifact` owns the authoring skill as well as the viewer\\n\\n`collaborate-with-user` does not travel as a separate skill directory. Its\\nrules become part of `cobuilder-artifact`'s own `SKILL.md`, with the long\\nmaterial in `references/`: the composition rules, the theme-token rules, the\\nhonesty rule, and the relationship to the architecture skill's two report\\ntemplates. One plugin then owns both halves of presenting something to a\\nperson — the page it builds and the answer it collects.\\n\\n**The agent doing this work must invoke the `skill-development` skill first.**\\nFolding a standalone `SKILL.md` into another plugin's skill is exactly the\\njob that has established practice, and this build should follow it rather\\nthan reinvent a layout.\\n\\n### Pages leave `.lavish/`\\n\\n`.lavish/` is another tool's directory and this plugin's pages have no\\nbusiness in it. A generated page belongs in the bundle, because it is derived\\nfrom an authored markdown file and a rebuild can reproduce it:\\n\\n```\\n<bundle-dir>/pages/<slug>.html          the generated page\\n<bundle-dir>/exports/publish-manifest.json   already records the URL\\n```\\n\\nThe markdown stays the source of record in `docs/`, unchanged. Moving the\\nexisting six pages is part of E5, and each keeps its published URL, because\\na URL is bound to the artifact rather than to the local path.\\n\\n## Types and signatures\\n\\n### `shared/_bundle_meta.py`\\n\\n```python\\nSCHEMA_VERSION = \\\"1.3\\\"                            # was \\\"1.2\\\"\\nSCHEMA_VERSION_KNOWN = {\\\"1.0\\\", \\\"1.1\\\", \\\"1.2\\\", \\\"1.3\\\"}\\nCURRENT_BUNDLE_FORMAT = 3                         # was 2\\n\\nclass BundleIncompatible(RuntimeError):\\n    \\\"\\\"\\\"Raised when a bundle demands a reader this plugin cannot be.\\\"\\\"\\\"\\n\\ndef require_compatible(bundle_dir: Path, plugin: str) -> None:\\n    \\\"\\\"\\\"Refuse to write into a bundle this plugin is too old to read.\\n\\n    Reads <bundle_dir>/bundle.json. Raises BundleIncompatible when\\n    min_reader_schema > SCHEMA_VERSION, or when bundle_format >\\n    CURRENT_BUNDLE_FORMAT. Returns None and is silent otherwise.\\n    A missing bundle.json is a new bundle and is compatible.\\n    \\\"\\\"\\\"\\n\\ndef stamp_generator(bundle_dir: Path, plugin: str, version: str) -> None:\\n    \\\"\\\"\\\"Record this plugin's version in bundle.json's generators map.\\\"\\\"\\\"\\n```\\n\\nEvery script that writes into a bundle calls `require_compatible()` as its\\nfirst statement after argument parsing. Today three scripts stamp\\n`SCHEMA_VERSION` with no check at all: `extract_story.py:599`,\\n`export_artifact.py:428`, and `_manifest.py:76`.\\n\\n### `bundle.json`, at format 3\\n\\n```json\\n{\\n  \\\"bundle_format\\\": 3,\\n  \\\"schema_version\\\": \\\"1.3\\\",\\n  \\\"min_reader_schema\\\": \\\"1.3\\\",\\n  \\\"generators\\\": {\\n    \\\"cobuilder-pr\\\": \\\"0.5.0\\\",\\n    \\\"cobuilder-artifact\\\": \\\"0.5.0\\\"\\n  },\\n  \\\"migrated_at\\\": \\\"2026-08-21T09:00:00Z\\\"\\n}\\n```\\n\\n`generator_version`, a scalar, becomes `generators`, a map keyed by plugin.\\n`min_reader_schema` is the floor a reader must meet and is raised only by a\\nmigration that makes a bundle unreadable to an older plugin. A migration that\\nonly adds an optional field leaves it alone.\\n\\n### `scripts/build_index.py`\\n\\n```python\\ndef build_index(repo: Path, bundle_dir: Path) -> dict:\\n    \\\"\\\"\\\"Full rebuild. Reads docs/ plus git. Never merges, never authors.\\\"\\\"\\\"\\n\\ndef collect_adrs(repo: Path)      -> list[dict]\\ndef collect_designs(repo: Path)   -> list[dict]\\ndef collect_contexts(repo: Path)  -> list[dict]   # includes boundary rules\\ndef collect_districts(bundle: Path) -> list[dict] # from inventory.yaml\\ndef collect_plans(repo: Path)     -> list[dict]   # gates and slices\\ndef collect_pull_requests(repo: Path) -> list[dict]  # git plus gh\\ndef resolve_joins(entities: dict) -> dict\\ndef compute_sources(repo: Path)   -> dict         # freshness\\n\\ndef is_stale(index: dict, repo: Path) -> bool:\\n    \\\"\\\"\\\"True when any tracked subtree hash or the git head has moved.\\\"\\\"\\\"\\n```\\n\\n`index.json`:\\n\\n```json\\n{\\n  \\\"schema_version\\\": \\\"1.3\\\",\\n  \\\"sources\\\": {\\n    \\\"git_head\\\": \\\"642ecb3\\\",\\n    \\\"trees\\\": {\\\"docs/architecture/adr\\\": \\\"sha256:…\\\",\\n              \\\"docs/architecture/designs\\\": \\\"sha256:…\\\",\\n              \\\"docs/architecture/contexts\\\": \\\"sha256:…\\\",\\n              \\\"docs/plans\\\": \\\"sha256:…\\\"}\\n  },\\n  \\\"entities\\\": {\\n    \\\"adr\\\": [...], \\\"design\\\": [...], \\\"epic\\\": [...],\\n    \\\"context\\\": [...], \\\"district\\\": [...], \\\"boundary_rule\\\": [...],\\n    \\\"pull_request\\\": [...], \\\"slice\\\": [...], \\\"publication\\\": [...]\\n  },\\n  \\\"joins\\\": {\\n    \\\"adr_to_context\\\":  {\\\"ADR-0016\\\": \\\"cobuilder-packaging\\\"},\\n    \\\"adr_to_district\\\": {\\\"ADR-0007\\\": \\\"viewer\\\"},\\n    \\\"context_verifies_district\\\": {\\\"cobuilder-packaging\\\": [\\\"packaging\\\"]},\\n    \\\"adr_to_pull_request\\\": {\\\"ADR-0016\\\": {\\\"pr\\\": 11, \\\"via\\\": \\\"epic\\\",\\n                                         \\\"path\\\": [\\\"plugin-split/E1\\\"]}},\\n    \\\"slice_to_epic\\\": {\\\"cobuilder-family/3\\\": \\\"plugin-split/E1\\\"},\\n    \\\"epic_to_pull_request\\\": {\\\"plugin-split/E1\\\": 11},\\n    \\\"district_uncovered\\\": [\\\"narrative\\\", \\\"audio\\\"]\\n  }\\n}\\n```\\n\\n`joins` is materialised rather than computed in the viewer. A join resolved\\nonce in Python is a join the viewer cannot get wrong, and `district_uncovered`\\nis the describe backlog ADR-0018 names.\\n\\n### An ADR is not an epic\\n\\nThey are different things and neither contains the other.\\n\\nAn **ADR** is a decision. It outlives the work that carried it out, it can be\\nchallenged years later, and its state machine describes the life of the\\ndecision. An **epic** is a unit of work with a branch and an end.\\n\\nThe relation is many-to-many. One epic can carry several decisions: E1 lands\\nthe renames and every ADR the renames settle. One decision can take several\\nepics: ADR-0016 needs E1, E1b, E2, and E3 before the code matches it. That is\\nexactly why the code-matches-the-decision axis is derived rather than stored\\non the ADR.\\n\\n### How `adr_to_pull_request` is derived\\n\\nTwo paths, tried in order. Nothing is authored.\\n\\n1. **Direct.** The ADR's frontmatter carries `source_pr`. Retro-extraction\\n   sets it, because the record was written from a merged pull request. Done.\\n2. **Through the design and the epic.** Otherwise:\\n\\n```\\nADR-NNNN\\n  → design            the design whose adr list names it, or whose\\n                      directory the ADR's `related` paths point into\\n  → epic              goal.json.epics[] entries belonging to that design\\n  → branch            epics[].branch\\n  → pull_request      the PR whose head branch equals that branch,\\n                      from `gh pr list --head <branch> --state all`\\n```\\n\\n`via` records which path resolved, and `path` records the epics it went\\nthrough. A reader can then see *why* an ADR is attached to a pull request,\\nwhich matters when the answer is wrong.\\n\\n**The gap this exposes: a slice names no epic today.** `04-slices.md` lists\\nslices in build order and nothing ties a slice to the epic it advances. The\\njoin therefore stops at the epic and cannot reach a slice. Gate 4 fixes this\\nat the source: **every slice line declares `epic: <design>/<epic-id>`.** The\\nalternative is to guess from the files a slice touches, which is a heuristic\\nin a chain built to avoid them.\\n\\n**What stays underivable.** An epic with no branch has no pull request, and\\nthat is correct rather than missing — it is work that has not started. The\\nindex reports it as unstarted and does not search for a PR.\\n\\n### `scripts/serve_bundle.py`\\n\\n```python\\ndef serve(bundle_dir: Path, port: int, allow_write: bool) -> None:\\n    \\\"\\\"\\\"Static files, plus POST /feedback when allow_write is true.\\n\\n    Binds 127.0.0.1 only. Never binds a routable address.\\n    \\\"\\\"\\\"\\n\\ndef append_thread(bundle_dir: Path, record: dict) -> str:\\n    \\\"\\\"\\\"Validate, assign a ulid, append one line, return the id.\\n    Opens with 'a' and one write call, so a concurrent append cannot\\n    interleave. Never rewrites and never truncates.\\n    \\\"\\\"\\\"\\n```\\n\\nAccepted routes: `GET` for any file under the bundle root, and\\n`POST /feedback`. Nothing else. A `DELETE` is not implemented, because a read\\nnever deletes and neither does anything else.\\n\\n### `scripts/watch_feedback.py`\\n\\n```python\\ndef watch(bundle_dir: Path, since: int, timeout: int = 900) -> list[dict]:\\n    \\\"\\\"\\\"Block until threads.ndjson grows past byte offset `since`, or the\\n    timeout expires. Print the new lines as JSON and exit 0. Exit 2 on\\n    timeout with no new lines, so a caller can tell the two apart.\\n    \\\"\\\"\\\"\\n```\\n\\nPolls the file size once a second. It does not watch the filesystem, because\\na watcher is a dependency and this is a local file the same machine writes.\\n\\n### The comment record\\n\\nExactly as ADR-0019 specifies, including the agent reply that carries\\n`\\\"author\\\": \\\"agent\\\"` and `\\\"replies_to\\\": \\\"<thread id>\\\"`, and the state change\\nthat carries only `id`, `state`, and `ts`. **The ledger is append-only, so a\\nstate is the last line that names it, not a field edited in place.**\\n\\n```python\\ndef thread_view(bundle_dir: Path, thread_id: str) -> dict:\\n    \\\"\\\"\\\"Fold every line naming this id into one thread: the root comment,\\n    its replies in append order, and the transition history.\\\"\\\"\\\"\\n\\ndef project_threads(bundle_dir: Path) -> dict:\\n    \\\"\\\"\\\"Fold the whole ledger once into feedback/threads-state.json:\\n    {thread_id: {state, updated_by, updated_at, reply_count, offset}}.\\n    Rewritten in full after each append. Never authored.\\n    \\\"\\\"\\\"\\n```\\n\\n**The current state is a field, and the log is still append-only.** The\\nearlier draft had every reader fold the file to learn a state, and that was\\nthe wrong trade. `threads-state.json` is a projection rebuilt from the\\nledger, so a reader gets the state in one lookup and keeps the audit trail\\nthat says who moved it and when. The projection is derived and disposable —\\ndelete it and the next append rebuilds it from the ledger.\\n\\nThe rule the projection must not break: **the ledger is the truth and the\\nfield is the cache.** A writer appends to the ledger first and projects\\nsecond. A projection that disagrees with the ledger is a bug in the\\nprojection, and `project_threads()` is cheap enough to rebuild whenever the\\nanswer is in doubt. This is the same shape as `index.json` — derived, full\\nrebuild, never merged — so it needs no new idea, only the same discipline.\\n\\nE6 ships `thread_view()`, `project_threads()`, and the reply record. It does\\nnot ship the conversation interface — the drawer lists replies flat.\\nRendering a thread as a conversation is a later slice, and the record shape\\nexists now so that slice adds no migration.\\n\\n## Call stack\\n\\n### Publishing a design, end to end\\n\\n```\\n/cobuilder-architect:design\\n  → migrate_bundle.py --bundle-dir …            (shared, unconditional)\\n  → require_compatible(bundle, \\\"cobuilder-architect\\\")\\n  → interview, explore, challenge                (Claude judgment)\\n  → write docs/architecture/designs/<name>/*.json\\n  → write docs/architecture/adr/ADR-NNNN-*.md\\n  → when no context covers the region:\\n        describe procedure, in-process\\n        → write docs/architecture/contexts/<id>/{canvas.md,boundary.yaml}\\n        → boundary.verifies = [districts covered]\\n  → validate_decision_state.py\\n  → hand off: name cobuilder-implement, stop\\n```\\n\\n### Building the index and reading it\\n\\n```\\nany cobuilder-artifact mode\\n  → migrate_bundle.py\\n  → is_stale(index, repo)?\\n        yes → build_index.py\\n                collect_* ×6  →  resolve_joins  →  compute_sources\\n                write data/index.json and data/index.js\\n  → serve_bundle.py --allow-write\\n        viewer/index.html\\n          <script src=\\\"../data/index.js\\\">  →  window.INDEX\\n          renderMode(mode)  →  Designs | Pull requests | Decisions\\n                               | Contexts | Builds\\n```\\n\\n### A comment, end to end\\n\\n```\\nreader clicks any element in the viewer\\n  → onSelect(event)\\n      computeSelector(el)     walk ≤5 ancestors, short-circuit on an id\\n      captureRange(selection) node path plus character offset per end\\n      openDrawer(anchor)      shifts the page, does not cover it\\n  → reader types, submits\\n      POST /feedback  →  append_thread()  →  ulid returned\\n      fallback when the server is read-only: localStorage, then the\\n      existing \\\"Copy review as markdown\\\" hand-off\\n  → agent side\\n      watch_feedback.py --since <offset>\\n        blocks → prints new lines → exits 0\\n        harness re-invokes the session with the threads in hand\\n      agent acts, appends a reply line, appends a state line\\n```\\n\\n## Test plan\\n\\n**This repository has no test suite today.** Gate 3 introduces one, because\\nthe red-green-validate loop of `cobuilder-implement` cannot run without it.\\n`tests/` at the marketplace root, run with\\n`uv run --with pytest pytest tests/`. No CI. No package manager. The\\nconstraint that this repo stays prose plus PEP-723 scripts holds, and pytest\\narrives through `--with` rather than through a lockfile.\\n\\n### Compatibility gate\\n\\n- `test_require_compatible_passes_on_equal_schema`\\n- `test_require_compatible_raises_when_bundle_demands_newer_reader` —\\n  `min_reader_schema` `1.4` against a `1.3` plugin raises `BundleIncompatible`\\n- `test_require_compatible_raises_on_newer_bundle_format`\\n- `test_require_compatible_allows_missing_bundle_json` — a new bundle\\n- `test_write_scripts_call_require_compatible_first` — greps each writer for\\n  the call. Crude, and it is the test that actually prevents the regression.\\n- `test_stamp_generator_preserves_other_plugins` — writing as\\n  `cobuilder-pr` must not drop `cobuilder-artifact`'s entry\\n\\n### Migration to format 3\\n\\n- `test_scalar_generator_version_becomes_a_map`\\n- `test_migration_refuses_when_an_authored_field_changes_outside_touches`\\n- `test_migration_writes_a_backup_before_it_writes_the_new_story`\\n- `test_viewer_refreshes_unconditionally` — no version gate, the bug that\\n  motivated the mechanism\\n\\n### The index\\n\\n- `test_index_resolves_an_adr_to_its_context`\\n- `test_index_resolves_an_adr_to_a_context_through_a_district` — the join\\n  ADR-0018 adds\\n- `test_district_with_no_context_appears_in_district_uncovered`\\n- `test_epic_id_is_scoped_to_its_design` — two designs both carrying `E1`\\n- `test_is_stale_detects_a_changed_adr`\\n- `test_is_stale_detects_a_moved_git_head`\\n- `test_index_is_a_full_rebuild` — deleting an ADR removes it from the index\\n- `test_build_index_never_writes_into_docs`\\n\\n### The ledger\\n\\n- `test_append_thread_assigns_a_ulid_and_returns_it`\\n- `test_append_thread_never_rewrites_an_existing_line`\\n- `test_two_concurrent_appends_both_survive`\\n- `test_state_change_is_a_new_line_not_an_edit`\\n- `test_read_does_not_delete` — the point of departure from the lavish CLI\\n- `test_thread_view_folds_a_reply_under_its_root`\\n- `test_an_agent_reply_carries_author_agent_and_replies_to`\\n- `test_projection_state_equals_the_last_transition_in_the_ledger`\\n- `test_deleting_the_projection_rebuilds_it_from_the_ledger`\\n- `test_projection_records_who_moved_the_state_and_when`\\n- `test_watch_exits_2_on_timeout_with_no_new_lines`\\n- `test_serve_rejects_a_post_to_any_path_other_than_feedback`\\n- `test_serve_binds_loopback_only`\\n\\n### The split itself\\n\\n- `test_no_plugin_path_reference_crosses_a_pillar` — greps every plugin for\\n  `${CLAUDE_PLUGIN_ROOT}/skills/<other pillar>`. This is the E1b regression\\n  guard, and it is the single most valuable test in the list.\\n- `test_every_plugin_manifest_validates` — `claude plugin validate` on each\\n- `test_no_page_is_written_into_dot_lavish`\\n- `test_slice_declares_an_epic` — every line in `04-slices.md` names one\\n- `test_adr_reaches_a_pull_request_through_its_epic` — the two-path derivation\\n- `test_an_epic_with_no_branch_is_reported_unstarted_not_missing`\\n- `test_shared_symlink_dereferences_to_a_real_directory` — E2's proof,\\n  written as a test rather than as a one-off check\\n\\n## Found while writing this document\\n\\n**`explore-design` and `design` are the same command, and the command goes.**\\n`commands/explore-design.md:27` dispatches `Skill(\\\"architecture\\\", args=\\\"design\\n$ARGUMENTS\\\")`, which is character-for-character what `commands/design.md`\\ndispatches. Divergent exploration is stage 3 of Design mode and runs either\\nway, so the second command adds a name and no behaviour. **Decided: delete\\nthe command. Divergent exploration stays an agent-invoked step inside Design\\nmode and is not a user-facing entry point.** `cobuilder-architect` ships six\\nmodes. E1 carries the deletion.\\n\\n**The backlog already exists and nobody was reading it.** Deferred work has a\\nhome, and it is `goal.json.epics[]`. Each entry carries an `id`, a `branch`,\\na `pr`, and a `state`, and an epic with a null branch is work that is planned\\nand not started. That is a backlog item. Nothing new is needed to hold one.\\n\\nSo the three levels are:\\n\\n| Level | Where it lives | Lifetime |\\n|---|---|---|\\n| **Epic** | `goal.json.epics[]` in a design | survives the session, survives the branch |\\n| **Slice** | `04-slices.md` for a feature, one line each in build order | survives the session, dies with the feature |\\n| **Task** | inside a slice, in the session doing it | dies with the session, deliberately |\\n\\nA task below slice level is not durable and should not be. A slice is small\\nenough that its tasks are the work of one sitting, and writing them down\\ncosts more than redoing them.\\n\\n**The Builds view gains a Backlog lane**, and it is a query rather than a\\nfile: every epic with a null branch, plus every slice not yet scored, ranked\\nby the count of ADRs that depend on the epic. Nothing authors it, and nothing\\ncan go stale against a list it computes.\\n\\n`plugin-split/goal.json` is now the real list. It held five epics from the\\nfirst draft while `02-architecture.md` had grown to eight. It carries all\\nnine, including **E8, rendering a thread as a conversation** — which is the\\nanswer to where that deferred work went.\\n\\n**Rendering a thread as a conversation needs no ADR.** ADR-0019 already\\ndecides the reply channel, the record shape, and that `replies_to` exists.\\nRendering those replies as a thread is deferred work under that decision, not\\na competing option, and a record with no rejected alternative is a task in\\nADR clothing. One thing would earn its own record: if the viewer's threads\\nand the Artifact platform's threads have to merge into one inbox, that is a\\nreal decision with real alternatives. It is not in scope for E6.\\n\\n## Least confident decisions\\n\\n1. **`serve_bundle.py` replaces `python3 -m http.server` outright.** A\\n   smaller change keeps the stock server for reading and adds the write\\n   endpoint only under a flag. That leaves two servers to keep in step. One\\n   server with `--allow-write` off by default is simpler and it is still a\\n   real application where there used to be none.\\n\\n2. **Materialised `joins` in `index.json`.** It duplicates what the entities\\n   already state, and it goes stale in exactly the same rebuild the entities\\n   do, so the duplication costs size rather than correctness. The argument\\n   against is that a viewer bug and an index bug now look identical.\\n\\n3. **pytest through `uv run --with`.** It keeps the no-package-manager\\n   constraint and it re-resolves pytest on every run. A slow test command is\\n   a test command people skip.\\n\\n4. **A projection file beside the ledger.** It is a second file that can\\n   disagree with the first. The alternative is a fold on every read, which\\n   was the earlier draft and which made the common operation the expensive\\n   one. Two files with a stated direction of truth is the better trade, and\\n   it is the trade `index.json` already makes.\\n\\n5. **`shared/` as a sibling of `plugins/` rather than duplicated content in\\n   git.** Everything rests on the symlink dereference. If E2 finds it does\\n   not work for a directory of Python, the fallback is a copy step before\\n   publish, and that fallback is a build step in a repo that has none.\\n\",\n+        \"approved_date\": null,\n+        \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/03-program-design.md\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing\",\n+        \"feature_slug\": \"gate-doc-surfacing\",\n+        \"gate\": 3,\n+        \"title\": \"Program Design: Gate doc surfacing\",\n+        \"body_md\": \"# Program Design: Gate doc surfacing\\n\\n## Files\\n- `shared/build_index.py` — modified. New constants `PROGRAM_DESIGN_FIELDS`,\\n  `EPIC_DESIGN_FIELDS`; new functions `discover_plan_gate_docs()`,\\n  `project_program_design()`, `project_epic_design()`; `collect_designs()`\\n  or a new `collect_gate_docs()` wires the new entities into the index;\\n  `resolve_feature_gates()` attaches `doc` per gate.\\n- `plugins/artifact/viewer/index.html` — modified. New functions\\n  `openGateDocSheet(kind, id)`, `closeGateDocSheet()`,\\n  `renderGateDocSheet()`. New markup block for the sheet (siblings of\\n  `#adr-sheet`). `renderBuildsMainContent()`'s `gate-rail-grid` and\\n  `renderEpicCard()` gain click wiring.\\n- `tests/test_build_index_gate_docs.py` — new. Covers the projection\\n  functions and `resolve_feature_gates()`'s new `doc` field.\\n\\n## Types & signatures\\n\\n```python\\n# shared/build_index.py\\nPROGRAM_DESIGN_FIELDS: tuple[str, ...]\\nEPIC_DESIGN_FIELDS: tuple[str, ...]\\n\\ndef discover_plan_gate_docs(repo: Path) -> list[dict]:\\n    \\\"\\\"\\\"One dict per docs/plans/<slug>/{03-program-design.md, epic-*-design.md}\\n    found, with slug, kind ('program'|'epic'), epic_id (if kind == 'epic'),\\n    path, and raw markdown text.\\\"\\\"\\\"\\n\\ndef project_program_design(feature_slug: str, path: Path, text: str) -> dict:\\n    \\\"\\\"\\\"-> {\\\"id\\\": feature_slug, \\\"feature_slug\\\", \\\"gate\\\": 3, \\\"title\\\",\\n    \\\"body_md\\\", \\\"approved_date\\\", \\\"source_path\\\"}, fields filtered through\\n    project_fields(..., PROGRAM_DESIGN_FIELDS).\\\"\\\"\\\"\\n\\ndef project_epic_design(epic_id: str, feature_slug: str, path: Path, text: str) -> dict:\\n    \\\"\\\"\\\"-> {\\\"id\\\": epic_id, \\\"epic_id\\\", \\\"feature_slug\\\", \\\"title\\\", \\\"body_md\\\",\\n    \\\"approved_date\\\", \\\"source_path\\\"}, filtered through\\n    project_fields(..., EPIC_DESIGN_FIELDS).\\\"\\\"\\\"\\n```\\n\\n```javascript\\n// plugins/artifact/viewer/index.html\\nfunction openGateDocSheet(kind, id) { /* kind: 'program' | 'epic' */ }\\nfunction closeGateDocSheet() { }\\nfunction renderGateDocSheet() { /* reads window.INDEX.entities.program_design\\n  or .epic_design by id, renders via renderMarkdown() into the sheet body */ }\\n```\\n\\n## Call stack\\n\\nIndex build (top to bottom):\\n```\\nmain()\\n  build_index(repo)\\n    collect_designs(repo)          # unchanged\\n    discover_plan_gate_docs(repo)  # new\\n      project_program_design()     # new, one call per feature with a Gate 3 doc\\n      project_epic_design()        # new, one call per epic-*-design.md found\\n    resolve_joins(repo)\\n      resolve_feature_gates(repo)  # extended: attaches doc id where found\\n```\\n\\nViewer click (top to bottom):\\n```\\ngate-card click / epic design-doc chip click\\n  openGateDocSheet(kind, id)\\n    state.gateDocOpen = true; state.activeGateDocKind = kind; state.activeGateDocId = id\\n    renderGateDocSheet()\\n      lookup window.INDEX.entities.program_design[id] or .epic_design[id]\\n      renderMarkdown(record.body_md)   # reused, unchanged\\n    renderSheetVisibility()            # existing mutual-exclusivity function, extended\\n```\\n\\n## Test plan\\n- `test_build_index_gate_docs.py::test_project_program_design_reads_title_and_body`\\n- `test_build_index_gate_docs.py::test_project_epic_design_parses_epic_id_from_filename`\\n- `test_build_index_gate_docs.py::test_discover_plan_gate_docs_skips_plan_dir_with_no_gate_docs`\\n- `test_build_index_gate_docs.py::test_resolve_feature_gates_attaches_doc_id_when_program_design_exists`\\n- `test_build_index_gate_docs.py::test_resolve_feature_gates_gate3_doc_absent_when_no_program_design_md`\\n- `test_build_index_gate_docs.py::test_build_index_end_to_end_index_json_carries_new_entity_keys`\\n\\nManual dogfood check (no automated frontend test harness in this repo):\\nopen the rebuilt viewer, navigate to Builds, click this feature's own Gate\\n3 card once `03-program-design.md` exists (after this gate is approved),\\nconfirm the sheet shows this document's content.\\n\\n## Least confident decisions\\n- Whether `collect_designs()` should absorb the new entities directly, or\\n  a separate `collect_gate_docs()` function is cleaner — deferred to\\n  implementation; either satisfies the same `data/index.json` shape.\\n- Whether the sheet reuses the *same* DOM nodes as `#adr-sheet` with a mode\\n  flag, or gets its own sibling `#gate-doc-sheet` block — leaning toward a\\n  sibling block, since `renderAdrSheet` and a future `renderGateDocSheet`\\n  read different record shapes and a shared node risks stale content on a\\n  fast open/open across the two kinds.\\n\",\n+        \"approved_date\": null,\n+        \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/gate-doc-surfacing/03-program-design.md\"\n+      }\n+    ],\n+    \"epic_design\": [\n+      {\n+        \"id\": \"cobuilder-family/E1\",\n+        \"epic_id\": \"E1\",\n+        \"feature_slug\": \"cobuilder-family\",\n+        \"title\": \"Epic Technical Solution Design: E1 — One plugin becomes five\",\n+        \"body_md\": \"# Epic Technical Solution Design: E1 — One plugin becomes five\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E1\\n\\nThis design was written on 2026-08-25, after this epic's four slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE1 turns one plugin into five sibling plugins under `plugins/`, each with\\nits own `.claude-plugin/plugin.json`, `commands/`, `skills/`, and `scripts/`.\\nIt renames three modes inside the single plugin first, deletes the\\nduplicate `explore-design` command, fixes 33 cross-pillar references, then\\nsplits the tree into five manifests, and ports two skills in from outside\\nthe repository. ADR-0016 decided the five-way split. ADR-0017 decided the\\nvendoring mechanism E1 relies on but does not itself implement (E2 proves\\nit, E3 gates it).\\n\\n## Files Touched\\n\\nThe whole split landed in commit `4156641` (\\\"Slice 10 change\\\"), together\\nwith parts of E3, E4, and half of E5. `git show --stat 4156641` lists the\\nfull file move. The load-bearing paths today are:\\n\\n- `.claude-plugin/marketplace.json` — one file, five plugin entries.\\n- `plugins/cobuilder-architect/.claude-plugin/plugin.json`,\\n  `plugins/cobuilder-pr/.claude-plugin/plugin.json`,\\n  `plugins/cobuilder-implement/.claude-plugin/plugin.json`,\\n  `plugins/cobuilder-artifact/.claude-plugin/plugin.json`,\\n  `plugins/cobuilder-full-lifecycle/.claude-plugin/plugin.json` — one\\n  manifest per plugin.\\n- `plugins/cobuilder-architect/commands/{design,review,maintenance,\\n  decisions,describe,debug}.md` — six modes, `explore-design.md` deleted.\\n- `plugins/cobuilder-architect/skills/architecture/` — moved from the\\n  repository root, unchanged in content apart from path updates.\\n- `plugins/cobuilder-pr/skills/odyssey/` — the renamed modes: the old\\n  `submit` became `generate`, and the old `generate` became `review`.\\n- `plugins/cobuilder-artifact/scripts/{serve_bundle.py,export_artifact.py,\\n  export_index.py,record_publish.py}` and\\n  `plugins/cobuilder-artifact/viewer/index.html`.\\n- `plugins/cobuilder-full-lifecycle/skills/cobuilder-full/SKILL.md` and\\n  `plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py`.\\n- Each plugin carries a `shared` symlink at its own root, verified by\\n  `test_shared_is_a_symlink_in_the_source_tree` in\\n  `tests/test_plugin_manifests.py`.\\n\\n## Types & Signatures\\n\\nE1 carries no new Python functions of its own. Its unit of work is the\\nmanifest and the command dispatch line, not a function signature. The\\ncontract each command file holds is a one-line `Skill(...)` dispatch, for\\nexample `plugins/cobuilder-architect/commands/design.md` dispatching\\n`Skill(\\\"architecture\\\", args=\\\"design $ARGUMENTS\\\")`. `tests/test_commands.py`\\nasserts this shape directly:\\n\\n```python\\ndef test_command_dispatches_to_a_declared_mode(command_path): ...\\ndef test_explore_design_command_does_not_exist(): ...\\ndef test_no_two_commands_in_the_same_plugin_dispatch_identically(): ...\\ndef test_cobuilder_pr_ships_its_own_review_command(): ...\\ndef test_architecture_skill_declares_exactly_six_modes(): ...\\n```\\n\\n## Slice Decomposition\\n\\nPer `docs/plans/cobuilder-family/04-slices.md`, in build order:\\n\\n1. **Slice 4 — renames inside today's single plugin.** No dependency. Six\\n   architecture modes land, `explore-design` is deleted, `submit` becomes\\n   `generate`, and the old `generate` becomes `review`. Completed on attempt\\n   2, score 1.00.\\n2. **Slice 5 — the 33 cross-pillar references are fixed.** Depends on\\n   slice 4 naming the modes the references must point to. The regression\\n   test runs and passes while the repository is still one plugin, so it\\n   guards the split rather than reporting on it. Completed on attempt 3,\\n   score 1.00.\\n3. **Slice 6 — five plugins, five manifests.** Depends on slices 4 and 5.\\n   `claude plugin validate` passes on each, and the family installs.\\n   Completed, score 1.00.\\n4. **Slice 7 — the two ports land.** Depends on slice 6 existing so the\\n   ported skills have a plugin to land in. `cobuilder-implement` is renamed\\n   and shipped, `collaborate-with-user` folds into `cobuilder-artifact`,\\n   and the orientation skill is written. Completed, score 1.00.\\n\\n## Test Plan\\n\\n- `tests/test_plugin_manifests.py` — `test_manifest_parses_and_has_\\n  required_fields`, `test_no_agent_hook_or_mcp_server` (asserts none of the\\n  five plugins declares an agent, a hook, or an MCP server, per the\\n  installation rule in `CLAUDE.md`), `test_marketplace_lists_all_five_\\n  plugins`, `test_umbrella_plugin_depends_on_the_other_four`, and\\n  `test_shared_is_a_symlink_in_the_source_tree`.\\n- `tests/test_commands.py` — the eight assertions listed above, covering\\n  the rename, the deletion, and the one-dispatch-per-command rule.\\n- `tests/test_pillar_boundaries.py` — `test_no_cross_pillar_references_in_\\n  repo` is the regression test slice 5 exists to write. It greps every\\n  plugin for `${CLAUDE_PLUGIN_ROOT}/skills/<other pillar>` and fails on a\\n  hit. The file also carries thirteen evasion tests (`test_evasion_*`)\\n  covering relative paths, backslashes, mixed case, and files outside the\\n  original allowlist.\\n- `tests/test_script_references.py` — `test_runnable_commands_name_\\n  existing_scripts` checks that a script path named in prose actually\\n  exists on disk after the move.\\n\\n## Risks & Open Questions\\n\\n- **The split landed inside a much larger commit.** `4156641` also carries\\n  parts of E3, E4, and half of E5. There is no commit boundary that isolates\\n  E1 on its own, so `git show --stat` on that one commit is the closest\\n  thing to an E1-only diff, and it is not a clean one.\\n- **`plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py` exists.**\\n  `03-program-design.md`'s Files section proposed `scripts/watch_feedback.py`\\n  under `cobuilder-artifact`. The shipped location is under\\n  `cobuilder-full-lifecycle` instead. This is a placement drift from the\\n  program design, not a defect, and no ADR records the reason for the\\n  change.\\n- **No commit is titled after E1 or after any of its four slices by name.**\\n  The slice table's \\\"completed\\\" status and the rubric scores are the only\\n  record that ties specific work to specific slice numbers. A future\\n  session auditing this epic has to read `04-slices.md` and the rubric\\n  files, not `git log`, to find slice boundaries.\\n\",\n+        \"approved_date\": null,\n+        \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E1-design.md\"\n+      },\n+      {\n+        \"id\": \"cobuilder-family/E3\",\n+        \"epic_id\": \"E3\",\n+        \"feature_slug\": \"cobuilder-family\",\n+        \"title\": \"Epic Technical Solution Design: E3 — The seam is version-safe\",\n+        \"body_md\": \"# Epic Technical Solution Design: E3 — The seam is version-safe\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E3\\n\\nThis design was written on 2026-08-25, after this epic's two slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE3 makes the bundle safe to write from five independently versioned\\nplugins. It adds `require_compatible()` and `stamp_generator()`, raises\\n`bundle_format` to 3 and `schema_version` to 1.3, replaces the scalar\\n`generator_version` with a `generators` map keyed by plugin name, and gates\\nevery bundle-writing script behind the compatibility check. ADR-0017 governs\\nthis epic.\\n\\n## Files Touched\\n\\n- `shared/_bundle_meta.py` — `SCHEMA_VERSION = \\\"1.3\\\"`,\\n  `SCHEMA_VERSION_KNOWN = {\\\"1.0\\\", \\\"1.1\\\", \\\"1.2\\\", \\\"1.3\\\"}`,\\n  `CURRENT_BUNDLE_FORMAT = 3`, the `BundleIncompatible` exception, and both\\n  new functions.\\n- `shared/migrate_bundle.py` — the layout and schema ladders that carry a\\n  bundle from format 2 to format 3, and the scalar-to-map migration for\\n  `generators`.\\n- Every script that writes into a bundle now calls `require_compatible()`\\n  first: `shared/build_index.py`, `plugins/cobuilder-artifact/scripts/\\n  export_artifact.py`, `plugins/cobuilder-artifact/scripts/serve_bundle.py`\\n  (through `shared/ledger.py`), and `plugins/cobuilder-pr/scripts/\\n  extract_story.py`.\\n- `shared/ledger.py` — the append-only comment ledger this epic's gate\\n  gets wired in front of, in the sense that every write path funnels\\n  through `LedgerPaths` and the compatibility check runs before it.\\n\\n## Types & Signatures\\n\\nRead directly from `shared/_bundle_meta.py`:\\n\\n```python\\ndef read_plugin_version() -> str: ...\\ndef read_plugin_name() -> str: ...\\n\\nclass BundleIncompatible(RuntimeError): ...\\n\\ndef _version_tuple(version: str) -> tuple[int, ...]: ...\\n\\ndef require_compatible(bundle_dir: Path, plugin: str) -> None:\\n    \\\"\\\"\\\"Raise BundleIncompatible when the bundle's min_reader_schema exceeds\\n    this plugin's SCHEMA_VERSION, or when bundle_format exceeds\\n    CURRENT_BUNDLE_FORMAT. Silent otherwise. A missing bundle.json is a new\\n    bundle and passes.\\\"\\\"\\\"\\n\\ndef stamp_generator(bundle_dir: Path, plugin: str, version: str) -> None:\\n    \\\"\\\"\\\"Write this plugin's version into bundle.json's generators map,\\n    without touching another plugin's existing entry.\\\"\\\"\\\"\\n```\\n\\n`bundle.json` at format 3 carries `generators` as a map, for example\\n`{\\\"cobuilder-pr\\\": \\\"0.5.0\\\", \\\"cobuilder-artifact\\\": \\\"0.5.0\\\"}`, replacing the\\nsingle `generator_version` scalar the program design's Types and\\nsignatures section shows as the format-2 shape.\\n\\n## Slice Decomposition\\n\\nPer `04-slices.md`:\\n\\n1. **Slice 2 — the compatibility gate exists.** No dependency on E1 or E4.\\n   `require_compatible()` and `stamp_generator()` pass their own tests,\\n   called by nothing yet. Completed, score 1.00.\\n2. **Slice 3 — the gate is wired and the bundle migrates.** Depends on\\n   slice 2. Format 3, `min_reader_schema`, the `generators` map, and every\\n   writer gated. Completed, score 1.00.\\n\\nBoth slices land before E1's slice 6 splits the tree into five plugins, per\\n`04-slices.md`'s stated build order: \\\"Slices 2 through 5 stay inside\\ntoday's single plugin,\\\" so the compatibility gate is proven before the\\ndirectory move that would make debugging both changes at once harder.\\n\\n## Test Plan\\n\\n- `tests/test_bundle_meta.py` — eleven tests, including\\n  `test_newer_min_reader_schema_raises`, `test_equal_min_reader_schema_\\n  passes`, `test_version_comparison_is_numeric_not_string`,\\n  `test_version_comparison_older_dotted_passes`, `test_newer_bundle_format_\\n  raises`, `test_missing_bundle_json_passes_silently`, `test_stamp_\\n  generator_preserves_other_plugin`, `test_stamp_generator_overwrites_own_\\n  prior_entry`, `test_stamp_generator_keeps_old_scalar_field`, and\\n  `test_stamp_generator_creates_bundle_json_when_missing`.\\n- `tests/test_migrate_bundle.py` — `test_build_generators_map_preserves_\\n  old_scalar`, `test_build_generators_map_keeps_existing_map_entries`,\\n  `test_full_migration_turns_scalar_into_map`, `test_guard_rejects_\\n  undeclared_authored_field_change`, `test_data_ladder_stops_before_later_\\n  steps_on_violation`, `test_main_writes_nothing_on_guard_violation`,\\n  `test_backup_written_before_new_story`, `test_viewer_refreshes_even_at_\\n  current_version`, and `test_viewer_refresh_is_not_version_gated_in_\\n  source` — this last pair is the regression test for the exact bug\\n  `CLAUDE.md`'s Bundle versioning section names: a version gate on the\\n  viewer refresh that once let a stale viewer silently drop diagram\\n  support.\\n- `tests/test_build_index.py` — `test_calls_compatibility_gate_before_\\n  first_write` confirms `build_index.py` itself calls `require_compatible()`\\n  before it writes, closing the loop between E3's gate and E4's index.\\n\\n## Risks & Open Questions\\n\\n- **The program design lists three specific call sites as ungated before\\n  this epic** — `extract_story.py:599`, `export_artifact.py:428`, and\\n  `_manifest.py:76`. This design did not re-verify each of the three by\\n  line number against the tree as built. `test_write_scripts_call_require_\\n  compatible_first`, named in `03-program-design.md`'s test plan, is the\\n  test that would settle this, and its presence or absence under those\\n  exact names was not confirmed while writing this document.\\n- **`min_reader_schema` has never been raised in anger.** Every migration\\n  shipped so far adds an optional field, so the floor has stayed at 1.3\\n  since it was introduced. Whether a future migration correctly raises it,\\n  rather than leaving it stale the way an old ADR's `maps_to.modules` block\\n  went stale (see `docs/architecture/designs/design-mode/assessment.json`,\\n  finding F3), is untested because no such migration exists yet.\\n\",\n+        \"approved_date\": null,\n+        \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E3-design.md\"\n+      },\n+      {\n+        \"id\": \"cobuilder-family/E4\",\n+        \"epic_id\": \"E4\",\n+        \"feature_slug\": \"cobuilder-family\",\n+        \"title\": \"Epic Technical Solution Design: E4 — The record index\",\n+        \"body_md\": \"# Epic Technical Solution Design: E4 — The record index\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E4\\n\\nThis design was written on 2026-08-25, after this epic's two slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE4 replaces the two separate projection scripts (`build_adrs.py` and\\n`build_designs.py`) with one `shared/build_index.py` that emits every\\nentity — ADR, design, epic, context, district, boundary rule, pull request,\\nslice, publication — with a stable id, and resolves the joins between them,\\nincluding an ADR reaching its pull request through a design and an epic.\\nADR-0018 governs this epic.\\n\\n## Files Touched\\n\\n- `shared/build_index.py` — the whole collect-and-resolve pipeline, 1163\\n  lines. `build_adrs.py` and `build_designs.py` no longer exist anywhere in\\n  the tree.\\n- `docs/architecture/designs/plugin-split/goal.json` and\\n  `docs/plans/cobuilder-family/04-slices.md` — the source data `collect_\\n  designs()` and `collect_plans()` read.\\n- `.cobuilder-architect/self/data/index.json` and `index.js` — the\\n  generated output, checked in as the self-bundle's own projection.\\n\\n## Types & Signatures\\n\\nRead directly from `shared/build_index.py`. Selected signatures, in the\\norder the pipeline calls them:\\n\\n```python\\ndef collect_adrs(repo: Path) -> tuple[list[dict], dict[str, dict], list[str]]: ...\\ndef collect_designs(repo: Path) -> tuple[list[dict], ...]: ...\\ndef collect_contexts(repo: Path) -> tuple[list[dict], list[dict], list[str]]: ...\\ndef collect_districts(bundle_dir: Path) -> tuple[list[dict], list[str]]: ...\\ndef collect_plans(repo: Path) -> tuple[list[dict], list[str]]: ...\\ndef collect_pull_requests(bundle_dir: Path) -> tuple[list[dict], list[str]]: ...\\ndef collect_publications(bundle_dir: Path) -> tuple[list[dict], list[str]]: ...\\n\\ndef gh_pr_for_branch(branch: str, warnings: list[str], gh_state: dict) -> int | None: ...\\ndef resolve_epic_pull_requests(...) -> tuple[dict[str, int], dict[str, str]]: ...\\ndef refine_epic_status(epic_status: dict[str, str], epic_to_pr: dict[str, int],\\n                        pull_requests: list[dict]) -> None: ...\\ndef adrs_reaching_design(adr_id: str, adr_record: dict,\\n                          design_records: dict[str, dict]) -> list[str]: ...\\ndef resolve_adr_to_pull_request(...) -> ...\\ndef resolve_slice_to_epic(...) -> ...\\ndef resolve_context_district_joins(...) -> ...\\ndef resolve_joins(...) -> dict: ...\\n\\ndef hash_tree(path: Path) -> str: ...\\ndef git_head(repo: Path) -> str | None: ...\\ndef compute_sources(repo: Path) -> dict: ...\\ndef is_stale(index: dict, repo: Path) -> bool: ...\\ndef build_index(repo: Path, bundle_dir: Path) -> tuple[dict, dict, dict, list[str]]: ...\\n```\\n\\n`collect_pull_requests(bundle_dir: Path)` reads `data/story.json`:\\n\\n```python\\ndef collect_pull_requests(bundle_dir: Path) -> tuple[list[dict], list[str]]:\\n    story_path = bundle_dir / \\\"data\\\" / \\\"story.json\\\"\\n    ...\\n    timeline = raw.get(\\\"timeline\\\")\\n    ...\\n    for entry in timeline:\\n        if not isinstance(entry, dict) or entry.get(\\\"pr\\\") is None:\\n            continue\\n        entities.append({\\\"id\\\": entry[\\\"pr\\\"], \\\"title\\\": entry.get(\\\"title\\\"),\\n                          \\\"state\\\": entry.get(\\\"status\\\"), ...})\\n    return entities, []\\n```\\n\\n`refine_epic_status` then tries to overwrite the placeholder state:\\n\\n```python\\ndef refine_epic_status(epic_status: dict[str, str], epic_to_pr: dict[str, int],\\n                        pull_requests: list[dict]) -> None:\\n    \\\"\\\"\\\"Replace the placeholder 'open' status with the real pull request state.\\\"\\\"\\\"\\n    state_by_number = {pr[\\\"id\\\"]: pr.get(\\\"state\\\") for pr in pull_requests}\\n    for epic_id, pr_number in epic_to_pr.items():\\n        state = state_by_number.get(pr_number)\\n        if state:\\n            epic_status[epic_id] = str(state).lower()\\n```\\n\\n## Slice Decomposition\\n\\nPer `04-slices.md`:\\n\\n1. **Slice 8 — the index holds the entities.** No dependency beyond E3's\\n   compatibility gate. `build_index.py` emits every entity, and the two\\n   scripts it replaces are gone. Completed, score 1.00.\\n2. **Slice 9 — the index resolves the joins.** Depends on slice 8's entity\\n   set existing to join against. `adr_to_pull_request`, `slice_to_epic`,\\n   `district_uncovered`, and freshness (`is_stale`). Completed, score 1.00.\\n\\n## Test Plan\\n\\n`tests/test_build_index.py` carries twenty tests. The ones specific to\\neach slice:\\n\\n- Slice 8: `test_every_entity_type_appears_with_correct_count`,\\n  `test_deleting_a_source_document_removes_it_on_rebuild` (proves the index\\n  is a full rebuild, not a merge), `test_writes_nothing_into_docs`,\\n  `test_calls_compatibility_gate_before_first_write`, and\\n  `test_slice_8_entities_still_present_alongside_joins`.\\n- Slice 9: `test_adr_reaches_pull_request_direct_path`, `test_adr_reaches_\\n  pull_request_through_design_and_epic`, `test_adr_with_no_reachable_\\n  design_is_unresolved_not_guessed`, `test_epic_with_no_branch_is_\\n  unstarted`, `test_epic_with_branch_but_no_pull_request_is_reported_not_\\n  errored`, `test_district_and_context_resolve_both_ways`, `test_uncovered_\\n  district_is_listed`, `test_every_slice_declares_an_existing_epic`,\\n  `test_a_bare_epic_header_id_is_unresolved_not_guessed`, `test_an_epic_\\n  header_naming_an_undeclared_epic_is_unresolved`, `test_all_fourteen_\\n  real_slices_resolve_from_the_scoped_id`, `test_changing_an_authored_\\n  document_marks_the_index_stale`, `test_moving_the_git_head_marks_the_\\n  index_stale`, `test_epic_id_scoped_to_design_no_collision`, and\\n  `test_gate_source_scan`.\\n\\nNo test in the suite asserts the open-pull-request behavior described\\nbelow under Risks. `test_epic_with_branch_but_no_pull_request_is_reported_\\nnot_errored` covers an epic with no matching pull request at all, which is\\na different case from an epic whose pull request exists but is open, not\\nmerged.\\n\\n## Risks & Open Questions\\n\\n- **An open pull request does not resolve to a real state.**\\n  `collect_pull_requests()` reads only `data/story.json`, which holds\\n  narrated merged pull requests. An open pull request is never narrated\\n  into `story.json`, so it never becomes a `pull_request` entity.\\n  `resolve_epic_pull_requests()` still finds the PR number through `gh pr\\n  list --head <branch>`, and sets a hardcoded placeholder state of\\n  `\\\"open\\\"`. `refine_epic_status()` then tries to look that number up in\\n  `pull_requests`, finds nothing, and the placeholder survives unchanged.\\n  Verified by reading `collect_pull_requests` and `refine_epic_status`\\n  together in `shared/build_index.py`. This is the exact behavior the\\n  epic's own PR (pull request 11, still open at the time of writing) hits\\n  today: `resolve_epic_pull_requests` reports E1 through E6 as `\\\"open\\\"` by\\n  construction, not because the placeholder was refined against a real\\n  state.\\n- **Closing this gap needs a decision, not a fix.** Either `collect_pull_\\n  requests()` gains a second, `gh`-backed path for a pull request with no\\n  narration yet, or the placeholder stays and the index documents it as a\\n  known limit. This design does not resolve which.\\n- **`hash_tree()` and `is_stale()` were read but not exercised by hand**\\n  against a real edit during this write-up. The corresponding tests,\\n  `test_changing_an_authored_document_marks_the_index_stale` and `test_\\n  moving_the_git_head_marks_the_index_stale`, stand in for that\\n  verification.\\n\",\n+        \"approved_date\": null,\n+        \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E4-design.md\"\n+      },\n+      {\n+        \"id\": \"cobuilder-family/E5\",\n+        \"epic_id\": \"E5\",\n+        \"feature_slug\": \"cobuilder-family\",\n+        \"title\": \"Epic Technical Solution Design: E5 — One lifecycle surface\",\n+        \"body_md\": \"# Epic Technical Solution Design: E5 — One lifecycle surface\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E5\\n\\nThis design was written on 2026-08-25, after this epic's two slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE5 adds three viewer modes — Decisions, Contexts, and Builds — on top of\\nthe index E4 built, adds a computed Backlog lane inside the Builds mode,\\nand moves the six generated pages out of `.lavish/` into the bundle's own\\n`pages/` directory. ADR-0018 governs the record index and lifecycle surface\\nthis epic presents.\\n\\n## Files Touched\\n\\n- `plugins/cobuilder-artifact/viewer/index.html` — the five-mode top-left\\n  switcher (Designs, Pull requests, Decisions, Contexts, Builds), each\\n  mode's render function, and the Backlog lane computation.\\n- `scripts/build_builds_view.py` — reads the status document and the index\\n  to render the Builds page: `read_epics`, `read_rubrics`, `read_plan`,\\n  `current_doc`, `render`, and `main`.\\n- `.cobuilder-architect/self/pages/{builds-view.html,decisions-register.html,\\n  view-designs.html,artifact-transport-map.html,cobuilder-vocabulary.html,\\n  gate1-cobuilder-family.html}` — the six pages, now living in the bundle\\n  instead of `.lavish/`. `.lavish/` still holds its own copies as of this\\n  writing; `git status --short` does not list `.lavish/*.html` as deleted,\\n  so the move added a bundle copy without yet removing the `.lavish/`\\n  originals.\\n- `docs/architecture/contexts/cobuilder-packaging/{canvas.md,boundary.yaml}`\\n  — the one bounded context the Contexts mode renders today.\\n\\n## Types & Signatures\\n\\n`scripts/build_builds_view.py`:\\n\\n```python\\ndef read_epics(designs_dir: Path, slices_md: str) -> list[dict]: ...\\ndef read_rubrics(rubrics_dir: Path) -> tuple[dict, dict]: ...\\ndef read_plan(plan_dir: Path, designs_dir: Path, rubrics_dir: Path) -> dict: ...\\ndef current_doc(gates: list[dict]) -> tuple[str, str, bool]: ...\\ndef render(page: Path, plan_dir: Path, designs_dir: Path, rubrics_dir: Path) -> None: ...\\ndef main() -> None: ...\\n```\\n\\nThe viewer's Builds mode reads `window.INDEX.entities.epic` and\\n`window.INDEX.joins` directly, per the in-page label at line 3305 of\\n`plugins/cobuilder-artifact/viewer/index.html`: \\\"The Backlog lane is a\\ndynamic query over `window.INDEX.entities.epic` and `window.INDEX.joins`.\\nNo authored backlog file feeds it.\\\" The Backlog lane holds every epic with\\na null branch, plus every slice not yet scored, matching\\n`03-program-design.md`'s stated rule.\\n\\n## Slice Decomposition\\n\\nPer `04-slices.md`:\\n\\n1. **Slice 10 — the Decisions and Contexts modes.** Depends on E4's index\\n   existing to read from. A person browses every decision and every\\n   context, with the anchor distinction (a decision an ADR cites versus a\\n   decision it merely touches) visible. Completed, score 1.00.\\n2. **Slice 11 — the Builds mode and the Backlog lane.** Depends on slice\\n   10's mode-switcher scaffold and on E4's `epic_to_pull_request` and\\n   `slice_to_epic` joins. Gates driven by the status document, the Backlog\\n   lane computed from the index, and pages moved into the bundle.\\n   Completed, score 0.92 — the one slice in this feature that scored below\\n   1.00, though it is still recorded as completed rather than escalated.\\n\\n## Test Plan\\n\\n- `tests/test_viewer_modes.py` — `test_viewer_contains_all_five_mode_\\n  buttons`, `test_decisions_mode_lists_all_records_and_anchor_distinction`,\\n  `test_contexts_mode_leads_with_violations_and_uncovered_districts`,\\n  `test_contexts_mode_renders_boundary_record_as_readable_rules`, and\\n  `test_export_artifact_parses_updated_viewer` — this last test guards\\n  against the exact regression `CLAUDE.md`'s Publish mode notes describe:\\n  an artifact export that cannot parse a viewer carrying new mode markup.\\n- No test file name in `tests/` targets `scripts/build_builds_view.py` by\\n  name, and no `test_builds_*` or `test_backlog_*` function exists in the\\n  suite as collected. The Builds mode and the Backlog lane are exercised\\n  indirectly, through `test_viewer_contains_all_five_mode_buttons` and the\\n  index tests in `tests/test_build_index.py` that the lane's query depends\\n  on, but neither a dedicated unit test nor an assertion on `read_plan`'s\\n  or `current_doc`'s return shape was found.\\n\\n## Risks & Open Questions\\n\\n- **`scripts/build_builds_view.py` carries no direct test coverage.**\\n  This is a genuine gap, not a design choice recorded anywhere. A change to\\n  `current_doc`'s gate-ordering logic or to `read_rubrics`'s score parsing\\n  could regress silently until a person looks at the rendered page.\\n- **`.lavish/` still holds the six pages the move was meant to retire.**\\n  `git status --short` shows no deletion for any `.lavish/*.html` file, so\\n  either the removal step of slice 11 has not run, or it ran in a commit\\n  this design could not locate. The bundle's `pages/` directory and\\n  `.lavish/` currently disagree about which copy is the source of record.\\n- **The 0.92 score on slice 11 has no recorded reason in `00-status.md`'s\\n  Escalated section**, because the slice was accepted rather than\\n  escalated. What specifically fell short of 1.00 was not found in any\\n  file this design reviewed.\\n\",\n+        \"approved_date\": null,\n+        \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E5-design.md\"\n+      },\n+      {\n+        \"id\": \"cobuilder-family/E6\",\n+        \"epic_id\": \"E6\",\n+        \"feature_slug\": \"cobuilder-family\",\n+        \"title\": \"Epic Technical Solution Design: E6 — The reply channel\",\n+        \"body_md\": \"# Epic Technical Solution Design: E6 — The reply channel\\n\\nFeature: cobuilder-family\\nEpic ID: plugin-split/E6\\n\\nThis design was written on 2026-08-25, after this epic's three slices were\\nbuilt and accepted. Gate 4b did not run before implementation, so this\\ndocument records the design as built. It did not constrain the work. The\\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\\nderived without a written 4b design in hand.\\n\\n## Scope and Intent\\n\\nE6 adds an append-only comment ledger and its projection, an anchor\\ncomputed from the live DOM so a reader can click any element and attach a\\ncomment to it, a `POST /feedback` write endpoint, and a background wake\\ncommand an agent polls for new threads. ADR-0019 governs the ledger's\\nrecord shape.\\n\\n## Files Touched\\n\\n- `shared/ledger.py` — the whole ledger: append, fold, project, and the\\n  blocking watch function. 260-plus lines.\\n- `plugins/cobuilder-artifact/scripts/serve_bundle.py` — extends\\n  `http.server.SimpleHTTPRequestHandler` with a `POST /feedback` route,\\n  binds `127.0.0.1` only, and returns 403 when `--allow-write` is off.\\n- `plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py` — the\\n  background wake command. This is a placement difference from\\n  `03-program-design.md`, which names `scripts/watch_feedback.py` under\\n  `cobuilder-artifact`. The shipped file lives under\\n  `cobuilder-full-lifecycle` instead.\\n- `plugins/cobuilder-artifact/viewer/index.html` — `computeSelector()`,\\n  `captureRange()`, and `openDrawer()`, the client side of the anchor and\\n  comment flow the program design's call stack section names.\\n\\n## Types & Signatures\\n\\nRead directly from `shared/ledger.py`:\\n\\n```python\\nclass LedgerPaths: ...\\n\\ndef append_line(path: Path, obj: dict[str, Any]) -> None: ...\\ndef read_ledger(path: Path) -> list[dict[str, Any]]: ...\\ndef fold_threads(lines: list[dict[str, Any]]) -> dict[str, dict[str, Any]]: ...\\ndef project_threads(lines: list[dict[str, Any]]) -> dict[str, dict[str, Any]]: ...\\ndef write_projection(path: Path, projection: dict[str, dict[str, Any]]) -> None: ...\\ndef load_projection(path: Path) -> dict[str, dict[str, Any]]: ...\\n\\ndef append_comment(paths, anchor: str, text: str, author: str = \\\"human\\\") -> str: ...\\ndef append_reply(paths, thread_ulid: str, text: str, author: str = \\\"human\\\",\\n                  replies_to: str | None = None) -> str: ...\\ndef append_state(paths, thread_ulid: str, state: str) -> None: ...\\n\\ndef get_thread(paths: LedgerPaths, thread_ulid: str) -> dict[str, Any] | None: ...\\ndef get_all_threads(paths: LedgerPaths) -> dict[str, dict[str, Any]]: ...\\ndef rebuild_projection(paths: LedgerPaths) -> dict[str, dict[str, Any]]: ...\\n\\ndef watch_feedback(ledger_path: Path, since_offset: int = 0,\\n                    timeout_seconds: float = 30.0) -> tuple[list[dict[str, Any]], int]:\\n    \\\"\\\"\\\"Block until new lines appear past since_offset. Returns\\n    (new_lines, new_offset). Times out with ([], since_offset) after\\n    timeout_seconds. The caller (watch_feedback.py) exits 2 on an empty\\n    result and 0 otherwise.\\\"\\\"\\\"\\n```\\n\\n`plugins/cobuilder-artifact/scripts/serve_bundle.py`:\\n\\n```python\\nclass FeedbackHandler(SimpleHTTPRequestHandler):\\n    ledger_paths: LedgerPaths | None = None\\n    allow_write: bool = False\\n    def do_POST(self) -> None: ...   # only /feedback; 403 when read-only,\\n                                       # 400 on missing anchor or text,\\n                                       # 201 with {\\\"ulid\\\": ..., \\\"status\\\": \\\"appended\\\"}\\n\\ndef serve_bundle(bundle_dir: Path, port: int = 0, allow_write: bool = False) -> int: ...\\ndef main() -> None: ...\\n```\\n\\n`plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py`'s `main()`\\ncalls `watch_feedback(paths.ledger, args.since, args.timeout)`, prints each\\nnew line as one JSON object per line, and exits 0 or 2 depending on\\nwhether any line came back.\\n\\n## Slice Decomposition\\n\\nPer `04-slices.md`:\\n\\n1. **Slice 12 — the ledger and its projection.** No server involved.\\n   Append, fold, and project are file operations, tested without HTTP.\\n   Completed, score 0.92.\\n2. **Slice 13 — the anchor and the write endpoint.** Depends on slice 12's\\n   ledger existing for `serve_bundle.py` to append into. Click to anchor a\\n   sentence, `POST /feedback` appends, and the drawer keeps the text in\\n   view. Completed, score 1.00.\\n3. **Slice 14 — the wake command and the whole loop.** Depends on slices\\n   12 and 13. The background command blocks and returns new threads, and\\n   the loop runs once end to end. Completed, score 1.00.\\n\\n## Test Plan\\n\\n- `tests/test_ledger.py` — twelve tests: `test_append_comment_assigns_\\n  ulid_and_returns_it`, `test_append_thread_never_rewrites_an_existing_\\n  line`, `test_two_concurrent_appends_both_survive`, `test_state_change_\\n  is_a_new_line_not_an_edit`, `test_read_does_not_delete`, `test_thread_\\n  view_folds_a_reply_under_its_root`, `test_an_agent_reply_carries_author_\\n  agent_and_replies_to`, `test_projection_state_equals_the_last_\\n  transition_in_the_ledger`, `test_deleting_the_projection_rebuilds_it_\\n  from_the_ledger`, `test_projection_records_who_moved_the_state_and_\\n  when`, `test_get_thread_returns_none_for_missing`, and `test_\\n  projection_is_disposable_full_rebuild`.\\n- `tests/test_server_watch.py` — its tests are methods inside three\\n  classes, so a bare `def test` search at the start of a line misses them.\\n  Read in full, it carries `TestServer` (`test_post_feedback_appends_to_\\n  ledger`, `test_post_feedback_returns_403_when_read_only`, `test_post_\\n  feedback_rejects_other_paths`, `test_post_feedback_validates_required_\\n  fields`, `test_server_binds_loopback_only`), `TestWatchCommand`\\n  (`test_watch_exits_0_on_new_lines`, `test_watch_exits_2_on_timeout`,\\n  `test_watch_tracks_offset_per_reader`), and `TestEndToEndLoop`\\n  (`test_full_loop_comment_to_reply`, covering comment, wake, agent reply,\\n  state change, and the projection in one run). This covers every case\\n  `03-program-design.md`'s test plan names for the ledger's server and\\n  watch behavior.\\n\\n## Risks & Open Questions\\n\\n- **`test_full_loop_comment_to_reply` carries a self-correcting bug in its\\n  own body**, not in the code it tests: it first checks\\n  `hasattr(sys.modules[__name__], 'get_all_threads')`, which is always\\n  false because `get_all_threads` was never imported at module level, then\\n  falls through to a local `from ledger import get_all_threads` on the\\n  next line and proceeds correctly. The test passes, but the dead\\n  `hasattr` branch reads as leftover exploratory code that a review should\\n  clean up.\\n- **The wake command's placement under `cobuilder-full-lifecycle` rather\\n  than `cobuilder-artifact`** was not explained by any commit message or\\n  ADR this design reviewed. `cobuilder-artifact` owns `serve_bundle.py`,\\n  the other half of the same feature, so the two scripts a full loop needs\\n  now live in two different plugins.\\n- **`select.select()` on a plain file descriptor, inside `watch_feedback`,\\n  is a Linux-and-BSD idiom that does not reliably signal on every\\n  filesystem.** The function falls back to a 0.5-second poll when `select`\\n  raises `OSError`, so the polling path is exercised whenever the fast\\n  path is unavailable, but no test in the suite forces that fallback path\\n  specifically.\\n\",\n+        \"approved_date\": null,\n+        \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/cobuilder-family/epic-E6-design.md\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing/E1\",\n+        \"epic_id\": \"E1\",\n+        \"feature_slug\": \"gate-doc-surfacing\",\n+        \"title\": \"Epic Technical Solution Design: E1 — Index gate docs into data/index.json\",\n+        \"body_md\": \"# Epic Technical Solution Design: E1 — Index gate docs into data/index.json\\n\\nFeature: gate-doc-surfacing\\nEpic ID: E1\\n\\n## Scope and Intent\\nProject `docs/plans/<slug>/03-program-design.md` and\\n`docs/plans/<slug>/epic-<id>-design.md` files into two new\\n`data/index.json` entity arrays, `program_design` and `epic_design`, so the\\nviewer has something to read. Attach a `doc` reference to the matching\\nGate 3 entry in `joins.feature_gates`, and a `design_doc` reference to the\\nmatching epic in `entities.epic`. No viewer changes in this epic — that is\\nE2.\\n\\n## Files Touched\\n- `shared/build_index.py` — add `PROGRAM_DESIGN_FIELDS`,\\n  `EPIC_DESIGN_FIELDS`, `discover_plan_gate_docs()`,\\n  `project_program_design()`, `project_epic_design()`; wire the new\\n  entities into the top-level `build_index()`/`main()` flow the same way\\n  `collect_designs()` already is; extend `resolve_feature_gates()` and the\\n  epic entity emission in `collect_designs()` with the new `doc`/`design_doc`\\n  references.\\n- `tests/test_build_index_gate_docs.py` — new test file.\\n\\n## Types & Signatures\\n\\n```python\\nPROGRAM_DESIGN_FIELDS: tuple[str, ...] = (\\n    \\\"id\\\", \\\"feature_slug\\\", \\\"gate\\\", \\\"title\\\", \\\"body_md\\\", \\\"approved_date\\\", \\\"source_path\\\",\\n)\\nEPIC_DESIGN_FIELDS: tuple[str, ...] = (\\n    \\\"id\\\", \\\"epic_id\\\", \\\"feature_slug\\\", \\\"title\\\", \\\"body_md\\\", \\\"approved_date\\\", \\\"source_path\\\",\\n)\\n\\ndef discover_plan_gate_docs(repo: Path) -> list[dict]:\\n    \\\"\\\"\\\"Walk docs/plans/*/. Return one dict per gate doc found:\\n    {\\\"kind\\\": \\\"program\\\", \\\"feature_slug\\\": str, \\\"path\\\": Path, \\\"text\\\": str}\\n    or\\n    {\\\"kind\\\": \\\"epic\\\", \\\"epic_id\\\": str, \\\"feature_slug\\\": str, \\\"path\\\": Path, \\\"text\\\": str}.\\n    An epic id is parsed from the filename epic-<id>-design.md; a file\\n    whose id segment fails to parse is skipped, not raised on — this\\n    function never crashes a full index rebuild over one malformed name.\\n    \\\"\\\"\\\"\\n\\ndef project_program_design(feature_slug: str, path: Path, text: str) -> dict:\\n    \\\"\\\"\\\"Extract a title (first '# ' heading) and approved_date (from the\\n    matching 00-status.md's Gate 3 line, if resolvable) alongside the raw\\n    body. Returns a dict filtered through project_fields(..., PROGRAM_DESIGN_FIELDS).\\\"\\\"\\\"\\n\\ndef project_epic_design(epic_id: str, feature_slug: str, path: Path, text: str) -> dict:\\n    \\\"\\\"\\\"Same shape, keyed on epic_id instead of feature_slug.\\\"\\\"\\\"\\n```\\n\\n`id` for a `program_design` entity is the bare `feature_slug` (one Gate 3\\ndoc per feature, so no collision risk). `id` for an `epic_design` entity is\\nthe bare `epic_id` parsed from its filename — note this is the *unscoped*\\nepic id (`E1`), not the `<design>/<epic-id>` scoped id `entities.epic`\\nuses, because a technical design file lives under one `docs/plans/<slug>/`\\ndirectory already, so `feature_slug` carries the scoping context instead.\\n`entities.epic[].design_doc` stores the *scoped* lookup path\\n(`epic_design[<feature_slug>/<epic_id>]` is wrong; the correct join is:\\nlook up `epic_design` by its own `epic_id` key, disambiguated by also\\nmatching `feature_slug`, since two features could each have an `E1`).\\nImplementation note: to avoid this exact ambiguity, `epic_design`'s\\n`id` field is actually `f\\\"{feature_slug}/{epic_id}\\\"`, matching\\n`entities.epic`'s own `<design>/<epic-id>` scoping convention. This\\nsupersedes the plain `epic_id` framing above — keep them consistent\\nduring implementation; the test plan below locks this down.\\n\\n## Slice Decomposition\\n1. **Slice 1 (tracer bullet).** Add the constants and empty-list wiring so\\n   `data/index.json` always carries `program_design: []` and\\n   `epic_design: []`, even with zero gate docs on disk. No dependency.\\n2. **Slice 2 (real parsing).** Implement `discover_plan_gate_docs()`,\\n   `project_program_design()`, `project_epic_design()`, and the\\n   `feature_gates`/`epic` join attachment. Depends on slice 1's constants.\\n3. **Slice 3 (edge cases).** Missing `docs/plans/` entirely, a plan\\n   directory with only some gate docs, a malformed epic filename. Depends\\n   on slice 2.\\n\\n## Test Plan\\n`tests/test_build_index_gate_docs.py`:\\n- `test_project_program_design_reads_title_and_body` — feed a small\\n  markdown fixture, assert `title`/`body_md`/`source_path` match.\\n- `test_project_epic_design_id_is_scoped_feature_slug_and_epic_id` — locks\\n  down the `<feature_slug>/<epic_id>` id convention from the note above.\\n- `test_discover_plan_gate_docs_skips_malformed_epic_filename` — a file\\n  named `epic-design.md` (no id segment) is skipped, not raised on.\\n- `test_discover_plan_gate_docs_no_plans_dir_returns_empty_list` — repo\\n  with no `docs/plans/` at all.\\n- `test_resolve_feature_gates_attaches_doc_when_program_design_exists`.\\n- `test_resolve_feature_gates_gate3_doc_key_absent_when_no_md_file`.\\n- `test_build_index_end_to_end_index_json_has_program_design_and_epic_design_keys`\\n  — run against this repo's own tree; assert both keys exist, and (once\\n  this feature's own gate docs land) that this feature's own entities are\\n  among them.\\n\\n## Risks & Open Questions\\n- The id-scoping ambiguity flagged above (`epic_id` alone vs.\\n  `<feature_slug>/<epic_id>`) must be resolved consistently between the\\n  producer (`project_epic_design`) and the consumer\\n  (`entities.epic[].design_doc` lookup in E2) — the test plan pins the\\n  chosen shape so E2 cannot silently diverge from it.\\n- `approved_date` sourcing from `00-status.md`'s Gate 3/4b lines duplicates\\n  what `resolve_feature_gates()` already parses there. Acceptable\\n  duplication since the two functions read the same file for different\\n  fields, not the same field twice.\\n\",\n+        \"approved_date\": null,\n+        \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/gate-doc-surfacing/epic-E1-design.md\"\n+      },\n+      {\n+        \"id\": \"gate-doc-surfacing/E2\",\n+        \"epic_id\": \"E2\",\n+        \"feature_slug\": \"gate-doc-surfacing\",\n+        \"title\": \"Epic Technical Solution Design: E2 — Surface gate docs in the Builds view\",\n+        \"body_md\": \"# Epic Technical Solution Design: E2 — Surface gate docs in the Builds view\\n\\nFeature: gate-doc-surfacing\\nEpic ID: E2\\n\\n## Scope and Intent\\nMake the Builds view's Gate Rail cards and epic design chips clickable,\\nopening a read-only sheet that shows a `program_design` or `epic_design`\\nentity's content. Depends on E1 shipping the entities first — this epic\\nreads `window.INDEX.entities.program_design`/`.epic_design`, both empty\\narrays until E1 lands, so E2's tracer bullet is safe to build against a\\nfixture even before E1 slice 2 completes.\\n\\n## Files Touched\\n- `plugins/artifact/viewer/index.html` — new sheet markup block (sibling of\\n  `#adr-sheet`, ~line 1022), new functions `openGateDocSheet(kind, id)`,\\n  `closeGateDocSheet()`, `renderGateDocSheet()`; extend\\n  `closeAnyOpenSheet()` (~line 4003) and `renderSheetVisibility()`\\n  (~line 3960) for mutual exclusivity; wire click handlers into\\n  `renderBuildsMainContent()`'s `gate-rail-grid` (~line 3249) and\\n  `renderEpicCard()` (~line 3282).\\n\\n## Types & Signatures\\n\\n```javascript\\n// state additions, alongside existing state.adrOpen / state.activeAdrId\\nstate.gateDocOpen = false;\\nstate.activeGateDocKind = null; // 'program' | 'epic'\\nstate.activeGateDocId = null;   // matches program_design/epic_design id\\n\\nfunction openGateDocSheet(kind, id){\\n  if (state.commentsOpen) state.commentsOpen = false;\\n  if (state.adrOpen) state.adrOpen = false;\\n  if (state.assessmentOpen) state.assessmentOpen = false;\\n  if (state.audioDialogOpen) stopAudio();\\n  state.gateDocOpen = true;\\n  state.activeGateDocKind = kind;\\n  state.activeGateDocId = id;\\n  renderGateDocSheet();\\n  renderSheetVisibility();\\n}\\nfunction closeGateDocSheet(){\\n  state.gateDocOpen = false;\\n  renderSheetVisibility();\\n}\\nfunction renderGateDocSheet(){\\n  const index = window.INDEX || {};\\n  const entities = index.entities || {};\\n  const table = state.activeGateDocKind === 'program'\\n    ? (entities.program_design || {})\\n    : (entities.epic_design || {});\\n  const record = table[state.activeGateDocId];\\n  // ... title/meta/body, reusing renderMarkdown() for record.body_md,\\n  // and the \\\"record not captured\\\" empty-card fallback openAdrSheet uses\\n  // when id/record is missing.\\n}\\n```\\n\\n**Resolved during Gate 4b:** every existing `window.INDEX.entities.<type>`\\nis array-shaped (confirmed against `entities.adr`/`entities.epic` in the\\ncurrent `data/index.json`), unlike the separate dict-shaped `window.ADRS`.\\n`program_design`/`epic_design` follow the same array convention as every\\nother entity, for consistency. `renderGateDocSheet` looks a record up with\\n`(entities.program_design || []).find(r => r.id === id)` (or the\\n`epic_design` equivalent) rather than a dict key access.\\n\\n## Slice Decomposition\\n1. **Slice 4 (tracer bullet).** Sheet markup + `openGateDocSheet`/\\n   `closeGateDocSheet`/`renderSheetVisibility` wiring, gate-rail-grid card\\n   click handler. Renders title only, empty body — proves the plumbing\\n   without depending on E1's real content.\\n2. **Slice 5 (real content).** `renderGateDocSheet` renders `body_md` via\\n   `renderMarkdown()`. Epic card design-doc chip added to `renderEpicCard`.\\n   Depends on E1 slice 2 (real entities) for a meaningful demo, but the\\n   render code itself only depends on slice 4's plumbing.\\n3. **Slice 6 (edge cases).** No-`doc` gate card stays non-interactive\\n   (no click handler attached at all, not a handler that no-ops); mutual\\n   exclusivity with `#adr-sheet`/`#assessment-sheet`/comments drawer/audio\\n   dialog, both directions.\\n\\n## Test Plan\\nNo JS test harness exists in this repo for the viewer (it is one static\\nHTML file with no build step, per ADR-0001/ADR-0020). Verification is\\nmanual, via the `run` skill or a direct `python3 -m http.server` serve of\\n`.cobuilder-architect/self/`, exercising:\\n- a Gate Rail card with a `doc` opens the sheet and shows body content\\n  (slice 4+5)\\n- a Gate Rail card with no `doc` has no click affordance (slice 6)\\n- opening the gate-doc sheet while the ADR sheet is open closes the ADR\\n  sheet, and vice versa (slice 6)\\n- an epic card with `design_doc` shows a chip; clicking it opens the sheet\\n  scoped to that epic's doc (slice 5)\\n\\n## Risks & Open Questions\\n- No JS test harness means slices 4-6 lean on manual dogfood verification\\n  more than the repo's usual pytest-backed rigor. Rubric criteria for this\\n  epic's slices are written as concrete manual-check steps, not automated\\n  assertions, for this reason.\\n\",\n+        \"approved_date\": null,\n+        \"source_path\": \"/Users/theb/Documents/Windsurf/cobuilder-plugin/docs/plans/gate-doc-surfacing/epic-E2-design.md\"\n+      }\n     ]\n   },\n   \"joins\": {\n@@ -1182,6 +1358,14 @@\n         \"path\": [\n           \"book-index-tiering/E1\"\n         ]\n+      },\n+      \"ADR-0022\": {\n+        \"pr\": null,\n+        \"via\": \"epic\",\n+        \"path\": [\n+          \"gate-doc-surfacing/E1\",\n+          \"gate-doc-surfacing/E2\"\n+        ]\n       }\n     },\n     \"epic_to_pull_request\": {\n@@ -1206,6 +1390,8 @@\n       \"cobuilder-implement/E1\": \"merged\",\n       \"cobuilder-implement/E2\": \"merged\",\n       \"design-mode/E1\": \"merged\",\n+      \"gate-doc-surfacing/E1\": \"no-pull-request\",\n+      \"gate-doc-surfacing/E2\": \"no-pull-request\",\n       \"inflight-record-store/E1\": \"unstarted\",\n       \"inflight-record-store/E2\": \"unstarted\",\n       \"inflight-record-store/E3\": \"unstarted\",\n@@ -1245,7 +1431,13 @@\n       \"cobuilder-family/11\": \"plugin-split/E5\",\n       \"cobuilder-family/12\": \"plugin-split/E6\",\n       \"cobuilder-family/13\": \"plugin-split/E6\",\n-      \"cobuilder-family/14\": \"plugin-split/E6\"\n+      \"cobuilder-family/14\": \"plugin-split/E6\",\n+      \"gate-doc-surfacing/1\": \"gate-doc-surfacing/E1\",\n+      \"gate-doc-surfacing/2\": \"gate-doc-surfacing/E1\",\n+      \"gate-doc-surfacing/3\": \"gate-doc-surfacing/E1\",\n+      \"gate-doc-surfacing/4\": \"gate-doc-surfacing/E2\",\n+      \"gate-doc-surfacing/5\": \"gate-doc-surfacing/E2\",\n+      \"gate-doc-surfacing/6\": \"gate-doc-surfacing/E2\"\n     },\n     \"slice_to_epic_unresolved\": {},\n     \"context_verifies_district\": {\n@@ -1265,7 +1457,8 @@\n       \"ADR-0017\": \"cobuilder-packaging\",\n       \"ADR-0018\": \"cobuilder-packaging\",\n       \"ADR-0019\": \"cobuilder-packaging\",\n-      \"ADR-0020\": \"cobuilder-packaging\"\n+      \"ADR-0020\": \"cobuilder-packaging\",\n+      \"ADR-0022\": \"viewer\"\n     },\n     \"adr_to_district\": {\n       \"ADR-0001\": \"scripts\",\n@@ -1300,7 +1493,8 @@\n         {\n           \"n\": 3,\n           \"name\": \"Program Design\",\n-          \"state\": \"APPROVED 2026-08-21\"\n+          \"state\": \"APPROVED 2026-08-21\",\n+          \"doc\": \"cobuilder-family\"\n         },\n         {\n           \"n\": 4,\n@@ -1308,6 +1502,29 @@\n           \"state\": \"APPROVED 2026-08-25\"\n         }\n       ],\n+      \"gate-doc-surfacing\": [\n+        {\n+          \"n\": 1,\n+          \"name\": \"Product\",\n+          \"state\": \"APPROVED 2026-09-01\"\n+        },\n+        {\n+          \"n\": 2,\n+          \"name\": \"Architecture\",\n+          \"state\": \"APPROVED 2026-09-01\"\n+        },\n+        {\n+          \"n\": 3,\n+          \"name\": \"Program Design\",\n+          \"state\": \"APPROVED 2026-09-01\",\n+          \"doc\": \"gate-doc-surfacing\"\n+        },\n+        {\n+          \"n\": 4,\n+          \"name\": \"Slice plan, epic designs, and rubrics\",\n+          \"state\": \"APPROVED 2026-09-01\"\n+        }\n+      ],\n       \"skill-collision-fix\": [\n         {\n           \"n\": 1,",
  ".cobuilder-architect/self/viewer/index.html": "diff --git a/.cobuilder-architect/self/viewer/index.html b/.cobuilder-architect/self/viewer/index.html\nindex 196bea3..289fe67 100644\n--- a/.cobuilder-architect/self/viewer/index.html\n+++ b/.cobuilder-architect/self/viewer/index.html\n@@ -512,21 +512,31 @@\n      just wider — reuses .drawer-header/.drawer-close/.drawer-body). Shares\n      #drawer-scrim with the comments drawer; JS enforces only one open at a time. ================= */\n   #adr-sheet,\n-  #assessment-sheet{\n+  #assessment-sheet,\n+  #gate-doc-sheet{\n     position:fixed;top:0;right:0;bottom:0;width:clamp(480px, 40vw, 720px);max-width:92vw;background:#fff;\n     border-left:1px solid var(--line);border-radius:14px 0 0 14px;box-shadow:-12px 0 34px rgba(20,25,35,0.2);\n     transform:translateX(100%);transition:transform .25s ease;z-index:41;\n     display:flex;flex-direction:column;min-height:0;\n   }\n   #adr-sheet.open,\n-  #assessment-sheet.open{transform:translateX(0);}\n+  #assessment-sheet.open,\n+  #gate-doc-sheet.open{transform:translateX(0);}\n   #adr-sheet .drawer-header,\n-  #assessment-sheet .drawer-header{align-items:flex-start;}\n+  #assessment-sheet .drawer-header,\n+  #gate-doc-sheet .drawer-header{align-items:flex-start;}\n   .adr-sheet-meta{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:6px;}\n   .adr-state-badge{font:10px 'JetBrains Mono',monospace;letter-spacing:.04em;padding:3px 9px;border-radius:10px;border:1px solid;text-transform:uppercase;white-space:nowrap;}\n   .adr-sheet-sourceline{font:11px 'JetBrains Mono',monospace;color:var(--ink-dim);}\n   #adr-sheet .drawer-body,\n-  #assessment-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+  #assessment-sheet .drawer-body,\n+  #gate-doc-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+  .gate-rail-feature{margin-bottom:14px;}\n+  .gate-rail-feature-label{font-size:11px;color:var(--ink-dim);letter-spacing:.03em;margin-bottom:6px;}\n+  .gate-rail-clickable{cursor:pointer;}\n+  .gate-rail-clickable:hover{box-shadow:0 0 0 2px var(--accent, oklch(0.55 0.15 250)) inset;}\n+  .epic-design-doc-chip{display:inline-flex;align-items:center;gap:4px;font:10px 'JetBrains Mono',monospace;letter-spacing:.02em;padding:2px 8px;border-radius:9px;border:1px solid var(--line);color:var(--ink-dim);cursor:pointer;background:oklch(0.97 0.01 250);}\n+  .epic-design-doc-chip:hover{color:var(--ink);border-color:oklch(0.55 0.15 250);}\n \n   /* Design epic row — the Designs tab's level-3 epic blocks (renderDesignMainContent). */\n   .design-epic-row{display:flex;align-items:center;gap:8px;flex-wrap:wrap;font:11.5px 'JetBrains Mono',monospace;color:oklch(0.3 0.02 250);}\n@@ -1046,6 +1056,21 @@\n     <div class=\"drawer-body\" id=\"assessment-sheet-body\"></div>\n   </div>\n \n+  <!-- Gate document sheet (same Sheet family as ADR/assessment above), read-only.\n+       Reads window.INDEX.entities.program_design / .epic_design for the clicked\n+       Gate Rail card or epic design-doc chip (ADR-0022). Shares #drawer-scrim;\n+       only one sheet is ever open. -->\n+  <div id=\"gate-doc-sheet\" role=\"dialog\" aria-label=\"Gate document\">\n+    <div class=\"drawer-header\">\n+      <div>\n+        <h2 id=\"gate-doc-sheet-title\">Gate document</h2>\n+        <div class=\"adr-sheet-meta\" id=\"gate-doc-sheet-meta\"></div>\n+      </div>\n+      <button class=\"drawer-close\" id=\"gate-doc-sheet-close-btn\" title=\"Close (Esc)\">&times;</button>\n+    </div>\n+    <div class=\"drawer-body\" id=\"gate-doc-sheet-body\"></div>\n+  </div>\n+\n   <!-- Narration audio dialog (shadcn Dialog look, centered). Shares #drawer-scrim above;\n        mutually exclusive with the comments drawer / ADR sheet — see openAudioDialog. -->\n   <div id=\"audio-dialog-layer\">\n@@ -1168,6 +1193,10 @@ window.INDEX = window.INDEX || {};\n     activeAdrId: null,\n     // Generate-mode assessment sheet: mutually exclusive with both panels above.\n     assessmentOpen: false,\n+    // Gate document sheet (ADR-0022): mutually exclusive with every sheet above.\n+    gateDocOpen: false,\n+    activeGateDocKind: null, // 'program' | 'epic'\n+    activeGateDocId: null,\n     // First-class view mode. 'prs' is the current pull-request viewer.\n     // 'designs' mirrors it with a three-level design view. Persisted.\n     viewMode: 'prs',\n@@ -3209,14 +3238,19 @@ window.INDEX = window.INDEX || {};\n     const sliceToEpic = joins.slice_to_epic || {};\n     const featureGatesMap = joins.feature_gates || {};\n \n-    const gates = (featureGatesMap['cobuilder-family'] && featureGatesMap['cobuilder-family'].length > 0)\n-      ? featureGatesMap['cobuilder-family']\n-      : [\n+    // Every feature with a real docs/plans/<slug>/00-status.md gets its own rail.\n+    // A bundle with none yet (nothing built through implement mode) falls back to\n+    // one demo rail so the section never renders empty.\n+    const featureSlugsWithGates = Object.keys(featureGatesMap).filter(k => (featureGatesMap[k] || []).length > 0).sort();\n+    const gateRails = featureSlugsWithGates.length\n+      ? featureSlugsWithGates.map(slug => ({ feature: slug, gates: featureGatesMap[slug] }))\n+      : [{ feature: null, gates: [\n           { n: 1, name: 'Product', state: 'APPROVED 2026-08-20' },\n           { n: 2, name: 'Architecture', state: 'APPROVED 2026-08-21' },\n           { n: 3, name: 'Program Design', state: 'APPROVED 2026-08-21' },\n           { n: 4, name: 'Slice plan + rubrics', state: 'APPROVED 2026-08-21' }\n-        ];\n+        ] }];\n+    const totalGates = gateRails.reduce((sum, r) => sum + r.gates.length, 0);\n \n     const slicesByEpic = {};\n     slices.forEach(s => {\n@@ -3238,30 +3272,36 @@ window.INDEX = window.INDEX || {};\n       return a.id.localeCompare(b.id);\n     });\n \n-    // Section 1: Active Feature Gate Rail & Approval State (C3)\n+    // Section 1: Active Feature Gate Rail & Approval State (C3). One rail per\n+    // feature; a card with a `doc` (ADR-0022) opens the gate-doc sheet on click.\n     const gatesHtml = `\n       <div class=\"builds-section\" id=\"builds-gates-section\">\n         <div class=\"section-title-bar\">\n           <h3 class=\"section-title\">Active Feature Gate Rail & Approval State</h3>\n-          <span class=\"section-count mono\">${gates.length} Gates Approved</span>\n-        </div>\n-        <p class=\"section-intro\">Gate status chrome derived from status records. Active build runs on Gate 4.</p>\n-        <div class=\"gate-rail-grid\">\n-          ${gates.map(g => {\n-            const isApproved = (g.state || '').toUpperCase().startsWith('APPROVED');\n-            const isCurrent = g.n === 4;\n-            const statusClass = isApproved ? 'is-approved' : (isCurrent ? 'is-current' : 'is-planned');\n-            return `\n-              <div class=\"gate-card ${statusClass}\">\n-                <div class=\"gate-header\">\n-                  <div class=\"gate-num-badge mono\">Gate ${g.n}</div>\n-                  <div class=\"gate-status-pill ${isApproved ? 'ok' : (isCurrent ? 'now' : 'wait')}\">${escapeHtml(g.state)}</div>\n-                </div>\n-                <div class=\"gate-name\">${escapeHtml(g.name)}</div>\n-                <div class=\"gate-desc\">${isApproved ? `Approved on ${escapeHtml(g.state.replace(/^APPROVED\\s*/i, ''))}` : 'In progress'}</div>\n-              </div>`;\n-          }).join('')}\n+          <span class=\"section-count mono\">${totalGates} Gates Approved</span>\n         </div>\n+        <p class=\"section-intro\">Gate status chrome derived from status records. Click a gate card with a document icon to read it.</p>\n+        ${gateRails.map(rail => `\n+          <div class=\"gate-rail-feature\">\n+            ${rail.feature ? `<div class=\"gate-rail-feature-label mono\">${escapeHtml(rail.feature)}</div>` : ''}\n+            <div class=\"gate-rail-grid\">\n+              ${rail.gates.map(g => {\n+                const isApproved = (g.state || '').toUpperCase().startsWith('APPROVED');\n+                const isCurrent = g.n === 4;\n+                const statusClass = isApproved ? 'is-approved' : (isCurrent ? 'is-current' : 'is-planned');\n+                const clickAttrs = g.doc ? ` data-gate-doc-id=\"${escapeHtml(g.doc)}\" tabindex=\"0\" role=\"button\"` : '';\n+                return `\n+                  <div class=\"gate-card ${statusClass}${g.doc ? ' gate-rail-clickable' : ''}\"${clickAttrs}>\n+                    <div class=\"gate-header\">\n+                      <div class=\"gate-num-badge mono\">Gate ${g.n}</div>\n+                      <div class=\"gate-status-pill ${isApproved ? 'ok' : (isCurrent ? 'now' : 'wait')}\">${escapeHtml(g.state)}</div>\n+                    </div>\n+                    <div class=\"gate-name\">${escapeHtml(g.name)}${g.doc ? ' &#128196;' : ''}</div>\n+                    <div class=\"gate-desc\">${isApproved ? `Approved on ${escapeHtml(g.state.replace(/^APPROVED\\s*/i, ''))}` : 'In progress'}</div>\n+                  </div>`;\n+              }).join('')}\n+            </div>\n+          </div>`).join('')}\n       </div>`;\n \n     // Section 2: Backlog Lane (C1 & C2), grouped by parent design (\"program\")\n@@ -3304,6 +3344,10 @@ window.INDEX = window.INDEX || {};\n           <span class=\"deferred-text\">No slice yet &mdash; this epic is deferred past active scope.</span>\n         </div>`;\n \n+      const designDocChip = epic.design_doc\n+        ? `<span class=\"epic-design-doc-chip\" data-epic-design-id=\"${escapeHtml(epic.design_doc)}\" tabindex=\"0\" role=\"button\">&#128196; Design</span>`\n+        : '';\n+\n       return `\n         <div class=\"backlog-card ${isDeferred ? 'is-deferred' : ''}\">\n           <div class=\"backlog-card-header\">\n@@ -3311,6 +3355,7 @@ window.INDEX = window.INDEX || {};\n               <code>${escapeHtml(epic.epic_id || epic.id)}</code>\n             </div>\n             <span class=\"backlog-badge ${statusClass}\">${statusLabel}</span>\n+            ${designDocChip}\n           </div>\n           <div class=\"backlog-card-body\">\n             <p class=\"backlog-epic-note\">${escapeHtml(epic.note || 'No description recorded in goal.json.')}</p>\n@@ -3451,6 +3496,13 @@ window.INDEX = window.INDEX || {};\n         ${sectionsHtml}\n       </div>`);\n \n+    main.querySelectorAll('[data-gate-doc-id]').forEach(el => {\n+      el.addEventListener('click', () => openGateDocSheet('program', el.dataset.gateDocId));\n+    });\n+    main.querySelectorAll('[data-epic-design-id]').forEach(el => {\n+      el.addEventListener('click', () => openGateDocSheet('epic', el.dataset.epicDesignId));\n+    });\n+\n     main.scrollTop = 0;\n     hideCue();\n   }\n@@ -3627,6 +3679,7 @@ window.INDEX = window.INDEX || {};\n     if (state.commentsOpen) state.commentsOpen = false;\n     if (state.adrOpen) state.adrOpen = false;\n     if (state.assessmentOpen) state.assessmentOpen = false;\n+    if (state.gateDocOpen) state.gateDocOpen = false;\n     state.audioDialogOpen = true;\n     renderAudioDialogContent();\n     const dialog = document.getElementById('audio-dialog');\n@@ -3959,13 +4012,15 @@ window.INDEX = window.INDEX || {};\n     document.getElementById('content-col').classList.toggle('drawer-open', state.commentsOpen);\n     document.getElementById('adr-sheet').classList.toggle('open', state.adrOpen);\n     document.getElementById('assessment-sheet').classList.toggle('open', state.assessmentOpen);\n+    document.getElementById('gate-doc-sheet').classList.toggle('open', state.gateDocOpen);\n     const scrim = document.getElementById('drawer-scrim');\n-    scrim.classList.toggle('open', state.adrOpen || state.assessmentOpen || state.audioDialogOpen);\n+    scrim.classList.toggle('open', state.adrOpen || state.assessmentOpen || state.gateDocOpen || state.audioDialogOpen);\n     scrim.classList.toggle('audio-dim', state.audioDialogOpen);\n   }\n   function openCommentsDrawer(){\n     if (state.adrOpen){ state.adrOpen = false; }\n     if (state.assessmentOpen){ state.assessmentOpen = false; }\n+    if (state.gateDocOpen){ state.gateDocOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.commentsOpen = true;\n     renderSheetVisibility();\n@@ -3978,6 +4033,7 @@ window.INDEX = window.INDEX || {};\n   function openAdrSheet(id){\n     if (state.commentsOpen){ state.commentsOpen = false; }\n     if (state.assessmentOpen){ state.assessmentOpen = false; }\n+    if (state.gateDocOpen){ state.gateDocOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.adrOpen = true;\n     state.activeAdrId = id;\n@@ -3991,6 +4047,7 @@ window.INDEX = window.INDEX || {};\n   function openAssessmentSheet(){\n     if (state.commentsOpen){ state.commentsOpen = false; }\n     if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.gateDocOpen){ state.gateDocOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.assessmentOpen = true;\n     renderAssessmentSheet();\n@@ -4000,10 +4057,26 @@ window.INDEX = window.INDEX || {};\n     state.assessmentOpen = false;\n     renderSheetVisibility();\n   }\n+  function openGateDocSheet(kind, id){\n+    if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n+    if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n+    state.gateDocOpen = true;\n+    state.activeGateDocKind = kind;\n+    state.activeGateDocId = id;\n+    renderGateDocSheet();\n+    renderSheetVisibility();\n+  }\n+  function closeGateDocSheet(){\n+    state.gateDocOpen = false;\n+    renderSheetVisibility();\n+  }\n   function closeAnyOpenSheet(){\n     if (state.commentsOpen) closeCommentsDrawer();\n     else if (state.adrOpen) closeAdrSheet();\n     else if (state.assessmentOpen) closeAssessmentSheet();\n+    else if (state.gateDocOpen) closeGateDocSheet();\n     else if (state.audioDialogOpen) stopAudio();\n   }\n \n@@ -4269,6 +4342,33 @@ window.INDEX = window.INDEX || {};\n       </div>`;\n   }\n \n+  function renderGateDocSheet(){\n+    const kind = state.activeGateDocKind;\n+    const id = state.activeGateDocId;\n+    const entities = (window.INDEX && window.INDEX.entities) || {};\n+    const table = kind === 'epic' ? (entities.epic_design || []) : (entities.program_design || []);\n+    const record = id ? table.find(r => r.id === id) : null;\n+    const titleEl = document.getElementById('gate-doc-sheet-title');\n+    const metaEl = document.getElementById('gate-doc-sheet-meta');\n+    const body = document.getElementById('gate-doc-sheet-body');\n+\n+    if (!record){\n+      titleEl.textContent = id || 'Gate document';\n+      metaEl.innerHTML = '';\n+      body.innerHTML = `\n+        <div class=\"empty-card\">\n+          <div class=\"empty-detail\">Gate document not found in this bundle's index.</div>\n+        </div>`;\n+      return;\n+    }\n+\n+    titleEl.textContent = record.title || record.id;\n+    const sourceLine = record.source_path ? `source: ${record.source_path}` : '';\n+    const gateLine = kind === 'epic' ? 'Gate 4b' : 'Gate 3';\n+    metaEl.innerHTML = `<span class=\"adr-sheet-sourceline\">${escapeHtml(gateLine)}${sourceLine ? ' · ' + escapeHtml(sourceLine) : ''}</span>`;\n+    body.innerHTML = renderMarkdown(record.body_md || '');\n+  }\n+\n   function renderAssessmentSheet(){\n     const p = pr();\n     const a = p && p.assessment;\n@@ -4719,6 +4819,8 @@ window.INDEX = window.INDEX || {};\n   if (adrSheetCloseBtn) adrSheetCloseBtn.addEventListener('click', closeAdrSheet);\n   const assessmentSheetCloseBtn = document.getElementById('assessment-sheet-close-btn');\n   if (assessmentSheetCloseBtn) assessmentSheetCloseBtn.addEventListener('click', closeAssessmentSheet);\n+  const gateDocSheetCloseBtn = document.getElementById('gate-doc-sheet-close-btn');\n+  if (gateDocSheetCloseBtn) gateDocSheetCloseBtn.addEventListener('click', closeGateDocSheet);\n   const drawerScrim = document.getElementById('drawer-scrim');\n   if (drawerScrim) drawerScrim.addEventListener('click', closeAnyOpenSheet);\n   const btnAddComment = document.getElementById('btn-add-comment');\n@@ -4745,7 +4847,7 @@ window.INDEX = window.INDEX || {};\n   window.addEventListener('keydown', (e) => {\n     // Escape must close whichever sheet is open, even while the comment input is\n     // focused — checked before the input/textarea early-return below so it always works.\n-    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n+    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.assessmentOpen || state.gateDocOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n \n     const tag = document.activeElement && document.activeElement.tagName;\n     if (tag === 'TEXTAREA' || tag === 'INPUT') return;",
  ".cobuilder/rubrics/gate-doc-surfacing/manifest.yaml": "diff --git a/.cobuilder/rubrics/gate-doc-surfacing/manifest.yaml b/.cobuilder/rubrics/gate-doc-surfacing/manifest.yaml\nnew file mode 100644\nindex 0000000..461124c\n--- /dev/null\n+++ b/.cobuilder/rubrics/gate-doc-surfacing/manifest.yaml\n@@ -0,0 +1,25 @@\n+feature: gate-doc-surfacing\n+test_command: \"python3 -m pytest tests/test_build_index_gate_docs.py -v\"\n+thresholds:\n+  accept: 0.90\n+  investigate: 0.70\n+max_attempts: 3\n+slices:\n+  - id: 1\n+    name: \"E1 tracer bullet: constants and empty entity lists\"\n+    weight: 0.10\n+  - id: 2\n+    name: \"E1 real parsing: project Gate 3/4b docs\"\n+    weight: 0.25\n+  - id: 3\n+    name: \"E1 edge cases\"\n+    weight: 0.10\n+  - id: 4\n+    name: \"E2 tracer bullet: clickable Gate Rail card opens empty sheet\"\n+    weight: 0.15\n+  - id: 5\n+    name: \"E2 real content: render body, add epic chip\"\n+    weight: 0.25\n+  - id: 6\n+    name: \"E2 edge cases: no-doc cards, mutual exclusivity\"\n+    weight: 0.15",
  ".cobuilder/rubrics/gate-doc-surfacing/slice-1.md": "diff --git a/.cobuilder/rubrics/gate-doc-surfacing/slice-1.md b/.cobuilder/rubrics/gate-doc-surfacing/slice-1.md\nnew file mode 100644\nindex 0000000..a2506bc\n--- /dev/null\n+++ b/.cobuilder/rubrics/gate-doc-surfacing/slice-1.md\n@@ -0,0 +1,44 @@\n+# Rubric: Slice 1 — E1 tracer bullet: constants and empty entity lists\n+\n+Feature: gate-doc-surfacing\n+Epic: E1\n+Slice goal: `data/index.json` carries `program_design` and `epic_design` keys (empty lists when no gate docs exist yet)\n+Test command: python3 -m pytest tests/test_build_index_gate_docs.py -v\n+\n+## Criteria\n+\n+### C1 — index.json always carries both new entity keys [CRITICAL]\n+**Must be true:** Running `shared/build_index.py` against any repo, including\n+one with no `docs/plans/` directory at all, produces `data/index.json` with\n+top-level `entities.program_design` and `entities.epic_design` keys present\n+as lists (possibly empty), never missing or `null`.\n+**Evidence to check:**\n+- Run `uv run shared/build_index.py` against this repo; `python3 -c \"import json; d=json.load(open('.cobuilder-architect/self/data/index.json')); assert 'program_design' in d['entities'] and 'epic_design' in d['entities']\"`.\n+- Run the test suite; find a test asserting the empty-repo case.\n+**Scoring:**\n+- 1.0 — both keys present as lists in every case, with a passing test for the no-`docs/plans/` case.\n+- 0.5 — keys present when gate docs exist, but missing or erroring when none do.\n+- 0.0 — keys absent, or build_index.py crashes.\n+\n+### C2 — no regression to existing entity counts\n+**Must be true:** All previously-existing entity types (`adr`, `design`,\n+`epic`, `context`, `district`, `boundary_rule`, `pull_request`, `slice`,\n+`publication`) keep their pre-slice counts unchanged by this slice alone.\n+**Evidence to check:**\n+- Compare `entities.<type>.length` before and after this slice for each\n+  existing type; only `slice` may legitimately differ, and only because of\n+  this feature's own plan docs already on disk, not because of this slice's\n+  code.\n+**Scoring:**\n+- 1.0 — no unexplained count changes.\n+- 0.0 — an existing entity type's count changed with no explanation traceable to pre-existing plan/design files.\n+\n+## Regression check\n+- All tests that passed before this slice must still pass:\n+  `python3 -m pytest tests/ -q`.\n+- Files outside `shared/build_index.py` and the new test file must remain unchanged.\n+\n+## Out of scope — do not penalise\n+- Actually reading or parsing gate doc content (slice 2).\n+- Attaching `doc`/`design_doc` join references (slice 2).\n+- Any viewer change (epic E2).",
  ".cobuilder/rubrics/gate-doc-surfacing/slice-2.md": "diff --git a/.cobuilder/rubrics/gate-doc-surfacing/slice-2.md b/.cobuilder/rubrics/gate-doc-surfacing/slice-2.md\nnew file mode 100644\nindex 0000000..ac05a31\n--- /dev/null\n+++ b/.cobuilder/rubrics/gate-doc-surfacing/slice-2.md\n@@ -0,0 +1,63 @@\n+# Rubric: Slice 2 — E1 real parsing: project Gate 3/4b docs\n+\n+Feature: gate-doc-surfacing\n+Epic: E1\n+Slice goal: A repo with a `03-program-design.md` and an `epic-*-design.md` produces populated `program_design`/`epic_design` entities, and `joins.feature_gates[slug][3].doc` points at the projected id\n+Test command: python3 -m pytest tests/test_build_index_gate_docs.py -v\n+\n+## Criteria\n+\n+### C1 — program_design entity is correctly projected [CRITICAL]\n+**Must be true:** For a feature slug with a `03-program-design.md` file,\n+`entities.program_design` contains one record with `id == feature_slug`,\n+`feature_slug`, `gate == 3`, a non-empty `title` (from the file's first `#`\n+heading), and `body_md` containing the file's content.\n+**Evidence to check:**\n+- Run against this repo's own `docs/plans/gate-doc-surfacing/03-program-design.md`;\n+  confirm a `program_design` record with `id == \"gate-doc-surfacing\"` exists\n+  in the rebuilt `data/index.json` and its `title` reads \"Program Design:\n+  Gate doc surfacing\".\n+- Read `test_project_program_design_reads_title_and_body` and run it.\n+**Scoring:**\n+- 1.0 — record present with correct id/title/body for a real fixture and for this repo's own doc.\n+- 0.5 — record present but title or body_md is empty/wrong.\n+- 0.0 — no record produced.\n+\n+### C2 — epic_design entity id is scoped `<feature_slug>/<epic_id>` [CRITICAL]\n+**Must be true:** For `docs/plans/gate-doc-surfacing/epic-E1-design.md`, the\n+projected `epic_design` record's `id` is exactly `\"gate-doc-surfacing/E1\"`\n+— matching `entities.epic`'s own scoping convention, per epic-E1-design.md's\n+resolved decision.\n+**Evidence to check:**\n+- Rebuild the index; find the `epic_design` record for this repo's own\n+  `epic-E1-design.md` and `epic-E2-design.md`; assert their ids are\n+  `gate-doc-surfacing/E1` and `gate-doc-surfacing/E2`.\n+- Read and run `test_project_epic_design_id_is_scoped_feature_slug_and_epic_id`.\n+**Scoring:**\n+- 1.0 — ids exactly match the scoped convention for both this repo's real epic design files.\n+- 0.5 — ids present but using the bare unscoped epic id instead (collision risk across features).\n+- 0.0 — no epic_design records produced at all.\n+\n+### C3 — joins.feature_gates gains a doc reference on Gate 3\n+**Must be true:** `joins.feature_gates[\"gate-doc-surfacing\"]`'s entry for\n+gate `n == 3` carries a `doc` field equal to the `program_design` entity's\n+id.\n+**Evidence to check:**\n+- Inspect the rebuilt index's `joins.feature_gates[\"gate-doc-surfacing\"]`\n+  list; find the entry with `n: 3`; confirm it has `doc: \"gate-doc-surfacing\"`.\n+- Run `test_resolve_feature_gates_attaches_doc_when_program_design_exists`.\n+**Scoring:**\n+- 1.0 — `doc` field present and correct on the Gate 3 entry only (not on other gates).\n+- 0.5 — `doc` present but on the wrong gate entry, or present on every gate regardless of doc existence.\n+- 0.0 — no `doc` field added anywhere.\n+\n+## Regression check\n+- `python3 -m pytest tests/ -q` — all prior-passing tests still pass.\n+- Slice 1's empty-list behavior for a feature slug with no gate docs still holds.\n+\n+## Out of scope — do not penalise\n+- The `entities.epic[].design_doc` join (belongs conceptually to E1 but is\n+  covered by slice 2's C2/C3 scope only for feature_gates — if the epic-side\n+  join is deferred to a later slice by the implementer, note it, don't fail C3 for it).\n+- Missing-file/malformed-filename handling (slice 3).\n+- Any viewer change (epic E2).",
  ".cobuilder/rubrics/gate-doc-surfacing/slice-3.md": "diff --git a/.cobuilder/rubrics/gate-doc-surfacing/slice-3.md b/.cobuilder/rubrics/gate-doc-surfacing/slice-3.md\nnew file mode 100644\nindex 0000000..a5835ce\n--- /dev/null\n+++ b/.cobuilder/rubrics/gate-doc-surfacing/slice-3.md\n@@ -0,0 +1,49 @@\n+# Rubric: Slice 3 — E1 edge cases\n+\n+Feature: gate-doc-surfacing\n+Epic: E1\n+Slice goal: A plan directory with no gate docs, and a repo with no `docs/plans/` at all, both still produce a clean index build with empty (not missing) entity keys\n+Test command: python3 -m pytest tests/test_build_index_gate_docs.py -v\n+\n+## Criteria\n+\n+### C1 — no docs/plans/ directory at all does not crash the build [CRITICAL]\n+**Must be true:** `discover_plan_gate_docs()` and the full `build_index.py`\n+run cleanly against a repo tree with no `docs/plans/` directory, returning\n+an empty list / empty entity arrays rather than raising.\n+**Evidence to check:**\n+- Run `test_discover_plan_gate_docs_no_plans_dir_returns_empty_list`.\n+- Confirm the function doesn't call `Path.iterdir()` on a non-existent path\n+  without a existence check first (read the function).\n+**Scoring:**\n+- 1.0 — clean empty result, test passes.\n+- 0.0 — raises `FileNotFoundError` or similar.\n+\n+### C2 — a plan directory with only some gate docs is handled\n+**Must be true:** A `docs/plans/<slug>/` directory with a `04-slices.md`\n+but no `03-program-design.md` produces no `program_design` record for that\n+slug, without affecting other slugs' entities.\n+**Evidence to check:** run the test for this case (any existing plan\n+directory in this repo missing a Gate 3 doc, if one exists, or a fixture).\n+**Scoring:**\n+- 1.0 — no spurious record, no crash, other slugs unaffected.\n+- 0.5 — no crash, but an incorrect empty-body record is created instead of no record.\n+- 0.0 — crash, or other slugs' entities corrupted.\n+\n+### C3 — a malformed epic design filename is skipped, not fatal [CRITICAL]\n+**Must be true:** A file matching `epic-design.md` (no id segment) or\n+`epic-*-design.md` with an unparseable id is skipped by\n+`discover_plan_gate_docs()` without raising and without stopping discovery\n+of other, well-formed files in the same or other plan directories.\n+**Evidence to check:** `test_discover_plan_gate_docs_skips_malformed_epic_filename`.\n+**Scoring:**\n+- 1.0 — skipped cleanly, other files still discovered, test passes.\n+- 0.0 — crashes the whole index build, or silently drops well-formed sibling files too.\n+\n+## Regression check\n+- `python3 -m pytest tests/ -q` — full suite still green.\n+- Slices 1 and 2's criteria still hold against this repo's own real gate docs.\n+\n+## Out of scope — do not penalise\n+- Any viewer change (epic E2).\n+- Recovering or repairing a malformed filename — skipping is sufficient.",
  ".cobuilder/rubrics/gate-doc-surfacing/slice-4.md": "diff --git a/.cobuilder/rubrics/gate-doc-surfacing/slice-4.md b/.cobuilder/rubrics/gate-doc-surfacing/slice-4.md\nnew file mode 100644\nindex 0000000..874f6c6\n--- /dev/null\n+++ b/.cobuilder/rubrics/gate-doc-surfacing/slice-4.md\n@@ -0,0 +1,52 @@\n+# Rubric: Slice 4 — E2 tracer bullet: clickable Gate Rail card opens empty sheet\n+\n+Feature: gate-doc-surfacing\n+Epic: E2\n+Slice goal: Clicking a Gate Rail card with a `doc` opens a sheet showing the gate's title and an empty body; a card with no `doc` stays non-interactive\n+Test command: manual browser check via the run skill (no JS test harness in this repo)\n+\n+## Criteria\n+\n+### C1 — a Gate Rail card with a `doc` opens a sheet on click [CRITICAL]\n+**Must be true:** Serving the rebuilt bundle and opening the Builds view,\n+clicking a Gate Rail card whose gate entry has a `doc` field opens a\n+visible sheet element showing at least the gate's name/title.\n+**Evidence to check:** browser check — serve `.cobuilder-architect/self/`\n+with `python3 -m http.server`, open the viewer, navigate to Builds, click\n+the Gate 3 card for `gate-doc-surfacing` (once E1 has produced its `doc`\n+field), confirm a sheet opens.\n+**Scoring:**\n+- 1.0 — sheet opens and shows a title on click.\n+- 0.5 — clicking does something (e.g. console log) but no visible sheet appears.\n+- 0.0 — no response to the click at all.\n+\n+### C2 — a Gate Rail card with no `doc` has no click affordance\n+**Must be true:** A gate whose entry has no `doc` field is visually\n+distinguishable as non-interactive (e.g. no pointer cursor / no click\n+handler attached) and clicking it does nothing.\n+**Evidence to check:** browser check — click a gate card with no `doc`\n+(e.g. an approved gate before E1 attaches its doc reference, or any gate\n+number with no matching file); confirm nothing opens.\n+**Scoring:**\n+- 1.0 — no sheet opens, and the card shows no interactive affordance (cursor/hover state).\n+- 0.5 — no sheet opens, but the card still looks clickable (misleading affordance).\n+- 0.0 — clicking a no-doc card opens an empty or broken sheet.\n+\n+### C3 — the sheet closes via its close control\n+**Must be true:** The opened sheet has a working close button/control that\n+hides it.\n+**Evidence to check:** browser check — after opening the sheet in C1,\n+click its close control; confirm it hides.\n+**Scoring:**\n+- 1.0 — closes cleanly.\n+- 0.0 — no close control, or clicking it does nothing.\n+\n+## Regression check\n+- Existing ADR sheet, assessment sheet, and comments drawer still open and\n+  close correctly (manual spot check) — this slice adds a sibling, not a\n+  replacement.\n+\n+## Out of scope — do not penalise\n+- Rendering the actual body_md content (slice 5).\n+- The epic card design-doc chip (slice 5).\n+- Mutual exclusivity with other sheets (slice 6).",
  ".cobuilder/rubrics/gate-doc-surfacing/slice-5.md": "diff --git a/.cobuilder/rubrics/gate-doc-surfacing/slice-5.md b/.cobuilder/rubrics/gate-doc-surfacing/slice-5.md\nnew file mode 100644\nindex 0000000..992c181\n--- /dev/null\n+++ b/.cobuilder/rubrics/gate-doc-surfacing/slice-5.md\n@@ -0,0 +1,58 @@\n+# Rubric: Slice 5 — E2 real content: render body, add epic chip\n+\n+Feature: gate-doc-surfacing\n+Epic: E2\n+Slice goal: The sheet renders `body_md` through the existing `renderMarkdown()`; an epic card with a `design_doc` shows a chip that opens the same sheet scoped to that epic\n+Test command: manual browser check via the run skill (no JS test harness in this repo)\n+\n+## Criteria\n+\n+### C1 — the gate doc sheet renders real markdown content [CRITICAL]\n+**Must be true:** Opening the Gate 3 sheet for `gate-doc-surfacing` (this\n+feature's own real `03-program-design.md`, once E1 has projected it) shows\n+its actual headings and body text, rendered as HTML (headings as `<h*>`,\n+lists as `<ul>/<ol>`, not raw markdown syntax visible on the page).\n+**Evidence to check:** browser check — open the sheet, confirm \"Files\",\n+\"Types & signatures\", \"Call stack\" etc. render as headings, and no literal\n+`##` or `**` characters are visible in the rendered text.\n+**Scoring:**\n+- 1.0 — headings, lists, and inline formatting all render correctly via `renderMarkdown()`.\n+- 0.5 — text appears but as raw unrendered markdown (e.g. visible `##`).\n+- 0.0 — sheet body stays empty or shows an error.\n+\n+### C2 — an epic card with an approved 4b design shows a chip [CRITICAL]\n+**Must be true:** In the Builds view's Backlog Lane, an epic card for\n+`gate-doc-surfacing/E1` or `.../E2` (both have approved epic-*-design.md\n+files) shows a small chip distinct from the existing Planned/Deferred\n+badge.\n+**Evidence to check:** browser check — locate the `gate-doc-surfacing`\n+program group in the Backlog Lane, confirm E1 and E2's cards show a\n+design-doc chip.\n+**Scoring:**\n+- 1.0 — chip visible on both epics that have an approved 4b design.\n+- 0.5 — chip visible but visually indistinguishable from existing badges (no clear affordance).\n+- 0.0 — no chip appears on either card.\n+\n+### C3 — clicking the chip opens the sheet scoped to that epic\n+**Must be true:** Clicking E1's chip shows E1's `epic-E1-design.md`\n+content; clicking E2's chip shows E2's `epic-E2-design.md` content —\n+different content per epic, not the same record for both.\n+**Evidence to check:** browser check — click each chip in turn, confirm\n+the sheet body differs and matches the corresponding file's real content\n+(e.g. E1's sheet mentions \"PROGRAM_DESIGN_FIELDS\", E2's mentions\n+\"openGateDocSheet\").\n+**Scoring:**\n+- 1.0 — each chip opens its own epic's correct content.\n+- 0.5 — chip opens a sheet but shows the wrong epic's content, or always the same content.\n+- 0.0 — chip has no effect.\n+\n+## Regression check\n+- Slice 4's criteria (click-to-open, close control, no-doc card stays\n+  inert) still hold.\n+- `python3 -m pytest tests/ -q` still passes (this slice is viewer-only,\n+  so should be a no-op on the suite, but confirm no accidental Python edits).\n+\n+## Out of scope — do not penalise\n+- An epic with no approved 4b design (single-slice epics, marked `n/a`\n+  for 4b elsewhere in the repo) showing no chip — that is correct, not a defect.\n+- Mutual exclusivity with other sheets (slice 6).",
  ".cobuilder/rubrics/gate-doc-surfacing/slice-6.md": "diff --git a/.cobuilder/rubrics/gate-doc-surfacing/slice-6.md b/.cobuilder/rubrics/gate-doc-surfacing/slice-6.md\nnew file mode 100644\nindex 0000000..3163e77\n--- /dev/null\n+++ b/.cobuilder/rubrics/gate-doc-surfacing/slice-6.md\n@@ -0,0 +1,55 @@\n+# Rubric: Slice 6 — E2 edge cases: no-doc cards, mutual exclusivity\n+\n+Feature: gate-doc-surfacing\n+Epic: E2\n+Slice goal: A Gate Rail card whose gate has no matching doc opens nothing; opening the gate-doc sheet closes any open ADR/assessment/comments sheet, and vice versa\n+Test command: manual browser check via the run skill (no JS test harness in this repo)\n+\n+## Criteria\n+\n+### C1 — opening the gate-doc sheet closes an already-open ADR sheet [CRITICAL]\n+**Must be true:** With the ADR sheet open (e.g. from an ADR reference\n+button), clicking a Gate Rail card with a `doc` closes the ADR sheet and\n+opens the gate-doc sheet — never both visible at once.\n+**Evidence to check:** browser check — open an ADR sheet, then click a\n+gate-doc-bearing Gate Rail card; confirm the ADR sheet is gone and the\n+gate-doc sheet is showing.\n+**Scoring:**\n+- 1.0 — clean handoff, only one sheet visible at any time.\n+- 0.5 — the old sheet visually lingers briefly but is gone after the click settles.\n+- 0.0 — both sheets stack/overlap, or the click is blocked entirely.\n+\n+### C2 — opening the ADR sheet closes an already-open gate-doc sheet\n+**Must be true:** The reverse direction of C1 also holds — the gate-doc\n+sheet's `openGateDocSheet` closing logic is symmetric with existing\n+`openAdrSheet`/`openAssessmentSheet` calls closing it in turn.\n+**Evidence to check:** browser check — open the gate-doc sheet, then click\n+an ADR reference; confirm the gate-doc sheet closes.\n+**Scoring:**\n+- 1.0 — symmetric close confirmed both directions (this criterion plus C1).\n+- 0.0 — only one direction works.\n+\n+### C3 — a Gate Rail card with no matching doc never opens the sheet [CRITICAL]\n+**Must be true:** Every gate whose `joins.feature_gates` entry has no `doc`\n+field (approved gates from before this feature shipped, or any gate on a\n+feature that predates E1) shows no click response at all — repeats slice\n+4's C2, now checked against multiple real features in this repo, not just\n+one fixture.\n+**Evidence to check:** browser check — spot-check at least two other\n+features' Gate Rails (e.g. `cobuilder-family` if shown, or any other\n+feature's gates) with no `doc` field; confirm none of them open a sheet.\n+**Scoring:**\n+- 1.0 — confirmed inert across at least two real no-doc gates, not just one.\n+- 0.5 — inert for the one fixture case but not verified more broadly.\n+- 0.0 — any no-doc card opens an empty or broken sheet.\n+\n+## Regression check\n+- Slices 4 and 5's criteria still hold.\n+- Comments drawer and audio dialog mutual exclusivity (pre-existing\n+  behavior) is unaffected — spot check both still close correctly relative\n+  to the new sheet.\n+- `python3 -m pytest tests/ -q` still passes.\n+\n+## Out of scope — do not penalise\n+- Any new sheet content correctness — covered by slice 5.\n+- Performance of many rapid open/close cycles.",
  "docs/architecture/adr/ADR-0022-gate-doc-projection-into-the-record-index.md": "diff --git a/docs/architecture/adr/ADR-0022-gate-doc-projection-into-the-record-index.md b/docs/architecture/adr/ADR-0022-gate-doc-projection-into-the-record-index.md\nnew file mode 100644\nindex 0000000..6c51832\n--- /dev/null\n+++ b/docs/architecture/adr/ADR-0022-gate-doc-projection-into-the-record-index.md\n@@ -0,0 +1,126 @@\n+---\n+# --- doc-gardener required frontmatter ---\n+title: \"ADR-0022 — Gate documents projected into the record index\"\n+status: active\n+type: architecture\n+last_verified: 2026-09-01\n+owner: bjornslib\n+# --- 42010 decision-record index (schema: references/decision-records.md §2) ---\n+id: ADR-0022\n+name: \"Gate documents projected into the record index\"\n+state: decided\n+groups: [viewer, data-model]\n+approved_by: \"\"\n+problem: \"Gate 3 program-design docs and Gate 4b epic technical-design docs live under docs/plans/<slug>/, but the viewer's index (ADR-0018) tracks only adr, design, epic, slice, and pull_request entities. A reviewer watching a build's progress in the viewer sees the Gate Rail's approval state but never the document behind a gate, and the workaround — a one-off Claude Artifact publish — drifts from the source and is undiscoverable from the viewer itself.\"\n+decision: \"Add two new index entities, program_design and epic_design, projected from docs/plans/<slug>/03-program-design.md and epic-<id>-design.md by shared/build_index.py, and surface them as a clickable state on the Builds view's existing Gate Rail cards and epic cards, rather than as a new viewer tab or a continued ad hoc Artifact-publish workflow.\"\n+alternatives:\n+  - option: \"A new top-level Plans or Gate Docs tab in the viewer\"\n+    rejected_because: \"ADR-0018 decided one lifecycle surface with a derived record index specifically to stop the reading-surface count growing per artifact type. A gate doc is scoped to a build already shown in the Builds view.\"\n+  - option: \"Parse gate docs client-side in the viewer at render time, skipping the index\"\n+    rejected_because: \"The viewer is one committed HTML file with no filesystem access beyond its bundled data/*.js scripts. ADR-0018's record index exists to resolve joins once, not to be bypassed per artifact type.\"\n+  - option: \"Execute ADR-0020's viewer-parts split as part of this change\"\n+    rejected_because: \"ADR-0020 is decided but explicitly not executed yet. Starting that execution as a side effect of this narrower change is scope it does not own.\"\n+  - option: \"Keep publishing gate docs as one-off Claude Artifacts\"\n+    rejected_because: \"This is the workaround that exposed the gap: no rebuild hook ties it to docs/plans/, so it drifts, and it is not discoverable from the viewer itself.\"\n+forces:\n+  - \"The Builds view (renderBuildsMainContent) already renders a Gate Rail keyed on feature_slug and epic cards keyed on epic id — both keys a gate doc can reuse without a new join table.\"\n+  - \"shared/build_index.py already has a field-projection pattern (GOAL_FIELDS/EPIC_FIELDS/INTENT_FIELDS/ASSESSMENT_FIELDS plus project_fields) that a new entity type should extend, not replace.\"\n+  - \"The viewer ships as one self-contained file today (ADR-0001, narrowed by ADR-0020), so any UI change lands in plugins/artifact/viewer/index.html directly.\"\n+related_decisions:\n+  - { type: depends-on, target: ADR-0018 }\n+  - { type: is-related-to, target: ADR-0020 }\n+history:\n+  - { state: decided, date: 2026-09-01 }\n+maps_to:\n+  context: viewer\n+  modules: [shared/build_index.py, plugins/artifact/viewer/index.html]\n+  rule: \"A gate document is indexed and read-only from the viewer, joined to its build by the same key (feature_slug or epic id) the build's own Gate Rail or epic card already carries. Never a new top-level reading surface for it.\"\n+delivers:\n+  capability: \"A reviewer reads a build's Gate 3 program design or Gate 4b epic design from inside the odyssey viewer, without leaving it for a one-off Artifact or a raw file.\"\n+  benefit: \"The gate doc a reviewer reads is always the current one, rebuilt with the rest of the index, instead of a snapshot that can silently drift from docs/plans/.\"\n+  beneficiary: [operator, developer]\n+related:\n+  - \"docs/architecture/designs/gate-doc-surfacing/goal.json\"\n+---\n+\n+# ADR-0022 — Gate documents projected into the record index\n+\n+## Context\n+\n+`implement`'s build skill produces two authored documents per feature\n+that the viewer never reads: `docs/plans/<slug>/03-program-design.md`\n+(Gate 3, one per feature) and `docs/plans/<slug>/epic-<id>-design.md`\n+(Gate 4b, one per multi-slice epic). `shared/build_index.py`'s\n+`resolve_feature_gates()` already parses `00-status.md` to show each\n+gate's approval state on the Builds view's Gate Rail, but it has never\n+read the gate document's content. A colleague session approving Gate 3\n+asked to see the program design in the viewer and found nothing indexed\n+for it — a real gap, not a bug, because the index's five entity types\n+(`adr`, `design`, `epic`, `slice`, `pull_request`) have no sixth for a\n+gate document. The immediate fix was publishing the doc as a standalone\n+Claude Artifact, which reads well once but has no rebuild hook back to\n+`docs/plans/` and is not discoverable from the viewer itself.\n+\n+## Options considered\n+\n+1. **A new top-level viewer tab for plans or gate docs.** Rejected.\n+   ADR-0018 decided one lifecycle surface precisely to stop the reading\n+   surface count growing with every new artifact type. A gate doc has no\n+   independent existence — it is always scoped to one build, which the\n+   Builds view already renders.\n+2. **Client-side parsing in the viewer at render time.** Rejected. The\n+   viewer is one committed HTML file with access only to its bundled\n+   `data/*.js` scripts, never the raw repository tree, so this option is\n+   not mechanically available even before weighing it against ADR-0018.\n+3. **Execute ADR-0020's viewer-parts split now, and add gate docs to the\n+   new structure.** Rejected. ADR-0020 is decided, not executed — CLAUDE.md\n+   is explicit that `viewer/src/` does not exist yet and the viewer is\n+   still one committed file. Bundling that execution into this narrower\n+   change would make this decision own scope it did not ask for.\n+4. **Two new index entities, surfaced on the existing Gate Rail and epic\n+   cards (chosen).** `program_design` keys on `feature_slug`, matching\n+   `joins.feature_gates`. `epic_design` keys on epic id, matching\n+   `entities.epic`. Both follow the existing `project_fields(source,\n+   fields)` pattern. The viewer makes a Gate Rail card and, where an\n+   approved 4b design exists, an epic card, clickable — opening a\n+   read-only sheet modeled on the existing `openAdrSheet`.\n+\n+## Decision\n+\n+Add `program_design` and `epic_design` as index entities in\n+`shared/build_index.py`, projected from `docs/plans/<slug>/*.md`. Surface\n+them as a clickable state on `plugins/artifact/viewer/index.html`'s\n+existing Gate Rail cards and epic cards, in `renderBuildsMainContent`.\n+No new join table: both entities key on values the Builds view already\n+carries. Out of scope: a new viewer tab, editing a gate doc from the\n+viewer, and any change to how `00-status.md`'s own approval-state line is\n+parsed or displayed.\n+\n+## Consequences\n+\n+- **Positive:** A gate document becomes readable from the one surface\n+  already built to show a build's progress, kept current by the same\n+  `build_index.py` rebuild every other entity relies on.\n+- **Constraint introduced:** A gate document is indexed and read-only from\n+  the viewer, joined to its build by the key the build's own Gate Rail or\n+  epic card already carries. It never gets a new top-level reading\n+  surface.\n+- **Negative / accepted:** The sheet pattern (`openAdrSheet`) was built for\n+  ADR-length text. A long program-design doc may not read as well in the\n+  same component; this is deferred to implementation-time verification\n+  against a real doc, not resolved here.\n+\n+## Value delivered\n+\n+- **New capability:** A reviewer reads a build's Gate 3 or Gate 4b document\n+  from inside the odyssey viewer.\n+- **Benefit:** The document a reviewer sees is always the current one,\n+  rebuilt with the index, rather than a one-off Artifact snapshot that can\n+  drift from `docs/plans/` with no warning.\n+- **Beneficiary:** operator, developer.\n+\n+## Maps to\n+\n+Context `viewer`, module(s) `shared/build_index.py`,\n+`plugins/artifact/viewer/index.html`. See the boundary record's rule and\n+the context canvas.",
  "docs/architecture/designs/gate-doc-surfacing/assessment.json": "diff --git a/docs/architecture/designs/gate-doc-surfacing/assessment.json b/docs/architecture/designs/gate-doc-surfacing/assessment.json\nnew file mode 100644\nindex 0000000..7358f3b\n--- /dev/null\n+++ b/docs/architecture/designs/gate-doc-surfacing/assessment.json\n@@ -0,0 +1,42 @@\n+{\n+  \"stage\": \"design\",\n+  \"verdict\": \"proceed\",\n+  \"risk_tier\": \"low\",\n+  \"regret_risk\": \"low\",\n+  \"summary\": \"The projection follows an established pattern exactly (project_fields, GOAL_FIELDS-style tuples) and the viewer attachment point (Gate Rail, epic cards) already exists and already has a sheet precedent (openAdrSheet). The main open risk is UX scale — gate docs are prose, unlike the short structured records the sheet pattern was built for — not architectural fit.\",\n+  \"findings\": [\n+    {\n+      \"kind\": \"prediction\",\n+      \"id\": \"F1\",\n+      \"severity\": \"low\",\n+      \"title\": \"New entities follow the existing projection pattern with no new join mechanism\",\n+      \"detail\": \"program_design and epic_design key on feature_slug and epic_id respectively, both of which the index already carries (joins.feature_gates is keyed by feature_slug; entities.epic already has an id). No new join table is needed — the viewer looks the doc up by a key it already has when rendering the Gate Rail or an epic card.\",\n+      \"cites\": \"ADR-0018 (record index resolves joins, not per-artifact ad hoc lookups); shared/build_index.py's project_fields pattern\",\n+      \"mitigation\": \"None needed if the field-projection constants for the two new entities follow GOAL_FIELDS/EPIC_FIELDS exactly, as intent.json's approach specifies.\"\n+    },\n+    {\n+      \"kind\": \"prediction\",\n+      \"id\": \"F2\",\n+      \"severity\": \"medium\",\n+      \"title\": \"The sheet UX was built for short structured text, not multi-hundred-line prose\",\n+      \"detail\": \"openAdrSheet renders ADR frontmatter plus a body that is typically well under a couple hundred lines. A 03-program-design.md or epic-<id>-design.md can run considerably longer once Files/Types/Call stack/Test plan sections are all filled in. Untested at this stage whether the same sheet component reads well at that length.\",\n+      \"cites\": \"district: viewer; intent.json unknowns\",\n+      \"mitigation\": \"Confirm during implementation against a real gate doc from this repo's own docs/plans/ tree (e.g. cobuilder-family's 03-program-design.md) before considering the epic done. If unreadable, scroll the sheet body rather than truncate — dropping content silently would misrepresent the record.\"\n+    },\n+    {\n+      \"kind\": \"prediction\",\n+      \"id\": \"F3\",\n+      \"severity\": \"low\",\n+      \"title\": \"A design with no docs/plans/<slug>/ directory renders an empty or absent Gate Rail — undecided which\",\n+      \"detail\": \"Every existing backlog design (maintainable-viewer, inflight-record-store, and now gate-doc-surfacing itself) has no docs/plans/ directory yet, because implement mode hasn't started on them. The Gate Rail today falls back to a hardcoded four-gate placeholder (renderBuildsMainContent's featureGatesMap fallback) when no real gates exist for 'cobuilder-family'. This design does not resolve whether that fallback is still appropriate once real per-design gate data exists for other designs.\",\n+      \"cites\": \"plugins/artifact/viewer/index.html:3212-3219 (gate fallback literal)\",\n+      \"mitigation\": \"Scope this to E2's implementation: render nothing for a design with no docs/plans/ directory, rather than the current single hardcoded fallback, which only ever matched one feature.\"\n+    }\n+  ],\n+  \"boundary_checks\": [\n+    { \"rule\": \"No agents, no hooks, no MCP servers in the install surface\", \"result\": \"holds\", \"note\": \"This adds index-projection code and viewer markup only, both already part of the artifact plugin's existing surface.\" },\n+    { \"rule\": \"The viewer stays one self-contained committed file (current, pre-ADR-0020-execution shape)\", \"result\": \"holds\", \"note\": \"E2 edits plugins/artifact/viewer/index.html directly. No viewer/src/ split is introduced by this design.\" },\n+    { \"rule\": \"docs/ stays authored source; .cobuilder-architect/ stays derived\", \"result\": \"holds\", \"note\": \"The new entities are derived projections of docs/plans/*.md into data/index.json, the same relationship every other entity in the index already has to its docs/ source.\" }\n+  ],\n+  \"drift\": []\n+}",
  "docs/architecture/designs/gate-doc-surfacing/diagrams/level-1.mmd": "diff --git a/docs/architecture/designs/gate-doc-surfacing/diagrams/level-1.mmd b/docs/architecture/designs/gate-doc-surfacing/diagrams/level-1.mmd\nnew file mode 100644\nindex 0000000..8357d45\n--- /dev/null\n+++ b/docs/architecture/designs/gate-doc-surfacing/diagrams/level-1.mmd\n@@ -0,0 +1,30 @@\n+C4Container\n+    title Gate doc surfacing — program and epic design docs reach the Builds view\n+\n+    Person(engineer, \"Engineer\", \"Reads a build's gate rail and backlog lane in the viewer\")\n+\n+    Container_Boundary(docsdir, \"docs/plans/<slug>/ — authored\") {\n+        Container(status, \"00-status.md\", \"Markdown\", \"Gate approval state, parsed by resolve_feature_gates()\")\n+        Container(progdesign, \"03-program-design.md\", \"Markdown\", \"Gate 3 program-design doc, one per feature slug\")\n+        Container(epicdesign, \"epic-<id>-design.md\", \"Markdown\", \"Gate 4b epic technical-design doc, one per multi-slice epic\")\n+    }\n+\n+    Container_Boundary(shared, \"shared/ — index build\") {\n+        Container(buildindex, \"build_index.py\", \"Python\", \"project_fields() projects program_design and epic_design entities\")\n+    }\n+\n+    ContainerDb(indexjson, \"data/index.json\", \"JSON\", \"joins.feature_gates carries a doc-reference field per gate; program_design and epic_design entities keyed by feature_slug and epic id\")\n+\n+    Container_Boundary(viewerdir, \"plugins/artifact/viewer/\") {\n+        Container(viewer, \"index.html\", \"Single-file HTML/JS\", \"renderBuildsMainContent(), gate-rail-grid, renderEpicCard()\")\n+        Container(sheet, \"Gate/epic design sheet\", \"JS modal\", \"Read-only, modeled on openAdrSheet()\")\n+    }\n+\n+    Rel(engineer, viewer, \"Opens the Builds view\")\n+    Rel(status, buildindex, \"Gate approval state parsed by resolve_feature_gates()\")\n+    Rel(progdesign, buildindex, \"Projected as program_design entity\")\n+    Rel(epicdesign, buildindex, \"Projected as epic_design entity\")\n+    Rel(buildindex, indexjson, \"Rebuilds joins.feature_gates plus the two new entities\")\n+    Rel(indexjson, viewer, \"Loaded as window.INDEX\")\n+    Rel(viewer, sheet, \"Gate card or design-doc chip click opens\")\n+    Rel(sheet, engineer, \"Shows gate doc or epic design content, read-only\")",
  "docs/architecture/designs/gate-doc-surfacing/diagrams/level-2.mmd": "diff --git a/docs/architecture/designs/gate-doc-surfacing/diagrams/level-2.mmd b/docs/architecture/designs/gate-doc-surfacing/diagrams/level-2.mmd\nnew file mode 100644\nindex 0000000..444b7c0\n--- /dev/null\n+++ b/docs/architecture/designs/gate-doc-surfacing/diagrams/level-2.mmd\n@@ -0,0 +1,37 @@\n+sequenceDiagram\n+    participant Author as Engineer (authoring)\n+    participant Status as 00-status.md\n+    participant ProgDoc as 03-program-design.md\n+    participant EpicDoc as epic-<id>-design.md\n+    participant BuildIndex as build_index.py\n+    participant IndexJson as data/index.json\n+    participant Viewer as viewer/index.html\n+    participant Sheet as design-doc sheet (openAdrSheet pattern)\n+    participant Reader as Engineer (reading)\n+\n+    Author->>Status: Marks Gate 3 / Gate 4b approved\n+    Author->>ProgDoc: Writes program-design doc\n+    Author->>EpicDoc: Writes epic technical-design doc\n+\n+    Note over BuildIndex: Index rebuild (odyssey / implement modes call this)\n+    BuildIndex->>Status: resolve_feature_gates() parses gate approval state\n+    BuildIndex->>ProgDoc: project_fields(source, fields) reads doc if present\n+    BuildIndex->>EpicDoc: project_fields(source, fields) reads doc if present\n+    BuildIndex->>IndexJson: Writes program_design entity keyed by feature_slug\n+    BuildIndex->>IndexJson: Writes epic_design entity keyed by epic id\n+    BuildIndex->>IndexJson: Writes joins.feature_gates with doc-reference field per gate\n+\n+    Reader->>Viewer: Opens Builds view\n+    Viewer->>IndexJson: Reads window.INDEX\n+    Viewer->>Viewer: renderBuildsMainContent() renders gate-rail-grid\n+    Viewer->>Viewer: renderEpicCard() adds design-doc chip when epic_design is approved\n+\n+    Reader->>Viewer: Clicks a gate card in gate-rail-grid\n+    Viewer->>Sheet: openGateDesignSheet(feature_slug, gate_n)\n+    Sheet->>IndexJson: Looks up joins.feature_gates doc reference\n+    Sheet-->>Reader: Renders program-design doc content, read-only\n+\n+    Reader->>Viewer: Clicks a design-doc chip on an epic card\n+    Viewer->>Sheet: openGateDesignSheet(epic_id, scope=\"epic\")\n+    Sheet->>IndexJson: Looks up epic_design entity by epic id\n+    Sheet-->>Reader: Renders epic technical-design doc content, read-only",
  "docs/architecture/designs/gate-doc-surfacing/diagrams/level-3.mmd": "diff --git a/docs/architecture/designs/gate-doc-surfacing/diagrams/level-3.mmd b/docs/architecture/designs/gate-doc-surfacing/diagrams/level-3.mmd\nnew file mode 100644\nindex 0000000..e371fe4\n--- /dev/null\n+++ b/docs/architecture/designs/gate-doc-surfacing/diagrams/level-3.mmd\n@@ -0,0 +1,81 @@\n+classDiagram\n+    class project_fields {\n+        +project_fields(source, fields) dict\n+    }\n+\n+    class ProgramDesignEntity {\n+        <<index entity>>\n+        +feature_slug string\n+        +path string\n+        +fields dict\n+    }\n+\n+    class EpicDesignEntity {\n+        <<index entity>>\n+        +epic_id string\n+        +path string\n+        +fields dict\n+        +approved bool\n+    }\n+\n+    class resolve_feature_gates {\n+        +resolve_feature_gates(slug) list~GateEntry~\n+    }\n+\n+    class GateEntry {\n+        +n int\n+        +name string\n+        +state string\n+        +doc_ref string\n+    }\n+\n+    class BuildIndexScript {\n+        <<shared/build_index.py>>\n+        +collect_program_designs() dict\n+        +collect_epic_designs() dict\n+        +build_index() void\n+    }\n+\n+    class IndexJson {\n+        <<data/index.json>>\n+        +program_design dict\n+        +epic_design dict\n+        +joins.feature_gates list~GateEntry~\n+    }\n+\n+    class renderBuildsMainContent {\n+        <<viewer function>>\n+        +renderBuildsMainContent(main) void\n+    }\n+\n+    class renderEpicCard {\n+        <<viewer function>>\n+        +renderEpicCard(epic) string\n+    }\n+\n+    class openAdrSheet {\n+        <<existing viewer function>>\n+        +openAdrSheet(id) void\n+    }\n+\n+    class openGateDesignSheet {\n+        <<new viewer function>>\n+        +openGateDesignSheet(key, scope) void\n+    }\n+\n+    BuildIndexScript --> project_fields : uses for both entities\n+    BuildIndexScript --> resolve_feature_gates : calls, then attaches doc_ref\n+    resolve_feature_gates --> GateEntry : produces list of\n+    project_fields --> ProgramDesignEntity : produces\n+    project_fields --> EpicDesignEntity : produces\n+    BuildIndexScript --> IndexJson : writes program_design, epic_design, joins.feature_gates\n+    GateEntry --> IndexJson : embedded in joins.feature_gates\n+\n+    renderBuildsMainContent --> IndexJson : reads window.INDEX\n+    renderBuildsMainContent --> renderEpicCard : calls per epic\n+    renderEpicCard --> EpicDesignEntity : reads approved flag for chip\n+    renderBuildsMainContent --> openGateDesignSheet : gate-card click handler\n+    renderEpicCard --> openGateDesignSheet : design-doc chip click handler\n+    openGateDesignSheet ..> openAdrSheet : modeled on\n+\n+    note for openGateDesignSheet \"Read-only sheet, scoped by feature_slug\\nfor a gate doc or epic id for an epic design doc\"",
  "docs/architecture/designs/gate-doc-surfacing/goal.json": "diff --git a/docs/architecture/designs/gate-doc-surfacing/goal.json b/docs/architecture/designs/gate-doc-surfacing/goal.json\nnew file mode 100644\nindex 0000000..e811071\n--- /dev/null\n+++ b/docs/architecture/designs/gate-doc-surfacing/goal.json\n@@ -0,0 +1,55 @@\n+{\n+  \"name\": \"gate-doc-surfacing\",\n+  \"title\": \"Surface Gate 3 and Gate 4b docs in the Builds view\",\n+  \"adr\": \"ADR-0022\",\n+  \"adrs\": [\n+    \"ADR-0022\",\n+    \"ADR-0018\",\n+    \"ADR-0020\"\n+  ],\n+  \"created\": \"2026-09-01\",\n+  \"outcome\": \"Gate documents (03-program-design.md, epic-<id>-design.md) are readable from the viewer's Builds view, not just from docs/plans/ or a one-off Artifact publish.\",\n+  \"done_when\": [\n+    \"Gate Rail cards in the Builds view open a sheet showing the gate's doc content\",\n+    \"Epic cards with an approved 4b design show a design-doc chip that opens the same sheet type\",\n+    \"data/index.json carries program_design and epic_design entities projected from docs/plans/<slug>/03-program-design.md and epic-<id>-design.md\"\n+  ],\n+  \"abort_if\": [\n+    \"a gate's doc content duplicates what 00-status.md's approval-state line already shows well enough that a sheet adds no reading value\"\n+  ],\n+  \"min_work\": {\n+    \"derived_from\": \"conversation: colleague session couldn't view Gate 3 progress in the viewer; agreed the Builds Gate Rail is the right home over a new tab or continued artifact publishing\",\n+    \"challenge_stage_run\": true,\n+    \"note\": \"Design mode ran to completion on 2026-09-01: grounded against ADR-0018/ADR-0020 and the viewer/build_index.py source, interviewed, challenged (four alternatives rejected, recorded in intent.json and ADR-0022), drafted ADR-0022, intent.json, assessment.json, narrative.json, pr-draft.md, and diagrams/level-{1,2,3}.mmd. Reviewed and approved in one round, no material feedback.\"\n+  },\n+  \"limits\": {\n+    \"warn_after_rounds\": 6,\n+    \"cutoff_rounds\": 10\n+  },\n+  \"epics\": [\n+    {\n+      \"id\": \"E1\",\n+      \"branch\": \"design/gate-doc-surfacing\",\n+      \"pr\": null,\n+      \"state\": \"completed\",\n+      \"note\": \"shared/build_index.py: add PROGRAM_DESIGN_FIELDS and EPIC_DESIGN_FIELDS projection tuples plus project_program_design/project_epic_design, following the project_fields(source, fields) pattern used by project_goal/project_intent/project_assessment. Parse docs/plans/<slug>/03-program-design.md and epic-<id>-design.md; emit program_design and epic_design entities into data/index.json. Extend feature_gates join entries with a doc reference where one exists.\"\n+    },\n+    {\n+      \"id\": \"E2\",\n+      \"branch\": \"design/gate-doc-surfacing\",\n+      \"pr\": null,\n+      \"state\": \"completed\",\n+      \"note\": \"plugins/artifact/viewer/index.html: make gate-rail-grid cards (renderBuildsMainContent, ~line 3249) clickable, opening a gate-doc sheet modeled on openAdrSheet. Add a design-doc chip to renderEpicCard (~line 3282) for epics with an epic_design entity.\"\n+    }\n+  ],\n+  \"stage\": \"decided\",\n+  \"supersedes\": null,\n+  \"rounds\": [\n+    {\n+      \"n\": 1,\n+      \"date\": \"2026-09-01\",\n+      \"result\": \"approved\",\n+      \"material_change\": false\n+    }\n+  ]\n+}",
  "docs/architecture/designs/gate-doc-surfacing/intent.json": "diff --git a/docs/architecture/designs/gate-doc-surfacing/intent.json b/docs/architecture/designs/gate-doc-surfacing/intent.json\nnew file mode 100644\nindex 0000000..d14a380\n--- /dev/null\n+++ b/docs/architecture/designs/gate-doc-surfacing/intent.json\n@@ -0,0 +1,49 @@\n+{\n+  \"captured\": \"2026-09-01\",\n+  \"source\": \"design\",\n+  \"design_name\": \"gate-doc-surfacing\",\n+  \"authorship\": \"agent-assisted\",\n+  \"problem\": \"Gate 3 program-design docs and Gate 4b epic technical-design docs live at docs/plans/<slug>/*.md, but the viewer's index tracks only adr, design, epic, slice, and pull_request entities. A reviewer who wants to watch a build's Gate 3 progress in the viewer sees nothing — the Gate Rail already shows approval state (00-status.md), but never the document content behind it. The workaround was publishing the doc as a one-off Claude Artifact: readable, but disconnected from docs/plans/, no rebuild hook, and undiscoverable from the viewer itself.\",\n+  \"why_now\": \"The prior session's Gate 3 approval loop surfaced this live: the engineer asked to see the design in the viewer, the agent found no entity for it, and improvised an Artifact instead. That workaround is now a repeatable gap, not a one-time surprise, and the viewer's Builds view (renderBuildsMainContent) already renders a Gate Rail and epic cards that are the natural attachment point.\",\n+  \"approach\": \"Extend shared/build_index.py to project docs/plans/<slug>/03-program-design.md and epic-<id>-design.md into two new index entities, program_design and epic_design, following the existing GOAL_FIELDS/project_fields(source, fields) pattern. Extend joins.feature_gates entries with a doc reference where one exists. In the viewer, make Gate Rail cards clickable, opening a sheet modeled on openAdrSheet, and add a design-doc chip to epic cards that have an approved 4b design.\",\n+  \"alternatives\": [\n+    {\n+      \"option\": \"Add a new top-level 'Plans' or 'Gate Docs' tab in the viewer\",\n+      \"rejected_because\": \"ADR-0018 decided one lifecycle surface with a derived record index precisely to stop the reading surface count from growing per artifact type. A gate doc is scoped to a build already living in the viewer's Builds view (district: viewer) — a sibling tab duplicates that context instead of joining it.\"\n+    },\n+    {\n+      \"option\": \"Parse gate docs ad hoc in the viewer's client-side JS at render time, skipping the index\",\n+      \"rejected_because\": \"ADR-0018's whole point is that joins get resolved once, in build_index.py, not scattered across client fetches. The viewer is also a single committed HTML file with no direct filesystem access model beyond its bundled data/ scripts (district: viewer) — it cannot read docs/plans/ at render time at all.\"\n+    },\n+    {\n+      \"option\": \"Split viewer/index.html into parts now, and add gate docs to the new module structure\",\n+      \"rejected_because\": \"ADR-0020 decided the viewer-parts split but is explicitly not executed yet (CLAUDE.md: 'treat that ADR as a plan, not as the current shape of the file'). Starting that execution as a side effect of this design is scope creep this design does not own.\"\n+    },\n+    {\n+      \"option\": \"Keep publishing gate docs as one-off Claude Artifacts\",\n+      \"rejected_because\": \"This is the workaround that exposed the gap: no rebuild hook ties it to docs/plans/, so it drifts, and it is not discoverable from the viewer itself — the problem this design exists to fix.\"\n+    }\n+  ],\n+  \"out_of_scope\": [\n+    \"a new top-level viewer tab for plans or gate docs\",\n+    \"editing a gate doc from the viewer — read-only sheet only\",\n+    \"changing how 00-status.md's approval state is parsed or displayed\",\n+    \"PDF or standalone-artifact export of a gate doc\",\n+    \"executing ADR-0020's viewer-parts split — this design writes into the current monolithic viewer/index.html, the file that ships today\"\n+  ],\n+  \"risks\": [\n+    \"a gate doc grows long enough that the sheet reading UX (built for ADR text, a few hundred lines) degrades\",\n+    \"program_design/epic_design entity ids collide with, or get confused with, the existing epic entity's <design>/<epic-id> scoping\",\n+    \"the sheet becomes stale between build_index.py runs the same way the pre-fix Artifact was, if a future gate step forgets to rebuild — mitigated by the just-added viewer-rebuild-per-gate step in implement/skills/build/SKILL.md's approval protocol\"\n+  ],\n+  \"testing\": \"Not yet built. shared/build_index.py already has field-projection tests for GOAL_FIELDS/EPIC_FIELDS/etc — the new PROGRAM_DESIGN_FIELDS/EPIC_DESIGN_FIELDS constants and their project_* functions should get the same coverage shape.\",\n+  \"reviewer_focus\": [\n+    \"whether program_design/epic_design entity ids collide with the epic entity's own id scheme\",\n+    \"whether the sheet UX holds up for a long program-design doc\",\n+    \"whether the Gate Rail card / epic chip is discoverable without extra instruction\"\n+  ],\n+  \"unknowns\": [\n+    \"how large a typical 03-program-design.md or epic-<id>-design.md gets across real features, and whether the sheet needs pagination or truncation\",\n+    \"whether a design with no docs/plans/<slug>/ directory at all (a design never taken to implement mode) should render an empty Gate Rail or hide it entirely\"\n+  ]\n+}",
  "docs/architecture/designs/gate-doc-surfacing/narrative.json": "diff --git a/docs/architecture/designs/gate-doc-surfacing/narrative.json b/docs/architecture/designs/gate-doc-surfacing/narrative.json\nnew file mode 100644\nindex 0000000..62117a8\n--- /dev/null\n+++ b/docs/architecture/designs/gate-doc-surfacing/narrative.json\n@@ -0,0 +1,56 @@\n+{\n+  \"landscape\": {\n+    \"narration\": \"This design closes a visibility gap in the odyssey viewer: a build's Gate 3 program design and Gate 4b epic designs stay invisible to the one surface built to show build progress, so someone reviewing a build has to leave the viewer to read them.\",\n+    \"tagline\": \"Show the gate document, not just the gate's approval state.\"\n+  },\n+  \"problem_solution\": {\n+    \"narration\": \"The Builds view already shows a Gate Rail of approval state, keyed off 00-status.md. It never carries the document behind each gate. A reviewer who wants to read Gate 3 has no path from the viewer to the doc, and the one-off fix — publishing the doc as a Claude Artifact — drifts from the source and is not discoverable from the viewer itself.\",\n+    \"problem\": \"Gate 3 and Gate 4b documents exist under docs/plans/<slug>/ but have no entity in the viewer's index, so they never appear in the Builds view no matter how often the bundle rebuilds.\",\n+    \"solution\": \"Project the two document types into two new index entities, program_design and epic_design, then make the Gate Rail cards and epic cards that already exist in the Builds view open a sheet showing that content.\",\n+    \"beats\": [\n+      {\n+        \"kind\": \"background\",\n+        \"text\": \"A colleague session tried to open the viewer mid-Gate-3 and found nothing there. The Gate Rail's approval-state chrome was current; the document behind it was not indexed at all.\"\n+      },\n+      {\n+        \"kind\": \"intuition\",\n+        \"text\": \"The Builds view already has the join key a gate doc needs — feature_slug for a program design, epic id for an epic design. Attach the doc to the card that already represents that build, instead of building a new surface to hold it.\"\n+      }\n+    ],\n+    \"alternatives\": [\n+      {\n+        \"option\": \"A new top-level Plans tab\",\n+        \"rejected\": \"ADR-0018 decided one lifecycle surface. A gate doc is scoped to a build already shown in Builds.\"\n+      },\n+      {\n+        \"option\": \"Parse gate docs client-side, skipping the index\",\n+        \"rejected\": \"The viewer is one committed HTML file with no filesystem access beyond its bundled data/ scripts.\"\n+      },\n+      {\n+        \"option\": \"Execute ADR-0020's viewer-parts split as part of this design\",\n+        \"rejected\": \"ADR-0020 is decided but not executed. Starting that execution here is scope this design does not own.\"\n+      },\n+      {\n+        \"option\": \"Keep publishing gate docs as one-off Artifacts\",\n+        \"rejected\": \"No rebuild hook, drifts from docs/plans/, undiscoverable from the viewer — the problem this design fixes.\"\n+      }\n+    ]\n+  },\n+  \"architecture\": {\n+    \"narration\": \"shared/build_index.py gains two field-projection constants and two loader functions, following the same project_fields(source, fields) shape every other entity already uses. The viewer gains a sheet, reusing openAdrSheet's pattern, wired to the Gate Rail and epic cards that renderBuildsMainContent already renders.\",\n+    \"beats\": [\n+      {\n+        \"kind\": \"forces\",\n+        \"text\": \"The viewer stays one self-contained file today, so this attaches to plugins/artifact/viewer/index.html directly, not to a future viewer/src/ split.\"\n+      },\n+      {\n+        \"kind\": \"contract\",\n+        \"text\": \"program_design keys on feature_slug, matching joins.feature_gates. epic_design keys on epic id, matching entities.epic. No new join table.\"\n+      },\n+      {\n+        \"kind\": \"boundary\",\n+        \"text\": \"Read-only. No editing a gate doc from the viewer, no change to how 00-status.md's approval state itself is parsed or displayed.\"\n+      }\n+    ]\n+  }\n+}",
  "docs/architecture/designs/gate-doc-surfacing/pr-draft.md": "diff --git a/docs/architecture/designs/gate-doc-surfacing/pr-draft.md b/docs/architecture/designs/gate-doc-surfacing/pr-draft.md\nnew file mode 100644\nindex 0000000..4bd40ab\n--- /dev/null\n+++ b/docs/architecture/designs/gate-doc-surfacing/pr-draft.md\n@@ -0,0 +1,91 @@\n+## Problem\n+\n+Gate 3 program-design docs (`03-program-design.md`) and Gate 4b epic\n+technical-design docs (`epic-<id>-design.md`) live under\n+`docs/plans/<slug>/`, but the viewer's index tracks only five entity\n+types — `adr`, `design`, `epic`, `slice`, `pull_request`. A build's Gate\n+Rail in the Builds view already shows approval state, read from\n+`00-status.md`, but the document behind each gate never appears. A\n+colleague session mid-Gate-3 hit this directly: asked the viewer to show\n+the design, found nothing indexed, and worked around it by publishing the\n+doc as a one-off Claude Artifact.\n+\n+## Why this approach\n+\n+Add two new index entities, `program_design` and `epic_design`, projected\n+from `docs/plans/<slug>/*.md` by `shared/build_index.py`, following the\n+same `project_fields(source, fields)` pattern every other entity already\n+uses (`GOAL_FIELDS`, `EPIC_FIELDS`, `INTENT_FIELDS`, `ASSESSMENT_FIELDS`).\n+Key `program_design` on `feature_slug`, matching `joins.feature_gates`.\n+Key `epic_design` on epic id, matching `entities.epic`. No new join table.\n+\n+In the viewer, make the Gate Rail cards in `renderBuildsMainContent`\n+clickable, opening a read-only sheet modeled on the existing\n+`openAdrSheet`. Add a design-doc chip to an epic card that has an approved\n+4b design, opening the same sheet type scoped to that epic.\n+\n+## Alternatives considered\n+\n+- **A new top-level Plans tab** — rejected because ADR-0018 decided one\n+  lifecycle surface with a derived record index specifically to stop the\n+  reading-surface count growing per artifact type. A gate doc is scoped to\n+  a build already shown in the Builds view.\n+- **Parse gate docs client-side at render time, skipping the index** —\n+  rejected because the viewer is one committed HTML file with no\n+  filesystem access beyond its bundled `data/*.js` scripts, and because\n+  ADR-0018's whole point is resolving joins once, in `build_index.py`.\n+- **Execute ADR-0020's viewer-parts split as part of this design** —\n+  rejected because ADR-0020 is decided but explicitly not executed yet.\n+  Starting that execution as a side effect here is scope this design does\n+  not own.\n+- **Keep publishing gate docs as one-off Claude Artifacts** — rejected\n+  because it is the workaround that exposed the gap: no rebuild hook ties\n+  it to `docs/plans/`, so it drifts, and it is not discoverable from the\n+  viewer itself.\n+\n+## Out of scope\n+\n+- A new top-level viewer tab.\n+- Editing a gate doc from the viewer — read-only sheet only.\n+- Changing how `00-status.md`'s approval state is parsed or displayed.\n+- PDF or standalone-artifact export of a gate doc.\n+- Executing ADR-0020's viewer-parts split.\n+\n+## Risks\n+\n+- A gate doc can run considerably longer than the ADR text the sheet\n+  pattern was built for. Untested whether the same sheet component reads\n+  well at that length.\n+- `program_design`/`epic_design` entity ids could be confused with the\n+  existing epic entity's `<design>/<epic-id>` scoping if not kept\n+  distinct.\n+- The sheet goes stale if a future gate step skips the bundle rebuild —\n+  mitigated by the viewer-rebuild-per-gate step already added to\n+  `implement/skills/build/SKILL.md`'s approval protocol.\n+\n+## How this was tested\n+\n+No code changes yet; this design writes `docs/` artifacts only.\n+Verification for this pass: `goal.json` parses, `build_index.py` runs\n+clean and picks up the new backlog design, and the existing 312-test\n+packaging suite passes unaffected. Implementation-time verification is\n+described in `goal.json.done_when`.\n+\n+## Where to focus\n+\n+- Whether the sheet UX holds up for a real, long program-design doc — try\n+  it against this repo's own `docs/plans/cobuilder-family/03-program-design.md`.\n+- Whether `program_design`/`epic_design` entity ids collide with, or read\n+  as confusable with, the epic entity's own id scheme.\n+- Whether the Gate Rail's current hardcoded four-gate fallback for a\n+  design with no `docs/plans/<slug>/` directory still makes sense once\n+  real per-design gate data exists for designs other than\n+  `cobuilder-family`.\n+\n+The author flagged these parts as not fully understood:\n+- How large a typical gate doc gets across real features in practice —\n+  unverified beyond this repo's own examples.\n+\n+---\n+\n+Authorship: agent-generated.",
  "docs/plans/gate-doc-surfacing/00-status.md": "diff --git a/docs/plans/gate-doc-surfacing/00-status.md b/docs/plans/gate-doc-surfacing/00-status.md\nnew file mode 100644\nindex 0000000..f6864ef\n--- /dev/null\n+++ b/docs/plans/gate-doc-surfacing/00-status.md\n@@ -0,0 +1,49 @@\n+# Status: Gate doc surfacing\n+\n+- Gate 1 — Product: APPROVED 2026-09-01\n+- Gate 2 — Architecture: APPROVED 2026-09-01\n+- Gate 3 — Program Design: APPROVED 2026-09-01\n+- Gate 4 — Slice plan, epic designs, and rubrics: APPROVED 2026-09-01\n+  - 4a Slice plan: APPROVED 2026-09-01\n+  - 4b Epic technical solution designs: APPROVED 2026-09-01\n+  - 4c Blind rubrics: APPROVED 2026-09-01\n+\n+Design mode: gate-doc-surfacing | none | declined\n+Hindsight: unavailable\n+\n+## Slices\n+- [x] Slice 1 — E1 tracer bullet: constants and empty entity lists   score: 1.00\n+- [x] Slice 2 — E1 real parsing: project Gate 3/4b docs              score: 1.00\n+- [x] Slice 3 — E1 edge cases                                        score: 1.00\n+- [x] Slice 4 — E2 tracer bullet: clickable Gate Rail → empty sheet  score: 1.00\n+- [x] Slice 5 — E2 real content: render body, add epic chip          score: 1.00\n+- [x] Slice 6 — E2 edge cases: no-doc cards, mutual exclusivity      score: 1.00\n+\n+## Escalated\n+none yet\n+\n+## Dogfood note\n+Verified live via claude-in-chrome against a locally served\n+`.cobuilder-architect/self/` bundle: this feature's own Gate 3 card\n+(gate-doc-surfacing) opens its real `03-program-design.md`; E1/E2 chips\n+open their own distinct `epic-*-design.md` docs; a no-doc gate card\n+(e.g. Gate 1) stays inert; sheet close button works. One environment gap\n+found and worked around: `migrate_bundle.py`'s vendored copy under the\n+plugin cache can't locate this repo's own `viewer/index.html` source (it\n+looks under its own `PLUGIN_ROOT`, which resolves inside the cache, not\n+this checkout) — `.cobuilder-architect/self/viewer/index.html` was\n+refreshed with a direct `cp` instead. Worth a follow-up for anyone\n+developing this plugin family from a local checkout rather than an\n+installed marketplace copy.\n+\n+## Notes for a fresh session\n+Design record: `docs/architecture/designs/gate-doc-surfacing/` (ADR-0022).\n+This run executed the full gate sequence end-to-end at the user's explicit\n+request, dogfooding the just-added \"rebuild the viewer at every gate\"\n+step in `implement`'s approval protocol. Gates 1-4 were drafted directly\n+by the orchestrating session (not interviewed live) because the requester\n+had already worked through the problem, approach, and boundaries in the\n+preceding design-mode conversation — the gate documents ground themselves\n+in that same context rather than starting blank. Approvals were recorded\n+by the orchestrating session itself under that same explicit end-to-end\n+instruction, not asked turn-by-turn.",
  "docs/plans/gate-doc-surfacing/01-product.md": "diff --git a/docs/plans/gate-doc-surfacing/01-product.md b/docs/plans/gate-doc-surfacing/01-product.md\nnew file mode 100644\nindex 0000000..d84b567\n--- /dev/null\n+++ b/docs/plans/gate-doc-surfacing/01-product.md\n@@ -0,0 +1,25 @@\n+# Product: Gate doc surfacing\n+\n+## Problem\n+A reviewer opens the bundle viewer to check on a build's progress and finds\n+the Gate Rail's approval checkmarks, but not the document behind each gate.\n+Gate 3's program design and Gate 4b's epic designs stay invisible, so the\n+reviewer has to leave the viewer and read raw files, or wait for someone to\n+publish a one-off copy elsewhere.\n+\n+## Success metric\n+A reviewer clicks a Gate Rail card or an epic's design chip in the Builds\n+view and reads that gate's document without leaving the viewer. Measured by:\n+this feature's own dogfood run against the `gate-doc-surfacing` design's own\n+build shows Gate 3 and Gate 4b content in the running viewer.\n+\n+## Announcement — the blog post before the feature\n+The Builds view now shows what a gate actually decided, not just that it was\n+approved. Click any Gate Rail card to read that gate's document inline. An\n+epic with an approved technical design carries a small chip that opens the\n+same reading view, scoped to that epic. Nothing to configure — the next\n+bundle rebuild picks up any gate doc already on disk.\n+\n+## Screens\n+- `mockups/gate-sheet.html` — the read-only sheet showing a gate document's\n+  content, modeled on the existing ADR sheet.",
  "docs/plans/gate-doc-surfacing/02-architecture.md": "diff --git a/docs/plans/gate-doc-surfacing/02-architecture.md b/docs/plans/gate-doc-surfacing/02-architecture.md\nnew file mode 100644\nindex 0000000..ae0bdf4\n--- /dev/null\n+++ b/docs/plans/gate-doc-surfacing/02-architecture.md\n@@ -0,0 +1,49 @@\n+# Architecture: Gate doc surfacing\n+\n+## Fit\n+- `shared/build_index.py` — existing `collect_designs()` (build_index.py:342-457)\n+  and its `GOAL_FIELDS`/`EPIC_FIELDS`/`project_fields()` pattern\n+  (build_index.py:230-268) get two siblings: `PROGRAM_DESIGN_FIELDS`,\n+  `EPIC_DESIGN_FIELDS`, `project_program_design()`, `project_epic_design()`.\n+  `resolve_feature_gates()` (build_index.py:967-990) gains an optional\n+  `doc` key per gate entry.\n+- `plugins/artifact/viewer/index.html` — `renderBuildsMainContent()`\n+  (~line 3202), its `gate-rail-grid` cards (~line 3249) and\n+  `renderEpicCard()` (~line 3282) get click handlers. The existing\n+  `openAdrSheet`/`renderAdrSheet`/`renderMarkdown` (lines 3978-4082) give a\n+  ready-made sheet and markdown renderer to reuse for the new sheet, rather\n+  than building either from scratch.\n+\n+## Endpoints\n+none — this is a static-bundle projection and client-side viewer change,\n+no server.\n+\n+## Data\n+- `data/index.json` gains two entity arrays: `program_design` and\n+  `epic_design`.\n+  - `program_design`: `{feature_slug, gate: 3, title, body_md, approved_date, source_path}`\n+  - `epic_design`: `{epic_id, feature_slug, title, body_md, approved_date, source_path}`\n+- `joins.feature_gates[slug][n]` gains an optional `doc: <program_design id>`\n+  key when a matching doc exists for that feature+gate.\n+- `entities.epic[].design_doc` gains an optional `<epic_design id>` when a\n+  4b design exists for that epic.\n+\n+## Flow\n+1. `build_index.py` walks `docs/plans/*/`. For each plan directory, it\n+   reads `03-program-design.md` if present and projects one\n+   `program_design` entity keyed on the directory's slug. It reads every\n+   `epic-*-design.md` and projects one `epic_design` entity per file, keyed\n+   on the epic id parsed from the filename.\n+2. `resolve_feature_gates()` looks up the `program_design` id for a\n+   feature's Gate 3 entry and attaches it as `doc`.\n+3. In the viewer, `renderBuildsMainContent()` reads `window.INDEX.entities.program_design`\n+   and `.epic_design` (falling back to empty objects if absent, so an older\n+   bundle degrades to today's non-clickable cards). A Gate Rail card with a\n+   `doc` becomes clickable; clicking calls `openGateDocSheet(id)`, which\n+   reuses `renderMarkdown()` to render `body_md` into the existing sheet\n+   markup pattern.\n+4. An epic card with a `design_doc` gets a small chip; clicking it opens\n+   the same sheet type, scoped to that epic's doc.\n+\n+## External\n+none.",
  "docs/plans/gate-doc-surfacing/03-program-design.md": "diff --git a/docs/plans/gate-doc-surfacing/03-program-design.md b/docs/plans/gate-doc-surfacing/03-program-design.md\nnew file mode 100644\nindex 0000000..dc79125\n--- /dev/null\n+++ b/docs/plans/gate-doc-surfacing/03-program-design.md\n@@ -0,0 +1,94 @@\n+# Program Design: Gate doc surfacing\n+\n+## Files\n+- `shared/build_index.py` — modified. New constants `PROGRAM_DESIGN_FIELDS`,\n+  `EPIC_DESIGN_FIELDS`; new functions `discover_plan_gate_docs()`,\n+  `project_program_design()`, `project_epic_design()`; `collect_designs()`\n+  or a new `collect_gate_docs()` wires the new entities into the index;\n+  `resolve_feature_gates()` attaches `doc` per gate.\n+- `plugins/artifact/viewer/index.html` — modified. New functions\n+  `openGateDocSheet(kind, id)`, `closeGateDocSheet()`,\n+  `renderGateDocSheet()`. New markup block for the sheet (siblings of\n+  `#adr-sheet`). `renderBuildsMainContent()`'s `gate-rail-grid` and\n+  `renderEpicCard()` gain click wiring.\n+- `tests/test_build_index_gate_docs.py` — new. Covers the projection\n+  functions and `resolve_feature_gates()`'s new `doc` field.\n+\n+## Types & signatures\n+\n+```python\n+# shared/build_index.py\n+PROGRAM_DESIGN_FIELDS: tuple[str, ...]\n+EPIC_DESIGN_FIELDS: tuple[str, ...]\n+\n+def discover_plan_gate_docs(repo: Path) -> list[dict]:\n+    \"\"\"One dict per docs/plans/<slug>/{03-program-design.md, epic-*-design.md}\n+    found, with slug, kind ('program'|'epic'), epic_id (if kind == 'epic'),\n+    path, and raw markdown text.\"\"\"\n+\n+def project_program_design(feature_slug: str, path: Path, text: str) -> dict:\n+    \"\"\"-> {\"id\": feature_slug, \"feature_slug\", \"gate\": 3, \"title\",\n+    \"body_md\", \"approved_date\", \"source_path\"}, fields filtered through\n+    project_fields(..., PROGRAM_DESIGN_FIELDS).\"\"\"\n+\n+def project_epic_design(epic_id: str, feature_slug: str, path: Path, text: str) -> dict:\n+    \"\"\"-> {\"id\": epic_id, \"epic_id\", \"feature_slug\", \"title\", \"body_md\",\n+    \"approved_date\", \"source_path\"}, filtered through\n+    project_fields(..., EPIC_DESIGN_FIELDS).\"\"\"\n+```\n+\n+```javascript\n+// plugins/artifact/viewer/index.html\n+function openGateDocSheet(kind, id) { /* kind: 'program' | 'epic' */ }\n+function closeGateDocSheet() { }\n+function renderGateDocSheet() { /* reads window.INDEX.entities.program_design\n+  or .epic_design by id, renders via renderMarkdown() into the sheet body */ }\n+```\n+\n+## Call stack\n+\n+Index build (top to bottom):\n+```\n+main()\n+  build_index(repo)\n+    collect_designs(repo)          # unchanged\n+    discover_plan_gate_docs(repo)  # new\n+      project_program_design()     # new, one call per feature with a Gate 3 doc\n+      project_epic_design()        # new, one call per epic-*-design.md found\n+    resolve_joins(repo)\n+      resolve_feature_gates(repo)  # extended: attaches doc id where found\n+```\n+\n+Viewer click (top to bottom):\n+```\n+gate-card click / epic design-doc chip click\n+  openGateDocSheet(kind, id)\n+    state.gateDocOpen = true; state.activeGateDocKind = kind; state.activeGateDocId = id\n+    renderGateDocSheet()\n+      lookup window.INDEX.entities.program_design[id] or .epic_design[id]\n+      renderMarkdown(record.body_md)   # reused, unchanged\n+    renderSheetVisibility()            # existing mutual-exclusivity function, extended\n+```\n+\n+## Test plan\n+- `test_build_index_gate_docs.py::test_project_program_design_reads_title_and_body`\n+- `test_build_index_gate_docs.py::test_project_epic_design_parses_epic_id_from_filename`\n+- `test_build_index_gate_docs.py::test_discover_plan_gate_docs_skips_plan_dir_with_no_gate_docs`\n+- `test_build_index_gate_docs.py::test_resolve_feature_gates_attaches_doc_id_when_program_design_exists`\n+- `test_build_index_gate_docs.py::test_resolve_feature_gates_gate3_doc_absent_when_no_program_design_md`\n+- `test_build_index_gate_docs.py::test_build_index_end_to_end_index_json_carries_new_entity_keys`\n+\n+Manual dogfood check (no automated frontend test harness in this repo):\n+open the rebuilt viewer, navigate to Builds, click this feature's own Gate\n+3 card once `03-program-design.md` exists (after this gate is approved),\n+confirm the sheet shows this document's content.\n+\n+## Least confident decisions\n+- Whether `collect_designs()` should absorb the new entities directly, or\n+  a separate `collect_gate_docs()` function is cleaner — deferred to\n+  implementation; either satisfies the same `data/index.json` shape.\n+- Whether the sheet reuses the *same* DOM nodes as `#adr-sheet` with a mode\n+  flag, or gets its own sibling `#gate-doc-sheet` block — leaning toward a\n+  sibling block, since `renderAdrSheet` and a future `renderGateDocSheet`\n+  read different record shapes and a shared node risks stale content on a\n+  fast open/open across the two kinds.",
  "docs/plans/gate-doc-surfacing/04-slices.md": "diff --git a/docs/plans/gate-doc-surfacing/04-slices.md b/docs/plans/gate-doc-surfacing/04-slices.md\nnew file mode 100644\nindex 0000000..d9bfb52\n--- /dev/null\n+++ b/docs/plans/gate-doc-surfacing/04-slices.md\n@@ -0,0 +1,12 @@\n+# Slice plan: Gate doc surfacing\n+\n+| # | Epic | Slice | Ends with | Score | State |\n+|---|---|---|---|---|---|\n+| | **`gate-doc-surfacing/E1` — Index gate docs into data/index.json.** shared/build_index.py projects docs/plans/<slug>/03-program-design.md and epic-<id>-design.md into new program_design and epic_design entities. | | | | |\n+| 1 | `gate-doc-surfacing/E1` | Tracer bullet: constants and empty entity lists | `data/index.json` carries `program_design` and `epic_design` keys (empty lists when no gate docs exist yet) | — | pending |\n+| 2 | `gate-doc-surfacing/E1` | Real parsing: project Gate 3 and Gate 4b docs | A repo with a `03-program-design.md` and an `epic-*-design.md` produces populated `program_design`/`epic_design` entities, and `joins.feature_gates[slug][3].doc` points at the projected id | — | pending |\n+| 3 | `gate-doc-surfacing/E1` | Edge cases: missing files, malformed epic id, no plans dir | A plan directory with no gate docs, and a repo with no `docs/plans/` at all, both still produce a clean index build with empty (not missing) entity keys | — | pending |\n+| | **`gate-doc-surfacing/E2` — Surface gate docs in the Builds view.** plugins/artifact/viewer/index.html gets a read-only sheet, wired to the Gate Rail and epic cards. | | | | |\n+| 4 | `gate-doc-surfacing/E2` | Tracer bullet: clickable Gate Rail card opens an empty sheet | Clicking a Gate Rail card with a `doc` opens a sheet showing the gate's title and an empty body; a card with no `doc` stays non-interactive | — | pending |\n+| 5 | `gate-doc-surfacing/E2` | Real content: render the gate doc body, add the epic chip | The sheet renders `body_md` through the existing `renderMarkdown()`; an epic card with a `design_doc` shows a chip that opens the same sheet scoped to that epic | — | pending |\n+| 6 | `gate-doc-surfacing/E2` | Edge cases: no entity, mutual exclusivity with other sheets | A Gate Rail card whose gate has no matching doc opens nothing; opening the gate-doc sheet closes any open ADR/assessment/comments sheet, and vice versa | — | pending |",
  "docs/plans/gate-doc-surfacing/epic-E1-design.md": "diff --git a/docs/plans/gate-doc-surfacing/epic-E1-design.md b/docs/plans/gate-doc-surfacing/epic-E1-design.md\nnew file mode 100644\nindex 0000000..2744fb3\n--- /dev/null\n+++ b/docs/plans/gate-doc-surfacing/epic-E1-design.md\n@@ -0,0 +1,107 @@\n+# Epic Technical Solution Design: E1 — Index gate docs into data/index.json\n+\n+Feature: gate-doc-surfacing\n+Epic ID: E1\n+\n+## Scope and Intent\n+Project `docs/plans/<slug>/03-program-design.md` and\n+`docs/plans/<slug>/epic-<id>-design.md` files into two new\n+`data/index.json` entity arrays, `program_design` and `epic_design`, so the\n+viewer has something to read. Attach a `doc` reference to the matching\n+Gate 3 entry in `joins.feature_gates`, and a `design_doc` reference to the\n+matching epic in `entities.epic`. No viewer changes in this epic — that is\n+E2.\n+\n+## Files Touched\n+- `shared/build_index.py` — add `PROGRAM_DESIGN_FIELDS`,\n+  `EPIC_DESIGN_FIELDS`, `discover_plan_gate_docs()`,\n+  `project_program_design()`, `project_epic_design()`; wire the new\n+  entities into the top-level `build_index()`/`main()` flow the same way\n+  `collect_designs()` already is; extend `resolve_feature_gates()` and the\n+  epic entity emission in `collect_designs()` with the new `doc`/`design_doc`\n+  references.\n+- `tests/test_build_index_gate_docs.py` — new test file.\n+\n+## Types & Signatures\n+\n+```python\n+PROGRAM_DESIGN_FIELDS: tuple[str, ...] = (\n+    \"id\", \"feature_slug\", \"gate\", \"title\", \"body_md\", \"approved_date\", \"source_path\",\n+)\n+EPIC_DESIGN_FIELDS: tuple[str, ...] = (\n+    \"id\", \"epic_id\", \"feature_slug\", \"title\", \"body_md\", \"approved_date\", \"source_path\",\n+)\n+\n+def discover_plan_gate_docs(repo: Path) -> list[dict]:\n+    \"\"\"Walk docs/plans/*/. Return one dict per gate doc found:\n+    {\"kind\": \"program\", \"feature_slug\": str, \"path\": Path, \"text\": str}\n+    or\n+    {\"kind\": \"epic\", \"epic_id\": str, \"feature_slug\": str, \"path\": Path, \"text\": str}.\n+    An epic id is parsed from the filename epic-<id>-design.md; a file\n+    whose id segment fails to parse is skipped, not raised on — this\n+    function never crashes a full index rebuild over one malformed name.\n+    \"\"\"\n+\n+def project_program_design(feature_slug: str, path: Path, text: str) -> dict:\n+    \"\"\"Extract a title (first '# ' heading) and approved_date (from the\n+    matching 00-status.md's Gate 3 line, if resolvable) alongside the raw\n+    body. Returns a dict filtered through project_fields(..., PROGRAM_DESIGN_FIELDS).\"\"\"\n+\n+def project_epic_design(epic_id: str, feature_slug: str, path: Path, text: str) -> dict:\n+    \"\"\"Same shape, keyed on epic_id instead of feature_slug.\"\"\"\n+```\n+\n+`id` for a `program_design` entity is the bare `feature_slug` (one Gate 3\n+doc per feature, so no collision risk). `id` for an `epic_design` entity is\n+the bare `epic_id` parsed from its filename — note this is the *unscoped*\n+epic id (`E1`), not the `<design>/<epic-id>` scoped id `entities.epic`\n+uses, because a technical design file lives under one `docs/plans/<slug>/`\n+directory already, so `feature_slug` carries the scoping context instead.\n+`entities.epic[].design_doc` stores the *scoped* lookup path\n+(`epic_design[<feature_slug>/<epic_id>]` is wrong; the correct join is:\n+look up `epic_design` by its own `epic_id` key, disambiguated by also\n+matching `feature_slug`, since two features could each have an `E1`).\n+Implementation note: to avoid this exact ambiguity, `epic_design`'s\n+`id` field is actually `f\"{feature_slug}/{epic_id}\"`, matching\n+`entities.epic`'s own `<design>/<epic-id>` scoping convention. This\n+supersedes the plain `epic_id` framing above — keep them consistent\n+during implementation; the test plan below locks this down.\n+\n+## Slice Decomposition\n+1. **Slice 1 (tracer bullet).** Add the constants and empty-list wiring so\n+   `data/index.json` always carries `program_design: []` and\n+   `epic_design: []`, even with zero gate docs on disk. No dependency.\n+2. **Slice 2 (real parsing).** Implement `discover_plan_gate_docs()`,\n+   `project_program_design()`, `project_epic_design()`, and the\n+   `feature_gates`/`epic` join attachment. Depends on slice 1's constants.\n+3. **Slice 3 (edge cases).** Missing `docs/plans/` entirely, a plan\n+   directory with only some gate docs, a malformed epic filename. Depends\n+   on slice 2.\n+\n+## Test Plan\n+`tests/test_build_index_gate_docs.py`:\n+- `test_project_program_design_reads_title_and_body` — feed a small\n+  markdown fixture, assert `title`/`body_md`/`source_path` match.\n+- `test_project_epic_design_id_is_scoped_feature_slug_and_epic_id` — locks\n+  down the `<feature_slug>/<epic_id>` id convention from the note above.\n+- `test_discover_plan_gate_docs_skips_malformed_epic_filename` — a file\n+  named `epic-design.md` (no id segment) is skipped, not raised on.\n+- `test_discover_plan_gate_docs_no_plans_dir_returns_empty_list` — repo\n+  with no `docs/plans/` at all.\n+- `test_resolve_feature_gates_attaches_doc_when_program_design_exists`.\n+- `test_resolve_feature_gates_gate3_doc_key_absent_when_no_md_file`.\n+- `test_build_index_end_to_end_index_json_has_program_design_and_epic_design_keys`\n+  — run against this repo's own tree; assert both keys exist, and (once\n+  this feature's own gate docs land) that this feature's own entities are\n+  among them.\n+\n+## Risks & Open Questions\n+- The id-scoping ambiguity flagged above (`epic_id` alone vs.\n+  `<feature_slug>/<epic_id>`) must be resolved consistently between the\n+  producer (`project_epic_design`) and the consumer\n+  (`entities.epic[].design_doc` lookup in E2) — the test plan pins the\n+  chosen shape so E2 cannot silently diverge from it.\n+- `approved_date` sourcing from `00-status.md`'s Gate 3/4b lines duplicates\n+  what `resolve_feature_gates()` already parses there. Acceptable\n+  duplication since the two functions read the same file for different\n+  fields, not the same field twice.",
  "docs/plans/gate-doc-surfacing/epic-E2-design.md": "diff --git a/docs/plans/gate-doc-surfacing/epic-E2-design.md b/docs/plans/gate-doc-surfacing/epic-E2-design.md\nnew file mode 100644\nindex 0000000..a70e8f8\n--- /dev/null\n+++ b/docs/plans/gate-doc-surfacing/epic-E2-design.md\n@@ -0,0 +1,98 @@\n+# Epic Technical Solution Design: E2 — Surface gate docs in the Builds view\n+\n+Feature: gate-doc-surfacing\n+Epic ID: E2\n+\n+## Scope and Intent\n+Make the Builds view's Gate Rail cards and epic design chips clickable,\n+opening a read-only sheet that shows a `program_design` or `epic_design`\n+entity's content. Depends on E1 shipping the entities first — this epic\n+reads `window.INDEX.entities.program_design`/`.epic_design`, both empty\n+arrays until E1 lands, so E2's tracer bullet is safe to build against a\n+fixture even before E1 slice 2 completes.\n+\n+## Files Touched\n+- `plugins/artifact/viewer/index.html` — new sheet markup block (sibling of\n+  `#adr-sheet`, ~line 1022), new functions `openGateDocSheet(kind, id)`,\n+  `closeGateDocSheet()`, `renderGateDocSheet()`; extend\n+  `closeAnyOpenSheet()` (~line 4003) and `renderSheetVisibility()`\n+  (~line 3960) for mutual exclusivity; wire click handlers into\n+  `renderBuildsMainContent()`'s `gate-rail-grid` (~line 3249) and\n+  `renderEpicCard()` (~line 3282).\n+\n+## Types & Signatures\n+\n+```javascript\n+// state additions, alongside existing state.adrOpen / state.activeAdrId\n+state.gateDocOpen = false;\n+state.activeGateDocKind = null; // 'program' | 'epic'\n+state.activeGateDocId = null;   // matches program_design/epic_design id\n+\n+function openGateDocSheet(kind, id){\n+  if (state.commentsOpen) state.commentsOpen = false;\n+  if (state.adrOpen) state.adrOpen = false;\n+  if (state.assessmentOpen) state.assessmentOpen = false;\n+  if (state.audioDialogOpen) stopAudio();\n+  state.gateDocOpen = true;\n+  state.activeGateDocKind = kind;\n+  state.activeGateDocId = id;\n+  renderGateDocSheet();\n+  renderSheetVisibility();\n+}\n+function closeGateDocSheet(){\n+  state.gateDocOpen = false;\n+  renderSheetVisibility();\n+}\n+function renderGateDocSheet(){\n+  const index = window.INDEX || {};\n+  const entities = index.entities || {};\n+  const table = state.activeGateDocKind === 'program'\n+    ? (entities.program_design || {})\n+    : (entities.epic_design || {});\n+  const record = table[state.activeGateDocId];\n+  // ... title/meta/body, reusing renderMarkdown() for record.body_md,\n+  // and the \"record not captured\" empty-card fallback openAdrSheet uses\n+  // when id/record is missing.\n+}\n+```\n+\n+**Resolved during Gate 4b:** every existing `window.INDEX.entities.<type>`\n+is array-shaped (confirmed against `entities.adr`/`entities.epic` in the\n+current `data/index.json`), unlike the separate dict-shaped `window.ADRS`.\n+`program_design`/`epic_design` follow the same array convention as every\n+other entity, for consistency. `renderGateDocSheet` looks a record up with\n+`(entities.program_design || []).find(r => r.id === id)` (or the\n+`epic_design` equivalent) rather than a dict key access.\n+\n+## Slice Decomposition\n+1. **Slice 4 (tracer bullet).** Sheet markup + `openGateDocSheet`/\n+   `closeGateDocSheet`/`renderSheetVisibility` wiring, gate-rail-grid card\n+   click handler. Renders title only, empty body — proves the plumbing\n+   without depending on E1's real content.\n+2. **Slice 5 (real content).** `renderGateDocSheet` renders `body_md` via\n+   `renderMarkdown()`. Epic card design-doc chip added to `renderEpicCard`.\n+   Depends on E1 slice 2 (real entities) for a meaningful demo, but the\n+   render code itself only depends on slice 4's plumbing.\n+3. **Slice 6 (edge cases).** No-`doc` gate card stays non-interactive\n+   (no click handler attached at all, not a handler that no-ops); mutual\n+   exclusivity with `#adr-sheet`/`#assessment-sheet`/comments drawer/audio\n+   dialog, both directions.\n+\n+## Test Plan\n+No JS test harness exists in this repo for the viewer (it is one static\n+HTML file with no build step, per ADR-0001/ADR-0020). Verification is\n+manual, via the `run` skill or a direct `python3 -m http.server` serve of\n+`.cobuilder-architect/self/`, exercising:\n+- a Gate Rail card with a `doc` opens the sheet and shows body content\n+  (slice 4+5)\n+- a Gate Rail card with no `doc` has no click affordance (slice 6)\n+- opening the gate-doc sheet while the ADR sheet is open closes the ADR\n+  sheet, and vice versa (slice 6)\n+- an epic card with `design_doc` shows a chip; clicking it opens the sheet\n+  scoped to that epic's doc (slice 5)\n+\n+## Risks & Open Questions\n+- No JS test harness means slices 4-6 lean on manual dogfood verification\n+  more than the repo's usual pytest-backed rigor. Rubric criteria for this\n+  epic's slices are written as concrete manual-check steps, not automated\n+  assertions, for this reason.",
  "docs/pull-requests/branch-design-gate-doc-surfacing/assessment.json": "diff --git a/docs/pull-requests/branch-design-gate-doc-surfacing/assessment.json b/docs/pull-requests/branch-design-gate-doc-surfacing/assessment.json\nnew file mode 100644\nindex 0000000..d1caa9f\n--- /dev/null\n+++ b/docs/pull-requests/branch-design-gate-doc-surfacing/assessment.json\n@@ -0,0 +1,73 @@\n+{\n+  \"stage\": \"pre\",\n+  \"generated\": \"2026-09-03\",\n+  \"verdict\": \"concerns\",\n+  \"risk_tier\": \"architectural\",\n+  \"summary\": \"Two changes ship in one PR: the gate-doc-surfacing design (program_design/epic_design entities per ADR-0022, plus the viewer's clickable Gate Rail and design-doc chip) and an unrelated fix to plugins/implement/skills/build/workflows/slice-loop.js, found while dogfooding the implement workflow in a sister repo. Both are sound on their own; the summary finding below is that they are bundled.\",\n+  \"sensible\": {\n+    \"answer\": \"Yes, against the stated problem for the design half: the Builds view already renders a Gate Rail and epic cards from an approval-state parse, but never the document content behind it, and this change attaches that content through the same index-projection pattern ADR-0018 already established. The slice-loop.js half is also sensible: a workflow script that can never be found (wrong Workflow-tool invocation) and can never run past its Gate 4b check (imports a Node API the runtime does not provide) is not a partially-working feature, it is dead code with tests never exercising it.\",\n+    \"evidence\": [\"ADR-0018\", \"ADR-0022\", \"shared/build_index.py:discover_plan_gate_docs\", \"plugins/implement/skills/build/workflows/slice-loop.js\"]\n+  },\n+  \"maintainability\": {\n+    \"answer\": \"Helps: program_design/epic_design follow the same GOAL_FIELDS/project_fields(source, fields) shape every other entity in build_index.py already uses, so a future entity kind has one more precedent to copy rather than a special case to learn. The slice-loop.js fix removes a call the runtime silently could never satisfy and replaces it with an explicit caller-supplied field (epicDesignExists), which is more maintainable than a check that looked correct in isolation but failed to import at all. Hurts slightly: the PR mixes a feature and a bugfix, so `git blame` on workflows/slice-loop.js now attributes an unrelated design's commit range to a fix that has nothing to do with gate-doc surfacing.\",\n+    \"constraint_introduced\": \"A gate document's content is now reachable only through the record index (program_design/epic_design), never parsed ad hoc by the viewer. Workflow scripts in this plugin family must never import a Node.js/filesystem API; any existence check a script needs must be computed by the orchestrating session and passed in through `args`.\",\n+    \"evidence\": [\"shared/build_index.py:249-250 (PROGRAM_DESIGN_FIELDS/EPIC_DESIGN_FIELDS)\", \"plugins/implement/skills/build/workflows/slice-loop.js (epicDesignExists)\", \"tests/test_build_index_gate_docs.py\"]\n+  },\n+  \"pattern\": {\n+    \"verdict\": \"conforms\",\n+    \"answer\": \"program_design/epic_design entities are a new kind, but they use the same project_fields(source, fields) projection every existing entity (GOAL_FIELDS, EPIC_FIELDS, INTENT_FIELDS, ASSESSMENT_FIELDS) already uses, and resolve_feature_gates() attaches the doc reference to the existing Gate 3 status parse rather than adding a parallel one. The slice-loop.js fix conforms to the Workflow tool's own documented contract (scriptPath for a plugin-shipped script, no filesystem access in the script body) rather than inventing a workaround.\",\n+    \"duplicates\": [],\n+    \"evidence\": [\"shared/build_index.py:246-250\", \"shared/build_index.py:1073-1090 (resolve_feature_gates)\"]\n+  },\n+  \"findings\": [\n+    {\n+      \"severity\": \"concern\",\n+      \"claim\": \"This PR bundles two unrelated changes: the gate-doc-surfacing design (six commits) and a fix to plugins/implement/skills/build/workflows/slice-loop.js's Workflow-tool invocation and filesystem-access bug (one commit), found incidentally while using the plugin in a different repo. A reviewer who reviews only the design will miss the workflow fix, and vice versa.\",\n+      \"evidence\": \"git log master..design/gate-doc-surfacing --oneline (7 commits: 6b89c69 through 210676c)\",\n+      \"district\": \"docs\",\n+      \"suggestion\": \"Accept as one PR since the fix is already committed on this branch and splitting it now means branch surgery, but call out the workflow fix explicitly in the PR description so a reviewer does not read it as part of the gate-doc-surfacing design.\"\n+    },\n+    {\n+      \"severity\": \"note\",\n+      \"claim\": \".cobuilder-architect/self/inventory.yaml still lists the pre-plugin-split districts (skills, scripts, commands, viewer, docs, .cobuilder-architect) rather than the current plugins/{architect,pr,artifact,implement,cobuilder-full-lifecycle}/ layout, so the district delta and boundary checks below are approximated by path rather than derived from a current baseline.\",\n+      \"evidence\": \".cobuilder-architect/self/inventory.yaml:4-27 (dated 2026-08-19, predates the plugin-prefix-drop rename)\",\n+      \"district\": \"docs\",\n+      \"suggestion\": \"Re-run baseline mode against the current tree so future assessments do not have to approximate district names.\"\n+    },\n+    {\n+      \"severity\": \"note\",\n+      \"claim\": \"No automated test exercises the corrected Workflow-tool invocation (scriptPath vs name) or the new epicDesignExists caller contract, because doing so requires the Workflow tool's own runtime, which the repo's pytest suite does not have access to.\",\n+      \"evidence\": \"plugins/implement/skills/build/workflows/slice-loop.js (no matching tests/ file)\",\n+      \"district\": \"scripts\",\n+      \"suggestion\": \"Treat the next real program-scale build (multiple epics per 04-slices.md) as the practical verification of this fix, since that is the first scenario that will actually invoke the workflow.\"\n+    }\n+  ],\n+  \"boundary_checks\": [\n+    {\n+      \"rule\": \"Inner layers (domain, business logic) never import outer layers (HTTP, UI, DB drivers, framework code).\",\n+      \"source\": \"stacks/generic.md\",\n+      \"result\": \"not-checkable\",\n+      \"evidence\": \"This plugin family has no domain/adapter split to grep for; shared/build_index.py and the workflow script both read the filesystem and the plans directory by design.\"\n+    },\n+    {\n+      \"rule\": \"Configuration crosses into code in one place, not scattered env reads.\",\n+      \"source\": \"stacks/generic.md\",\n+      \"result\": \"pass\",\n+      \"evidence\": \"Neither half of this diff adds a new environment-variable read; grep for os.environ / os.getenv in the diff hunks returns nothing.\"\n+    },\n+    {\n+      \"rule\": \"No plugin's script or skill file names another plugin's plugins/<other-name>/... path directly.\",\n+      \"source\": \"ADR-0016\",\n+      \"result\": \"pass\",\n+      \"evidence\": \"grep of the diff for plugins/(architect|pr|artifact|implement|cobuilder-full-lifecycle)/ hits only each file's own plugin path (plugins/artifact/viewer/index.html, plugins/implement/skills/build/...), never a cross-plugin reference.\"\n+    }\n+  ],\n+  \"delta\": {\n+    \"districts_added\": [\"shared/build_index.py:program_design,epic_design entities\"],\n+    \"districts_changed\": [\"plugins/artifact/viewer (Gate Rail click, epic design-doc chip)\", \"shared (build_index.py projection)\", \"plugins/implement/skills/build (slice-loop.js, SKILL.md, slice-loop.md)\", \"docs/plans/gate-doc-surfacing\", \"docs/architecture/adr (ADR-0022)\", \"docs/architecture/designs/gate-doc-surfacing\"],\n+    \"edges_added\": [\"shared/build_index.py -> docs/plans/<slug>/03-program-design.md,epic-<id>-design.md\", \"plugins/artifact/viewer/index.html -> window.INDEX.entities.program_design,epic_design\"],\n+    \"edges_removed\": []\n+  },\n+  \"regret_risk\": \"If this merges as written, the team gains a real gate-doc reading surface in the viewer and a workflow-invocation fix that unblocks the next multi-epic program-scale build, at the cost of a PR whose git history reviewers must read as two stories, not one. The likelier six-month regret is narrower: the inventory.yaml staleness noted above means the next assessment on this branch's district (or any branch touching plugins/) keeps approximating districts by path instead of reading a derived baseline, and that approximation compounds each time nobody re-runs baseline mode. A second, smaller regret is that the slice-loop.js fix has no automated coverage of its own; if a future edit reintroduces a Node API import above the meta literal, only the next real program-scale build will catch it, at the cost of a wasted Workflow invocation rather than a fast test failure.\",\n+  \"drift\": []\n+}",
  "docs/pull-requests/branch-design-gate-doc-surfacing/assessment.md": "diff --git a/docs/pull-requests/branch-design-gate-doc-surfacing/assessment.md b/docs/pull-requests/branch-design-gate-doc-surfacing/assessment.md\nnew file mode 100644\nindex 0000000..4c2bc93\n--- /dev/null\n+++ b/docs/pull-requests/branch-design-gate-doc-surfacing/assessment.md\n@@ -0,0 +1,67 @@\n+# Assessment — Branch design-gate-doc-surfacing\n+\n+**Verdict:** Concerns  \n+**Risk tier:** Architectural  \n+**Stage:** pre-merge  \n+**Author-flagged unknowns:** 2 (raises the tier by one step)\n+\n+## Summary\n+\n+Two changes ship in one PR: the gate-doc-surfacing design (program_design/epic_design entities per ADR-0022, plus the viewer's clickable Gate Rail and design-doc chip) and an unrelated fix to plugins/implement/skills/build/workflows/slice-loop.js, found while dogfooding the implement workflow in a sister repo. Both are sound on their own; the summary finding below is that they are bundled.\n+\n+## Question 1 — is this sensible?\n+\n+Yes, against the stated problem for the design half: the Builds view already renders a Gate Rail and epic cards from an approval-state parse, but never the document content behind it, and this change attaches that content through the same index-projection pattern ADR-0018 already established. The slice-loop.js half is also sensible: a workflow script that can never be found (wrong Workflow-tool invocation) and can never run past its Gate 4b check (imports a Node API the runtime does not provide) is not a partially-working feature, it is dead code with tests never exercising it.\n+\n+Evidence: `ADR-0018` `ADR-0022` `shared/build_index.py:discover_plan_gate_docs` `plugins/implement/skills/build/workflows/slice-loop.js`\n+\n+## Question 2 — maintainability and readability\n+\n+Helps: program_design/epic_design follow the same GOAL_FIELDS/project_fields(source, fields) shape every other entity in build_index.py already uses, so a future entity kind has one more precedent to copy rather than a special case to learn. The slice-loop.js fix removes a call the runtime silently could never satisfy and replaces it with an explicit caller-supplied field (epicDesignExists), which is more maintainable than a check that looked correct in isolation but failed to import at all. Hurts slightly: the PR mixes a feature and a bugfix, so `git blame` on workflows/slice-loop.js now attributes an unrelated design's commit range to a fix that has nothing to do with gate-doc surfacing.\n+\n+**Constraint introduced:** A gate document's content is now reachable only through the record index (program_design/epic_design), never parsed ad hoc by the viewer. Workflow scripts in this plugin family must never import a Node.js/filesystem API; any existence check a script needs must be computed by the orchestrating session and passed in through `args`.\n+\n+Evidence: `shared/build_index.py:249-250 (PROGRAM_DESIGN_FIELDS/EPIC_DESIGN_FIELDS)` `plugins/implement/skills/build/workflows/slice-loop.js (epicDesignExists)` `tests/test_build_index_gate_docs.py`\n+\n+## Question 3 — new pattern, duplicate, or reinvention?\n+\n+**Conforms to an existing pattern**\n+\n+program_design/epic_design entities are a new kind, but they use the same project_fields(source, fields) projection every existing entity (GOAL_FIELDS, EPIC_FIELDS, INTENT_FIELDS, ASSESSMENT_FIELDS) already uses, and resolve_feature_gates() attaches the doc reference to the existing Gate 3 status parse rather than adding a parallel one. The slice-loop.js fix conforms to the Workflow tool's own documented contract (scriptPath for a plugin-shipped script, no filesystem access in the script body) rather than inventing a workaround.\n+\n+Evidence: `shared/build_index.py:246-250` `shared/build_index.py:1073-1090 (resolve_feature_gates)`\n+\n+## Will we regret this?\n+\n+If this merges as written, the team gains a real gate-doc reading surface in the viewer and a workflow-invocation fix that unblocks the next multi-epic program-scale build, at the cost of a PR whose git history reviewers must read as two stories, not one. The likelier six-month regret is narrower: the inventory.yaml staleness noted above means the next assessment on this branch's district (or any branch touching plugins/) keeps approximating districts by path instead of reading a derived baseline, and that approximation compounds each time nobody re-runs baseline mode. A second, smaller regret is that the slice-loop.js fix has no automated coverage of its own; if a future edit reintroduces a Node API import above the meta literal, only the next real program-scale build will catch it, at the cost of a wasted Workflow invocation rather than a fast test failure.\n+\n+## Findings\n+\n+| Severity | Finding | Evidence |\n+|---|---|---|\n+| concern | This PR bundles two unrelated changes: the gate-doc-surfacing design (six commits) and a fix to plugins/implement/skills/build/workflows/slice-loop.js's Workflow-tool invocation and filesystem-access bug (one commit), found incidentally while using the plugin in a different repo. A reviewer who reviews only the design will miss the workflow fix, and vice versa. | `git log master..design/gate-doc-surfacing --oneline (7 commits: 6b89c69 through 210676c)` |\n+| note | .cobuilder-architect/self/inventory.yaml still lists the pre-plugin-split districts (skills, scripts, commands, viewer, docs, .cobuilder-architect) rather than the current plugins/{architect,pr,artifact,implement,cobuilder-full-lifecycle}/ layout, so the district delta and boundary checks below are approximated by path rather than derived from a current baseline. | `.cobuilder-architect/self/inventory.yaml:4-27 (dated 2026-08-19, predates the plugin-prefix-drop rename)` |\n+| note | No automated test exercises the corrected Workflow-tool invocation (scriptPath vs name) or the new epicDesignExists caller contract, because doing so requires the Workflow tool's own runtime, which the repo's pytest suite does not have access to. | `plugins/implement/skills/build/workflows/slice-loop.js (no matching tests/ file)` |\n+\n+**Suggestions**\n+\n+- **This PR bundles two unrelated changes: the gate-doc-surfacing design (six commits) and a fix to plugins/implement/skills/build/workflows/slice-loop.js's Workflow-tool invocation and filesystem-access bug (one commit), found incidentally while using the plugin in a different repo. A reviewer who reviews only the design will miss the workflow fix, and vice versa.** — Accept as one PR since the fix is already committed on this branch and splitting it now means branch surgery, but call out the workflow fix explicitly in the PR description so a reviewer does not read it as part of the gate-doc-surfacing design.\n+- **.cobuilder-architect/self/inventory.yaml still lists the pre-plugin-split districts (skills, scripts, commands, viewer, docs, .cobuilder-architect) rather than the current plugins/{architect,pr,artifact,implement,cobuilder-full-lifecycle}/ layout, so the district delta and boundary checks below are approximated by path rather than derived from a current baseline.** — Re-run baseline mode against the current tree so future assessments do not have to approximate district names.\n+- **No automated test exercises the corrected Workflow-tool invocation (scriptPath vs name) or the new epicDesignExists caller contract, because doing so requires the Workflow tool's own runtime, which the repo's pytest suite does not have access to.** — Treat the next real program-scale build (multiple epics per 04-slices.md) as the practical verification of this fix, since that is the first scenario that will actually invoke the workflow.\n+\n+## Boundary checks\n+\n+| Result | Rule | Source | Evidence |\n+|---|---|---|---|\n+| not-checkable | Inner layers (domain, business logic) never import outer layers (HTTP, UI, DB drivers, framework code). | `stacks/generic.md` | `This plugin family has no domain/adapter split to grep for; shared/build_index.py and the workflow script both read the filesystem and the plans directory by design.` |\n+| pass | Configuration crosses into code in one place, not scattered env reads. | `stacks/generic.md` | `Neither half of this diff adds a new environment-variable read; grep for os.environ / os.getenv in the diff hunks returns nothing.` |\n+| pass | No plugin's script or skill file names another plugin's plugins/<other-name>/... path directly. | `ADR-0016` | `grep of the diff for plugins/(architect\\|pr\\|artifact\\|implement\\|cobuilder-full-lifecycle)/ hits only each file's own plugin path (plugins/artifact/viewer/index.html, plugins/implement/skills/build/...), never a cross-plugin reference.` |\n+\n+## District delta\n+\n+**Districts added:** `shared/build_index.py:program_design,epic_design entities`\n+**Edges added:** `shared/build_index.py -> docs/plans/<slug>/03-program-design.md,epic-<id>-design.md`, `plugins/artifact/viewer/index.html -> window.INDEX.entities.program_design,epic_design`\n+\n+---\n+\n+_Generated by pr generate mode on 2026-09-03._",
  "docs/pull-requests/branch-design-gate-doc-surfacing/description.md": "diff --git a/docs/pull-requests/branch-design-gate-doc-surfacing/description.md b/docs/pull-requests/branch-design-gate-doc-surfacing/description.md\nnew file mode 100644\nindex 0000000..fb41a04\n--- /dev/null\n+++ b/docs/pull-requests/branch-design-gate-doc-surfacing/description.md\n@@ -0,0 +1,56 @@\n+# Branch design-gate-doc-surfacing\n+\n+## Problem\n+\n+Gate 3 program-design docs and Gate 4b epic technical-design docs live at docs/plans/<slug>/*.md, but the viewer's index tracks only adr, design, epic, slice, and pull_request entities. A reviewer who wants to watch a build's Gate 3 progress in the viewer sees nothing: the Gate Rail already shows approval state from 00-status.md, but never the document content behind it. A prior session's workaround was publishing the doc as a one-off Claude Artifact, disconnected from docs/plans/ with no rebuild hook and undiscoverable from the viewer.\n+\n+The gap surfaced live when an engineer asked to see a Gate 3 design in the viewer and the agent found no entity for it. The Builds view (renderBuildsMainContent) already renders a Gate Rail and epic cards, which are the natural attachment point for a doc link, so the fix slots into an existing surface rather than adding a new one.\n+\n+## Why this approach\n+\n+Extend shared/build_index.py with discover_plan_gate_docs()/project_program_design()/project_epic_design(), following the existing GOAL_FIELDS/project_fields(source, fields) pattern, to project docs/plans/<slug>/03-program-design.md and epic-<id>-design.md into two new index entities, program_design and epic_design (ADR-0022). resolve_feature_gates() now attaches a doc reference to a feature's Gate 3 entry when a program-design doc exists. In the viewer, Gate Rail cards became clickable, opening a sheet modeled on the existing ADR sheet, and epic cards with an approved Gate 4b design gained a design-doc chip. Both entities and the viewer change are covered by tests/test_build_index_gate_docs.py (10 cases). Separately, while using this plugin's implement workflow in an unrelated sister repo, two defects surfaced in plugins/implement/skills/build/workflows/slice-loop.js: it invoked the Workflow tool with `name: \"slice-loop\"`, which only resolves built-in or .claude/workflows/-registered workflows and never finds a plugin-shipped script, and it imported node:fs and called existsSync() for a Gate 4b check, which workflow scripts cannot do because they run with no filesystem access at all (and the import also violated the requirement that `export const meta` be the script's first statement). Both are fixed here: the SKILL.md and reference doc now say to invoke with `scriptPath`, and the existence check moves to the orchestrating session (e.g. via verify_gate.py), which passes the result in as each slice's new `epicDesignExists` field.\n+\n+## Alternatives considered\n+\n+- **Add a new top-level 'Plans' or 'Gate Docs' tab in the viewer** — rejected because ADR-0018 decided one lifecycle surface with a derived record index precisely to stop the reading surface count from growing per artifact type. A gate doc is scoped to a build already living in the viewer's Builds view (district: viewer), a sibling tab duplicates that context instead of joining it.\n+- **Parse gate docs ad hoc in the viewer's client-side JS at render time, skipping the index** — rejected because ADR-0018's whole point is that joins get resolved once, in build_index.py, not scattered across client fetches. The viewer is also a single committed HTML file with no direct filesystem access model beyond its bundled data/ scripts (district: viewer): it cannot read docs/plans/ at render time at all.\n+- **Split viewer/index.html into parts now, and add gate docs to the new module structure** — rejected because ADR-0020 decided the viewer-parts split but is explicitly not executed yet. Starting that execution as a side effect of this design is scope creep this design does not own.\n+- **Keep publishing gate docs as one-off Claude Artifacts** — rejected because This is the workaround that exposed the gap: no rebuild hook ties it to docs/plans/, so it drifts, and it is not discoverable from the viewer itself.\n+- **For the slice-loop fix, have the orchestrating session poll or retry existsSync inside the workflow script** — rejected because Workflow scripts have no filesystem or Node.js API access at all (confirmed against the tool's own authoring reference), so no retry inside the script can make the check work. The only place the check can run is the orchestrating session, before it invokes the workflow.\n+\n+## Out of scope\n+\n+- a new top-level viewer tab for plans or gate docs\n+- editing a gate doc from the viewer, read-only sheet only\n+- changing how 00-status.md's approval state is parsed or displayed\n+- PDF or standalone-artifact export of a gate doc\n+- executing ADR-0020's viewer-parts split, this design writes into the current monolithic viewer/index.html\n+- re-scoring or re-running any slice that already ran under the old, broken slice-loop.js invocation\n+\n+## Risks\n+\n+- a gate doc grows long enough that the sheet reading UX (built for ADR text, a few hundred lines) degrades\n+- program_design/epic_design entity ids collide with, or get confused with, the existing epic entity's <design>/<epic-id> scoping\n+- the sheet becomes stale between build_index.py runs the same way the pre-fix Artifact was, if a future gate step forgets to rebuild\n+- a future edit to slice-loop.js reintroduces a Node.js API call (fs, path, etc.) above the meta literal, which fails the same two ways this fix corrects\n+\n+## How this was tested\n+\n+tests/test_build_index_gate_docs.py (10 cases) covers discover_plan_gate_docs, project_program_design, project_epic_design, and the Gate 3 doc-reference attachment in resolve_feature_gates. E2 was verified live in-browser against the running viewer (see commit 651b07a). The slice-loop.js fix was verified by reading the workflow-authoring reference's constraints (no filesystem access, meta must be the first statement) and by running the full pytest suite (322 passed, 4 pre-existing unrelated Pillow/webp failures from an arch-mismatched local Pillow install) after the change; no automated test exercises the Workflow tool invocation itself, since that requires the tool's runtime.\n+\n+## Where to focus\n+\n+- whether program_design/epic_design entity ids collide with the epic entity's own id scheme\n+- whether the sheet UX holds up for a long program-design doc\n+- whether the Gate Rail card / epic chip is discoverable without extra instruction\n+- whether bundling the unrelated slice-loop.js fix into this design's PR, rather than a separate PR, is acceptable given it was found while dogfooding this same plugin\n+\n+The author flagged these parts as not fully understood:\n+\n+- how large a typical 03-program-design.md or epic-<id>-design.md gets across real features, and whether the sheet needs pagination or truncation\n+- whether a design with no docs/plans/<slug>/ directory at all (a design never taken to implement mode) should render an empty Gate Rail or hide it entirely\n+\n+---\n+\n+_Intent inferred from the PR body, the commit messages, and the branch name. Not stated by the author._\n+_Authorship: agent-assisted._",
  "docs/pull-requests/branch-design-gate-doc-surfacing/intent.json": "diff --git a/docs/pull-requests/branch-design-gate-doc-surfacing/intent.json b/docs/pull-requests/branch-design-gate-doc-surfacing/intent.json\nnew file mode 100644\nindex 0000000..19a2737\n--- /dev/null\n+++ b/docs/pull-requests/branch-design-gate-doc-surfacing/intent.json\n@@ -0,0 +1,56 @@\n+{\n+  \"captured\": \"2026-09-03\",\n+  \"source\": \"inferred\",\n+  \"authorship\": \"agent-assisted\",\n+  \"design\": { \"name\": \"gate-doc-surfacing\", \"epic\": null },\n+  \"problem\": \"Gate 3 program-design docs and Gate 4b epic technical-design docs live at docs/plans/<slug>/*.md, but the viewer's index tracks only adr, design, epic, slice, and pull_request entities. A reviewer who wants to watch a build's Gate 3 progress in the viewer sees nothing: the Gate Rail already shows approval state from 00-status.md, but never the document content behind it. A prior session's workaround was publishing the doc as a one-off Claude Artifact, disconnected from docs/plans/ with no rebuild hook and undiscoverable from the viewer.\",\n+  \"why_now\": \"The gap surfaced live when an engineer asked to see a Gate 3 design in the viewer and the agent found no entity for it. The Builds view (renderBuildsMainContent) already renders a Gate Rail and epic cards, which are the natural attachment point for a doc link, so the fix slots into an existing surface rather than adding a new one.\",\n+  \"approach\": \"Extend shared/build_index.py with discover_plan_gate_docs()/project_program_design()/project_epic_design(), following the existing GOAL_FIELDS/project_fields(source, fields) pattern, to project docs/plans/<slug>/03-program-design.md and epic-<id>-design.md into two new index entities, program_design and epic_design (ADR-0022). resolve_feature_gates() now attaches a doc reference to a feature's Gate 3 entry when a program-design doc exists. In the viewer, Gate Rail cards became clickable, opening a sheet modeled on the existing ADR sheet, and epic cards with an approved Gate 4b design gained a design-doc chip. Both entities and the viewer change are covered by tests/test_build_index_gate_docs.py (10 cases). Separately, while using this plugin's implement workflow in an unrelated sister repo, two defects surfaced in plugins/implement/skills/build/workflows/slice-loop.js: it invoked the Workflow tool with `name: \\\"slice-loop\\\"`, which only resolves built-in or .claude/workflows/-registered workflows and never finds a plugin-shipped script, and it imported node:fs and called existsSync() for a Gate 4b check, which workflow scripts cannot do because they run with no filesystem access at all (and the import also violated the requirement that `export const meta` be the script's first statement). Both are fixed here: the SKILL.md and reference doc now say to invoke with `scriptPath`, and the existence check moves to the orchestrating session (e.g. via verify_gate.py), which passes the result in as each slice's new `epicDesignExists` field.\",\n+  \"alternatives\": [\n+    {\n+      \"option\": \"Add a new top-level 'Plans' or 'Gate Docs' tab in the viewer\",\n+      \"rejected_because\": \"ADR-0018 decided one lifecycle surface with a derived record index precisely to stop the reading surface count from growing per artifact type. A gate doc is scoped to a build already living in the viewer's Builds view (district: viewer), a sibling tab duplicates that context instead of joining it.\"\n+    },\n+    {\n+      \"option\": \"Parse gate docs ad hoc in the viewer's client-side JS at render time, skipping the index\",\n+      \"rejected_because\": \"ADR-0018's whole point is that joins get resolved once, in build_index.py, not scattered across client fetches. The viewer is also a single committed HTML file with no direct filesystem access model beyond its bundled data/ scripts (district: viewer): it cannot read docs/plans/ at render time at all.\"\n+    },\n+    {\n+      \"option\": \"Split viewer/index.html into parts now, and add gate docs to the new module structure\",\n+      \"rejected_because\": \"ADR-0020 decided the viewer-parts split but is explicitly not executed yet. Starting that execution as a side effect of this design is scope creep this design does not own.\"\n+    },\n+    {\n+      \"option\": \"Keep publishing gate docs as one-off Claude Artifacts\",\n+      \"rejected_because\": \"This is the workaround that exposed the gap: no rebuild hook ties it to docs/plans/, so it drifts, and it is not discoverable from the viewer itself.\"\n+    },\n+    {\n+      \"option\": \"For the slice-loop fix, have the orchestrating session poll or retry existsSync inside the workflow script\",\n+      \"rejected_because\": \"Workflow scripts have no filesystem or Node.js API access at all (confirmed against the tool's own authoring reference), so no retry inside the script can make the check work. The only place the check can run is the orchestrating session, before it invokes the workflow.\"\n+    }\n+  ],\n+  \"out_of_scope\": [\n+    \"a new top-level viewer tab for plans or gate docs\",\n+    \"editing a gate doc from the viewer, read-only sheet only\",\n+    \"changing how 00-status.md's approval state is parsed or displayed\",\n+    \"PDF or standalone-artifact export of a gate doc\",\n+    \"executing ADR-0020's viewer-parts split, this design writes into the current monolithic viewer/index.html\",\n+    \"re-scoring or re-running any slice that already ran under the old, broken slice-loop.js invocation\"\n+  ],\n+  \"risks\": [\n+    \"a gate doc grows long enough that the sheet reading UX (built for ADR text, a few hundred lines) degrades\",\n+    \"program_design/epic_design entity ids collide with, or get confused with, the existing epic entity's <design>/<epic-id> scoping\",\n+    \"the sheet becomes stale between build_index.py runs the same way the pre-fix Artifact was, if a future gate step forgets to rebuild\",\n+    \"a future edit to slice-loop.js reintroduces a Node.js API call (fs, path, etc.) above the meta literal, which fails the same two ways this fix corrects\"\n+  ],\n+  \"testing\": \"tests/test_build_index_gate_docs.py (10 cases) covers discover_plan_gate_docs, project_program_design, project_epic_design, and the Gate 3 doc-reference attachment in resolve_feature_gates. E2 was verified live in-browser against the running viewer (see commit 651b07a). The slice-loop.js fix was verified by reading the workflow-authoring reference's constraints (no filesystem access, meta must be the first statement) and by running the full pytest suite (322 passed, 4 pre-existing unrelated Pillow/webp failures from an arch-mismatched local Pillow install) after the change; no automated test exercises the Workflow tool invocation itself, since that requires the tool's runtime.\",\n+  \"reviewer_focus\": [\n+    \"whether program_design/epic_design entity ids collide with the epic entity's own id scheme\",\n+    \"whether the sheet UX holds up for a long program-design doc\",\n+    \"whether the Gate Rail card / epic chip is discoverable without extra instruction\",\n+    \"whether bundling the unrelated slice-loop.js fix into this design's PR, rather than a separate PR, is acceptable given it was found while dogfooding this same plugin\"\n+  ],\n+  \"unknowns\": [\n+    \"how large a typical 03-program-design.md or epic-<id>-design.md gets across real features, and whether the sheet needs pagination or truncation\",\n+    \"whether a design with no docs/plans/<slug>/ directory at all (a design never taken to implement mode) should render an empty Gate Rail or hide it entirely\"\n+  ]\n+}",
  "plugins/artifact/viewer/index.html": "diff --git a/plugins/artifact/viewer/index.html b/plugins/artifact/viewer/index.html\nindex 196bea3..289fe67 100644\n--- a/plugins/artifact/viewer/index.html\n+++ b/plugins/artifact/viewer/index.html\n@@ -512,21 +512,31 @@\n      just wider — reuses .drawer-header/.drawer-close/.drawer-body). Shares\n      #drawer-scrim with the comments drawer; JS enforces only one open at a time. ================= */\n   #adr-sheet,\n-  #assessment-sheet{\n+  #assessment-sheet,\n+  #gate-doc-sheet{\n     position:fixed;top:0;right:0;bottom:0;width:clamp(480px, 40vw, 720px);max-width:92vw;background:#fff;\n     border-left:1px solid var(--line);border-radius:14px 0 0 14px;box-shadow:-12px 0 34px rgba(20,25,35,0.2);\n     transform:translateX(100%);transition:transform .25s ease;z-index:41;\n     display:flex;flex-direction:column;min-height:0;\n   }\n   #adr-sheet.open,\n-  #assessment-sheet.open{transform:translateX(0);}\n+  #assessment-sheet.open,\n+  #gate-doc-sheet.open{transform:translateX(0);}\n   #adr-sheet .drawer-header,\n-  #assessment-sheet .drawer-header{align-items:flex-start;}\n+  #assessment-sheet .drawer-header,\n+  #gate-doc-sheet .drawer-header{align-items:flex-start;}\n   .adr-sheet-meta{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-top:6px;}\n   .adr-state-badge{font:10px 'JetBrains Mono',monospace;letter-spacing:.04em;padding:3px 9px;border-radius:10px;border:1px solid;text-transform:uppercase;white-space:nowrap;}\n   .adr-sheet-sourceline{font:11px 'JetBrains Mono',monospace;color:var(--ink-dim);}\n   #adr-sheet .drawer-body,\n-  #assessment-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+  #assessment-sheet .drawer-body,\n+  #gate-doc-sheet .drawer-body{overflow-y:auto;gap:16px;}\n+  .gate-rail-feature{margin-bottom:14px;}\n+  .gate-rail-feature-label{font-size:11px;color:var(--ink-dim);letter-spacing:.03em;margin-bottom:6px;}\n+  .gate-rail-clickable{cursor:pointer;}\n+  .gate-rail-clickable:hover{box-shadow:0 0 0 2px var(--accent, oklch(0.55 0.15 250)) inset;}\n+  .epic-design-doc-chip{display:inline-flex;align-items:center;gap:4px;font:10px 'JetBrains Mono',monospace;letter-spacing:.02em;padding:2px 8px;border-radius:9px;border:1px solid var(--line);color:var(--ink-dim);cursor:pointer;background:oklch(0.97 0.01 250);}\n+  .epic-design-doc-chip:hover{color:var(--ink);border-color:oklch(0.55 0.15 250);}\n \n   /* Design epic row — the Designs tab's level-3 epic blocks (renderDesignMainContent). */\n   .design-epic-row{display:flex;align-items:center;gap:8px;flex-wrap:wrap;font:11.5px 'JetBrains Mono',monospace;color:oklch(0.3 0.02 250);}\n@@ -1046,6 +1056,21 @@\n     <div class=\"drawer-body\" id=\"assessment-sheet-body\"></div>\n   </div>\n \n+  <!-- Gate document sheet (same Sheet family as ADR/assessment above), read-only.\n+       Reads window.INDEX.entities.program_design / .epic_design for the clicked\n+       Gate Rail card or epic design-doc chip (ADR-0022). Shares #drawer-scrim;\n+       only one sheet is ever open. -->\n+  <div id=\"gate-doc-sheet\" role=\"dialog\" aria-label=\"Gate document\">\n+    <div class=\"drawer-header\">\n+      <div>\n+        <h2 id=\"gate-doc-sheet-title\">Gate document</h2>\n+        <div class=\"adr-sheet-meta\" id=\"gate-doc-sheet-meta\"></div>\n+      </div>\n+      <button class=\"drawer-close\" id=\"gate-doc-sheet-close-btn\" title=\"Close (Esc)\">&times;</button>\n+    </div>\n+    <div class=\"drawer-body\" id=\"gate-doc-sheet-body\"></div>\n+  </div>\n+\n   <!-- Narration audio dialog (shadcn Dialog look, centered). Shares #drawer-scrim above;\n        mutually exclusive with the comments drawer / ADR sheet — see openAudioDialog. -->\n   <div id=\"audio-dialog-layer\">\n@@ -1168,6 +1193,10 @@ window.INDEX = window.INDEX || {};\n     activeAdrId: null,\n     // Generate-mode assessment sheet: mutually exclusive with both panels above.\n     assessmentOpen: false,\n+    // Gate document sheet (ADR-0022): mutually exclusive with every sheet above.\n+    gateDocOpen: false,\n+    activeGateDocKind: null, // 'program' | 'epic'\n+    activeGateDocId: null,\n     // First-class view mode. 'prs' is the current pull-request viewer.\n     // 'designs' mirrors it with a three-level design view. Persisted.\n     viewMode: 'prs',\n@@ -3209,14 +3238,19 @@ window.INDEX = window.INDEX || {};\n     const sliceToEpic = joins.slice_to_epic || {};\n     const featureGatesMap = joins.feature_gates || {};\n \n-    const gates = (featureGatesMap['cobuilder-family'] && featureGatesMap['cobuilder-family'].length > 0)\n-      ? featureGatesMap['cobuilder-family']\n-      : [\n+    // Every feature with a real docs/plans/<slug>/00-status.md gets its own rail.\n+    // A bundle with none yet (nothing built through implement mode) falls back to\n+    // one demo rail so the section never renders empty.\n+    const featureSlugsWithGates = Object.keys(featureGatesMap).filter(k => (featureGatesMap[k] || []).length > 0).sort();\n+    const gateRails = featureSlugsWithGates.length\n+      ? featureSlugsWithGates.map(slug => ({ feature: slug, gates: featureGatesMap[slug] }))\n+      : [{ feature: null, gates: [\n           { n: 1, name: 'Product', state: 'APPROVED 2026-08-20' },\n           { n: 2, name: 'Architecture', state: 'APPROVED 2026-08-21' },\n           { n: 3, name: 'Program Design', state: 'APPROVED 2026-08-21' },\n           { n: 4, name: 'Slice plan + rubrics', state: 'APPROVED 2026-08-21' }\n-        ];\n+        ] }];\n+    const totalGates = gateRails.reduce((sum, r) => sum + r.gates.length, 0);\n \n     const slicesByEpic = {};\n     slices.forEach(s => {\n@@ -3238,30 +3272,36 @@ window.INDEX = window.INDEX || {};\n       return a.id.localeCompare(b.id);\n     });\n \n-    // Section 1: Active Feature Gate Rail & Approval State (C3)\n+    // Section 1: Active Feature Gate Rail & Approval State (C3). One rail per\n+    // feature; a card with a `doc` (ADR-0022) opens the gate-doc sheet on click.\n     const gatesHtml = `\n       <div class=\"builds-section\" id=\"builds-gates-section\">\n         <div class=\"section-title-bar\">\n           <h3 class=\"section-title\">Active Feature Gate Rail & Approval State</h3>\n-          <span class=\"section-count mono\">${gates.length} Gates Approved</span>\n-        </div>\n-        <p class=\"section-intro\">Gate status chrome derived from status records. Active build runs on Gate 4.</p>\n-        <div class=\"gate-rail-grid\">\n-          ${gates.map(g => {\n-            const isApproved = (g.state || '').toUpperCase().startsWith('APPROVED');\n-            const isCurrent = g.n === 4;\n-            const statusClass = isApproved ? 'is-approved' : (isCurrent ? 'is-current' : 'is-planned');\n-            return `\n-              <div class=\"gate-card ${statusClass}\">\n-                <div class=\"gate-header\">\n-                  <div class=\"gate-num-badge mono\">Gate ${g.n}</div>\n-                  <div class=\"gate-status-pill ${isApproved ? 'ok' : (isCurrent ? 'now' : 'wait')}\">${escapeHtml(g.state)}</div>\n-                </div>\n-                <div class=\"gate-name\">${escapeHtml(g.name)}</div>\n-                <div class=\"gate-desc\">${isApproved ? `Approved on ${escapeHtml(g.state.replace(/^APPROVED\\s*/i, ''))}` : 'In progress'}</div>\n-              </div>`;\n-          }).join('')}\n+          <span class=\"section-count mono\">${totalGates} Gates Approved</span>\n         </div>\n+        <p class=\"section-intro\">Gate status chrome derived from status records. Click a gate card with a document icon to read it.</p>\n+        ${gateRails.map(rail => `\n+          <div class=\"gate-rail-feature\">\n+            ${rail.feature ? `<div class=\"gate-rail-feature-label mono\">${escapeHtml(rail.feature)}</div>` : ''}\n+            <div class=\"gate-rail-grid\">\n+              ${rail.gates.map(g => {\n+                const isApproved = (g.state || '').toUpperCase().startsWith('APPROVED');\n+                const isCurrent = g.n === 4;\n+                const statusClass = isApproved ? 'is-approved' : (isCurrent ? 'is-current' : 'is-planned');\n+                const clickAttrs = g.doc ? ` data-gate-doc-id=\"${escapeHtml(g.doc)}\" tabindex=\"0\" role=\"button\"` : '';\n+                return `\n+                  <div class=\"gate-card ${statusClass}${g.doc ? ' gate-rail-clickable' : ''}\"${clickAttrs}>\n+                    <div class=\"gate-header\">\n+                      <div class=\"gate-num-badge mono\">Gate ${g.n}</div>\n+                      <div class=\"gate-status-pill ${isApproved ? 'ok' : (isCurrent ? 'now' : 'wait')}\">${escapeHtml(g.state)}</div>\n+                    </div>\n+                    <div class=\"gate-name\">${escapeHtml(g.name)}${g.doc ? ' &#128196;' : ''}</div>\n+                    <div class=\"gate-desc\">${isApproved ? `Approved on ${escapeHtml(g.state.replace(/^APPROVED\\s*/i, ''))}` : 'In progress'}</div>\n+                  </div>`;\n+              }).join('')}\n+            </div>\n+          </div>`).join('')}\n       </div>`;\n \n     // Section 2: Backlog Lane (C1 & C2), grouped by parent design (\"program\")\n@@ -3304,6 +3344,10 @@ window.INDEX = window.INDEX || {};\n           <span class=\"deferred-text\">No slice yet &mdash; this epic is deferred past active scope.</span>\n         </div>`;\n \n+      const designDocChip = epic.design_doc\n+        ? `<span class=\"epic-design-doc-chip\" data-epic-design-id=\"${escapeHtml(epic.design_doc)}\" tabindex=\"0\" role=\"button\">&#128196; Design</span>`\n+        : '';\n+\n       return `\n         <div class=\"backlog-card ${isDeferred ? 'is-deferred' : ''}\">\n           <div class=\"backlog-card-header\">\n@@ -3311,6 +3355,7 @@ window.INDEX = window.INDEX || {};\n               <code>${escapeHtml(epic.epic_id || epic.id)}</code>\n             </div>\n             <span class=\"backlog-badge ${statusClass}\">${statusLabel}</span>\n+            ${designDocChip}\n           </div>\n           <div class=\"backlog-card-body\">\n             <p class=\"backlog-epic-note\">${escapeHtml(epic.note || 'No description recorded in goal.json.')}</p>\n@@ -3451,6 +3496,13 @@ window.INDEX = window.INDEX || {};\n         ${sectionsHtml}\n       </div>`);\n \n+    main.querySelectorAll('[data-gate-doc-id]').forEach(el => {\n+      el.addEventListener('click', () => openGateDocSheet('program', el.dataset.gateDocId));\n+    });\n+    main.querySelectorAll('[data-epic-design-id]').forEach(el => {\n+      el.addEventListener('click', () => openGateDocSheet('epic', el.dataset.epicDesignId));\n+    });\n+\n     main.scrollTop = 0;\n     hideCue();\n   }\n@@ -3627,6 +3679,7 @@ window.INDEX = window.INDEX || {};\n     if (state.commentsOpen) state.commentsOpen = false;\n     if (state.adrOpen) state.adrOpen = false;\n     if (state.assessmentOpen) state.assessmentOpen = false;\n+    if (state.gateDocOpen) state.gateDocOpen = false;\n     state.audioDialogOpen = true;\n     renderAudioDialogContent();\n     const dialog = document.getElementById('audio-dialog');\n@@ -3959,13 +4012,15 @@ window.INDEX = window.INDEX || {};\n     document.getElementById('content-col').classList.toggle('drawer-open', state.commentsOpen);\n     document.getElementById('adr-sheet').classList.toggle('open', state.adrOpen);\n     document.getElementById('assessment-sheet').classList.toggle('open', state.assessmentOpen);\n+    document.getElementById('gate-doc-sheet').classList.toggle('open', state.gateDocOpen);\n     const scrim = document.getElementById('drawer-scrim');\n-    scrim.classList.toggle('open', state.adrOpen || state.assessmentOpen || state.audioDialogOpen);\n+    scrim.classList.toggle('open', state.adrOpen || state.assessmentOpen || state.gateDocOpen || state.audioDialogOpen);\n     scrim.classList.toggle('audio-dim', state.audioDialogOpen);\n   }\n   function openCommentsDrawer(){\n     if (state.adrOpen){ state.adrOpen = false; }\n     if (state.assessmentOpen){ state.assessmentOpen = false; }\n+    if (state.gateDocOpen){ state.gateDocOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.commentsOpen = true;\n     renderSheetVisibility();\n@@ -3978,6 +4033,7 @@ window.INDEX = window.INDEX || {};\n   function openAdrSheet(id){\n     if (state.commentsOpen){ state.commentsOpen = false; }\n     if (state.assessmentOpen){ state.assessmentOpen = false; }\n+    if (state.gateDocOpen){ state.gateDocOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.adrOpen = true;\n     state.activeAdrId = id;\n@@ -3991,6 +4047,7 @@ window.INDEX = window.INDEX || {};\n   function openAssessmentSheet(){\n     if (state.commentsOpen){ state.commentsOpen = false; }\n     if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.gateDocOpen){ state.gateDocOpen = false; }\n     if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n     state.assessmentOpen = true;\n     renderAssessmentSheet();\n@@ -4000,10 +4057,26 @@ window.INDEX = window.INDEX || {};\n     state.assessmentOpen = false;\n     renderSheetVisibility();\n   }\n+  function openGateDocSheet(kind, id){\n+    if (state.commentsOpen){ state.commentsOpen = false; }\n+    if (state.adrOpen){ state.adrOpen = false; }\n+    if (state.assessmentOpen){ state.assessmentOpen = false; }\n+    if (state.audioDialogOpen) stopAudio(); // mutual exclusivity: closes + stops the audio dialog\n+    state.gateDocOpen = true;\n+    state.activeGateDocKind = kind;\n+    state.activeGateDocId = id;\n+    renderGateDocSheet();\n+    renderSheetVisibility();\n+  }\n+  function closeGateDocSheet(){\n+    state.gateDocOpen = false;\n+    renderSheetVisibility();\n+  }\n   function closeAnyOpenSheet(){\n     if (state.commentsOpen) closeCommentsDrawer();\n     else if (state.adrOpen) closeAdrSheet();\n     else if (state.assessmentOpen) closeAssessmentSheet();\n+    else if (state.gateDocOpen) closeGateDocSheet();\n     else if (state.audioDialogOpen) stopAudio();\n   }\n \n@@ -4269,6 +4342,33 @@ window.INDEX = window.INDEX || {};\n       </div>`;\n   }\n \n+  function renderGateDocSheet(){\n+    const kind = state.activeGateDocKind;\n+    const id = state.activeGateDocId;\n+    const entities = (window.INDEX && window.INDEX.entities) || {};\n+    const table = kind === 'epic' ? (entities.epic_design || []) : (entities.program_design || []);\n+    const record = id ? table.find(r => r.id === id) : null;\n+    const titleEl = document.getElementById('gate-doc-sheet-title');\n+    const metaEl = document.getElementById('gate-doc-sheet-meta');\n+    const body = document.getElementById('gate-doc-sheet-body');\n+\n+    if (!record){\n+      titleEl.textContent = id || 'Gate document';\n+      metaEl.innerHTML = '';\n+      body.innerHTML = `\n+        <div class=\"empty-card\">\n+          <div class=\"empty-detail\">Gate document not found in this bundle's index.</div>\n+        </div>`;\n+      return;\n+    }\n+\n+    titleEl.textContent = record.title || record.id;\n+    const sourceLine = record.source_path ? `source: ${record.source_path}` : '';\n+    const gateLine = kind === 'epic' ? 'Gate 4b' : 'Gate 3';\n+    metaEl.innerHTML = `<span class=\"adr-sheet-sourceline\">${escapeHtml(gateLine)}${sourceLine ? ' · ' + escapeHtml(sourceLine) : ''}</span>`;\n+    body.innerHTML = renderMarkdown(record.body_md || '');\n+  }\n+\n   function renderAssessmentSheet(){\n     const p = pr();\n     const a = p && p.assessment;\n@@ -4719,6 +4819,8 @@ window.INDEX = window.INDEX || {};\n   if (adrSheetCloseBtn) adrSheetCloseBtn.addEventListener('click', closeAdrSheet);\n   const assessmentSheetCloseBtn = document.getElementById('assessment-sheet-close-btn');\n   if (assessmentSheetCloseBtn) assessmentSheetCloseBtn.addEventListener('click', closeAssessmentSheet);\n+  const gateDocSheetCloseBtn = document.getElementById('gate-doc-sheet-close-btn');\n+  if (gateDocSheetCloseBtn) gateDocSheetCloseBtn.addEventListener('click', closeGateDocSheet);\n   const drawerScrim = document.getElementById('drawer-scrim');\n   if (drawerScrim) drawerScrim.addEventListener('click', closeAnyOpenSheet);\n   const btnAddComment = document.getElementById('btn-add-comment');\n@@ -4745,7 +4847,7 @@ window.INDEX = window.INDEX || {};\n   window.addEventListener('keydown', (e) => {\n     // Escape must close whichever sheet is open, even while the comment input is\n     // focused — checked before the input/textarea early-return below so it always works.\n-    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.assessmentOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n+    if (e.key === 'Escape' && (state.commentsOpen || state.adrOpen || state.assessmentOpen || state.gateDocOpen || state.audioDialogOpen)){ closeAnyOpenSheet(); return; }\n \n     const tag = document.activeElement && document.activeElement.tagName;\n     if (tag === 'TEXTAREA' || tag === 'INPUT') return;",
  "plugins/implement/skills/build/SKILL.md": "diff --git a/plugins/implement/skills/build/SKILL.md b/plugins/implement/skills/build/SKILL.md\nindex a09bbb4..1f6b884 100644\n--- a/plugins/implement/skills/build/SKILL.md\n+++ b/plugins/implement/skills/build/SKILL.md\n@@ -224,7 +224,16 @@ Run this protocol at every gate and before implementing an epic:\n 3. Ask the user: **\"Approve Gate N, or what should change?\"**\n 4. The user must clearly approve before you proceed.\n 5. Record approval in `00-status.md`.\n-6. If later work invalidates a decision, update the document, set status to \"in\n+6. **Refresh the bundle and start the viewer.** `00-status.md` just changed\n+   what the Builds Backlog Lane shows. Rebuild the self-bundle projection,\n+   then start the viewer so the user can watch the gate land, the same way\n+   step 5 after each slice does:\n+   ```bash\n+   uv run \"${CLAUDE_PLUGIN_ROOT}/shared/build_index.py\"\n+   ```\n+   Then `Skill(\"cobuilder-artifacts\", args=\"view\")`. Reuses an\n+   already-running server for this hub; never starts a second one.\n+7. If later work invalidates a decision, update the document, set status to \"in\n    progress\", and request approval again.\n \n ---\n@@ -454,7 +463,7 @@ on scope alone:\n \n | Mode | When to use | Instructions |\n |---|---|---|\n-| **Workflow script** | The feature is a program: `04-slices.md` groups slices under more than one epic | Invoke the Workflow tool, running `workflows/slice-loop.js`. Do not ask the user whether to use it first — a program-scale build always runs this way in a harness where the Workflow tool exists. |\n+| **Workflow script** | The feature is a program: `04-slices.md` groups slices under more than one epic | Invoke the Workflow tool with `scriptPath: \"${CLAUDE_PLUGIN_ROOT}/skills/build/workflows/slice-loop.js\"` — not `name: \"slice-loop\"`, which only resolves built-in or `.claude/workflows/`-registered workflows and will not find a plugin-shipped script. Before invoking it, confirm any required Gate 4b epic design files exist (e.g. with `verify_gate.py`) and pass the result in as each slice's `epicDesignExists`; the script itself has no filesystem access. Do not ask the user whether to use it first — a program-scale build always runs this way in a harness where the Workflow tool exists. |\n | **Manual** | A single epic, a single slice, or a configuration-only change | Spawn subagents from [references/slice-loop.md](references/slice-loop.md), one role at a time. |\n \n A harness with no Workflow tool always uses Manual, regardless of scope.\n@@ -477,9 +486,8 @@ A harness with no Workflow tool always uses Manual, regardless of scope.\n 3. **Record the score** in `00-status.md` and mark the slice complete.\n 4. **Route gaps** below 1.0 using the gap decision tree in\n    [references/validation-scoring.md](references/validation-scoring.md).\n-5. **Refresh the bundle and start the viewer.** `00-status.md` just\n-   changed what the Builds Backlog Lane shows. Rebuild the self-bundle\n-   projection, then start the viewer so the user can watch progress land:\n+5. **Refresh the bundle and start the viewer**, same as step 6 of the\n+   approval protocol above:\n    ```bash\n    uv run \"${CLAUDE_PLUGIN_ROOT}/shared/build_index.py\"\n    ```",
  "plugins/implement/skills/build/references/slice-loop.md": "diff --git a/plugins/implement/skills/build/references/slice-loop.md b/plugins/implement/skills/build/references/slice-loop.md\nindex af7cf9c..e97b344 100644\n--- a/plugins/implement/skills/build/references/slice-loop.md\n+++ b/plugins/implement/skills/build/references/slice-loop.md\n@@ -231,7 +231,13 @@ mode.\n \n **Scripted (multi-agent workflows).** `workflows/slice-loop.js` in this skill\n runs the loop with deterministic control flow. The user must explicitly opt in\n-to multi-agent orchestration before running the script.\n+to multi-agent orchestration before running the script. Invoke it with\n+`Workflow({scriptPath: \"${CLAUDE_PLUGIN_ROOT}/skills/build/workflows/slice-loop.js\", args: {...}})`\n+— `name: \"slice-loop\"` will not resolve, since the Workflow tool's `name`\n+input only looks up built-in or `.claude/workflows/`-registered workflows,\n+not plugin-shipped scripts. The script also has no filesystem access, so\n+each slice's `epicDesignExists` must be computed and passed in by the\n+orchestrating session (see the script's own args comment).\n \n ---\n ",
  "plugins/implement/skills/build/workflows/slice-loop.js": "diff --git a/plugins/implement/skills/build/workflows/slice-loop.js b/plugins/implement/skills/build/workflows/slice-loop.js\nindex f55bb2e..eaa5e29 100644\n--- a/plugins/implement/skills/build/workflows/slice-loop.js\n+++ b/plugins/implement/skills/build/workflows/slice-loop.js\n@@ -1,5 +1,3 @@\n-import { existsSync } from 'node:fs'\n-\n export const meta = {\n   name: 'slice-loop',\n   description: 'Build approved vertical slices via red-green-validate, gated on an independent blind score',\n@@ -16,7 +14,13 @@ export const meta = {\n // args: {\n //   slug:         feature slug, e.g. \"webhook-retry\"\n //   testCommand:  exact suite command, e.g. \"pytest tests/ -v\"\n-//   slices:       [{ id, name, goal, epicId }]   in build order\n+//   slices:       [{ id, name, goal, epicId, epicDesignExists }]   in build order\n+//                 epicDesignExists is required for any slice whose epic carries\n+//                 more than one slice (Gate 4b). Workflow scripts have no\n+//                 filesystem access, so the orchestrating session must check\n+//                 `docs/plans/<slug>/epic-<epicId>-design.md` itself — e.g. with\n+//                 verify_gate.py — before invoking this workflow, and pass the\n+//                 result in.\n //   accept:       optional, default 0.90\n //   maxAttempts:  optional, default 3\n // }\n@@ -125,16 +129,19 @@ for (const s of slices) {\n   // epics ship with zero epic-*-design.md files while 00-status.md still\n   // read Gate 4 as approved. Stop here instead, the same way a missing\n   // rubric already stops scoring — an absent artifact must halt the loop,\n-  // not be routed around.\n-  if (needsEpicDesign && !existsSync(epicDesignPath)) {\n+  // not be routed around. Workflow scripts have no filesystem access, so\n+  // the existence check itself ran in the orchestrating session (e.g. via\n+  // verify_gate.py) before this workflow was invoked, and its result\n+  // travels in on `s.epicDesignExists`.\n+  if (needsEpicDesign && s.epicDesignExists !== true) {\n     log(\n       `Slice ${s.id} stopped: epic ${s.epicId} carries ${slicesPerEpic[s.epicId]} slices, `\n-      + `so Gate 4b requires ${epicDesignPath}, which does not exist.`,\n+      + `so Gate 4b requires ${epicDesignPath}, which the caller did not confirm exists.`,\n     )\n     results.push({\n       slice: s,\n       verdict: 'ERROR',\n-      reason: `Gate 4b missing: ${epicDesignPath} does not exist for epic ${s.epicId}`,\n+      reason: `Gate 4b missing: ${epicDesignPath} not confirmed to exist for epic ${s.epicId}`,\n     })\n     break\n   }",
  "shared/build_index.py": "diff --git a/shared/build_index.py b/shared/build_index.py\nindex daad857..3a8b9bd 100644\n--- a/shared/build_index.py\n+++ b/shared/build_index.py\n@@ -246,6 +246,8 @@ GOAL_FIELDS = (\n EPIC_FIELDS = (\"id\", \"slug\", \"branch\", \"pr\", \"state\", \"outcome\")\n INTENT_FIELDS = (\"problem\", \"approach\", \"alternatives\")\n ASSESSMENT_FIELDS = (\"verdict\", \"findings\")\n+PROGRAM_DESIGN_FIELDS = (\"id\", \"feature_slug\", \"gate\", \"title\", \"body_md\", \"approved_date\", \"source_path\")\n+EPIC_DESIGN_FIELDS = (\"id\", \"epic_id\", \"feature_slug\", \"title\", \"body_md\", \"approved_date\", \"source_path\")\n \n \n def discover_goal_files(designs_dir: Path) -> list[Path]:\n@@ -633,6 +635,108 @@ def collect_plans(repo: Path) -> tuple[list[dict], list[str]]:\n     return entities, failures\n \n \n+# --------------------------------------------------------------------------\n+# Gate documents — feeds \"program_design\" + \"epic_design\" entities, from\n+# docs/plans/<slug>/03-program-design.md and epic-<id>-design.md (ADR-0022)\n+# --------------------------------------------------------------------------\n+\n+EPIC_DESIGN_FILENAME_RE = re.compile(r\"^epic-(.+)-design\\.md$\")\n+\n+\n+def _extract_title(text: str, fallback: str) -> str:\n+    for line in text.splitlines():\n+        stripped = line.strip()\n+        if stripped.startswith(\"# \"):\n+            return stripped[2:].strip()\n+    return fallback\n+\n+\n+def discover_plan_gate_docs(repo: Path) -> list[dict]:\n+    plans_dir = repo / PLANS_SOURCE_SUBDIR\n+    if not plans_dir.is_dir():\n+        return []\n+    found: list[dict] = []\n+    for slug_dir in sorted(p for p in plans_dir.iterdir() if p.is_dir()):\n+        feature_slug = slug_dir.name\n+        program_path = slug_dir / \"03-program-design.md\"\n+        if program_path.is_file():\n+            try:\n+                text = program_path.read_text()\n+            except OSError:\n+                pass\n+            else:\n+                found.append(\n+                    {\n+                        \"kind\": \"program\",\n+                        \"feature_slug\": feature_slug,\n+                        \"epic_id\": None,\n+                        \"path\": program_path,\n+                        \"text\": text,\n+                    }\n+                )\n+        for epic_path in sorted(slug_dir.glob(\"epic-*-design.md\")):\n+            match = EPIC_DESIGN_FILENAME_RE.match(epic_path.name)\n+            if not match:\n+                continue\n+            epic_id = match.group(1)\n+            try:\n+                text = epic_path.read_text()\n+            except OSError:\n+                continue\n+            found.append(\n+                {\n+                    \"kind\": \"epic\",\n+                    \"feature_slug\": feature_slug,\n+                    \"epic_id\": epic_id,\n+                    \"path\": epic_path,\n+                    \"text\": text,\n+                }\n+            )\n+    return found\n+\n+\n+def project_program_design(feature_slug: str, path: Path, text: str) -> dict:\n+    source = {\n+        \"id\": feature_slug,\n+        \"feature_slug\": feature_slug,\n+        \"gate\": 3,\n+        \"title\": _extract_title(text, feature_slug),\n+        \"body_md\": text,\n+        \"approved_date\": None,\n+        \"source_path\": str(path),\n+    }\n+    return project_fields(source, PROGRAM_DESIGN_FIELDS)\n+\n+\n+def project_epic_design(epic_id: str, feature_slug: str, path: Path, text: str) -> dict:\n+    source = {\n+        \"id\": f\"{feature_slug}/{epic_id}\",\n+        \"epic_id\": epic_id,\n+        \"feature_slug\": feature_slug,\n+        \"title\": _extract_title(text, epic_id),\n+        \"body_md\": text,\n+        \"approved_date\": None,\n+        \"source_path\": str(path),\n+    }\n+    return project_fields(source, EPIC_DESIGN_FIELDS)\n+\n+\n+def collect_gate_docs(repo: Path) -> tuple[list[dict], list[dict], list[str]]:\n+    failures: list[str] = []\n+    program_entities: list[dict] = []\n+    epic_entities: list[dict] = []\n+    for doc in discover_plan_gate_docs(repo):\n+        if doc[\"kind\"] == \"program\":\n+            program_entities.append(\n+                project_program_design(doc[\"feature_slug\"], doc[\"path\"], doc[\"text\"])\n+            )\n+        else:\n+            epic_entities.append(\n+                project_epic_design(doc[\"epic_id\"], doc[\"feature_slug\"], doc[\"path\"], doc[\"text\"])\n+            )\n+    return program_entities, epic_entities, failures\n+\n+\n # --------------------------------------------------------------------------\n # Pull requests — feeds \"pull_request\" entities, from the bundle's story.json\n # --------------------------------------------------------------------------\n@@ -969,6 +1073,9 @@ def resolve_feature_gates(repo: Path) -> dict[str, list[dict]]:\n     gates_by_feature: dict[str, list[dict]] = {}\n     if not plans_dir.is_dir():\n         return gates_by_feature\n+    program_design_features = {\n+        doc[\"feature_slug\"] for doc in discover_plan_gate_docs(repo) if doc[\"kind\"] == \"program\"\n+    }\n     for status_path in sorted(plans_dir.glob(\"*/00-status.md\")):\n         feature = status_path.parent.name\n         gates = []\n@@ -976,13 +1083,14 @@ def resolve_feature_gates(repo: Path) -> dict[str, list[dict]]:\n             for line in status_path.read_text().splitlines():\n                 gm = STATUS_GATE_RE.search(line.strip())\n                 if gm:\n-                    gates.append(\n-                        {\n-                            \"n\": int(gm.group(1)),\n-                            \"name\": gm.group(2).strip(),\n-                            \"state\": gm.group(3).strip(),\n-                        }\n-                    )\n+                    gate = {\n+                        \"n\": int(gm.group(1)),\n+                        \"name\": gm.group(2).strip(),\n+                        \"state\": gm.group(3).strip(),\n+                    }\n+                    if gate[\"n\"] == 3 and feature in program_design_features:\n+                        gate[\"doc\"] = feature\n+                    gates.append(gate)\n         except OSError:\n             continue\n         if gates:\n@@ -1126,6 +1234,14 @@ def build_index(repo: Path, bundle_dir: Path) -> tuple[dict, dict, dict, list[st\n     publication_entities, publication_failures = collect_publications(bundle_dir)\n     failures.extend(publication_failures)\n \n+    program_design_entities, epic_design_entities, gate_doc_failures = collect_gate_docs(repo)\n+    failures.extend(gate_doc_failures)\n+\n+    epic_design_ids = {r[\"id\"] for r in epic_design_entities}\n+    for epic in epic_entities:\n+        if epic[\"id\"] in epic_design_ids:\n+            epic[\"design_doc\"] = epic[\"id\"]\n+\n     entities = {\n         \"adr\": adr_entities,\n         \"design\": design_entities,\n@@ -1136,6 +1252,8 @@ def build_index(repo: Path, bundle_dir: Path) -> tuple[dict, dict, dict, list[st\n         \"pull_request\": pull_request_entities,\n         \"slice\": slice_entities,\n         \"publication\": publication_entities,\n+        \"program_design\": program_design_entities,\n+        \"epic_design\": epic_design_entities,\n     }\n \n     joins, join_warnings = resolve_joins(repo, adrs_viewer, designs_viewer, entities)",
  "tests/test_build_index_gate_docs.py": "diff --git a/tests/test_build_index_gate_docs.py b/tests/test_build_index_gate_docs.py\nnew file mode 100644\nindex 0000000..39e9eff\n--- /dev/null\n+++ b/tests/test_build_index_gate_docs.py\n@@ -0,0 +1,160 @@\n+\"\"\"Tests for build_index.py's Gate 3 / Gate 4b document projection (ADR-0022).\n+\n+Run with: uv run --with pytest pytest tests/test_build_index_gate_docs.py -v\n+\"\"\"\n+from __future__ import annotations\n+\n+import subprocess\n+import sys\n+from pathlib import Path\n+\n+sys.path.insert(0, str(Path(__file__).resolve().parent.parent / \"shared\"))\n+\n+import build_index  # noqa: E402\n+\n+\n+def init_repo(repo: Path) -> None:\n+    subprocess.run([\"git\", \"init\", \"-q\"], cwd=repo, check=True)\n+    subprocess.run([\"git\", \"config\", \"user.email\", \"test@example.com\"], cwd=repo, check=True)\n+    subprocess.run([\"git\", \"config\", \"user.name\", \"Test\"], cwd=repo, check=True)\n+    (repo / \"README.md\").write_text(\"placeholder\\n\")\n+    subprocess.run([\"git\", \"add\", \".\"], cwd=repo, check=True)\n+    subprocess.run([\"git\", \"commit\", \"-q\", \"-m\", \"init\"], cwd=repo, check=True)\n+\n+\n+def write_program_design(repo: Path, slug: str, title: str = \"Program Design: Widget\", body: str = \"## Files\\nfoo.py\\n\") -> Path:\n+    plan_dir = repo / \"docs\" / \"plans\" / slug\n+    plan_dir.mkdir(parents=True, exist_ok=True)\n+    path = plan_dir / \"03-program-design.md\"\n+    path.write_text(f\"# {title}\\n\\n{body}\")\n+    return path\n+\n+\n+def write_epic_design(repo: Path, slug: str, epic_id: str, title: str | None = None) -> Path:\n+    plan_dir = repo / \"docs\" / \"plans\" / slug\n+    plan_dir.mkdir(parents=True, exist_ok=True)\n+    path = plan_dir / f\"epic-{epic_id}-design.md\"\n+    title = title or f\"Epic Technical Solution Design: {epic_id}\"\n+    path.write_text(f\"# {title}\\n\\n## Scope and Intent\\nsomething\\n\")\n+    return path\n+\n+\n+def write_status(repo: Path, slug: str, gate3_state: str = \"APPROVED 2026-09-01\") -> None:\n+    plan_dir = repo / \"docs\" / \"plans\" / slug\n+    plan_dir.mkdir(parents=True, exist_ok=True)\n+    (plan_dir / \"00-status.md\").write_text(\n+        f\"# Status: {slug}\\n\\n\"\n+        f\"- Gate 1 — Product: APPROVED 2026-09-01\\n\"\n+        f\"- Gate 2 — Architecture: APPROVED 2026-09-01\\n\"\n+        f\"- Gate 3 — Program Design: {gate3_state}\\n\"\n+    )\n+\n+\n+# ---------------------------------------------------------------------\n+# C1 (slice 1) — index.json always carries both new entity keys\n+# ---------------------------------------------------------------------\n+\n+\n+def test_build_index_has_program_design_and_epic_design_keys_with_no_plans_dir(tmp_path):\n+    init_repo(tmp_path)\n+    index, _, _, failures = build_index.build_index(tmp_path, tmp_path / \".cobuilder-architect\" / \"self\")\n+    assert failures == []\n+    assert index[\"entities\"][\"program_design\"] == []\n+    assert index[\"entities\"][\"epic_design\"] == []\n+\n+\n+# ---------------------------------------------------------------------\n+# Slice 2 — real parsing\n+# ---------------------------------------------------------------------\n+\n+\n+def test_project_program_design_reads_title_and_body(tmp_path):\n+    init_repo(tmp_path)\n+    path = write_program_design(tmp_path, \"widget-feature\", title=\"Program Design: Widget Feature\")\n+    text = path.read_text()\n+    record = build_index.project_program_design(\"widget-feature\", path, text)\n+    assert record[\"id\"] == \"widget-feature\"\n+    assert record[\"feature_slug\"] == \"widget-feature\"\n+    assert record[\"gate\"] == 3\n+    assert record[\"title\"] == \"Program Design: Widget Feature\"\n+    assert \"## Files\" in record[\"body_md\"]\n+\n+\n+def test_project_epic_design_id_is_scoped_feature_slug_and_epic_id(tmp_path):\n+    init_repo(tmp_path)\n+    path = write_epic_design(tmp_path, \"widget-feature\", \"E1\")\n+    text = path.read_text()\n+    record = build_index.project_epic_design(\"E1\", \"widget-feature\", path, text)\n+    assert record[\"id\"] == \"widget-feature/E1\"\n+    assert record[\"epic_id\"] == \"E1\"\n+    assert record[\"feature_slug\"] == \"widget-feature\"\n+\n+\n+def test_discover_plan_gate_docs_finds_program_and_epic_docs(tmp_path):\n+    init_repo(tmp_path)\n+    write_program_design(tmp_path, \"widget-feature\")\n+    write_epic_design(tmp_path, \"widget-feature\", \"E1\")\n+    write_epic_design(tmp_path, \"widget-feature\", \"E2\")\n+    found = build_index.discover_plan_gate_docs(tmp_path)\n+    kinds = sorted((f[\"kind\"], f.get(\"epic_id\")) for f in found)\n+    assert kinds == [(\"epic\", \"E1\"), (\"epic\", \"E2\"), (\"program\", None)]\n+\n+\n+def test_build_index_end_to_end_projects_real_gate_docs(tmp_path):\n+    init_repo(tmp_path)\n+    write_program_design(tmp_path, \"widget-feature\", title=\"Program Design: Widget Feature\")\n+    write_epic_design(tmp_path, \"widget-feature\", \"E1\")\n+    write_status(tmp_path, \"widget-feature\")\n+    index, _, _, failures = build_index.build_index(tmp_path, tmp_path / \".cobuilder-architect\" / \"self\")\n+    assert failures == []\n+    pd_ids = {r[\"id\"] for r in index[\"entities\"][\"program_design\"]}\n+    ed_ids = {r[\"id\"] for r in index[\"entities\"][\"epic_design\"]}\n+    assert pd_ids == {\"widget-feature\"}\n+    assert ed_ids == {\"widget-feature/E1\"}\n+\n+\n+def test_resolve_feature_gates_attaches_doc_when_program_design_exists(tmp_path):\n+    init_repo(tmp_path)\n+    write_program_design(tmp_path, \"widget-feature\")\n+    write_status(tmp_path, \"widget-feature\")\n+    gates = build_index.resolve_feature_gates(tmp_path)\n+    gate3 = next(g for g in gates[\"widget-feature\"] if g[\"n\"] == 3)\n+    assert gate3.get(\"doc\") == \"widget-feature\"\n+\n+\n+def test_resolve_feature_gates_gate3_doc_key_absent_when_no_md_file(tmp_path):\n+    init_repo(tmp_path)\n+    write_status(tmp_path, \"widget-feature\")  # status exists, but no 03-program-design.md\n+    gates = build_index.resolve_feature_gates(tmp_path)\n+    gate3 = next(g for g in gates[\"widget-feature\"] if g[\"n\"] == 3)\n+    assert \"doc\" not in gate3\n+\n+\n+# ---------------------------------------------------------------------\n+# Slice 3 — edge cases\n+# ---------------------------------------------------------------------\n+\n+\n+def test_discover_plan_gate_docs_no_plans_dir_returns_empty_list(tmp_path):\n+    init_repo(tmp_path)\n+    assert build_index.discover_plan_gate_docs(tmp_path) == []\n+\n+\n+def test_discover_plan_gate_docs_skips_malformed_epic_filename(tmp_path):\n+    init_repo(tmp_path)\n+    plan_dir = tmp_path / \"docs\" / \"plans\" / \"widget-feature\"\n+    plan_dir.mkdir(parents=True)\n+    (plan_dir / \"epic-design.md\").write_text(\"# no id segment\\n\")\n+    write_epic_design(tmp_path, \"widget-feature\", \"E1\")\n+    found = build_index.discover_plan_gate_docs(tmp_path)\n+    epic_ids = sorted(f[\"epic_id\"] for f in found if f[\"kind\"] == \"epic\")\n+    assert epic_ids == [\"E1\"]\n+\n+\n+def test_discover_plan_gate_docs_slug_with_only_slices_has_no_program_design(tmp_path):\n+    init_repo(tmp_path)\n+    plan_dir = tmp_path / \"docs\" / \"plans\" / \"no-gate3\"\n+    plan_dir.mkdir(parents=True)\n+    (plan_dir / \"04-slices.md\").write_text(\"| # | Epic | Slice | Ends with | Score | State |\\n|---|---|---|---|---|---|\\n\")\n+    found = build_index.discover_plan_gate_docs(tmp_path)\n+    assert found == []"
};
