window.INDEX = {"schema_version": "1.3", "sources": {"git_head": "74fb1a8999e2d9f65894069396f2339a1a2916c2", "trees": {"docs/architecture/adr": "sha256:8c9b27d528f4a38689cefbfb2f38c6cc8ddeeedc35f19ef9d3f33f2df216f293", "docs/architecture/designs": "sha256:71aa2cee9ee652f6f20c251660b44bc75bd07e9b4d592a2dbc6a94b8453eac08", "docs/architecture/contexts": "sha256:b85daa6bfabf8944b88764cdad745c29c02f7882a31c3c5d45995633546caad0", "docs/plans": "sha256:328a36a30c27dd8f484103c05429e6cdba579e302140f27c5d1eb5387bc9410f"}}, "entities": {"adr": [{"id": "ADR-0001", "title": "Flatten the bundle viewer into one self-contained file for Claude Artifact publishing", "state": "approved"}, {"id": "ADR-0002", "title": "Track publish provenance with a commit SHA + content hash, not by re-threading git history", "state": "approved"}, {"id": "ADR-0003", "title": "Unify bundle storage under .prodyssey/, self-analysis into .prodyssey/self/", "state": "approved"}, {"id": "ADR-0004", "title": "Keep the ste-writing skill dev-local, out of the plugin's install surface", "state": "approved"}, {"id": "ADR-0005", "title": "Diagram authoring delegated to a per-PR subagent, never the orchestrating Claude or a script", "state": "approved"}, {"id": "ADR-0006", "title": "Three-phase, guard-checked bundle self-migration instead of one version-gated ladder", "state": "approved"}, {"id": "ADR-0007", "title": "Anchor the GEMINI_API_KEY / .env lookup to the hub's cwd, never to a --repo target", "state": "approved"}, {"id": "ADR-0008", "title": "Cap and lock third-party dependencies in the three Gemini-calling scripts", "state": "approved"}, {"id": "ADR-0009", "title": "Submit mode ends the pre stage by opening the real PR, never a synthetic key", "state": "approved"}, {"id": "ADR-0010", "title": "Interview self-consistency check compares blind author accounts by LLM judgment, not keyword matching", "state": "approved"}, {"id": "ADR-0011", "title": "Design mode: capture intent before the code exists", "state": "decided"}, {"id": "ADR-0012", "title": "CoBuilder Implement: a design becomes verified code", "state": "approved"}, {"id": "ADR-0013", "title": "Design mode and cobuilder-implement: split the first gate, join through a file", "state": "approved"}, {"id": "ADR-0014", "title": "Authored source lives in docs/; the bundle is derived", "state": "decided"}, {"id": "ADR-0015", "title": "Four shipped skills, architecture modes self-only", "state": "decided"}, {"id": "ADR-0016", "title": "Five sibling plugins, with the bundle as the only seam", "state": "approved"}, {"id": "ADR-0017", "title": "Vendored shared code, and a compatibility gate the bundle owns", "state": "approved"}, {"id": "ADR-0018", "title": "One lifecycle surface, and a record index that joins it", "state": "approved"}, {"id": "ADR-0019", "title": "Anchored comments, kept as a durable ledger", "state": "approved"}, {"id": "ADR-0020", "title": "Viewer parts and an author-time build", "state": "rejected"}, {"id": "ADR-0021", "title": "book-index.md gains a nano tier, and full-book loading stops being unconditional", "state": "decided"}, {"id": "ADR-0022", "title": "Gate documents projected into the record index", "state": "decided"}, {"id": "ADR-0023", "title": "A TypeScript and React viewer, compiled at author time", "state": "decided"}, {"id": "ADR-0024", "title": "Vendored design-to-code: a trimmed three-step skill behind a conditional Gate 2b", "state": "approved"}, {"id": "ADR-0025", "title": "Simulate paths to close the open set, from the common ancestor, and show one validated path", "state": "rejected"}, {"id": "ADR-0026", "title": "Execute an accepted path onto integration branches, never onto the default branch", "state": "rejected"}, {"id": "ADR-0027", "title": "FlightDeck: one surface, two modes, a ledger-recorded merge-order recommendation", "state": "decided"}, {"id": "ADR-0028", "title": "A level is sections on a track, and the box owns the scroll", "state": "decided"}], "design": [{"id": "book-index-tiering", "name": "book-index-tiering", "outcome": "book-index.md offers nano, mini, and full tiers for all 14 vendored books, with a 3-nano-minimum judgment-gated escalation rule; Review/Maintenance's mandatory 14-file security corpus load gets the same cheap-signal-before-expensive-read shape, opposite default bias, no vendoring.", "stage": "review"}, {"id": "build-workflow-polish", "name": "build-workflow-polish", "outcome": "An engineer running architect:design into implement:start never loses the handoff, 04-slices.md is always written in the shape verify_gate.py actually parses, a validated slice is committed without an extra prompt, and the bundle viewer borrows the tab-based Diagram / Data flow / Data types layout a colleague prototyped.", "stage": "superseded"}, {"id": "cobuilder-implement", "name": "cobuilder-implement", "outcome": "The architecture design produced by cobuilder-architect carries directly into implementation: its intent is decomposed into testable epics, and each epic ships as vertical slices that an independent validator scores at 0.90 or better before the next one starts.", "stage": "approved"}, {"id": "cobuilder-viewer", "name": "cobuilder-viewer", "outcome": "A reader opens the bundle on three goal-shaped surfaces, built as TypeScript and React and compiled to one file. FlightDeck, the surface that replaces the Pull requests tab, shows a single pull request at today's parity, or lets the reviewer select several open pull requests to get a validated merge-path recommendation and a narrative for that set.", "stage": "review"}, {"id": "design-mode", "name": "design-mode", "outcome": "An engineer can design a change with the plugin before writing it, and the resulting intent, ADR, and diagrams carry through to the pull request that implements it.", "stage": "implemented"}, {"id": "gate-doc-surfacing", "name": "gate-doc-surfacing", "outcome": "Gate documents (03-program-design.md, epic-<id>-design.md) are readable from the viewer's Builds view, not just from docs/plans/ or a one-off Artifact publish.", "stage": "decided"}, {"id": "gate-state-resolution", "name": "gate-state-resolution", "outcome": "A gate line reads as approved, as not applicable, or as open in both Builds views, and a view opens only a document that the plan holds.", "stage": "approved"}, {"id": "inflight-record-store", "name": "inflight-record-store", "outcome": "A reader on the main branch sees every piece of in-flight work across all open pull requests, and no pull request owner hits a merge conflict caused by that view.", "stage": "backlog"}, {"id": "interaction-design-gate", "name": "interaction-design-gate", "outcome": "A session that builds a user interface writes an interaction design specification before implementation starts, and a browser check inside the slice loop catches the defects that specification exists to prevent.", "stage": "decided"}, {"id": "lean-bundle-diffs", "name": "lean-bundle-diffs", "outcome": "A fresh clone of this repo is not weighed down by every merged PR's full diff text, and the viewer still shows a diff for any PR a reader opens.", "stage": "backlog"}, {"id": "maintainable-viewer", "name": "maintainable-viewer", "outcome": "An engineer changes one view mode of the bundle viewer by opening one file of a few hundred lines, and the file that ships is still one self-contained HTML page that publishes as an Artifact unchanged.", "stage": "superseded"}, {"id": "plugin-split", "name": "plugin-split", "outcome": "A user installs one stage of the architecture lifecycle without the other three, and four independently-installed plugins write into one bundle without a stale copy destroying authored or paid content.", "stage": "implemented"}, {"id": "react-viewer", "name": "react-viewer", "outcome": "A reader opens the bundle on three goal-shaped surfaces instead of five record-type tabs, an engineer changes one surface by opening one typed React component, and the file that ships is still one self-contained HTML page that publishes as an Artifact unchanged.", "stage": "superseded"}, {"id": "review-flight-deck", "name": "review-flight-deck", "outcome": "On every pull request change, the flightdeck simulates possible paths to close the whole open set, replays the best one or two with real git, and shows the reviewer one validated path. The reviewer reads what they would have when it is done, then what each pull request adds, then whether the merges hold.", "stage": "superseded"}, {"id": "ubiquitous-language", "name": "ubiquitous-language", "outcome": "One term means one thing across every plugin, skill, design, and ADR in the repo, and a new or colliding term cannot reach a merged pull request undetected.", "stage": "backlog"}], "epic": [{"id": "book-index-tiering/E1", "design": "book-index-tiering", "epic_id": "E1", "branch": "design/book-index-tiering", "pr": 16, "state": "open", "note": null}, {"id": "build-workflow-polish/E1", "design": "build-workflow-polish", "epic_id": "E1", "branch": null, "pr": null, "state": "completed", "note": "Stage 7 in architect's references/design-mode.md ends design mode after creating the first branch, with no pointer to what runs next. Add one line after the branch is created: 'Design mode's work ends here. To build this design's epics, run /implement:start.' Do not have design mode invoke implement:start itself — architecture modes are self-only in scope, and implement already owns Gate 1's design-record lookup (build/SKILL.md's 'Optional: design mode record' section). This is a one-line prose addition to a mode-governing reference file, so it still needs the Gate-selection exception's behavioral rubric, not a skip."}, {"id": "build-workflow-polish/E2", "design": "build-workflow-polish", "epic_id": "E2", "branch": null, "pr": null, "state": "completed", "note": "04-slices.md's Gate 4a instructions in build/SKILL.md describe the table only through the worked example, and never say the table must match shared/slice_table.py's exact column shape (six columns: #, Epic, Slice, Ends with, Score, State, with the epic-header row's precise empty-cell pattern) or point to that module as the source of truth. Add an explicit sentence and a link, so an authoring session does not freehand a table shape verify_gate.py silently fails to parse. Consider also having verify_gate.py surface parse_table()'s `unparsed` list by name when it is non-empty, instead of only reporting downstream symptoms like a missing epic."}, {"id": "build-workflow-polish/E3", "design": "build-workflow-polish", "epic_id": "E3", "branch": null, "pr": null, "state": "completed", "note": "build/SKILL.md's 'After each slice' section (the four numbered steps following the red-green-validate loop) records the score and asks 'Proceed to the next slice, or adjust direction?' but never commits. In practice this produces a spurious 'want to commit this checkpoint first?' prompt after every slice instead of an automatic commit. Change step 2 (or add a step) so that once VALIDATE scores a slice at or above 0.90, the orchestrator commits the slice's changes to git immediately, before asking whether to proceed — no confirmation question for the commit itself, only for what comes after. A score below threshold still routes through the existing gap decision tree in references/validation-scoring.md, and does not commit. Below-threshold slices accepted with reservations after three attempts should also commit, since that path already represents a final accepted state, not a pending one."}, {"id": "build-workflow-polish/E4", "design": "build-workflow-polish", "epic_id": "E4", "branch": null, "pr": null, "state": "planned", "note": "A colleague's scratch artifact (/private/tmp/.../diagram-viewer.html, session-local, not part of this repo) uses a top-of-panel tab switcher across three views — Diagram, Data flow, Data types — instead of stacking them, plus a right-hand info rail. Evaluate adopting the same top-tab pattern for viewer/index.html's per-PR diagram levels (currently level 1/2/3 switching, per ADR-0020's viewer/src/ plan), and separately evaluate narrowing the left-hand navigation rail so it takes less width once tabs handle top-level switching. This touches only the viewer, so it can run through architect:design on its own without waiting on E1-E3."}, {"id": "cobuilder-implement/E1", "design": "cobuilder-implement", "epic_id": "E1", "branch": "design/design-mode", "pr": 11, "state": "open", "note": "Shares a branch and a pull request with the design-mode design. See ADR-0013: a branch may carry more than one design."}, {"id": "cobuilder-implement/E2", "design": "cobuilder-implement", "epic_id": "E2", "branch": "docs/post-pr11-cleanup", "pr": 12, "state": "open", "note": "Gate 4b is enforced instead of merely specified. The per-epic technical solution design already exists in SKILL.md section 4b, with a six-section template, and five downstream files reference the artifact. Nothing checks that it was written. Checked on 2026-08-24: zero epic-<id>-design.md files exist, and six epics shipped without one. Four failures allow that. Gate 4's only exit check counts rubrics, so a Gate 4 that skips 4b still passes. slice-loop.js builds the epic-design path and never checks the file exists, so the red and green agents are told to read a file that is absent and quietly fall back to 03-program-design.md. rubric-authoring.md tells the author to update the technical design first, and nobody checks that either. No test asserts the artifact. The root cause is that 4b is the only Gate 4 sub-step whose output nothing downstream mechanically requires: the loop cannot score without rubrics or iterate without a slice plan, but it can read an absent design. This epic adds the exit check, the existence guard in the workflow, a test, and sub-step tracking in 00-status.md so an approved Gate 4 cannot hide a missing 4b. Raised 2026-08-22, re-scoped 2026-08-24 from 'write the process' to 'enforce the process' after reading the skill."}, {"id": "cobuilder-viewer/E1", "design": "cobuilder-viewer", "epic_id": "E1", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "planned", "note": "Carried from react-viewer E1 unchanged. Must land before any bundled build, or publishing breaks silently. It stays in the narrowed scope because the markers live inside the committed index.html, and E2 changes how that file is produced. This epic is the regression guard on a shipped mode: publishing must keep working while the build underneath it changes."}, {"id": "cobuilder-viewer/E2", "design": "cobuilder-viewer", "epic_id": "E2", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "planned", "note": "Carried from react-viewer E2 unchanged. Vite plus vite-plugin-singlefile, a lockfile, and a pinned Node version. Starts with a spike: byte-equal rebuild across two machines, size with React inlined, and whether a Claude Artifact renders a Mermaid block React injects after page load.", "design_doc": "cobuilder-viewer/E2"}, {"id": "cobuilder-viewer/E3", "design": "cobuilder-viewer", "epic_id": "E3", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "planned", "note": "Carried from react-viewer E3, extended per ADR-0027's answer to Q13. Define the placeholder type and a trivial compile-time exhaustiveness check now, in this epic, not retrofitted when E7 starts."}, {"id": "cobuilder-viewer/E4", "design": "cobuilder-viewer", "epic_id": "E4", "branch": "design/cobuilder-viewer/work-prototype", "pr": 21, "state": "open", "note": "New in round 2, at the engineer's direction. Prototype each surface in detail before committing to the build. review-flight-deck already used that discipline twice for FlightDeck: flightdeck-prototype.html, then flightdeck-hindsight-fork.html against real multi-author data. react-viewer's own 2026-09-07 prototype proved the Work board's shape, and it was never committed. This epic repeats that work and keeps the artifact, at docs/architecture/designs/cobuilder-viewer/prototypes/work-prototype.html. The engineer chose one pull request for the design, so all eighteen epics share one branch, design/cobuilder-viewer/work-prototype. Work starts here, because the three prototypes come before any React build."}, {"id": "cobuilder-viewer/E5", "design": "cobuilder-viewer", "epic_id": "E5", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "planned", "note": "Carried from react-viewer E4, with one vocabulary change. The level-and-section model replaced the lens model, per ADR-0028: a level is a sequence of sections, one section on screen at a time on a horizontal track, and the box owns the scroll rather than the pane. react-viewer's lens list described navigation rather than content, and it contradicted itself: every place that named the lenses listed six (intent, problem and solution, architecture, risks and verdict, build, pull requests), while three places said seven, including this epic's outcome. No seventh lens was ever named, and nothing renders a lens now. This design's paged levels are Intent, Problem & Solution, Architecture, and Build, and each one states its own sections. The criterion that survives from that outcome is the sparse record: a design whose records are goal.json only must render. Seven designs in this repository carry one, and they are the fixture for it. E4's prototype is the reference and the approval gate for this epic, not the uncommitted 2026-09-07 prototype. E4 shipped as a React application under plugins/artifact/viewer/src/variations/sections-e/, not as the static HTML file round 2's note asked for.", "design_doc": "cobuilder-viewer/E5"}, {"id": "cobuilder-viewer/E6", "design": "cobuilder-viewer", "epic_id": "E6", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "planned", "note": "New in round 2, at the engineer's direction. review-flight-deck's own flightdeck-prototype.html and flightdeck-hindsight-fork.html already prototyped multi-pull-request mode in detail against real data. Neither showed it composed with single-pull-request mode as one surface with a switch, and that composition is what this epic adds. It reuses both existing prototypes rather than rebuilding either half. Commits to docs/architecture/designs/cobuilder-viewer/prototypes/flightdeck-prototype.html."}, {"id": "cobuilder-viewer/E7", "design": "cobuilder-viewer", "epic_id": "E7", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "planned", "note": "Carried from react-viewer E5, renamed from 'Flight deck' to FlightDeck's single-pull-request mode. Parity covers the four levels, diagrams, scene art, narration audio, the diff, and the intent and assessment sheet. Ships before E10, because E10 depends on the typed PullRequest shape this epic exercises first.", "design_doc": "cobuilder-viewer/E7"}, {"id": "cobuilder-viewer/E8", "design": "cobuilder-viewer", "epic_id": "E8", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "New in round 2, at the engineer's direction. Unlike Work and FlightDeck, this surface has no prior prototype. Commits to docs/architecture/designs/cobuilder-viewer/prototypes/reference-prototype.html. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E9", "design": "cobuilder-viewer", "epic_id": "E9", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "Carried from react-viewer E6 unchanged. tests/test_viewer_modes.py asserts the five data-mode buttons. That test changes with this epic, and the change is the point, not a regression. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E10", "design": "cobuilder-viewer", "epic_id": "E10", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "Carried from review-flight-deck F0 unchanged. When this design measured it, 8 of 12 pull requests in this repository carried no intent block. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E11", "design": "cobuilder-viewer", "epic_id": "E11", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "Carried from review-flight-deck F1. This epic also settles the open-pull-request entity question (Q13) for the record index, so no separate inflight-record-store epic duplicates it. The mode reads the auto-merge split from the team's own rules, and this design does not define them. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E12", "design": "cobuilder-viewer", "epic_id": "E12", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "Merges review-flight-deck F2 and F3 into one epic, per ADR-0027's rejection of a live-in-browser or GitHub-Action mechanism. It must classify the three non-authored conflict shapes: a shared generated file, a hot file, and identical content added on both sides. A route that fails the replay stays, and the failure becomes its reason for losing. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E13", "design": "cobuilder-viewer", "epic_id": "E13", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "Merges review-flight-deck F4 and F5. flightdeck-hindsight-fork.html, and E6's composed prototype, are the reference layout. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E14", "design": "cobuilder-viewer", "epic_id": "E14", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "New in this merge, from the challenge gate's adoption of the auditor and 3am-on-call diverge candidates. The proposal line must record the exact ancestor SHA and per-step simulation inputs a later replay needs, which is new schema work neither source design carried. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E15", "design": "cobuilder-viewer", "epic_id": "E15", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "Carried from review-flight-deck F6 unchanged. ADR-0027 records the decision, folding ADR-0026 forward. The first epic in this design that writes to a remote. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E16", "design": "cobuilder-viewer", "epic_id": "E16", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "Carried from react-viewer E7 unchanged. Closes the loop on E1. Measures the real budget with React inlined, and settles the Mermaid question the abort condition names. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E17", "design": "cobuilder-viewer", "epic_id": "E17", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "Carried from react-viewer E8 unchanged. build-workflow-polish stays superseded through this design now, not directly through react-viewer. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E18", "design": "cobuilder-viewer", "epic_id": "E18", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "deferred", "note": "Carried from react-viewer E9 unchanged. Settles ADR-0019's anchoring under React, which owns the DOM the anchor walk uses. Deferred on 2026-09-22, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. Left the build; not deleted and not renumbered."}, {"id": "cobuilder-viewer/E19", "design": "cobuilder-viewer", "epic_id": "E19", "branch": "design/cobuilder-viewer/work-prototype", "pr": null, "state": "planned", "note": "New in round 3, when the engineer narrowed the scope to the Work surface and FlightDeck at parity. The shell had no landing surface before it: a reader arrived on a route that named a work item, and nothing listed the bundle's designs. The board lists them, says what each one holds, and opens one.", "design_doc": "cobuilder-viewer/E19"}, {"id": "design-mode/E1", "design": "design-mode", "epic_id": "E1", "branch": "design/design-mode", "pr": 11, "state": "open", "note": "Design mode as a pre-code interview and challenge gate. Shares a branch and a pull request with the cobuilder-implement design. See ADR-0013: a branch may carry more than one design."}, {"id": "gate-doc-surfacing/E1", "design": "gate-doc-surfacing", "epic_id": "E1", "branch": "design/gate-doc-surfacing", "pr": 19, "state": "open", "note": "shared/build_index.py: add PROGRAM_DESIGN_FIELDS and EPIC_DESIGN_FIELDS projection tuples plus project_program_design/project_epic_design, following the project_fields(source, fields) pattern used by project_goal/project_intent/project_assessment. Parse docs/plans/<slug>/03-program-design.md and epic-<id>-design.md; emit program_design and epic_design entities into data/index.json. Extend feature_gates join entries with a doc reference where one exists.", "design_doc": "gate-doc-surfacing/E1"}, {"id": "gate-doc-surfacing/E2", "design": "gate-doc-surfacing", "epic_id": "E2", "branch": "design/gate-doc-surfacing", "pr": 19, "state": "open", "note": "plugins/artifact/viewer/index.html: make gate-rail-grid cards (renderBuildsMainContent, ~line 3249) clickable, opening a gate-doc sheet modeled on openAdrSheet. Add a design-doc chip to renderEpicCard (~line 3282) for epics with an epic_design entity.", "design_doc": "gate-doc-surfacing/E2"}, {"id": "gate-state-resolution/E1", "design": "gate-state-resolution", "epic_id": "E1", "branch": null, "pr": null, "state": "planned", "note": "Teach both Builds views the three meanings of a gate state. `is_resolved(state)` in `plugins/artifact/scripts/build_builds_view.py` holds the test, and `current_doc()` receives the document map filtered to the files the plan holds, so a returned document always exists. The static page gains the `na` treatment and stops printing the text `undefined` for a Gate 2b document. The viewer rail gains the `is-na` card class, the `na` pill class, the words \"Not applicable\", and a header count of approved gates. Slices 1 and 2.", "design_doc": "gate-state-resolution/E1"}, {"id": "inflight-record-store/E1", "design": "inflight-record-store", "epic_id": "E1", "branch": null, "pr": null, "state": "planned", "note": "Open pull requests become entities. collect_pull_requests() merges gh pr list --state all metadata over the narrated story.json entities. An open pull request gets an entity with state: \"open\" and a narrated: false flag. This removes the placeholder in refine_epic_status(). It reads metadata only, checks out no branch, and creates no conflict. This is the tracer bullet, and it is useful on its own. It does not depend on E2, E3, or E4, and can ship alone."}, {"id": "inflight-record-store/E2", "design": "inflight-record-store", "epic_id": "E2", "branch": null, "pr": null, "state": "planned", "note": "A record store with a merge strategy. One file per record, append-only, with a defined resolution rule for a concurrent write. Modelled on beads. This epic decides the storage shape and nothing else."}, {"id": "inflight-record-store/E3", "design": "inflight-record-store", "epic_id": "E3", "branch": null, "pr": null, "state": "planned", "note": "Cross-branch collection without a conflict. Records written by a session on a branch reach the shared view without any branch writing a shared generated file."}, {"id": "inflight-record-store/E4", "design": "inflight-record-store", "epic_id": "E4", "branch": null, "pr": null, "state": "planned", "note": "The active-work view. The viewer renders in-flight work beside merged history, so a reader sees one timeline rather than two."}, {"id": "interaction-design-gate/E1", "design": "interaction-design-gate", "epic_id": "E1", "branch": null, "pr": null, "state": "planned", "note": "Vendor the skill into `plugins/implement/skills/design-to-code/` and trim it from five steps to three. Step 1 goes because Gate 1 `01-product.md` already holds the product intent, and Step 4 goes because the slice loop owns code. The four concepts the gate depends on, initial state plus timer arming, transitions on state change, visibility gating, and declared defaults, reach the specification through the rewritten template, the JSONC schema, and the implementation rules. Slices 1 and 2.", "design_doc": "interaction-design-gate/E1"}, {"id": "interaction-design-gate/E2", "design": "interaction-design-gate", "epic_id": "E2", "branch": null, "pr": null, "state": "planned", "note": "Add Gate 2b to the build workflow. A tracked line in `00-status.md` between Gate 2 and Gate 3, a `## Gate 2b` section in `build/SKILL.md`, the `2b` line in the status template, the resume rule that keeps Gate 3 from starting early, and the two consumption rules that let `rubric-authoring.md` and `slice-loop.md` read the artifacts. Slices 3 and 4.", "design_doc": "interaction-design-gate/E2"}, {"id": "interaction-design-gate/E3", "design": "interaction-design-gate", "epic_id": "E3", "branch": null, "pr": null, "state": "planned", "note": "Make the gate mechanical rather than remembered. A `2b` group in `verify_gate.py`, a `2b` label in both gate-line regexes, `GATE_DOCS[\"2b\"]`, an `interaction_design` entity in the index, and the Builds view rail. Three call sites assume the last gate is number 4, so this epic is larger than it looks. It is the epic to cut. Slices 5 to 7.", "design_doc": "interaction-design-gate/E3"}, {"id": "lean-bundle-diffs/E1", "design": "lean-bundle-diffs", "epic_id": "E1", "branch": null, "pr": null, "state": "planned", "note": "Decide the replacement shape: the viewer reads a diff by running git diff against the PR's recorded commit SHAs at serve time (view mode already runs from inside the repo), versus keeping diffs-pr{N}.js exactly as it is today but gitignoring it, regenerating it from git on demand the way exports/branch-*/diff.json already works for generate mode. Either removes the committed weight; they differ in whether a served bundle still needs a live git checkout next to it."}, {"id": "lean-bundle-diffs/E2", "design": "lean-bundle-diffs", "epic_id": "E2", "branch": null, "pr": null, "state": "planned", "note": "Whichever shape E1 picks, implement it in extract_diffs.py and, if the viewer starts requesting diffs at view time instead of reading a static file, in artifact's serve_bundle.py. Update verify_bundle.py's expectations accordingly."}, {"id": "lean-bundle-diffs/E3", "design": "lean-bundle-diffs", "epic_id": "E3", "branch": null, "pr": null, "state": "planned", "note": "Migrate the existing committed bundles (self, and the two fixture bundles) so history stops growing: remove the checked-in diffs-pr{N}.js files, or move them under .gitignore, per E1's decision. This does not touch story.json's authored fields, so it is a bundle-file change, not a schema migration."}, {"id": "maintainable-viewer/E1", "design": "maintainable-viewer", "epic_id": "E1", "branch": null, "pr": null, "state": "planned", "note": "The build exists and is guarded. build_viewer.py concatenates ordered parts under viewer/src/ into the committed viewer/index.html, and a test rebuilds from the parts and fails on any difference. The first split is mechanical only, cut at the file's own 35 banner comments, with no line rewritten. This epic is the tracer bullet: it proves or disproves that the single IIFE separates by concatenation alone, which is the abort condition the whole design rests on."}, {"id": "maintainable-viewer/E2", "design": "maintainable-viewer", "epic_id": "E2", "branch": null, "pr": null, "state": "planned", "note": "The exporter seam becomes declared. export_artifact.py stops matching incidental literals and matches named markers the parts emit deliberately. Today it hard-errors with not-found-verbatim when a literal moves, so the publish pipeline breaks silently under any refactor until somebody publishes. This must land before the parts are rewritten, not after, or every later epic risks breaking publishing without a test noticing."}, {"id": "maintainable-viewer/E3", "design": "maintainable-viewer", "epic_id": "E3", "branch": null, "pr": null, "state": "planned", "note": "One definition per fragment. A template layer replaces the 45 innerHTML string blocks, and the rail-item, empty-card, and empty-detail fragments collapse from 4, 16, and 18 copies to one each. The viewer uses no template element today, so the mechanism is a design choice this epic makes and does not inherit."}, {"id": "maintainable-viewer/E4", "design": "maintainable-viewer", "epic_id": "E4", "branch": null, "pr": null, "state": "planned", "note": "Each mode owns its own part. The five view modes stop interleaving: the 50 scattered mode-predicate calls collapse into one registry, and renderContextsMainContent, renderBuildsMainContent, and renderMainContent stop being three functions of over 200 lines that live beside each other. Adding a sixth mode becomes one new part plus one registry entry."}, {"id": "plugin-split/E1", "design": "plugin-split", "epic_id": "E1", "branch": "design/design-mode", "pr": 11, "state": "open", "note": "One plugin becomes five. The whole packaging split: the mode renames inside today's single plugin, the deleted duplicate explore-design command, the 33 cross-pillar references, the five manifests, and the two ports. The rename is a rotation, not a split: odyssey submit becomes cobuilder-pr:generate, and the old odyssey generate, which narrates merged history, becomes cobuilder-pr:review. Four slices."}, {"id": "plugin-split/E2", "design": "plugin-split", "epic_id": "E2", "branch": "design/design-mode", "pr": 11, "state": "open", "note": "Shared code survives an install. Prove the marketplace symlink dereference, then vendor the four bundle modules and the two shared skills. One slice, because it is a spike whose result decides whether the rest of the design stands. goal.json.abort_if names its failure."}, {"id": "plugin-split/E3", "design": "plugin-split", "epic_id": "E3", "branch": "design/design-mode", "pr": 11, "state": "open", "note": "The seam is version-safe. require_compatible(), min_reader_schema, the generators map, bundle format 3, and schema 1.3, so two plugins at different versions cannot corrupt one bundle. Two slices."}, {"id": "plugin-split/E4", "design": "plugin-split", "epic_id": "E4", "branch": "design/design-mode", "pr": 11, "state": "open", "note": "The record index. build_index.py replaces the two projection scripts, emits every entity with a stable id, and resolves every join including an ADR to its pull request. Two slices."}, {"id": "plugin-split/E5", "design": "plugin-split", "epic_id": "E5", "branch": "design/design-mode", "pr": 11, "state": "open", "note": "One lifecycle surface. The Decisions, Contexts, and Builds modes, the Backlog lane, and moving generated pages out of the foreign directory into the bundle. Two slices (10, 11)."}, {"id": "plugin-split/E6", "design": "plugin-split", "epic_id": "E6", "branch": "design/design-mode", "pr": 11, "state": "open", "note": "The reply channel. The append-only ledger and its projection, the anchor computed from the live DOM, the write endpoint, and the background wake command. Three slices (12, 13, 14)."}, {"id": "plugin-split/E7", "design": "plugin-split", "epic_id": "E7", "branch": null, "pr": null, "state": "planned", "note": "Threads read as conversations. The drawer renders a reply under the comment it answers. Deferred past E6 on purpose: E6 ships the record shape, so this epic adds no migration and carries no slice yet."}, {"id": "plugin-split/E8", "design": "plugin-split", "epic_id": "E8", "branch": "rename-plugins-drop-cobuilder-prefix", "pr": 17, "state": "merged", "note": "The cobuilder- prefix drops from the four non-umbrella plugins named in E1's table (cobuilder-architect, cobuilder-pr, cobuilder-artifact, cobuilder-implement become architect, pr, artifact, implement), since it duplicates the marketplace repo's own name. cobuilder-full-lifecycle keeps its prefix, being the umbrella the repo's own name already carries. Merged as PR #17, then a same-epic follow-up (uncommitted at the time this entry was written) fixes a second-order bug the rename introduced: two of the four renamed plugins (implement, artifact) already shipped a skill folder named after themselves, and once the plugin took that same name too, Skill() resolution inside Claude Code broke -- reproduced directly, not environment-specific. Fixed by renaming the colliding skill folders (implement's skill to build, its command to start; artifact's skill to cobuilder-artifacts) and, for consistency, cobuilder-full-lifecycle's orientation skill to cobuilder-full. See ADR-0016's addendum."}, {"id": "react-viewer/E1", "design": "react-viewer", "epic_id": "E1", "branch": null, "pr": null, "state": "planned", "note": "The exporter rewrites the viewer by verbatim string replacement today and hard-errors when a literal moves. A bundled build moves every literal, so this seam must become explicit before any build lands."}, {"id": "react-viewer/E2", "design": "react-viewer", "epic_id": "E2", "branch": null, "pr": null, "state": "planned", "note": "Vite plus vite-plugin-singlefile, a lockfile, and a pinned Node version. This epic starts with a spike, at the engineer's direction: publish one throwaway bundled page and measure the byte-equal rebuild across two machines, the size with React inlined, and whether a Claude Artifact renders a Mermaid block that React injects after page load. Two of the three abort conditions are settled by that spike, before five epics of work depend on the answer."}, {"id": "react-viewer/E3", "design": "react-viewer", "epic_id": "E3", "branch": null, "pr": null, "state": "planned", "note": "The viewer resolves joins by hand in each render path today. index.json already computed them. This epic makes the index the source and the types the contract."}, {"id": "react-viewer/E4", "design": "react-viewer", "epic_id": "E4", "branch": null, "pr": null, "state": "planned", "note": "The prototype of 2026-09-07 is the reference for this surface."}, {"id": "react-viewer/E5", "design": "react-viewer", "epic_id": "E5", "branch": null, "pr": null, "state": "planned", "note": "Parity covers the four levels, diagrams, scene art, narration audio, the diff, and the intent and assessment sheet."}, {"id": "react-viewer/E6", "design": "react-viewer", "epic_id": "E6", "branch": null, "pr": null, "state": "planned", "note": "tests/test_viewer_modes.py asserts the five data-mode buttons. That test changes with this epic, and the change is the point, not a regression."}, {"id": "react-viewer/E7", "design": "react-viewer", "epic_id": "E7", "branch": null, "pr": null, "state": "planned", "note": "Closes the loop on E1. Measures the real budget with React inlined, and settles the Mermaid question the third abort condition names."}, {"id": "react-viewer/E8", "design": "react-viewer", "epic_id": "E8", "branch": null, "pr": null, "state": "planned", "note": "build-workflow-polish is superseded by this design at the engineer's direction. Its E4, the tab-based viewer layout, is absorbed by E4 and E5 here. Its other three epics are not viewer work and would be lost silently: E1 adds the pointer from design mode stage 7 to /implement:start, E2 states the six-column slice-table shape that shared/slice_table.py parses, and E3 commits an accepted slice without a second prompt. They are carried here so the supersede is safe."}, {"id": "react-viewer/E9", "design": "react-viewer", "epic_id": "E9", "branch": null, "pr": null, "state": "planned", "note": "The comment mechanism does not work today, and the engineer confirmed it on 2026-09-07. Three causes, all verified in the tree. First, LEDGER_AVAILABLE at viewer/index.html line 1399 tests only whether fetch exists in the browser, which is always true, so it never tells a reviewer that no ledger server is listening. Second, a failed POST falls back to localStorage and shows 'Will sync when server is reachable', although nothing syncs: no code reads that key back. Third, a published Artifact cannot reach any server, because the Artifact CSP blocks fetch, so every comment left where review actually happens is written to one browser's localStorage and lost. This epic also settles ADR-0019's anchoring under React, which owns the DOM the anchor walk uses. The transport is decided: the served viewer keeps POST /feedback under --allow-write, and the published Artifact marks comments read-only and exports them for paste-back through the copy-as-markdown path that already exists. The Artifact platform's own storage is rejected, because it would make the published page behave differently from the served one, which is the split ADR-0001 exists to prevent."}, {"id": "review-flight-deck/F0", "design": "review-flight-deck", "epic_id": "F0", "branch": "design/review-flight-deck", "pr": null, "state": "planned", "note": "New in round 4. It closes the gap the prototype had to show as a warning: 8 of 12 pull requests in this repository carry no intent block. Reuses generate mode whole, and adds only the check for an existing description."}, {"id": "review-flight-deck/F1", "design": "review-flight-deck", "epic_id": "F1", "branch": "design/review-flight-deck", "pr": null, "state": "planned", "note": "The same work as the first epic of the inflight-record-store design. Build it once, in shared code. The auto-merge split is read from the team's own rules, and this design does not define them."}, {"id": "review-flight-deck/F2", "design": "review-flight-deck", "epic_id": "F2", "branch": "design/review-flight-deck", "pr": null, "state": "planned", "note": "divergent-exploration.md already generates competing options in isolation and criticises them in a separate pass. The baseline is load-bearing: simulating against the default branch inverts the order whenever an open pull request restructures the tree."}, {"id": "review-flight-deck/F3", "design": "review-flight-deck", "epic_id": "F3", "branch": "design/review-flight-deck", "pr": null, "state": "planned", "note": "New in round 4, and the step that makes the recommendation something to stand behind. Read-only against a scratch worktree. A route that fails the replay is kept, with the failure as its reason for losing. Systems that precompute mergeable slices may already do this part, so keep the interface narrow enough to swap one in. It must also classify what it cannot merge. A superseded branch and an unresolvable conflict are different findings and get different treatments in the layout."}, {"id": "review-flight-deck/F4", "design": "review-flight-deck", "epic_id": "F4", "branch": "design/review-flight-deck", "pr": null, "state": "planned", "note": "Round 5 fixed the register. State the outcome first, then the contributions, and never lead with the mechanics. The engineer's worked example: perform the renames and restructuring across the repository, which merges without conflict, then incorporate the new viewer into the resulting structure and naming conventions."}, {"id": "review-flight-deck/F5", "design": "review-flight-deck", "epic_id": "F5", "branch": "design/review-flight-deck", "pr": null, "state": "planned", "note": "Reuses the existing view modes, the level rail, and the ledger from ADR-0019. flightdeck-prototype.html is the reference layout."}, {"id": "review-flight-deck/F6", "design": "review-flight-deck", "epic_id": "F6", "branch": "design/review-flight-deck", "pr": null, "state": "planned", "note": "New in round 6, and the first epic in this design that writes to a remote. ADR-0026 records the decision. The integration branch per bundle is what makes a path abandonable: delete N branches and the default branch never saw any of it. The default branch receives one pull request, from the last bundle's branch."}, {"id": "ubiquitous-language/E1", "design": "ubiquitous-language", "epic_id": "E1", "branch": null, "pr": null, "state": "planned", "note": "One language record, and it is the tracer bullet. docs/architecture/language.md becomes the single source of truth for a term, its owning context, its meaning, and what it must not be confused with. It is seeded from CLAUDE.md's existing Vocabulary table and from cobuilder-packaging's canvas section 3. shared/build_index.py projects it into the record index, so the viewer can render it. This epic is independently shippable and does not depend on E2 through E5."}, {"id": "ubiquitous-language/E2", "design": "ubiquitous-language", "epic_id": "E2", "branch": null, "pr": null, "state": "planned", "note": "Design mode gains a language stage. A design that introduces a new term must declare it or reuse an existing entry from the language record. A collision with an existing term becomes a challenge-stage finding. This fits design mode's existing no-citation-no-challenge rule, because the language record gives the challenge stage something concrete to cite."}, {"id": "ubiquitous-language/E3", "design": "ubiquitous-language", "epic_id": "E3", "branch": null, "pr": null, "state": "planned", "note": "The mechanical consumer, in cobuilder-implement. A Gate 4b technical solution design that names a concept absent from the language record fails the gate. This epic is what stops the whole program from rotting the way canvas section 3 did. E1 and E2 without E3 are documentation waiting to go stale."}, {"id": "ubiquitous-language/E4", "design": "ubiquitous-language", "epic_id": "E4", "branch": null, "pr": null, "state": "planned", "note": "A verifier that exits non-zero. A script finds a term used in a design or an ADR and absent from the language record, and fails the check. It is modeled on plugins/cobuilder-implement/scripts/verify_gate.py, which exits non-zero on any failed Gate 4 sub-step. The honest open question is that deciding what counts as a term mechanically, rather than by judgement, is the hard part of this epic and may need its own exploration."}, {"id": "ubiquitous-language/E5", "design": "ubiquitous-language", "epic_id": "E5", "branch": null, "pr": null, "state": "planned", "note": "Repair the existing drift. Refresh cobuilder-packaging's canvas section 3 and its boundary.yaml public_interface against the shipped five-plugin tree, delete the now-false homonym comment, and reconcile the two glossaries into E1's record."}], "context": [{"id": "cobuilder-packaging", "name": "Plugin packaging and dispatch", "path": ".", "verifies": ["commands", "skills", "scripts", "viewer", "docs"]}], "district": [{"id": "skills", "label": "Skills", "paths": ["skills"]}, {"id": "scripts", "label": "Scripts", "paths": ["scripts"]}, {"id": "commands", "label": "Commands", "paths": ["commands"]}, {"id": "viewer", "label": "Viewer", "paths": ["viewer"]}, {"id": "docs", "label": "Docs", "paths": ["docs"]}, {"id": ".cobuilder-architect", "label": "Bundle Store", "paths": [".cobuilder-architect"]}], "boundary_rule": [{"id": "cobuilder-packaging/forbidden-dependency/1", "context": "cobuilder-packaging", "kind": "forbidden-dependency", "detail": {"target": "another plugin's files (post-ADR-0016 split)", "why": "ADR-0016: 'No plugin reads or imports another plugin's files. A plugin may hand off to another plugin's mode by name, and the state that crosses the handoff travels in the bundle and in docs/.' Not yet enforceable -- only one plugin (cobuilder-architect) exists today, so there is no sibling to violate this against. Recorded now so the rule is in place before the split lands."}}, {"id": "cobuilder-packaging/forbidden-dependency/2", "context": "cobuilder-packaging", "kind": "forbidden-dependency", "detail": {"target": "skills/odyssey/SKILL.md", "from": "skills/architecture", "why": "SMELL: skills/architecture/SKILL.md lines 83-84 hardcode the path 'skills/odyssey/SKILL.md' inside Design mode's Hub-resolution step. Verified by grep. Harmless today (both skills ship in one plugin), but this is exactly the edge ADR-0016 forbids once architecture and odyssey become separate plugins (cobuilder-architect and cobuilder-pr). Needs an ADR-sanctioned fix (e.g. duplicate the Hub-resolution text, or move it to a shared vendored module) before the split, or it becomes real drift the day the split ships."}}, {"id": "cobuilder-packaging/forbidden-dependency/3", "context": "cobuilder-packaging", "kind": "forbidden-dependency", "detail": {"target": "skills/architecture/SKILL.md", "from": "skills/odyssey", "why": "SMELL: skills/odyssey/SKILL.md lines 32-33 name and point at 'skills/architecture/SKILL.md' when describing where Design mode lives. Same coupling as the entry above, reversed direction. Verified by grep."}}, {"id": "cobuilder-packaging/forbidden-dependency/4", "context": "cobuilder-packaging", "kind": "forbidden-dependency", "detail": {"target": "skills/architecture/references/*", "from": "scripts (build_designs.py, build_adrs.py, validate_decision_state.py)", "why": "SMELL: three scripts embed a literal 'skills/architecture/references/...' path inside a validation-failure error message (build_designs.py:299, build_adrs.py:217, validate_decision_state.py:433). Text, not an import, so it does not execute a cross-plugin call today. Still a coupling a post-split remediation hint cannot resolve, because the reading plugin (e.g. cobuilder-pr) cannot open a path inside cobuilder-architect's cache."}}, {"id": "cobuilder-packaging/forbidden-dependency/5", "context": "cobuilder-packaging", "kind": "forbidden-dependency", "detail": {"target": "skills/odyssey/references/*", "from": "scripts (build_diagrams.py, render_review.py, verify_bundle.py)", "why": "SMELL: same pattern, reversed. build_diagrams.py:9 and :273, render_review.py:9, and verify_bundle.py:86 embed 'skills/odyssey/references/...' in comments or error text. Verified by grep."}}, {"id": "cobuilder-packaging/forbidden-dependency/6", "context": "cobuilder-packaging", "kind": "forbidden-dependency", "detail": {"target": "scripts/*.py (as a Python import, not a shell invocation)", "from": "viewer/index.html", "why": "The viewer never imports or executes anything under scripts/. Its one mention of 'scripts/' (line 854) is a code comment naming the script that produces data/diagrams.js, not a reference the browser resolves. Recorded as a confirmed absence, not a smell -- this is the rule holding, not breaking."}}, {"id": "cobuilder-packaging/module-invariant/1", "context": "cobuilder-packaging", "kind": "module-invariant", "detail": {"id": "plugin-manifest", "path": ".claude-plugin/", "public_interface": ["plugin.json", "marketplace.json"], "allowed_inbound": [], "allowed_outbound": [], "rule": "Static JSON only. No module in this repo reads or writes it at runtime."}}, {"id": "cobuilder-packaging/module-invariant/2", "context": "cobuilder-packaging", "kind": "module-invariant", "detail": {"id": "commands", "path": "commands/", "members": ["baseline.md", "generate.md", "view.md", "publish.md", "submit.md", "design.md", "review.md", "maintenance.md", "decisions.md", "describe.md", "debug.md", "explore-design.md"], "public_interface": [], "allowed_inbound": [], "allowed_outbound": ["skills-architecture", "skills-odyssey"], "rule": "Each command file makes exactly one Skill(...) dispatch call and nothing else. Verified: every commands/*.md greps to exactly one Skill(\"architecture\"|\"odyssey\", ...) line."}}, {"id": "cobuilder-packaging/module-invariant/3", "context": "cobuilder-packaging", "kind": "module-invariant", "detail": {"id": "skills-architecture", "path": "skills/architecture/", "public_interface": ["Skill(\"architecture\", args=...)"], "allowed_inbound": ["commands"], "allowed_outbound": ["scripts", "skills-mermaid", "skills-ste-writing"], "rule": "SMELL: also reaches skills-odyssey via a hardcoded SKILL.md path reference (see forbidden_dependencies). Should be [] toward skills-odyssey once ADR-0016 lands."}}, {"id": "cobuilder-packaging/module-invariant/4", "context": "cobuilder-packaging", "kind": "module-invariant", "detail": {"id": "skills-odyssey", "path": "skills/odyssey/", "public_interface": ["Skill(\"odyssey\", args=...)"], "allowed_inbound": ["commands"], "allowed_outbound": ["scripts", "skills-mermaid", "skills-ste-writing"], "rule": "SMELL: also reaches skills-architecture via a hardcoded SKILL.md path reference (see forbidden_dependencies). Should be [] toward skills-architecture once ADR-0016 lands."}}, {"id": "cobuilder-packaging/module-invariant/5", "context": "cobuilder-packaging", "kind": "module-invariant", "detail": {"id": "skills-mermaid", "path": "skills/mermaid/", "public_interface": ["Skill(\"mermaid\")", "Skill(\"cobuilder-architect:mermaid\")"], "allowed_inbound": ["skills-architecture", "skills-odyssey"], "allowed_outbound": [], "rule": "Diagram-authoring rules only. Invoked by a per-PR/per-design subagent, never by the orchestrating Claude directly, per CLAUDE.md."}}, {"id": "cobuilder-packaging/module-invariant/6", "context": "cobuilder-packaging", "kind": "module-invariant", "detail": {"id": "skills-ste-writing", "path": "skills/ste-writing/", "public_interface": ["Skill(\"ste-writing\")", "Skill(\"cobuilder-architect:ste-writing\")", "ste-lint.py"], "allowed_inbound": ["skills-architecture", "skills-odyssey"], "allowed_outbound": [], "rule": "Writing-style rules and a rules-only linter. No dependency on any other module verified."}}, {"id": "cobuilder-packaging/module-invariant/7", "context": "cobuilder-packaging", "kind": "module-invariant", "detail": {"id": "skills-collaborate-with-user", "path": "skills/collaborate-with-user/", "public_interface": ["Skill(\"collaborate-with-user\")"], "allowed_inbound": [], "allowed_outbound": [], "rule": "References skills/architecture/SKILL.md's 'Report Generation' section in prose (line 95) to say it does not duplicate that convention. This is documentation cross-talk, not a code or Skill-dispatch edge -- no forbidden_dependencies entry warranted."}}, {"id": "cobuilder-packaging/module-invariant/8", "context": "cobuilder-packaging", "kind": "module-invariant", "detail": {"id": "scripts", "path": "scripts/", "members": ["_bundle_meta.py", "_manifest.py", "build_adrs.py", "build_designs.py", "build_diagrams.py", "compute_scores.py", "export_artifact.py", "export_index.py", "extract_diffs.py", "extract_story.py", "generate_audio.py", "generate_prompts.py", "html_to_pdf.py", "migrate_bundle.py", "record_publish.py", "render_review.py", "validate_decision_state.py", "verify_bundle.py"], "public_interface": ["_bundle_meta.SCHEMA_VERSION", "_bundle_meta.SCHEMA_VERSION_KNOWN", "_bundle_meta.CURRENT_BUNDLE_FORMAT", "_manifest.rewrite_manifest"], "allowed_inbound": ["skills-architecture", "skills-odyssey"], "allowed_outbound": [], "rule": "Every script that reads or writes bundle data imports _bundle_meta.SCHEMA_VERSION for its own version stamp. _bundle_meta.py itself is a leaf: stdlib-only, no imports beyond __future__. ADR-0017's require_compatible() gate does not exist in this file yet -- verified absent."}}, {"id": "cobuilder-packaging/module-invariant/9", "context": "cobuilder-packaging", "kind": "module-invariant", "detail": {"id": "viewer", "path": "viewer/", "members": ["index.html"], "public_interface": ["window.STORY, window.ODYSSEY, window.DIFFS, window.ADRS, window.DIAGRAMS, window.DESIGNS (populated by sibling <script src> tags)"], "allowed_inbound": [], "allowed_outbound": [], "rule": "viewer/index.html never reaches into scripts/ (grep-verified: the one 'scripts/' mention is a code comment, not a live reference). It is not self-contained -- see CLAUDE.md 'The viewer is not self-contained' -- and depends on being served from the bundle root, a sibling of viewer/."}}, {"id": "cobuilder-packaging/context-map/1", "context": "cobuilder-packaging", "kind": "context-map", "detail": {"with": "bundle-directory", "pattern": "open-host-service", "note": "Not yet a documented bounded context. Scripts write it, the viewer reads it; neither reads the other's source. Data-only, versioned by bundle.json / story.json meta.schema_version."}}], "pull_request": [{"id": 1, "title": "Master", "state": "merged", "commit": "7bd668f", "date": "2026-07-22"}, {"id": 2, "title": "docs: add CLAUDE.md with codebase orientation + artifact feasibility findings", "state": "merged", "commit": "96c1531", "date": "2026-07-24"}, {"id": 3, "title": "Unify bundle storage under .prodyssey/, self-analysis into .prodyssey/self/", "state": "merged", "commit": "fb4be22", "date": "2026-07-28"}, {"id": 4, "title": "Mermaid diagrams for levels 1-3, and self-upgrading bundles", "state": "merged", "commit": "065654d", "date": "2026-08-02"}, {"id": 5, "title": "Close the API-key trust boundary, and fix three correctness bugs", "state": "merged", "commit": "789081f", "date": "2026-08-05"}, {"id": 6, "title": "Pr Odyssey Improvements 7Suouc", "state": "merged", "commit": "7473b7f", "date": "2026-08-05"}, {"id": 7, "title": "Pr Odyssey Improvements 7Suouc", "state": "merged", "commit": "630aa4f", "date": "2026-08-05"}, {"id": 8, "title": "Ste Writing Style Claude Fd5Fzs", "state": "merged", "commit": "d9a81d7", "date": "2026-08-06"}, {"id": 9, "title": "Deduplicate rewrite_manifest(); add interview self-consistency check; STE clarity pass", "state": "merged", "commit": "139de94", "date": "2026-08-06"}, {"id": 10, "title": "Swift Best Practices Fbx48M", "state": "merged", "commit": "6b2fce6", "date": "2026-08-06"}, {"id": 11, "title": "Design cobuilder-architect and cobuilder-implement, split repo into five plugins and skills", "state": "merged", "commit": "a389892", "date": "2026-08-25"}, {"id": 12, "title": "Retire adr-draft, close Gate 4b retrospectively, and record two ADR maintenance rules", "state": "merged", "commit": "be93c04", "date": "2026-08-25"}, {"id": 16, "title": "book-index.md gains a nano tier, and the security corpus stops reading unconditionally in full", "state": "open", "commit": "6bad882c30e7ea84a2e1d950e604859ffd211595", "date": "2026-08-26"}, {"id": 17, "title": "Rename plugins to drop the cobuilder- prefix; fix Mermaid click/copy-paste in the feedback drawer", "state": "open", "commit": "1d0c5ceb27a7df8dfaf4e3d36e2ebf9993a27735", "date": "2026-08-26"}, {"id": 19, "title": "Gate-doc surfacing in the viewer, plus a slice-loop workflow-invocation fix", "state": "open", "commit": "9ee7ce2330f2f7e6fc20c4e72e4a777416350dc7", "date": "2026-09-02"}, {"id": 21, "title": "viewer: the Work board lands, with the plan for the rest of the program", "state": "open", "commit": "74fb1a8999e2d9f65894069396f2339a1a2916c2", "date": "2026-09-23"}], "slice": [{"id": "cobuilder-family/4", "feature": "cobuilder-family", "n": 4, "title": "Renames inside today's single plugin", "ends_with": "Six architecture modes, `explore-design` deleted, `submit` renamed to `generate`, and the old `generate` renamed to `review`", "score": "1.00", "attempts": 2, "state": "completed"}, {"id": "cobuilder-family/5", "feature": "cobuilder-family", "n": 5, "title": "The 33 cross-pillar references are fixed", "ends_with": "The grep regression test passes while the repo is still one plugin", "score": "1.00", "attempts": 3, "state": "completed"}, {"id": "cobuilder-family/6", "feature": "cobuilder-family", "n": 6, "title": "Five plugins, five manifests", "ends_with": "`claude plugin validate` passes on each, and the family installs", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/7", "feature": "cobuilder-family", "n": 7, "title": "The two ports land", "ends_with": "`cobuilder-implement` renamed and shipped, `collaborate-with-user` folded in, the orientation skill written", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/1", "feature": "cobuilder-family", "n": 1, "title": "**Tracer bullet: two plugins, one shared module**", "ends_with": "Two throwaway plugins install from a local marketplace and both resolve the shared module from their own cache", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/2", "feature": "cobuilder-family", "n": 2, "title": "The compatibility gate exists", "ends_with": "`require_compatible()` and `stamp_generator()` pass their tests, called by nothing yet", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/3", "feature": "cobuilder-family", "n": 3, "title": "The gate is wired and the bundle migrates", "ends_with": "Format 3, `min_reader_schema`, the generators map, every writer gated", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/8", "feature": "cobuilder-family", "n": 8, "title": "The index holds the entities", "ends_with": "`build_index.py` emits every entity, and the two scripts it replaces are gone", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/9", "feature": "cobuilder-family", "n": 9, "title": "The index resolves the joins", "ends_with": "`adr_to_pull_request`, `slice_to_epic`, `district_uncovered`, and freshness", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/10", "feature": "cobuilder-family", "n": 10, "title": "The Decisions and Contexts modes", "ends_with": "A person browses every decision and every context, with the anchor distinction visible", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/11", "feature": "cobuilder-family", "n": 11, "title": "The Builds mode and the Backlog lane", "ends_with": "Gates driven by the status document, the Backlog lane computed from the index, pages moved into the bundle", "score": "0.92", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/12", "feature": "cobuilder-family", "n": 12, "title": "The ledger and its projection", "ends_with": "Append, fold, and project, with no server involved", "score": "0.92", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/13", "feature": "cobuilder-family", "n": 13, "title": "The anchor and the write endpoint", "ends_with": "Click to anchor a sentence, `POST /feedback` appends, the drawer keeps the text in view", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-family/14", "feature": "cobuilder-family", "n": 14, "title": "The wake command and the whole loop", "ends_with": "The background command blocks and returns new threads, and the loop runs once end to end", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-viewer/1", "feature": "cobuilder-viewer", "n": 1, "title": "**Tracer bullet: a publish survives a marker rename**", "ends_with": "The exporter matches the built file's named markers, and a real publish of the current viewer still succeeds after the marker names move", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/2", "feature": "cobuilder-viewer", "n": 2, "title": "Two builds produce the same bytes", "ends_with": "Two builds from the same lockfile on the same machine produce the same bytes, so the byte-equal guard is possible at all", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/3", "feature": "cobuilder-viewer", "n": 3, "title": "The build owns the committed file", "ends_with": "The build reproduces `plugins/artifact/viewer/index.html` byte for byte, and one edited byte makes the rebuild test fail", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/4", "feature": "cobuilder-viewer", "n": 4, "title": "One typed model reads the bundle", "ends_with": "A level renders from `index.json` with no join derived in a component, and a field renamed on `PullRequest` and not on `OpenPullRequest` fails the type check", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/5", "feature": "cobuilder-viewer", "n": 5, "title": "The Work prototype, reviewed", "ends_with": "The prototype renders this repository's own designs across the Work board, and the engineer approves it or names what to change", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/8", "feature": "cobuilder-viewer", "n": 8, "title": "The section model", "ends_with": "One box is on screen, the strip, the pager bar, and the two arrow keys move one index, and the pane reports no scroll at all", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/9", "feature": "cobuilder-viewer", "n": 9, "title": "The level's progress", "ends_with": "The strip reads 0 at the first section's top, 25 percent at its bottom, 50 on the second, 100 on the last, and never decreases on a forward walk", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/10", "feature": "cobuilder-viewer", "n": 10, "title": "Every level renders its own sections", "ends_with": "A reader walks Intent, Problem & Solution, Architecture, Build, Pull requests, and Shipped of one work item, and the envisioned pull request leads the Pull requests level", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/11", "feature": "cobuilder-viewer", "n": 11, "title": "The diagram tiles open their drawing", "ends_with": "A tile shows the drawing's miniature scaled to its box, a press opens it whole with zoom from 50 to 400 percent, Escape and the backdrop close it, and a failed render shows the authored source", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/12", "feature": "cobuilder-viewer", "n": 12, "title": "A record opens in the Sheet", "ends_with": "Pressing a decision, a boundary rule, or an epic's design opens the whole record, and a part link scrolls the record and moves focus to that heading", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/13", "feature": "cobuilder-viewer", "n": 13, "title": "A goal.json-only design renders", "ends_with": "One of the seven sparse designs shows every level it can fill, and the rail gates the levels it cannot", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/14", "feature": "cobuilder-viewer", "n": 14, "title": "The FlightDeck prototype, reviewed", "ends_with": "One surface shows single-pull-request narration at parity and the multi-pull-request path as its second mode behind one switch, and the engineer approves it or names what to change", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/15", "feature": "cobuilder-viewer", "n": 15, "title": "A single pull request at parity", "ends_with": "A reader opens one pull request and reads its four narration levels, its diagrams, its art, its audio, and its intent and assessment sheet, as the shipped viewer does today", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/16", "feature": "cobuilder-viewer", "n": 16, "title": "The joins rail", "ends_with": "The same page lists the decisions, the design, and the epics that reach that pull request", "score": "—", "attempts": 0, "state": "planned"}, {"id": "cobuilder-viewer/6", "feature": "cobuilder-viewer", "n": 6, "title": "The shell lands on the bundle's designs", "ends_with": "Opening the viewer with no work item named shows every design as a row, each row stating its stage and the records it holds, and no error state", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "cobuilder-viewer/7", "feature": "cobuilder-viewer", "n": 7, "title": "A row opens the item's Work surface", "ends_with": "Pressing a row lands on that item's Work surface at its first level, with the route naming the item", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "gate-doc-surfacing/1", "feature": "gate-doc-surfacing", "n": 1, "title": "Tracer bullet: constants and empty entity lists", "ends_with": "`data/index.json` carries `program_design` and `epic_design` keys (empty lists when no gate docs exist yet)", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "gate-doc-surfacing/2", "feature": "gate-doc-surfacing", "n": 2, "title": "Real parsing: project Gate 3 and Gate 4b docs", "ends_with": "A repo with a `03-program-design.md` and an `epic-*-design.md` produces populated `program_design`/`epic_design` entities, and `joins.feature_gates[slug][3].doc` points at the projected id", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "gate-doc-surfacing/3", "feature": "gate-doc-surfacing", "n": 3, "title": "Edge cases: missing files, malformed epic id, no plans dir", "ends_with": "A plan directory with no gate docs, and a repo with no `docs/plans/` at all, both still produce a clean index build with empty (not missing) entity keys", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "gate-doc-surfacing/4", "feature": "gate-doc-surfacing", "n": 4, "title": "Tracer bullet: clickable Gate Rail card opens an empty sheet", "ends_with": "Clicking a Gate Rail card with a `doc` opens a sheet showing the gate's title and an empty body; a card with no `doc` stays non-interactive", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "gate-doc-surfacing/5", "feature": "gate-doc-surfacing", "n": 5, "title": "Real content: render the gate doc body, add the epic chip", "ends_with": "The sheet renders `body_md` through the existing `renderMarkdown()`; an epic card with a `design_doc` shows a chip that opens the same sheet scoped to that epic", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "gate-doc-surfacing/6", "feature": "gate-doc-surfacing", "n": 6, "title": "Edge cases: no entity, mutual exclusivity with other sheets", "ends_with": "A Gate Rail card whose gate has no matching doc opens nothing; opening the gate-doc sheet closes any open ADR/assessment/comments sheet, and vice versa", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "gate-state-resolution/1", "feature": "gate-state-resolution", "n": 1, "title": "Tracer bullet: the static page resolves the state and the document", "ends_with": "`skill-collision-fix` opens on a document it holds, or on no document and a stated reason. A plan that holds `interaction-design.md` draws a rail button labelled \"Interaction design\". An `n/a` gate carries the closed treatment.", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "gate-state-resolution/2", "feature": "gate-state-resolution", "n": 2, "title": "Real content and edge cases: the viewer rail resolves the state and the count", "ends_with": "The viewer rail describes an `n/a` gate as \"Not applicable\" and paints it apart from a planned gate. The rail header reads the approved count out of the total. An approved gate and the current gate keep their present treatment.", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "interaction-design-gate/1", "feature": "interaction-design-gate", "n": 1, "title": "Tracer bullet: the skill skeleton and its file set", "ends_with": "`plugins/implement/skills/design-to-code/SKILL.md` exists, its frontmatter parses, its overview names three steps, and every relative path it cites resolves inside the directory. The three product-brief files are absent.", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "interaction-design-gate/2", "feature": "interaction-design-gate", "n": 2, "title": "Real content and edge cases: the four concepts reach the spec, and the empty paths stay cheap", "ends_with": "The state table carries an initial-state column and a timer-arming column, `4.1 Transition Table` carries a trigger, a guard and a reset, `3.3 Visibility Gating` names the condition, and `2.3 Declared Defaults` names the default, with a matching `references/jsonc-schema.md` key for each. A feature with no front end has a stated one-line path, no file cites a deleted template, and no file calls a product brief a PRD or a brief.", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "interaction-design-gate/3", "feature": "interaction-design-gate", "n": 3, "title": "Tracer bullet: the gate line and its section", "ends_with": "`plugins/implement/skills/build/SKILL.md` carries a `## Gate 2b` section after the Gate 2 section, and the `00-status.md` template carries a `2b` line between the Gate 2 and Gate 3 lines.", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "interaction-design-gate/4", "feature": "interaction-design-gate", "n": 4, "title": "Real content and edge cases: the three paths, the two consumers, and the returned session", "ends_with": "The skill states all three paths (no front end, a front end, an earlier approval). `rubric-authoring.md` names both artifacts as derivation sources, and `slice-loop.md` requires RED to read both and VALIDATE's front-end criteria to name a browser check. The resume rule names Gate 2b, so a returned session does not skip it, `commands/start.md` lists the two new artifacts, and `plugin.json` reads 0.2.0.", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "interaction-design-gate/5", "feature": "interaction-design-gate", "n": 5, "title": "Tracer bullet: the 2b group in `verify_gate.py`", "ends_with": "`verify_gate.py --plan docs/plans/interaction-design-gate` prints a `2b` block, reports `n/a` for this plan, and exits 0 for the 2b group. A plan with a pending 2b line exits non-zero.", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "interaction-design-gate/6", "feature": "interaction-design-gate", "n": 6, "title": "Real content: the index and the Builds view rail", "ends_with": "`data/index.json` carries an `interaction_design` array, `joins.feature_gates` carries the 2b entry, and the Builds view rail shows a 2b card whose document opens in the existing gate-doc sheet.", "score": "1.00", "attempts": 1, "state": "completed"}, {"id": "interaction-design-gate/7", "feature": "interaction-design-gate", "n": 7, "title": "Edge cases: the three integer-only assumptions", "ends_with": "`resolve_feature_gates()` writes a string `n`, `viewer/index.html` no longer compares `g.n` to the literal `4`, and `current_doc()` treats an `n/a` gate as done. `cobuilder-family` and `gate-doc-surfacing` still report `Overall: OK`.", "score": "1.00", "attempts": 1, "state": "completed"}], "publication": [{"id": "exports/pr-2.html", "pull_request": 2, "artifact_url": "https://claude.ai/code/artifact/ea2c0f34-c5e5-46f7-b619-2bb7c72b5583", "published_at": "2026-07-22T09:56:34Z"}, {"id": "exports/index.html", "pull_request": null, "artifact_url": "https://claude.ai/code/artifact/4bf5fff8-f70e-4e68-8553-2b8a254c5e79", "published_at": "2026-07-22T09:56:48Z"}], "program_design": [{"id": "cobuilder-family", "feature_slug": "cobuilder-family", "gate": 3, "title": "Program Design: the cobuilder family", "body_md": "# Program Design: the cobuilder family\n\nGate 3. The decisions an agent would otherwise make silently while\nimplementing Gate 2. Every one of them is cheap to change here and expensive\nto change once a slice has landed.\n\nGoverned by ADR-0016, ADR-0017, ADR-0018, and ADR-0019. Where this document\nand an ADR disagree, the ADR wins and this document is wrong.\n\n## Files\n\n### The repository becomes a marketplace\n\n```\n.claude-plugin/marketplace.json        one file, five plugin entries\nshared/                                 vendored by symlink into each plugin\n  _bundle_meta.py\n  _manifest.py\n  migrate_bundle.py\n  verify_bundle.py\n  skills/ste-writing/\n  skills/mermaid/\nplugins/\n  cobuilder-architect/\n  cobuilder-pr/\n  cobuilder-implement/\n  cobuilder-artifact/\n  cobuilder-full-lifecycle/\ntests/                                  new, see Test plan\n```\n\n`shared/` sits at the marketplace root and each plugin carries a symlink to\nit at `<plugin>/shared`. The install copy dereferences the link, so each\nplugin's cache holds its own real copy. This is the mechanism ADR-0017\nchose and the one E2 exists to prove.\n\n### Where each existing file lands\n\n| From | To | Why |\n|---|---|---|\n| `skills/architecture/` | `cobuilder-architect` | design, review, maintenance, decisions, describe, debug — six modes, since `explore-design` is deleted |\n| `skills/odyssey/` | `cobuilder-pr` | baseline, generate, review. `view` and `publish` go to `cobuilder-artifact` instead |\n| `skills/ste-writing/`, `skills/mermaid/` | `shared/skills/` | both are cited from both pillars, which is exactly what vendoring is for |\n| `.claude/skills/collaborate-with-user/` | `cobuilder-artifact`, folded into its `SKILL.md` and `references/` | it presents a decision, which is the artifact pillar's job |\n| `viewer/index.html` | `cobuilder-artifact` | the reading surface |\n| `extract_story.py`, `extract_diffs.py`, `generate_prompts.py`, `generate_audio.py`, `build_diagrams.py`, `render_review.py` | `cobuilder-pr` | all six only run inside the per-PR sweep or submit |\n| `build_adrs.py`, `build_designs.py` | deleted | `build_index.py` replaces both, see below |\n| `validate_decision_state.py`, `compute_scores.py`, `html_to_pdf.py` | `cobuilder-architect` | decisions mode and the two review reports |\n| `export_artifact.py`, `export_index.py`, `record_publish.py` | `cobuilder-artifact` | publish |\n| `_bundle_meta.py`, `_manifest.py`, `migrate_bundle.py`, `verify_bundle.py` | `shared/` | every plugin that touches a bundle needs all four |\n| `~/.claude/skills/cobuilder-factory/` | `cobuilder-implement` | the port and the rename |\n\n### New files\n\n| File | Plugin | Purpose |\n|---|---|---|\n| `scripts/build_index.py` | `cobuilder-artifact` | builds `data/index.json` and `index.js` |\n| `scripts/serve_bundle.py` | `cobuilder-artifact` | replaces `python3 -m http.server`, adds `POST /feedback` |\n| `scripts/watch_feedback.py` | `cobuilder-artifact` | the background wake command |\n| `skills/cobuilder-full/SKILL.md` | `cobuilder-full-lifecycle` | names the four modes and when to reach for each |\n\n**`build_index.py` lives in the artifact plugin and reads what the other\npillars wrote.** Not only `cobuilder-architect`'s ADRs, designs, and\ncontexts. Also `cobuilder-pr`'s `story.json`, its assessments, and its\n`docs/pull-requests/`, and `cobuilder-implement`'s `docs/plans/`. That is not\na seam violation. Every one of those paths is inside the *target repo* or\ninside the bundle. None resolves into another plugin's root. This is the\ndistinction ADR-0016 draws, and it is the one an implementing agent is most\nlikely to get wrong.\n\nSettled at Gate 3 review: the index is a repo-wide artifact rather than one\npillar's output, so it belongs with the pillar that presents the repo.\n\n### `cobuilder-artifact` owns the authoring skill as well as the viewer\n\n`collaborate-with-user` does not travel as a separate skill directory. Its\nrules become part of `cobuilder-artifact`'s own `SKILL.md`, with the long\nmaterial in `references/`: the composition rules, the theme-token rules, the\nhonesty rule, and the relationship to the architecture skill's two report\ntemplates. One plugin then owns both halves of presenting something to a\nperson — the page it builds and the answer it collects.\n\n**The agent doing this work must invoke the `skill-development` skill first.**\nFolding a standalone `SKILL.md` into another plugin's skill is exactly the\njob that has established practice, and this build should follow it rather\nthan reinvent a layout.\n\n### Pages leave `.lavish/`\n\n`.lavish/` is another tool's directory and this plugin's pages have no\nbusiness in it. A generated page belongs in the bundle, because it is derived\nfrom an authored markdown file and a rebuild can reproduce it:\n\n```\n<bundle-dir>/pages/<slug>.html          the generated page\n<bundle-dir>/exports/publish-manifest.json   already records the URL\n```\n\nThe markdown stays the source of record in `docs/`, unchanged. Moving the\nexisting six pages is part of E5, and each keeps its published URL, because\na URL is bound to the artifact rather than to the local path.\n\n## Types and signatures\n\n### `shared/_bundle_meta.py`\n\n```python\nSCHEMA_VERSION = \"1.3\"                            # was \"1.2\"\nSCHEMA_VERSION_KNOWN = {\"1.0\", \"1.1\", \"1.2\", \"1.3\"}\nCURRENT_BUNDLE_FORMAT = 3                         # was 2\n\nclass BundleIncompatible(RuntimeError):\n    \"\"\"Raised when a bundle demands a reader this plugin cannot be.\"\"\"\n\ndef require_compatible(bundle_dir: Path, plugin: str) -> None:\n    \"\"\"Refuse to write into a bundle this plugin is too old to read.\n\n    Reads <bundle_dir>/bundle.json. Raises BundleIncompatible when\n    min_reader_schema > SCHEMA_VERSION, or when bundle_format >\n    CURRENT_BUNDLE_FORMAT. Returns None and is silent otherwise.\n    A missing bundle.json is a new bundle and is compatible.\n    \"\"\"\n\ndef stamp_generator(bundle_dir: Path, plugin: str, version: str) -> None:\n    \"\"\"Record this plugin's version in bundle.json's generators map.\"\"\"\n```\n\nEvery script that writes into a bundle calls `require_compatible()` as its\nfirst statement after argument parsing. Today three scripts stamp\n`SCHEMA_VERSION` with no check at all: `extract_story.py:599`,\n`export_artifact.py:428`, and `_manifest.py:76`.\n\n### `bundle.json`, at format 3\n\n```json\n{\n  \"bundle_format\": 3,\n  \"schema_version\": \"1.3\",\n  \"min_reader_schema\": \"1.3\",\n  \"generators\": {\n    \"cobuilder-pr\": \"0.5.0\",\n    \"cobuilder-artifact\": \"0.5.0\"\n  },\n  \"migrated_at\": \"2026-08-21T09:00:00Z\"\n}\n```\n\n`generator_version`, a scalar, becomes `generators`, a map keyed by plugin.\n`min_reader_schema` is the floor a reader must meet and is raised only by a\nmigration that makes a bundle unreadable to an older plugin. A migration that\nonly adds an optional field leaves it alone.\n\n### `scripts/build_index.py`\n\n```python\ndef build_index(repo: Path, bundle_dir: Path) -> dict:\n    \"\"\"Full rebuild. Reads docs/ plus git. Never merges, never authors.\"\"\"\n\ndef collect_adrs(repo: Path)      -> list[dict]\ndef collect_designs(repo: Path)   -> list[dict]\ndef collect_contexts(repo: Path)  -> list[dict]   # includes boundary rules\ndef collect_districts(bundle: Path) -> list[dict] # from inventory.yaml\ndef collect_plans(repo: Path)     -> list[dict]   # gates and slices\ndef collect_pull_requests(repo: Path) -> list[dict]  # git plus gh\ndef resolve_joins(entities: dict) -> dict\ndef compute_sources(repo: Path)   -> dict         # freshness\n\ndef is_stale(index: dict, repo: Path) -> bool:\n    \"\"\"True when any tracked subtree hash or the git head has moved.\"\"\"\n```\n\n`index.json`:\n\n```json\n{\n  \"schema_version\": \"1.3\",\n  \"sources\": {\n    \"git_head\": \"642ecb3\",\n    \"trees\": {\"docs/architecture/adr\": \"sha256:…\",\n              \"docs/architecture/designs\": \"sha256:…\",\n              \"docs/architecture/contexts\": \"sha256:…\",\n              \"docs/plans\": \"sha256:…\"}\n  },\n  \"entities\": {\n    \"adr\": [...], \"design\": [...], \"epic\": [...],\n    \"context\": [...], \"district\": [...], \"boundary_rule\": [...],\n    \"pull_request\": [...], \"slice\": [...], \"publication\": [...]\n  },\n  \"joins\": {\n    \"adr_to_context\":  {\"ADR-0016\": \"cobuilder-packaging\"},\n    \"adr_to_district\": {\"ADR-0007\": \"viewer\"},\n    \"context_verifies_district\": {\"cobuilder-packaging\": [\"packaging\"]},\n    \"adr_to_pull_request\": {\"ADR-0016\": {\"pr\": 11, \"via\": \"epic\",\n                                         \"path\": [\"plugin-split/E1\"]}},\n    \"slice_to_epic\": {\"cobuilder-family/3\": \"plugin-split/E1\"},\n    \"epic_to_pull_request\": {\"plugin-split/E1\": 11},\n    \"district_uncovered\": [\"narrative\", \"audio\"]\n  }\n}\n```\n\n`joins` is materialised rather than computed in the viewer. A join resolved\nonce in Python is a join the viewer cannot get wrong, and `district_uncovered`\nis the describe backlog ADR-0018 names.\n\n### An ADR is not an epic\n\nThey are different things and neither contains the other.\n\nAn **ADR** is a decision. It outlives the work that carried it out, it can be\nchallenged years later, and its state machine describes the life of the\ndecision. An **epic** is a unit of work with a branch and an end.\n\nThe relation is many-to-many. One epic can carry several decisions: E1 lands\nthe renames and every ADR the renames settle. One decision can take several\nepics: ADR-0016 needs E1, E1b, E2, and E3 before the code matches it. That is\nexactly why the code-matches-the-decision axis is derived rather than stored\non the ADR.\n\n### How `adr_to_pull_request` is derived\n\nTwo paths, tried in order. Nothing is authored.\n\n1. **Direct.** The ADR's frontmatter carries `source_pr`. Retro-extraction\n   sets it, because the record was written from a merged pull request. Done.\n2. **Through the design and the epic.** Otherwise:\n\n```\nADR-NNNN\n  → design            the design whose adr list names it, or whose\n                      directory the ADR's `related` paths point into\n  → epic              goal.json.epics[] entries belonging to that design\n  → branch            epics[].branch\n  → pull_request      the PR whose head branch equals that branch,\n                      from `gh pr list --head <branch> --state all`\n```\n\n`via` records which path resolved, and `path` records the epics it went\nthrough. A reader can then see *why* an ADR is attached to a pull request,\nwhich matters when the answer is wrong.\n\n**The gap this exposes: a slice names no epic today.** `04-slices.md` lists\nslices in build order and nothing ties a slice to the epic it advances. The\njoin therefore stops at the epic and cannot reach a slice. Gate 4 fixes this\nat the source: **every slice line declares `epic: <design>/<epic-id>`.** The\nalternative is to guess from the files a slice touches, which is a heuristic\nin a chain built to avoid them.\n\n**What stays underivable.** An epic with no branch has no pull request, and\nthat is correct rather than missing — it is work that has not started. The\nindex reports it as unstarted and does not search for a PR.\n\n### `scripts/serve_bundle.py`\n\n```python\ndef serve(bundle_dir: Path, port: int, allow_write: bool) -> None:\n    \"\"\"Static files, plus POST /feedback when allow_write is true.\n\n    Binds 127.0.0.1 only. Never binds a routable address.\n    \"\"\"\n\ndef append_thread(bundle_dir: Path, record: dict) -> str:\n    \"\"\"Validate, assign a ulid, append one line, return the id.\n    Opens with 'a' and one write call, so a concurrent append cannot\n    interleave. Never rewrites and never truncates.\n    \"\"\"\n```\n\nAccepted routes: `GET` for any file under the bundle root, and\n`POST /feedback`. Nothing else. A `DELETE` is not implemented, because a read\nnever deletes and neither does anything else.\n\n### `scripts/watch_feedback.py`\n\n```python\ndef watch(bundle_dir: Path, since: int, timeout: int = 900) -> list[dict]:\n    \"\"\"Block until threads.ndjson grows past byte offset `since`, or the\n    timeout expires. Print the new lines as JSON and exit 0. Exit 2 on\n    timeout with no new lines, so a caller can tell the two apart.\n    \"\"\"\n```\n\nPolls the file size once a second. It does not watch the filesystem, because\na watcher is a dependency and this is a local file the same machine writes.\n\n### The comment record\n\nExactly as ADR-0019 specifies, including the agent reply that carries\n`\"author\": \"agent\"` and `\"replies_to\": \"<thread id>\"`, and the state change\nthat carries only `id`, `state`, and `ts`. **The ledger is append-only, so a\nstate is the last line that names it, not a field edited in place.**\n\n```python\ndef thread_view(bundle_dir: Path, thread_id: str) -> dict:\n    \"\"\"Fold every line naming this id into one thread: the root comment,\n    its replies in append order, and the transition history.\"\"\"\n\ndef project_threads(bundle_dir: Path) -> dict:\n    \"\"\"Fold the whole ledger once into feedback/threads-state.json:\n    {thread_id: {state, updated_by, updated_at, reply_count, offset}}.\n    Rewritten in full after each append. Never authored.\n    \"\"\"\n```\n\n**The current state is a field, and the log is still append-only.** The\nearlier draft had every reader fold the file to learn a state, and that was\nthe wrong trade. `threads-state.json` is a projection rebuilt from the\nledger, so a reader gets the state in one lookup and keeps the audit trail\nthat says who moved it and when. The projection is derived and disposable —\ndelete it and the next append rebuilds it from the ledger.\n\nThe rule the projection must not break: **the ledger is the truth and the\nfield is the cache.** A writer appends to the ledger first and projects\nsecond. A projection that disagrees with the ledger is a bug in the\nprojection, and `project_threads()` is cheap enough to rebuild whenever the\nanswer is in doubt. This is the same shape as `index.json` — derived, full\nrebuild, never merged — so it needs no new idea, only the same discipline.\n\nE6 ships `thread_view()`, `project_threads()`, and the reply record. It does\nnot ship the conversation interface — the drawer lists replies flat.\nRendering a thread as a conversation is a later slice, and the record shape\nexists now so that slice adds no migration.\n\n## Call stack\n\n### Publishing a design, end to end\n\n```\n/cobuilder-architect:design\n  → migrate_bundle.py --bundle-dir …            (shared, unconditional)\n  → require_compatible(bundle, \"cobuilder-architect\")\n  → interview, explore, challenge                (Claude judgment)\n  → write docs/architecture/designs/<name>/*.json\n  → write docs/architecture/adr/ADR-NNNN-*.md\n  → when no context covers the region:\n        describe procedure, in-process\n        → write docs/architecture/contexts/<id>/{canvas.md,boundary.yaml}\n        → boundary.verifies = [districts covered]\n  → validate_decision_state.py\n  → hand off: name cobuilder-implement, stop\n```\n\n### Building the index and reading it\n\n```\nany cobuilder-artifact mode\n  → migrate_bundle.py\n  → is_stale(index, repo)?\n        yes → build_index.py\n                collect_* ×6  →  resolve_joins  →  compute_sources\n                write data/index.json and data/index.js\n  → serve_bundle.py --allow-write\n        viewer/index.html\n          <script src=\"../data/index.js\">  →  window.INDEX\n          renderMode(mode)  →  Designs | Pull requests | Decisions\n                               | Contexts | Builds\n```\n\n### A comment, end to end\n\n```\nreader clicks any element in the viewer\n  → onSelect(event)\n      computeSelector(el)     walk ≤5 ancestors, short-circuit on an id\n      captureRange(selection) node path plus character offset per end\n      openDrawer(anchor)      shifts the page, does not cover it\n  → reader types, submits\n      POST /feedback  →  append_thread()  →  ulid returned\n      fallback when the server is read-only: localStorage, then the\n      existing \"Copy review as markdown\" hand-off\n  → agent side\n      watch_feedback.py --since <offset>\n        blocks → prints new lines → exits 0\n        harness re-invokes the session with the threads in hand\n      agent acts, appends a reply line, appends a state line\n```\n\n## Test plan\n\n**This repository has no test suite today.** Gate 3 introduces one, because\nthe red-green-validate loop of `cobuilder-implement` cannot run without it.\n`tests/` at the marketplace root, run with\n`uv run --with pytest pytest tests/`. No CI. No package manager. The\nconstraint that this repo stays prose plus PEP-723 scripts holds, and pytest\narrives through `--with` rather than through a lockfile.\n\n### Compatibility gate\n\n- `test_require_compatible_passes_on_equal_schema`\n- `test_require_compatible_raises_when_bundle_demands_newer_reader` —\n  `min_reader_schema` `1.4` against a `1.3` plugin raises `BundleIncompatible`\n- `test_require_compatible_raises_on_newer_bundle_format`\n- `test_require_compatible_allows_missing_bundle_json` — a new bundle\n- `test_write_scripts_call_require_compatible_first` — greps each writer for\n  the call. Crude, and it is the test that actually prevents the regression.\n- `test_stamp_generator_preserves_other_plugins` — writing as\n  `cobuilder-pr` must not drop `cobuilder-artifact`'s entry\n\n### Migration to format 3\n\n- `test_scalar_generator_version_becomes_a_map`\n- `test_migration_refuses_when_an_authored_field_changes_outside_touches`\n- `test_migration_writes_a_backup_before_it_writes_the_new_story`\n- `test_viewer_refreshes_unconditionally` — no version gate, the bug that\n  motivated the mechanism\n\n### The index\n\n- `test_index_resolves_an_adr_to_its_context`\n- `test_index_resolves_an_adr_to_a_context_through_a_district` — the join\n  ADR-0018 adds\n- `test_district_with_no_context_appears_in_district_uncovered`\n- `test_epic_id_is_scoped_to_its_design` — two designs both carrying `E1`\n- `test_is_stale_detects_a_changed_adr`\n- `test_is_stale_detects_a_moved_git_head`\n- `test_index_is_a_full_rebuild` — deleting an ADR removes it from the index\n- `test_build_index_never_writes_into_docs`\n\n### The ledger\n\n- `test_append_thread_assigns_a_ulid_and_returns_it`\n- `test_append_thread_never_rewrites_an_existing_line`\n- `test_two_concurrent_appends_both_survive`\n- `test_state_change_is_a_new_line_not_an_edit`\n- `test_read_does_not_delete` — the point of departure from the lavish CLI\n- `test_thread_view_folds_a_reply_under_its_root`\n- `test_an_agent_reply_carries_author_agent_and_replies_to`\n- `test_projection_state_equals_the_last_transition_in_the_ledger`\n- `test_deleting_the_projection_rebuilds_it_from_the_ledger`\n- `test_projection_records_who_moved_the_state_and_when`\n- `test_watch_exits_2_on_timeout_with_no_new_lines`\n- `test_serve_rejects_a_post_to_any_path_other_than_feedback`\n- `test_serve_binds_loopback_only`\n\n### The split itself\n\n- `test_no_plugin_path_reference_crosses_a_pillar` — greps every plugin for\n  `${CLAUDE_PLUGIN_ROOT}/skills/<other pillar>`. This is the E1b regression\n  guard, and it is the single most valuable test in the list.\n- `test_every_plugin_manifest_validates` — `claude plugin validate` on each\n- `test_no_page_is_written_into_dot_lavish`\n- `test_slice_declares_an_epic` — every line in `04-slices.md` names one\n- `test_adr_reaches_a_pull_request_through_its_epic` — the two-path derivation\n- `test_an_epic_with_no_branch_is_reported_unstarted_not_missing`\n- `test_shared_symlink_dereferences_to_a_real_directory` — E2's proof,\n  written as a test rather than as a one-off check\n\n## Found while writing this document\n\n**`explore-design` and `design` are the same command, and the command goes.**\n`commands/explore-design.md:27` dispatches `Skill(\"architecture\", args=\"design\n$ARGUMENTS\")`, which is character-for-character what `commands/design.md`\ndispatches. Divergent exploration is stage 3 of Design mode and runs either\nway, so the second command adds a name and no behaviour. **Decided: delete\nthe command. Divergent exploration stays an agent-invoked step inside Design\nmode and is not a user-facing entry point.** `cobuilder-architect` ships six\nmodes. E1 carries the deletion.\n\n**The backlog already exists and nobody was reading it.** Deferred work has a\nhome, and it is `goal.json.epics[]`. Each entry carries an `id`, a `branch`,\na `pr`, and a `state`, and an epic with a null branch is work that is planned\nand not started. That is a backlog item. Nothing new is needed to hold one.\n\nSo the three levels are:\n\n| Level | Where it lives | Lifetime |\n|---|---|---|\n| **Epic** | `goal.json.epics[]` in a design | survives the session, survives the branch |\n| **Slice** | `04-slices.md` for a feature, one line each in build order | survives the session, dies with the feature |\n| **Task** | inside a slice, in the session doing it | dies with the session, deliberately |\n\nA task below slice level is not durable and should not be. A slice is small\nenough that its tasks are the work of one sitting, and writing them down\ncosts more than redoing them.\n\n**The Builds view gains a Backlog lane**, and it is a query rather than a\nfile: every epic with a null branch, plus every slice not yet scored, ranked\nby the count of ADRs that depend on the epic. Nothing authors it, and nothing\ncan go stale against a list it computes.\n\n`plugin-split/goal.json` is now the real list. It held five epics from the\nfirst draft while `02-architecture.md` had grown to eight. It carries all\nnine, including **E8, rendering a thread as a conversation** — which is the\nanswer to where that deferred work went.\n\n**Rendering a thread as a conversation needs no ADR.** ADR-0019 already\ndecides the reply channel, the record shape, and that `replies_to` exists.\nRendering those replies as a thread is deferred work under that decision, not\na competing option, and a record with no rejected alternative is a task in\nADR clothing. One thing would earn its own record: if the viewer's threads\nand the Artifact platform's threads have to merge into one inbox, that is a\nreal decision with real alternatives. It is not in scope for E6.\n\n## Least confident decisions\n\n1. **`serve_bundle.py` replaces `python3 -m http.server` outright.** A\n   smaller change keeps the stock server for reading and adds the write\n   endpoint only under a flag. That leaves two servers to keep in step. One\n   server with `--allow-write` off by default is simpler and it is still a\n   real application where there used to be none.\n\n2. **Materialised `joins` in `index.json`.** It duplicates what the entities\n   already state, and it goes stale in exactly the same rebuild the entities\n   do, so the duplication costs size rather than correctness. The argument\n   against is that a viewer bug and an index bug now look identical.\n\n3. **pytest through `uv run --with`.** It keeps the no-package-manager\n   constraint and it re-resolves pytest on every run. A slow test command is\n   a test command people skip.\n\n4. **A projection file beside the ledger.** It is a second file that can\n   disagree with the first. The alternative is a fold on every read, which\n   was the earlier draft and which made the common operation the expensive\n   one. Two files with a stated direction of truth is the better trade, and\n   it is the trade `index.json` already makes.\n\n5. **`shared/` as a sibling of `plugins/` rather than duplicated content in\n   git.** Everything rests on the symlink dereference. If E2 finds it does\n   not work for a directory of Python, the fallback is a copy step before\n   publish, and that fallback is a build step in a repo that has none.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-family/03-program-design.md"}, {"id": "cobuilder-viewer", "feature_slug": "cobuilder-viewer", "gate": 3, "title": "Program Design: cobuilder-viewer", "body_md": "# Program Design: cobuilder-viewer\n\nGate 3. The decisions an agent would otherwise make silently while implementing\nGate 2. Every one of them is cheap to change here and expensive to change once a\nslice has landed.\n\nThis design builds one program with three surfaces. Work reads a design. FlightDeck\nreads a pull request, or the whole open set. Reference reads the decisions and the\nmap coverage. A Vite build compiles all three into the committed viewer file.\n\n**The scope narrowed on 2026-09-22.** The engineer cut this program to two surfaces:\nthe Work surface, and the current pull request viewer renamed to FlightDeck at parity.\nEleven epics deferred on that date, E8 to E18, and their material stays in this\ndocument rather than leaving it. That material is the Reference surface, the describe\npath, the whole multi-pull-request mode and its simulator, the path runner, the\nledger's three state subtypes, publish parity, and the feedback path. None of it is\npart of the build. The live ladder is `04-slices.md`: sixteen slices across E1, E2,\nE3, E4, E5, E6, E7, and E19. Read every section below that serves a deferred epic as\ndesign on the shelf, ready for the epic that returns.\n\nFive ADRs govern it. ADR-0023 holds the build. ADR-0027 holds one FlightDeck surface\nwith two modes. ADR-0028 holds a level as sections on a track. ADR-0018 holds the\nrecord index, and ADR-0019 holds the ledger. Where this document and an ADR\ndisagree, the ADR wins and this document is wrong.\n\n## Files\n\n### The build, and the file it produces\n\n```\nplugins/artifact/viewer/\n  package.json                     scripts: dev, build, typecheck. React 19, Vite 7,\n                                   @vitejs/plugin-react, vite-plugin-singlefile\n  vite.config.ts                   root src/, the single-file plugin, outDir today\n                                   ../dist, alias @ -> src/, port 5273\n  tsconfig.json                    strict, noUnusedLocals, noUnusedParameters\n  src/index.html                   the dev entry\n  src/main.tsx                     mounts the shell\n  src/index.css                    the token set: ground, surface, ink, band, tint\n  index.html                       THE COMMITTED OUTPUT. The build writes here once\n                                   E2 lands, and a test rebuilds and compares\n  dist/                            today's scratch output. Deleted once index.html\n                                   is the build's target\n```\n\n**Why `vite.config.ts` changes.** It builds to `../dist` today, and its own comment\nsays ADR-0023's output rule is under revision while the shipped file keeps working.\nE2 settles that: the build writes `plugins/artifact/viewer/index.html`, and the\ncommitted file becomes a build artifact rather than a hand-maintained one.\n\n**Why the markers exist.** `export_artifact.py` rewrites the viewer by verbatim\nstring replacement and hard-errors when a literal moves. E1 gives the build's output a small set\nof named markers. The exporter then matches those names, rather than whatever\nliteral happens to sit between them. That is the seam, and it is the first slice.\n\n### The typed data layer\n\n```\nsrc/data/\n  global.d.ts                 the six window globals the bundle's script tags define:\n                              STORY, ODYSSEY, DIFFS, ADRS, DESIGNS, DIAGRAMS\n  types.ts                    every record type, the index shape, and the join results.\n                              Declares PullRequest here, and OpenPullRequest as its\n                              branded extension. ADR-0027 requires exactly one\n                              declaration of each\n  bundle.ts                   loads data/index.json, reads the globals, and resolves\n                              every join in one place. Exposes entitiesOf, joinsOf,\n                              and loadIndex\n  designs.ts / adrs.ts        record bodies, keyed by design and by decision id\n```\n\n**Why one module resolves the joins.** ADR-0018 built the joins into\n`data/index.json`. The shell reads them and derives none of them. A second\nderivation in the viewer would be a second source for the same fact.\n\n### The shell, and the section model\n\n```\nsrc/shell/\n  App.tsx                     the boot: index load, route, theme, the rail and the top\n                              bar, and the pane\n  model.ts                    buildWorkItems, gatesOf, levelsOf, readRoute, routeHref,\n                              the section lists per level\n  hooks.ts                    useIndexLoad, useHashRoute, useDocumentNoScroll,\n                              useVisibleWidthCap, useCollapsedRail, useFocusOnChange,\n                              useScrollResetOnRoute, useSectionAvailability\n  Pager.tsx                   SectionStrip, SectionStage, SectionPager, LevelProgress,\n                              useSectionPaging, withinOf. ADR-0028 owns this shape\n  panels/                     the sections each level pages, one file per level\n    Intent.tsx                Why, Done when, Abort if, Out of scope\n    ProblemSolution.tsx       Problem and solution, Risks, Assessment, Unknowns\n    Architecture.tsx          Diagrams, Architecture Decisions, Boundaries, Districts\n                              and alternatives considered\n    Build.tsx                 the epic runs, the slices, the rubrics\n    PullRequests.tsx          the envisioned pull request first, then the real ones,\n                              then FlightDeck\n    Shipped.tsx               the stage, the publications, and the deploy gap\n  atoms.tsx                   Panel, Box, Chip, TextList, and the band and tint classes\n  DiagramTiles.tsx            the diagram tiles, the dialog, the zoom, and the offline\n                              source fallback\n  Sheet.tsx                   the record sheet, and its jumplink row\n  Rail.tsx / TopBar.tsx       the traverse and the work item\n  Board.tsx                   the board E19 adds: one row per design, its stage,\n                              and its six record marks\n```\n\n**Why the section model lives in `Pager.tsx` and not in each level.** One index\ndrives the strip, the pager bar, and the two arrow keys, and one function computes\nthe level's progress. A level states its sections and nothing else.\n\n**Why the prototypes are separate directories.** `src/variations/sections-e/` and\n`src/variations/app-shell/` hold the built prototype and its comparison. They are\nscaffolding for the design decision, they are not the shipped surface, and the\nslices below port what they proved into `src/shell/`.\n\n### FlightDeck\n\n```\nsrc/flightdeck/\n  OpenSet.tsx                 the multi-pull-request mode: every open pull request,\n                              its head branch, its changed files, its design, and the\n                              ones the auto-merge rules already handled\n  SinglePr.tsx                the single-pull-request mode, at parity with today's\n                              viewer, plus the joins rail\n  ModeSwitch.tsx              one surface, two modes\n  MergePlan.tsx               renders data/merge-plan.json. It computes nothing\n```\n\n### Reference\n\n```\nsrc/reference/\n  Decisions.tsx               every decision, with its rule and its anchors\n  Coverage.tsx                the contexts, the districts, and the uncovered ones\n```\n\n**Why the five record-type tabs go.** E9 removes them. Reference is the surface those tabs\npointed at. A tab per record type also grows with the corpus, which section 2.1 of\nthe interaction design forbids.\n\n### Outside the browser\n\n```\nplugins/pr/scripts/\n  simulate_merge_order.py     reads the selected open set, simulates at least three\n                              paths against the common ancestor, replays the best one\n                              or two on a scratch worktree, and writes\n                              <bundle-dir>/data/merge-plan.json\n  execute_path.py             the runner: one integration branch per bundle, chained\n                              from the common ancestor, never the default branch\nshared/ledger.py              gains the merge-order proposal, the acceptance, and the\n                              post-merge actual as subtypes of the existing state line\nplugins/artifact/scripts/\n  export_artifact.py          matches the build's named markers, and drops or\n                              transcodes audio when a level exceeds the 16 MiB budget\n```\n\n### Tests\n\n```\ntests/test_viewer_build.py          new. Runs the viewer build and compares the result\n                                    against the committed index.html, byte for byte\ntests/test_sections.py              new. The section model's own claims, against the\n                                    served viewer\ntests/test_simulate_merge_order.py  new. The simulator, against a fixture repository\ntests/test_execute_path.py          new. The runner, against a fixture repository\ntests/test_ledger.py                extended. The three new state subtypes\ntests/test_export_artifact_budget.py new. The budget, and the degradation order\n```\n\n## Types & signatures\n\n```ts\n/* src/data/types.ts — the record types the index carries. */\n\nexport type DesignStage =\n  | \"backlog\" | \"design\" | \"review\" | \"approved\" | \"implemented\" | \"superseded\";\n\nexport interface DesignEntity {\n  id: string;\n  name: string;\n  outcome: string;\n  stage: DesignStage;\n}\n\nexport interface EpicEntity {\n  id: string;            // \"<design>/<epic-id>\"\n  design: string;\n  epic_id: string;       // \"E5\"\n  branch: string | null;\n  pr: number | null;\n  state: string;         // the ENTITY's own state, not the refined one\n  note: string | null;\n}\n\nexport interface SliceEntity {\n  id: string; n: number; title: string; ends_with: string | null;\n  score: number | null; state: string; attempts: number; feature: string;\n}\n\nexport interface PullRequest {\n  id: number;\n  title: string;\n  state: \"open\" | \"merged\" | \"closed\";\n  /** The head branch. Present on an open pull request, absent on a merged one. */\n  head: string | null;\n}\n\n/**\n * ADR-0027 fixes this shape, and E3 declares it once.\n * A field renamed on PullRequest and not here fails the build.\n */\nexport type OpenPullRequest = PullRequest & { status: \"open\" };\n\n/* The index, as data/index.json holds it. */\nexport interface RecordIndex {\n  entities: {\n    design: DesignEntity[]; epic: EpicEntity[]; slice: SliceEntity[];\n    adr: AdrEntity[]; boundary_rule: BoundaryRule[]; district: DistrictEntity[];\n    context: ContextEntity[]; pull_request: PullRequest[]; publication: Publication[];\n    epic_design: EpicDesignEntity[]; interaction_design: InteractionDesignEntity[];\n  };\n  joins: {\n    epic_status: Record<string, string>;\n    epic_to_pull_request: Record<string, number[]>;\n    slice_to_epic: Record<string, string>;\n    slice_to_epic_unresolved: string[];\n    adr_to_pull_request: Record<string, number[]>;\n    adr_to_context: Record<string, string[]>;\n    adr_to_district: Record<string, string[]>;\n    context_verifies_district: Record<string, string[]>;\n    district_uncovered: string[];\n    feature_gates: Record<string, GateStep[]>;\n  };\n}\n\nexport function loadIndex(): Promise<RecordIndex>;\nexport function entitiesOf(index: RecordIndex): RecordIndex[\"entities\"];\nexport function joinsOf(index: RecordIndex): RecordIndex[\"joins\"];\n\n/* src/shell/model.ts — the shell's own derivations. It computes no join. */\nexport function buildWorkItems(index: RecordIndex, designs: DesignRecords): Map<string, WorkItem>;\nexport function gatesOf(work: WorkItem): Gates;\nexport function levelsOf(work: WorkItem): Record<LevelKey, LevelState>;\nexport function readRoute(): Route;\nexport function routeHref(workId: string, section: SectionKey, tail?: string): string;\nexport function epicGroups(work: WorkItem): EpicEntity[][];\n\n/* src/shell/Pager.tsx — ADR-0028's shape. */\nexport interface Paging {\n  index: number; count: number;\n  select(index: number): void; previous(): void; next(): void;\n}\n\n/** The active section, clamped, and reset to `start` on a route change. */\nexport function useSectionPaging(count: number, routeKey: string, start?: number): Paging;\n\n/** How far through one box the reader is, from 0 to 1. A box with no range is 1. */\nexport function withinOf(box: HTMLElement | null): number;\n\n/** (index + within) / count, as a motion value the progress strip reads. */\nexport function useLevelProgress(index: number, count: number, boxRef: RefObject<HTMLElement | null>): MotionValue<number>;\n\nexport function SectionStrip(props: { targets: JumpTarget[]; activeIndex: number; onSelect(index: number): void }): JSX.Element;\nexport function SectionStage(props: { index: number; activeBoxRef?: RefObject<HTMLDivElement | null>; children: ReactNode }): JSX.Element;\nexport function SectionPager(props: { index: number; count: number; onPrevious(): void; onNext(): void }): JSX.Element;\n\n/* src/shell/DiagramTiles.tsx */\nexport function DiagramTiles(props: { levels: string[]; sources: Record<string, string>; theme: Theme }): JSX.Element;\nexport function loadMermaid(theme: Theme): Promise<MermaidApi>;\nexport function uncap(svg: string): string;\n\n/* src/shell/Sheet.tsx */\nexport function useSheetHeadings(scroll: HTMLElement | null, keys: readonly unknown[]): SheetHeading[];\n\n/* Off the browser. Python, in plugins/pr/scripts/. */\ndef simulate(selected: list[OpenPullRequest], ancestor: str, scratch: Path) -> MergePlan: ...\ndef replay(candidates: list[CandidatePath], scratch: Path) -> list[ReplayResult]: ...\ndef execute(path: MergePlan, pace: Literal[\"one-at-a-time\", \"all-at-once\"]) -> ExecutionRecord: ...\n```\n\n**The section model's own types are three, and nothing else needs one.** A section\nis a rendered element plus the text its own heading carries. The shell reads the\ntext from the DOM rather than storing it, so a label cannot disagree with the\nheading it names.\n\n## Call stack\n\n### Booting the shell and loading the index\n\n```\nmain.tsx\n  → createRoot(#root).render(<App />)\n  → App()\n       useIndexLoad()            → loadIndex() + loadDesigns() + loadAdrs()\n                                     fetch data/index.json\n                                     read the six window globals\n                                     resolve the joins once\n       useHashRoute()            → readRoute() from window.location.hash\n       readRoute()               → ROUTE_PREFIX, the four fields\n       useDocumentNoScroll()     → html and body carry no overflow\n       useVisibleWidthCap()      → cap the shell to the screen, never the window\n       useSectionAvailability()  → the nearest section this work can fill\n       buildWorkItems(index, designs) → one WorkItem per design\n       <Frame>                   → SidebarProvider, the shell's own token block\n            <TopBar />           → the work item, the stage, the theme\n            <Rail />             → the levels and the gated groups\n            <pane>               → the only scrolling region, on a stacking section\n```\n\nEvery count, state, and join on that path comes from the index. The shell renders.\n\n### A level rendering its sections\n\n```\npane\n  → SECTION_TITLE / SECTION_LEAD   the level's own words\n  → paged ? drop the band, keep <h1 id=\"work-section-heading\" class=\"sr-only\">\n  → pagedSections({ section, work, levelState, … })   the ordered array\n       intent              → Why, Done when, Abort if, Out of scope\n       problem-and-solution→ Problem and solution, Risks, Assessment, Unknowns\n       architecture        → Diagrams, Architecture Decisions, Boundaries,\n                             Districts and alternatives considered\n       build               → epicGroups(work) → one run per six epics\n  → <PagedLevel sections targets routeKey start>\n       useSectionPaging(count, routeKey, start)\n       <SectionStrip targets activeIndex onSelect>      the headings\n       <SectionStage index activeBoxRef>                the track\n            one <SectionBox> per section, inert off screen\n       <LevelProgress index count boxRef>               the 4 px strip\n       <SectionPager index count onPrevious onNext>\n```\n\n`useJumpTargets(paneId, keys)` reads the panels the boxes rendered. So the strip's\nlabels come from the headings on the page, and its count comes from the same array\nthe boxes come from.\n\n### A section change, and the progress strip\n\n```\nreader presses Next, a heading, or ArrowRight\n  → useSectionPaging() step or select\n       setState(clamp(next, count))\n       a route change resets to `start`, which is the run holding an epic deep link\n  → re-render\n       SectionStage      animate x: -index * 100%     (150 ms reduced-motion aware)\n       SectionStrip      aria-current=\"step\" moves\n       SectionPager      \"Section N of M\" moves; Previous and Next disable at the ends\n       LevelProgress     useScroll({ container: boxRef })  → scrollYProgress\n                         useTransform(within => (index + within) / count)\n                         ScrollProgress value={progress}\n       one box becomes active; the others carry inert\n```\n\nThe index is view state. Nothing on that path writes the route. The progress strip\nmeasures the box, so a box that cannot scroll reads as fully read, and the value\nnever resets on a section change.\n\n### A diagram tile opening its dialog\n\n```\n<DiagramsSection levels sources theme>\n  → useEffect on mount          loadMermaid(theme)        one runtime, shared\n       api.render(id, source) per level, sequentially      uncap(result.svg)\n  → <DiagramTile>               scale = tileWidth / drawing.offsetWidth\n  → reader presses the tile     setOpen(level)\n  → <DiagramDialog>             createPortal(document.body)\n       zoom steps 50…400 % of fit\n       the drawing's box is sized in pixels, so the body's own scroll grows\n       Escape, the backdrop, and Close all close it\n       a failed render shows the authored source as text\n```\n\n### A record opening its Sheet\n\n```\nreader presses a control inside a panel\n  → openSheet(subject)          the subject: a decision, a boundary rule, or an\n                                epic's technical solution design\n  → <RecordSheet subject open>  the registry's Sheet, portal, Escape, 44 px close\n       <SheetJumpLinks headings scroll>\n            useSheetHeadings(scroll, keys)   reads [data-sheet-heading] in the record\n            the callback ref puts the scroll container in state when it mounts\n       reader presses a part link\n            scroll.scrollTo(top + delta, behavior: reduce ? instant : smooth)\n            heading.setAttribute(\"tabindex\", \"-1\"); heading.focus({ preventScroll: true })\n```\n\n## Test plan\n\nThe repository's own suite is Python: `tests/`, run with\n`uv run --with pytest pytest tests/ -v`. The viewer is TypeScript and React, so the\nplan splits into three kinds of case, and each kind names its runner.\n\n### The byte-equal rebuild test (E2, pytest)\n\n`tests/test_viewer_build.py`:\n\n1. `test_build_reproduces_the_committed_viewer` — run `npm run build` in\n   `plugins/artifact/viewer/` and compare its output against\n   `plugins/artifact/viewer/index.html`, byte for byte. Skips when `npm` or\n   `node_modules` is absent, so the suite still runs on a checkout with no Node.\n2. `test_editing_the_output_fails_the_test` — write one byte into a temporary copy\n   of the committed file and assert the comparison fails. A rebuild test that cannot\n   fail proves nothing.\n3. `test_every_named_marker_survives_a_build` — assert `export_artifact.py`'s marker\n   names all appear in the built output. That is E1's seam, and it is the assertion\n   that keeps the exporter working.\n\n### The section model's claims (pytest against the served viewer)\n\n`tests/test_sections.py`. Each case asserts one claim ADR-0028 makes, and the Gate 4c\nrubrics score the same claims on the built surface. The harness serves the bundle\nrooted at `.cobuilder-architect/self/` and measures the DOM, which is how the\nprototype's numbers in this document were taken.\n\n1. `test_paged_level_pane_does_not_scroll` — on each of the four paged levels, the\n   pane's `scrollHeight - clientHeight` is zero.\n2. `test_one_box_is_in_view` — one box's rect sits inside the pane's, and no other's.\n3. `test_strip_count_matches_box_count` — the strip's headings equal the boxes, and\n   the pager reads `Section 1 of N` for that N.\n4. `test_next_moves_exactly_one_box` — the track's transform moves by exactly one\n   box width, forward and back.\n5. `test_progress_is_level_progress` — the reading is 0 at the first section's top,\n   one count's share higher mid-box, and the full walk never decreases.\n6. `test_a_box_with_no_range_reads_as_read` — a section that fits one screen\n   contributes its whole share.\n7. `test_arrow_keys_step_and_are_guarded` — `ArrowLeft` and `ArrowRight` step, and a\n   press inside an `input`, a `textarea`, or a `select`, or with a modifier, does not.\n8. `test_paged_level_drops_the_band_and_keeps_the_id` — no band element, and\n   `#work-section-heading` exists as an `sr-only` `h1` carrying the level's title.\n9. `test_a_section_that_stacks_keeps_the_pane_scroll` — Rubrics scrolls its pane and\n   has no strip.\n10. `test_sparse_design_renders` — one of the seven `goal.json`-only designs renders\n    every level it can fill, and the rail gates what it cannot.\n\n### The rest of the program (pytest)\n\n11. `test_export_artifact_marker_seam` — a publish survives a marker rename.\n12. `test_open_pull_request_drift_fails_the_build` — renaming a field on\n    `PullRequest` and not on `OpenPullRequest` fails `npm run typecheck`.\n13. `test_simulate_writes_a_plan_against_the_ancestor` — a fixture repository gives a\n    plan whose recorded ancestor is the common ancestor, never the default head.\n14. `test_replay_counts_real_conflicts` — a fixture with a known conflict reports it.\n15. `test_execute_never_touches_the_default_branch` — the runner's merges land on\n    `flightdeck/<path>/<n>-<slug>` and the default branch keeps its head.\n16. `test_ledger_three_subtypes_fold` — a proposal, an acceptance, and an actual\n    append as `state` subtypes, and `fold_threads` keeps all three.\n17. `test_audit_reports_a_hash_mismatch` — a mismatched tree hash is a stated state.\n18. `test_publish_holds_the_budget` — the exporter drops or transcodes over-budget\naudio in the documented order, and the published file is under 16 MiB.\n19. `test_describe_leaves_a_described_pull_request_alone` — the body stays identical.\n20. `test_anchor_survives_a_rerender` — an anchor taken before a re-render still\n    resolves to its sentence after it.\n\n### Cases the rubrics score, not pytest\n\nTwo claims need a human or a scripted browser, and the repository's suite holds\nneither today. This document states each one as a case for Gate 4c to score:\n\n- **The tile dialog's zoom reads whole percentages and the drawing stays reachable**\n  at 50 %, 100 %, and 400 %, measured in the browser.\n- **The published Artifact renders every surface** under the platform's CSP, which\n  only the platform can answer.\n\n## Least confident decisions\n\n1. **E4, E6, and E8 ask for a \"detailed static HTML prototype\". The Work prototype\n   shipped as a React application instead.** `variations/sections-e/` is\n   the artifact the engineer reviewed. Either the three prototype epics accept the\n   React prototype as their deliverable, or the static-HTML wording stands and each\n   prototype is a second artifact built beside the real one. This document assumes\n   the React prototype satisfies E4 and that E6 and E8 follow it, and it could be\n   wrong about all three.\n2. **The two prototype epics E6 and E8 may collapse into their build epics.** A\n   prototype whose only job is approval is cheap work, if the build epic already\n   exists to hang it on. This document keeps them separate because E7 and E9 measure\n   their own parity against an approved prototype, and that approval is the gate.\n3. **The diagram runtime loads when the Diagrams section mounts.** It used to load\n   on the reader's press, because the runtime is large. A tile cannot wait for a\n   press, so the rule changed. The cost is that a reader who opens Architecture to\n   read decisions pays for a runtime they never use. A lazy per-tile load is the\n   obvious alternative, and this document does not take it.\n4. **`ui-spec.jsonc` has no validator.** `verify_gate.py` checks that the file\n   exists and reads nothing inside it. So the component specification is a document\n   two people can disagree about with no test to settle it. Writing a schema check\n   is real work, and nothing in this program owns it.\n5. **The build's output path moves from `dist/` to the committed `index.html`.**\n   `vite.config.ts` says ADR-0023's output rule is under revision. E2 needs the move,\n   and every `export_artifact.py` run after it rewrites a build artifact. A stale\n   committed file is a real risk here, and the byte-equal test is the only guard.\n6. **The byte-equal test depends on a pinned toolchain.** Two machines with the same\n   lockfile and the same Node version should produce the same bytes. Nobody has\n   proven that yet. If it fails, the guard test fails for a reason nobody can act on. The spike\n   in E2 is where that gets answered.\n7. **The design settles where the runner's merges come from. It does not settle who\n   deletes the integration branches.** E15 leaves N branches per path behind, and no\n   epic owns their removal. It is out of scope in `intent.json`, and it stays out of scope here.\n8. **The 16 MiB budget assumes audio is the thing that breaks first.** That is what\n   the earlier artifact experiment measured. A pull request carrying four levels of\n   scene art and inlined React could break it with no audio at all. No epic measures\n   the degradation order this document implies on a real bundle.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-viewer/03-program-design.md"}, {"id": "gate-doc-surfacing", "feature_slug": "gate-doc-surfacing", "gate": 3, "title": "Program Design: Gate doc surfacing", "body_md": "# Program Design: Gate doc surfacing\n\n## Files\n- `shared/build_index.py` — modified. New constants `PROGRAM_DESIGN_FIELDS`,\n  `EPIC_DESIGN_FIELDS`; new functions `discover_plan_gate_docs()`,\n  `project_program_design()`, `project_epic_design()`; `collect_designs()`\n  or a new `collect_gate_docs()` wires the new entities into the index;\n  `resolve_feature_gates()` attaches `doc` per gate.\n- `plugins/artifact/viewer/index.html` — modified. New functions\n  `openGateDocSheet(kind, id)`, `closeGateDocSheet()`,\n  `renderGateDocSheet()`. New markup block for the sheet (siblings of\n  `#adr-sheet`). `renderBuildsMainContent()`'s `gate-rail-grid` and\n  `renderEpicCard()` gain click wiring.\n- `tests/test_build_index_gate_docs.py` — new. Covers the projection\n  functions and `resolve_feature_gates()`'s new `doc` field.\n\n## Types & signatures\n\n```python\n# shared/build_index.py\nPROGRAM_DESIGN_FIELDS: tuple[str, ...]\nEPIC_DESIGN_FIELDS: tuple[str, ...]\n\ndef discover_plan_gate_docs(repo: Path) -> list[dict]:\n    \"\"\"One dict per docs/plans/<slug>/{03-program-design.md, epic-*-design.md}\n    found, with slug, kind ('program'|'epic'), epic_id (if kind == 'epic'),\n    path, and raw markdown text.\"\"\"\n\ndef project_program_design(feature_slug: str, path: Path, text: str) -> dict:\n    \"\"\"-> {\"id\": feature_slug, \"feature_slug\", \"gate\": 3, \"title\",\n    \"body_md\", \"approved_date\", \"source_path\"}, fields filtered through\n    project_fields(..., PROGRAM_DESIGN_FIELDS).\"\"\"\n\ndef project_epic_design(epic_id: str, feature_slug: str, path: Path, text: str) -> dict:\n    \"\"\"-> {\"id\": epic_id, \"epic_id\", \"feature_slug\", \"title\", \"body_md\",\n    \"approved_date\", \"source_path\"}, filtered through\n    project_fields(..., EPIC_DESIGN_FIELDS).\"\"\"\n```\n\n```javascript\n// plugins/artifact/viewer/index.html\nfunction openGateDocSheet(kind, id) { /* kind: 'program' | 'epic' */ }\nfunction closeGateDocSheet() { }\nfunction renderGateDocSheet() { /* reads window.INDEX.entities.program_design\n  or .epic_design by id, renders via renderMarkdown() into the sheet body */ }\n```\n\n## Call stack\n\nIndex build (top to bottom):\n```\nmain()\n  build_index(repo)\n    collect_designs(repo)          # unchanged\n    discover_plan_gate_docs(repo)  # new\n      project_program_design()     # new, one call per feature with a Gate 3 doc\n      project_epic_design()        # new, one call per epic-*-design.md found\n    resolve_joins(repo)\n      resolve_feature_gates(repo)  # extended: attaches doc id where found\n```\n\nViewer click (top to bottom):\n```\ngate-card click / epic design-doc chip click\n  openGateDocSheet(kind, id)\n    state.gateDocOpen = true; state.activeGateDocKind = kind; state.activeGateDocId = id\n    renderGateDocSheet()\n      lookup window.INDEX.entities.program_design[id] or .epic_design[id]\n      renderMarkdown(record.body_md)   # reused, unchanged\n    renderSheetVisibility()            # existing mutual-exclusivity function, extended\n```\n\n## Test plan\n- `test_build_index_gate_docs.py::test_project_program_design_reads_title_and_body`\n- `test_build_index_gate_docs.py::test_project_epic_design_parses_epic_id_from_filename`\n- `test_build_index_gate_docs.py::test_discover_plan_gate_docs_skips_plan_dir_with_no_gate_docs`\n- `test_build_index_gate_docs.py::test_resolve_feature_gates_attaches_doc_id_when_program_design_exists`\n- `test_build_index_gate_docs.py::test_resolve_feature_gates_gate3_doc_absent_when_no_program_design_md`\n- `test_build_index_gate_docs.py::test_build_index_end_to_end_index_json_carries_new_entity_keys`\n\nManual dogfood check (no automated frontend test harness in this repo):\nopen the rebuilt viewer, navigate to Builds, click this feature's own Gate\n3 card once `03-program-design.md` exists (after this gate is approved),\nconfirm the sheet shows this document's content.\n\n## Least confident decisions\n- Whether `collect_designs()` should absorb the new entities directly, or\n  a separate `collect_gate_docs()` function is cleaner — deferred to\n  implementation; either satisfies the same `data/index.json` shape.\n- Whether the sheet reuses the *same* DOM nodes as `#adr-sheet` with a mode\n  flag, or gets its own sibling `#gate-doc-sheet` block — leaning toward a\n  sibling block, since `renderAdrSheet` and a future `renderGateDocSheet`\n  read different record shapes and a shared node risks stale content on a\n  fast open/open across the two kinds.\n", "approved_date": null, "source_path": "docs/plans/gate-doc-surfacing/03-program-design.md"}, {"id": "gate-state-resolution", "feature_slug": "gate-state-resolution", "gate": 3, "title": "Program design: Gate state resolution", "body_md": "# Program design: Gate state resolution\n\n## Files\n\n| File | Change |\n|---|---|\n| `plugins/artifact/scripts/build_builds_view.py` | Add `is_resolved()`. Give `current_doc()` the filtered document map, and let it return no document. Name a title and an ask note for the Gate 2b document. |\n| `.cobuilder-architect/self/pages/builds-view.html` | Add the `na` gate treatment. Guard the crumb against a missing document. Key the approval prompt on the gate, and state when a gate holds no document. Emit an empty ask gate when nothing waits. |\n| `plugins/artifact/viewer/index.html` | Give an `n/a` gate its own card class, its own pill class, and the words \"Not applicable\". Count approved gates in the header. |\n| `tests/test_build_builds_view.py` | Cover the four shapes of `current_doc()`, the `is_resolved()` predicate, and the summary line. |\n| `tests/test_viewer_modes.py` | Cover the `n/a` treatment and the header count. |\n\n`shared/build_index.py` needs no change. It already projects the state, the\ndocument, and the document kind, and slice 7 of `interaction-design-gate`\nalready added the `interaction` kind.\n\n## Types & signatures\n\n```python\n# plugins/artifact/scripts/build_builds_view.py\n\nTITLES = {\n    # Was: no entry. GATE_DOCS named \"interaction-design.md\" for gate 2b\n    # without a title, so the rail printed the text \"undefined\".\n    \"interaction-design.md\": \"Interaction design\",\n    # ...the existing entries...\n}\n\nASK_NOTES = {\n    # Was: no entry. A page that opened on a pending Gate 2b printed an\n    # empty approval prompt.\n    \"2b\": \"Approval moves to Gate 3 — program design — where the files, \"\n          \"the type signatures, and the test plan are written before any \"\n          \"implementation exists.\",\n    # ...the existing entries...\n}\n\n\ndef is_resolved(state: str) -> bool:\n    \"\"\"A resolved gate needs no answer: it reads APPROVED or n/a.\n\n    One name for the test, because two call sites asked the same question\n    and one of them asked it inline.\n    \"\"\"\n    return state.startswith(\"APPROVED\") or state.startswith(\"n/a\")\n\n\ndef current_doc(\n    gates: list[dict], present: dict[str, list[str]]\n) -> tuple[str, str | None, bool]:\n    \"\"\"The gate the page opens on, and the document to show with it.\n\n    An open gate is the first gate that still needs an answer. The page\n    shows that gate's first document when the plan holds it, and no\n    document when it does not.\n\n    Every gate resolved means nothing waits. The page then shows the last\n    gate that holds a document.\n\n    `present` is the document map filtered to the files the plan holds, so\n    a returned document always exists on disk.\n\n    Returns the gate, its document or None, and whether the gate waits for\n    an answer.\n    \"\"\"\n    for g in gates:\n        if not is_resolved(g[\"state\"]):\n            docs = present.get(g[\"n\"], [])\n            return g[\"n\"], (docs[0] if docs else None), True\n    for g in reversed(gates):\n        docs = present.get(g[\"n\"], [])\n        if docs:\n            return g[\"n\"], docs[0], False\n    return (gates[-1][\"n\"] if gates else \"1\"), None, False\n```\n\n```python\n# render(), inside the fence rewrite loop.\n\ngate, doc, pending = current_doc(payload[\"gates\"], present)\n\n# Was: titles = {k: v for k, v in TITLES.items() if k in payload[\"docs\"]}\n# An unlisted document vanished from the map, so the rail printed the\n# text \"undefined\" for it. Every held document now carries a title.\ntitles = {k: TITLES.get(k, k) for k in payload[\"docs\"]}\n\n# Was: f'var cur={{gate:\"{gate}\",doc:\"{doc}\"}};'\n# A missing document must be null, not the text \"None\".\nlines[i] = f\"var cur={{gate:{json.dumps(gate)},doc:{json.dumps(doc)}}};\"\n\n# Was: json.dumps(gate). The ask block keys on the gate, so an empty ask\n# gate is what tells the page that nothing waits.\nlines[i] = f\"var ASKGATE={json.dumps(gate if pending else '')};\"\n\n# Was: doc if pending else \"\". A missing document must not fire the\n# \"rendered from the markdown\" note, which names a path.\nlines[i] = f\"var ASKDOC={json.dumps(doc if (pending and doc) else '')};\"\n\n# Was: f'buildRail(); go(\"{gate}\",\"{doc}\");'\nlines[i] = f\"buildRail(); go({json.dumps(gate)}, {json.dumps(doc)});\"\n\n# Was: \"'awaiting approval' if pending else 'all gates approved'\".\n# An n/a gate is not an approved gate, and the word \"approved\" claimed\n# something the plan had not said.\nf\"{'awaiting approval' if pending else 'no gate awaiting an answer'}\"\n```\n\n```javascript\n// .cobuilder-architect/self/pages/builds-view.html\n\n/* Was: return s.indexOf('approved')===0?'done'\n                     :s.indexOf('in progress')===0?'now':'todo';\n   An \"n/a\" gate fell to 'todo', which drew it as not started. */\nfunction cls(g){var s=g.state.toLowerCase();\n  return s.indexOf('approved')===0?'done'\n       :s.indexOf('n/a')===0?'na'\n       :s.indexOf('in progress')===0?'now':'todo';}\n\n/* go(): a missing document must read as a name, not as the text \"null\". */\nvar path=doc?((PATHS&&PATHS[doc])||doc):('Gate '+gate);\n\n/* go(): the prompt answers a gate, and a gate with no document has\n   nothing to approve. */\nvar have=!!(doc&&D[doc]);\nif(ASKGATE&&gate===ASKGATE&&have) ask='<div class=\"ask\">...';\nif(ASKGATE&&gate===ASKGATE&&!have) note+='<div class=\"note\">This gate holds no document yet...</div>';\n```\n\n```css\n/* .cobuilder-architect/self/pages/builds-view.html — the closed treatment. */\n.gate.na .dot{background:var(--sunk);border-color:var(--line);color:var(--ink3)}\n.gate.na .stt{color:var(--ink3)}\n.pill.na{background:var(--sunk);color:var(--ink3)}\n```\n\n```javascript\n// plugins/artifact/viewer/index.html\n\n/* Was: const statusClass = isApproved ? 'is-approved'\n                         : (isCurrent ? 'is-current' : 'is-planned');\n   An \"n/a\" gate fell to 'is-planned' and read as not started. */\nconst isNa = /^n\\/a/i.test((g.state || '').trim());\nconst statusClass = isApproved ? 'is-approved'\n                  : isNa ? 'is-na'\n                  : (isCurrent ? 'is-current' : 'is-planned');\n\n/* Was: isApproved ? 'ok' : (isCurrent ? 'now' : 'wait') */\nconst pillClass = isApproved ? 'ok' : isNa ? 'na' : (isCurrent ? 'now' : 'wait');\n\n/* Was: isApproved ? `Approved on ...` : 'In progress' */\nconst gateDesc = isApproved ? `Approved on ${...}` : isNa ? 'Not applicable' : 'In progress';\n\n/* Was: const totalGates = gateRails.reduce((sum, r) => sum + r.gates.length, 0);\n   The label then read \"N Gates Approved\" over a count of every gate. */\nconst totalGates = gateRails.reduce((sum, r) => sum + r.gates.length, 0);\nconst approvedGates = gateRails.reduce((sum, r) =>\n  sum + r.gates.filter(g => (g.state || '').trim().toUpperCase().startsWith('APPROVED')).length, 0);\n// label: `${approvedGates} of ${totalGates} Gates Approved`\n```\n", "approved_date": null, "source_path": "docs/plans/gate-state-resolution/03-program-design.md"}, {"id": "interaction-design-gate", "feature_slug": "interaction-design-gate", "gate": 3, "title": "Program design: Interaction design gate", "body_md": "# Program design: Interaction design gate\n\n## Files\n\n### New\n\n- `plugins/implement/skills/design-to-code/SKILL.md`\n- `plugins/implement/skills/design-to-code/references/jsonc-schema.md`\n- `plugins/implement/skills/design-to-code/references/research-workflow.md`\n- `plugins/implement/skills/design-to-code/references/shadcn-patterns.md`\n- `plugins/implement/skills/design-to-code/references/implementation-rules.md`\n- `plugins/implement/skills/design-to-code/templates/interaction-design.md`\n- `plugins/implement/skills/design-to-code/examples/workflow-matrix-interaction-design.md`\n- `plugins/implement/skills/design-to-code/examples/voice-dashboard.jsonc`\n- `tests/test_design_to_code_skill.py`\n- `tests/test_gate_2b.py`\n- `docs/plans/interaction-design-gate/interaction-design.md`. The plan's own\n  artifact. It is written only if Gate 2b applies, and it does not apply, so\n  this file is not created.\n\n### Modified\n\n| File | Change |\n|---|---|\n| `plugins/implement/scripts/verify_gate.py` | A `2b` group and its status regex. The docstring and the `--json` payload gain the `2b` key. |\n| `shared/build_index.py` | `STATUS_GATE_RE` accepts a `2b` label. A new `interaction_design` array. `resolve_feature_gates()` writes a string `n`. |\n| `plugins/artifact/scripts/build_builds_view.py` | `GATE_LINE` accepts `2b`. `GATE_DOCS` gains a `2b` key. `current_doc()` treats `n/a` as done. |\n| `plugins/implement/skills/build/SKILL.md` | The `## Gate 2b` section. The `2b` line in the `00-status.md` template. The resume rule. |\n| `plugins/implement/skills/build/references/rubric-authoring.md` | §4 gains both artifacts as derivation sources. |\n| `plugins/implement/skills/build/references/slice-loop.md` | RED reads both documents. VALIDATE's front-end criteria name a browser check. |\n| `plugins/implement/commands/start.md` | The \"What this writes\" list. |\n| `plugins/implement/.claude-plugin/plugin.json` | `version` 0.1.0 to 0.2.0. |\n| `tests/test_pillar_boundaries.py` | `KNOWN_OTHER_SKILLS` gains `design-to-code`. |\n\n### Deleted\n\nThree files of the upstream five-step skill do not travel. Each one serves the\nproduct brief, which Gate 1 owns.\n\n- `templates/brief-template.md`\n- `templates/prd.md`\n- `examples/workflow-matrix-prd.md`\n\n## Types & signatures\n\n### `plugins/implement/scripts/verify_gate.py`\n\n```python\n# The 00-status.md Gate 2b line, e.g.:\n#   - Gate 2b — Interaction design: pending | APPROVED 2026-09-14 | n/a (no UI) — Screens: \"...\"\nSTATUS_2B_RE = re.compile(\n    r\"^\\s*-\\s*Gate\\s*2b\\b.*?:\\s*(APPROVED\\b.*|pending|in progress|n/a.*)\\s*$\",\n    re.IGNORECASE,\n)\n\n# The load-bearing headings of templates/interaction-design.md. Each one\n# carries a concept that no other approved document states.\nREQUIRED_INTERACTION_SECTIONS = [\n    \"## 2. Information Architecture\",\n    \"### 2.3 Declared Defaults\",\n    \"### 3.2 Component States\",\n    \"### 3.3 Visibility Gating\",\n    \"### 4.1 Transition Table\",\n    \"### 4.2 Timing Tokens\",\n    \"### 11.2 Hit Targets\",\n    \"### 11.3 Scroll Ownership\",\n]\n\n\ndef check_2b_status(status_text: str | None) -> str:\n    \"\"\"Return \"ok\", \"n/a\", or \"pending\" for the 2b line in 00-status.md.\n\n    A status file with no 2b line returns \"n/a\". The gate is newer than those\n    files, so their silence means \"this gate did not exist\", not \"this gate is\n    waiting\". A 2b line that is present and unapproved returns \"pending\".\n    \"\"\"\n\n\ndef check_interaction_sections(text: str) -> str:\n    \"\"\"Return \"ok\", or \"incomplete:<missing,...>\". Mirrors\n    check_design_sections().\"\"\"\n\n\ndef check_2b(plan_dir: Path, status_text: str | None) -> dict[str, str]:\n    \"\"\"Return the 2b group.\n\n    Keys:\n      interaction.file      \"ok\" | \"missing\" | \"n/a\"\n      interaction.sections  \"ok\" | \"incomplete:<...>\" | \"missing\" | \"n/a\"\n      ui_spec.file          \"ok\" | \"missing\" | \"n/a\"\n      interaction.approved  \"ok\" | \"pending\" | \"n/a\"\n    \"\"\"\n```\n\n`is_ok()` already accepts `\"n/a\"`, so no change is needed there. The `--json`\npayload gains `\"2b\": results`. The exit rule stays: exit 0 only when every key\nis ok or n/a.\n\n### `shared/build_index.py`\n\n```python\n# Was: r\"^-\\s*Gate\\s*(\\d+)\\s*[—–-]\\s*([^:]+):\\s*(.+)$\"\n# The label group now accepts a letter suffix, so \"Gate 2b\" parses.\nSTATUS_GATE_RE = re.compile(r\"^-\\s*Gate\\s*(\\d+\\w?)\\s*[—–-]\\s*([^:]+):\\s*(.+)$\")\n\n\ndef discover_interaction_design_docs(repo: Path) -> list[dict]:\n    \"\"\"Project docs/plans/<slug>/interaction-design.md into entities.\n\n    Mirrors discover_plan_gate_docs() under ADR-0022. Returns one entity per\n    plan that carries the file. The entity carries feature_slug, gate, title,\n    state, source_path, and body_md.\n    \"\"\"\n\n\ndef resolve_feature_gates(repo: Path) -> dict[str, list[dict]]:\n    \"\"\"Unchanged contract. `n` becomes a string for every gate, not only 2b,\n    so one type serves the whole array.\"\"\"\n```\n\n### `plugins/artifact/scripts/build_builds_view.py`\n\n```python\n# Was: re.compile(r\"- Gate (\\d) — ([^:]+): (.+)\")\nGATE_LINE = re.compile(r\"- Gate (\\d\\w?) — ([^:]+): (.+)\")\n\nGATE_DOCS = {\n    \"1\": [\"01-product.md\"],\n    \"2\": [\"02-architecture.md\", \"02a-artifact-map.md\",\n          \"02b-view-designs.md\", \"02c-record-model.md\"],\n    \"2b\": [\"interaction-design.md\"],\n    \"3\": [\"03-program-design.md\"],\n    \"4\": [\"04-slices.md\", \"rubric-manifest\"] + [...],\n}\n\n\ndef current_doc(gates: list[dict]) -> tuple[str, str, bool]:\n    \"\"\"A gate counts as done when its state starts with \"APPROVED\" or with\n    \"n/a\". The second test is new: an \"n/a\" gate needs no answer, so the page\n    must not open on it.\"\"\"\n```\n\n### `tests/test_gate_2b.py`\n\n```python\ndef test_missing_2b_line_is_n/a(tmp_path): ...\ndef test_pending_2b_line_fails(tmp_path): ...\ndef test_approved_2b_line_passes_with_both_files(tmp_path): ...\ndef test_approved_2b_line_fails_when_a_section_is_absent(tmp_path): ...\ndef test_n/a_2b_line_passes_without_any_artifact(tmp_path): ...\ndef test_two_historical_plans_still_pass(): ...\n```\n\n### `tests/test_design_to_code_skill.py`\n\n```python\ndef test_skill_frontmatter_parses(): ...\ndef test_overview_names_three_steps(): ...\ndef test_every_relative_path_cited_resolves(): ...\ndef test_removed_brief_files_are_not_cited(): ...\ndef test_no_product_brief_terminology(): ...\n```\n", "approved_date": null, "source_path": "docs/plans/interaction-design-gate/03-program-design.md"}], "epic_design": [{"id": "cobuilder-family/E1", "epic_id": "E1", "feature_slug": "cobuilder-family", "title": "Epic Technical Solution Design: E1 — One plugin becomes five", "body_md": "# Epic Technical Solution Design: E1 — One plugin becomes five\n\nFeature: cobuilder-family\nEpic ID: plugin-split/E1\n\nThis design was written on 2026-08-25, after this epic's four slices were\nbuilt and accepted. Gate 4b did not run before implementation, so this\ndocument records the design as built. It did not constrain the work. The\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\nderived without a written 4b design in hand.\n\n## Scope and Intent\n\nE1 turns one plugin into five sibling plugins under `plugins/`, each with\nits own `.claude-plugin/plugin.json`, `commands/`, `skills/`, and `scripts/`.\nIt renames three modes inside the single plugin first, deletes the\nduplicate `explore-design` command, fixes 33 cross-pillar references, then\nsplits the tree into five manifests, and ports two skills in from outside\nthe repository. ADR-0016 decided the five-way split. ADR-0017 decided the\nvendoring mechanism E1 relies on but does not itself implement (E2 proves\nit, E3 gates it).\n\n## Files Touched\n\nThe whole split landed in commit `4156641` (\"Slice 10 change\"), together\nwith parts of E3, E4, and half of E5. `git show --stat 4156641` lists the\nfull file move. The load-bearing paths today are:\n\n- `.claude-plugin/marketplace.json` — one file, five plugin entries.\n- `plugins/cobuilder-architect/.claude-plugin/plugin.json`,\n  `plugins/cobuilder-pr/.claude-plugin/plugin.json`,\n  `plugins/cobuilder-implement/.claude-plugin/plugin.json`,\n  `plugins/cobuilder-artifact/.claude-plugin/plugin.json`,\n  `plugins/cobuilder-full-lifecycle/.claude-plugin/plugin.json` — one\n  manifest per plugin.\n- `plugins/cobuilder-architect/commands/{design,review,maintenance,\n  decisions,describe,debug}.md` — six modes, `explore-design.md` deleted.\n- `plugins/cobuilder-architect/skills/architecture/` — moved from the\n  repository root, unchanged in content apart from path updates.\n- `plugins/cobuilder-pr/skills/odyssey/` — the renamed modes: the old\n  `submit` became `generate`, and the old `generate` became `review`.\n- `plugins/cobuilder-artifact/scripts/{serve_bundle.py,export_artifact.py,\n  export_index.py,record_publish.py}` and\n  `plugins/cobuilder-artifact/viewer/index.html`.\n- `plugins/cobuilder-full-lifecycle/skills/cobuilder-full/SKILL.md` and\n  `plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py`.\n- Each plugin carries a `shared` symlink at its own root, verified by\n  `test_shared_is_a_symlink_in_the_source_tree` in\n  `tests/test_plugin_manifests.py`.\n\n## Types & Signatures\n\nE1 carries no new Python functions of its own. Its unit of work is the\nmanifest and the command dispatch line, not a function signature. The\ncontract each command file holds is a one-line `Skill(...)` dispatch, for\nexample `plugins/cobuilder-architect/commands/design.md` dispatching\n`Skill(\"architecture\", args=\"design $ARGUMENTS\")`. `tests/test_commands.py`\nasserts this shape directly:\n\n```python\ndef test_command_dispatches_to_a_declared_mode(command_path): ...\ndef test_explore_design_command_does_not_exist(): ...\ndef test_no_two_commands_in_the_same_plugin_dispatch_identically(): ...\ndef test_cobuilder_pr_ships_its_own_review_command(): ...\ndef test_architecture_skill_declares_exactly_six_modes(): ...\n```\n\n## Slice Decomposition\n\nPer `docs/plans/cobuilder-family/04-slices.md`, in build order:\n\n1. **Slice 4 — renames inside today's single plugin.** No dependency. Six\n   architecture modes land, `explore-design` is deleted, `submit` becomes\n   `generate`, and the old `generate` becomes `review`. Completed on attempt\n   2, score 1.00.\n2. **Slice 5 — the 33 cross-pillar references are fixed.** Depends on\n   slice 4 naming the modes the references must point to. The regression\n   test runs and passes while the repository is still one plugin, so it\n   guards the split rather than reporting on it. Completed on attempt 3,\n   score 1.00.\n3. **Slice 6 — five plugins, five manifests.** Depends on slices 4 and 5.\n   `claude plugin validate` passes on each, and the family installs.\n   Completed, score 1.00.\n4. **Slice 7 — the two ports land.** Depends on slice 6 existing so the\n   ported skills have a plugin to land in. `cobuilder-implement` is renamed\n   and shipped, `collaborate-with-user` folds into `cobuilder-artifact`,\n   and the orientation skill is written. Completed, score 1.00.\n\n## Test Plan\n\n- `tests/test_plugin_manifests.py` — `test_manifest_parses_and_has_\n  required_fields`, `test_no_agent_hook_or_mcp_server` (asserts none of the\n  five plugins declares an agent, a hook, or an MCP server, per the\n  installation rule in `CLAUDE.md`), `test_marketplace_lists_all_five_\n  plugins`, `test_umbrella_plugin_depends_on_the_other_four`, and\n  `test_shared_is_a_symlink_in_the_source_tree`.\n- `tests/test_commands.py` — the eight assertions listed above, covering\n  the rename, the deletion, and the one-dispatch-per-command rule.\n- `tests/test_pillar_boundaries.py` — `test_no_cross_pillar_references_in_\n  repo` is the regression test slice 5 exists to write. It greps every\n  plugin for `${CLAUDE_PLUGIN_ROOT}/skills/<other pillar>` and fails on a\n  hit. The file also carries thirteen evasion tests (`test_evasion_*`)\n  covering relative paths, backslashes, mixed case, and files outside the\n  original allowlist.\n- `tests/test_script_references.py` — `test_runnable_commands_name_\n  existing_scripts` checks that a script path named in prose actually\n  exists on disk after the move.\n\n## Risks & Open Questions\n\n- **The split landed inside a much larger commit.** `4156641` also carries\n  parts of E3, E4, and half of E5. There is no commit boundary that isolates\n  E1 on its own, so `git show --stat` on that one commit is the closest\n  thing to an E1-only diff, and it is not a clean one.\n- **`plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py` exists.**\n  `03-program-design.md`'s Files section proposed `scripts/watch_feedback.py`\n  under `cobuilder-artifact`. The shipped location is under\n  `cobuilder-full-lifecycle` instead. This is a placement drift from the\n  program design, not a defect, and no ADR records the reason for the\n  change.\n- **No commit is titled after E1 or after any of its four slices by name.**\n  The slice table's \"completed\" status and the rubric scores are the only\n  record that ties specific work to specific slice numbers. A future\n  session auditing this epic has to read `04-slices.md` and the rubric\n  files, not `git log`, to find slice boundaries.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-family/epic-E1-design.md"}, {"id": "cobuilder-family/E3", "epic_id": "E3", "feature_slug": "cobuilder-family", "title": "Epic Technical Solution Design: E3 — The seam is version-safe", "body_md": "# Epic Technical Solution Design: E3 — The seam is version-safe\n\nFeature: cobuilder-family\nEpic ID: plugin-split/E3\n\nThis design was written on 2026-08-25, after this epic's two slices were\nbuilt and accepted. Gate 4b did not run before implementation, so this\ndocument records the design as built. It did not constrain the work. The\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\nderived without a written 4b design in hand.\n\n## Scope and Intent\n\nE3 makes the bundle safe to write from five independently versioned\nplugins. It adds `require_compatible()` and `stamp_generator()`, raises\n`bundle_format` to 3 and `schema_version` to 1.3, replaces the scalar\n`generator_version` with a `generators` map keyed by plugin name, and gates\nevery bundle-writing script behind the compatibility check. ADR-0017 governs\nthis epic.\n\n## Files Touched\n\n- `shared/_bundle_meta.py` — `SCHEMA_VERSION = \"1.3\"`,\n  `SCHEMA_VERSION_KNOWN = {\"1.0\", \"1.1\", \"1.2\", \"1.3\"}`,\n  `CURRENT_BUNDLE_FORMAT = 3`, the `BundleIncompatible` exception, and both\n  new functions.\n- `shared/migrate_bundle.py` — the layout and schema ladders that carry a\n  bundle from format 2 to format 3, and the scalar-to-map migration for\n  `generators`.\n- Every script that writes into a bundle now calls `require_compatible()`\n  first: `shared/build_index.py`, `plugins/cobuilder-artifact/scripts/\n  export_artifact.py`, `plugins/cobuilder-artifact/scripts/serve_bundle.py`\n  (through `shared/ledger.py`), and `plugins/cobuilder-pr/scripts/\n  extract_story.py`.\n- `shared/ledger.py` — the append-only comment ledger this epic's gate\n  gets wired in front of, in the sense that every write path funnels\n  through `LedgerPaths` and the compatibility check runs before it.\n\n## Types & Signatures\n\nRead directly from `shared/_bundle_meta.py`:\n\n```python\ndef read_plugin_version() -> str: ...\ndef read_plugin_name() -> str: ...\n\nclass BundleIncompatible(RuntimeError): ...\n\ndef _version_tuple(version: str) -> tuple[int, ...]: ...\n\ndef require_compatible(bundle_dir: Path, plugin: str) -> None:\n    \"\"\"Raise BundleIncompatible when the bundle's min_reader_schema exceeds\n    this plugin's SCHEMA_VERSION, or when bundle_format exceeds\n    CURRENT_BUNDLE_FORMAT. Silent otherwise. A missing bundle.json is a new\n    bundle and passes.\"\"\"\n\ndef stamp_generator(bundle_dir: Path, plugin: str, version: str) -> None:\n    \"\"\"Write this plugin's version into bundle.json's generators map,\n    without touching another plugin's existing entry.\"\"\"\n```\n\n`bundle.json` at format 3 carries `generators` as a map, for example\n`{\"cobuilder-pr\": \"0.5.0\", \"cobuilder-artifact\": \"0.5.0\"}`, replacing the\nsingle `generator_version` scalar the program design's Types and\nsignatures section shows as the format-2 shape.\n\n## Slice Decomposition\n\nPer `04-slices.md`:\n\n1. **Slice 2 — the compatibility gate exists.** No dependency on E1 or E4.\n   `require_compatible()` and `stamp_generator()` pass their own tests,\n   called by nothing yet. Completed, score 1.00.\n2. **Slice 3 — the gate is wired and the bundle migrates.** Depends on\n   slice 2. Format 3, `min_reader_schema`, the `generators` map, and every\n   writer gated. Completed, score 1.00.\n\nBoth slices land before E1's slice 6 splits the tree into five plugins, per\n`04-slices.md`'s stated build order: \"Slices 2 through 5 stay inside\ntoday's single plugin,\" so the compatibility gate is proven before the\ndirectory move that would make debugging both changes at once harder.\n\n## Test Plan\n\n- `tests/test_bundle_meta.py` — eleven tests, including\n  `test_newer_min_reader_schema_raises`, `test_equal_min_reader_schema_\n  passes`, `test_version_comparison_is_numeric_not_string`,\n  `test_version_comparison_older_dotted_passes`, `test_newer_bundle_format_\n  raises`, `test_missing_bundle_json_passes_silently`, `test_stamp_\n  generator_preserves_other_plugin`, `test_stamp_generator_overwrites_own_\n  prior_entry`, `test_stamp_generator_keeps_old_scalar_field`, and\n  `test_stamp_generator_creates_bundle_json_when_missing`.\n- `tests/test_migrate_bundle.py` — `test_build_generators_map_preserves_\n  old_scalar`, `test_build_generators_map_keeps_existing_map_entries`,\n  `test_full_migration_turns_scalar_into_map`, `test_guard_rejects_\n  undeclared_authored_field_change`, `test_data_ladder_stops_before_later_\n  steps_on_violation`, `test_main_writes_nothing_on_guard_violation`,\n  `test_backup_written_before_new_story`, `test_viewer_refreshes_even_at_\n  current_version`, and `test_viewer_refresh_is_not_version_gated_in_\n  source` — this last pair is the regression test for the exact bug\n  `CLAUDE.md`'s Bundle versioning section names: a version gate on the\n  viewer refresh that once let a stale viewer silently drop diagram\n  support.\n- `tests/test_build_index.py` — `test_calls_compatibility_gate_before_\n  first_write` confirms `build_index.py` itself calls `require_compatible()`\n  before it writes, closing the loop between E3's gate and E4's index.\n\n## Risks & Open Questions\n\n- **The program design lists three specific call sites as ungated before\n  this epic** — `extract_story.py:599`, `export_artifact.py:428`, and\n  `_manifest.py:76`. This design did not re-verify each of the three by\n  line number against the tree as built. `test_write_scripts_call_require_\n  compatible_first`, named in `03-program-design.md`'s test plan, is the\n  test that would settle this, and its presence or absence under those\n  exact names was not confirmed while writing this document.\n- **`min_reader_schema` has never been raised in anger.** Every migration\n  shipped so far adds an optional field, so the floor has stayed at 1.3\n  since it was introduced. Whether a future migration correctly raises it,\n  rather than leaving it stale the way an old ADR's `maps_to.modules` block\n  went stale (see `docs/architecture/designs/design-mode/assessment.json`,\n  finding F3), is untested because no such migration exists yet.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-family/epic-E3-design.md"}, {"id": "cobuilder-family/E4", "epic_id": "E4", "feature_slug": "cobuilder-family", "title": "Epic Technical Solution Design: E4 — The record index", "body_md": "# Epic Technical Solution Design: E4 — The record index\n\nFeature: cobuilder-family\nEpic ID: plugin-split/E4\n\nThis design was written on 2026-08-25, after this epic's two slices were\nbuilt and accepted. Gate 4b did not run before implementation, so this\ndocument records the design as built. It did not constrain the work. The\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\nderived without a written 4b design in hand.\n\n## Scope and Intent\n\nE4 replaces the two separate projection scripts (`build_adrs.py` and\n`build_designs.py`) with one `shared/build_index.py` that emits every\nentity — ADR, design, epic, context, district, boundary rule, pull request,\nslice, publication — with a stable id, and resolves the joins between them,\nincluding an ADR reaching its pull request through a design and an epic.\nADR-0018 governs this epic.\n\n## Files Touched\n\n- `shared/build_index.py` — the whole collect-and-resolve pipeline, 1163\n  lines. `build_adrs.py` and `build_designs.py` no longer exist anywhere in\n  the tree.\n- `docs/architecture/designs/plugin-split/goal.json` and\n  `docs/plans/cobuilder-family/04-slices.md` — the source data `collect_\n  designs()` and `collect_plans()` read.\n- `.cobuilder-architect/self/data/index.json` and `index.js` — the\n  generated output, checked in as the self-bundle's own projection.\n\n## Types & Signatures\n\nRead directly from `shared/build_index.py`. Selected signatures, in the\norder the pipeline calls them:\n\n```python\ndef collect_adrs(repo: Path) -> tuple[list[dict], dict[str, dict], list[str]]: ...\ndef collect_designs(repo: Path) -> tuple[list[dict], ...]: ...\ndef collect_contexts(repo: Path) -> tuple[list[dict], list[dict], list[str]]: ...\ndef collect_districts(bundle_dir: Path) -> tuple[list[dict], list[str]]: ...\ndef collect_plans(repo: Path) -> tuple[list[dict], list[str]]: ...\ndef collect_pull_requests(bundle_dir: Path) -> tuple[list[dict], list[str]]: ...\ndef collect_publications(bundle_dir: Path) -> tuple[list[dict], list[str]]: ...\n\ndef gh_pr_for_branch(branch: str, warnings: list[str], gh_state: dict) -> int | None: ...\ndef resolve_epic_pull_requests(...) -> tuple[dict[str, int], dict[str, str]]: ...\ndef refine_epic_status(epic_status: dict[str, str], epic_to_pr: dict[str, int],\n                        pull_requests: list[dict]) -> None: ...\ndef adrs_reaching_design(adr_id: str, adr_record: dict,\n                          design_records: dict[str, dict]) -> list[str]: ...\ndef resolve_adr_to_pull_request(...) -> ...\ndef resolve_slice_to_epic(...) -> ...\ndef resolve_context_district_joins(...) -> ...\ndef resolve_joins(...) -> dict: ...\n\ndef hash_tree(path: Path) -> str: ...\ndef git_head(repo: Path) -> str | None: ...\ndef compute_sources(repo: Path) -> dict: ...\ndef is_stale(index: dict, repo: Path) -> bool: ...\ndef build_index(repo: Path, bundle_dir: Path) -> tuple[dict, dict, dict, list[str]]: ...\n```\n\n`collect_pull_requests(bundle_dir: Path)` reads `data/story.json`:\n\n```python\ndef collect_pull_requests(bundle_dir: Path) -> tuple[list[dict], list[str]]:\n    story_path = bundle_dir / \"data\" / \"story.json\"\n    ...\n    timeline = raw.get(\"timeline\")\n    ...\n    for entry in timeline:\n        if not isinstance(entry, dict) or entry.get(\"pr\") is None:\n            continue\n        entities.append({\"id\": entry[\"pr\"], \"title\": entry.get(\"title\"),\n                          \"state\": entry.get(\"status\"), ...})\n    return entities, []\n```\n\n`refine_epic_status` then tries to overwrite the placeholder state:\n\n```python\ndef refine_epic_status(epic_status: dict[str, str], epic_to_pr: dict[str, int],\n                        pull_requests: list[dict]) -> None:\n    \"\"\"Replace the placeholder 'open' status with the real pull request state.\"\"\"\n    state_by_number = {pr[\"id\"]: pr.get(\"state\") for pr in pull_requests}\n    for epic_id, pr_number in epic_to_pr.items():\n        state = state_by_number.get(pr_number)\n        if state:\n            epic_status[epic_id] = str(state).lower()\n```\n\n## Slice Decomposition\n\nPer `04-slices.md`:\n\n1. **Slice 8 — the index holds the entities.** No dependency beyond E3's\n   compatibility gate. `build_index.py` emits every entity, and the two\n   scripts it replaces are gone. Completed, score 1.00.\n2. **Slice 9 — the index resolves the joins.** Depends on slice 8's entity\n   set existing to join against. `adr_to_pull_request`, `slice_to_epic`,\n   `district_uncovered`, and freshness (`is_stale`). Completed, score 1.00.\n\n## Test Plan\n\n`tests/test_build_index.py` carries twenty tests. The ones specific to\neach slice:\n\n- Slice 8: `test_every_entity_type_appears_with_correct_count`,\n  `test_deleting_a_source_document_removes_it_on_rebuild` (proves the index\n  is a full rebuild, not a merge), `test_writes_nothing_into_docs`,\n  `test_calls_compatibility_gate_before_first_write`, and\n  `test_slice_8_entities_still_present_alongside_joins`.\n- Slice 9: `test_adr_reaches_pull_request_direct_path`, `test_adr_reaches_\n  pull_request_through_design_and_epic`, `test_adr_with_no_reachable_\n  design_is_unresolved_not_guessed`, `test_epic_with_no_branch_is_\n  unstarted`, `test_epic_with_branch_but_no_pull_request_is_reported_not_\n  errored`, `test_district_and_context_resolve_both_ways`, `test_uncovered_\n  district_is_listed`, `test_every_slice_declares_an_existing_epic`,\n  `test_a_bare_epic_header_id_is_unresolved_not_guessed`, `test_an_epic_\n  header_naming_an_undeclared_epic_is_unresolved`, `test_all_fourteen_\n  real_slices_resolve_from_the_scoped_id`, `test_changing_an_authored_\n  document_marks_the_index_stale`, `test_moving_the_git_head_marks_the_\n  index_stale`, `test_epic_id_scoped_to_design_no_collision`, and\n  `test_gate_source_scan`.\n\nNo test in the suite asserts the open-pull-request behavior described\nbelow under Risks. `test_epic_with_branch_but_no_pull_request_is_reported_\nnot_errored` covers an epic with no matching pull request at all, which is\na different case from an epic whose pull request exists but is open, not\nmerged.\n\n## Risks & Open Questions\n\n- **An open pull request does not resolve to a real state.**\n  `collect_pull_requests()` reads only `data/story.json`, which holds\n  narrated merged pull requests. An open pull request is never narrated\n  into `story.json`, so it never becomes a `pull_request` entity.\n  `resolve_epic_pull_requests()` still finds the PR number through `gh pr\n  list --head <branch>`, and sets a hardcoded placeholder state of\n  `\"open\"`. `refine_epic_status()` then tries to look that number up in\n  `pull_requests`, finds nothing, and the placeholder survives unchanged.\n  Verified by reading `collect_pull_requests` and `refine_epic_status`\n  together in `shared/build_index.py`. This is the exact behavior the\n  epic's own PR (pull request 11, still open at the time of writing) hits\n  today: `resolve_epic_pull_requests` reports E1 through E6 as `\"open\"` by\n  construction, not because the placeholder was refined against a real\n  state.\n- **Closing this gap needs a decision, not a fix.** Either `collect_pull_\n  requests()` gains a second, `gh`-backed path for a pull request with no\n  narration yet, or the placeholder stays and the index documents it as a\n  known limit. This design does not resolve which.\n- **`hash_tree()` and `is_stale()` were read but not exercised by hand**\n  against a real edit during this write-up. The corresponding tests,\n  `test_changing_an_authored_document_marks_the_index_stale` and `test_\n  moving_the_git_head_marks_the_index_stale`, stand in for that\n  verification.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-family/epic-E4-design.md"}, {"id": "cobuilder-family/E5", "epic_id": "E5", "feature_slug": "cobuilder-family", "title": "Epic Technical Solution Design: E5 — One lifecycle surface", "body_md": "# Epic Technical Solution Design: E5 — One lifecycle surface\n\nFeature: cobuilder-family\nEpic ID: plugin-split/E5\n\nThis design was written on 2026-08-25, after this epic's two slices were\nbuilt and accepted. Gate 4b did not run before implementation, so this\ndocument records the design as built. It did not constrain the work. The\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\nderived without a written 4b design in hand.\n\n## Scope and Intent\n\nE5 adds three viewer modes — Decisions, Contexts, and Builds — on top of\nthe index E4 built, adds a computed Backlog lane inside the Builds mode,\nand moves the six generated pages out of `.lavish/` into the bundle's own\n`pages/` directory. ADR-0018 governs the record index and lifecycle surface\nthis epic presents.\n\n## Files Touched\n\n- `plugins/cobuilder-artifact/viewer/index.html` — the five-mode top-left\n  switcher (Designs, Pull requests, Decisions, Contexts, Builds), each\n  mode's render function, and the Backlog lane computation.\n- `scripts/build_builds_view.py` — reads the status document and the index\n  to render the Builds page: `read_epics`, `read_rubrics`, `read_plan`,\n  `current_doc`, `render`, and `main`.\n- `.cobuilder-architect/self/pages/{builds-view.html,decisions-register.html,\n  view-designs.html,artifact-transport-map.html,cobuilder-vocabulary.html,\n  gate1-cobuilder-family.html}` — the six pages, now living in the bundle\n  instead of `.lavish/`. `.lavish/` still holds its own copies as of this\n  writing; `git status --short` does not list `.lavish/*.html` as deleted,\n  so the move added a bundle copy without yet removing the `.lavish/`\n  originals.\n- `docs/architecture/contexts/cobuilder-packaging/{canvas.md,boundary.yaml}`\n  — the one bounded context the Contexts mode renders today.\n\n## Types & Signatures\n\n`scripts/build_builds_view.py`:\n\n```python\ndef read_epics(designs_dir: Path, slices_md: str) -> list[dict]: ...\ndef read_rubrics(rubrics_dir: Path) -> tuple[dict, dict]: ...\ndef read_plan(plan_dir: Path, designs_dir: Path, rubrics_dir: Path) -> dict: ...\ndef current_doc(gates: list[dict]) -> tuple[str, str, bool]: ...\ndef render(page: Path, plan_dir: Path, designs_dir: Path, rubrics_dir: Path) -> None: ...\ndef main() -> None: ...\n```\n\nThe viewer's Builds mode reads `window.INDEX.entities.epic` and\n`window.INDEX.joins` directly, per the in-page label at line 3305 of\n`plugins/cobuilder-artifact/viewer/index.html`: \"The Backlog lane is a\ndynamic query over `window.INDEX.entities.epic` and `window.INDEX.joins`.\nNo authored backlog file feeds it.\" The Backlog lane holds every epic with\na null branch, plus every slice not yet scored, matching\n`03-program-design.md`'s stated rule.\n\n## Slice Decomposition\n\nPer `04-slices.md`:\n\n1. **Slice 10 — the Decisions and Contexts modes.** Depends on E4's index\n   existing to read from. A person browses every decision and every\n   context, with the anchor distinction (a decision an ADR cites versus a\n   decision it merely touches) visible. Completed, score 1.00.\n2. **Slice 11 — the Builds mode and the Backlog lane.** Depends on slice\n   10's mode-switcher scaffold and on E4's `epic_to_pull_request` and\n   `slice_to_epic` joins. Gates driven by the status document, the Backlog\n   lane computed from the index, and pages moved into the bundle.\n   Completed, score 0.92 — the one slice in this feature that scored below\n   1.00, though it is still recorded as completed rather than escalated.\n\n## Test Plan\n\n- `tests/test_viewer_modes.py` — `test_viewer_contains_all_five_mode_\n  buttons`, `test_decisions_mode_lists_all_records_and_anchor_distinction`,\n  `test_contexts_mode_leads_with_violations_and_uncovered_districts`,\n  `test_contexts_mode_renders_boundary_record_as_readable_rules`, and\n  `test_export_artifact_parses_updated_viewer` — this last test guards\n  against the exact regression `CLAUDE.md`'s Publish mode notes describe:\n  an artifact export that cannot parse a viewer carrying new mode markup.\n- No test file name in `tests/` targets `scripts/build_builds_view.py` by\n  name, and no `test_builds_*` or `test_backlog_*` function exists in the\n  suite as collected. The Builds mode and the Backlog lane are exercised\n  indirectly, through `test_viewer_contains_all_five_mode_buttons` and the\n  index tests in `tests/test_build_index.py` that the lane's query depends\n  on, but neither a dedicated unit test nor an assertion on `read_plan`'s\n  or `current_doc`'s return shape was found.\n\n## Risks & Open Questions\n\n- **`scripts/build_builds_view.py` carries no direct test coverage.**\n  This is a genuine gap, not a design choice recorded anywhere. A change to\n  `current_doc`'s gate-ordering logic or to `read_rubrics`'s score parsing\n  could regress silently until a person looks at the rendered page.\n- **`.lavish/` still holds the six pages the move was meant to retire.**\n  `git status --short` shows no deletion for any `.lavish/*.html` file, so\n  either the removal step of slice 11 has not run, or it ran in a commit\n  this design could not locate. The bundle's `pages/` directory and\n  `.lavish/` currently disagree about which copy is the source of record.\n- **The 0.92 score on slice 11 has no recorded reason in `00-status.md`'s\n  Escalated section**, because the slice was accepted rather than\n  escalated. What specifically fell short of 1.00 was not found in any\n  file this design reviewed.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-family/epic-E5-design.md"}, {"id": "cobuilder-family/E6", "epic_id": "E6", "feature_slug": "cobuilder-family", "title": "Epic Technical Solution Design: E6 — The reply channel", "body_md": "# Epic Technical Solution Design: E6 — The reply channel\n\nFeature: cobuilder-family\nEpic ID: plugin-split/E6\n\nThis design was written on 2026-08-25, after this epic's three slices were\nbuilt and accepted. Gate 4b did not run before implementation, so this\ndocument records the design as built. It did not constrain the work. The\nrubrics for this epic in `.cobuilder/rubrics/cobuilder-family/` were\nderived without a written 4b design in hand.\n\n## Scope and Intent\n\nE6 adds an append-only comment ledger and its projection, an anchor\ncomputed from the live DOM so a reader can click any element and attach a\ncomment to it, a `POST /feedback` write endpoint, and a background wake\ncommand an agent polls for new threads. ADR-0019 governs the ledger's\nrecord shape.\n\n## Files Touched\n\n- `shared/ledger.py` — the whole ledger: append, fold, project, and the\n  blocking watch function. 260-plus lines.\n- `plugins/cobuilder-artifact/scripts/serve_bundle.py` — extends\n  `http.server.SimpleHTTPRequestHandler` with a `POST /feedback` route,\n  binds `127.0.0.1` only, and returns 403 when `--allow-write` is off.\n- `plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py` — the\n  background wake command. This is a placement difference from\n  `03-program-design.md`, which names `scripts/watch_feedback.py` under\n  `cobuilder-artifact`. The shipped file lives under\n  `cobuilder-full-lifecycle` instead.\n- `plugins/cobuilder-artifact/viewer/index.html` — `computeSelector()`,\n  `captureRange()`, and `openDrawer()`, the client side of the anchor and\n  comment flow the program design's call stack section names.\n\n## Types & Signatures\n\nRead directly from `shared/ledger.py`:\n\n```python\nclass LedgerPaths: ...\n\ndef append_line(path: Path, obj: dict[str, Any]) -> None: ...\ndef read_ledger(path: Path) -> list[dict[str, Any]]: ...\ndef fold_threads(lines: list[dict[str, Any]]) -> dict[str, dict[str, Any]]: ...\ndef project_threads(lines: list[dict[str, Any]]) -> dict[str, dict[str, Any]]: ...\ndef write_projection(path: Path, projection: dict[str, dict[str, Any]]) -> None: ...\ndef load_projection(path: Path) -> dict[str, dict[str, Any]]: ...\n\ndef append_comment(paths, anchor: str, text: str, author: str = \"human\") -> str: ...\ndef append_reply(paths, thread_ulid: str, text: str, author: str = \"human\",\n                  replies_to: str | None = None) -> str: ...\ndef append_state(paths, thread_ulid: str, state: str) -> None: ...\n\ndef get_thread(paths: LedgerPaths, thread_ulid: str) -> dict[str, Any] | None: ...\ndef get_all_threads(paths: LedgerPaths) -> dict[str, dict[str, Any]]: ...\ndef rebuild_projection(paths: LedgerPaths) -> dict[str, dict[str, Any]]: ...\n\ndef watch_feedback(ledger_path: Path, since_offset: int = 0,\n                    timeout_seconds: float = 30.0) -> tuple[list[dict[str, Any]], int]:\n    \"\"\"Block until new lines appear past since_offset. Returns\n    (new_lines, new_offset). Times out with ([], since_offset) after\n    timeout_seconds. The caller (watch_feedback.py) exits 2 on an empty\n    result and 0 otherwise.\"\"\"\n```\n\n`plugins/cobuilder-artifact/scripts/serve_bundle.py`:\n\n```python\nclass FeedbackHandler(SimpleHTTPRequestHandler):\n    ledger_paths: LedgerPaths | None = None\n    allow_write: bool = False\n    def do_POST(self) -> None: ...   # only /feedback; 403 when read-only,\n                                       # 400 on missing anchor or text,\n                                       # 201 with {\"ulid\": ..., \"status\": \"appended\"}\n\ndef serve_bundle(bundle_dir: Path, port: int = 0, allow_write: bool = False) -> int: ...\ndef main() -> None: ...\n```\n\n`plugins/cobuilder-full-lifecycle/scripts/watch_feedback.py`'s `main()`\ncalls `watch_feedback(paths.ledger, args.since, args.timeout)`, prints each\nnew line as one JSON object per line, and exits 0 or 2 depending on\nwhether any line came back.\n\n## Slice Decomposition\n\nPer `04-slices.md`:\n\n1. **Slice 12 — the ledger and its projection.** No server involved.\n   Append, fold, and project are file operations, tested without HTTP.\n   Completed, score 0.92.\n2. **Slice 13 — the anchor and the write endpoint.** Depends on slice 12's\n   ledger existing for `serve_bundle.py` to append into. Click to anchor a\n   sentence, `POST /feedback` appends, and the drawer keeps the text in\n   view. Completed, score 1.00.\n3. **Slice 14 — the wake command and the whole loop.** Depends on slices\n   12 and 13. The background command blocks and returns new threads, and\n   the loop runs once end to end. Completed, score 1.00.\n\n## Test Plan\n\n- `tests/test_ledger.py` — twelve tests: `test_append_comment_assigns_\n  ulid_and_returns_it`, `test_append_thread_never_rewrites_an_existing_\n  line`, `test_two_concurrent_appends_both_survive`, `test_state_change_\n  is_a_new_line_not_an_edit`, `test_read_does_not_delete`, `test_thread_\n  view_folds_a_reply_under_its_root`, `test_an_agent_reply_carries_author_\n  agent_and_replies_to`, `test_projection_state_equals_the_last_\n  transition_in_the_ledger`, `test_deleting_the_projection_rebuilds_it_\n  from_the_ledger`, `test_projection_records_who_moved_the_state_and_\n  when`, `test_get_thread_returns_none_for_missing`, and `test_\n  projection_is_disposable_full_rebuild`.\n- `tests/test_server_watch.py` — its tests are methods inside three\n  classes, so a bare `def test` search at the start of a line misses them.\n  Read in full, it carries `TestServer` (`test_post_feedback_appends_to_\n  ledger`, `test_post_feedback_returns_403_when_read_only`, `test_post_\n  feedback_rejects_other_paths`, `test_post_feedback_validates_required_\n  fields`, `test_server_binds_loopback_only`), `TestWatchCommand`\n  (`test_watch_exits_0_on_new_lines`, `test_watch_exits_2_on_timeout`,\n  `test_watch_tracks_offset_per_reader`), and `TestEndToEndLoop`\n  (`test_full_loop_comment_to_reply`, covering comment, wake, agent reply,\n  state change, and the projection in one run). This covers every case\n  `03-program-design.md`'s test plan names for the ledger's server and\n  watch behavior.\n\n## Risks & Open Questions\n\n- **`test_full_loop_comment_to_reply` carries a self-correcting bug in its\n  own body**, not in the code it tests: it first checks\n  `hasattr(sys.modules[__name__], 'get_all_threads')`, which is always\n  false because `get_all_threads` was never imported at module level, then\n  falls through to a local `from ledger import get_all_threads` on the\n  next line and proceeds correctly. The test passes, but the dead\n  `hasattr` branch reads as leftover exploratory code that a review should\n  clean up.\n- **The wake command's placement under `cobuilder-full-lifecycle` rather\n  than `cobuilder-artifact`** was not explained by any commit message or\n  ADR this design reviewed. `cobuilder-artifact` owns `serve_bundle.py`,\n  the other half of the same feature, so the two scripts a full loop needs\n  now live in two different plugins.\n- **`select.select()` on a plain file descriptor, inside `watch_feedback`,\n  is a Linux-and-BSD idiom that does not reliably signal on every\n  filesystem.** The function falls back to a 0.5-second poll when `select`\n  raises `OSError`, so the polling path is exercised whenever the fast\n  path is unavailable, but no test in the suite forces that fallback path\n  specifically.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-family/epic-E6-design.md"}, {"id": "cobuilder-viewer/E19", "epic_id": "E19", "feature_slug": "cobuilder-viewer", "title": "Epic Technical Solution Design: E19 — The Work board", "body_md": "# Epic Technical Solution Design: E19 — The Work board\n\nFeature: cobuilder-viewer\nEpic ID: cobuilder-viewer/E19\n\n## Scope and Intent\n\nE19 is the surface that lists every work item before a reader picks one. The shell had no\nlanding surface. A route that named a work item rendered it. A route that named none\nfell through to the error the shell keeps for an unknown id. A reader arriving on the\nbundle had nowhere to start.\n\n**The board is the landing surface.** The bare route `#/` renders it, and the\nrail's top entry returns to it from any level. The route that names an unknown id keeps\nits own error, because an address that resolved to nothing is not the board. The shipped\nshell owns the bare route, because the prototype's `#/sections-e` address belonged to the\ncomparison harness it sat behind.\n\n**The board is a flat list, not four lanes.** The approved lane rule survives as the\norder. A lane board does not fit this bundle. A superseded design holds no lane, and\nthis bundle holds four of them, so a lane arrangement would need a tab for work every\nreader must still reach. A row also carries more than a card does: a stage, six record\nmarks, and an epic and a slice figure.\n\nThe boundary. The board is not a paged level: it draws no section strip, no pager, and no\nlevel progress bar. It draws no reading-progress strip and no section-link bar either,\nbecause the page is a list of short rows and a progress reading would sit at one value. It\nopens the Work surface and changes nothing inside it. It does not list records: it says\nwhich records an item holds and opens the item.\n\n`plugins/artifact/viewer/src/variations/sections-e/Board.tsx` and its `readiness.ts` are\nthe working reference. Both were built and measured.\n\n## Files Touched\n\n- `plugins/artifact/viewer/src/shell/Board.tsx` — new. The board, ported from\n  the prototype with no change of behaviour.\n- `plugins/artifact/viewer/src/shell/readiness.ts` — new. The six record kinds,\n  the three readiness words, and the one derivation. Moved with the board, because the\n  board is its only reader.\n- `plugins/artifact/viewer/src/shell/Board.test.tsx` — new. The board's own\n  cases.\n- `plugins/artifact/viewer/src/shell/readiness.test.ts` — new. The readiness\n  rule's cases, which need no DOM.\n- `plugins/artifact/viewer/src/shell/App.tsx` — modified. `board = route.workId === null`\n  selects the board, and the pane renders it instead of the unknown-id error. The\n  work-item branch keeps that error.\n- `plugins/artifact/viewer/src/shell/Rail.tsx` — modified. The top entry `Work` opens the\n  board, and it takes `board` and `workCount` props.\n- `plugins/artifact/viewer/src/shell/TopBar.tsx` — modified. The bar states the board\n  rather than a missing work item.\n- `plugins/artifact/viewer/src/shell/model.ts` — modified. `boardHref()` returns the bare\n  route prefix.\n- `plugins/artifact/viewer/src/shell/records.ts` — modified. `MinWork` and the `min_work`\n  field on `goal`, which the goal verdict reads.\n- `plugins/artifact/viewer/src/shell/atoms.tsx` — modified. `TONE_MARK`, `TONE_PILL`,\n  `StateBadge`, and `toneForStage`, which the row's marks and badge read.\n\n## Types & Signatures\n\n```ts\n/* readiness.ts — the six authored records, and how complete each one is. */\nexport type RecordKind =\n  | \"goal\" | \"intent\" | \"narrative\" | \"assessment\" | \"pr-draft\" | \"diagrams\";\n\nexport const RECORD_KINDS: RecordKind[];\nexport const RECORD_LABEL: Record<RecordKind, string>;\n\nexport type Readiness = \"present\" | \"partial\" | \"absent\";\n\nexport interface Verdict {\n  state: Readiness;\n  /** The parts the record is missing. Empty unless the state is `partial`. */\n  missing: string[];\n}\n\nexport type RecordVerdicts = Record<RecordKind, Verdict>;\n\n/** One verdict per kind. A design `designs.js` misses reads absent on all six. */\nexport function recordVerdicts(record: DesignRecord | undefined): RecordVerdicts;\n\n/** How many of the six records exist, whole or in part. */\nexport function presentCount(verdicts: RecordVerdicts): number;\n\n/* Board.tsx — the props, as the component takes them. */\nexport interface BoardProps {\n  /** The resolved rows the record index yields, one per design. */\n  rows: DesignRow[];\n  /** The per-design record map `data/designs.js` carries. */\n  records: DesignRecords;\n  /** False while the index is in flight, so the board states that and nothing else. */\n  ready: boolean;\n  /** The heading the shell moves focus to on a route change. */\n  headingId: string;\n}\n\nexport function Board(props: BoardProps): JSX.Element;\n\n/* The row type, and the records it reads. Both are the shipped data layer. */\nexport interface DesignRow {\n  design: DesignEntity;\n  epics: EpicEntity[];\n  slices: SliceEntity[];\n  /** Epics whose joined status is completed or merged. */\n  done: number;\n  /** Pull requests this design's epics point at, deduplicated and sorted. */\n  pullRequests: number[];\n}\n\nexport type DesignRecord = /* one design's authored records, as designs.js writes them */;\nexport type DesignRecords = Record<string, DesignRecord>;\n\n/* model.ts */\n/** The bare route: the one destination that states no section. */\nexport function boardHref(): string;\n\n/* records.ts */\nexport interface MinWork {\n  derived_from?: string;\n  alternatives_explored?: number;\n  boundary_rules_checked?: boolean;\n  challenge_stage_run?: boolean;\n  note?: string;\n}\n```\n\n**The board reads the shipped data layer, not the variation's scaffolding.** An earlier\ndraft of this design took `works: Map<string, WorkItem>`, and `WorkItem` belongs to\n`variations/sections-e/model.ts`. That is the mockup's own copy, held together for the\nreason `atoms.tsx` gives: every directory under `variations/` stays independently\nremovable. The plan deletes the variations once the shipped surface exists. A shipped\ncomponent that took their type would break on the day the scaffolding went. The board\ntherefore takes `rows: DesignRow[]` from `@/data/types`, and it takes the record map\nbeside them. `src/components/work/WorkBoard.tsx` and `src/components/work/DesignCard.tsx`\nare the pattern, and the board follows it.\nThe board takes no address prop, for the reason the rail takes none: `src/shell/Board.tsx`\nimports `routeHref` from `./model`, so the shell keeps routing and the board keeps\npresentation.\n\n**A row's address names a level the design can fill, not a fixed level.** The board holds a\n`rowSection(row, records)` rule with three cases. `intent` comes first, because Intent is\navailable exactly when `data/designs.js` carries a record for the design, and the record map\nsits beside the rows already. A design that file misses fills no level, so its row names\n`build`, which fills when the row carries an epic. `intent` is the shell's own last resort.\nA single fixed address would be wrong for the second case: the shell would land on a gated\nlevel, redirect away from it, and state the redirect.\n\n**Where the modules live.** The board is a surface of the shell, so its two modules\nlive in `src/shell/`, beside `Sheet.tsx` and `DiagramTiles.tsx`. The approved program\ndesign states the rule: the slices port what the prototype proved into `src/shell/`\n(`03-program-design.md`, Files). `src/components/` holds the reusable primitives and\nthe prototype areas: `ui/`, `smoothui/`, and the older lane board at `work/`.\n\n**The order rule, and its two edges.** `STAGE_ORDER` holds `backlog`, `decided`,\n`approved`, `review`, `implemented`, `superseded`. A superseded design sorts after every\nlive stage, because no work waits on it. A stage outside that vocabulary sorts after even\nsuperseded, so a newer bundle still renders rather than disappearing. Two designs of one\nstage sort by id, so the order is total and stable.\n\n**The six marks, one per kind, in one order.** A mark states the record's name, whether\nit exists, and whether it is whole. A whole record takes a solid mark; a gap takes a\ndashed mark with a shape marker beside it, which survives greyscale. A `partial` verdict\nnames the empty part in its tooltip. One derivation produces every verdict, and every row\nrenders it, so two rows cannot disagree about one record.\n\n**The row carries the item's figures.** The stage comes from `work.design.stage`. The\nrecord count comes from `presentCount`. The epic figure counts epics whose refined state\nis `completed` or `merged`, read from the join and never from the entity. The slice\nfigure counts slices whose state is `completed`, and it hides when the item carries none.\n\n## Slice Decomposition\n\nPer `docs/plans/cobuilder-viewer/04-slices.md`, in build order.\n\n- **Slice 6 — The shell lands on the bundle's designs.** Depends on: slice 4 (the typed\n  model). The bare route renders the board: one row per design, each row stating its\n  stage and its six record marks, and no error state. The board's key renders beside the\n  list, because the three readiness words are new to a reader here.\n- **Slice 7 — A row opens the item's Work surface.** Depends on: slice 6, and slice 8\n  (the section model the opened surface renders). Pressing a row lands on that item's\n  Work surface at its first level, with the route naming the item.\n\nSlice 7 is not slice 6's interaction detail. Two states: a bundle a reader can survey,\nand one item a reader can enter. Slice 6 renders the list with no navigation out of it,\nand slice 7 is the transition the board exists for.\n\n## Test Plan\n\nThe suite is **vitest**, beside the modules it tests: `npm test` runs `vitest run` in\n`plugins/artifact/viewer/`, over `src/shell/Board.test.tsx` and\n`src/shell/readiness.test.ts`. Vitest is already a dependency of this package.\nThe two files sit in the same directory as the code they cover.\n\n`readiness.test.ts` needs no DOM, because the rule is pure. `Board.test.tsx` renders the\ncomponent with `@testing-library/react` inside a `TooltipProvider`, and it reads the rows\nby role.\n\n- **`readiness.test.ts`** — slice 6. `recordVerdicts` and `presentCount` against fixtures\n  written the way `data/designs.js` writes a record: a whole record reads `present`, a\n  record with an empty named part reads `partial` and names it, an absent record reads\n  `absent`, and a design `designs.js` does not carry reads absent on all six.\n- **`Board.test.tsx`** — slice 6. The row count equals the designs it was given, one row\n  per design. Each row states the stage its design records. Each row carries one mark per\n  `RECORD_KINDS` entry, in that order. A design with no record renders six dashed marks\n  and a design with all six renders six solid ones. A `goal.json`-only design's goal mark\n  names the empty part, because the goal records `challenge_stage_run` false. The order\n  follows the lane rule, and a superseded design sorts after every live stage. A stage\n  outside the vocabulary sorts last. The board draws no section strip, no pager bar, no\n  level progress bar, and no reading-progress strip.\n- **The rendered page** — slices 6 and 7. The bare route renders the board and not the\n  unknown-id error. The rail's `Work` entry returns to it. A row's href is that item's\n  route, and a press lands on that item's Work surface at its first level. A sparse design\n  opens. The pane owns the scroll, and the document does not.\n\n**The third group has no runner yet, and that gap is known.** The repository holds no\nbrowser driver. The Python suite under `tests/` checks packaging invariants, and vitest\nruns in jsdom without a served bundle. The Gate 4c rubrics therefore score the page-level\nclaims above, through the ChromeDevTools MCP tools. A later session that adds a browser\nharness should move them there. Their absence from CI is a known gap, not an\noversight.\n\n## Risks & Open Questions\n\n- **Two board implementations exist in the tree.** `src/components/work/WorkBoard.tsx`\n  holds the lane arrangement with cards, taken from the approved static prototype, and\n  `src/shell/Board.tsx` holds the flat list this epic ships. Two boards that\n  disagree about one bundle is a real hazard. Either somebody deletes the lane board when E19\n  lands, or it stays as a named alternative the way `variations/app-shell/` is. This\n  design ships\n  the flat list and leaves the older file in place, and that is a decision a reader should\n  confirm.\n- **The readiness derivation reaches into record shapes.** `narrative.json` wraps its\n  three levels under `levels` in one design and authors them at the top level in others.\n  The rule reads both shapes, because a rule that saw only the flat one reported three\n  levels missing while the text sat one key away. A third shape would need the same\n  treatment.\n- **`challenge_stage_run` is the one goal field that decides `partial`.** A backlog entry\n  records it false, and the verdict names design mode's stages 2 to 7 as the missing part.\n  A design whose goal omits the field entirely reads `present`, which may be generous.\n  Measured on 2026-09-22: of the seven sparse designs, five record the field false and read\n  `partial`, and two record it true and read `present` while no `intent.json`,\n  `narrative.json`, or `assessment.json` exists beside their goal. The rule trusts the\n  recorded field, so the board reports those two as whole. A stricter rule would\n  cross-check the sibling records. That change is not made here.\n- **The board has no filter and no search.** Fifteen rows is a readable list. A bundle\n  with sixty designs would not be, and nothing in this epic says what happens then.\n- **The board states completeness, which no other panel does.** Every panel in this shell\n  states absence and never presence. The board breaks that rule on purpose, because its\n  job is to let a reader compare two items before opening either. A later session that\n  \"fixes\" the board to state absence only would remove the reason it exists.\n- **The three readiness words are new to a reader here.** The key explains them, and the\n  key is a paragraph of prose above the list. If a later change removes the key, the marks\n  become unreadable.\n- **The board's address rule repeats two rules the model owns.** `model.ts` sets\n  `levelsOf`'s `intent.available` to `record !== undefined`, and `gatesOf`'s `build` to\n  `work.epics.length >= 1`. `Board.tsx`'s `rowSection` states both again. They agree today,\n  so no row names a section the shell redirects away from. A later change to either model\n  rule would not reach the board. The board cannot call `levelsOf`, because that function\n  takes a `WorkItem` and the board takes `DesignRow[]`. The fix is to lift the rule into one\n  exported function in `model.ts` that both callers use. It is not made here.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-viewer/epic-E19-design.md"}, {"id": "cobuilder-viewer/E2", "epic_id": "E2", "feature_slug": "cobuilder-viewer", "title": "Epic Technical Solution Design: E2 — The build pipeline", "body_md": "# Epic Technical Solution Design: E2 — The build pipeline\n\nFeature: cobuilder-viewer\nEpic ID: cobuilder-viewer/E2\n\n## Scope and Intent\n\nE2 makes `plugins/artifact/viewer/index.html` a build output. Today a hand-written file\nsits there. `vite.config.ts` writes its build to `../dist` instead, because\nADR-0023's output rule was still under revision when that config landed. E2 settles it:\nthe build writes the committed file, and a test rebuilds it and fails on any difference.\n\nThis epic carries the program's riskiest unknown. A guarded artifact is only a guard if\ntwo builds produce the same bytes, so the question comes first and the pipeline second.\n\nThe boundary. E2 changes how the build produces that file, and nothing about what the file contains.\nIt adds no panel, no route, and no record read. It does not touch the exporter beyond\nthe named markers E1 puts in the output. It does not change the bundle, the record\nindex, or any script outside `plugins/artifact/viewer/`.\n\n## Files Touched\n\n- `plugins/artifact/viewer/vite.config.ts` — modified. `build.outDir` moves from\n  `../dist` to the plugin root, so the build writes `index.html` in place. The\n  single-file plugin and the `@` alias stay as they are.\n- `plugins/artifact/viewer/package.json` — modified. The build script gains the\n  `tsc --noEmit` step it already carries, and a `build:check` script the test calls.\n- `plugins/artifact/viewer/index.html` — modified by every build from here on. It\n  becomes a build artifact, and no hand edit to it survives a rebuild.\n- `plugins/artifact/viewer/src/main.tsx` — modified. It mounts the shell the build\n  emits, and it is where the named markers reach the output.\n- `plugins/artifact/viewer/src/index.html` — modified. The dev entry gains the same\n  marker comments the build passes through, so dev and built output agree.\n- `plugins/artifact/viewer/dist/` — deleted. The scratch output has no reader once the\n  build writes the committed file.\n- `tests/test_viewer_build.py` — new. The byte-equal guard.\n- `plugins/artifact/viewer/package-lock.json` — unchanged, and load-bearing. The\n  byte-equal test compares two builds from this one lockfile.\n- `.nvmrc` or `package.json`'s `engines` field — modified. The Node major pins too, for\n  the same reason the lockfile does.\n\n## Types & Signatures\n\nE2 carries no application type. Its contracts are the build's output path, the marker\nnames, and the Python helpers the guard calls.\n\n```ts\n/* vite.config.ts — the output contract. */\nexport default defineConfig({\n  root: \"src\",\n  build: {\n    outDir: \"..\",          // the plugin root, so the build writes index.html in place\n    emptyOutDir: false,    // never true here: the outDir holds package.json and src/\n    target: \"es2022\",\n  },\n});\n```\n\n```python\n# tests/test_viewer_build.py\nVIEWER = REPO_ROOT / \"plugins\" / \"artifact\" / \"viewer\"\nCOMMITTED = VIEWER / \"index.html\"\n\ndef npm_available() -> bool: ...\ndef run_build() -> bytes:\n    \"\"\"Run the viewer's build and return the bytes it wrote to COMMITTED.\"\"\"\ndef committed_bytes() -> bytes: ...\ndef marker_names() -> list[str]:\n    \"\"\"The names export_artifact.py matches, read from the exporter itself.\"\"\"\n```\n\n**The marker contract.** E1 puts a small set of named markers in the built output:\none in the document head, one where the per-pull-request data block begins, and one\nwhere it ends. The exporter matches those names and never a literal that sits between\nthem. E2 asserts the names survive a build, and it does not own their spelling.\n\n**The determinism contract.** Two runs of `npm ci && npm run build`, on one machine,\nfrom one lockfile, with one Node major, produce the same bytes. That is the property\nslice 2 measures, and slice 3 is the guard that depends on it.\n\n## Slice Decomposition\n\nPer `docs/plans/cobuilder-viewer/04-slices.md`, in build order. E1's slice 1 runs\nfirst, because the markers it adds are what the build carries through.\n\n- **Slice 2 — Two builds produce the same bytes.** Depends on: nothing in this epic.\n  Runs the build twice on the same input and compares. Its end is a measurement: either\n  the bytes match, or the spike reports what varies.\n- **Slice 3 — The build owns the committed file.** Depends on: slice 2, because the\n  guard is meaningless while the output varies. Wires the output path, commits the\n  built file, and adds the test that fails on any difference.\n\nSlice 3 is not slice 2's edge cases. They are two states: a toolchain that can\nreproduce itself, and a pipeline that holds the committed file to it.\n\n## Test Plan\n\n`tests/test_viewer_build.py`, run by `uv run --with pytest pytest tests/ -v`. Every\ncase skips with a clear reason when `npm` or `node_modules` is absent, so a checkout\nwith no Node still passes the suite.\n\n- `test_two_builds_produce_the_same_bytes` — slice 2. Build twice, compare, and report\n  the first differing byte offset when they differ.\n- `test_build_reproduces_the_committed_viewer` — slice 3. Compare a fresh build against\n  `index.html`.\n- `test_editing_the_output_fails_the_test` — slice 3. Write one byte into a temporary\n  copy of the committed file and assert the comparison fails. A guard that cannot fail\n  proves nothing.\n- `test_every_marker_name_survives_a_build` — slice 3. Read the names from\n  `export_artifact.py` and assert each one appears in the built output.\n- `test_out_dir_is_the_plugin_root` — slice 3. Read `vite.config.ts` and assert\n  `build.outDir` resolves to the plugin root, so a later edit cannot quietly restore\n  `../dist`.\n- `test_the_build_does_not_empty_the_plugin_root` — slice 3. Assert `emptyOutDir` is\n  false. A build that empties the plugin root deletes `package.json` and `src/`.\n\n## Risks & Open Questions\n\n- **The output path is the epic's open problem.** `vite.config.ts` writes to `../dist`\n  today, with its own comment saying ADR-0023's output rule is under revision while the\n  shipped file keeps working. E2 moves the output to the commit, and that is a real\n  change to a shipped file: every publish after it rewrites a build artifact. The\n  byte-equal guard is the only thing standing between that and a stale commit, so the\n  guard is a precondition of the move rather than a companion to it.\n- **`emptyOutDir` must be false.** The output directory becomes the plugin root, which\n  holds `package.json`, `src/`, and `node_modules/`. A build that empties its own\n  output directory deletes the project.\n- **Byte-equality across two machines is unproven.** The lockfile pins dependencies and\n  the engines field pins Node. Neither pins the operating system, the file ordering a\n  filesystem reports, or a locale. Slice 2 answers this for one machine. The second\n  machine is a real question and this design does not settle it.\n- **A contributor now needs Node and npm to change the viewer.** The repository has\n  never required either. It is a stated risk in `intent.json`, and no work in this\n  program answers it.\n- **The tests skip when Node is absent.** That keeps the suite runnable. It also\n  means a CI runner without Node passes the guard's cases by not running them. Whoever\n  wires CI must add Node, or the guard is nominal.\n- **The build's dependency on the markers is one-way.** E1 asserts the names exist in\n  the source. E2 asserts they survive the build. If the single-file plugin ever\n  rewrites an HTML comment, both pass and publishing breaks. Slice 2's byte comparison\n  is the only case that would notice.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-viewer/epic-E2-design.md"}, {"id": "cobuilder-viewer/E5", "epic_id": "E5", "feature_slug": "cobuilder-viewer", "title": "Epic Technical Solution Design: E5 — The Work surface", "body_md": "# Epic Technical Solution Design: E5 — The Work surface\n\nFeature: cobuilder-viewer\nEpic ID: cobuilder-viewer/E5\n\n## Scope and Intent\n\nE5 is the Work surface: one work item, four levels that page their sections, and the\npanels that read the bundle's records. It carries the section model ADR-0028 decided,\nand it is the largest epic in the narrowed program at six slices.\n\nThe three claims the epic exists for:\n\n1. **A level is a sequence of sections.** One section is on screen at a time, on a\n   horizontal track, and the box owns the scroll rather than the pane.\n2. **One index drives the strip, the pager bar, and the two arrow keys.** The index is\n   view state, so a step writes no route.\n3. **The progress strip measures the level**, as `(index + within) / count`, where a box\n   with no scroll range counts as fully read.\n\nThe boundary. E5 renders one work item that a reader already chose. It does not own the\nboard that lists them (E19), the FlightDeck surface (E6, E7), the record Sheet's own\nlayout beyond the row that opens it, or any publishing path. It reads the record index\nand derives no join.\n\n`plugins/artifact/viewer/src/variations/sections-e/` is the built reference. The\nprototype holds the model as measured, and the numbers in this design come from it.\n\n## Files Touched\n\n- `plugins/artifact/viewer/src/shell/App.tsx` — new. The boot, the route, the frame,\n  the rail, the top bar, and the pane.\n- `plugins/artifact/viewer/src/shell/Pager.tsx` — new. `SectionStrip`, `SectionStage`,\n  `SectionPager`, `LevelProgress`, `useSectionPaging`, and `withinOf`.\n- `plugins/artifact/viewer/src/shell/model.ts` — new. `buildWorkItems`, `gatesOf`,\n  `levelsOf`, `readRoute`, `routeHref`, `epicGroups`, `boardHref`.\n- `plugins/artifact/viewer/src/shell/hooks.ts` — new. `useIndexLoad`, `useHashRoute`,\n  `useDocumentNoScroll`, `useVisibleWidthCap`, `useCollapsedRail`, `useFocusOnChange`,\n  `useScrollResetOnRoute`, `useSectionAvailability`.\n- `plugins/artifact/viewer/src/shell/panels/Intent.tsx` — new. Why, Done when, Abort if,\n  Out of scope.\n- `plugins/artifact/viewer/src/shell/panels/ProblemSolution.tsx` — new. Problem and\n  solution, Risks, Assessment, Unknowns.\n- `plugins/artifact/viewer/src/shell/panels/Architecture.tsx` — new. Diagrams,\n  Architecture Decisions, Boundaries, Districts and alternatives considered.\n- `plugins/artifact/viewer/src/shell/panels/Build.tsx` — new. The epic runs, the slices,\n  the rubrics.\n- `plugins/artifact/viewer/src/shell/panels/PullRequests.tsx` — new. The envisioned pull\n  request first, then the real ones, then the FlightDeck entry.\n- `plugins/artifact/viewer/src/shell/panels/Shipped.tsx` — new. The stage, the\n  publications, and the missing deploy record.\n- `plugins/artifact/viewer/src/shell/atoms.tsx` — new. `Panel`, `Box`, `Chip`,\n  `TextList`, `StateBadge`, `TONE_PILL`, `TONE_MARK`, and the band and tint classes.\n- `plugins/artifact/viewer/src/shell/DiagramTiles.tsx` — new. The tiles, the dialog, the\n  zoom, and the offline source fallback.\n- `plugins/artifact/viewer/src/shell/Sheet.tsx` — new. The record sheet and its\n  jumplink row.\n- `plugins/artifact/viewer/src/shell/Rail.tsx`, `TopBar.tsx` — new. The traverse and the\n  work item.\n- `plugins/artifact/viewer/src/index.css` — modified. The token set the surface reads.\n- `tests/test_sections.py` — new. The section model's claims against the served viewer.\n\n## Types & Signatures\n\n```ts\n/* Pager.tsx — ADR-0028's shape. */\nexport interface Paging {\n  index: number;\n  count: number;\n  select(index: number): void;\n  previous(): void;\n  next(): void;\n}\n\n/** The active section. It clamps to the list, and a route change resets it to `start`. */\nexport function useSectionPaging(count: number, routeKey: string, start?: number): Paging;\n\n/** How far through one box the reader is. A box with no scroll range counts as read. */\nexport function withinOf(box: HTMLElement | null): number;\n\nexport function SectionStrip(props: {\n  targets: JumpTarget[]; activeIndex: number; onSelect(index: number): void;\n}): JSX.Element;\n\nexport function SectionStage(props: {\n  index: number;\n  activeBoxRef?: RefObject<HTMLDivElement | null>;\n  children: ReactNode;\n}): JSX.Element;\n\nexport function SectionPager(props: {\n  index: number; count: number; onPrevious(): void; onNext(): void;\n}): JSX.Element;\n\n/* model.ts */\nexport type SectionKey =\n  | \"intent\" | \"problem-and-solution\" | \"architecture\"\n  | \"build\" | \"pull-requests\" | \"shipped\";\n\nexport type LevelKey = \"intent\" | \"problem-and-solution\" | \"architecture\";\n\nexport interface Route {\n  workId: string | null; section: SectionKey;\n  sub: string | null; subId: string | null;\n}\n\nexport interface LevelState { key: LevelKey; available: boolean; missing: string[] }\nexport interface Gates { build: boolean; pullRequests: boolean; shipped: boolean; rubrics: boolean }\n\nexport function readRoute(): Route;\nexport function routeHref(workId: string, section: SectionKey, tail?: string): string;\nexport function levelsOf(work: WorkItem): Record<LevelKey, LevelState>;\nexport function gatesOf(work: WorkItem): Gates;\n/** The Build level's sections: the epics in delivery order, six to a section. */\nexport function epicGroups(work: WorkItem): EpicEntity[][];\n\n/* DiagramTiles.tsx */\nexport function loadMermaid(theme: Theme): Promise<MermaidApi>;\nexport function uncap(svg: string): string;\nexport function DiagramTiles(props: {\n  levels: string[]; sources: Record<string, string>; theme: Theme;\n}): JSX.Element;\n\n/* Sheet.tsx */\nexport interface SheetSubject { /* one member per record kind */ }\nexport function useSheetHeadings(scroll: HTMLElement | null, keys: readonly unknown[]): SheetHeading[];\n```\n\n**The section lists, and where each comes from.** Intent holds Diagrams (the bundle's\nfirst diagram level), Why, Done when, Abort if, and Out of scope. Problem & Solution\nholds Problem and solution, Risks, Assessment, and Unknowns. Architecture holds the\nmechanism diagrams, Architecture Decisions, Boundaries, and Districts and alternatives\nconsidered. Build holds its epic runs. The first three are fixed lists; Build's comes\nfrom `epicGroups`.\n\n**A paged section carries no fold.** Every panel of a paged section renders open, so\n`Panel` receives no `collapsible` prop on these levels.\n\n## Slice Decomposition\n\nPer `docs/plans/cobuilder-viewer/04-slices.md`, in build order.\n\n- **Slice 8 — The section model.** Depends on: slice 7 (E19 opens a work item), slice 4\n  (the typed model), slice 5 (the approved prototype). The track, the strip, the pager,\n  the arrow keys, one box on screen, and no pane scroll.\n- **Slice 9 — The level's progress.** Depends on: slice 8, because the strip measures\n  the box the model puts on screen.\n- **Slice 10 — Every level renders its own sections.** Depends on: slice 8. The six\n  sections' panels, and the envisioned pull request leading Pull requests.\n- **Slice 11 — The diagram tiles open their drawing.** Depends on: slice 10, because the\n  tiles live in Architecture's Diagrams section and Intent's first section.\n- **Slice 12 — A record opens in the Sheet.** Depends on: slice 10, because a Sheet\n  opens from a control inside a panel.\n- **Slice 13 — A goal.json-only design renders.** Depends on: slice 10. Seven designs in\n  this repository carry one record, and each must render every level it can fill.\n\nSlice 9 does not fold into slice 8. They are two states: a model that moves one box, and\na reading that reports the level's position. ADR-0028 states them as two claims.\n\n## Test Plan\n\n`tests/test_sections.py`, run by `uv run --with pytest pytest tests/ -v`. The harness\nserves the bundle rooted at `.cobuilder-architect/self/`, which is the parent of\n`viewer/`, and measures the DOM. The Gate 4c rubrics score the same claims on the built\nsurface, because the repository's suite holds no browser driver today.\n\n**What `tests/test_sections.py` can measure, and what it cannot.** The file cannot\nmeasure the served page. The repository holds no browser driver, and a Python test\nreaches the DOM only through jsdom, which holds no layout engine. jsdom reports zero\nfor `scrollHeight`, for `clientHeight`, and for a bounding rect, and its computed style\ncarries no layout-derived value. So the file measures the tree, the roles, the\nattributes, and the class logic. The geometry claims in this document's test list — a\npane whose scroll height equals its client height, one box's rect inside the pane's, a\ntrack transform of one box width, and a progress reading — need a real browser. Those\nclaims belong to the Gate 4c rubrics, scored through the ChromeDevTools MCP tools, and\n`epic-E19-design.md` states the same rule.\n\n- `test_paged_level_pane_does_not_scroll` — slice 8. On each of the four paged levels,\n  the pane's `scrollHeight - clientHeight` is zero.\n- `test_one_box_is_in_view` — slice 8. One box's rect sits inside the pane's, and no\n  other box's does.\n- `test_strip_count_matches_box_count` — slice 8. The strip's headings equal the boxes,\n  and the pager reads `Section 1 of N` for that N.\n- `test_next_moves_exactly_one_box` — slice 8. The track's transform moves by exactly\n  one box width, forward and back.\n- `test_arrow_keys_step_and_are_guarded` — slice 8. The two arrow keys step, and a press\n  inside an `input`, a `textarea`, or a `select`, or with a modifier held, does not.\n- `test_progress_is_level_progress` — slice 9. The reading is 0 at the first section's\n  top, one count's share higher mid-box, and the full walk never decreases.\n- `test_box_with_no_range_reads_as_read` — slice 9. A section that fits one screen\n  contributes its whole share.\n- `test_every_level_renders_its_own_sections` — slice 10. Each section of each level\n  renders its own panels from the bundle's records.\n- `test_the_envisioned_pull_request_leads` — slice 10. On the Pull requests level, the\n  drafted pull request renders above the real ones.\n- `test_diagram_tile_opens_the_dialog` — slice 11. A press opens the dialog with the\n  drawing; Escape and the backdrop close it.\n- `test_diagram_zoom_steps` — slice 11. The zoom reads 50, 100, and 400 percent, and the\n  drawing's box grows with it.\n- `test_diagram_failure_shows_the_source` — slice 11. With the CDN blocked, the tile\n  states the failure and the dialog shows the authored source.\n- `test_record_opens_in_the_sheet` — slice 12. A press opens the record, and a part link\n  scrolls the sheet and moves focus to that heading.\n- `test_sparse_design_renders` — slice 13. A `goal.json`-only design renders every level\n  it can fill, and the rail gates the rest.\n\n## Risks & Open Questions\n\n- **The DOM claims have no runner in the suite.** The repository's tests are Python and\n  check packaging invariants. The cases above need a served page and a DOM read. Until a\n  driver lands, the Gate 4c rubrics carry the scoring, and the pytest cases are stated\n  for the day a driver arrives.\n- **The diagram runtime loads when the Diagrams section mounts.** It used to load on the\n  reader's press. A tile cannot wait for a press, so the rule changed, and a reader who\n  opens Architecture for the decisions pays for a runtime they never use. A lazy\n  per-tile load is the obvious alternative and this design does not take it.\n- **The section index lives in component state.** A reader cannot share a link to one\n  section of one level. A record inside a section is still shareable, and an epic deep\n  link lands on the run that holds it.\n- **A box that cannot scroll reads as a full bar.** Honest, and it reads as complete on a\n  short section. No work in this epic changes it.\n- **The strip reads the DOM.** `useJumpTargets` observes the pane, so a section that\n  renames itself renames its own link. The cost is a one-frame lag after a section\n  change: the strip holds the previous section's labels until the observer fires. The\n  pager's count comes from the sections array, so a count never disagrees.\n- **Six sections in one slice is a large slice.** Slice 10 crosses every panel the Work\n  surface holds. It is one observable state, and no smaller cut leaves a state a reader\n  can see.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-viewer/epic-E5-design.md"}, {"id": "cobuilder-viewer/E7", "epic_id": "E7", "feature_slug": "cobuilder-viewer", "title": "Epic Technical Solution Design: E7 — FlightDeck, one pull request", "body_md": "# Epic Technical Solution Design: E7 — FlightDeck, one pull request\n\nFeature: cobuilder-viewer\nEpic ID: cobuilder-viewer/E7\n\n## Scope and Intent\n\nE7 rebuilds the current pull request viewer as a React surface, at parity, and adds a\njoins rail. It is the second of the narrowed program's two surfaces: the Work surface\nreads a design, and FlightDeck reads a pull request.\n\n**Parity covers six things, and the epic names them.** Four narration levels, diagrams,\nscene art, narration audio, the diff, and the intent and assessment sheet. The joins rail is the one addition parity does not carry. It lists the decisions, the\ndesign, and the epics that reach that pull request. The shipped viewer answers that\nquestion only by asking a reader to search.\n\nThe boundary. E7 builds single-pull-request mode and nothing else. The multi-pull-request\nmode, the merge-order simulator, the ledger's three state subtypes, the path runner, and\nevery publishing path are deferred with E11 to E18. The mode switch ships with one live\nmode. The two modes are one surface in the design, and the second mode's arrival must\nnot reshape the first.\n\n## Files Touched\n\n- `plugins/artifact/viewer/src/flightdeck/SinglePr.tsx` — new. One pull request, at\n  parity: the four levels, the diagrams, the art, the audio, the diff, and the sheets.\n- `plugins/artifact/viewer/src/flightdeck/Levels.tsx` — new. The four narration levels\n  and their navigation, ported from the shipped viewer's own level reader.\n- `plugins/artifact/viewer/src/flightdeck/Diagrams.tsx` — new. The per-pull-request\n  diagram levels, rendered through `DiagramTiles.tsx`.\n- `plugins/artifact/viewer/src/flightdeck/Art.tsx`, `Audio.tsx` — new. The hero frames\n  at `assets/pr-{N}/level-{L}.png` and the narration at `data/audio/pr{N}_{level}.wav`.\n- `plugins/artifact/viewer/src/flightdeck/Diff.tsx` — new. The diff view, ported as it\n  stands. `intent.json` puts the diff's own rendering out of scope, so the port changes\n  no part of how a diff reads.\n- `plugins/artifact/viewer/src/flightdeck/ModeSwitch.tsx` — new. One surface, two modes.\n  Only the single-pull-request mode is live in this program.\n- `plugins/artifact/viewer/src/flightdeck/JoinsRail.tsx` — new. The decisions, the\n  design, and the epics that reach the pull request.\n- `plugins/artifact/viewer/src/shell/panels/PullRequests.tsx` — modified. Its FlightDeck\n  entry routes into the surface.\n- `plugins/artifact/viewer/src/data/types.ts` — modified. `PullRequest` gains the fields\n  parity needs, and `OpenPullRequest` stays the declared extension.\n- `tests/test_flightdeck_parity.py` — new. The parity cases.\n\n## Types & Signatures\n\n```ts\n/* The pull request the surface reads. The record index holds one per merged or open\n   pull request, and the bundle's own narration files hold the rest. */\nexport interface PullRequest {\n  id: number;\n  title: string;\n  state: \"open\" | \"merged\" | \"closed\";\n  head: string | null;\n}\n\n/** ADR-0027's branded extension. E3 declares it, and this epic consumes it. */\nexport type OpenPullRequest = PullRequest & { status: \"open\" };\n\n/** One narration level of one pull request, as the bundle's story.json holds it. */\nexport interface Level {\n  level: 1 | 2 | 3 | 4;\n  title: string;\n  narration: string;\n  /** Scene art, when the bundle carries it. Level 4 has no diagram. */\n  art: string | null;\n  audio: string | null;\n  diagram: string | null;\n}\n\nexport function levelsOf(story: StoryEntry): Level[];\n\n/** The joins that reach one pull request, read from the index. Never derived here. */\nexport interface ReachingJoins {\n  decisions: AdrEntity[];\n  design: DesignEntity | null;\n  epics: EpicEntity[];\n}\n\nexport function joinsReaching(index: RecordIndex, pr: number): ReachingJoins;\n\n/* JoinsRail.tsx */\nexport function JoinsRail(props: { joins: ReachingJoins; onOpenPr(n: number): void }): JSX.Element;\n\n/* SinglePr.tsx */\nexport function SinglePr(props: {\n  pr: number;\n  story: StoryEntry;\n  index: RecordIndex;\n  onOpenPr(n: number): void;\n}): JSX.Element;\n\n/* ModeSwitch.tsx */\nexport type Mode = \"single\" | \"multi\";\nexport function ModeSwitch(props: { mode: Mode; onMode(mode: Mode): void }): JSX.Element;\n```\n\n**The asset paths are relative, and they stay relative.** The surface requests\n`../assets/pr-{N}/level-{L}.png` and `../data/audio/pr{N}_{level}.wav`, exactly as the\nshipped viewer does, so the same bundle serves both and a published export can rewrite\nthe two maps it already rewrites.\n\n## Slice Decomposition\n\nPer `docs/plans/cobuilder-viewer/04-slices.md`, in build order.\n\n- **Slice 15 — A single pull request at parity.** Depends on: slice 5 (the approved Work\n  prototype, which fixes the shell) and slice 14 (the approved FlightDeck prototype,\n  which fixes the surface's shape). It also depends on slices 8 to 10, the section model\n  the surface sits beside. Its end is a reader opening one pull request and reading all\n  six parts of parity.\n- **Slice 16 — The joins rail.** Depends on: slice 15, because the rail renders on the\n  same page. Its end is that page listing the decisions, the design, and the epics that\n  reach that pull request.\n\nSlice 16 is not slice 15's edge cases. Two states: a pull request that reads as it reads\ntoday, and a pull request that says where it came from.\n\n## Test Plan\n\n`tests/test_flightdeck_parity.py`, run by `uv run --with pytest pytest tests/ -v`. The\nsuite reads the built bundle and the served page, and it asserts against the shipped\nviewer's own output where it can.\n\n- `test_four_levels_render` — slice 15. A pull request in the bundle renders four\n  levels, and each one carries its title and its narration.\n- `test_diagrams_render_for_levels_one_to_three` — slice 15. The three diagram levels\n  render, and level 4 carries none.\n- `test_scene_art_paths_resolve` — slice 15. Every level that records art has a file at\n  `assets/pr-{N}/level-{L}.png` in the bundle.\n- `test_narration_audio_paths_resolve` — slice 15. Every level that records audio has a\n  file at `data/audio/pr{N}_{level}.wav`.\n- `test_diff_renders` — slice 15. The diff renders for a pull request the bundle carries\n  a diff for, and it carries the same file entries the shipped viewer shows.\n- `test_intent_and_assessment_sheet_opens` — slice 15. The sheet opens with the intent\n  and the assessment the pull request records.\n- `test_parity_against_the_shipped_viewer` — slice 15. The six parts of parity are\n  present for one pull request in both readers.\n- `test_joins_rail_lists_what_reaches` — slice 16. The rail lists the decisions linked\n  to the design, the design itself, and the epics whose branch or pull request carries\n  the change.\n- `test_joins_rail_states_an_empty_join` — slice 16. A pull request nothing links to\n  states that, rather than rendering an empty rail.\n- `test_multi_mode_is_not_live` — slice 15. The mode switch offers the second mode and\n  says it is not built, so no reader meets a dead control.\n\n## Risks & Open Questions\n\n- **This epic measures parity against a file E2 changes.** The shipped viewer is the\n  reference, and E2 makes that file a build output. Until E2 lands, the reference is stable; after\n  it, the reference is rebuilt from the React source E7 writes. That circularity is real. The\n  honest reading is that the team measures parity once, before E2's move, and records it.\n- **Scene art and audio are paid assets.** They live in the bundle, they are megabytes,\n  and a published Artifact cannot carry them under its budget. Publishing defers with E16,\n  so this epic carries the paths and not the budget work.\n- **The mode switch offers a mode that is not built.** ADR-0027 makes the two modes one\n  surface, and the second one defers. A switch that offers a dead control is worse than a\n  switch that is absent, so the disabled state has to state why.\n- **The joins rail reads a join the index holds.** `adr_to_pull_request` resolves a\n  decision to a pull request. The rail reads that join rather than deriving it. A decision\n  that reached a pull request through an epic the join does not cover would be absent\n  from the rail, and this design does not widen the join.\n- **Only merged pull requests carry four levels of narration.** An open pull request in\n  this bundle holds fewer records. The surface must state what it lacks rather than render\n  an empty level. The prototype's rule applies: absence is a state, not a gap.\n", "approved_date": null, "source_path": "docs/plans/cobuilder-viewer/epic-E7-design.md"}, {"id": "gate-doc-surfacing/E1", "epic_id": "E1", "feature_slug": "gate-doc-surfacing", "title": "Epic Technical Solution Design: E1 — Index gate docs into data/index.json", "body_md": "# Epic Technical Solution Design: E1 — Index gate docs into data/index.json\n\nFeature: gate-doc-surfacing\nEpic ID: E1\n\n## Scope and Intent\nProject `docs/plans/<slug>/03-program-design.md` and\n`docs/plans/<slug>/epic-<id>-design.md` files into two new\n`data/index.json` entity arrays, `program_design` and `epic_design`, so the\nviewer has something to read. Attach a `doc` reference to the matching\nGate 3 entry in `joins.feature_gates`, and a `design_doc` reference to the\nmatching epic in `entities.epic`. No viewer changes in this epic — that is\nE2.\n\n## Files Touched\n- `shared/build_index.py` — add `PROGRAM_DESIGN_FIELDS`,\n  `EPIC_DESIGN_FIELDS`, `discover_plan_gate_docs()`,\n  `project_program_design()`, `project_epic_design()`; wire the new\n  entities into the top-level `build_index()`/`main()` flow the same way\n  `collect_designs()` already is; extend `resolve_feature_gates()` and the\n  epic entity emission in `collect_designs()` with the new `doc`/`design_doc`\n  references.\n- `tests/test_build_index_gate_docs.py` — new test file.\n\n## Types & Signatures\n\n```python\nPROGRAM_DESIGN_FIELDS: tuple[str, ...] = (\n    \"id\", \"feature_slug\", \"gate\", \"title\", \"body_md\", \"approved_date\", \"source_path\",\n)\nEPIC_DESIGN_FIELDS: tuple[str, ...] = (\n    \"id\", \"epic_id\", \"feature_slug\", \"title\", \"body_md\", \"approved_date\", \"source_path\",\n)\n\ndef discover_plan_gate_docs(repo: Path) -> list[dict]:\n    \"\"\"Walk docs/plans/*/. Return one dict per gate doc found:\n    {\"kind\": \"program\", \"feature_slug\": str, \"path\": Path, \"text\": str}\n    or\n    {\"kind\": \"epic\", \"epic_id\": str, \"feature_slug\": str, \"path\": Path, \"text\": str}.\n    An epic id is parsed from the filename epic-<id>-design.md; a file\n    whose id segment fails to parse is skipped, not raised on — this\n    function never crashes a full index rebuild over one malformed name.\n    \"\"\"\n\ndef project_program_design(feature_slug: str, path: Path, text: str) -> dict:\n    \"\"\"Extract a title (first '# ' heading) and approved_date (from the\n    matching 00-status.md's Gate 3 line, if resolvable) alongside the raw\n    body. Returns a dict filtered through project_fields(..., PROGRAM_DESIGN_FIELDS).\"\"\"\n\ndef project_epic_design(epic_id: str, feature_slug: str, path: Path, text: str) -> dict:\n    \"\"\"Same shape, keyed on epic_id instead of feature_slug.\"\"\"\n```\n\n`id` for a `program_design` entity is the bare `feature_slug` (one Gate 3\ndoc per feature, so no collision risk). `id` for an `epic_design` entity is\nthe bare `epic_id` parsed from its filename — note this is the *unscoped*\nepic id (`E1`), not the `<design>/<epic-id>` scoped id `entities.epic`\nuses, because a technical design file lives under one `docs/plans/<slug>/`\ndirectory already, so `feature_slug` carries the scoping context instead.\n`entities.epic[].design_doc` stores the *scoped* lookup path\n(`epic_design[<feature_slug>/<epic_id>]` is wrong; the correct join is:\nlook up `epic_design` by its own `epic_id` key, disambiguated by also\nmatching `feature_slug`, since two features could each have an `E1`).\nImplementation note: to avoid this exact ambiguity, `epic_design`'s\n`id` field is actually `f\"{feature_slug}/{epic_id}\"`, matching\n`entities.epic`'s own `<design>/<epic-id>` scoping convention. This\nsupersedes the plain `epic_id` framing above — keep them consistent\nduring implementation; the test plan below locks this down.\n\n## Slice Decomposition\n1. **Slice 1 (tracer bullet).** Add the constants and empty-list wiring so\n   `data/index.json` always carries `program_design: []` and\n   `epic_design: []`, even with zero gate docs on disk. No dependency.\n2. **Slice 2 (real parsing).** Implement `discover_plan_gate_docs()`,\n   `project_program_design()`, `project_epic_design()`, and the\n   `feature_gates`/`epic` join attachment. Depends on slice 1's constants.\n3. **Slice 3 (edge cases).** Missing `docs/plans/` entirely, a plan\n   directory with only some gate docs, a malformed epic filename. Depends\n   on slice 2.\n\n## Test Plan\n`tests/test_build_index_gate_docs.py`:\n- `test_project_program_design_reads_title_and_body` — feed a small\n  markdown fixture, assert `title`/`body_md`/`source_path` match.\n- `test_project_epic_design_id_is_scoped_feature_slug_and_epic_id` — locks\n  down the `<feature_slug>/<epic_id>` id convention from the note above.\n- `test_discover_plan_gate_docs_skips_malformed_epic_filename` — a file\n  named `epic-design.md` (no id segment) is skipped, not raised on.\n- `test_discover_plan_gate_docs_no_plans_dir_returns_empty_list` — repo\n  with no `docs/plans/` at all.\n- `test_resolve_feature_gates_attaches_doc_when_program_design_exists`.\n- `test_resolve_feature_gates_gate3_doc_key_absent_when_no_md_file`.\n- `test_build_index_end_to_end_index_json_has_program_design_and_epic_design_keys`\n  — run against this repo's own tree; assert both keys exist, and (once\n  this feature's own gate docs land) that this feature's own entities are\n  among them.\n\n## Risks & Open Questions\n- The id-scoping ambiguity flagged above (`epic_id` alone vs.\n  `<feature_slug>/<epic_id>`) must be resolved consistently between the\n  producer (`project_epic_design`) and the consumer\n  (`entities.epic[].design_doc` lookup in E2) — the test plan pins the\n  chosen shape so E2 cannot silently diverge from it.\n- `approved_date` sourcing from `00-status.md`'s Gate 3/4b lines duplicates\n  what `resolve_feature_gates()` already parses there. Acceptable\n  duplication since the two functions read the same file for different\n  fields, not the same field twice.\n", "approved_date": null, "source_path": "docs/plans/gate-doc-surfacing/epic-E1-design.md"}, {"id": "gate-doc-surfacing/E2", "epic_id": "E2", "feature_slug": "gate-doc-surfacing", "title": "Epic Technical Solution Design: E2 — Surface gate docs in the Builds view", "body_md": "# Epic Technical Solution Design: E2 — Surface gate docs in the Builds view\n\nFeature: gate-doc-surfacing\nEpic ID: E2\n\n## Scope and Intent\nMake the Builds view's Gate Rail cards and epic design chips clickable,\nopening a read-only sheet that shows a `program_design` or `epic_design`\nentity's content. Depends on E1 shipping the entities first — this epic\nreads `window.INDEX.entities.program_design`/`.epic_design`, both empty\narrays until E1 lands, so E2's tracer bullet is safe to build against a\nfixture even before E1 slice 2 completes.\n\n## Files Touched\n- `plugins/artifact/viewer/index.html` — new sheet markup block (sibling of\n  `#adr-sheet`, ~line 1022), new functions `openGateDocSheet(kind, id)`,\n  `closeGateDocSheet()`, `renderGateDocSheet()`; extend\n  `closeAnyOpenSheet()` (~line 4003) and `renderSheetVisibility()`\n  (~line 3960) for mutual exclusivity; wire click handlers into\n  `renderBuildsMainContent()`'s `gate-rail-grid` (~line 3249) and\n  `renderEpicCard()` (~line 3282).\n\n## Types & Signatures\n\n```javascript\n// state additions, alongside existing state.adrOpen / state.activeAdrId\nstate.gateDocOpen = false;\nstate.activeGateDocKind = null; // 'program' | 'epic'\nstate.activeGateDocId = null;   // matches program_design/epic_design id\n\nfunction openGateDocSheet(kind, id){\n  if (state.commentsOpen) state.commentsOpen = false;\n  if (state.adrOpen) state.adrOpen = false;\n  if (state.assessmentOpen) state.assessmentOpen = false;\n  if (state.audioDialogOpen) stopAudio();\n  state.gateDocOpen = true;\n  state.activeGateDocKind = kind;\n  state.activeGateDocId = id;\n  renderGateDocSheet();\n  renderSheetVisibility();\n}\nfunction closeGateDocSheet(){\n  state.gateDocOpen = false;\n  renderSheetVisibility();\n}\nfunction renderGateDocSheet(){\n  const index = window.INDEX || {};\n  const entities = index.entities || {};\n  const table = state.activeGateDocKind === 'program'\n    ? (entities.program_design || {})\n    : (entities.epic_design || {});\n  const record = table[state.activeGateDocId];\n  // ... title/meta/body, reusing renderMarkdown() for record.body_md,\n  // and the \"record not captured\" empty-card fallback openAdrSheet uses\n  // when id/record is missing.\n}\n```\n\n**Resolved during Gate 4b:** every existing `window.INDEX.entities.<type>`\nis array-shaped (confirmed against `entities.adr`/`entities.epic` in the\ncurrent `data/index.json`), unlike the separate dict-shaped `window.ADRS`.\n`program_design`/`epic_design` follow the same array convention as every\nother entity, for consistency. `renderGateDocSheet` looks a record up with\n`(entities.program_design || []).find(r => r.id === id)` (or the\n`epic_design` equivalent) rather than a dict key access.\n\n## Slice Decomposition\n1. **Slice 4 (tracer bullet).** Sheet markup + `openGateDocSheet`/\n   `closeGateDocSheet`/`renderSheetVisibility` wiring, gate-rail-grid card\n   click handler. Renders title only, empty body — proves the plumbing\n   without depending on E1's real content.\n2. **Slice 5 (real content).** `renderGateDocSheet` renders `body_md` via\n   `renderMarkdown()`. Epic card design-doc chip added to `renderEpicCard`.\n   Depends on E1 slice 2 (real entities) for a meaningful demo, but the\n   render code itself only depends on slice 4's plumbing.\n3. **Slice 6 (edge cases).** No-`doc` gate card stays non-interactive\n   (no click handler attached at all, not a handler that no-ops); mutual\n   exclusivity with `#adr-sheet`/`#assessment-sheet`/comments drawer/audio\n   dialog, both directions.\n\n## Test Plan\nNo JS test harness exists in this repo for the viewer (it is one static\nHTML file with no build step, per ADR-0001/ADR-0020). Verification is\nmanual, via the `run` skill or a direct `python3 -m http.server` serve of\n`.cobuilder-architect/self/`, exercising:\n- a Gate Rail card with a `doc` opens the sheet and shows body content\n  (slice 4+5)\n- a Gate Rail card with no `doc` has no click affordance (slice 6)\n- opening the gate-doc sheet while the ADR sheet is open closes the ADR\n  sheet, and vice versa (slice 6)\n- an epic card with `design_doc` shows a chip; clicking it opens the sheet\n  scoped to that epic's doc (slice 5)\n\n## Risks & Open Questions\n- No JS test harness means slices 4-6 lean on manual dogfood verification\n  more than the repo's usual pytest-backed rigor. Rubric criteria for this\n  epic's slices are written as concrete manual-check steps, not automated\n  assertions, for this reason.\n", "approved_date": null, "source_path": "docs/plans/gate-doc-surfacing/epic-E2-design.md"}, {"id": "gate-state-resolution/E1", "epic_id": "E1", "feature_slug": "gate-state-resolution", "title": "Epic E1 design: Two renderers read one gate state", "body_md": "# Epic E1 design: Two renderers read one gate state\n\n## Scope and Intent\n\nThis epic repairs the two Builds views. Both read the same gate line, and both\nresolve it wrongly.\n\nThe Builds view ships twice. The viewer edition is one page in the artifact\nplugin. The static edition is generated from a plan and served on its own. A\nperson who opens either page must read the same three answers: which gates are\nclosed, which gate waits, and which document belongs to a gate.\n\nOne predicate carries the meaning of a gate state. `is_resolved(state)` holds\nthe test, so one place decides that an `n/a` gate is closed. The static page\ncalls it through `current_doc()`. The viewer rail tests the state prefix in\nits own template.\n\nThe epic adds no gate, no document type, and no schema field. It changes what\ntwo renderers print.\n\n## Files Touched\n\n| File | Change |\n|---|---|\n| `plugins/artifact/scripts/build_builds_view.py` | Add `is_resolved()`. Give `current_doc()` the filtered document map, and let it return no document. Name a title and an ask note for the Gate 2b document. Rename the summary clause. |\n| `.cobuilder-architect/self/pages/builds-view.html` | Add the `na` gate treatment, guard the crumb, key the prompt on the gate, and state when a gate holds no document. |\n| `plugins/artifact/viewer/index.html` | Add the `is-na` card class, the `na` pill class, and the words \"Not applicable\". Count approved gates in the header. |\n| `tests/test_build_builds_view.py` | Cover the four shapes of `current_doc()`, the predicate, and the summary clause. |\n| `tests/test_viewer_modes.py` | Cover the `n/a` treatment and the header count. |\n\nNo file outside those five changes. `shared/build_index.py` already projects a\ngate state, a document, and a document kind.\n\n## Types & Signatures\n\n```python\ndef is_resolved(state: str) -> bool\ndef current_doc(\n    gates: list[dict], present: dict[str, list[str]]\n) -> tuple[str, str | None, bool]\n```\n\n```javascript\n// .cobuilder-architect/self/pages/builds-view.html\nfunction cls(g)                        // g.state gains the 'na' branch\nfunction go(gate, doc)                 // doc may be null\nfunction buildRail()                   // TITLE[f] may fall back to the file name\n```\n\n```javascript\n// plugins/artifact/viewer/index.html\nconst isNa = /^n\\/a/i.test((g.state || '').trim());\nconst statusClass = ...\nconst pillClass = ...\nconst approvedGates = ...\n```\n\n## Slice Decomposition\n\nSlice 1 is the tracer bullet. It walks one document from the generator to the\nscreen. It proves the whole path: the parser, the filter, the fence rewrite,\nand the render. It ends on the static page for a plan that holds only\n`00-status.md` and `interaction-design.md`.\n\nSlice 2 repeats the shape in the second renderer. The viewer reads the\nprojected records and not the generator, so the work is a template change and\na count.\n\nThe two slices share no file, so one agent holds each of them. Slice 2 does\nnot wait for slice 1.\n\n## Test Plan\n\n| Level | What it proves |\n|---|---|\n| Unit | `current_doc()` returns a document the plan holds, or `None`. `is_resolved()` accepts `APPROVED` and `n/a` and rejects `pending`. |\n| Unit | The summary clause reads \"no gate awaiting an answer\", and not \"all gates approved\". |\n| Page | The generator writes `null` for a missing document, and never the text `None`. |\n| Browser | The static page for `skill-collision-fix` opens no absent document. The static page for a plan holding `interaction-design.md` labels the rail button \"Interaction design\". |\n| Browser | The viewer rail describes an `n/a` gate as \"Not applicable\", and its header reads the approved count out of the total. |\n\nEvery browser reading runs on a seeded fixture under `/tmp`. No test writes\nthe tracked bundle.\n\n## Risks & Open Questions\n\n**The rail and the page disagree about \"current\".** The viewer marks the last\ngate as current. The static page opens the first gate that waits. This epic\nchanges neither rule. The difference is recorded for a later decision.\n\n**An `n/a` state is free text.** `00-status.md` allows `n/a` followed by any\nreason. Both renderers test the prefix `n/a` and nothing more. A plan that\nwrites `not applicable` reads as open. The status template asks for the prefix\n`n/a`, and `verify_gate.py` already requires it.\n\n**The `n/a` pill quotes the whole state line.** A reason such as\n`n/a (no UI) — Screens: \"...\"` is long for a pill. This epic keeps the text\nand changes only the treatment. Shortening the text is a separate question.\n\n**Gate 2b for this plan reads `n/a`.** The feature adds no screen, no control,\nand no flow. It changes the labels on an existing screen. A reader who wants a\nfull interaction design for a label change should say so, and the 2b line\nshould then move to `pending`.\n", "approved_date": null, "source_path": "docs/plans/gate-state-resolution/epic-E1-design.md"}, {"id": "interaction-design-gate/E1", "epic_id": "E1", "feature_slug": "interaction-design-gate", "title": "Epic E1 design: The vendored skill", "body_md": "# Epic E1 design: The vendored skill\n\n## Scope and Intent\n\nFork the `design-to-code` skill from the think-with-ai repository into the\nimplement plugin as a three-step skill. Trim it to the part that `implement`\ndoes not already own.\n\n`implement` owns three documents before Gate 3: what the feature is for, how it\nfits, and how it is built. It owns no document that states how the interface\nbehaves. This skill writes that document. It writes nothing else.\n\nThe fork is deliberate and one-way. The two copies are not kept in step. When\nthis copy changes, the think-with-ai copy is updated afterwards.\n\n## Files Touched\n\nEight files travel. Three do not.\n\n| File | Travels | Why |\n|---|---|---|\n| `SKILL.md` | yes, rewritten | Three steps replace five. Wording becomes epic-based. Paths become plan-directory paths. |\n| `references/jsonc-schema.md` | yes | The schema for `ui-spec.jsonc`. It carries the new `interaction` block. |\n| `references/research-workflow.md` | yes | Step 2. Unchanged in substance. |\n| `references/shadcn-patterns.md` | yes | Step 2 component patterns. Unchanged in substance. |\n| `references/implementation-rules.md` | yes, reframed | Its browser checks move from Step 4 to the GREEN and VALIDATE instructions. |\n| `templates/interaction-design.md` | yes | The Gate 2b template. Twelve sections. |\n| `examples/workflow-matrix-interaction-design.md` | yes | The one worked instance of that template. |\n| `examples/voice-dashboard.jsonc` | yes | The one worked instance of the JSONC schema. |\n| `templates/brief-template.md` | no | Serves the product brief, which `01-product.md` replaces. |\n| `templates/prd.md` | no | Serves the product brief, which `01-product.md` replaces. |\n| `examples/workflow-matrix-prd.md` | no | An instance of the deleted PRD template. |\n\n`references/brief-template.md` does not travel because it does not exist. The\nupstream copy is a byte-identical duplicate of `templates/brief-template.md`,\nand it was deleted at source.\n\n### Step map\n\n| Upstream | Vendored | Reason |\n|---|---|---|\n| Step 1 — Image to brief and PRD | removed | Gate 1 `01-product.md` already holds the product intent, and Gate 1 forbids technical detail. |\n| Step 1b — Interaction design specification | Step 1 | This is the gap the change fills. |\n| Step 2 — Research components and patterns | Step 2 | Unchanged. |\n| Step 3 — Generate the JSONC specification | Step 3 | Unchanged, except that interactions now travel into it. |\n| Step 4 — Implement and verify in a browser | removed | The slice loop owns code. RED writes the failing contract, GREEN writes the code, VALIDATE scores it against a blind rubric. A second code path would bypass all four gates. |\n\nThe useful content of Step 4 survives as checks inside `implementation-rules.md`\nand as the browser clause in VALIDATE's criteria.\n\n### Terminology map\n\n| Upstream word | Vendored word |\n|---|---|\n| PRD | `epic-<epic-id>-design.md` (Gate 4b) |\n| Brief | `01-product.md` (Gate 1) |\n| \"Step N approval\" | A gate approval recorded in `00-status.md` |\n| \"Implement from the JSONC\" | The slice loop: RED, GREEN, VALIDATE |\n| \"Output path\" | `docs/plans/<feature-slug>/` |\n| \"Feature\" | \"Epic\", where the epic is the unit of design |\n\n## Types & Signatures\n\nThe skill has no code. Its contract is its file structure and its frontmatter.\n\n```yaml\n---\ntitle: \"design-to-code\"\ndescription: >-\n  Turn an approved design into an interaction design specification and a\n  JSONC component spec. Use when a feature has a front end and its states,\n  transitions, visibility rules, and defaults must be recorded before\n  implementation.\nstatus: active\ntype: skill\nlast_verified: 2026-09-14\n---\n```\n\nTwo artifacts leave the skill:\n\n```jsonc\n// docs/plans/<feature-slug>/interaction-design.md\n// Markdown. Twelve sections. The template in templates/interaction-design.md\n// supplies the skeleton. Eight of the twelve headings are required, because\n// verify_gate.py checks for them.\n```\n\n```jsonc\n// docs/plans/<feature-slug>/ui-spec.jsonc\n{\n  \"components\": [\n    {\n      \"name\": \"string\",\n      \"interaction\": {\n        \"states\": { \"<state>\": \"<classes>\" },\n        \"initial\": \"<state>\",\n        \"timerArming\": { \"<state>\": \"<event>\" }\n      }\n    }\n  ],\n  \"interactions\": {\n    \"stateClasses\": [\"default\", \"hover\", \"focusVisible\", \"pressed\", \"selected\",\n                     \"disabled\", \"loading\", \"empty\", \"error\", \"transient\"],\n    \"transitions\": [{ \"trigger\": \"string\", \"from\": \"string\", \"to\": \"string\",\n                      \"durationToken\": \"string\", \"guard\": \"string\",\n                      \"reset\": \"string\" }],\n    \"visibility\": [{ \"element\": \"string\", \"condition\": \"string\" }],\n    \"defaults\": [{ \"setting\": \"string\", \"value\": \"string\" }],\n    \"timingTokens\": { \"string\": \"string\" },\n    \"layering\": [{ \"surface\": \"string\", \"zIndexToken\": \"string\" }],\n    \"hitTargets\": [{ \"element\": \"string\", \"size\": \"string\" }],\n    \"scrollOwnership\": [{ \"surface\": \"string\", \"owner\": \"string\" }]\n  }\n}\n```\n\n## Slice Decomposition\n\n- **Slice 1** creates the directory, the `SKILL.md` skeleton, and the eight\n  files. It proves the shape: three steps, valid frontmatter, no dangling\n  relative path.\n- **Slice 2** fills the four concepts into the template, adds the matching\n  JSONC keys, and covers the paths that have nothing to design. It proves that\n  the prose and the schema agree, which is what makes the artifact usable at\n  Gate 4c, and that the skill costs nothing when a feature has no front end.\n\nSlice 2 carries the risk. It holds both the content and the negative paths, so\none score now covers both. A half-finished slice scores low without saying which\nhalf failed, and the validator must read the slice's two halves separately.\n\n## Test Plan\n\n`tests/test_design_to_code_skill.py` — new. It reads the tree, so it needs no\nnetwork and no model.\n\n| Check | Method |\n|---|---|\n| Frontmatter parses | `yaml.safe_load` on the frontmatter block |\n| Three steps are named | The overview names three steps and no fourth |\n| Every cited path resolves | Extract relative paths from the Markdown, assert each exists |\n| Deleted files are not cited | Grep for `brief-template`, `templates/prd.md`, `workflow-matrix-prd` |\n| Terminology is epic-based | Grep for `PRD` and for the word `brief` as a document name |\n| Required headings are present | Assert all eight `REQUIRED_INTERACTION_SECTIONS` appear in the template |\n\nThe last check is the important one. It couples the template to the gate check,\nso a later edit to either one fails a test rather than a gate.\n\n`tests/test_pillar_boundaries.py` must also pass. `design-to-code` joins\n`KNOWN_OTHER_SKILLS`, or `assert_no_unscanned_pillar()` fails.\n\n## Risks & Open Questions\n\n- **The fork drifts.** The two copies diverge by design, so a fix applied to one\n  does not reach the other. Accepted. The change that matters runs in one\n  direction: cobuilder-plugin first, think-with-ai afterwards.\n- **The name is close to `architect:design`.** `architect:design` means\n  \"interview an engineer and write `intent.json`\". `implement:design-to-code`\n  means \"turn a supplied design into a specification\". A session could confuse\n  them. Mitigation: the `description` field names a supplied design and a front\n  end, and `architect:design` does not.\n- **Eight files may still be too many.** `references/shadcn-patterns.md` is\n  1254 words about one component library. If a later build shows that Step 2\n  reads it rarely, it should go. Not measured yet.\n- **The template is long.** 2002 words with inline guidance. A filler who\n  ignores the guidance writes a shorter document. The eight required headings\n  are the floor, not the ceiling.\n", "approved_date": null, "source_path": "docs/plans/interaction-design-gate/epic-E1-design.md"}, {"id": "interaction-design-gate/E2", "epic_id": "E2", "feature_slug": "interaction-design-gate", "title": "Epic E2 design: Gate 2b in the build flow", "body_md": "# Epic E2 design: Gate 2b in the build flow\n\n## Scope and Intent\n\nAdd the gate to the build skill. The skill must ask for the design at the right\nmoment, record the answer in one place, and hand both artifacts to the two\nsteps that consume them.\n\nThe gate sits between Gate 2 (Architecture) and Gate 3 (Program Design). Three\nreasons fix that position:\n\n1. Gate 1 is headed \"no tech talk\". Its rules forbid database details, schemas,\n   and endpoints. A specification that names state classes, timing tokens,\n   pointer events, and scroll ownership cannot live there.\n2. Gate 2's `## Fit` section is where the layers are named in engineering\n   terms. The interaction design needs that answer.\n3. Gate 2b is the last point before Gate 3 names types and signatures. A change\n   after that point costs a rewrite of the program design.\n\nGate 2b is its own tracked line. It is not a sub-step of Gate 2. Gate 2\napproves the architecture, then Gate 2b runs or records `n/a`. Each line gets\none approval, and the reason for the position survives.\n\n## Files Touched\n\n| File | Change |\n|---|---|\n| `plugins/implement/skills/build/SKILL.md` | A `## Gate 2b` section after the Gate 2 section. A `2b` line in the `00-status.md` template. The resume rule names 2b. |\n| `plugins/implement/skills/build/references/rubric-authoring.md` | §4 gains both artifacts as derivation sources. |\n| `plugins/implement/skills/build/references/slice-loop.md` | RED reads both documents. VALIDATE's front-end criteria name a browser check. |\n| `plugins/implement/commands/start.md` | The \"What this writes\" list. |\n| `plugins/implement/.claude-plugin/plugin.json` | `version` 0.1.0 to 0.2.0. |\n\n### The `00-status.md` template line\n\nThe line goes between the Gate 2 line and the Gate 3 line:\n\n```markdown\n- Gate 1 — Product: pending | in progress | APPROVED <date>\n- Gate 2 — Architecture: pending | in progress | APPROVED <date>\n- Gate 2b — Interaction design: pending | in progress | APPROVED <date> | n/a (no UI) — Screens: \"<the ## Screens entry>\"\n- Gate 3 — Program Design: pending | in progress | APPROVED <date>\n- Gate 4 — Slice plan, epic designs, and rubrics: pending | in progress | APPROVED <date>\n```\n\nThe `n/a` form must quote the `## Screens` entry it relied on. `verify_gate.py`\ncannot tell whether a front end exists, so the quoting requirement is the only\ncheck on that answer. The quote makes the reason auditable, because a reviewer\ncompares two documents in seconds.\n\n## Types & Signatures\n\nThe build skill has no code. The change is prose, plus one status line.\n\nThree paths must be written out exactly:\n\n```markdown\n### Path 1 — No front end\nWrite:\n  - Gate 2b — Interaction design: n/a (no UI) — Screens: \"<the entry>\"\nThen continue to Gate 3.\n\n### Path 2 — A front end\n1. Ask the person to supply the design: an image, a set of screenshots, or a\n   link to a rendered page. Do not invent a design.\n2. Invoke implement:design-to-code and run its three steps.\n3. Request approval, then record APPROVED <date> on the 2b line.\nIf the person declines, write n/a (declined) with the reason.\n\n### Path 3 — An earlier approval\nRead 00-status.md. When the 2b line already reads APPROVED or n/a, do not\nrepeat the gate unless the requirements changed.\n```\n\nThe consumption rule for the rubric author, added to `rubric-authoring.md` §4:\n\n```markdown\nA criterion about behaviour cites the interaction design. A criterion about a\nstate, a transition, a visibility rule, or a default cites\ndocs/plans/<slug>/interaction-design.md. A criterion about a class name or a\ntoken cites docs/plans/<slug>/ui-spec.jsonc.\n```\n\nThe consumption rule for the slice loop, added to `slice-loop.md`:\n\n```markdown\nRED reads 03-program-design.md, the epic design, the interaction design, and\nui-spec.jsonc. A front-end slice derives its failing contract from all four.\nA front-end criterion in VALIDATE names a check a browser can make with real\npointer input. A component test that calls .click() is not that check.\n```\n\n## Slice Decomposition\n\n- **Slice 3** writes the section and the status line. It proves the gate is\n  visible to a session that reads `00-status.md`.\n- **Slice 4** writes the three paths, the two consumption rules, and the\n  returned-session rule. It proves the artifact reaches Gate 4c and the slice\n  loop, which is the only thing that makes the gate more than paperwork.\n\nSlice 4 carries the risk. A gate with no consumer gets skipped, however well it\nis documented. That lesson is already recorded in this repository's `CLAUDE.md`\nfor Gate 4b, which ran for zero of five multi-slice epics while nothing\ndownstream required it.\n\n## Test Plan\n\nThe build skill is prose, so most of this epic is verified by reading. Three\nchecks are mechanical.\n\n| Check | Method |\n|---|---|\n| The `2b` line is in the template, between Gate 2 and Gate 3 | `tests/test_gate_2b.py` parses the template block and asserts the order |\n| `commands/start.md` lists both artifacts | Grep for `interaction-design.md` and `ui-spec.jsonc` |\n| `plugin.json` reads 0.2.0 | `tests/test_plugin_manifests.py` already validates the manifest. Extend it, or assert the version. |\n\n`tests/test_commands.py` must keep passing. It requires a matching mode heading\nin a skill for each command, and this change adds no command, so it should be\nuntouched.\n\n## Risks & Open Questions\n\n- **The `n/a` answer is self-reported.** A session that does not want the gate\n  can write `n/a` and move on. The quote requirement makes the lie auditable\n  rather than impossible. Accepted: the alternative is a second opinion on every\n  feature, which costs more than the gate saves.\n- **A session may run the gate after Gate 3 has started.** The skill states the\n  order. Nothing enforces it mechanically, because `verify_gate.py` reads\n  documents rather than a clock. Accepted.\n- **The three-step skill may still be too long for a small feature.** A feature\n  with one screen and one state carries a twelve-section document. Open\n  question: should the skill allow a short form? Not decided. The first few\n  builds should settle it.\n- **Step 2 needs two optional MCP tools.** A session without them skips the\n  lookups. The skill must say so, or the step reads as blocked.\n", "approved_date": null, "source_path": "docs/plans/interaction-design-gate/epic-E2-design.md"}, {"id": "interaction-design-gate/E3", "epic_id": "E3", "feature_slug": "interaction-design-gate", "title": "Epic E3 design: The mechanical consumer and the projection", "body_md": "# Epic E3 design: The mechanical consumer and the projection\n\n## Scope and Intent\n\nTwo jobs, both about the gate being real rather than recorded.\n\n1. `verify_gate.py` checks the gate. A 2b line that reads pending fails the\n   script, so a session cannot reach a slice while the design is missing.\n2. The Builds view shows the document. A reviewer reads the interaction design\n   the same way as the other gate documents, without opening the file system.\n\nThis epic is separable. If the reviewer wants a smaller change, this is the\nepic to cut. Gate 2b still works without it, because `verify_gate.py` needs no\nviewer and the rubric author reads a file. The cost of cutting it is that a\nreviewer must open `docs/plans/<slug>/interaction-design.md` by hand.\n\nThis epic is also larger than it first looks. Three places assume a gate label\nis a small integer. Each one is named below, with its line number.\n\n## Files Touched\n\n| File | Change |\n|---|---|\n| `plugins/implement/scripts/verify_gate.py` | A `STATUS_2B_RE` regex, `REQUIRED_INTERACTION_SECTIONS`, and three functions: `check_2b_status()`, `check_interaction_sections()`, `check_2b()`. The `--json` payload and the printed table gain a 2b block. The docstring changes. |\n| `shared/build_index.py` | `STATUS_GATE_RE` accepts a letter suffix. A new `discover_interaction_design_docs()` function. A new `interaction_design` key beside `program_design` and `epic_design` (line 1255). |\n| `plugins/artifact/scripts/build_builds_view.py` | `GATE_LINE` accepts a letter suffix. `GATE_DOCS` gains a `\"2b\"` key. `current_doc()` treats an `n/a` gate as done. |\n| `plugins/artifact/viewer/index.html` | Line 3290 stops comparing `g.n` to the literal `4`. |\n| `tests/test_gate_2b.py` | New. Six tests. |\n| `tests/test_build_index_gate_docs.py` | Covers the new entity, following the pattern already used for `program_design`. |\n\n### The three integer-only assumptions\n\n| # | Location | Code today | Why it breaks |\n|---|---|---|---|\n| 1 | `shared/build_index.py:1091` | `if gate[\"n\"] == 3 and feature in program_design_features:` | `n` is an integer, so `\"2b\"` never equals it. The comparison is also untyped, so a string `n` silently stops matching gate 3. |\n| 2 | `plugins/artifact/viewer/index.html:3290` | `const isCurrent = g.n === 4;` | The rail marks gate 4 as current. The literal assumes the last gate is number 4. |\n| 3 | `plugins/artifact/scripts/build_builds_view.py:181` | `if not g[\"state\"].startswith(\"APPROVED\"):` | An `n/a` gate is not approved, so the page opens on a gate that asks for nothing. |\n\nAssumption 3 is the sharpest. It is a live defect for the `n/a` convention\nitself, not only for Gate 2b. If Gate 4b ever reads `n/a` for every epic, the\nsame bug appears today.\n\n## Types & Signatures\n\n```python\n# --- plugins/implement/scripts/verify_gate.py -----------------------------\n\ndef check_2b_status(status_text: str | None) -> str:\n    \"\"\"Return \"ok\", \"n/a\", or \"pending\" for the 2b line.\n\n    No 2b line at all returns \"n/a\". The gate is newer than those status\n    files, so silence means the gate did not exist. This choice keeps\n    docs/plans/cobuilder-family and docs/plans/gate-doc-surfacing passing.\n    A 2b line that is present and unapproved returns \"pending\".\n    \"\"\"\n\n\ndef check_interaction_sections(text: str) -> str:\n    \"\"\"Return \"ok\", or \"incomplete:<missing,...>\". Mirrors\n    check_design_sections(), which Gate 4b already uses.\"\"\"\n\n\ndef check_2b(plan_dir: Path, status_text: str | None) -> dict[str, str]:\n    \"\"\"Return {interaction.file, interaction.sections, ui_spec.file,\n    interaction.approved}.\"\"\"\n\n\n# --- shared/build_index.py ------------------------------------------------\n\nSTATUS_GATE_RE = re.compile(r\"^-\\s*Gate\\s*(\\d+\\w?)\\s*[—–-]\\s*([^:]+):\\s*(.+)$\")\n\n\ndef discover_interaction_design_docs(repo: Path) -> list[dict]:\n    \"\"\"One entity per docs/plans/<slug>/interaction-design.md.\n\n    Keys: feature_slug, gate (\"2b\"), title, state, source_path, body_md.\n    Mirrors discover_plan_gate_docs() under ADR-0022.\n    \"\"\"\n\n\ndef resolve_feature_gates(repo: Path) -> dict[str, list[dict]]:\n    \"\"\"`n` becomes str(gm.group(1)) for every gate, so one type serves the\n    array. Callers that compare a number must compare a string.\"\"\"\n```\n\n```javascript\n// --- plugins/artifact/viewer/index.html ----------------------------------\n// Was: const isCurrent = g.n === 4;\n// The last gate in the array is the current one when every gate is done.\nconst isCurrent = g === gates[gates.length - 1] && g.n !== '2b';\n```\n\n## Slice Decomposition\n\n- **Slice 5** adds the 2b group to `verify_gate.py`. It proves enforcement works\n  before anything depends on the viewer. This slice is the mechanical consumer\n  that the ADR requires.\n- **Slice 6** projects the entity and renders the rail card. It proves a\n  reviewer can read the document in the Builds view.\n- **Slice 7** repairs the three integer-only assumptions. It proves the two\n  historical plans still pass, which is the regression test that matters.\n\nSlice 7 carries the risk. A regex widened to `(\\d+\\w?)` matches more than\n`2b`, so the tests must show that gates 1 to 4 behave exactly as before.\n\n## Test Plan\n\n`tests/test_gate_2b.py` — new. Fixtures in `tmp_path`, no network.\n\n| Test | Assertion |\n|---|---|\n| Missing 2b line | Reports `n/a` and exits 0 |\n| Pending 2b line | Reports `pending` and exits non-zero |\n| Approved, both files, all sections | Reports `ok` and exits 0 |\n| Approved, one section absent | Reports `incomplete:` and exits non-zero |\n| `n/a` line, no artifact | Reports `n/a` and exits 0 |\n| Two historical plans | `cobuilder-family` and `gate-doc-surfacing` both report `Overall: OK` |\n\n`tests/test_build_index_gate_docs.py` gains a case for `interaction_design`,\nfollowing the pattern already there for `program_design`.\n\nThe last row of the first table is the regression test. It runs the script\nagainst the two plans that pass today, so a widened regex cannot quietly break\nthem.\n\n## Risks & Open Questions\n\n- **The 2b label widens two regexes.** `(\\d+)` becomes `(\\d+\\w?)`. A plan with a\n  line such as `- Gate 2b — ...` in prose could match, so the regex stays\n  anchored at the line start. Covered by slice 9.\n- **`n` changes type in `data/index.json`.** This is a breaking change for any\n  consumer outside this repository. The viewer is the only consumer found.\n  Open question: should `n` stay an integer, with the label in a second field?\n  That would keep `feature_gates` stable and add `label`. Not decided, and\n  slice 8 should settle it.\n- **The viewer's landing gate changes.** Adding a 2b gate to a plan that has one\n  changes which gate the page opens on. For plans with no 2b line, nothing\n  changes. Accepted.\n- **`current_doc()` and `n/a` is a pre-existing defect.** Fixing it is worth\n  doing on its own line, because Gate 4b can read `n/a` today. Open question:\n  should that fix ship in this epic, or in its own change?\n- **The Builds view is a static bundle.** The rail reads `data/index.json`, so\n  the document appears only after an index rebuild. Accepted. Every other gate\n  document behaves the same way.\n- **This plan's epic joins resolve.** Epic identity comes from\n  `docs/architecture/designs/<name>/goal.json`. A design record was written by\n  hand on 2026-09-14 at `docs/architecture/designs/interaction-design-gate/`,\n  holding the outcome, the epic registry for E1 to E3, and `stage: decided`.\n  `uv run shared/build_index.py` printed no warnings, and all seven slices joined\n  to their epics.\n\n  The record is the minimum the index reads. Design mode did not run, so its\n  four companion files are absent on purpose. `min_work.note` says so, and\n  ADR-0024 holds the reasoning.\n", "approved_date": null, "source_path": "docs/plans/interaction-design-gate/epic-E3-design.md"}], "interaction_design": [{"feature_slug": "cobuilder-viewer", "gate": "2b", "title": "Work board: Interaction Design Specification", "state": "APPROVED 2026-09-22", "source_path": "docs/plans/cobuilder-viewer/interaction-design.md", "body_md": "# Work board: Interaction Design Specification\n\n**Version:** 2.4\n**Date:** 2026-09-22\n**Author:** design session with bjornslib\n**Product document:** `01-product.md` (not yet written. The product intent currently lives in `docs/architecture/designs/cobuilder-viewer/goal.json`.)\n\nVersion 2.0 replaces the surface-first shape with an application shell and a\nthree-level content hierarchy. The three layout variations built on\n2026-09-18 are inputs to this document, not candidates against it. Their tile\narrangements remain useful inside the scroll pane. Their page-scrolling frame\nis superseded by section 2.1.\n\nVersion 2.1 records five cleanups the engineer asked for after reading the\nfirst build of the shell. The shell stops quoting where a value came from. It\nstops announcing that a record is present. The points of Problem and solution\nbecome striped rows inside one card per column. An absent panel states the\nabsence in two words. Assessment, Risks, and Unknowns start closed. Sections\n1.1, 2.2, 2.3, 3.2, 3.3, 4.1, 6.4, 6.5, 7.2, 10, and 11.2 carry the changes.\n\nVersion 2.2 records what the engineer asked for after reading the second\nbuild. The contract moves to the left of Intent, and the Identity box goes.\nBranch, epic count, and supersedes move to a top-line panel, and the contract\nreads on two lines. Every pull request element comes off a decision row and off\nthe ADR sheet, and every control that navigates becomes a SmoothUI\n`smooth-button`.\n\nThe Architecture level reorders itself around what a reader arrives for. The\nsection-link bar sits at the top of the pane. Sections 2.1, 2.2, 3.2, 3.3, 4.1,\n5.1, 6.1, 6.4, 6.5, 6.8, 8.1, 9.3, 10, 11.1, 11.2, and 11.3 carry the changes.\n\nVersion 2.3 applies the approved Material 3 design scheme to the shell. The\nground moves from a cool grey to a pale teal, and every heading takes a band.\nThe three heading levels stay apart. No hover state may fill with the heading\ncolour, and the engineer set that rule in one sentence. Three surfaces take a\ntilt, and no reading surface does. A reading-progress strip joins the pane's\ntop edge. Sections 3.2, 4.2, 6.1, 8.3, and 11.3 carry the changes.\n\nVersion 2.4 records the section model. A level is a sequence of sections, one\nsection on screen at a time on a horizontal track. **The box owns the scroll,\nnever the pane.** So a paged level's pane does not move at all, and the frame a\nreader sees never shifts. The section strip, the pager bar, and the two arrow\nkeys move one shared index. The strip and the pager derive their labels from the\npanels themselves, so a label cannot disagree with the heading it names. The\nprogress strip measures the level rather than one box, as `(index + within) /\ncount`. Intent, Problem & Solution, Architecture, and Build page; Rubrics does\nnot. A paged level drops its visible heading band and keeps an `sr-only` `h1`,\nso the focus move still lands. The diagram runtime now loads when the Diagrams\nsection mounts, because a tile cannot wait for a press. The envisioned pull\nrequest moves to the Pull requests level, which fills when a work has a drafted\npull request and no longer redirects. Sections 2, 2.3, 11, 11.2, and 11.3 carry\nthe changes, and ADR-0028 records the decision.\n\n---\n\n## 1. Overview\n\nThe viewer is an application, not a document. A reader opens it on one piece of\nwork and moves through that work without losing their place. The shell holds the\nwork item in a fixed top bar, the sections in a fixed left navigation, and every\npiece of content in one scroll pane.\n\nOne piece of work has three faces. **Intent** states why the work exists and what\nwould make it done. **Problem and solution** states what was wrong, what answers\nit, and what the answer costs. **Architecture** states the decisions, the\nboundaries, and the diagrams. A reader moves between the three without leaving\nthe work item.\n\nBuild and pull requests extend the same three faces rather than replacing them.\nA reader in Build still reaches the problem and the architecture, because those\nanswer questions that arrive while building.\n\n### 1.1 Core Design Principles\n\n1. **One work item, held in one place.** The top bar names the current work item\n   and never scrolls away. Every section renders that item.\n2. **The left navigation is the traverse.** Moving from design to build to pull\n   request is one click in a fixed rail, never a trip back to a list.\n3. **The page does not scroll. Only the content does.** The top bar and the left\n   navigation stay in place. One scroll pane owns the rest.\n4. **A section appears when the work reaches it.** A section the work cannot fill\n   is absent, not empty. Build appears when the work has epics. Pull requests\n   appear when a branch or a pull request exists.\n5. **Absence is a state of its own.** A record that does not exist is stated in\n   place, not hidden and not rendered as a blank box. The panel names no file and\n   no field, because a reader cannot act on either.\n6. **The shell renders. It never computes.** Every count, state, and join comes\n   from `data/index.json`.\n7. **The reading surface quotes no source.** A panel prints no field path, no\n   file name, and no join name. A reader who wants an outcome does not need to\n   know that the field is called `goal.outcome`. The rail is the one exception,\n   and section 3.3 states why.\n8. **Presence is never announced. Absence is.** A record that exists renders its\n   own content and no marker. A reader needs to hear only what is not there, so\n   one readiness state survives: absent. It reads `not present`.\n\n---\n\n## 2. Information Architecture\n\n### 2.1 Navigation Structure\n\nThe shell has three fixed regions and one content column.\n\n```\n┌──────────────────────────────────────────────────────────────────┐\n│ TOP BAR (fixed)   [ work item ] [ stage ] [ search ] [ theme ]   │\n├────────────────────┬─────────────────────────────────────────────┤\n│ LEFT NAV (fixed)   │  [ top line: branch · epics · supersedes ]   │\n│                    │                                             │\n│ THE WORK           │  CONTENT COLUMN                              │\n│   Intent           │   a paged level, or a section that stacks    │\n│   Problem & Sol.   │                                             │\n│   Architecture     │                                             │\n│ BUILD              │                                             │\n│   Epics            │                                             │\n│   Rubrics          │                                             │\n│ PULL REQUESTS      │                                             │\n│   This work's      │                                             │\n│   FlightDeck       │                                             │\n│ SHIPPED            │                                             │\n└────────────────────┴─────────────────────────────────────────────┘\n```\n\n**A level renders in one of two modes, and the mode decides what owns the\nscroll.**\n\n**A paged level** lays its sections on a horizontal track. One section is on\nscreen at a time, and the pane does not scroll at all. The frame a reader sees\ntherefore never moves.\n\n```\n┌──────────────────────────────────────────────────────────────────┐\n│ [ top line: branch · epics · supersedes ]                        │\n│ ──────── level progress ──────────────────────────────────────── │\n│ [ Diagrams ] [ Why ] [ Done when ] [ Abort if ] [ Out of scope ] │\n│ ┌──────────────────────────────────────────────────────────────┐ │\n│ │ ONE SECTION AT THE FULL WIDTH OF THE STAGE                   │ │\n│ │ the box owns the scroll, on both axes                        │ │\n│ └──────────────────────────────────────────────────────────────┘ │\n│ [ ‹ Previous ]        Section 1 of 5        [ Next › ]           │\n└──────────────────────────────────────────────────────────────────┘\n```\n\n**A section that stacks** keeps the pane's own scroll. The shell's older shape\nstands. A sticky band holds the progress strip and the section links, and the pane\nbelow it holds every panel of that section.\n\n```\n┌──────────────────────────────────────────────────────────────────┐\n│ [ top line: branch · epics · supersedes ]                        │\n│ ──────── progress over the pane ──────────────────────────────── │\n│ [ section links, sticky, one per panel on the page ]             │\n│ ┌──────────────────────────────────────────────────────────────┐ │\n│ │ THE PANE, the only scrolling region below this band          │ │\n│ └──────────────────────────────────────────────────────────────┘ │\n└──────────────────────────────────────────────────────────────────┘\n```\n\n**Four levels page, and three sections stack.**\n\n| Section | Mode | Sections on the track |\n|---|---|---|\n| Intent | paged | Diagrams, Why, Done when, Abort if, Out of scope |\n| Problem & Solution | paged | Problem and solution, Risks, Assessment, Unknowns |\n| Architecture | paged | Diagrams, Architecture Decisions, Boundaries, Districts and alternatives considered |\n| Build | paged | three runs of six epics, in delivery order |\n| Build / Rubrics | stacks | — |\n| Pull requests | stacks | — |\n| Shipped | stacks | — |\n\n**A section is view state, never a route.** The strip, the pager bar, and the two\narrow keys move one shared index, and a step writes no hash. So a reader pages\nthrough Architecture without the route changing under them. A route that names\none of a section's records still lands on the right section. A deep link to an\nepic lands on the run that holds it, with that epic open.\n\n**The strip and the pager bar hold no list of their own.** Both read the sections\nthe level actually rendered. The pager reads its count from the same array of\nsections the boxes come from. A section that renames itself renames its own link.\nSo no label can disagree with the heading it names.\n\n**The section links are not a fixed region.** On a section that stacks they are\n`sticky` children of the scroll pane, so the pane keeps the only scroll. A link\nmoves the pane to a panel on the page. It never moves the window. A paged level\nhas no section links at all. The strip names the sections there, and nothing\nmoves the pane, because the pane does not scroll. Section 11.3 states why the\nwindow cannot move.\n\n```\nviewer\n└── work/:workId                     the work item, held for every section\n    ├── :section                     intent | problem-and-solution | architecture\n    │                                | build | pull-requests | shipped\n    ├── build/epics\n    ├── build/epics/:epicId          opens one epic, and its slices with it\n    ├── build/rubrics\n    └── pull-requests/:prNumber      one pull request\n```\n\nThe route carries the work item and the section. The nav renders from the route,\nso a deep link lands on the same screen a reader reached by clicking.\n\n**The rail lists sections, never records.** This is the rule that decides every\nfuture argument about what belongs in the rail, and it has two reasons.\n\nThe first is meaning. A slice belongs to one epic and carries no meaning outside\nit, so the rail never lists slices. The same holds for anything that exists only\nas a row inside something else. A design document is different, and it is worth\nstating where the line falls: a document is opened, read, and closed, so it has\nmeaning on its own. It still does not go in the rail, for the second reason.\n\nThe second is size, and it is the stronger one. A program's epic and slice count\nis unbounded. `cobuilder-viewer` carries eighteen epics today, and the corpus\ncarries twenty-nine slices. A rail that lists records grows with the corpus,\noverflows the viewport, and turns a fixed navigation into a scrolling one. The\nrail's job is to say where a reader can go, not to enumerate what is there.\n\nSo the rail carries the three levels and the gated groups, and nothing else. An\nepic's design document, its slices, its rubrics and its pull requests are reached\nfrom the epic's own detail view.\n\n**Section gating.** A section appears when the work can fill it. Sections are\ngated, not disabled, because an empty section teaches nothing and costs a click.\nSection 3.3 states each rule.\n\n**The three levels persist.** Intent, Problem and solution, and Architecture sit\nabove the Build group in the rail, and they stay reachable from every other\nsection. A reader in Build who needs the constraint the architecture states\nclicks it in the same rail, then clicks back.\n\n### 2.2 Data Model\n\nThe shell reads `data/index.json`. It reads `data/designs.js` for record bodies.\n\n| Entity | Key fields read | Source |\n|---|---|---|\n| design | `id`, `name`, `outcome`, `stage` | `entities.design` |\n| epic | `id`, `epic_id`, `design`, `branch`, `pr`, `state`, `note` | `entities.epic` |\n| slice | `id`, `n`, `title`, `ends_with`, `score`, `state`, `attempts`, `feature` | `entities.slice` |\n| adr | `id`, `title`, `state`, plus `maps_to.rule` | `entities.adr` |\n| boundary rule | `id`, `name`, `why` | `entities.boundary_rule` |\n| district | `id`, `label`, `summary` | `entities.district` |\n| pull request | `id`, `title`, `state` | `entities.pull_request` |\n| publication | `id`, `target`, `url` | `entities.publication` |\n| design records | `goal`, `intent`, `narrative`, `assessment`, `pr_draft`, diagrams | `data/designs.js` |\n\n| Join | What it answers |\n|---|---|\n| `epic_status` | the real state of each epic |\n| `epic_to_pull_request` | which pull request carries an epic |\n| `slice_to_epic` | which epic owns a slice |\n| `slice_to_epic_unresolved` | slices that belong to no epic |\n| `adr_to_pull_request`, `adr_to_context`, `adr_to_district` | where a decision lands |\n| `feature_gates` | gate state per feature |\n| `context_verifies_district`, `district_uncovered` | map coverage |\n\n**What each level reads, and which records back it.** A level is available when\nat least one of its records exists. It is disabled only when every one of them is\nabsent. This rule matters because a sparse design still fills a level: `goal.json`\nalone is enough for Intent and for Architecture, so `inflight-record-store` has\none disabled level, not three.\n\n| Level | Records that back it | Fields |\n|---|---|---|\n| Intent | `goal.json` alone is enough. `intent.json` adds depth, and the first diagram level adds the container drawing | `goal.outcome`, `goal.done_when`, `goal.abort_if`, `intent.out_of_scope`, the first entry of `diagrams/` |\n| Problem and solution | `intent.json`, `narrative.json`, or `assessment.json` | `narrative.problem_solution` beats by kind, `intent.problem`, `intent.approach`, `intent.risks`, `intent.unknowns`, `assessment.verdict` and `assessment.findings`, `intent.alternatives` |\n| Architecture | `goal.json`'s `adrs[]`, or the design's `diagrams/` | the linked ADRs with their `maps_to.rule`, `boundary_rule` entities, districts and contexts touched, the diagram levels after the first, and for an epic the technical solution design's `Types & Signatures` |\n\n**The Work surface pages four levels, and the sections come from the records.**\nThe Build level is the one level whose sections are computed rather than listed:\nits epics are cut into runs of six, in the delivery order the epic ids carry, so\na section is one screen and the level's spine stays short. ADR-0028 records the\nchoice and why the epic state could not decide it.\n\n**The first diagram level is the overview, and it belongs to Intent.** That level\nis the container drawing. It names the surfaces and the people who use them, and\nthat answers what this work is and who it is for. The levels after it are\nmechanism, and they belong to Architecture. The cut is positional, so it holds for\na bundle with two levels as well as three.\n\n**The work item's own facts sit above every section, not inside Intent.** The\nshell draws one top line holding the branch, the epic count, and what the work\nsupersedes. Version 2.1 kept those in an Identity panel inside Intent, and\nversion 2.2 dissolved that panel. The work item's name and stage already live\nin the top bar, so the top line does not repeat them. It renders for every\nsection, because a reader in Build asks which branch they are on just as often\nas a reader in Intent.\n\n**What the corpus cannot supply.** The shell names these rather than implying\nthem.\n\n| Missing | Consequence |\n|---|---|\n| A deploy record | the Shipped section reports `stage: implemented` and the publication entities, and states that no deploy record exists |\n| `feature_gates` is empty | the Rubrics section states that no gate record exists, and must not read as a pass |\n| Epics carry four states | the intended six render as a marked prototype ladder beside the real value |\n| All 29 slices are `completed` | the intended seven render the same way |\n| An epic has no `outcome` in the index | the outcome comes from `goal.epics[]`, and the panel states the outcome and no source |\n\n**What the shell never prints.** A panel names the record it read, never the file\nor the field it read it from. `goal.outcome` is `Why`. `goal.done_when` is `Done\nwhen`. The heading carries the meaning, and the path carries none. Seventeen\nsites printed a path in version 2.0, and version 2.1 removed every one.\n\n### 2.3 Declared Defaults\n\n| Setting | Default value | Alternatives | Declared where |\n|---|---|---|---|\n| Theme | **light** | dark | `data-theme` on the root |\n| Theme persistence | none | per reader | not implemented. A published file cannot persist |\n| Section | `intent` | the other sections | route |\n| Work item | none | a design or an epic id | route |\n| Section index on a paged level | **the first section** | any later section | component state, reset by a route change |\n| Section index for an epic deep link | **the run that holds that epic** | the first section | route |\n| Left nav state | expanded | a group collapsed | component state |\n| Rail collapse | expanded above 1200 px | collapsed | viewport |\n| Panel body on a paged level | **open. No fold exists** | — | not configurable |\n| Panel body on a stacking section | open | closed, for a panel that declares it | component state |\n| Diagram runtime | **loads when the Diagrams section mounts** | — | section mount |\n| Diagram tile zoom | **100 %, the drawing fitted to the dialog** | 50 % to 400 %, in steps | component state, per dialog |\n| Record sheet jumplink row | one link per part the record marked | no row, for a record that marks none | the record |\n| State ladder display | real value only | real plus intended | component state |\n| Motion | follows `prefers-reduced-motion` | forced off | media query |\n\n**A paged level has no fold.** Version 2.1 made Assessment, Risks, and Unknowns\nstart closed, and the reason was the stacked page. The pane is 558 px tall at\n1280x633, and a section of four open panels ran to several thousand pixels. On a\npaged level each panel is its own section, so a fold inside it is a fold inside a\nfold. A reader who arrives at a section sees the whole of it. The panels keep\nnothing hidden: an absent panel is still `absent`, which is a stated state rather\nthan a fold.\n\n**Theme is light.** It does not follow the operating-system preference, because\nthe engineer declared light on 2026-09-18.\n\n**The diagram runtime loads when the Diagrams section mounts.** It used to load\non the reader's press and at no other moment, because the Mermaid runtime is\nlarge. A tile cannot wait for a press: the tile is the drawing. So the rule\nbecomes: the runtime loads when the Diagrams section mounts, and at no other\nmoment. Nothing on any other level pays for it. A tile whose render fails states\nthe failure in one line. Its dialog then shows the authored source as text. That\nis the only place the source survives, and no source toggle exists for the\nworking case.\n\n**The progress strip measures the level, not one box.** On a paged level its\nvalue is `(index + within) / count`. `within` is the active box's own scroll\nprogress. A box with no scroll range counts as fully read, so a section that fits\none screen contributes its whole share with no special case. The value never\nresets on a section change, and it never decreases on a forward walk. On a section\nthat stacks, the strip measures the pane's own offset, as it always has.\n\n### 2.4 Presence and Absence\n\nTwo statements about the same record used to render together: the record's own\ncontent, and a pill saying the record was present. The second statement carried\nnothing. Version 2.1 removed the present pill and the partial pill, so one\nreadiness state survives.\n\n| State | Frame | Marker | Word |\n|---|---|---|---|\n| present | a solid card | none | none |\n| absent | a dashed card in the muted surface | a strikethrough circle | not present |\n\nThe partial state is gone rather than renamed. A record that holds some of its\nfields is a record, and the panel renders the fields it holds. A record that\nholds none of them is absent.\n\n---\n\n## 3. States\n\n### 3.1 Screen States\n\n| State | Trigger | What the person sees |\n|---|---|---|\n| Loading | the shell mounts and the index has not resolved | the top bar and the rail render from the route, and the scroll pane shows a skeleton at the real content size. The shell never waits, because the shell does not depend on the index |\n| Empty | the index resolved and holds no work | the scroll pane names the bundle path it read, and the command that writes a bundle |\n| Populated | the index resolved | the rail, the top bar, and the section |\n| Section absent | the work cannot fill the section | the section is gated, so the route redirects to the nearest section the work can fill |\n| Error | the index request failed | the failed path, the reason, and a retry |\n| Partial | the index resolved and a record body failed | the section renders from the index, and the affected panel states its own error. One failed body never blanks a section |\n\n### 3.2 Component States\n\n#### Surface palette\n\nThe ground moved from a cool grey to a pale teal on 2026-09-21, and the approved\narchitecture concept made the change. The engineer's reason is that the page then\nreads as a colour rather than as the absence of one.\n\n| Token | Value | Holds |\n|---|---|---|\n| `--ground` | `#e9f2f3` | the page behind the cards |\n| `--surface` | `#ffffff` | a card. It stays white and is never tinted with the ground |\n| `--surface-2` | `#f4fafb` | the first container step, for a panel's inner block |\n| `--surface-3` | `#dfecee` | the second container step, for a hover on a container |\n| `--line` | `#cfe0e2` | a card's border |\n| `--line-soft` | `#e4eff1` | a rule inside a card |\n\nThe two container steps and the two hairlines carry the same teal as the ground,\nso the hierarchy a reader reads from them survives the move. The dark theme picks\nits own steps rather than reusing these, and its ground keeps the same teal.\n\n#### Heading bands\n\nEvery heading in the shell carries a background, so a heading is a solid object\nrather than a line of larger type. Three levels exist, and each level takes its\nown treatment. The three stay apart on purpose. One treatment for all of them\nwould turn the page into a wall of teal.\n\n| Level | Component | Treatment |\n|---|---|---|\n| 1 | the section heading, the page's own `h1` | a solid `--band` fill, `--band-ink` text, a 16 px radius, and padding of 22 px by 26 px. It runs the pane's full width, above the panels |\n| 2 | every panel heading, the `Panel` component's title row | a solid `--band` fill, `--band-ink` text and icon, a 10 px radius. It sits inset inside its card, so the card's own border and padding show around it |\n| 3 | a sub-heading: the `SubHead` component's `h3`, and the target name on a boundary rule card | a `--tint` bar, `--tint-ink` text, a 6 px radius, and `inline-block`, so the bar hugs the text |\n\n| Token | Value | Holds |\n|---|---|---|\n| `--band` | `#006572` | the fill of levels 1 and 2 |\n| `--band-ink` | `#ffffff` | the text and the icons on that fill. It is 6.8 to 1 against `--band`, which clears WCAG AA |\n| `--tint` | `#cde7ed` | the fill of level 3 |\n| `--tint-ink` | `#10353b` | the text on that fill. It is 10.2 to 1 against `--tint` |\n\nAn absent panel takes the same band as a present one. The heading level belongs\nto the panel, so a panel that states an absence still sits at that level.\n\nThe count beside a panel heading reads `--band-ink` at 75 percent. The chevron\nreads `--band-ink` at 80 percent.\n\n**No hover, focus, selected, or active state fills with `--band`.** The engineer\nset this rule on 2026-09-21, and the sentence is theirs: \"Only the hover-over\ncolour must not be the same as the heading colour.\" A control that turns the\nheading colour on hover reads as a heading, and the reader then cannot tell a\nheading from a thing they can press. Every hover in the shell stays in the light\ntint family instead: `--surface-2`, `--surface-3`, `--accent-wash`, and `--tint`.\n\nA control that already sits on a band cannot take a fill at all. A white wash\nover `--band` mixes back to a colour close to `--band` itself. Such a control\ntakes a 1 px `--band-ink` ring at 35 percent instead, and it brightens its own\nink. The panel disclosure is the one case today.\n\n#### Tilt card\n\nThree surfaces carry a tilt: a boundary rule card, a design card on the Work\nboard, and an epic card in Build. A tilt belongs to an object a reader picks up\nwith the pointer.\n\n**A tilt never belongs to a reading surface.** The striped Problem and Solution\ncards, the decision rows, the diagrams panel, the record sheet, and every block\nof prose a reader reads in place take no tilt. A plane that moves under the\npointer makes text harder to read, and that is the opposite of what the surface\nis for.\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | the card at rest, at its own scale | Yes | Not applicable |\n| Hover | the card follows the pointer, up to a 12 degree rotation on each axis, and rises to 1.03 of its scale | No | Not applicable |\n| Focus-visible | the card takes the same tilt at 35 percent of its range, so a keyboard reader sees the object move | No | Not applicable |\n| Pressed | the card keeps its pointer tilt. The press belongs to the control inside it | No | Held while the pointer is down |\n| Selected | not applicable. A tilt marks an object, not a selection | No | Not applicable |\n| Disabled | not applicable | No | Not applicable |\n| Loading | not applicable | No | Not applicable |\n| Error | not applicable | No | Not applicable |\n| Empty | not applicable | No | Not applicable |\n| Transient | the card returns to rest when the pointer leaves, over the component's own 0.25 s spring | No | Ends when the card settles |\n\nThe tilt is the tilt-without-glare variant. The component holds a `glare`\nproperty, and it renders its radial glare overlay only when that property is\ntrue. The shell passes `false` at every call site, so the overlay never exists.\nA pale overlay would wash out the card's own text on a light ground.\n\nThe tilt respects `prefers-reduced-motion: reduce`. The component attaches no\npointer handler under that setting, so the card never moves.\n\n#### Top bar\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | fixed, one hairline rule beneath, the work item's name and status | Yes | Not applicable |\n| Hover | not applicable. The bar itself is not interactive | No | Not applicable |\n| Focus-visible | not applicable. Its children carry the rings | No | Not applicable |\n| Pressed | not applicable | No | Not applicable |\n| Selected | not applicable | No | Not applicable |\n| Disabled | not applicable | No | Not applicable |\n| Loading | the work item's name renders from the route, so the bar has no loading state | No | Not applicable |\n| Error | the status badge reads `unknown`, and the bar keeps the work item's name | No | Not applicable |\n| Empty | not applicable | No | Not applicable |\n| Transient | the bar never moves. A section change never animates the bar | No | Not applicable |\n\n#### Nav item\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | label in muted ink, no background | Yes, for items in a collapsed group | Not applicable |\n| Hover | background tints toward the surface, label darkens | No | Not applicable |\n| Focus-visible | 2 px ring at a 2 px offset | No | Not applicable |\n| Pressed | background tints further | No | Held while the pointer is down |\n| Selected | accent left rule, tinted background, label in accent ink | Yes, for the current section | Not applicable |\n| Disabled | opacity 45 percent, and the reason in a tooltip. Used for a level whose record is absent, so the reader sees the gap in place | No | Not applicable |\n| Loading | not applicable. The rail renders from the route | No | Not applicable |\n| Error | not applicable | No | Not applicable |\n| Empty | not applicable | No | Not applicable |\n| Transient | a brief highlight when a deep link lands | No | Starts on arrival, resets after 1200 ms |\n\n#### Nav group header\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | small uppercase mono label, chevron collapsed | No. Groups start expanded | Not applicable |\n| Hover | label darkens, chevron shifts 2 px | No | Not applicable |\n| Focus-visible | ring | No | Not applicable |\n| Pressed | label darkens further | No | Held while the pointer is down |\n| Selected | not applicable. A header is a disclosure, not a destination | No | Not applicable |\n| Disabled | not applicable | No | Not applicable |\n| Loading | not applicable | No | Not applicable |\n| Error | not applicable | No | Not applicable |\n| Empty | when every item in the group is gated, the header and the group both go | Yes, for a gated group | Not applicable |\n| Transient | not applicable | No | Not applicable |\n\n#### Work-item switcher\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | the work item's name, with a chevron | Yes | Not applicable |\n| Hover | background tints, chevron rotates 180 degrees over `--dur-fast` | No | Not applicable |\n| Focus-visible | ring | No | Not applicable |\n| Pressed | background tints further | No | Held while the pointer is down |\n| Selected | the open list marks the current item | No | Not applicable |\n| Disabled | not applicable. A reader may always change work item | No | Not applicable |\n| Loading | a skeleton the width of a design name | Yes, while the index resolves | Starts on mount, ends when the index settles |\n| Error | the control keeps the work item's name from the route, and states that no list is available | No | Not applicable |\n| Empty | the list states that the bundle holds no other work | No | Not applicable |\n| Transient | the list closes on `Escape` and on an outside click | No | Not applicable |\n\n#### Status badge\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | a pill carrying a word, a colour, and a shape marker | Yes | Not applicable |\n| Hover | a tooltip glosses the state in one sentence | No | Not applicable |\n| Focus-visible | ring | No | Not applicable |\n| Pressed | not applicable. A badge is not a control | No | Not applicable |\n| Selected | not applicable | No | Not applicable |\n| Disabled | not applicable | No | Not applicable |\n| Loading | not applicable | No | Not applicable |\n| Error | reads `unknown` in the neutral colour when the word is outside the vocabulary, and keeps the raw word in the tooltip | No | Not applicable |\n| Empty | not applicable | No | Not applicable |\n| Transient | not applicable | No | Not applicable |\n\n#### Contract block (Why, Done when, Abort if, Out of scope)\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | two lines on one band. `Why` owns the first line at full width, and `Done when`, `Abort if` and `Out of scope` share the second | Yes | Not applicable |\n| Hover | a list row tints | No | Not applicable |\n| Focus-visible | ring on the row | No | Not applicable |\n| Pressed | not applicable | No | Not applicable |\n| Selected | not applicable | No | Not applicable |\n| Disabled | not applicable | No | Not applicable |\n| Loading | a skeleton band at the block's real height | Yes, while the record resolves | Starts on mount, ends when the record settles |\n| Error | the block states that it could not read the record | No | Not applicable |\n| Empty | `Abort if` is often absent in the corpus. The column states that no abort condition is recorded, rather than rendering blank | No | Not applicable |\n| Transient | not applicable | No | Not applicable |\n\n#### Section-link bar\n\nA bar of one link per panel the page rendered. A link moves the scroll pane to\nthat panel's heading. The bar holds the links and nothing else, so a page whose\npanels are gated away renders no bar at all.\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | a row of links in muted ink, pinned to the pane's top edge | Yes, when the page holds at least one panel | Not applicable |\n| Hover | the link gains a border and a surface tint, and its label darkens | No | Not applicable |\n| Focus-visible | 2 px ring at a 2 px offset | No | Not applicable |\n| Pressed | the pane animates to the target | No | Ends when the pane settles at the target |\n| Selected | not applicable. The bar does not track the reader's offset, because that would cost a scroll listener for no answer | No | Not applicable |\n| Disabled | not applicable. A link is present only while its panel is | No | Not applicable |\n| Loading | not applicable. The bar renders from the panels the page already holds | No | Not applicable |\n| Error | not applicable. A panel that failed still carries a heading, so its link still works | No | Not applicable |\n| Empty | the page holds no panel, so the bar renders nothing | Yes, for a page with no panel | Not applicable |\n| Transient | the bar stays at the pane's top edge while the pane moves under it | No | Not applicable |\n\n#### Level section (Intent, Problem and solution, Architecture)\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | a heading, then its panels | Yes, for the current section | Not applicable |\n| Hover | not applicable at section level | No | Not applicable |\n| Focus-visible | not applicable. Its controls carry the rings | No | Not applicable |\n| Pressed | not applicable | No | Not applicable |\n| Selected | the section is current, marked in the rail | Yes, for the routed section | Not applicable |\n| Disabled | the section's nav item is disabled and names the absent record. The panel itself states the absence in two words | No | Not applicable |\n| Loading | panels render skeletons at their real size | No | Not applicable |\n| Error | the panel that failed states its reason, and the other panels still render | No | Not applicable |\n| Empty | the section's nav item is gated when the work cannot fill it at all | No | Not applicable |\n| Transient | a crossfade on section change | No | Ends when the section settles |\n\n#### Panel disclosure\n\nNo panel starts closed, so no panel carries a disclosure on arrival. The shell\nused to start Assessment, Risks, and Unknowns closed. That is gone: on a paged\nlevel each panel is its own section, so a fold inside a page is a fold inside a\nfold. A reader may still fold a panel, and the folded row keeps its heading and\nits count, so it says what it holds.\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | the band heading row, with the count and no chevron, because every panel arrives open | **Yes, on every panel** | Not applicable |\n| Hover | a 1 px `--band-ink` ring at 35 percent, and the chevron brightens to full `--band-ink`. No fill, because the row already carries the band | No | Not applicable |\n| Focus-visible | a 2 px `--band-ink` ring at a 2 px offset. The teal ring is 1.4 to 1 on the band, so it would not read there | No | Not applicable |\n| Pressed | the ring holds | No | Held while the pointer is down |\n| Selected | not applicable. A disclosure is not a destination | No | Not applicable |\n| Disabled | not applicable. An absent panel renders open and carries no disclosure | No | Not applicable |\n| Loading | not applicable. The panel renders from the record | No | Not applicable |\n| Error | not applicable | No | Not applicable |\n| Empty | an absent panel renders open, so the reader never has to open a panel to learn that a record is missing | No | Not applicable |\n| Transient | height and opacity animate together | No | Ends when the panel settles |\n\n#### Epic row\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | title, state badge, slice count, chevron collapsed | Yes | Not applicable |\n| Hover | row tints, chevron shifts 2 px | No | Not applicable |\n| Focus-visible | ring around the whole row | No | Not applicable |\n| Pressed | row tints further | No | Held while the pointer is down |\n| Selected | accent left rule, tinted background, its detail opens | No | Not applicable |\n| Disabled | not applicable | No | Not applicable |\n| Loading | not applicable. The row renders from the index | No | Not applicable |\n| Error | not applicable | No | Not applicable |\n| Empty | the row states that the epic carries no slices | Yes, when it carries none | Not applicable |\n| Transient | not applicable | No | Not applicable |\n\n#### Slice row\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | number, title, score, attempts, state badge | Yes | Not applicable |\n| Hover | row tints, title underlines | No | Not applicable |\n| Focus-visible | ring around the row | No | Not applicable |\n| Pressed | row tints further | No | Held while the pointer is down |\n| Selected | accent left rule, tinted background, its detail opens | No | Not applicable |\n| Disabled | not applicable | No | Not applicable |\n| Loading | not applicable | No | Not applicable |\n| Error | not applicable | No | Not applicable |\n| Empty | not applicable. A slice always carries its row fields | No | Not applicable |\n| Transient | not applicable | No | Not applicable |\n\n#### Diagram tile and dialog\n\nA tile is one drawing at miniature scale, one for each level the section was\ngiven. A press opens the drawing whole in a modal dialog. The dialog carries zoom\nout, zoom in, reset to fit, and close. The tile holds no source block and no\ncontrol of its own.\n\n| State | Visual treatment | Initial | Timer arming |\n|---|---|---|---|\n| Default | the drawing at miniature scale inside a fixed-height window, with one word naming its kind | **Yes** | Not applicable |\n| Hover | the tile border takes `--line`, and the pointer is a pointer | No | Not applicable |\n| Focus-visible | a 2 px `--ring` ring | No | Not applicable |\n| Pressed | the ring holds | No | Held while the pointer is down |\n| Selected | not applicable. A tile is not a destination | No | Not applicable |\n| Disabled | not applicable | No | Not applicable |\n| Loading | announced as rendering, with a skeleton at the tile's own height | No | **Starts when the Diagrams section mounts, never on a press.** The runtime loads on that mount |\n| Error | the tile states the failure in one line and names where the source is | No | Not applicable |\n| Empty | the section names the absent level | Yes, when a level has no diagram | Not applicable |\n| Transient | the drawing fades in | No | Ends when the render settles |\n\n### 3.3 Visibility Gating\n\nA gated control is absent. A disabled control is visible and inert. The shell\nuses each on purpose. **Sections are gated, because an empty section wastes a\nclick. Levels are disabled, because an absent record is a fact the reader needs\nto see in place.**\n\n| Control | Renders when | Removed when |\n|---|---|---|\n| Build group | the work has at least one epic | the work carries no epics |\n| Pull requests group | `epic_to_pull_request` names a pull request for one of this work's own epics | no epic carries a pull request |\n| Shipped group | `stage` is `implemented`, or a `publication` exists for a pull request this work's own epics carry | neither holds |\n| Rubrics item | `feature_gates` holds a gate record for this feature | the join is empty. The item then states that no gate record exists, because a missing record must not read as a pass |\n| Slice list under an epic | the epic owns at least one slice | the epic owns no slice. The epic row states the count of zero instead |\n| Render control | the diagram has not been rendered | the reader renders it |\n| Empty-state note | the panel's record is absent | the record exists |\n| Panel disclosure | the panel holds a record and declares a closed default | the record is absent, because an absent panel renders open |\n| Unresolved-slices panel | `slice_to_epic_unresolved` holds a slice | the join is empty |\n| Retry | the index request failed | a retry starts |\n| Level nav item | always present. It is disabled when its record is absent, so the gap stays visible | never |\n| Section-link bar | the page holds at least one panel | the page holds none. An empty bar would spend a strip of the pane on nothing |\n| Section link | its panel is on the page | its panel is gated away, or its section did not render it |\n\n**The rail names the absent records, and no other surface does.** A greyed nav\nitem with no reason is the one place a reader is genuinely stuck, so the rail\nkeeps its tooltip and its inline line, and both name the records. Every panel\nstops at the fact. This is an explicit engineer decision, taken on 2026-09-19.\n\n**A decision carries no pull request element at all.** Version 2.1 put the\npull request that carried a decision beside that decision, in the Architecture\nlevel. The engineer removed it in version 2.2. The reason is that the work\nitem's own state already says whether the work reached a pull request. So the\n`carried by PR N` chip is gone. The `no PR` chip is gone, and so is the block\ninside the ADR sheet. No epic row carries a pull request chip either. The\ndecision row keeps the decision's own state badge, and the whole record stays\none press away.\n\nThat removal does not touch the Pull requests group. The group still gates on\n`epic_to_pull_request` for this work's own epics. A pull request the work did\nnot open still does not enter it, and the Shipped gate still holds its rule.\n\n---\n\n## 4. Transitions\n\n### 4.1 Transition Table\n\n| Trigger | Source state | Target state | Timing | Reset condition |\n|---|---|---|---|---|\n| Shell mounts | — | Loading | immediate | the index settles |\n| Index resolves | Loading | Populated | `--dur-base` crossfade | — |\n| Index resolves with no work | Loading | Empty | `--dur-base` crossfade | a bundle is written |\n| Index request fails | Loading | Error | `--dur-instant` | the reader presses retry |\n| Reader selects a section | current section | that section, and the track slides one box | `--dur-base` slide | another section is selected |\n| Reader selects a gated section | current section | redirect to the nearest available section | immediate | — |\n| Reader selects another work item | work A | work B, same section where B fills it | `--dur-slow` crossfade | — |\n| Reader expands a nav group | collapsed | expanded | `--dur-expand` | the reader collapses it |\n| Reader folds a panel | open | folded | `--dur-expand` | the reader unfolds it |\n| Reader opens an epic | collapsed | selected, detail open | `--dur-expand` | the selection clears |\n| Reader selects a slice | none or another | that slice selected | `--dur-base` slide | the selection clears |\n| The Diagrams section mounts | none | rendering | immediate | the render settles or fails |\n| Render settles | rendering | rendered | `--dur-base` fade | — |\n| Render fails | rendering | error | `--dur-instant` | the reader leaves the section and returns |\n| Reader toggles the theme | light or dark | the other | `--dur-fast` icon, then `--dur-base` tokens | the reader toggles back |\n| Reader arrives from a deep link | Loading | Populated, target selected | `--dur-slow`, then a highlight | the highlight elapses |\n| Deep link names work the bundle lacks | Loading | Error naming the id | `--dur-instant` | the reader returns to the rail |\n| A record body fails | Populated | Partial | `--dur-instant` | the body loads |\n| Reader selects a jump-bar link on a stacked level | the pane's current offset | the pane's offset at the target's heading | `--dur-base`, through the pane's own smooth scroll | the reader scrolls, or selects another link |\n\nNo transition here is armed by a timer. Every timing is a duration, not a\ncountdown. The one delayed state, the deep-link highlight, starts on arrival and\nresets when it elapses. The diagram render is the only deferred load, and it\nstarts when its section mounts rather than on a press.\n\nTwo transitions move a region rather than crossfading one, and both name a token.\nA section change slides the track one box. A jump-bar press on a stacked level\nmoves the pane. Under `prefers-reduced-motion: reduce` each resolves to\n`--dur-instant`, so the reader arrives in one frame.\n\n### 4.2 Timing Tokens\n\n| Token | Value | Used for |\n|---|---|---|\n| `--dur-instant` | 0 ms | every error state, so a failure is never animated away from the reader |\n| `--dur-fast` | 150 ms | hover tints, the theme icon, the chevron rotation |\n| `--dur-base` | 200 ms | section slides, the render fade, theme tokens |\n| `--dur-slow` | 250 ms | work-item changes, deep-link arrival |\n| `--dur-expand` | 220 ms | nav groups and epic disclosure |\n| `--dur-highlight` | 1200 ms | the deep-link arrival highlight |\n| `--ease-out` | `cubic-bezier(0.22, 0.75, 0.3, 1)` | every entrance |\n| `--ease-in-out` | `cubic-bezier(0.4, 0, 0.2, 1)` | every reversible toggle |\n\nEvery duration in section 4.1 names a token from this table. Under\n`prefers-reduced-motion: reduce`, every token resolves to `--dur-instant` except\n`--dur-highlight`, which keeps its 1200 ms because it marks a location rather\nthan moving anything.\n\nThe tilt card's return to rest is the one duration outside this table. The\ncomponent ships its own 0.25 s spring with a bounce of 0.1, and the shell does\nnot restate it. Section 3.2's tilt card table states it, and section 4.1 holds\nno row for it, because a pointer-tracked transform is an animation rather than a\nstate change. The hover tints are absent from section 4.1 for the same reason.\n\n---\n\n## 5. Interaction Flows\n\n### 5.1 Open a piece of work and read its intent\n\nEntry condition: the reader opens the viewer on a work item.\n\n1. The shell renders the top bar and the rail from the route. Neither waits for\n   the index.\n2. The index resolves. The rail fills in, and the gated sections appear or stay\n   absent.\n3. The reader lands on Intent, unless the route names another section.\n4. Intent shows the contract: `Why` on its own line, then `Done when`, `Abort\n   if`, and `Out of scope` on the second. The work item's branch, epic count,\n   and supersedes already sit on the top line above the heading.\n\nEdge cases:\n- The work has no abort condition: the column says so.\n- The work is superseded: the top bar says what superseded it.\n- The route names a section the work cannot fill: the shell redirects and states\n  that it did.\n- The route names work the bundle lacks: Error, naming the id.\n\n### 5.2 Move from design to build without losing the work\n\nEntry condition: a reader is in Architecture and wants the epics.\n\n1. The reader clicks Build in the rail. The rail does not move. The top bar does\n   not move.\n2. The scroll pane crossfades to the epic list for the same work item.\n3. The reader opens an epic. Its slices list beneath it.\n4. The reader clicks Problem and solution in the rail, which is still there. The\n   scroll pane returns to that level for the same work item.\n\nEdge cases:\n- The work carries no epics: Build is absent, and the rail says why on hover.\n- The reader selects a slice, then leaves Build: the selection clears.\n- The reader changes work item while in Build: the shell lands on Build for the\n  new item, or on Intent when the new item has no epics.\n\n### 5.3 Traverse onward to a pull request\n\nEntry condition: the work reaches a branch or a pull request.\n\n1. The Pull requests group appears in the rail.\n2. The reader opens it. The work's pull requests list, each with its state.\n3. The reader opens one. The scroll pane shows that pull request's own three\n   levels: its intent, its problem and solution, and its architecture.\n4. FlightDeck stays one click away in the same group, and it carries the whole\n   open set.\n\nEdge case: the work has a branch and no pull request. The group renders, and its\nitem states that a branch exists with no pull request recorded against it.\n\n---\n\n## 6. Component Specifications\n\n### 6.1 Shell\n\nThree fixed regions and one scroll pane. The top bar and the rail hold their\nposition at every scroll offset. Only the scroll pane scrolls.\n\nThree strips sit at the top of the pane, and none is a fourth fixed region. The\nreading-progress strip and the section-link bar share one sticky band, and the\nbar's own band sits directly under it.\n\nThe **reading-progress strip** reports the pane's own offset as one 4 px bar. It\nspans the pane's full width. It measures the pane element and never the window,\nbecause the document cannot scroll. Section 11.3 states why.\n\nThe **section-link bar** sits under the strip in the same band, and it is\n`sticky`. It holds one anchor per panel the page rendered, in document order.\nThe bar reads the panels rather than a list of its own. A panel a section gated\naway therefore gains no link. A panel that only renders when its record exists\ngains one.\n\nThe **top line** sits under the bar. It holds the work item's branch, its epic\ncount, and what it supersedes, on one line. Section 2.2 states why it exists\nand why it names no work item and no stage.\n\n### 6.2 Work-item switcher\n\nA shadcn `Popover` over Radix, listing every design and epic the index holds.\nChoosing one rewrites the route and keeps the section where the new item can\nfill it.\n\n### 6.3 Rail\n\nA `nav` element carrying three groups. Each item is an anchor, so it is\nfocusable, right-clickable, and openable in a new tab. The rail collapses to\nicons under 1200 px and becomes a drawer under 768 px.\n\n### 6.4 Contract block\n\nThe contract leads Intent and it starts at the pane's left edge. Version 2.1\nkept it to the right of an Identity panel, and version 2.2 dissolved that panel,\nso nothing holds the contract off the left edge any more.\n\nA two-line band. `Why` takes the first line at full width, because the outcome\nis the sentence the whole work answers to. `Done when`, `Abort if`, and `Out of\nscope` share the second line, because a reader weighs those three against each\nother. A column carries its heading word and nothing else.\n\n### 6.5 Level panels\n\nA `Card` per record. Problem and solution splits the narrative beats by kind into\nits two columns, so a constraint sits beside the problem it constrains.\n\nEach of those two columns is one shadcn `Card`. Its heading is the column word,\n`PROBLEM` or `SOLUTION`. The authored problem or approach prose is the card's\nlead, set as prose above the rows and not boxed. The beats are the rows under it,\neach one a bullet and its text. **The rows alternate in tint.** The first row\npaints `--surface`, which is the card's own colour, and the next paints\n`--surface-2`. The pattern repeats down the list. Both tokens are redefined under\n`data-theme=\"dark\"`, so the stripe holds in either theme. The tint follows the\nrow's index rather than `odd:`, so a row added above the list cannot shift the\npattern.\n\nThe earlier shape gave every beat its own outlined box and then painted two more\nboxes amber and green below. That was two treatments for one thing, and version\n2.1 removed the second one.\n\nAlternatives considered is not here. A rejected option is architectural, and each\ndecision carries its own alternatives, so the panel sits in Architecture.\n\nThe Architecture level orders its tiles by what a reader arrives for. Diagrams\nlead, because the picture answers the shape question fastest. Linked\nArchitecture Decisions follows, with Districts and contexts touched on the\nright, because a reader who wants one usually wants the other. Boundary rules\nspans the pane's full width. This corpus carries sixteen rules, and a one-third\ntile turned them into a long column of narrow cards. The full width therefore\ncarries a multi-column grid.\n\n### 6.6 Status badge and state ladders\n\nA shadcn `Badge` per state family. Where the corpus carries fewer states than the\nloop produces, a dashed ladder renders beside the real value and is marked as a\nprototype target.\n\n### 6.7 Diagram panel\n\nA `ScrollArea` holding the Mermaid source, with a `Button` that renders it. The\nruntime loads on that press and not before.\n\n### 6.8 Controls that navigate\n\nEvery control that moves the reader somewhere is a SmoothUI `smooth-button`,\ntaken from the live registry at `https://smoothui.dev/r/smooth-button.json`.\nThis shell carries three: `Open the whole record`, on a decision row, `Open the\nwhole rule`, on a boundary rule, and `Open the design`, on an epic's Gate 4b\ndocument.\n\nThe variant is `outline`, not a filled brand button. A reading surface is not a\nmarketing page. A filled button on a page whose subject is a record shouts\nlouder than the record does. Each control carries a trailing arrow, because the\narrow says the control moves the reader forward rather than toggling something\nin place. The size is the registry's `lg`, which is 44 px, so section 11.2's hit\ntarget holds.\n\nThe registry item's `tokens.json` dependency adds no file and no `css` block. It\ndoes carry CSS variables, so the token block it wrote into `index.css` is\nexpected rather than a failure.\n\nThe item imports `Slot` from `@radix-ui/react-slot`. This application depends on\nthe unified `radix-ui` package instead, and that package already exports `Slot`.\nThe import therefore points at `radix-ui`, and no second copy of the package is\ninstalled.\n\n---\n\n## 7. Error States and Edge Cases\n\n### 7.1 Index request fails\n\nThe shell still renders the top bar and the rail from the route. The scroll pane\nnames the path it read, the reason it failed, and one retry. It does not fall\nback to a cached index, because a stale index shown as current is worse than a\nstated failure.\n\n### 7.2 Cases to Check\n\n| Case | Required behaviour |\n|---|---|\n| A `stage` outside the vocabulary | the badge reads `unknown`, and the raw word stays in the tooltip |\n| An epic with no slices | the epic row states the count of zero, and no slices block renders |\n| A slice with no epic | it appears in the unresolved-slices panel |\n| `feature_gates` is empty | the Rubrics item states that no gate record exists. It must not read as a pass |\n| An epic has no design document | the epic's own panel tells the two cases apart. A work with no plan directory states that none of its epics can have one. A work with a plan states that this epic has none |\n| A design carries only `goal.json` | every level renders its own absence, and the work reads as deliberately sparse |\n| A superseded design | the top bar names what superseded it |\n| A dangling ADR reference | the Architecture panel names the reference that does not resolve |\n| An epic outcome missing from the index | the panel reads the outcome from `goal.epics[]` and states no source |\n| A pull request with no branch | the group renders and names the branch |\n| Two designs share a name | the id disambiguates, and both render |\n| No deploy record exists | the Shipped group says so, and reports `stage` and any publication instead |\n| An absent record in a closed panel | the panel renders open, because an absence behind a disclosure is an absence the reader cannot see |\n| A panel a section gated away | the section-link bar holds no link for it, because the bar reads the panels the page rendered |\n| A page with no panel at all | the section-link bar renders nothing, rather than an empty strip |\n\n---\n\n## 8. Accessibility\n\n### 8.1 Keyboard Navigation\n\nThe rail is a `nav` of anchors, reachable in order after the top bar. A skip link\nleads from the top bar to the scroll pane. Groups expand on `Enter` or `Space`.\n`Escape` closes the work-item switcher and returns focus to its trigger. Every\nrow is a button, so it is focusable without added `tabindex`.\n\nThe section-link bar is a second `nav` of anchors, and it sits at the start of\nthe scroll pane, so a keyboard reader meets it before the section's own content.\nA press moves the pane and leaves focus on the target panel, so the reader lands\nwhere the pointer would have.\n\n### 8.2 Screen Reader Support\n\nThe rail carries an accessible name. The current item carries `aria-current`.\nGated groups announce their absence in a live region when the index settles, so\na reader who cannot see the rail still learns what the work lacks. The scroll\npane carries an accessible name that names the section and the work item.\n\n### 8.3 Colour Independence\n\nEvery state carries a word. No state relies on colour alone. The status badge and\nthe state ladders pair a colour with a shape marker, so the rail and the badges\nsurvive greyscale.\n\nA heading band carries text, so its two values hold a contrast ratio rather than\na look. White on `--band` is 6.8 to 1, and `--tint-ink` on `--tint` is 10.2 to 1.\nBoth clear WCAG AA for body text. The focus ring is `--band-ink` on a band,\nbecause the teal ring is 1.4 to 1 there.\n\n### 8.4 Focus Management\n\nChoosing a section moves focus to the section heading. Opening an epic leaves\nfocus on the epic row. Selecting a slice leaves focus on the row and announces\nits panel. Changing work item moves focus to the top bar's work-item name. A deep\nlink moves focus to the target and scrolls it into view, honouring reduced\nmotion.\n\n---\n\n## 9. Responsive Considerations\n\n### 9.1 Desktop (over 1200 px)\n\nThe rail is expanded at 248 px. The top bar holds every control on one line. The\nscroll pane runs the full remaining width.\n\n### 9.2 Tablet (768 px to 1200 px)\n\nThe rail collapses to 56 px of icons, with names in tooltips. The top bar keeps\nits controls and drops the search field. Panels stack to one column.\n\n### 9.3 Mobile (under 768 px)\n\nThe rail becomes a drawer behind one control in the top bar. The top bar holds\nthe work item and that control. The scroll pane is the whole screen. The three\nlevels become a horizontal tab strip above the content. Font sizes hold. Only the\nlayout narrows.\n\nThe section-link bar wraps to more than one row when the pane is narrow, so it\nkeeps every link rather than dropping the ones that do not fit. The top line\nwraps the same way.\n\n---\n\n## 10. Animation Specifications\n\n| Element | Animation | Token |\n|---|---|---|\n| Section change | a crossfade of the scroll pane, no slide | `--dur-base` |\n| Work-item change | a crossfade, and the top bar's name updates without moving | `--dur-slow` |\n| Nav group expand | height and opacity together | `--dur-expand` |\n| Panel disclosure | height and opacity together | `--dur-expand` |\n| Epic disclosure | the same, and the slice list reveals | `--dur-expand` |\n| Slice panel | a 12 px slide with a fade | `--dur-base` |\n| Theme change | the icon crossfades, then the tokens fade | `--dur-fast`, then `--dur-base` |\n| Arrival highlight | a background pulse on the target | `--dur-highlight` |\n| Diagram render | a fade at the diagram's own aspect ratio | `--dur-base` |\n| Section-link jump | the pane's offset moves to the target's heading | `--dur-base`, or `--dur-instant` under reduced motion |\n\nNothing animates on scroll. Nothing parallaxes. The fixed regions never move.\nThe section-link bar is the one element that holds its position while the pane\nmoves. It is sticky inside the pane, not fixed to the window.\n\n---\n\n## 11. Layering, Hit Targets, and Scroll Ownership\n\n### 11.1 Layering\n\nOne scale, named once.\n\n| Layer | Token | Holds |\n|---|---|---|\n| 0 | `--layer-base` | the shell and every panel |\n| 1 | `--layer-raised` | a hovered or selected row, and the sticky band of a section that stacks |\n| 2 | `--layer-fixed` | the top bar and the rail |\n| 3 | `--layer-panel` | the work-item switcher, the tooltips, and the diagram dialog's backdrop |\n| 4 | `--layer-modal` | the mobile nav drawer, the record sheet, and the diagram dialog |\n\n### 11.2 Hit Targets\n\n| Control | Hit target element | Bounds |\n|---|---|---|\n| Nav item | the anchor's full row | 248 px wide, at least 36 px tall |\n| Nav group header | the whole header row | full rail width, at least 32 px tall |\n| Collapsed nav item | the icon's anchor | 56 px wide, at least 44 px tall |\n| Work-item switcher | the trigger button | at least 44 px tall |\n| Status badge | the badge, for its tooltip | at least 24 px tall. The tooltip opens on hover and on focus |\n| Theme switcher | the icon button | 44 px square |\n| Epic row | the whole row, not the chevron | full pane width, at least 48 px tall |\n| Slice row | the whole row | full pane width, at least 44 px tall |\n| Panel disclosure | the panel's heading row, on a panel that declares one | full panel width, at least 36 px tall |\n| Section heading in the strip | the anchor | at least 32 px tall, and the anchor stays focusable |\n| Pager control | the button | at least 44 px tall |\n| Diagram tile | the whole tile | the tile's full width, 200 px tall. The drawing inside carries `pointer-events: none`, so the tile is one target |\n| Zoom control | the button | 44 px square |\n| Dialog close | the button | 44 px square |\n| Sheet jumplink | the anchor | at least 32 px tall |\n| Render control | the button | at least 44 px tall |\n| Navigating control | the button | at least 44 px tall |\n| Section link | the anchor | at least 32 px tall |\n| Contract row | the row, when it links to its source | the row's full height |\n| Mobile nav drawer trigger | the button | 44 px square |\n\nAn icon is never the sole hit target. Every icon sits inside a target at least\n44 px in each direction, and the two exceptions above are anchors, which meet the\nsame rule at 32 px because a heading is a word rather than an icon.\n\n**The two arrow keys are a hit target too.** `ArrowLeft` and `ArrowRight` step the\nsection index on a paged level, and they act wherever focus sits. The binding\nignores an event whose target is an `input`, a `textarea`, or a `select`. It also\nignores one with a modifier key held.\n\n### 11.3 Scroll Ownership\n\n| Region | Owns the scroll | Why |\n|---|---|---|\n| The document | **nothing** | the page does not scroll. The shell fills the viewport exactly |\n| The pane | both axes, on a section that stacks | it is the only scrolling region for that mode, and it holds every panel of the section |\n| The pane | **nothing, on a paged level** | the level lays its sections on a track and one box is on screen, so the pane has no overflow to own. The frame a reader sees never moves |\n| A section box | both axes, when its section is taller than the box | the box is the only region that scrolls on a paged level. The stage clips the track, never a box's content |\n| The section strip | itself, sideways, when the headings exceed the width | a record with many sections keeps every heading reachable |\n| The pager bar | none | it holds one row of two controls and a count |\n| The rail | itself, when its items exceed the viewport | a long rail must stay usable, and the rail never scrolls the pane |\n| The top bar | none | it holds one line at every width |\n| The work-item switcher list | itself | its list can exceed the viewport |\n| The diagram dialog's body | both axes | a drawing at 400 % is far wider and taller than the dialog |\n| The record sheet's container | both axes | an ADR body holds tables and long prose |\n| A tooltip | none | it is transient and never scrolls |\n| The section-link bar | none. It is `sticky` inside the pane, so it rides the pane's own scroll and adds no region | it holds one row of links, and it never scrolls anything |\n| The progress strip | none. It holds one 4 px bar | on a paged level it reports the level's own position, and on a section that stacks it reports the pane's offset |\n\nThe document never scrolls, so `html` and `body` carry no overflow. Every wheel\nevent not consumed by a nested region scrolls the innermost region that can take\nit. Where two regions could claim a gesture, the innermost owner wins, and the\nouter region does not move.\n\n**A section link moves the pane element, never the window.** The document cannot\nscroll at all, so a hash write or a `window.scrollTo` would land nowhere. The\nlink intercepts the press, measures the target against the pane, and sets the\npane's own offset. Under `prefers-reduced-motion: reduce` the pane arrives in\none frame instead of animating.\n\n**A section heading in the strip moves nothing at all except the index.** On a\npaged level the pane cannot scroll, so there is no offset to set. The press sets\nthe shared index, and the track slides one box. The box then owns its own scroll.\nThe heading stays focusable, and the press is prevented so the route never moves.\n\n**The progress strip measures the level on a paged level and the pane on a\nsection that stacks.** The document cannot scroll, so a window-scoped progress\nbar would read zero at every position. On a section that stacks the strip takes\nthe pane's own element and reports that element's offset, as it always has. On a\npaged level the strip instead reports `(index + within) / count`. `within` is the\nactive box's own scroll progress, and a box with no scroll range counts as fully\nread. That value answers how far through the level a reader is. A pane-scoped bar\ncannot answer that question once the pane stops scrolling. The strip spans the\nlevel's full width and sits above the section strip.\n\n**Five rules make \"no scroll\" safe rather than a way to hide content.** A layout\nthat fills the viewport and hides its overflow will clip whatever does not fit,\nand the reader has no way to reach it. These rules prevent that.\n\n1. **The shell fills its parent, never the viewport.** It uses `h-full`, not\n   `h-dvh`. The viewport height belongs to whoever owns the page. When a host\n   renders the shell beneath its own chrome, `h-dvh` makes the shell taller than\n   the space it was given, and the surplus is clipped at one edge.\n2. **The scrolling region scrolls on both axes.** `overflow: auto`, never\n   `overflow-y: auto` with `overflow-x: hidden`. A region that hides its\n   horizontal overflow clips a wide table with no scrollbar and no way to reach\n   it. The region is the pane on a section that stacks. It is the section box on a\n   paged level, where the pane itself has nothing to scroll.\n3. **A wide block scrolls itself.** Every table, code block, and diagram wraps in\n   its own `overflow-x: auto` container, so the region keeps its own scroll and the\n   wide block keeps its integrity.\n4. **Every flex and grid child carries `min-w-0`.** A flex child defaults to its\n   content width, so one wide row stretches the region and pushes its siblings off\n   the edge.\n5. **The shell fills the visible screen, never the whole window.** It caps itself\n   to the narrower of the window and the screen. This rule is the price of rule 2,\n   and it exists because of a real failure. A reader ran a 1600 px window on a\n   1512 px screen. The surplus 88 px sat outside the display. A normal page would\n   let them scroll sideways to reach it. This shell cannot, on purpose, so 88 px\n   of content was unreachable and no scrollbar could appear to say so. The reader\n   reported it as clipping, which is exactly what it looked like. A window is not\n   a screen, and a shell with no document scroll must never assume it is.\n\n**On a paged level, the stage clips only the track.** The stage's outer element\nis `overflow: hidden`, and what it hides is the boxes either side of the one on\nscreen. It never hides a box's content. Each box is its own two-axis scroll\nregion, and the boxes off screen carry `inert`. So nothing focusable sits where a\nreader cannot see it. This is the one clip box in the shell, and it is the reason\nrule 3 holds rather than bends on a paged level.\n\nMeasured on the mockup at 1600x1000 on 2026-09-19. The pane reported\n`overflow-x: hidden`, which is rule 2's defect: any block wider than the pane\nclipped with no scrollbar and no way to reach it. Rule 1 is a guard rather than a\nrepair. The shell did fit its parent, at 919 px against 919 px. The rule stays\nbecause the host, not the shell, owns the viewport height, and a host that does\nnot give the shell a definite height will break it.\n\n---\n\n## 12. Future Considerations\n\n1. **No deploy record exists.** The Shipped section reports `stage` and the\n   publication entities, and states the gap. A future record type would fill it.\n2. **`feature_gates` is empty.** Rubrics have nothing to read until it populates.\n3. **The epic and slice vocabularies need a schema change.** The build loop\n   produces six epic states and seven slice states. The corpus carries four and\n   one.\n4. **Epic outcomes are absent from the index.** They live in `goal.epics[]`, so\n   the Why line must read a second source and say so.\n5. **`epic.design_doc` misses five documents.** It builds a key from the design id\n   and compares against the planning slug, so `plugin-split`'s five documents do\n   not resolve. `shared/build_index.py` owns that fix.\n6. **The three variations remain useful.** Their tile arrangements are inputs to\n   the level panels inside the scroll pane.\n7. **The theme switcher persists nothing.** A published file cannot store a\n   preference.\n\n---\n\n## Document Validation Checklist\n\n- [x] Every interactive component has a state table in section 3.2.\n- [x] Every component marks exactly one initial state.\n- [x] Every timer names its arming event and its reset event. The only deferred\n      load is the diagram render, and it starts when its section mounts.\n- [x] Every control with a conditional existence appears in section 3.3.\n- [x] Every multi-valued setting appears in section 2.3 with a default.\n- [x] Every state change appears in section 4.1 with a source and target state.\n- [x] Every duration in section 4.1 names a token from section 4.2.\n- [x] Every flow names its entry condition and its edge cases.\n- [x] Every floating surface names a layer from section 11.1.\n- [x] Every control names its hit target element and its clickable bounds.\n- [x] Every overlapping region names the surface that owns the gesture.\n- [x] No section depends on the mockup for an answer.\n"}]}, "joins": {"adr_to_pull_request": {"ADR-0001": {"pr": 2, "via": "direct", "path": []}, "ADR-0002": {"pr": 2, "via": "direct", "path": []}, "ADR-0003": {"pr": 3, "via": "direct", "path": []}, "ADR-0004": {"pr": 3, "via": "direct", "path": []}, "ADR-0005": {"pr": 4, "via": "direct", "path": []}, "ADR-0006": {"pr": 4, "via": "direct", "path": []}, "ADR-0007": {"pr": 5, "via": "direct", "path": []}, "ADR-0008": {"pr": 5, "via": "direct", "path": []}, "ADR-0009": {"pr": 6, "via": "direct", "path": []}, "ADR-0010": {"pr": 9, "via": "direct", "path": []}, "ADR-0011": {"pr": 11, "via": "epic", "path": ["cobuilder-implement/E1", "cobuilder-implement/E2", "design-mode/E1"]}, "ADR-0012": {"pr": 11, "via": "epic", "path": ["cobuilder-implement/E1", "cobuilder-implement/E2"]}, "ADR-0013": {"pr": 11, "via": "epic", "path": ["cobuilder-implement/E1", "cobuilder-implement/E2", "interaction-design-gate/E1", "interaction-design-gate/E2", "interaction-design-gate/E3"]}, "ADR-0014": {"pr": 11, "via": "epic", "path": ["design-mode/E1"]}, "ADR-0015": {"pr": 11, "via": "epic", "path": ["cobuilder-implement/E1", "cobuilder-implement/E2", "design-mode/E1", "plugin-split/E1", "plugin-split/E2", "plugin-split/E3", "plugin-split/E4", "plugin-split/E5", "plugin-split/E6", "plugin-split/E7", "plugin-split/E8"]}, "ADR-0016": {"pr": 11, "via": "direct", "path": []}, "ADR-0017": {"pr": 11, "via": "direct", "path": []}, "ADR-0018": {"pr": 11, "via": "direct", "path": []}, "ADR-0019": {"pr": 11, "via": "direct", "path": []}, "ADR-0020": {"pr": 11, "via": "direct", "path": []}, "ADR-0021": {"pr": 16, "via": "epic", "path": ["book-index-tiering/E1"]}, "ADR-0022": {"pr": 19, "via": "epic", "path": ["gate-doc-surfacing/E1", "gate-doc-surfacing/E2"]}, "ADR-0023": {"pr": 21, "via": "epic", "path": ["build-workflow-polish/E1", "build-workflow-polish/E2", "build-workflow-polish/E3", "build-workflow-polish/E4", "cobuilder-viewer/E1", "cobuilder-viewer/E2", "cobuilder-viewer/E3", "cobuilder-viewer/E4", "cobuilder-viewer/E5", "cobuilder-viewer/E6", "cobuilder-viewer/E7", "cobuilder-viewer/E8", "cobuilder-viewer/E9", "cobuilder-viewer/E10", "cobuilder-viewer/E11", "cobuilder-viewer/E12", "cobuilder-viewer/E13", "cobuilder-viewer/E14", "cobuilder-viewer/E15", "cobuilder-viewer/E16", "cobuilder-viewer/E17", "cobuilder-viewer/E18", "cobuilder-viewer/E19", "react-viewer/E1", "react-viewer/E2", "react-viewer/E3", "react-viewer/E4", "react-viewer/E5", "react-viewer/E6", "react-viewer/E7", "react-viewer/E8", "react-viewer/E9"]}, "ADR-0024": {"pr": null, "via": "epic", "path": ["gate-state-resolution/E1", "interaction-design-gate/E1", "interaction-design-gate/E2", "interaction-design-gate/E3"]}, "ADR-0025": {"pr": null, "via": "epic", "path": ["review-flight-deck/F0", "review-flight-deck/F1", "review-flight-deck/F2", "review-flight-deck/F3", "review-flight-deck/F4", "review-flight-deck/F5", "review-flight-deck/F6"]}, "ADR-0026": {"pr": null, "via": "epic", "path": ["review-flight-deck/F0", "review-flight-deck/F1", "review-flight-deck/F2", "review-flight-deck/F3", "review-flight-deck/F4", "review-flight-deck/F5", "review-flight-deck/F6"]}, "ADR-0027": {"pr": 21, "via": "epic", "path": ["cobuilder-viewer/E1", "cobuilder-viewer/E2", "cobuilder-viewer/E3", "cobuilder-viewer/E4", "cobuilder-viewer/E5", "cobuilder-viewer/E6", "cobuilder-viewer/E7", "cobuilder-viewer/E8", "cobuilder-viewer/E9", "cobuilder-viewer/E10", "cobuilder-viewer/E11", "cobuilder-viewer/E12", "cobuilder-viewer/E13", "cobuilder-viewer/E14", "cobuilder-viewer/E15", "cobuilder-viewer/E16", "cobuilder-viewer/E17", "cobuilder-viewer/E18", "cobuilder-viewer/E19"]}, "ADR-0028": {"pr": 21, "via": "epic", "path": ["cobuilder-viewer/E1", "cobuilder-viewer/E2", "cobuilder-viewer/E3", "cobuilder-viewer/E4", "cobuilder-viewer/E5", "cobuilder-viewer/E6", "cobuilder-viewer/E7", "cobuilder-viewer/E8", "cobuilder-viewer/E9", "cobuilder-viewer/E10", "cobuilder-viewer/E11", "cobuilder-viewer/E12", "cobuilder-viewer/E13", "cobuilder-viewer/E14", "cobuilder-viewer/E15", "cobuilder-viewer/E16", "cobuilder-viewer/E17", "cobuilder-viewer/E18", "cobuilder-viewer/E19"]}}, "epic_to_pull_request": {"book-index-tiering/E1": 16, "cobuilder-implement/E1": 11, "cobuilder-implement/E2": 12, "cobuilder-viewer/E1": 21, "cobuilder-viewer/E2": 21, "cobuilder-viewer/E3": 21, "cobuilder-viewer/E4": 21, "cobuilder-viewer/E5": 21, "cobuilder-viewer/E6": 21, "cobuilder-viewer/E7": 21, "cobuilder-viewer/E8": 21, "cobuilder-viewer/E9": 21, "cobuilder-viewer/E10": 21, "cobuilder-viewer/E11": 21, "cobuilder-viewer/E12": 21, "cobuilder-viewer/E13": 21, "cobuilder-viewer/E14": 21, "cobuilder-viewer/E15": 21, "cobuilder-viewer/E16": 21, "cobuilder-viewer/E17": 21, "cobuilder-viewer/E18": 21, "cobuilder-viewer/E19": 21, "design-mode/E1": 11, "gate-doc-surfacing/E1": 19, "gate-doc-surfacing/E2": 19, "plugin-split/E1": 11, "plugin-split/E2": 11, "plugin-split/E3": 11, "plugin-split/E4": 11, "plugin-split/E5": 11, "plugin-split/E6": 11, "plugin-split/E8": 18}, "epic_status": {"book-index-tiering/E1": "open", "build-workflow-polish/E1": "unstarted", "build-workflow-polish/E2": "unstarted", "build-workflow-polish/E3": "unstarted", "build-workflow-polish/E4": "unstarted", "cobuilder-implement/E1": "merged", "cobuilder-implement/E2": "merged", "cobuilder-viewer/E1": "open", "cobuilder-viewer/E2": "open", "cobuilder-viewer/E3": "open", "cobuilder-viewer/E4": "open", "cobuilder-viewer/E5": "open", "cobuilder-viewer/E6": "open", "cobuilder-viewer/E7": "open", "cobuilder-viewer/E8": "open", "cobuilder-viewer/E9": "open", "cobuilder-viewer/E10": "open", "cobuilder-viewer/E11": "open", "cobuilder-viewer/E12": "open", "cobuilder-viewer/E13": "open", "cobuilder-viewer/E14": "open", "cobuilder-viewer/E15": "open", "cobuilder-viewer/E16": "open", "cobuilder-viewer/E17": "open", "cobuilder-viewer/E18": "open", "cobuilder-viewer/E19": "open", "design-mode/E1": "merged", "gate-doc-surfacing/E1": "open", "gate-doc-surfacing/E2": "open", "gate-state-resolution/E1": "unstarted", "inflight-record-store/E1": "unstarted", "inflight-record-store/E2": "unstarted", "inflight-record-store/E3": "unstarted", "inflight-record-store/E4": "unstarted", "interaction-design-gate/E1": "unstarted", "interaction-design-gate/E2": "unstarted", "interaction-design-gate/E3": "unstarted", "lean-bundle-diffs/E1": "unstarted", "lean-bundle-diffs/E2": "unstarted", "lean-bundle-diffs/E3": "unstarted", "maintainable-viewer/E1": "unstarted", "maintainable-viewer/E2": "unstarted", "maintainable-viewer/E3": "unstarted", "maintainable-viewer/E4": "unstarted", "plugin-split/E1": "merged", "plugin-split/E2": "merged", "plugin-split/E3": "merged", "plugin-split/E4": "merged", "plugin-split/E5": "merged", "plugin-split/E6": "merged", "plugin-split/E7": "unstarted", "plugin-split/E8": "open", "react-viewer/E1": "unstarted", "react-viewer/E2": "unstarted", "react-viewer/E3": "unstarted", "react-viewer/E4": "unstarted", "react-viewer/E5": "unstarted", "react-viewer/E6": "unstarted", "react-viewer/E7": "unstarted", "react-viewer/E8": "unstarted", "react-viewer/E9": "unstarted", "review-flight-deck/F0": "no-pull-request", "review-flight-deck/F1": "no-pull-request", "review-flight-deck/F2": "no-pull-request", "review-flight-deck/F3": "no-pull-request", "review-flight-deck/F4": "no-pull-request", "review-flight-deck/F5": "no-pull-request", "review-flight-deck/F6": "no-pull-request", "ubiquitous-language/E1": "unstarted", "ubiquitous-language/E2": "unstarted", "ubiquitous-language/E3": "unstarted", "ubiquitous-language/E4": "unstarted", "ubiquitous-language/E5": "unstarted"}, "slice_to_epic": {"cobuilder-family/4": "plugin-split/E1", "cobuilder-family/5": "plugin-split/E1", "cobuilder-family/6": "plugin-split/E1", "cobuilder-family/7": "plugin-split/E1", "cobuilder-family/1": "plugin-split/E2", "cobuilder-family/2": "plugin-split/E3", "cobuilder-family/3": "plugin-split/E3", "cobuilder-family/8": "plugin-split/E4", "cobuilder-family/9": "plugin-split/E4", "cobuilder-family/10": "plugin-split/E5", "cobuilder-family/11": "plugin-split/E5", "cobuilder-family/12": "plugin-split/E6", "cobuilder-family/13": "plugin-split/E6", "cobuilder-family/14": "plugin-split/E6", "cobuilder-viewer/1": "cobuilder-viewer/E1", "cobuilder-viewer/2": "cobuilder-viewer/E2", "cobuilder-viewer/3": "cobuilder-viewer/E2", "cobuilder-viewer/4": "cobuilder-viewer/E3", "cobuilder-viewer/5": "cobuilder-viewer/E4", "cobuilder-viewer/8": "cobuilder-viewer/E5", "cobuilder-viewer/9": "cobuilder-viewer/E5", "cobuilder-viewer/10": "cobuilder-viewer/E5", "cobuilder-viewer/11": "cobuilder-viewer/E5", "cobuilder-viewer/12": "cobuilder-viewer/E5", "cobuilder-viewer/13": "cobuilder-viewer/E5", "cobuilder-viewer/14": "cobuilder-viewer/E6", "cobuilder-viewer/15": "cobuilder-viewer/E7", "cobuilder-viewer/16": "cobuilder-viewer/E7", "cobuilder-viewer/6": "cobuilder-viewer/E19", "cobuilder-viewer/7": "cobuilder-viewer/E19", "gate-doc-surfacing/1": "gate-doc-surfacing/E1", "gate-doc-surfacing/2": "gate-doc-surfacing/E1", "gate-doc-surfacing/3": "gate-doc-surfacing/E1", "gate-doc-surfacing/4": "gate-doc-surfacing/E2", "gate-doc-surfacing/5": "gate-doc-surfacing/E2", "gate-doc-surfacing/6": "gate-doc-surfacing/E2", "gate-state-resolution/1": "gate-state-resolution/E1", "gate-state-resolution/2": "gate-state-resolution/E1", "interaction-design-gate/1": "interaction-design-gate/E1", "interaction-design-gate/2": "interaction-design-gate/E1", "interaction-design-gate/3": "interaction-design-gate/E2", "interaction-design-gate/4": "interaction-design-gate/E2", "interaction-design-gate/5": "interaction-design-gate/E3", "interaction-design-gate/6": "interaction-design-gate/E3", "interaction-design-gate/7": "interaction-design-gate/E3"}, "slice_to_epic_unresolved": {}, "context_verifies_district": {"cobuilder-packaging": ["commands", "skills", "scripts", "viewer", "docs"]}, "district_uncovered": [".cobuilder-architect"], "adr_to_context": {"ADR-0016": "cobuilder-packaging", "ADR-0017": "cobuilder-packaging", "ADR-0018": "cobuilder-packaging", "ADR-0019": "cobuilder-packaging", "ADR-0020": "cobuilder-packaging", "ADR-0022": "cobuilder-packaging", "ADR-0023": "cobuilder-packaging", "ADR-0024": "cobuilder-packaging", "ADR-0025": "cobuilder-packaging", "ADR-0026": "cobuilder-packaging", "ADR-0027": "cobuilder-packaging", "ADR-0028": "cobuilder-packaging"}, "adr_to_district": {"ADR-0001": "scripts", "ADR-0002": "scripts", "ADR-0003": ".prodyssey", "ADR-0004": "skills", "ADR-0005": "skills", "ADR-0006": "scripts", "ADR-0007": "scripts", "ADR-0008": "scripts", "ADR-0009": "commands", "ADR-0010": "skills", "ADR-0011": "skills", "ADR-0012": "skills", "ADR-0013": "skills", "ADR-0014": "scripts", "ADR-0015": "skills", "ADR-0021": "skills"}, "feature_gates": {"cobuilder-family": [{"n": "1", "name": "Product", "state": "APPROVED 2026-08-20"}, {"n": "2", "name": "Architecture", "state": "APPROVED 2026-08-21"}, {"n": "3", "name": "Program Design", "state": "APPROVED 2026-08-21", "doc": "cobuilder-family", "doc_kind": "program"}, {"n": "4", "name": "Slice plan, epic designs, and rubrics", "state": "APPROVED 2026-08-25"}], "cobuilder-viewer": [{"n": "1", "name": "Product", "state": "APPROVED 2026-09-22"}, {"n": "2", "name": "Architecture", "state": "APPROVED 2026-09-22"}, {"n": "2b", "name": "Interaction design", "state": "APPROVED 2026-09-22", "doc": "cobuilder-viewer", "doc_kind": "interaction"}, {"n": "3", "name": "Program Design", "state": "APPROVED 2026-09-22", "doc": "cobuilder-viewer", "doc_kind": "program"}, {"n": "4", "name": "Slice plan, epic designs, and rubrics", "state": "pending"}], "gate-doc-surfacing": [{"n": "1", "name": "Product", "state": "APPROVED 2026-09-01"}, {"n": "2", "name": "Architecture", "state": "APPROVED 2026-09-01"}, {"n": "3", "name": "Program Design", "state": "APPROVED 2026-09-01", "doc": "gate-doc-surfacing", "doc_kind": "program"}, {"n": "4", "name": "Slice plan, epic designs, and rubrics", "state": "APPROVED 2026-09-01"}], "gate-state-resolution": [{"n": "1", "name": "Product (no tech talk)", "state": "APPROVED 2026-09-14"}, {"n": "2", "name": "Architecture", "state": "APPROVED 2026-09-14"}, {"n": "2b", "name": "Interaction design", "state": "n/a (no new screen, no new interaction) — Screens: \"No new screen. One existing screen changes its gate labels and its document selection.\""}, {"n": "3", "name": "Program design", "state": "APPROVED 2026-09-14", "doc": "gate-state-resolution", "doc_kind": "program"}, {"n": "4", "name": "Slice plan, epic designs, and rubrics", "state": "APPROVED 2026-09-14"}], "interaction-design-gate": [{"n": "1", "name": "Product", "state": "APPROVED 2026-09-14"}, {"n": "2", "name": "Architecture", "state": "APPROVED 2026-09-14"}, {"n": "2b", "name": "Interaction design", "state": "n/a (no UI) — Screens: \"none — no new screen\""}, {"n": "3", "name": "Program Design", "state": "APPROVED 2026-09-14", "doc": "interaction-design-gate", "doc_kind": "program"}, {"n": "4", "name": "Slice plan, epic designs, and rubrics", "state": "APPROVED 2026-09-14"}], "skill-collision-fix": [{"n": "1", "name": "3", "state": "n/a, superseded by direct conversation record"}, {"n": "4", "name": "Slice plan, epic designs, and rubrics", "state": "in progress"}]}}};
