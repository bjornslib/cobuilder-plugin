{
  "name": "review-flight-deck",
  "title": "A reviewer surface above several pull requests",
  "adr": null,
  "adrs": [
    "ADR-0022"
  ],
  "created": "2026-09-01",
  "outcome": "A reviewer selects several pull requests, opens one surface that shows where they overlap, conflict, and reinforce each other, and leaves with a decision recorded against each pull request: review alone, merge together, or split.",
  "done_when": [
    "A reviewer names a set of pull requests and receives an ordered list of contested regions, plus a proposed partition of the set into review units.",
    "The proposal names the evidence behind every relationship it claims, and prints the reason instead of the assertion where it could not assess one.",
    "Each pull request in the set carries one recorded call in the ledger, and that call names the set it was judged against.",
    "The viewer renders the proposal and the recorded calls, and computes no relationship of its own.",
    "A copy served without a write path renders read-only and says so."
  ],
  "abort_if": [
    "The stop count for a set is not materially lower than the total number of authored files the pull requests change. Measured over 27 pairs of merged pull requests in this bundle, the ratio runs from 0.00 to 0.41, and every pair reduces. This criterion is met for the whole corpus.",
    "Superseded criterion, kept for the record: the first version of this test compared the stop count against the shared-file list, not against the total changed files. That was the wrong baseline, because a reviewer's alternative is reading both diffs in full, not reading the shared subset. On that wrong baseline only 9 of 27 pairs reduce, and pull requests 11 and 12 give 37 stops against 22 shared files.",
    "A proposed partition anchors reviewers more than it helps them, measured against a set they also reviewed without one."
  ],
  "min_work": {
    "derived_from": "3 districts touched (viewer, scripts, .cobuilder-architect). 4 colliding ADRs: ADR-0001 (one self-contained file under a blocking content policy), ADR-0018 (one lifecycle surface and a derived record index), ADR-0019 (the append-only ledger, with its vocabulary fixed at comment, reply, and state), ADR-0020 (viewer parts and an author-time build, decided and not executed). 1 boundary rule crossed: no plugin names another plugin's path, so the viewer reaches the proposal through the bundle.",
    "alternatives_explored": 9,
    "boundary_rules_checked": true,
    "challenge_stage_run": true,
    "note": "Stage 3 issued six isolated exploration frames and one separate critic pass. Stage 4 raised three challenges and the engineer answered all three. The engineer overruled the recommended approach and chose the Braid and the Cut together, on the ground that both do work on the reviewer's behalf before the reviewer arrives. That choice replaced the stated approach and moved the whole computation terminal-side, which also removed the ledger write-path defect the first challenge raised. Stage 7 asked whether this is one pull request or several. The engineer chose one, so every epic carries the same branch, design/review-flight-deck, and the six epics are units of work inside one review rather than six pull requests. A scratchpad run of E1's arithmetic over the cached diffs corrected the first abort criterion, which compared stops against shared files rather than against total changed files."
  },
  "limits": {
    "warn_after_rounds": 3,
    "cutoff_rounds": 6
  },
  "epics": [
    {
      "id": "E1",
      "slug": "region-table",
      "outcome": "A committed script parses the hunk headers in the cached diff files and prints the region table for a set. For pull requests 11 and 12 it reports 37 contested regions against 476 authored files changed, and it excludes derived output under .cobuilder-architect/ from both counts.",
      "branch": "design/review-flight-deck",
      "pr": null,
      "state": "planned",
      "note": "The tracer bullet. Its arithmetic already ran in a scratchpad over the eight cached diff files, so the risk it existed to retire is retired: region cutting reduces the reading surface for 27 of 27 pairs. The epic remains, because the script is not committed and nothing yet handles an open branch, where no cached diff exists."
    },
    {
      "id": "E2",
      "slug": "braid-mode",
      "outcome": "A mode in the pr plugin takes a set of pull request numbers, fetches the branches, runs git merge-tree, and prints an ordered stop list with an unassessable reason wherever a branch could not be fetched.",
      "branch": "design/review-flight-deck",
      "pr": null,
      "state": "planned",
      "note": "The proposer. Terminal-side by construction, because the browser can neither run git nor fetch."
    },
    {
      "id": "E3",
      "slug": "region-walk-and-ledger",
      "outcome": "The reviewer answers one keypress per stop, and each keypress appends one region_verdict line through shared/ledger.py with direct file access.",
      "branch": "design/review-flight-deck",
      "pr": null,
      "state": "planned",
      "note": "The two new line kinds sit outside the vocabulary ADR-0019 fixed, so each needs its own projection. fold_threads must keep ignoring them."
    },
    {
      "id": "E4",
      "slug": "braid-projection",
      "outcome": "A projection folds region verdicts into one disposition per pull request and into the pull-request-to-pull-request join the record index does not hold. A transitive edge is labelled derived, or the fold stops at pairwise.",
      "branch": "design/review-flight-deck",
      "pr": null,
      "state": "planned",
      "note": "The open question in ADR-0022's consequences is decided here, not before."
    },
    {
      "id": "E5",
      "slug": "partition-proposal",
      "outcome": "The mode proposes a partition of the set into review units, each carrying the reason it exists. An untouched unit records as accepted, stamped with the proposal id.",
      "branch": "design/review-flight-deck",
      "pr": null,
      "state": "planned",
      "note": "The Cut, cold-start. No precedent table, because the ledger is 0 bytes. History improves the reason ranking later and does not produce the partition."
    },
    {
      "id": "E6",
      "slug": "viewer-renders-braid",
      "outcome": "The viewer reads data/braid.js and renders the proposal and the recorded calls. It computes no relationship, and a copy served without a write path renders read-only and says so.",
      "branch": "design/review-flight-deck",
      "pr": null,
      "state": "planned",
      "note": "Last, and deliberately so. ADR-0020's viewer split is decided and not executed, so this epic should follow it rather than enlarge the file first."
    }
  ],
  "stage": "review",
  "supersedes": null,
  "rounds": [
    {
      "n": 1,
      "changed": true,
      "feedback_class": "material"
    }
  ]
}
