{
  "stage": "design",
  "verdict": "concerns",
  "risk_tier": "sensitive",
  "regret_risk": "If this design ships as written, the team lives with a Node toolchain in a repository that had none, and with a committed build artifact that only stays true while every engineer remembers to run the build. The guard test makes forgetting loud, so the durable regret is not staleness. It is reach: a contributor who could previously fix a viewer bug with a text editor now needs npm install first. That cost is paid on every future viewer change, and it is the one cost no epic in this design removes.",
  "summary": "The three-surface shape is grounded, and a working prototype against the real bundle is stronger evidence than most designs carry at this stage. The approach is sound and the ordering is right, with the exporter seam first. Two things hold this back from a clean verdict. The design reverses a rejected option in ADR-0020 that is four months old and still current, so it must supersede that ADR rather than sit next to it. And three of its four unknowns are unmeasured platform questions about publishing, which E7 answers last, although E2 commits the toolchain first.",
  "constraint_introduced": "plugins/artifact/viewer/index.html is a build output compiled from TypeScript and React sources under plugins/artifact/viewer/src/. An engineer edits a source file, never the output. A test rebuilds from source and fails when the committed output differs. The build never runs at install time and never in the browser.",
  "findings": [
    {"kind": "prediction", "id": "F1", "severity": "high",
     "title": "This design contradicts a rejected option in ADR-0020 and must supersede it, not coexist with it",
     "detail": "ADR-0020 rejected 'Adopt a bundler such as esbuild or Vite' because it 'adds a Node toolchain to a repository of prose, Python scripts, and one HTML file'. That statement is still true about the repository. What changed is the problem: ADR-0020 addressed the size of the existing file, and this design addresses a different information architecture. Two decided ADRs that answer the same question differently is drift by construction. ADR-0023 must state that it supersedes ADR-0020, and maintainable-viewer must move to stage superseded, or the store carries two live answers.",
     "cites": "ADR-0020, docs/architecture/designs/maintainable-viewer/goal.json"},
    {"kind": "prediction", "id": "F2", "severity": "high",
     "title": "The exporter seam breaks on the first bundled build unless E1 lands first",
     "detail": "export_artifact.py rewrites the viewer by verbatim string replacement, and it hard-errors when a literal is not found. A bundled and minified build moves or renames every literal it matches. Any ordering that lands the build before the seam leaves publishing broken until somebody publishes, which is the exact silent failure ADR-0020's forces already recorded. E1 is correctly first, and it must not be reordered.",
     "cites": "ADR-0020 forces, plugins/artifact/scripts/export_artifact.py"},
    {"kind": "prediction", "id": "F3", "severity": "medium",
     "title": "The anchored-comments ledger may not survive a React re-render",
     "detail": "ADR-0019 computes a comment anchor from the live DOM at annotation time, by walking up to five ancestors to find a stable selector. React owns the DOM and replaces nodes on state change, so an anchor captured under one render may not resolve under the next. The design lists this as an unknown, and it deserves an epic of its own or an explicit acceptance that anchors become surface-scoped rather than element-scoped.",
     "cites": "ADR-0019, plugins/artifact/viewer/index.html anchor walk"},
    {"kind": "prediction", "id": "F4", "severity": "medium",
     "title": "The toolchain is committed in E2, and the questions that would veto it are answered in E7",
     "detail": "Three of the four unknowns are publishing questions: byte-equal rebuild across machines, the inlined runtime against the 16 MiB budget, and post-load Mermaid rendering. Two of the three abort conditions depend on them. E7 measures them last, so five epics of work sit between the commitment and the evidence. A cheap spike inside E2, publishing one throwaway bundled page, would move the veto to the front.",
     "cites": "ADR-0001, docs/architecture/designs/react-viewer/goal.json abort_if"},
    {"kind": "prediction", "id": "F5", "severity": "low",
     "title": "The install-surface rule is not violated by this design",
     "detail": "The packaging context's public interface names commands, skills, and script constants. It does not name a build step, and /plugin install runs no build in either the current or the proposed shape. A Node toolchain that runs only on a maintainer's machine, and only when the viewer changes, sits outside the install surface. The rule of no agents, no hooks, and no MCP servers is also untouched.",
     "cites": "docs/architecture/contexts/cobuilder-packaging/boundary.yaml, ADR-0016"},
    {"kind": "prediction", "id": "F6", "severity": "low",
     "title": "Not a duplicate of any existing decision, and it absorbs one backlog design",
     "detail": "No ADR in the store of 22 decides a viewer framework. ADR-0018 decides the record index this design reads, ADR-0019 decides the ledger it must preserve, and ADR-0022 covers gate documents it renders. The only collision is ADR-0020 and its maintainable-viewer design, which F1 covers. build-workflow-polish also names a viewer tab layout in its outcome, and that item should be re-read once this design settles, because the layout it borrows may no longer exist.",
     "cites": "ADR-0018, ADR-0019, ADR-0022, docs/architecture/designs/build-workflow-polish/goal.json"}
  ],
  "boundary_checks": [
    {"rule": "no plugin reads another plugin's files", "result": "pass", "detail": "Every path this design touches sits inside plugins/artifact/ or shared/."},
    {"rule": "no agents, hooks, or MCP servers in the install surface", "result": "pass", "detail": "The build is a maintainer step. Nothing new installs."},
    {"rule": "ADR-0001: the published viewer is one self-contained file", "result": "pass", "detail": "vite-plugin-singlefile inlines every asset. The output stays one file."}
  ],
  "drift": []
}
