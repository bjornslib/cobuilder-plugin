# Slice plan: Gate state resolution

| # | Epic | Slice | Ends with | Score | State |
|---|---|---|---|---|---|
| | **`gate-state-resolution/E1` — Two renderers read one gate state.** One predicate names the three meanings, and a view shows only the documents a plan holds. | | | | |
| 1 | `gate-state-resolution/E1` | Tracer bullet: the static page resolves the state and the document | `skill-collision-fix` opens on a document it holds, or on no document and a stated reason. A plan that holds `interaction-design.md` draws a rail button labelled "Interaction design". An `n/a` gate carries the closed treatment. | — | pending |
| 2 | `gate-state-resolution/E1` | Real content and edge cases: the viewer rail resolves the state and the count | The viewer rail describes an `n/a` gate as "Not applicable" and paints it apart from a planned gate. The rail header reads the approved count out of the total. An approved gate and the current gate keep their present treatment. | — | pending |

Both slices end in a browser reading on a seeded fixture. Slice 1 reads the
static page. Slice 2 reads the viewer rail. The two slices share no file.
