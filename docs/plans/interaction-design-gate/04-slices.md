# Slice plan: Interaction design gate

| # | Epic | Slice | Ends with | Score | State |
|---|---|---|---|---|---|
| | **`interaction-design-gate/E1` — The vendored skill.** A three-step `design-to-code` lives in the implement plugin and states the four missing concepts. | | | | |
| 1 | `interaction-design-gate/E1` | Tracer bullet: the skill skeleton and its file set | `plugins/implement/skills/design-to-code/SKILL.md` exists, its frontmatter parses, its overview names three steps, and every relative path it cites resolves inside the directory. The three product-brief files are absent. | — | pending |
| 2 | `interaction-design-gate/E1` | Real content and edge cases: the four concepts reach the spec, and the empty paths stay cheap | The state table carries an initial-state column and a timer-arming column, `4.1 Transition Table` carries a trigger, a guard and a reset, `3.3 Visibility Gating` names the condition, and `2.3 Declared Defaults` names the default, with a matching `references/jsonc-schema.md` key for each. A feature with no front end has a stated one-line path, no file cites a deleted template, and no file calls a product brief a PRD or a brief. | — | pending |
| | **`interaction-design-gate/E2` — Gate 2b in the build flow.** The build skill asks for the design, records the answer, and hands the artifact to the rubric author and the slice loop. | | | | |
| 3 | `interaction-design-gate/E2` | Tracer bullet: the gate line and its section | `plugins/implement/skills/build/SKILL.md` carries a `## Gate 2b` section after the Gate 2 section, and the `00-status.md` template carries a `2b` line between the Gate 2 and Gate 3 lines. | — | pending |
| 4 | `interaction-design-gate/E2` | Real content and edge cases: the three paths, the two consumers, and the returned session | The skill states all three paths (no front end, a front end, an earlier approval). `rubric-authoring.md` names both artifacts as derivation sources, and `slice-loop.md` requires RED to read both and VALIDATE's front-end criteria to name a browser check. The resume rule names Gate 2b, so a returned session does not skip it, `commands/start.md` lists the two new artifacts, and `plugin.json` reads 0.2.0. | — | pending |
| | **`interaction-design-gate/E3` — The mechanical consumer and the projection.** `verify_gate.py` enforces the gate, and a reviewer reads the document in the Builds view. | | | | |
| 5 | `interaction-design-gate/E3` | Tracer bullet: the 2b group in `verify_gate.py` | `verify_gate.py --plan docs/plans/interaction-design-gate` prints a `2b` block, reports `n/a` for this plan, and exits 0 for the 2b group. A plan with a pending 2b line exits non-zero. | — | pending |
| 6 | `interaction-design-gate/E3` | Real content: the index and the Builds view rail | `data/index.json` carries an `interaction_design` array, `joins.feature_gates` carries the 2b entry, and the Builds view rail shows a 2b card whose document opens in the existing gate-doc sheet. | — | pending |
| 7 | `interaction-design-gate/E3` | Edge cases: the three integer-only assumptions | `resolve_feature_gates()` writes a string `n`, `viewer/index.html` no longer compares `g.n` to the literal `4`, and `current_doc()` treats an `n/a` gate as done. `cobuilder-family` and `gate-doc-surfacing` still report `Overall: OK`. | — | pending |
